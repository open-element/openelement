// Auto-generated by tools/generate-www-content-data.ts from collection:blog - do not edit
export interface GeneratedBlogPost {
  slug: string;
  frontmatter: {
    title: string;
    date: string;
    draft?: boolean;
    tags?: string[];
    excerpt?: string;
    type?: string;
    lang?: string;
  };
  content: string;
  html: string;
}

export const posts: GeneratedBlogPost[] = [
  {
    "slug": "core-architecture-simplification-report",
    "frontmatter": {
      "title": "@openelement/core 架构简化审查报告",
      "date": "2026-05-11",
      "draft": false,
      "tags": [
        "architecture",
        "decision"
      ],
      "type": "adr",
      "lang": "zh"
    },
    "content": "\n> 审查范围：`packages/core/src/` — 入口代码生成管线（entry-descriptor.ts、entry-renderer.ts、hono-entry.ts）\n> 核心原则：松耦合、高内聚、避免不必要的抽象、消除重复\n\n---\n\n## 发现 1: `CodeBuilder` 类是多余的抽象\n\n**位置**: `entry-renderer.ts:29-42`\n\n```ts\nclass CodeBuilder {\n  private lines: string[] = [];\n  push(line: string): void {\n    this.lines.push(line);\n  }\n  blank(): void {\n    this.lines.push('');\n  }\n  toString(): string {\n    return this.lines.join('\\n');\n  }\n}\n```\n\n**问题**: 这个类只是给 `string[]` 包了三层皮：`push` 是 `Array.push` 的透明代理，`blank` 是 `push('')` 的别名，`toString` 是 `join('\\n')`。没有任何额外逻辑（没有缩进管理、没有换行跟踪、没有格式化能力）。\n\n**影响**: 每一个 `render*` 函数都必须接受 `b: CodeBuilder` 作为参数，增加了参数耦合。因为 `CodeBuilder` 是私有的非导出类，所有渲染函数都无法独立于 `entry-renderer.ts` 测试。\n\n**建议**: 直接使用 `string[]`。`renderEntry` 函数内部声明 `const lines: string[] = []`，所有内部渲染函数改为接受 `lines: string[]`。\n\n```ts\n// 改动量示例 — 删除 class CodeBuilder，然后：\nexport function renderEntry(desc: EntryDescriptor): string {\n  const lines: string[] = [];\n  for (const imp of desc.imports) {\n    lines.push(renderImport(imp));\n  }\n  // ...\n  return lines.join('\\n');\n}\n```\n\n**影响评估**: 纯机械替换，零行为变化，所有测试通过。减少 14 行代码 + 消除一个不必要的抽象。\n\n---\n\n## 发现 2: `hono-entry.ts` 是几乎为零的薄外观层\n\n**位置**: `hono-entry.ts`\n\n```ts\nexport function generateHonoEntryCode(\n  routes: RouteEntry[],\n  options: HonoEntryOptions = {},\n): string {\n  const descriptor = buildEntryDescriptor(routes, options);\n  return renderEntry(descriptor);\n}\n```\n\n**问题**: 整个函数只有 3 行有效逻辑。它唯一的存在理由是 \"keep both pieces independently testable\"（保持两者独立可测）。但实际上 `buildEntryDescriptor` 和 `renderEntry` 已经各自在 `entry-descriptor.ts` 和 `entry-renderer.ts` 中独立存在且独立可测。两个测试文件也都直接从 `hono-entry.ts` 导入。\n\n**建议**: 选项 A — 将 `generateHonoEntryCode` 直接移到 `index.ts` 中（`index.ts` 已经在导入 `generateHonoEntryCode`），然后删除 `hono-entry.ts`。测试文件改为从 `entry-descriptor.ts` 和 `entry-renderer.ts` 直接导入。\n\n或者选项 B — 保留文件但消除 HonoEntryOptions 接口的重复定义（它与 `buildEntryDescriptor` 的 options 参数几乎一样）。\n\n**影响评估**: 减少 1 个文件（约 50 行），降低认知负担。测试导入路径需要更新。\n\n---\n\n## 发现 3: 内联错误转义与 `escapeHtml()` 重复\n\n**位置**: `entry-renderer.ts:238-239`\n\n```ts\n// 生成的虚拟模块代码中的一行：\nconst safeErr = String(err.stack || err).replace(/&/g, '&amp;').replace(/</g, '&lt;')\n  .replace(/>/g, '&gt;').replace(/\"/g, '&quot;').replace(/'/g, '&#39;');\n```\n\n**问题**: 这段 160+ 字符的内联 escaping 与 `html-escape.ts:33-38` 的 `escapeHtml()` 函数完全重复。后者是同一包中已经导出的工具函数。\n\n**建议**: 将生成的代码改为引用 `escapeHtml`：\n\n```ts\n// 生成的代码：\nimport { escapeHtml } from '@openelement/core/html-escape';\n// ...\nconst safeErr = escapeHtml(String(err.stack || err));\n```\n\n需要给生成代码添加 `escapeHtml` 的 import 声明到 `EntryDescriptor.imports` 中。\n\n**影响评估**: 减少生成的代码体积（每页少 ~160 字符），消除逻辑重复。如果将来需要修改转义规则，只需改一处。\n\n---\n\n## 发现 4: 内联 SSR 辅助函数每次构建重复生成\n\n**位置**: `entry-renderer.ts:386-401`\n\n生成的虚拟模块代码中每次都包含完整的 `__ssr` 函数定义：\n\n```ts\nasync function __ssr(tag, props = {}, sourceInfo = {}) {\n  if (!tag || !tag.includes(\"-\")) { ... }\n  const Cls = customElements.get(tag)\n  if (!Cls) { log.warn(...); return ... }\n  return renderDSD(tag, Cls, props, sourceInfo)\n}\n```\n\n此外还有 `__less_get_default_export` 辅助函数（如果有 islands 时生成）：\n\n```ts\nconst __less_get_default_export = (module) => module && module.default;\n```\n\n**问题**:\n\n- 每个页面入口复制粘贴相同逻辑，增加生成的代码体积\n- `__ssr` 依赖于 `renderDSD` 和 `log`（都已从 `@openelement/core/less-runtime` 导入），所以完全可以放在共享模块中\n- `__less_get_default_export` 是一个非常简单的箭头函数，可以内联到使用处\n\n**建议**: 将 `__ssr` 放在 `less-runtime.ts` 中导出，然后生成的代码只需 `import { ..., __ssr }`。`__less_get_default_export` 可以消除，直接写 `module?.default ?? module`。\n\n**影响评估**: 每个入口模块减少 ~200 字节。但是需要注意的是，`__ssr` 作为运行时依赖会增加 `less-runtime.ts` 的导入链——需要评估是否影响 tree-shaking。\n\n---\n\n## 发现 5: `renderPageRoute` 的 wrapInDocument 调用存在重复\n\n**位置**: `entry-renderer.ts:209-228`\n\n```ts\nif (matchingRenderers.length > 0) {\n  b.push(`    // Renderer wrapping (outer → inner)`);\n  b.push(`    let wrapped = html`);\n  for (const renderer of matchingRenderers) {\n    b.push(`    wrapped = ${renderer.varName}.default.wrap(wrapped, c)`);\n  }\n  b.push(`    return c.html(wrapInDocument(wrapped, {`); // ← wrapped\n  b.push(`      title: ${JSON.stringify(docConfig.title)},`);\n  b.push(`      lang: ${JSON.stringify(docConfig.lang)},`);\n  b.push(`      headExtras: ${headExtrasExpr},`);\n  b.push(`      cspNonce: c.get('cspNonce'),`);\n  b.push(`    }))`);\n} else {\n  b.push(`    return c.html(wrapInDocument(html, {`); // ← html\n  b.push(`      title: ${JSON.stringify(docConfig.title)},`);\n  b.push(`      lang: ${JSON.stringify(docConfig.lang)},`);\n  b.push(`      headExtras: ${headExtrasExpr},`);\n  b.push(`      cspNonce: c.get('cspNonce'),`);\n  b.push(`    }))`);\n}\n```\n\n**问题**: `wrapInDocument` 的 5 行参数在两个分支中完全相同，只有第一个参数从 `wrapped` 变成 `html`。这是典型的 \"提取差值\" 模式。\n\n**建议**: 将渲染器包装后的内容赋值给一个变量，然后统一调用 `wrapInDocument`：\n\n```ts\nb.push(`    // Renderer wrapping (outer → inner)`);\nb.push(`    let final = html`);\nfor (const renderer of matchingRenderers) {\n  b.push(`    final = ${renderer.varName}.default.wrap(final, c)`);\n}\nb.push(`    return c.html(wrapInDocument(final, {`);\nb.push(`      title: ${JSON.stringify(docConfig.title)},`);\nb.push(`      lang: ${JSON.stringify(docConfig.lang)},`);\nb.push(`      headExtras: ${headExtrasExpr},`);\nb.push(`      cspNonce: c.get('cspNonce'),`);\nb.push(`    }))`);\n```\n\n注意：即使 `matchingRenderers.length === 0`，`let final = html; ... final = renderer.wrap(...)` 循环也不会执行，`final` 就等于 `html` —— 所以不需要 `if/else`。当然，要确认零渲染器的循环体不会产生额外的空行注释。\n\n**影响评估**: 消除分支重复，减少6行代码，生成的代码行为完全相同。\n\n---\n\n## 发现 6: `renderCorsOrigin` 中 `string` 和 `Array` 分支做同样的事\n\n**位置**: `entry-renderer.ts:53-62`\n\n```ts\nfunction renderCorsOrigin(origin: CorsOriginConfig): string {\n  if (typeof origin === 'string') return JSON.stringify(origin);\n  if (Array.isArray(origin)) return JSON.stringify(origin);\n  return origin.body;\n}\n```\n\n**问题**: `string` 和 `Array` 两个分支都调用 `JSON.stringify(origin)`。`JSON.stringify` 对 string 和 array 都正确处理。只需检查是否是函数类型即可。\n\n**建议**:\n\n```ts\nfunction renderCorsOrigin(origin: CorsOriginConfig): string {\n  if (origin && typeof origin === 'object' && !Array.isArray(origin)) {\n    return origin.body; // Function type\n  }\n  return JSON.stringify(origin);\n}\n```\n\n或者更简洁：\n\n```ts\nfunction renderCorsOrigin(origin: CorsOriginConfig): string {\n  if (typeof origin === 'object' && !Array.isArray(origin)) return origin.body;\n  return JSON.stringify(origin);\n}\n```\n\n**影响评估**: 减少 4 行，逻辑不变，测试不需要修改。\n\n---\n\n## 发现 7: `ssr-handler.ts` 的内联转义也引入了重复\n\n**位置**: `ssr-handler.ts:31`\n\n```ts\nconst message = error.message.replace(/</g, '&lt;').replace(/>/g, '&gt;');\n// ...\nconst stack = error.stack ? error.stack.replace(/</g, '&lt;').replace(/>/g, '&gt;') : '';\n```\n\n**问题**: 虽然没有 `escapeHtml` 那么长的 escape chain，但 `<` 和 `>` 的转义逻辑也与 `html-escape.ts` 重复。`ssr-handler.ts` 已经导入了 `wrapInDocument` 来自 `html-escape.ts`，但没有导入 `escapeHtml`。\n\n**建议**: 导入并使用 `escapeHtml()`：\n\n```ts\nimport { escapeHtml, wrapInDocument } from './html-escape.js';\n\nexport function renderSsrError(...): string {\n  const message = escapeHtml(error.message);\n  const stack = error.stack ? escapeHtml(error.stack) : '';\n}\n```\n\n**影响评估**: 消除另一处逻辑重复，但这是 `ssr-handler.ts` 的重复，不是入口管线的问题——属于边界发现的额外简化点。\n\n---\n\n## 汇总\n\n| # | 发现                        | 位置                       | 简化收益                   | 风险                       | 工作量   |\n| - | --------------------------- | -------------------------- | -------------------------- | -------------------------- | -------- |\n| 1 | `CodeBuilder` 多余抽象      | entry-renderer.ts          | 消除不必要的类抽象         | 低（纯机械替换）           | ~10 分钟 |\n| 2 | `hono-entry.ts` 薄外观      | hono-entry.ts              | 减少 1 个文件 + 50 行      | 中（测试导入路径需要更新） | ~15 分钟 |\n| 3 | 内联转义重复                | entry-renderer.ts L239     | 消除逻辑重复，缩小生成代码 | 低（需加 import）          | ~5 分钟  |\n| 4 | `__ssr` 每次重构生成        | entry-renderer.ts L386-401 | 每入口减 ~200 字节         | 中（改动运行时依赖链）     | ~20 分钟 |\n| 5 | `wrapInDocument` 分支重复   | entry-renderer.ts L209-228 | 消除 6 行重复代码          | 低                         | ~5 分钟  |\n| 6 | `renderCorsOrigin` 分支合并 | entry-renderer.ts L53-62   | 减少 4 行                  | 低                         | ~3 分钟  |\n| 7 | `ssr-handler.ts` 内联转义   | ssr-handler.ts             | 消除额外重复               | 低                         | ~3 分钟  |\n\n**推荐优先级**: 1 → 6 → 3 → 5 → 2 → 7 → 4（从低风险高收益到中风险）\n\n前四项（#1、#6、#3、#5）可以在 30 分钟内完成，零行为变化，测试全部通过。\n",
    "html": "<blockquote>\n<p>审查范围：<code>packages/core/src/</code> — 入口代码生成管线（entry-descriptor.ts、entry-renderer.ts、hono-entry.ts）\n核心原则：松耦合、高内聚、避免不必要的抽象、消除重复</p>\n</blockquote>\n<hr />\n<h2>发现 1: <code>CodeBuilder</code> 类是多余的抽象</h2>\n<p><strong>位置</strong>: <code>entry-renderer.ts:29-42</code></p>\n<pre><code class=\"language-ts\">class CodeBuilder {\n  private lines: string[] = [];\n  push(line: string): void {\n    this.lines.push(line);\n  }\n  blank(): void {\n    this.lines.push(&#39;&#39;);\n  }\n  toString(): string {\n    return this.lines.join(&#39;\\n&#39;);\n  }\n}\n</code></pre>\n<p><strong>问题</strong>: 这个类只是给 <code>string[]</code> 包了三层皮：<code>push</code> 是 <code>Array.push</code> 的透明代理，<code>blank</code> 是 <code>push(&#39;&#39;)</code> 的别名，<code>toString</code> 是 <code>join(&#39;\\n&#39;)</code>。没有任何额外逻辑（没有缩进管理、没有换行跟踪、没有格式化能力）。</p>\n<p><strong>影响</strong>: 每一个 <code>render*</code> 函数都必须接受 <code>b: CodeBuilder</code> 作为参数，增加了参数耦合。因为 <code>CodeBuilder</code> 是私有的非导出类，所有渲染函数都无法独立于 <code>entry-renderer.ts</code> 测试。</p>\n<p><strong>建议</strong>: 直接使用 <code>string[]</code>。<code>renderEntry</code> 函数内部声明 <code>const lines: string[] = []</code>，所有内部渲染函数改为接受 <code>lines: string[]</code>。</p>\n<pre><code class=\"language-ts\">// 改动量示例 — 删除 class CodeBuilder，然后：\nexport function renderEntry(desc: EntryDescriptor): string {\n  const lines: string[] = [];\n  for (const imp of desc.imports) {\n    lines.push(renderImport(imp));\n  }\n  // ...\n  return lines.join(&#39;\\n&#39;);\n}\n</code></pre>\n<p><strong>影响评估</strong>: 纯机械替换，零行为变化，所有测试通过。减少 14 行代码 + 消除一个不必要的抽象。</p>\n<hr />\n<h2>发现 2: <code>hono-entry.ts</code> 是几乎为零的薄外观层</h2>\n<p><strong>位置</strong>: <code>hono-entry.ts</code></p>\n<pre><code class=\"language-ts\">export function generateHonoEntryCode(\n  routes: RouteEntry[],\n  options: HonoEntryOptions = {},\n): string {\n  const descriptor = buildEntryDescriptor(routes, options);\n  return renderEntry(descriptor);\n}\n</code></pre>\n<p><strong>问题</strong>: 整个函数只有 3 行有效逻辑。它唯一的存在理由是 &quot;keep both pieces independently testable&quot;（保持两者独立可测）。但实际上 <code>buildEntryDescriptor</code> 和 <code>renderEntry</code> 已经各自在 <code>entry-descriptor.ts</code> 和 <code>entry-renderer.ts</code> 中独立存在且独立可测。两个测试文件也都直接从 <code>hono-entry.ts</code> 导入。</p>\n<p><strong>建议</strong>: 选项 A — 将 <code>generateHonoEntryCode</code> 直接移到 <code>index.ts</code> 中（<code>index.ts</code> 已经在导入 <code>generateHonoEntryCode</code>），然后删除 <code>hono-entry.ts</code>。测试文件改为从 <code>entry-descriptor.ts</code> 和 <code>entry-renderer.ts</code> 直接导入。</p>\n<p>或者选项 B — 保留文件但消除 HonoEntryOptions 接口的重复定义（它与 <code>buildEntryDescriptor</code> 的 options 参数几乎一样）。</p>\n<p><strong>影响评估</strong>: 减少 1 个文件（约 50 行），降低认知负担。测试导入路径需要更新。</p>\n<hr />\n<h2>发现 3: 内联错误转义与 <code>escapeHtml()</code> 重复</h2>\n<p><strong>位置</strong>: <code>entry-renderer.ts:238-239</code></p>\n<pre><code class=\"language-ts\">// 生成的虚拟模块代码中的一行：\nconst safeErr = String(err.stack || err).replace(/&amp;/g, &#39;&amp;amp;&#39;).replace(/&lt;/g, &#39;&amp;lt;&#39;)\n  .replace(/&gt;/g, &#39;&amp;gt;&#39;).replace(/&quot;/g, &#39;&amp;quot;&#39;).replace(/&#39;/g, &#39;&amp;#39;&#39;);\n</code></pre>\n<p><strong>问题</strong>: 这段 160+ 字符的内联 escaping 与 <code>html-escape.ts:33-38</code> 的 <code>escapeHtml()</code> 函数完全重复。后者是同一包中已经导出的工具函数。</p>\n<p><strong>建议</strong>: 将生成的代码改为引用 <code>escapeHtml</code>：</p>\n<pre><code class=\"language-ts\">// 生成的代码：\nimport { escapeHtml } from &#39;@openelement/core/html-escape&#39;;\n// ...\nconst safeErr = escapeHtml(String(err.stack || err));\n</code></pre>\n<p>需要给生成代码添加 <code>escapeHtml</code> 的 import 声明到 <code>EntryDescriptor.imports</code> 中。</p>\n<p><strong>影响评估</strong>: 减少生成的代码体积（每页少 ~160 字符），消除逻辑重复。如果将来需要修改转义规则，只需改一处。</p>\n<hr />\n<h2>发现 4: 内联 SSR 辅助函数每次构建重复生成</h2>\n<p><strong>位置</strong>: <code>entry-renderer.ts:386-401</code></p>\n<p>生成的虚拟模块代码中每次都包含完整的 <code>__ssr</code> 函数定义：</p>\n<pre><code class=\"language-ts\">async function __ssr(tag, props = {}, sourceInfo = {}) {\n  if (!tag || !tag.includes(&quot;-&quot;)) { ... }\n  const Cls = customElements.get(tag)\n  if (!Cls) { log.warn(...); return ... }\n  return renderDSD(tag, Cls, props, sourceInfo)\n}\n</code></pre>\n<p>此外还有 <code>__less_get_default_export</code> 辅助函数（如果有 islands 时生成）：</p>\n<pre><code class=\"language-ts\">const __less_get_default_export = (module) =&gt; module &amp;&amp; module.default;\n</code></pre>\n<p><strong>问题</strong>:</p>\n<ul>\n<li>每个页面入口复制粘贴相同逻辑，增加生成的代码体积</li>\n<li><code>__ssr</code> 依赖于 <code>renderDSD</code> 和 <code>log</code>（都已从 <code>@openelement/core/less-runtime</code> 导入），所以完全可以放在共享模块中</li>\n<li><code>__less_get_default_export</code> 是一个非常简单的箭头函数，可以内联到使用处</li>\n</ul>\n<p><strong>建议</strong>: 将 <code>__ssr</code> 放在 <code>less-runtime.ts</code> 中导出，然后生成的代码只需 <code>import { ..., __ssr }</code>。<code>__less_get_default_export</code> 可以消除，直接写 <code>module?.default ?? module</code>。</p>\n<p><strong>影响评估</strong>: 每个入口模块减少 ~200 字节。但是需要注意的是，<code>__ssr</code> 作为运行时依赖会增加 <code>less-runtime.ts</code> 的导入链——需要评估是否影响 tree-shaking。</p>\n<hr />\n<h2>发现 5: <code>renderPageRoute</code> 的 wrapInDocument 调用存在重复</h2>\n<p><strong>位置</strong>: <code>entry-renderer.ts:209-228</code></p>\n<pre><code class=\"language-ts\">if (matchingRenderers.length &gt; 0) {\n  b.push(`    // Renderer wrapping (outer → inner)`);\n  b.push(`    let wrapped = html`);\n  for (const renderer of matchingRenderers) {\n    b.push(`    wrapped = ${renderer.varName}.default.wrap(wrapped, c)`);\n  }\n  b.push(`    return c.html(wrapInDocument(wrapped, {`); // ← wrapped\n  b.push(`      title: ${JSON.stringify(docConfig.title)},`);\n  b.push(`      lang: ${JSON.stringify(docConfig.lang)},`);\n  b.push(`      headExtras: ${headExtrasExpr},`);\n  b.push(`      cspNonce: c.get(&#39;cspNonce&#39;),`);\n  b.push(`    }))`);\n} else {\n  b.push(`    return c.html(wrapInDocument(html, {`); // ← html\n  b.push(`      title: ${JSON.stringify(docConfig.title)},`);\n  b.push(`      lang: ${JSON.stringify(docConfig.lang)},`);\n  b.push(`      headExtras: ${headExtrasExpr},`);\n  b.push(`      cspNonce: c.get(&#39;cspNonce&#39;),`);\n  b.push(`    }))`);\n}\n</code></pre>\n<p><strong>问题</strong>: <code>wrapInDocument</code> 的 5 行参数在两个分支中完全相同，只有第一个参数从 <code>wrapped</code> 变成 <code>html</code>。这是典型的 &quot;提取差值&quot; 模式。</p>\n<p><strong>建议</strong>: 将渲染器包装后的内容赋值给一个变量，然后统一调用 <code>wrapInDocument</code>：</p>\n<pre><code class=\"language-ts\">b.push(`    // Renderer wrapping (outer → inner)`);\nb.push(`    let final = html`);\nfor (const renderer of matchingRenderers) {\n  b.push(`    final = ${renderer.varName}.default.wrap(final, c)`);\n}\nb.push(`    return c.html(wrapInDocument(final, {`);\nb.push(`      title: ${JSON.stringify(docConfig.title)},`);\nb.push(`      lang: ${JSON.stringify(docConfig.lang)},`);\nb.push(`      headExtras: ${headExtrasExpr},`);\nb.push(`      cspNonce: c.get(&#39;cspNonce&#39;),`);\nb.push(`    }))`);\n</code></pre>\n<p>注意：即使 <code>matchingRenderers.length === 0</code>，<code>let final = html; ... final = renderer.wrap(...)</code> 循环也不会执行，<code>final</code> 就等于 <code>html</code> —— 所以不需要 <code>if/else</code>。当然，要确认零渲染器的循环体不会产生额外的空行注释。</p>\n<p><strong>影响评估</strong>: 消除分支重复，减少6行代码，生成的代码行为完全相同。</p>\n<hr />\n<h2>发现 6: <code>renderCorsOrigin</code> 中 <code>string</code> 和 <code>Array</code> 分支做同样的事</h2>\n<p><strong>位置</strong>: <code>entry-renderer.ts:53-62</code></p>\n<pre><code class=\"language-ts\">function renderCorsOrigin(origin: CorsOriginConfig): string {\n  if (typeof origin === &#39;string&#39;) return JSON.stringify(origin);\n  if (Array.isArray(origin)) return JSON.stringify(origin);\n  return origin.body;\n}\n</code></pre>\n<p><strong>问题</strong>: <code>string</code> 和 <code>Array</code> 两个分支都调用 <code>JSON.stringify(origin)</code>。<code>JSON.stringify</code> 对 string 和 array 都正确处理。只需检查是否是函数类型即可。</p>\n<p><strong>建议</strong>:</p>\n<pre><code class=\"language-ts\">function renderCorsOrigin(origin: CorsOriginConfig): string {\n  if (origin &amp;&amp; typeof origin === &#39;object&#39; &amp;&amp; !Array.isArray(origin)) {\n    return origin.body; // Function type\n  }\n  return JSON.stringify(origin);\n}\n</code></pre>\n<p>或者更简洁：</p>\n<pre><code class=\"language-ts\">function renderCorsOrigin(origin: CorsOriginConfig): string {\n  if (typeof origin === &#39;object&#39; &amp;&amp; !Array.isArray(origin)) return origin.body;\n  return JSON.stringify(origin);\n}\n</code></pre>\n<p><strong>影响评估</strong>: 减少 4 行，逻辑不变，测试不需要修改。</p>\n<hr />\n<h2>发现 7: <code>ssr-handler.ts</code> 的内联转义也引入了重复</h2>\n<p><strong>位置</strong>: <code>ssr-handler.ts:31</code></p>\n<pre><code class=\"language-ts\">const message = error.message.replace(/&lt;/g, &#39;&amp;lt;&#39;).replace(/&gt;/g, &#39;&amp;gt;&#39;);\n// ...\nconst stack = error.stack ? error.stack.replace(/&lt;/g, &#39;&amp;lt;&#39;).replace(/&gt;/g, &#39;&amp;gt;&#39;) : &#39;&#39;;\n</code></pre>\n<p><strong>问题</strong>: 虽然没有 <code>escapeHtml</code> 那么长的 escape chain，但 <code>&lt;</code> 和 <code>&gt;</code> 的转义逻辑也与 <code>html-escape.ts</code> 重复。<code>ssr-handler.ts</code> 已经导入了 <code>wrapInDocument</code> 来自 <code>html-escape.ts</code>，但没有导入 <code>escapeHtml</code>。</p>\n<p><strong>建议</strong>: 导入并使用 <code>escapeHtml()</code>：</p>\n<pre><code class=\"language-ts\">import { escapeHtml, wrapInDocument } from &#39;./html-escape.js&#39;;\n\nexport function renderSsrError(...): string {\n  const message = escapeHtml(error.message);\n  const stack = error.stack ? escapeHtml(error.stack) : &#39;&#39;;\n}\n</code></pre>\n<p><strong>影响评估</strong>: 消除另一处逻辑重复，但这是 <code>ssr-handler.ts</code> 的重复，不是入口管线的问题——属于边界发现的额外简化点。</p>\n<hr />\n<h2>汇总</h2>\n<table>\n<thead>\n<tr>\n<th>#</th>\n<th>发现</th>\n<th>位置</th>\n<th>简化收益</th>\n<th>风险</th>\n<th>工作量</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>1</td>\n<td><code>CodeBuilder</code> 多余抽象</td>\n<td>entry-renderer.ts</td>\n<td>消除不必要的类抽象</td>\n<td>低（纯机械替换）</td>\n<td>~10 分钟</td>\n</tr>\n<tr>\n<td>2</td>\n<td><code>hono-entry.ts</code> 薄外观</td>\n<td>hono-entry.ts</td>\n<td>减少 1 个文件 + 50 行</td>\n<td>中（测试导入路径需要更新）</td>\n<td>~15 分钟</td>\n</tr>\n<tr>\n<td>3</td>\n<td>内联转义重复</td>\n<td>entry-renderer.ts L239</td>\n<td>消除逻辑重复，缩小生成代码</td>\n<td>低（需加 import）</td>\n<td>~5 分钟</td>\n</tr>\n<tr>\n<td>4</td>\n<td><code>__ssr</code> 每次重构生成</td>\n<td>entry-renderer.ts L386-401</td>\n<td>每入口减 ~200 字节</td>\n<td>中（改动运行时依赖链）</td>\n<td>~20 分钟</td>\n</tr>\n<tr>\n<td>5</td>\n<td><code>wrapInDocument</code> 分支重复</td>\n<td>entry-renderer.ts L209-228</td>\n<td>消除 6 行重复代码</td>\n<td>低</td>\n<td>~5 分钟</td>\n</tr>\n<tr>\n<td>6</td>\n<td><code>renderCorsOrigin</code> 分支合并</td>\n<td>entry-renderer.ts L53-62</td>\n<td>减少 4 行</td>\n<td>低</td>\n<td>~3 分钟</td>\n</tr>\n<tr>\n<td>7</td>\n<td><code>ssr-handler.ts</code> 内联转义</td>\n<td>ssr-handler.ts</td>\n<td>消除额外重复</td>\n<td>低</td>\n<td>~3 分钟</td>\n</tr>\n</tbody></table>\n<p><strong>推荐优先级</strong>: 1 → 6 → 3 → 5 → 2 → 7 → 4（从低风险高收益到中风险）</p>\n<p>前四项（#1、#6、#3、#5）可以在 30 分钟内完成，零行为变化，测试全部通过。</p>\n"
  },
  {
    "slug": "adr-0009-full-repo-simplification",
    "frontmatter": {
      "title": "LessJS 全仓库简化方案 — ADR 0009",
      "date": "2026-05-10",
      "draft": false,
      "tags": [
        "architecture",
        "decision"
      ],
      "type": "adr",
      "lang": "zh"
    },
    "content": "\n> **基于**: ADR 0008 (消除 createServer + globalThis + .less/ IPC)\n> **扩展**: 在 ADR 0008 基础上，对整个仓库进行功能不变前提下的解耦、复用、轻量、简化与合并\n> **当前代码量**: core 5846 行 / ui 3063 行 / adapter-lit 739 行 / content 892 行 / rpc 270 行 / i18n 162 行\n\n---\n\n## 一、ADR 0008 执行层（已完成设计，直接落地）\n\nADR 0008 的 Phase C+B → A → D 是管线级重构，不在此重复。本方案将其作为 Layer 0 基础，在其之上叠加 Layer 1–3 的简化。\n\n| Layer | 范围 | 性质 |\n|-------|------|------|\n| **Layer 0** | ADR 0008: C+B → A → D | 管线级：消除 createServer、globalThis、.less/ IPC、runtime-shim |\n| **Layer 1** | @openelement/core 内部简化 | 模块级：删除多余抽象、消除重复、合并薄层 |\n| **Layer 2** | 跨包解耦与复用 | 包级：统一 escape 模式、统一 logger、消除重复数据流 |\n| **Layer 3** | 入口与导出精简 | API 级：合并 re-export 链、清理死代码导出 |\n\n---\n\n## 二、Layer 1 — @openelement/core 内部简化\n\n### 2.1 删除 `CodeBuilder` 类 → 用 `string[]` 替代\n\n**文件**: `entry-renderer.ts:29-42`\n\n```ts\n// 当前：14 行类定义 + 所有函数接受 b: CodeBuilder\nclass CodeBuilder {\n  private lines: string[] = [];\n  push(line: string): void { this.lines.push(line); }\n  blank(): void { this.lines.push(''); }\n  toString(): string { return this.lines.join('\\n'); }\n}\n```\n\n**改为**: `renderEntry()` 内部声明 `const lines: string[] = []`，所有 `render*` 函数改为接受 `lines: string[]`，`b.blank()` 改为 `lines.push('')`。\n\n**收益**: 消除一个无值抽象，减少 14 行，降低函数签名耦合。\n\n---\n\n### 2.2 合并 `hono-entry.ts` 到 `entry-renderer.ts`\n\n**文件**: `hono-entry.ts` (48 行)\n\n`generateHonoEntryCode()` 只有 3 行有效逻辑：\n\n```ts\nconst descriptor = buildEntryDescriptor(routes, options);\nreturn renderEntry(descriptor);\n```\n\n同时该文件 re-export 了 `buildEntryDescriptor`、`renderEntry`、`EntryDescriptor`——三者已各自在原文件中独立导出。\n\n**方案**:\n- 将 `generateHonoEntryCode` + `HonoEntryOptions` 移到 `entry-renderer.ts` 底部\n- 删除 `hono-entry.ts`\n- 所有导入点改为从 `entry-renderer.ts` 导入\n- `index.ts` 改为 `import { generateHonoEntryCode } from './entry-renderer.js'`\n\n**收益**: 减少 1 个文件 + 48 行，消除 re-export 链。\n\n**测试影响**: 两个测试文件需改导入路径：`entry-renderer.test.ts` 和 `entry-descriptor.test.ts`。\n\n---\n\n### 2.3 消除内联 HTML 转义 → 使用 `escapeHtml()`\n\n**文件**: `entry-renderer.ts:238-239` (生成的代码中)\n\n```ts\n// 当前：160 字符的 replace 链\nconst safeErr = String(err.stack || err).replace(/&/g,'&amp;').replace(/</g,'&lt;')...\n```\n\n**改为**: 在 `buildEntryDescriptor()` 中给 `imports` 增加 `escapeHtml` 的导入声明，生成的代码改为：\n\n```ts\nimport { escapeHtml } from '@openelement/core/html-escape';\n// ...\nconst safeErr = escapeHtml(String(err.stack || err));\n```\n\n**同理**: `ssr-handler.ts:31` 的内联 `.replace(/</g, '&lt;').replace(/>/g, '&gt;')` 也改为使用 `escapeHtml()`（该文件已从 `html-escape.ts` 导入 `wrapInDocument`）。\n\n**收益**: 消除两处逻辑重复，减少生成代码体积，单一真相源。\n\n---\n\n### 2.4 合并 `renderPageRoute` 的 wrapInDocument 分支\n\n**文件**: `entry-renderer.ts:209-228`\n\n当前有两个分支，`wrapInDocument` 的 5 行参数完全相同，仅第一个参数不同（`wrapped` vs `html`）。\n\n**改为**: 统一变量名，消除分支：\n\n```ts\nlines.push(`    let content = html`);\nfor (const renderer of matchingRenderers) {\n  lines.push(`    content = ${renderer.varName}.default.wrap(content, c)`);\n}\nlines.push(`    return c.html(wrapInDocument(content, {`);\nlines.push(`      title: ..., lang: ..., headExtras: ..., cspNonce: ...`);\nlines.push(`    }))`);\n```\n\n当 `matchingRenderers.length === 0` 时，循环不执行，`content === html`，行为不变。\n\n**收益**: 消除 6 行重复，逻辑更清晰。\n\n---\n\n### 2.5 合并 `renderCorsOrigin` 的重复分支\n\n**文件**: `entry-renderer.ts:53-62`\n\n```ts\n// 当前：string 和 Array 两个分支做同样的事\nif (typeof origin === 'string') { return JSON.stringify(origin); }\nif (Array.isArray(origin))   { return JSON.stringify(origin); }\n```\n\n**改为**:\n\n```ts\nfunction renderCorsOrigin(origin: CorsOriginConfig): string {\n  if (typeof origin === 'object' && !Array.isArray(origin)) return origin.body;\n  return JSON.stringify(origin);\n}\n```\n\n**收益**: 减少 4 行。\n\n---\n\n### 2.6 合并 `ssr-handler.ts` 到 `html-escape.ts`\n\n**文件**: `ssr-handler.ts` (63 行)\n\n当前 `ssr-handler.ts` 只做两件事：\n1. Re-export `wrapInDocument`（从 `html-escape.ts`）\n2. 定义 `renderSsrError()` 函数\n\n`html-escape.ts` 已经有 `wrapInDocument`、`escapeHtml`、`escapeAttr` 等——`renderSsrError()` 是 HTML 错误页面渲染，与 `html-escape.ts` 的\"HTML 输出工具\"定位完全一致。\n\n**方案**:\n- 将 `renderSsrError()` 移入 `html-escape.ts`\n- `ssr-handler.ts` 改为纯 re-export 文件（兼容性），或直接删除\n- `index.ts` 的 `export { renderSsrError, wrapInDocument } from './ssr-handler.js'` 改为 `from './html-escape.js'`\n\n**收益**: 减少 1 个文件的概念负担，HTML 渲染工具统一。\n\n---\n\n### 2.7 将 `__ssr` 辅助函数从生成代码移到 `less-runtime.ts`\n\n**文件**: `entry-renderer.ts:386-401` (每次构建都生成的 ~16 行代码)\n\n`__ssr()` 是一个完全确定性的辅助函数，逻辑只依赖 `renderDSD` 和 `log`——两者都在 `less-runtime.ts` 中已导出。\n\n**方案**: 将 `__ssr` 移到 `less-runtime.ts` 导出，生成的 entry 代码只需 `import { ..., __ssr } from '@openelement/core/less-runtime'`。\n\n**注意**: 这会增加 `less-runtime.ts` 的导出，但 `__ssr` 在 SSG bundle 中是 tree-shakeable 的（如果不被使用就不会包含）。\n\n**替代方案（更安全）**: 如果担心 `less-runtime.ts` 的职责膨胀，可以新建 `less-ssr-helper.ts` 并在 `entry-renderer.ts` 生成的代码中 `import { __ssr } from '@openelement/core/less-ssr-helper'`。\n\n**收益**: 每个入口模块减少 ~200 字节生成代码。\n\n---\n\n### 2.8 消除 `__less_get_default_export` 辅助函数\n\n**文件**: `entry-renderer.ts:348`\n\n```ts\nconst __less_get_default_export = (module) => module && module.default\n```\n\n这个函数只是 `module?.default` 的别名。可以直接内联：\n\n```ts\nconst __island_component_x = __island_x?.default\n```\n\n**收益**: 消除一个生成代码中的辅助函数，更符合 JS 惯例。\n\n---\n\n### 2.9 删除 `LessBuildContext.userResolveAlias`\n\n**文件**: `build-context.ts:42`\n\n`userResolveAlias` 的存在是为了 `createServer()` 路径下传递 resolve alias——ADR 0008 Phase C 消除 `createServer()` 后，此字段不再需要。\n\n**时机**: 与 ADR 0008 Phase C 同步。\n\n---\n\n## 三、Layer 2 — 跨包解耦与复用\n\n### 3.1 统一 escape 模式：`render-dsd.ts` 的 re-export 链\n\n**文件**: `render-dsd.ts:30-36`\n\n```ts\nexport {\n  escapeAttr, escapeAttrValue, escapeHtml,\n  type SafeHtml, type UnsafeHtml,\n} from './html-escape.js';\n```\n\n`render-dsd.ts` re-export `html-escape.ts` 的所有内容——这意味着消费者可以从 `@openelement/core/render-dsd` 或 `@openelement/core/html-escape` 两处导入同一函数。\n\n**问题**: 这是历史遗留。`render-dsd.ts` 的核心职责是 DSD 渲染，不应承担 escape 工具的导出。\n\n**方案**:\n- 保留 `deno.json` 中 `./html-escape` 导出路径\n- 在 `render-dsd.ts` 的 re-export 块上方添加 `@deprecated` 注释\n- 下一个大版本移除 re-export\n\n---\n\n### 3.2 `runtime-shim.ts` 的巨型字符串化代码是最大的技术债\n\n**文件**: `runtime-shim.ts` (3 行，但字符串内容 ~2000+ 字符)\n\n这是 `generate-runtime-shim.ts` 脚本从源码 AST 提取 + 编译 + 字符串化产物的硬编码副本。每次修改 `render-dsd.ts`、`html-escape.ts`、`render-nested.ts` 都必须重新运行脚本。\n\n**方案**: ADR 0008 Phase D 完成后，整个机制删除。这是 ADR 0008 的核心收益之一，不重复。\n\n**优先级**: 与 ADR 0008 Phase D 绑定。\n\n---\n\n### 3.3 `@openelement/content` 的 `blog-data.ts` → 与 `i18n-data.ts` 对齐\n\n**文件**: `packages/content/src/blog/blog-data.ts`\n\n当前使用 `globalThis[Symbol.for()]` 桥接，而 `@openelement/i18n` 的 `i18n-data.ts` 使用干净的模式（纯模块变量）。\n\n**方案**: ADR 0008 Phase B 已覆盖。执行后两者模式完全一致。\n\n---\n\n### 3.4 `entry-generators.ts` 的客户端入口代码可以简化\n\n**文件**: `entry-generators.ts` (115 行)\n\n生成的客户端入口代码内联了一个 `log` stub：\n\n```js\nvar log = {\n  warn: function() { var a = ['[LessJS]']; a.push.apply(a, arguments); ... },\n  error: function() { ... },\n};\n```\n\n这个 stub 与 `logger.ts` 的 `LessLogger` 逻辑重复，但因为它要运行在浏览器端且不能依赖模块系统，所以内联是合理的。\n\n**评估**: 此项不做简化——浏览器端 shim 的内联是刻意为之，避免引入构建时依赖。\n\n---\n\n### 3.5 `ssg-postprocess.ts` 的 HTML 遍历模式重复\n\n**文件**: `ssg-postprocess.ts`\n\n`injectClientScript`、`injectCspMeta`、`injectDsdPolyfill`、`injectViewTransitionMeta`、`injectSpeculationRules` 五个函数都有相同的外层循环：\n\n```ts\nconst entries = readdirSync(dir, { withFileTypes: true });\nfor (const entry of entries) {\n  const fullPath = join(dir, entry.name);\n  if (entry.isDirectory()) { /* 递归 */ }\n  else if (entry.name.endsWith('.html')) { /* 读取 → 修改 → 写入 */ }\n}\n```\n\n**方案**: 提取 `walkHtmlFiles(dir, visitor: (fullPath, content) => string | null)` 高阶函数：\n\n```ts\nfunction walkHtmlFiles(dir: string, visitor: (content: string, fullPath: string) => string | null): void {\n  const entries = readdirSync(dir, { withFileTypes: true });\n  for (const entry of entries) {\n    const fullPath = join(dir, entry.name);\n    if (entry.isDirectory()) { walkHtmlFiles(fullPath, visitor); return; }\n    if (entry.name.endsWith('.html')) {\n      const content = readFileSync(fullPath, 'utf-8');\n      const result = visitor(content, fullPath);\n      if (result !== null) writeFileSync(fullPath, result, 'utf-8');\n    }\n  }\n}\n```\n\n**收益**: 消除 5 处相同的目录遍历代码，减少 ~60 行重复。\n\n---\n\n### 3.6 `build-manifest.ts` 的 `collectFiles` 与 `ssg-postprocess.ts` 的遍历重复\n\n**文件**: `build-manifest.ts:59-88`\n\n`collectFiles()` 也是递归遍历文件系统，但与 `walkHtmlFiles` 不同——它收集信息不修改文件。两者可以共享 `walkDir` 基础函数。\n\n**方案**: 如果 3.5 提取了 `walkHtmlFiles`，可以进一步抽象为 `walkDir(dir, { onFile, onDir, filter })` 通配遍历器。但优先级低——`build-manifest.ts` 是纯可观察性工具，不影响功能。\n\n**评估**: 标记为 P2，不做在本轮。\n\n---\n\n## 四、Layer 3 — 入口与导出精简\n\n### 4.1 `render-dsd.ts` 的 re-export 链清理\n\n如 3.1 所述，`render-dsd.ts` re-export 了 `html-escape.ts` 和 `types.ts` 的内容。在 ADR 0008 完成后，`render-dsd.ts` 应只导出与 DSD 渲染直接相关的内容：\n\n**保留导出**:\n- `renderDSD`, `renderDSDByName` (核心功能)\n- `camelToKebab` (被 SSR 代码使用)\n- `renderNestedCustomElements` (从 render-nested.ts)\n\n**标记 @deprecated 的 re-export**:\n- `escapeAttr`, `escapeAttrValue`, `escapeHtml`, `SafeHtml`, `UnsafeHtml`\n- `ComponentLayer`, `DsdComponent`, `DsdOptions`, `HydrateEventDescriptor`, `RenderAdapter`, `registerAdapter`\n\n这些应引导消费者使用 `@openelement/core/html-escape` 和 `@openelement/core` 主入口。\n\n---\n\n### 4.2 `less-runtime.ts` 的导出精简\n\n**文件**: `less-runtime.ts` (15 行)\n\n```ts\nexport { log };\nexport { registerAdapter, renderDSD, renderDSDByName } from './render-dsd.js';\nexport { Hono } from 'hono';\nexport { wrapInDocument } from './ssr-handler.js';\n```\n\n问题：`export { Hono } from 'hono'` 让 `less-runtime` 成了 Hono 的 re-export 点。SSR bundle 已经 `import { Hono } from 'hono'`，不需要通过 `less-runtime` 间接获取。\n\n**方案**: 移除 `export { Hono }`，生成的 entry 代码直接 `import { Hono } from 'hono'`。\n\n**收益**: `less-runtime.ts` 职责更清晰——只导出 LessJS 自己的运行时 API。\n\n---\n\n### 4.3 `index.ts` 导出审计\n\n**文件**: `index.ts` (305 行)\n\n当前 `index.ts` 导出了大量内部细节，如：\n- `extractCustomElementTags` (island-manifest 内部)\n- `buildSpeculationRulesJson` (ssg-postprocess 内部)\n- `buildIslandChunkMap` (ssg-postprocess 内部)\n- `printBuildManifest`, `scanClientBuild`, `scanSSGOutput` (build-manifest 内部)\n\n这些都是 CLI 工具的内部函数，不应通过公共 API 暴露。\n\n**方案**:\n- 将 CLI 专用函数标记为 `@internal`（在 deno.json 的 exports 中不导出）\n- 或将其移到 `@openelement/core/cli/*` 导出路径下\n\n**收益**: 公共 API 表面更小，减少破坏性变更的风险。\n\n---\n\n### 4.4 `types.ts` 的职责拆分\n\n**文件**: `types.ts` (375 行)\n\n`types.ts` 承担了三个职责：\n1. **公共类型定义** (`FrameworkOptions`, `RouteEntry`, `DsdComponent` 等)\n2. **运行时状态** (`_adapter`, `registerAdapter`, `getAdapter`)\n3. **DSD 类型** (`DsdOptions`, `ComponentLayer`, `HydrateEventDescriptor`)\n\n其中第 2 项（模块级 `_adapter` + accessor）违反了\"类型文件不应有运行时状态\"的原则。\n\n**方案**: 将 `registerAdapter` / `getAdapter` + `_adapter` 移到 `adapter-registry.ts`（或 `less-runtime.ts`），`types.ts` 只保留类型定义。\n\n**收益**: 类型文件回到纯类型职责，运行时状态有明确的家。\n\n---\n\n## 五、简化后的文件结构预期\n\n### 变更汇总\n\n| 操作 | 文件 | 说明 |\n|------|------|------|\n| **删除** | `hono-entry.ts` | 合并到 entry-renderer.ts |\n| **删除** | `runtime-shim.ts` | ADR 0008 Phase D |\n| **删除** | `scripts/generate-runtime-shim.ts` | ADR 0008 Phase D |\n| **删除** | `ssr-handler.ts` | 合并到 html-escape.ts（留 re-export 兼容） |\n| **合并** | `CodeBuilder` → `string[]` | entry-renderer.ts 内部 |\n| **提取** | `walkHtmlFiles()` | ssg-postprocess.ts 内部，消除 5 处重复 |\n| **提取** | `__ssr` → less-runtime.ts | 或新建 less-ssr-helper.ts |\n| **移动** | `registerAdapter`/`getAdapter` | types.ts → adapter-registry.ts |\n| **移除** | `export { Hono }` | less-runtime.ts |\n| **标记** | `@deprecated` re-exports | render-dsd.ts 对 html-escape 的 re-export |\n| **清理** | `userResolveAlias` | build-context.ts（ADR 0008 Phase C 后） |\n\n### 预期行数变化\n\n| 文件 | 当前 | 预期 | 变化 |\n|------|------|------|------|\n| entry-renderer.ts | 472 | ~440 | -32 (删除 CodeBuilder + 合并分支 + 合并 CORS 分支) |\n| hono-entry.ts | 48 | 0 | -48 (删除) |\n| html-escape.ts | 137 | ~170 | +33 (接收 renderSsrError) |\n| ssr-handler.ts | 63 | ~5 | -58 (改 re-export) |\n| ssg-postprocess.ts | 409 | ~360 | -49 (提取 walkHtmlFiles) |\n| less-runtime.ts | 15 | ~30 | +15 (接收 __ssr) |\n| types.ts | 375 | ~345 | -30 (移出 adapter 状态) |\n| build-context.ts | 61 | ~55 | -6 (移出 userResolveAlias) |\n| **核心总计** | **5846** | **~5500** | **~350 行减少** |\n\n---\n\n## 六、执行顺序与依赖\n\n```\nLayer 0: ADR 0008 (Phase C+B → A → D)\n  │\n  │ ← 必须先完成，Layer 1 的一些简化依赖其结果\n  │\n  ├─→ Layer 1.1: 删除 CodeBuilder            (独立，随时可做)\n  ├─→ Layer 1.2: 合并 hono-entry.ts           (独立，随时可做)\n  ├─→ Layer 1.3: 消除内联 escape               (独立，随时可做)\n  ├─→ Layer 1.4: 合并 renderPageRoute 分支     (独立，随时可做)\n  ├─→ Layer 1.5: 合并 renderCorsOrigin 分支    (独立，随时可做)\n  ├─→ Layer 1.6: 合并 ssr-handler.ts          (独立，随时可做)\n  ├─→ Layer 1.7: __ssr 移到 less-runtime       (需 ADR 0008 Phase C 后)\n  ├─→ Layer 1.8: 消除 __less_get_default_export (独立)\n  ├─→ Layer 1.9: 移除 userResolveAlias        (需 ADR 0008 Phase C 后)\n  │\n  ├─→ Layer 2.1: render-dsd.ts re-export 标记   (独立)\n  ├─→ Layer 2.2: runtime-shim 删除              (ADR 0008 Phase D)\n  ├─→ Layer 2.3: blog-data.ts 对齐             (ADR 0008 Phase B)\n  ├─→ Layer 2.5: 提取 walkHtmlFiles            (独立)\n  │\n  └─→ Layer 3.1-3.4: 导出精简与拆分            (Layer 1 完成后)\n```\n\n**关键约束**:\n- Layer 1.1–1.6 和 1.8 **全部互相独立**，可以并行执行\n- Layer 1.7 和 1.9 **依赖 ADR 0008 Phase C**\n- Layer 3 的导出变更 **依赖 Layer 1 完成后**（避免中途改导出路径两次）\n\n---\n\n## 七、风险矩阵\n\n| 变更 | 破坏性 | 风险 | 缓解 |\n|------|--------|------|------|\n| 删除 CodeBuilder | 内部 | 零 | 纯机械替换 |\n| 合并 hono-entry.ts | 测试导入路径 | 低 | 2 个测试文件改路径 |\n| 消除内联 escape | 生成代码变化 | 低 | 快照测试覆盖 |\n| 合并 renderPageRoute 分支 | 生成代码变化 | 低 | 行为等价验证 |\n| 合并 ssr-handler.ts | 公共导出路径 | 中 | 保留 re-export 兼容层 |\n| __ssr 移到 less-runtime | 运行时依赖链 | 中 | 需验证 tree-shaking |\n| 移除 `export { Hono }` | 生成代码导入 | 低 | 改为直接 import |\n| 提取 walkHtmlFiles | 内部 | 低 | 纯重构 |\n| types.ts 拆分 | 导入路径 | 中 | 保留 re-export 兼容层 |\n\n---\n\n## 八、不在本方案范围内的事项\n\n| 事项 | 原因 |\n|------|------|\n| `entry-generators.ts` 的客户端 log stub | 浏览器端不能依赖模块系统，内联合理 |\n| `build-manifest.ts` 的文件遍历 | 纯可观察性工具，P2 |\n| `navigation.ts` 的 Navigation API 类型声明 | 标准 API 模拟，改动风险高收益低 |\n| `island.ts` 的 connectedCallback 包装 | 核心功能，不能简化 |\n| `render-nested.ts` 的 parse5 AST 遍历 | 性能关键路径，不能简化 |\n\n---\n\n_方案日期: 2026-05-10 | 基于 ADR 0008 + 全仓库审查_\n",
    "html": "<blockquote>\n<p><strong>基于</strong>: ADR 0008 (消除 createServer + globalThis + .less/ IPC)\n<strong>扩展</strong>: 在 ADR 0008 基础上，对整个仓库进行功能不变前提下的解耦、复用、轻量、简化与合并\n<strong>当前代码量</strong>: core 5846 行 / ui 3063 行 / adapter-lit 739 行 / content 892 行 / rpc 270 行 / i18n 162 行</p>\n</blockquote>\n<hr />\n<h2>一、ADR 0008 执行层（已完成设计，直接落地）</h2>\n<p>ADR 0008 的 Phase C+B → A → D 是管线级重构，不在此重复。本方案将其作为 Layer 0 基础，在其之上叠加 Layer 1–3 的简化。</p>\n<table>\n<thead>\n<tr>\n<th>Layer</th>\n<th>范围</th>\n<th>性质</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Layer 0</strong></td>\n<td>ADR 0008: C+B → A → D</td>\n<td>管线级：消除 createServer、globalThis、.less/ IPC、runtime-shim</td>\n</tr>\n<tr>\n<td><strong>Layer 1</strong></td>\n<td>@openelement/core 内部简化</td>\n<td>模块级：删除多余抽象、消除重复、合并薄层</td>\n</tr>\n<tr>\n<td><strong>Layer 2</strong></td>\n<td>跨包解耦与复用</td>\n<td>包级：统一 escape 模式、统一 logger、消除重复数据流</td>\n</tr>\n<tr>\n<td><strong>Layer 3</strong></td>\n<td>入口与导出精简</td>\n<td>API 级：合并 re-export 链、清理死代码导出</td>\n</tr>\n</tbody></table>\n<hr />\n<h2>二、Layer 1 — @openelement/core 内部简化</h2>\n<h3>2.1 删除 <code>CodeBuilder</code> 类 → 用 <code>string[]</code> 替代</h3>\n<p><strong>文件</strong>: <code>entry-renderer.ts:29-42</code></p>\n<pre><code class=\"language-ts\">// 当前：14 行类定义 + 所有函数接受 b: CodeBuilder\nclass CodeBuilder {\n  private lines: string[] = [];\n  push(line: string): void { this.lines.push(line); }\n  blank(): void { this.lines.push(&#39;&#39;); }\n  toString(): string { return this.lines.join(&#39;\\n&#39;); }\n}\n</code></pre>\n<p><strong>改为</strong>: <code>renderEntry()</code> 内部声明 <code>const lines: string[] = []</code>，所有 <code>render*</code> 函数改为接受 <code>lines: string[]</code>，<code>b.blank()</code> 改为 <code>lines.push(&#39;&#39;)</code>。</p>\n<p><strong>收益</strong>: 消除一个无值抽象，减少 14 行，降低函数签名耦合。</p>\n<hr />\n<h3>2.2 合并 <code>hono-entry.ts</code> 到 <code>entry-renderer.ts</code></h3>\n<p><strong>文件</strong>: <code>hono-entry.ts</code> (48 行)</p>\n<p><code>generateHonoEntryCode()</code> 只有 3 行有效逻辑：</p>\n<pre><code class=\"language-ts\">const descriptor = buildEntryDescriptor(routes, options);\nreturn renderEntry(descriptor);\n</code></pre>\n<p>同时该文件 re-export 了 <code>buildEntryDescriptor</code>、<code>renderEntry</code>、<code>EntryDescriptor</code>——三者已各自在原文件中独立导出。</p>\n<p><strong>方案</strong>:</p>\n<ul>\n<li>将 <code>generateHonoEntryCode</code> + <code>HonoEntryOptions</code> 移到 <code>entry-renderer.ts</code> 底部</li>\n<li>删除 <code>hono-entry.ts</code></li>\n<li>所有导入点改为从 <code>entry-renderer.ts</code> 导入</li>\n<li><code>index.ts</code> 改为 <code>import { generateHonoEntryCode } from &#39;./entry-renderer.js&#39;</code></li>\n</ul>\n<p><strong>收益</strong>: 减少 1 个文件 + 48 行，消除 re-export 链。</p>\n<p><strong>测试影响</strong>: 两个测试文件需改导入路径：<code>entry-renderer.test.ts</code> 和 <code>entry-descriptor.test.ts</code>。</p>\n<hr />\n<h3>2.3 消除内联 HTML 转义 → 使用 <code>escapeHtml()</code></h3>\n<p><strong>文件</strong>: <code>entry-renderer.ts:238-239</code> (生成的代码中)</p>\n<pre><code class=\"language-ts\">// 当前：160 字符的 replace 链\nconst safeErr = String(err.stack || err).replace(/&amp;/g,&#39;&amp;amp;&#39;).replace(/&lt;/g,&#39;&amp;lt;&#39;)...\n</code></pre>\n<p><strong>改为</strong>: 在 <code>buildEntryDescriptor()</code> 中给 <code>imports</code> 增加 <code>escapeHtml</code> 的导入声明，生成的代码改为：</p>\n<pre><code class=\"language-ts\">import { escapeHtml } from &#39;@openelement/core/html-escape&#39;;\n// ...\nconst safeErr = escapeHtml(String(err.stack || err));\n</code></pre>\n<p><strong>同理</strong>: <code>ssr-handler.ts:31</code> 的内联 <code>.replace(/&lt;/g, &#39;&amp;lt;&#39;).replace(/&gt;/g, &#39;&amp;gt;&#39;)</code> 也改为使用 <code>escapeHtml()</code>（该文件已从 <code>html-escape.ts</code> 导入 <code>wrapInDocument</code>）。</p>\n<p><strong>收益</strong>: 消除两处逻辑重复，减少生成代码体积，单一真相源。</p>\n<hr />\n<h3>2.4 合并 <code>renderPageRoute</code> 的 wrapInDocument 分支</h3>\n<p><strong>文件</strong>: <code>entry-renderer.ts:209-228</code></p>\n<p>当前有两个分支，<code>wrapInDocument</code> 的 5 行参数完全相同，仅第一个参数不同（<code>wrapped</code> vs <code>html</code>）。</p>\n<p><strong>改为</strong>: 统一变量名，消除分支：</p>\n<pre><code class=\"language-ts\">lines.push(`    let content = html`);\nfor (const renderer of matchingRenderers) {\n  lines.push(`    content = ${renderer.varName}.default.wrap(content, c)`);\n}\nlines.push(`    return c.html(wrapInDocument(content, {`);\nlines.push(`      title: ..., lang: ..., headExtras: ..., cspNonce: ...`);\nlines.push(`    }))`);\n</code></pre>\n<p>当 <code>matchingRenderers.length === 0</code> 时，循环不执行，<code>content === html</code>，行为不变。</p>\n<p><strong>收益</strong>: 消除 6 行重复，逻辑更清晰。</p>\n<hr />\n<h3>2.5 合并 <code>renderCorsOrigin</code> 的重复分支</h3>\n<p><strong>文件</strong>: <code>entry-renderer.ts:53-62</code></p>\n<pre><code class=\"language-ts\">// 当前：string 和 Array 两个分支做同样的事\nif (typeof origin === &#39;string&#39;) { return JSON.stringify(origin); }\nif (Array.isArray(origin))   { return JSON.stringify(origin); }\n</code></pre>\n<p><strong>改为</strong>:</p>\n<pre><code class=\"language-ts\">function renderCorsOrigin(origin: CorsOriginConfig): string {\n  if (typeof origin === &#39;object&#39; &amp;&amp; !Array.isArray(origin)) return origin.body;\n  return JSON.stringify(origin);\n}\n</code></pre>\n<p><strong>收益</strong>: 减少 4 行。</p>\n<hr />\n<h3>2.6 合并 <code>ssr-handler.ts</code> 到 <code>html-escape.ts</code></h3>\n<p><strong>文件</strong>: <code>ssr-handler.ts</code> (63 行)</p>\n<p>当前 <code>ssr-handler.ts</code> 只做两件事：</p>\n<ol>\n<li>Re-export <code>wrapInDocument</code>（从 <code>html-escape.ts</code>）</li>\n<li>定义 <code>renderSsrError()</code> 函数</li>\n</ol>\n<p><code>html-escape.ts</code> 已经有 <code>wrapInDocument</code>、<code>escapeHtml</code>、<code>escapeAttr</code> 等——<code>renderSsrError()</code> 是 HTML 错误页面渲染，与 <code>html-escape.ts</code> 的&quot;HTML 输出工具&quot;定位完全一致。</p>\n<p><strong>方案</strong>:</p>\n<ul>\n<li>将 <code>renderSsrError()</code> 移入 <code>html-escape.ts</code></li>\n<li><code>ssr-handler.ts</code> 改为纯 re-export 文件（兼容性），或直接删除</li>\n<li><code>index.ts</code> 的 <code>export { renderSsrError, wrapInDocument } from &#39;./ssr-handler.js&#39;</code> 改为 <code>from &#39;./html-escape.js&#39;</code></li>\n</ul>\n<p><strong>收益</strong>: 减少 1 个文件的概念负担，HTML 渲染工具统一。</p>\n<hr />\n<h3>2.7 将 <code>__ssr</code> 辅助函数从生成代码移到 <code>less-runtime.ts</code></h3>\n<p><strong>文件</strong>: <code>entry-renderer.ts:386-401</code> (每次构建都生成的 ~16 行代码)</p>\n<p><code>__ssr()</code> 是一个完全确定性的辅助函数，逻辑只依赖 <code>renderDSD</code> 和 <code>log</code>——两者都在 <code>less-runtime.ts</code> 中已导出。</p>\n<p><strong>方案</strong>: 将 <code>__ssr</code> 移到 <code>less-runtime.ts</code> 导出，生成的 entry 代码只需 <code>import { ..., __ssr } from &#39;@openelement/core/less-runtime&#39;</code>。</p>\n<p><strong>注意</strong>: 这会增加 <code>less-runtime.ts</code> 的导出，但 <code>__ssr</code> 在 SSG bundle 中是 tree-shakeable 的（如果不被使用就不会包含）。</p>\n<p><strong>替代方案（更安全）</strong>: 如果担心 <code>less-runtime.ts</code> 的职责膨胀，可以新建 <code>less-ssr-helper.ts</code> 并在 <code>entry-renderer.ts</code> 生成的代码中 <code>import { __ssr } from &#39;@openelement/core/less-ssr-helper&#39;</code>。</p>\n<p><strong>收益</strong>: 每个入口模块减少 ~200 字节生成代码。</p>\n<hr />\n<h3>2.8 消除 <code>__less_get_default_export</code> 辅助函数</h3>\n<p><strong>文件</strong>: <code>entry-renderer.ts:348</code></p>\n<pre><code class=\"language-ts\">const __less_get_default_export = (module) =&gt; module &amp;&amp; module.default\n</code></pre>\n<p>这个函数只是 <code>module?.default</code> 的别名。可以直接内联：</p>\n<pre><code class=\"language-ts\">const __island_component_x = __island_x?.default\n</code></pre>\n<p><strong>收益</strong>: 消除一个生成代码中的辅助函数，更符合 JS 惯例。</p>\n<hr />\n<h3>2.9 删除 <code>LessBuildContext.userResolveAlias</code></h3>\n<p><strong>文件</strong>: <code>build-context.ts:42</code></p>\n<p><code>userResolveAlias</code> 的存在是为了 <code>createServer()</code> 路径下传递 resolve alias——ADR 0008 Phase C 消除 <code>createServer()</code> 后，此字段不再需要。</p>\n<p><strong>时机</strong>: 与 ADR 0008 Phase C 同步。</p>\n<hr />\n<h2>三、Layer 2 — 跨包解耦与复用</h2>\n<h3>3.1 统一 escape 模式：<code>render-dsd.ts</code> 的 re-export 链</h3>\n<p><strong>文件</strong>: <code>render-dsd.ts:30-36</code></p>\n<pre><code class=\"language-ts\">export {\n  escapeAttr, escapeAttrValue, escapeHtml,\n  type SafeHtml, type UnsafeHtml,\n} from &#39;./html-escape.js&#39;;\n</code></pre>\n<p><code>render-dsd.ts</code> re-export <code>html-escape.ts</code> 的所有内容——这意味着消费者可以从 <code>@openelement/core/render-dsd</code> 或 <code>@openelement/core/html-escape</code> 两处导入同一函数。</p>\n<p><strong>问题</strong>: 这是历史遗留。<code>render-dsd.ts</code> 的核心职责是 DSD 渲染，不应承担 escape 工具的导出。</p>\n<p><strong>方案</strong>:</p>\n<ul>\n<li>保留 <code>deno.json</code> 中 <code>./html-escape</code> 导出路径</li>\n<li>在 <code>render-dsd.ts</code> 的 re-export 块上方添加 <code>@deprecated</code> 注释</li>\n<li>下一个大版本移除 re-export</li>\n</ul>\n<hr />\n<h3>3.2 <code>runtime-shim.ts</code> 的巨型字符串化代码是最大的技术债</h3>\n<p><strong>文件</strong>: <code>runtime-shim.ts</code> (3 行，但字符串内容 ~2000+ 字符)</p>\n<p>这是 <code>generate-runtime-shim.ts</code> 脚本从源码 AST 提取 + 编译 + 字符串化产物的硬编码副本。每次修改 <code>render-dsd.ts</code>、<code>html-escape.ts</code>、<code>render-nested.ts</code> 都必须重新运行脚本。</p>\n<p><strong>方案</strong>: ADR 0008 Phase D 完成后，整个机制删除。这是 ADR 0008 的核心收益之一，不重复。</p>\n<p><strong>优先级</strong>: 与 ADR 0008 Phase D 绑定。</p>\n<hr />\n<h3>3.3 <code>@openelement/content</code> 的 <code>blog-data.ts</code> → 与 <code>i18n-data.ts</code> 对齐</h3>\n<p><strong>文件</strong>: <code>packages/content/src/blog/blog-data.ts</code></p>\n<p>当前使用 <code>globalThis[Symbol.for()]</code> 桥接，而 <code>@openelement/i18n</code> 的 <code>i18n-data.ts</code> 使用干净的模式（纯模块变量）。</p>\n<p><strong>方案</strong>: ADR 0008 Phase B 已覆盖。执行后两者模式完全一致。</p>\n<hr />\n<h3>3.4 <code>entry-generators.ts</code> 的客户端入口代码可以简化</h3>\n<p><strong>文件</strong>: <code>entry-generators.ts</code> (115 行)</p>\n<p>生成的客户端入口代码内联了一个 <code>log</code> stub：</p>\n<pre><code class=\"language-js\">var log = {\n  warn: function() { var a = [&#39;[LessJS]&#39;]; a.push.apply(a, arguments); ... },\n  error: function() { ... },\n};\n</code></pre>\n<p>这个 stub 与 <code>logger.ts</code> 的 <code>LessLogger</code> 逻辑重复，但因为它要运行在浏览器端且不能依赖模块系统，所以内联是合理的。</p>\n<p><strong>评估</strong>: 此项不做简化——浏览器端 shim 的内联是刻意为之，避免引入构建时依赖。</p>\n<hr />\n<h3>3.5 <code>ssg-postprocess.ts</code> 的 HTML 遍历模式重复</h3>\n<p><strong>文件</strong>: <code>ssg-postprocess.ts</code></p>\n<p><code>injectClientScript</code>、<code>injectCspMeta</code>、<code>injectDsdPolyfill</code>、<code>injectViewTransitionMeta</code>、<code>injectSpeculationRules</code> 五个函数都有相同的外层循环：</p>\n<pre><code class=\"language-ts\">const entries = readdirSync(dir, { withFileTypes: true });\nfor (const entry of entries) {\n  const fullPath = join(dir, entry.name);\n  if (entry.isDirectory()) { /* 递归 */ }\n  else if (entry.name.endsWith(&#39;.html&#39;)) { /* 读取 → 修改 → 写入 */ }\n}\n</code></pre>\n<p><strong>方案</strong>: 提取 <code>walkHtmlFiles(dir, visitor: (fullPath, content) =&gt; string | null)</code> 高阶函数：</p>\n<pre><code class=\"language-ts\">function walkHtmlFiles(dir: string, visitor: (content: string, fullPath: string) =&gt; string | null): void {\n  const entries = readdirSync(dir, { withFileTypes: true });\n  for (const entry of entries) {\n    const fullPath = join(dir, entry.name);\n    if (entry.isDirectory()) { walkHtmlFiles(fullPath, visitor); return; }\n    if (entry.name.endsWith(&#39;.html&#39;)) {\n      const content = readFileSync(fullPath, &#39;utf-8&#39;);\n      const result = visitor(content, fullPath);\n      if (result !== null) writeFileSync(fullPath, result, &#39;utf-8&#39;);\n    }\n  }\n}\n</code></pre>\n<p><strong>收益</strong>: 消除 5 处相同的目录遍历代码，减少 ~60 行重复。</p>\n<hr />\n<h3>3.6 <code>build-manifest.ts</code> 的 <code>collectFiles</code> 与 <code>ssg-postprocess.ts</code> 的遍历重复</h3>\n<p><strong>文件</strong>: <code>build-manifest.ts:59-88</code></p>\n<p><code>collectFiles()</code> 也是递归遍历文件系统，但与 <code>walkHtmlFiles</code> 不同——它收集信息不修改文件。两者可以共享 <code>walkDir</code> 基础函数。</p>\n<p><strong>方案</strong>: 如果 3.5 提取了 <code>walkHtmlFiles</code>，可以进一步抽象为 <code>walkDir(dir, { onFile, onDir, filter })</code> 通配遍历器。但优先级低——<code>build-manifest.ts</code> 是纯可观察性工具，不影响功能。</p>\n<p><strong>评估</strong>: 标记为 P2，不做在本轮。</p>\n<hr />\n<h2>四、Layer 3 — 入口与导出精简</h2>\n<h3>4.1 <code>render-dsd.ts</code> 的 re-export 链清理</h3>\n<p>如 3.1 所述，<code>render-dsd.ts</code> re-export 了 <code>html-escape.ts</code> 和 <code>types.ts</code> 的内容。在 ADR 0008 完成后，<code>render-dsd.ts</code> 应只导出与 DSD 渲染直接相关的内容：</p>\n<p><strong>保留导出</strong>:</p>\n<ul>\n<li><code>renderDSD</code>, <code>renderDSDByName</code> (核心功能)</li>\n<li><code>camelToKebab</code> (被 SSR 代码使用)</li>\n<li><code>renderNestedCustomElements</code> (从 render-nested.ts)</li>\n</ul>\n<p><strong>标记 @deprecated 的 re-export</strong>:</p>\n<ul>\n<li><code>escapeAttr</code>, <code>escapeAttrValue</code>, <code>escapeHtml</code>, <code>SafeHtml</code>, <code>UnsafeHtml</code></li>\n<li><code>ComponentLayer</code>, <code>DsdComponent</code>, <code>DsdOptions</code>, <code>HydrateEventDescriptor</code>, <code>RenderAdapter</code>, <code>registerAdapter</code></li>\n</ul>\n<p>这些应引导消费者使用 <code>@openelement/core/html-escape</code> 和 <code>@openelement/core</code> 主入口。</p>\n<hr />\n<h3>4.2 <code>less-runtime.ts</code> 的导出精简</h3>\n<p><strong>文件</strong>: <code>less-runtime.ts</code> (15 行)</p>\n<pre><code class=\"language-ts\">export { log };\nexport { registerAdapter, renderDSD, renderDSDByName } from &#39;./render-dsd.js&#39;;\nexport { Hono } from &#39;hono&#39;;\nexport { wrapInDocument } from &#39;./ssr-handler.js&#39;;\n</code></pre>\n<p>问题：<code>export { Hono } from &#39;hono&#39;</code> 让 <code>less-runtime</code> 成了 Hono 的 re-export 点。SSR bundle 已经 <code>import { Hono } from &#39;hono&#39;</code>，不需要通过 <code>less-runtime</code> 间接获取。</p>\n<p><strong>方案</strong>: 移除 <code>export { Hono }</code>，生成的 entry 代码直接 <code>import { Hono } from &#39;hono&#39;</code>。</p>\n<p><strong>收益</strong>: <code>less-runtime.ts</code> 职责更清晰——只导出 LessJS 自己的运行时 API。</p>\n<hr />\n<h3>4.3 <code>index.ts</code> 导出审计</h3>\n<p><strong>文件</strong>: <code>index.ts</code> (305 行)</p>\n<p>当前 <code>index.ts</code> 导出了大量内部细节，如：</p>\n<ul>\n<li><code>extractCustomElementTags</code> (island-manifest 内部)</li>\n<li><code>buildSpeculationRulesJson</code> (ssg-postprocess 内部)</li>\n<li><code>buildIslandChunkMap</code> (ssg-postprocess 内部)</li>\n<li><code>printBuildManifest</code>, <code>scanClientBuild</code>, <code>scanSSGOutput</code> (build-manifest 内部)</li>\n</ul>\n<p>这些都是 CLI 工具的内部函数，不应通过公共 API 暴露。</p>\n<p><strong>方案</strong>:</p>\n<ul>\n<li>将 CLI 专用函数标记为 <code>@internal</code>（在 deno.json 的 exports 中不导出）</li>\n<li>或将其移到 <code>@openelement/core/cli/*</code> 导出路径下</li>\n</ul>\n<p><strong>收益</strong>: 公共 API 表面更小，减少破坏性变更的风险。</p>\n<hr />\n<h3>4.4 <code>types.ts</code> 的职责拆分</h3>\n<p><strong>文件</strong>: <code>types.ts</code> (375 行)</p>\n<p><code>types.ts</code> 承担了三个职责：</p>\n<ol>\n<li><strong>公共类型定义</strong> (<code>FrameworkOptions</code>, <code>RouteEntry</code>, <code>DsdComponent</code> 等)</li>\n<li><strong>运行时状态</strong> (<code>_adapter</code>, <code>registerAdapter</code>, <code>getAdapter</code>)</li>\n<li><strong>DSD 类型</strong> (<code>DsdOptions</code>, <code>ComponentLayer</code>, <code>HydrateEventDescriptor</code>)</li>\n</ol>\n<p>其中第 2 项（模块级 <code>_adapter</code> + accessor）违反了&quot;类型文件不应有运行时状态&quot;的原则。</p>\n<p><strong>方案</strong>: 将 <code>registerAdapter</code> / <code>getAdapter</code> + <code>_adapter</code> 移到 <code>adapter-registry.ts</code>（或 <code>less-runtime.ts</code>），<code>types.ts</code> 只保留类型定义。</p>\n<p><strong>收益</strong>: 类型文件回到纯类型职责，运行时状态有明确的家。</p>\n<hr />\n<h2>五、简化后的文件结构预期</h2>\n<h3>变更汇总</h3>\n<table>\n<thead>\n<tr>\n<th>操作</th>\n<th>文件</th>\n<th>说明</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>删除</strong></td>\n<td><code>hono-entry.ts</code></td>\n<td>合并到 entry-renderer.ts</td>\n</tr>\n<tr>\n<td><strong>删除</strong></td>\n<td><code>runtime-shim.ts</code></td>\n<td>ADR 0008 Phase D</td>\n</tr>\n<tr>\n<td><strong>删除</strong></td>\n<td><code>scripts/generate-runtime-shim.ts</code></td>\n<td>ADR 0008 Phase D</td>\n</tr>\n<tr>\n<td><strong>删除</strong></td>\n<td><code>ssr-handler.ts</code></td>\n<td>合并到 html-escape.ts（留 re-export 兼容）</td>\n</tr>\n<tr>\n<td><strong>合并</strong></td>\n<td><code>CodeBuilder</code> → <code>string[]</code></td>\n<td>entry-renderer.ts 内部</td>\n</tr>\n<tr>\n<td><strong>提取</strong></td>\n<td><code>walkHtmlFiles()</code></td>\n<td>ssg-postprocess.ts 内部，消除 5 处重复</td>\n</tr>\n<tr>\n<td><strong>提取</strong></td>\n<td><code>__ssr</code> → less-runtime.ts</td>\n<td>或新建 less-ssr-helper.ts</td>\n</tr>\n<tr>\n<td><strong>移动</strong></td>\n<td><code>registerAdapter</code>/<code>getAdapter</code></td>\n<td>types.ts → adapter-registry.ts</td>\n</tr>\n<tr>\n<td><strong>移除</strong></td>\n<td><code>export { Hono }</code></td>\n<td>less-runtime.ts</td>\n</tr>\n<tr>\n<td><strong>标记</strong></td>\n<td><code>@deprecated</code> re-exports</td>\n<td>render-dsd.ts 对 html-escape 的 re-export</td>\n</tr>\n<tr>\n<td><strong>清理</strong></td>\n<td><code>userResolveAlias</code></td>\n<td>build-context.ts（ADR 0008 Phase C 后）</td>\n</tr>\n</tbody></table>\n<h3>预期行数变化</h3>\n<table>\n<thead>\n<tr>\n<th>文件</th>\n<th>当前</th>\n<th>预期</th>\n<th>变化</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>entry-renderer.ts</td>\n<td>472</td>\n<td>~440</td>\n<td>-32 (删除 CodeBuilder + 合并分支 + 合并 CORS 分支)</td>\n</tr>\n<tr>\n<td>hono-entry.ts</td>\n<td>48</td>\n<td>0</td>\n<td>-48 (删除)</td>\n</tr>\n<tr>\n<td>html-escape.ts</td>\n<td>137</td>\n<td>~170</td>\n<td>+33 (接收 renderSsrError)</td>\n</tr>\n<tr>\n<td>ssr-handler.ts</td>\n<td>63</td>\n<td>~5</td>\n<td>-58 (改 re-export)</td>\n</tr>\n<tr>\n<td>ssg-postprocess.ts</td>\n<td>409</td>\n<td>~360</td>\n<td>-49 (提取 walkHtmlFiles)</td>\n</tr>\n<tr>\n<td>less-runtime.ts</td>\n<td>15</td>\n<td>~30</td>\n<td>+15 (接收 __ssr)</td>\n</tr>\n<tr>\n<td>types.ts</td>\n<td>375</td>\n<td>~345</td>\n<td>-30 (移出 adapter 状态)</td>\n</tr>\n<tr>\n<td>build-context.ts</td>\n<td>61</td>\n<td>~55</td>\n<td>-6 (移出 userResolveAlias)</td>\n</tr>\n<tr>\n<td><strong>核心总计</strong></td>\n<td><strong>5846</strong></td>\n<td><strong>~5500</strong></td>\n<td><strong>~350 行减少</strong></td>\n</tr>\n</tbody></table>\n<hr />\n<h2>六、执行顺序与依赖</h2>\n<pre><code>Layer 0: ADR 0008 (Phase C+B → A → D)\n  │\n  │ ← 必须先完成，Layer 1 的一些简化依赖其结果\n  │\n  ├─→ Layer 1.1: 删除 CodeBuilder            (独立，随时可做)\n  ├─→ Layer 1.2: 合并 hono-entry.ts           (独立，随时可做)\n  ├─→ Layer 1.3: 消除内联 escape               (独立，随时可做)\n  ├─→ Layer 1.4: 合并 renderPageRoute 分支     (独立，随时可做)\n  ├─→ Layer 1.5: 合并 renderCorsOrigin 分支    (独立，随时可做)\n  ├─→ Layer 1.6: 合并 ssr-handler.ts          (独立，随时可做)\n  ├─→ Layer 1.7: __ssr 移到 less-runtime       (需 ADR 0008 Phase C 后)\n  ├─→ Layer 1.8: 消除 __less_get_default_export (独立)\n  ├─→ Layer 1.9: 移除 userResolveAlias        (需 ADR 0008 Phase C 后)\n  │\n  ├─→ Layer 2.1: render-dsd.ts re-export 标记   (独立)\n  ├─→ Layer 2.2: runtime-shim 删除              (ADR 0008 Phase D)\n  ├─→ Layer 2.3: blog-data.ts 对齐             (ADR 0008 Phase B)\n  ├─→ Layer 2.5: 提取 walkHtmlFiles            (独立)\n  │\n  └─→ Layer 3.1-3.4: 导出精简与拆分            (Layer 1 完成后)\n</code></pre>\n<p><strong>关键约束</strong>:</p>\n<ul>\n<li>Layer 1.1–1.6 和 1.8 <strong>全部互相独立</strong>，可以并行执行</li>\n<li>Layer 1.7 和 1.9 <strong>依赖 ADR 0008 Phase C</strong></li>\n<li>Layer 3 的导出变更 <strong>依赖 Layer 1 完成后</strong>（避免中途改导出路径两次）</li>\n</ul>\n<hr />\n<h2>七、风险矩阵</h2>\n<table>\n<thead>\n<tr>\n<th>变更</th>\n<th>破坏性</th>\n<th>风险</th>\n<th>缓解</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>删除 CodeBuilder</td>\n<td>内部</td>\n<td>零</td>\n<td>纯机械替换</td>\n</tr>\n<tr>\n<td>合并 hono-entry.ts</td>\n<td>测试导入路径</td>\n<td>低</td>\n<td>2 个测试文件改路径</td>\n</tr>\n<tr>\n<td>消除内联 escape</td>\n<td>生成代码变化</td>\n<td>低</td>\n<td>快照测试覆盖</td>\n</tr>\n<tr>\n<td>合并 renderPageRoute 分支</td>\n<td>生成代码变化</td>\n<td>低</td>\n<td>行为等价验证</td>\n</tr>\n<tr>\n<td>合并 ssr-handler.ts</td>\n<td>公共导出路径</td>\n<td>中</td>\n<td>保留 re-export 兼容层</td>\n</tr>\n<tr>\n<td>__ssr 移到 less-runtime</td>\n<td>运行时依赖链</td>\n<td>中</td>\n<td>需验证 tree-shaking</td>\n</tr>\n<tr>\n<td>移除 <code>export { Hono }</code></td>\n<td>生成代码导入</td>\n<td>低</td>\n<td>改为直接 import</td>\n</tr>\n<tr>\n<td>提取 walkHtmlFiles</td>\n<td>内部</td>\n<td>低</td>\n<td>纯重构</td>\n</tr>\n<tr>\n<td>types.ts 拆分</td>\n<td>导入路径</td>\n<td>中</td>\n<td>保留 re-export 兼容层</td>\n</tr>\n</tbody></table>\n<hr />\n<h2>八、不在本方案范围内的事项</h2>\n<table>\n<thead>\n<tr>\n<th>事项</th>\n<th>原因</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>entry-generators.ts</code> 的客户端 log stub</td>\n<td>浏览器端不能依赖模块系统，内联合理</td>\n</tr>\n<tr>\n<td><code>build-manifest.ts</code> 的文件遍历</td>\n<td>纯可观察性工具，P2</td>\n</tr>\n<tr>\n<td><code>navigation.ts</code> 的 Navigation API 类型声明</td>\n<td>标准 API 模拟，改动风险高收益低</td>\n</tr>\n<tr>\n<td><code>island.ts</code> 的 connectedCallback 包装</td>\n<td>核心功能，不能简化</td>\n</tr>\n<tr>\n<td><code>render-nested.ts</code> 的 parse5 AST 遍历</td>\n<td>性能关键路径，不能简化</td>\n</tr>\n</tbody></table>\n<hr />\n<p><em>方案日期: 2026-05-10 | 基于 ADR 0008 + 全仓库审查</em></p>\n"
  },
  {
    "slug": "adr-0008-0009-unification-sop",
    "frontmatter": {
      "title": "SOP: ADR 0008 Completion + Single-Plugin Unification",
      "date": "2026-05-10",
      "draft": false,
      "tags": [
        "architecture",
        "decision"
      ],
      "type": "adr",
      "lang": "en"
    },
    "content": "\n> **Branch**: `dev` | **Created**: 2026-05-10 | **Status**: ✅ FULLY IMPLEMENTED\n\n## Background\n\nADR 0008 Phase C is complete (`createServer()` → `viteBuild + import()`).\nAll phases (A, B, C, D, E) are now fully implemented via ADR 0010/0011/0012.\n\n## Final State\n\n- **Phase A** (`.less/` file IPC → memory): ✅ Done via ADR 0010\n- **Phase B** (globalThis bridges → module vars): ✅ Done\n- **Phase C** (eliminate `createServer()`): ✅ Done — closeBundle inlines Phase 2/3 (ADR 0011)\n- **Phase D** (runtime-shim → virtual module): ✅ Done\n- **Phase E** (single-plugin API): ✅ Done — `lessjs()` extracted to `@openelement/app` (ADR 0012)\n\n## Key Changes from Original SOP\n\n- **Phase E updated**: `lessjs()` was originally in `@openelement/core` with dynamic `import()`. Per ADR 0012, it has been extracted to a new `@openelement/app` package with static imports for better type safety and correct dependency direction.\n- **globalThis fully deleted**: `getActiveContext()`/`setActiveContext()`/`clearActiveContext()` have been removed from `build-context.ts`. ctx is only passed via explicit parameter.\n- **content/i18n require explicit ctx**: `lessContent({ ctx })` and `lessI18n({ ctx })` — no auto-discovery fallback.\n\n## Architecture Summary\n\n```\n@openelement/app (lessjs entry)\n  ├── @openelement/core       (less(), LessBuildContext)\n  ├── @openelement/content    (lessContent)\n  └── @openelement/i18n       (lessI18n)\n```\n\nPublish order: rpc → ui → adapter-lit → signal → content → i18n → core → app → create\n\n---\n\n> **The sections below are the original implementation plan, kept as historical reference.\n> All tasks are complete. No further action needed.**\n\n## Current State of `.less/` Files (Phase A targets) — ALL ELIMINATED\n\nThese files are written by Phase 1 plugins and read by Phase 2/3 CLI scripts:\n\n| File                    | Writer                    | Reader                                         | Contains                                                                                                                                      |\n| ----------------------- | ------------------------- | ---------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |\n| `build-metadata.json`   | `less:build` closeBundle  | `build-client.ts`, `build-ssg.ts`              | islandTagNames, islandFiles, packageIslands, resolveAlias, ssrNoExternal, middleware, html, pwa, upgradeStrategy, viewTransition, speculation |\n| `head-extras.html`      | `build-ssg.ts`            | SSR entry (generated code reads it at runtime) | raw HTML string                                                                                                                               |\n| `.less-ssg-entry.ts`    | `build-ssg.ts`            | viteBuild SSR input                            | generated Hono entry code                                                                                                                     |\n| `.less-client-entry.ts` | `build-client.ts`         | viteBuild client input                         | generated client island entry                                                                                                                 |\n| `.less-runtime.ts`      | `less:core` config hook   | Vite alias resolution                          | runtime shim code                                                                                                                             |\n| `blog-options.json`     | `less:content` buildStart | `build-ssg.ts`                                 | `{ contentDir, basePath }`                                                                                                                    |\n| `nav-data.json`         | `less:content` buildStart | `build-ssg.ts` virtual:less-nav plugin         | NavSection[]                                                                                                                                  |\n| `header-nav.json`       | `less:content` buildStart | `build-ssg.ts` virtual:less-nav plugin         | HeaderNavLink[]                                                                                                                               |\n| `i18n-options.json`     | `less:i18n` buildStart    | `build-ssg.ts`                                 | LessI18nOptions                                                                                                                               |\n| `sitemap-options.json`  | `less:content` buildStart | `build-ssg.ts`                                 | SitemapOptions                                                                                                                                |\n\n**Total: 10 temp files.** Goal: 0.\n\n---\n\n## Phase Breakdown & Execution Order\n\n### Phase A: Eliminate `.less/` File IPC → `LessBuildContext` + Virtual Modules\n\n**Strategy**: All build-time data flows through `LessBuildContext` (in-memory). CLI scripts receive context directly instead of reading JSON files. Virtual modules resolve from context instead of reading `.less/` JSON.\n\n**Risk**: MEDIUM — touches the entire build pipeline, but changes are mechanical (read file → read context property).\n\n#### A.1: Expand `LessBuildContext` to carry all current `.less/` data\n\n**File**: `packages/core/src/build-context.ts`\n\nAdd these fields:\n\n```typescript\n// ─── From less:content buildStart ────────────────────────────\nblogOptions: { contentDir?: string; basePath?: string } | null = null;\nnavSections: import('./content-types.js').NavSection[] = [];\nheaderNav: import('./content-types.js').HeaderNavLink[] = [];\nsitemapOptions: import('./content-types.js').SitemapOptions | null = null;\n\n// ─── From less:i18n buildStart ───────────────────────────────\ni18nOptions: import('./i18n-types.js').LessI18nOptions | null = null;\n\n// ─── From less:build closeBundle ────────────────────────────\n// (replaces build-metadata.json)\nroot: string = '';\noutDir: string = 'dist';\nbase: string = '/';\nmiddleware: FrameworkOptions['middleware'] | null = null;\nhtml: FrameworkOptions['html'] | null = null;\npwa: FrameworkOptions['pwa'] | null = null;\nupgradeStrategy: string = 'lazy';\nviewTransition: boolean = true;\nspeculation: boolean | SpeculationRulesOptions | null = null;\nheadExtras: string = '';\n\n// ─── From entry-renderer ─────────────────────────────────────\nssgEntryCode: string = '';     // replaces .less-ssg-entry.ts\nclientEntryCode: string = '';  // replaces .less-client-entry.ts\n```\n\n**Note**: Type imports need to be duplicated or extracted to a shared types file. Prefer a lightweight `packages/core/src/shared-types.ts` that content/i18n can also reference, or use `Record<string, unknown>` with runtime casts.\n\n**Validation**: `deno check`, `deno lint`, existing tests pass.\n\n#### A.2: `less:content` write to `ctx` instead of `.less/` files\n\n**File**: `packages/content/src/index.ts`\n\n- `lessContent()` receives `LessBuildContext` as parameter (or accesses it via a shared symbol on Vite's `config` object)\n- `buildStart()` sets `ctx.blogOptions`, `ctx.navSections`, `ctx.headerNav`, `ctx.sitemapOptions`\n- Remove all `writeFileSync(join(lessDir, '*.json'))` calls\n\n**Problem**: `lessContent()` currently returns `Plugin[]` and doesn't know about `LessBuildContext`. Two options:\n\n**Option A (preferred)**: `lessContent()` accepts `{ ctx }` option. The `lessjs()` umbrella plugin creates the context and passes it to all sub-plugins.\n\n**Option B**: Store context on `config` via a `configResolved` hook symbol key. More coupling, less clean.\n\n→ **Go with Option A**. This is the path to single-plugin API.\n\n#### A.3: `less:i18n` write to `ctx` instead of `.less/` files\n\n**File**: `packages/i18n/src/index.ts`\n\n- `lessI18n()` accepts `{ ctx }` option\n- `buildStart()` sets `ctx.i18nOptions`\n- Remove `writeFileSync(join(lessDir, 'i18n-options.json'))`\n\n#### A.4: `less:build` write metadata to `ctx` instead of `.less/build-metadata.json`\n\n**File**: `packages/core/src/build.ts`\n\n- `closeBundle()` sets `ctx.root`, `ctx.base`, `ctx.resolveAlias`, etc.\n- Remove `writeFileSync(metadataPath, ...)`\n- The ctx is already passed to `buildPlugin()` — just extend what it writes\n\n#### A.5: `build-client.ts` read from `ctx` instead of `.less/build-metadata.json`\n\n**File**: `packages/core/src/cli/build-client.ts`\n\n- Accept `LessBuildContext` as parameter (passed from CLI orchestrator)\n- Remove `readFileSync(join(root, '.less', 'build-metadata.json'))`\n- Read `ctx.islandTagNames`, `ctx.islandFiles`, `ctx.packageIslands`, etc.\n- Write `.less-client-entry.ts` to memory (ctx.clientEntryCode) instead of filesystem\n\n**Problem**: `build-client.ts` is a standalone CLI script (`if (import.meta.main)`). It currently reads metadata from disk because Phase 1 (vite build) and Phase 2/3 are separate processes.\n\n**Solution**: Create a unified `build.ts` orchestrator that:\n\n1. Runs Phase 1 (vite build) → ctx gets populated by plugin hooks\n2. Runs Phase 2 (client build) → reads from ctx\n3. Runs Phase 3 (SSG) → reads from ctx\n4. All within the same Node.js process — no `.less/` IPC needed\n\nThis is the **key architectural change**. Instead of 3 separate CLI scripts, one orchestrator:\n\n```typescript\n// packages/core/src/cli/build.ts (new unified build command)\nexport async function build(options: FrameworkOptions) {\n  const ctx = new LessBuildContext(options);\n\n  // Phase 1: SSR build (vite build) — plugins populate ctx\n  await viteBuild({\n    plugins: [\n      lessCore(options, ctx),\n      lessContent({ ...options.content, ctx }),\n      lessI18n({ ...options.i18n, ctx }),\n    ],\n  });\n\n  // Phase 2: Client build — reads ctx\n  await buildClient(ctx);\n\n  // Phase 3: SSG render — reads ctx\n  await buildSSG(ctx);\n}\n```\n\n#### A.6: `build-ssg.ts` read from `ctx` instead of `.less/` files\n\n**File**: `packages/core/src/cli/build-ssg.ts`\n\n- Accept `LessBuildContext` as parameter\n- Replace all `readFileSync(join(root, '.less', '*.json'))` with `ctx.*` reads\n- Replace `.less-ssg-entry.ts` with `ctx.ssgEntryCode` (use virtual module or `define`)\n- Replace `virtual:less-nav` file read with `ctx.navSections` / `ctx.headerNav`\n- Replace `.less/head-extras.html` file read with `ctx.headExtras` (inline via `define`)\n- Keep `optionalPackageStubsPlugin()` — still needed for SSR build\n\n#### A.7: `entry-renderer.ts` headExtras — eliminate `.less/head-extras.html` read\n\n**File**: `packages/core/src/entry-renderer.ts`\n\nCurrently SSG entry code does:\n\n```typescript\nimport { readFileSync } from 'node:fs';\nlet __headExtras = readFileSync(join(process.cwd(), '.less', 'head-extras.html'), 'utf-8');\n```\n\nReplace with `define` injection:\n\n```typescript\n// In viteBuild config:\ndefine: {\n  __LESS_HEAD_EXTRAS__: JSON.stringify(options.headExtras);\n}\n\n// In generated entry code:\nconst __headExtras = __LESS_HEAD_EXTRAS__;\n```\n\nThis eliminates the runtime file read AND the `.less/head-extras.html` file.\n\n#### A.8: Clean up `.less/` directory creation\n\nAfter A.1–A.7, search for all remaining `.less/` references and remove:\n\n- `mkdirSync(lessTmpDir, ...)` calls\n- `writeFileSync(join(lessTmpDir, ...))` calls\n- Any `.less/` path references in comments/docs\n\n**Verification**: `grep -rn \"\\.less\" packages/ --include=\"*.ts\"` returns 0 results.\n\n---\n\n### Phase D: Replace `runtime-shim.ts` with `virtual:less-runtime`\n\n**Risk**: LOW — mechanical replacement.\n\n#### D.1: Create `virtual:less-runtime` Vite plugin\n\n**File**: `packages/core/src/index.ts` (inside `less()`)\n\nReplace the `less:core` config hook that writes `.less-runtime.ts` to disk:\n\n```typescript\n// BEFORE (writes to .less/.less-runtime.ts):\nconfig() {\n  writeFileSync(runtimePath, createRuntimeShimCode(), 'utf-8');\n  return { resolve: { alias: { '@openelement/core/less-runtime': runtimePath } } };\n}\n\n// AFTER (virtual module):\nconst VIRTUAL_RUNTIME_ID = 'virtual:less-runtime';\nconst RESOLVED_RUNTIME_ID = '\\0' + VIRTUAL_RUNTIME_ID;\n\n// In less:core plugin:\nresolveId(id) { if (id === VIRTUAL_RUNTIME_ID) return RESOLVED_RUNTIME_ID; }\nload(id) { if (id === RESOLVED_RUNTIME_ID) return createRuntimeShimCode(); }\n\n// In config() hook:\nreturn { resolve: { alias: { '@openelement/core/less-runtime': VIRTUAL_RUNTIME_ID } } };\n```\n\n#### D.2: Update `build-ssg.ts` SSR build to include `virtual:less-runtime` plugin\n\nThe SSG SSR build also needs to resolve `@openelement/core/less-runtime`. Add the same virtual module plugin to the SSR `viteBuild()` plugins array.\n\n#### D.3: Remove `createRuntimeShimCode()` from `runtime-shim.ts`\n\nThe function can stay (it generates the code), but it no longer writes to disk.\n\n**Verification**: `grep -rn \"\\.less-runtime\" packages/ --include=\"*.ts\"` returns 0 file-path references (only import IDs remain).\n\n---\n\n### Phase E: Single-Plugin API (`lessjs()`)\n\n**Risk**: LOW — pure API layer, internals unchanged.\n\n#### E.1: Create `lessjs()` umbrella function\n\n**File**: `packages/core/src/index.ts` (new export)\n\n```typescript\nexport function lessjs(options: LessjsOptions = {}): Plugin[] {\n  const ctx = new LessBuildContext(resolvedCoreOptions);\n\n  return [\n    ...less(resolvedCoreOptions, ctx),\n    ...(options.content ? lessContent({ ...options.content, ctx }) : []),\n    ...(options.i18n ? lessI18n({ ...options.i18n, ctx }) : []),\n  ];\n}\n```\n\nWhere `LessjsOptions`:\n\n```typescript\ninterface LessjsOptions extends FrameworkOptions {\n  content?: LessContentOptions;\n  i18n?: LessI18nOptions;\n}\n```\n\n#### E.2: Update `less()`, `lessContent()`, `lessI18n()` to accept `ctx`\n\nEach function gets an optional `ctx` parameter. When called from `lessjs()`, the shared context is used. When called standalone (backward compat), each creates its own context.\n\n#### E.3: Keep backward compatibility\n\n`less()`, `lessContent()`, `lessI18n()` remain exported and work independently. Users can still do:\n\n```typescript\nplugins: [less(), lessContent({ blog: true })];\n```\n\nBut `lessjs({ content: { blog: true } })` is the recommended path.\n\n#### E.4: Update CLI to use unified `build` command\n\nReplace the 3-script pipeline:\n\n```bash\ndeno task build        # runs: vite build + build:client + build:ssg\n```\n\nWith a single `build` command that uses the orchestrator from A.5.\n\n---\n\n## Execution Checklist\n\n### Phase A (Eliminate .less/ IPC) — ✅ COMPLETE\n\n- [x] A.1: Expand `LessBuildContext` with all `.less/` data fields\n- [x] A.2: `lessContent()` → write to `ctx` instead of `.less/` files\n- [x] A.3: `lessI18n()` → write to `ctx` instead of `.less/` files\n- [x] A.4: `less:build` → write metadata to `ctx` instead of `.less/build-metadata.json`\n- [x] A.5: Create unified `build.ts` orchestrator (replaces 3-script pipeline)\n- [x] A.6: `build-ssg.ts` → read from `ctx` instead of `.less/` files\n- [x] A.7: `entry-renderer.ts` → `define` injection replaces `head-extras.html` file read\n- [x] A.8: Clean up all remaining `.less/` references\n\n### Phase D (Virtual less-runtime) — ✅ COMPLETE\n\n- [x] D.1: Create `virtual:less-runtime` plugin in `less:core`\n- [x] D.2: Add virtual:less-runtime to SSR build plugins\n- [x] D.3: Remove `.less-runtime.ts` file write\n\n### Phase E (Single-plugin API) — ✅ COMPLETE (extracted to @openelement/app per ADR 0012)\n\n- [x] E.1: Create `lessjs()` umbrella function in `@openelement/app`\n- [x] E.2: Update `less()`, `lessContent()`, `lessI18n()` to accept shared `ctx`\n- [x] E.3: Verify backward compatibility (split-call with explicit `ctx` works)\n- [x] E.4: Update CLI to use unified build command (closeBundle inline per ADR 0011)\n\n---\n\n## Commit Strategy\n\n- **One commit per checkbox** (small, reviewable, revertable)\n- **Commit message format**: `refactor(core): A.1 — expand LessBuildContext with .less/ data fields`\n- **Run after each commit**: `deno fmt && deno lint && deno check` + full test suite\n- **Branch**: all on `dev`\n\n## Risk Mitigation\n\n1. **Incremental commits**: Each step is independently valid. If A.5 (unified orchestrator) proves complex, A.1–A.4 still eliminate `.less/` writes from plugins — the CLI can temporarily still read from a JSON dump.\n2. **Fallback**: If unified build orchestrator is too risky, keep Phase 1 as `vite build` (writes one metadata JSON to `.less/`) and have Phase 2/3 read it. This is a partial win: 10 files → 1 file.\n3. **Testing**: After each phase, run the full e2e build (`deno task build`) and verify output matches.\n\n## Definition of Done — ✅ ALL MET\n\n- `grep -rn \"\\.less\" packages/ --include=\"*.ts\"` returns 0 results (excluding comments)\n- `grep -rn \"globalThis\\[\" packages/ --include=\"*.ts\"` returns 0 results\n- `grep -rn \"getActiveContext\\|setActiveContext\\|clearActiveContext\" packages/ --include=\"*.ts\"` returns 0 results\n- User can write `plugins: [await lessjs({ content: { blog: true }, i18n: { locales: ['en', 'zh'] } })]` from `@openelement/app`\n- `lessjs()` uses static imports, not dynamic `import()` + try/catch\n- Build pipeline: single `viteBuild()` → `closeBundle()` inlines Phase 2/3\n",
    "html": "<blockquote>\n<p><strong>Branch</strong>: <code>dev</code> | <strong>Created</strong>: 2026-05-10 | <strong>Status</strong>: ✅ FULLY IMPLEMENTED</p>\n</blockquote>\n<h2>Background</h2>\n<p>ADR 0008 Phase C is complete (<code>createServer()</code> → <code>viteBuild + import()</code>).\nAll phases (A, B, C, D, E) are now fully implemented via ADR 0010/0011/0012.</p>\n<h2>Final State</h2>\n<ul>\n<li><strong>Phase A</strong> (<code>.less/</code> file IPC → memory): ✅ Done via ADR 0010</li>\n<li><strong>Phase B</strong> (globalThis bridges → module vars): ✅ Done</li>\n<li><strong>Phase C</strong> (eliminate <code>createServer()</code>): ✅ Done — closeBundle inlines Phase 2/3 (ADR 0011)</li>\n<li><strong>Phase D</strong> (runtime-shim → virtual module): ✅ Done</li>\n<li><strong>Phase E</strong> (single-plugin API): ✅ Done — <code>lessjs()</code> extracted to <code>@openelement/app</code> (ADR 0012)</li>\n</ul>\n<h2>Key Changes from Original SOP</h2>\n<ul>\n<li><strong>Phase E updated</strong>: <code>lessjs()</code> was originally in <code>@openelement/core</code> with dynamic <code>import()</code>. Per ADR 0012, it has been extracted to a new <code>@openelement/app</code> package with static imports for better type safety and correct dependency direction.</li>\n<li><strong>globalThis fully deleted</strong>: <code>getActiveContext()</code>/<code>setActiveContext()</code>/<code>clearActiveContext()</code> have been removed from <code>build-context.ts</code>. ctx is only passed via explicit parameter.</li>\n<li><strong>content/i18n require explicit ctx</strong>: <code>lessContent({ ctx })</code> and <code>lessI18n({ ctx })</code> — no auto-discovery fallback.</li>\n</ul>\n<h2>Architecture Summary</h2>\n<pre><code>@openelement/app (lessjs entry)\n  ├── @openelement/core       (less(), LessBuildContext)\n  ├── @openelement/content    (lessContent)\n  └── @openelement/i18n       (lessI18n)\n</code></pre>\n<p>Publish order: rpc → ui → adapter-lit → signal → content → i18n → core → app → create</p>\n<hr />\n<blockquote>\n<p><strong>The sections below are the original implementation plan, kept as historical reference.\nAll tasks are complete. No further action needed.</strong></p>\n</blockquote>\n<h2>Current State of <code>.less/</code> Files (Phase A targets) — ALL ELIMINATED</h2>\n<p>These files are written by Phase 1 plugins and read by Phase 2/3 CLI scripts:</p>\n<table>\n<thead>\n<tr>\n<th>File</th>\n<th>Writer</th>\n<th>Reader</th>\n<th>Contains</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>build-metadata.json</code></td>\n<td><code>less:build</code> closeBundle</td>\n<td><code>build-client.ts</code>, <code>build-ssg.ts</code></td>\n<td>islandTagNames, islandFiles, packageIslands, resolveAlias, ssrNoExternal, middleware, html, pwa, upgradeStrategy, viewTransition, speculation</td>\n</tr>\n<tr>\n<td><code>head-extras.html</code></td>\n<td><code>build-ssg.ts</code></td>\n<td>SSR entry (generated code reads it at runtime)</td>\n<td>raw HTML string</td>\n</tr>\n<tr>\n<td><code>.less-ssg-entry.ts</code></td>\n<td><code>build-ssg.ts</code></td>\n<td>viteBuild SSR input</td>\n<td>generated Hono entry code</td>\n</tr>\n<tr>\n<td><code>.less-client-entry.ts</code></td>\n<td><code>build-client.ts</code></td>\n<td>viteBuild client input</td>\n<td>generated client island entry</td>\n</tr>\n<tr>\n<td><code>.less-runtime.ts</code></td>\n<td><code>less:core</code> config hook</td>\n<td>Vite alias resolution</td>\n<td>runtime shim code</td>\n</tr>\n<tr>\n<td><code>blog-options.json</code></td>\n<td><code>less:content</code> buildStart</td>\n<td><code>build-ssg.ts</code></td>\n<td><code>{ contentDir, basePath }</code></td>\n</tr>\n<tr>\n<td><code>nav-data.json</code></td>\n<td><code>less:content</code> buildStart</td>\n<td><code>build-ssg.ts</code> virtual:less-nav plugin</td>\n<td>NavSection[]</td>\n</tr>\n<tr>\n<td><code>header-nav.json</code></td>\n<td><code>less:content</code> buildStart</td>\n<td><code>build-ssg.ts</code> virtual:less-nav plugin</td>\n<td>HeaderNavLink[]</td>\n</tr>\n<tr>\n<td><code>i18n-options.json</code></td>\n<td><code>less:i18n</code> buildStart</td>\n<td><code>build-ssg.ts</code></td>\n<td>LessI18nOptions</td>\n</tr>\n<tr>\n<td><code>sitemap-options.json</code></td>\n<td><code>less:content</code> buildStart</td>\n<td><code>build-ssg.ts</code></td>\n<td>SitemapOptions</td>\n</tr>\n</tbody></table>\n<p><strong>Total: 10 temp files.</strong> Goal: 0.</p>\n<hr />\n<h2>Phase Breakdown &amp; Execution Order</h2>\n<h3>Phase A: Eliminate <code>.less/</code> File IPC → <code>LessBuildContext</code> + Virtual Modules</h3>\n<p><strong>Strategy</strong>: All build-time data flows through <code>LessBuildContext</code> (in-memory). CLI scripts receive context directly instead of reading JSON files. Virtual modules resolve from context instead of reading <code>.less/</code> JSON.</p>\n<p><strong>Risk</strong>: MEDIUM — touches the entire build pipeline, but changes are mechanical (read file → read context property).</p>\n<h4>A.1: Expand <code>LessBuildContext</code> to carry all current <code>.less/</code> data</h4>\n<p><strong>File</strong>: <code>packages/core/src/build-context.ts</code></p>\n<p>Add these fields:</p>\n<pre><code class=\"language-typescript\">// ─── From less:content buildStart ────────────────────────────\nblogOptions: { contentDir?: string; basePath?: string } | null = null;\nnavSections: import(&#39;./content-types.js&#39;).NavSection[] = [];\nheaderNav: import(&#39;./content-types.js&#39;).HeaderNavLink[] = [];\nsitemapOptions: import(&#39;./content-types.js&#39;).SitemapOptions | null = null;\n\n// ─── From less:i18n buildStart ───────────────────────────────\ni18nOptions: import(&#39;./i18n-types.js&#39;).LessI18nOptions | null = null;\n\n// ─── From less:build closeBundle ────────────────────────────\n// (replaces build-metadata.json)\nroot: string = &#39;&#39;;\noutDir: string = &#39;dist&#39;;\nbase: string = &#39;/&#39;;\nmiddleware: FrameworkOptions[&#39;middleware&#39;] | null = null;\nhtml: FrameworkOptions[&#39;html&#39;] | null = null;\npwa: FrameworkOptions[&#39;pwa&#39;] | null = null;\nupgradeStrategy: string = &#39;lazy&#39;;\nviewTransition: boolean = true;\nspeculation: boolean | SpeculationRulesOptions | null = null;\nheadExtras: string = &#39;&#39;;\n\n// ─── From entry-renderer ─────────────────────────────────────\nssgEntryCode: string = &#39;&#39;;     // replaces .less-ssg-entry.ts\nclientEntryCode: string = &#39;&#39;;  // replaces .less-client-entry.ts\n</code></pre>\n<p><strong>Note</strong>: Type imports need to be duplicated or extracted to a shared types file. Prefer a lightweight <code>packages/core/src/shared-types.ts</code> that content/i18n can also reference, or use <code>Record&lt;string, unknown&gt;</code> with runtime casts.</p>\n<p><strong>Validation</strong>: <code>deno check</code>, <code>deno lint</code>, existing tests pass.</p>\n<h4>A.2: <code>less:content</code> write to <code>ctx</code> instead of <code>.less/</code> files</h4>\n<p><strong>File</strong>: <code>packages/content/src/index.ts</code></p>\n<ul>\n<li><code>lessContent()</code> receives <code>LessBuildContext</code> as parameter (or accesses it via a shared symbol on Vite&#39;s <code>config</code> object)</li>\n<li><code>buildStart()</code> sets <code>ctx.blogOptions</code>, <code>ctx.navSections</code>, <code>ctx.headerNav</code>, <code>ctx.sitemapOptions</code></li>\n<li>Remove all <code>writeFileSync(join(lessDir, &#39;*.json&#39;))</code> calls</li>\n</ul>\n<p><strong>Problem</strong>: <code>lessContent()</code> currently returns <code>Plugin[]</code> and doesn&#39;t know about <code>LessBuildContext</code>. Two options:</p>\n<p><strong>Option A (preferred)</strong>: <code>lessContent()</code> accepts <code>{ ctx }</code> option. The <code>lessjs()</code> umbrella plugin creates the context and passes it to all sub-plugins.</p>\n<p><strong>Option B</strong>: Store context on <code>config</code> via a <code>configResolved</code> hook symbol key. More coupling, less clean.</p>\n<p>→ <strong>Go with Option A</strong>. This is the path to single-plugin API.</p>\n<h4>A.3: <code>less:i18n</code> write to <code>ctx</code> instead of <code>.less/</code> files</h4>\n<p><strong>File</strong>: <code>packages/i18n/src/index.ts</code></p>\n<ul>\n<li><code>lessI18n()</code> accepts <code>{ ctx }</code> option</li>\n<li><code>buildStart()</code> sets <code>ctx.i18nOptions</code></li>\n<li>Remove <code>writeFileSync(join(lessDir, &#39;i18n-options.json&#39;))</code></li>\n</ul>\n<h4>A.4: <code>less:build</code> write metadata to <code>ctx</code> instead of <code>.less/build-metadata.json</code></h4>\n<p><strong>File</strong>: <code>packages/core/src/build.ts</code></p>\n<ul>\n<li><code>closeBundle()</code> sets <code>ctx.root</code>, <code>ctx.base</code>, <code>ctx.resolveAlias</code>, etc.</li>\n<li>Remove <code>writeFileSync(metadataPath, ...)</code></li>\n<li>The ctx is already passed to <code>buildPlugin()</code> — just extend what it writes</li>\n</ul>\n<h4>A.5: <code>build-client.ts</code> read from <code>ctx</code> instead of <code>.less/build-metadata.json</code></h4>\n<p><strong>File</strong>: <code>packages/core/src/cli/build-client.ts</code></p>\n<ul>\n<li>Accept <code>LessBuildContext</code> as parameter (passed from CLI orchestrator)</li>\n<li>Remove <code>readFileSync(join(root, &#39;.less&#39;, &#39;build-metadata.json&#39;))</code></li>\n<li>Read <code>ctx.islandTagNames</code>, <code>ctx.islandFiles</code>, <code>ctx.packageIslands</code>, etc.</li>\n<li>Write <code>.less-client-entry.ts</code> to memory (ctx.clientEntryCode) instead of filesystem</li>\n</ul>\n<p><strong>Problem</strong>: <code>build-client.ts</code> is a standalone CLI script (<code>if (import.meta.main)</code>). It currently reads metadata from disk because Phase 1 (vite build) and Phase 2/3 are separate processes.</p>\n<p><strong>Solution</strong>: Create a unified <code>build.ts</code> orchestrator that:</p>\n<ol>\n<li>Runs Phase 1 (vite build) → ctx gets populated by plugin hooks</li>\n<li>Runs Phase 2 (client build) → reads from ctx</li>\n<li>Runs Phase 3 (SSG) → reads from ctx</li>\n<li>All within the same Node.js process — no <code>.less/</code> IPC needed</li>\n</ol>\n<p>This is the <strong>key architectural change</strong>. Instead of 3 separate CLI scripts, one orchestrator:</p>\n<pre><code class=\"language-typescript\">// packages/core/src/cli/build.ts (new unified build command)\nexport async function build(options: FrameworkOptions) {\n  const ctx = new LessBuildContext(options);\n\n  // Phase 1: SSR build (vite build) — plugins populate ctx\n  await viteBuild({\n    plugins: [\n      lessCore(options, ctx),\n      lessContent({ ...options.content, ctx }),\n      lessI18n({ ...options.i18n, ctx }),\n    ],\n  });\n\n  // Phase 2: Client build — reads ctx\n  await buildClient(ctx);\n\n  // Phase 3: SSG render — reads ctx\n  await buildSSG(ctx);\n}\n</code></pre>\n<h4>A.6: <code>build-ssg.ts</code> read from <code>ctx</code> instead of <code>.less/</code> files</h4>\n<p><strong>File</strong>: <code>packages/core/src/cli/build-ssg.ts</code></p>\n<ul>\n<li>Accept <code>LessBuildContext</code> as parameter</li>\n<li>Replace all <code>readFileSync(join(root, &#39;.less&#39;, &#39;*.json&#39;))</code> with <code>ctx.*</code> reads</li>\n<li>Replace <code>.less-ssg-entry.ts</code> with <code>ctx.ssgEntryCode</code> (use virtual module or <code>define</code>)</li>\n<li>Replace <code>virtual:less-nav</code> file read with <code>ctx.navSections</code> / <code>ctx.headerNav</code></li>\n<li>Replace <code>.less/head-extras.html</code> file read with <code>ctx.headExtras</code> (inline via <code>define</code>)</li>\n<li>Keep <code>optionalPackageStubsPlugin()</code> — still needed for SSR build</li>\n</ul>\n<h4>A.7: <code>entry-renderer.ts</code> headExtras — eliminate <code>.less/head-extras.html</code> read</h4>\n<p><strong>File</strong>: <code>packages/core/src/entry-renderer.ts</code></p>\n<p>Currently SSG entry code does:</p>\n<pre><code class=\"language-typescript\">import { readFileSync } from &#39;node:fs&#39;;\nlet __headExtras = readFileSync(join(process.cwd(), &#39;.less&#39;, &#39;head-extras.html&#39;), &#39;utf-8&#39;);\n</code></pre>\n<p>Replace with <code>define</code> injection:</p>\n<pre><code class=\"language-typescript\">// In viteBuild config:\ndefine: {\n  __LESS_HEAD_EXTRAS__: JSON.stringify(options.headExtras);\n}\n\n// In generated entry code:\nconst __headExtras = __LESS_HEAD_EXTRAS__;\n</code></pre>\n<p>This eliminates the runtime file read AND the <code>.less/head-extras.html</code> file.</p>\n<h4>A.8: Clean up <code>.less/</code> directory creation</h4>\n<p>After A.1–A.7, search for all remaining <code>.less/</code> references and remove:</p>\n<ul>\n<li><code>mkdirSync(lessTmpDir, ...)</code> calls</li>\n<li><code>writeFileSync(join(lessTmpDir, ...))</code> calls</li>\n<li>Any <code>.less/</code> path references in comments/docs</li>\n</ul>\n<p><strong>Verification</strong>: <code>grep -rn &quot;\\.less&quot; packages/ --include=&quot;*.ts&quot;</code> returns 0 results.</p>\n<hr />\n<h3>Phase D: Replace <code>runtime-shim.ts</code> with <code>virtual:less-runtime</code></h3>\n<p><strong>Risk</strong>: LOW — mechanical replacement.</p>\n<h4>D.1: Create <code>virtual:less-runtime</code> Vite plugin</h4>\n<p><strong>File</strong>: <code>packages/core/src/index.ts</code> (inside <code>less()</code>)</p>\n<p>Replace the <code>less:core</code> config hook that writes <code>.less-runtime.ts</code> to disk:</p>\n<pre><code class=\"language-typescript\">// BEFORE (writes to .less/.less-runtime.ts):\nconfig() {\n  writeFileSync(runtimePath, createRuntimeShimCode(), &#39;utf-8&#39;);\n  return { resolve: { alias: { &#39;@openelement/core/less-runtime&#39;: runtimePath } } };\n}\n\n// AFTER (virtual module):\nconst VIRTUAL_RUNTIME_ID = &#39;virtual:less-runtime&#39;;\nconst RESOLVED_RUNTIME_ID = &#39;\\0&#39; + VIRTUAL_RUNTIME_ID;\n\n// In less:core plugin:\nresolveId(id) { if (id === VIRTUAL_RUNTIME_ID) return RESOLVED_RUNTIME_ID; }\nload(id) { if (id === RESOLVED_RUNTIME_ID) return createRuntimeShimCode(); }\n\n// In config() hook:\nreturn { resolve: { alias: { &#39;@openelement/core/less-runtime&#39;: VIRTUAL_RUNTIME_ID } } };\n</code></pre>\n<h4>D.2: Update <code>build-ssg.ts</code> SSR build to include <code>virtual:less-runtime</code> plugin</h4>\n<p>The SSG SSR build also needs to resolve <code>@openelement/core/less-runtime</code>. Add the same virtual module plugin to the SSR <code>viteBuild()</code> plugins array.</p>\n<h4>D.3: Remove <code>createRuntimeShimCode()</code> from <code>runtime-shim.ts</code></h4>\n<p>The function can stay (it generates the code), but it no longer writes to disk.</p>\n<p><strong>Verification</strong>: <code>grep -rn &quot;\\.less-runtime&quot; packages/ --include=&quot;*.ts&quot;</code> returns 0 file-path references (only import IDs remain).</p>\n<hr />\n<h3>Phase E: Single-Plugin API (<code>lessjs()</code>)</h3>\n<p><strong>Risk</strong>: LOW — pure API layer, internals unchanged.</p>\n<h4>E.1: Create <code>lessjs()</code> umbrella function</h4>\n<p><strong>File</strong>: <code>packages/core/src/index.ts</code> (new export)</p>\n<pre><code class=\"language-typescript\">export function lessjs(options: LessjsOptions = {}): Plugin[] {\n  const ctx = new LessBuildContext(resolvedCoreOptions);\n\n  return [\n    ...less(resolvedCoreOptions, ctx),\n    ...(options.content ? lessContent({ ...options.content, ctx }) : []),\n    ...(options.i18n ? lessI18n({ ...options.i18n, ctx }) : []),\n  ];\n}\n</code></pre>\n<p>Where <code>LessjsOptions</code>:</p>\n<pre><code class=\"language-typescript\">interface LessjsOptions extends FrameworkOptions {\n  content?: LessContentOptions;\n  i18n?: LessI18nOptions;\n}\n</code></pre>\n<h4>E.2: Update <code>less()</code>, <code>lessContent()</code>, <code>lessI18n()</code> to accept <code>ctx</code></h4>\n<p>Each function gets an optional <code>ctx</code> parameter. When called from <code>lessjs()</code>, the shared context is used. When called standalone (backward compat), each creates its own context.</p>\n<h4>E.3: Keep backward compatibility</h4>\n<p><code>less()</code>, <code>lessContent()</code>, <code>lessI18n()</code> remain exported and work independently. Users can still do:</p>\n<pre><code class=\"language-typescript\">plugins: [less(), lessContent({ blog: true })];\n</code></pre>\n<p>But <code>lessjs({ content: { blog: true } })</code> is the recommended path.</p>\n<h4>E.4: Update CLI to use unified <code>build</code> command</h4>\n<p>Replace the 3-script pipeline:</p>\n<pre><code class=\"language-bash\">deno task build        # runs: vite build + build:client + build:ssg\n</code></pre>\n<p>With a single <code>build</code> command that uses the orchestrator from A.5.</p>\n<hr />\n<h2>Execution Checklist</h2>\n<h3>Phase A (Eliminate .less/ IPC) — ✅ COMPLETE</h3>\n<ul>\n<li><input checked=\"\" disabled=\"\" type=\"checkbox\" /> A.1: Expand <code>LessBuildContext</code> with all <code>.less/</code> data fields</li>\n<li><input checked=\"\" disabled=\"\" type=\"checkbox\" /> A.2: <code>lessContent()</code> → write to <code>ctx</code> instead of <code>.less/</code> files</li>\n<li><input checked=\"\" disabled=\"\" type=\"checkbox\" /> A.3: <code>lessI18n()</code> → write to <code>ctx</code> instead of <code>.less/</code> files</li>\n<li><input checked=\"\" disabled=\"\" type=\"checkbox\" /> A.4: <code>less:build</code> → write metadata to <code>ctx</code> instead of <code>.less/build-metadata.json</code></li>\n<li><input checked=\"\" disabled=\"\" type=\"checkbox\" /> A.5: Create unified <code>build.ts</code> orchestrator (replaces 3-script pipeline)</li>\n<li><input checked=\"\" disabled=\"\" type=\"checkbox\" /> A.6: <code>build-ssg.ts</code> → read from <code>ctx</code> instead of <code>.less/</code> files</li>\n<li><input checked=\"\" disabled=\"\" type=\"checkbox\" /> A.7: <code>entry-renderer.ts</code> → <code>define</code> injection replaces <code>head-extras.html</code> file read</li>\n<li><input checked=\"\" disabled=\"\" type=\"checkbox\" /> A.8: Clean up all remaining <code>.less/</code> references</li>\n</ul>\n<h3>Phase D (Virtual less-runtime) — ✅ COMPLETE</h3>\n<ul>\n<li><input checked=\"\" disabled=\"\" type=\"checkbox\" /> D.1: Create <code>virtual:less-runtime</code> plugin in <code>less:core</code></li>\n<li><input checked=\"\" disabled=\"\" type=\"checkbox\" /> D.2: Add virtual:less-runtime to SSR build plugins</li>\n<li><input checked=\"\" disabled=\"\" type=\"checkbox\" /> D.3: Remove <code>.less-runtime.ts</code> file write</li>\n</ul>\n<h3>Phase E (Single-plugin API) — ✅ COMPLETE (extracted to @openelement/app per ADR 0012)</h3>\n<ul>\n<li><input checked=\"\" disabled=\"\" type=\"checkbox\" /> E.1: Create <code>lessjs()</code> umbrella function in <code>@openelement/app</code></li>\n<li><input checked=\"\" disabled=\"\" type=\"checkbox\" /> E.2: Update <code>less()</code>, <code>lessContent()</code>, <code>lessI18n()</code> to accept shared <code>ctx</code></li>\n<li><input checked=\"\" disabled=\"\" type=\"checkbox\" /> E.3: Verify backward compatibility (split-call with explicit <code>ctx</code> works)</li>\n<li><input checked=\"\" disabled=\"\" type=\"checkbox\" /> E.4: Update CLI to use unified build command (closeBundle inline per ADR 0011)</li>\n</ul>\n<hr />\n<h2>Commit Strategy</h2>\n<ul>\n<li><strong>One commit per checkbox</strong> (small, reviewable, revertable)</li>\n<li><strong>Commit message format</strong>: <code>refactor(core): A.1 — expand LessBuildContext with .less/ data fields</code></li>\n<li><strong>Run after each commit</strong>: <code>deno fmt &amp;&amp; deno lint &amp;&amp; deno check</code> + full test suite</li>\n<li><strong>Branch</strong>: all on <code>dev</code></li>\n</ul>\n<h2>Risk Mitigation</h2>\n<ol>\n<li><strong>Incremental commits</strong>: Each step is independently valid. If A.5 (unified orchestrator) proves complex, A.1–A.4 still eliminate <code>.less/</code> writes from plugins — the CLI can temporarily still read from a JSON dump.</li>\n<li><strong>Fallback</strong>: If unified build orchestrator is too risky, keep Phase 1 as <code>vite build</code> (writes one metadata JSON to <code>.less/</code>) and have Phase 2/3 read it. This is a partial win: 10 files → 1 file.</li>\n<li><strong>Testing</strong>: After each phase, run the full e2e build (<code>deno task build</code>) and verify output matches.</li>\n</ol>\n<h2>Definition of Done — ✅ ALL MET</h2>\n<ul>\n<li><code>grep -rn &quot;\\.less&quot; packages/ --include=&quot;*.ts&quot;</code> returns 0 results (excluding comments)</li>\n<li><code>grep -rn &quot;globalThis\\[&quot; packages/ --include=&quot;*.ts&quot;</code> returns 0 results</li>\n<li><code>grep -rn &quot;getActiveContext\\|setActiveContext\\|clearActiveContext&quot; packages/ --include=&quot;*.ts&quot;</code> returns 0 results</li>\n<li>User can write <code>plugins: [await lessjs({ content: { blog: true }, i18n: { locales: [&#39;en&#39;, &#39;zh&#39;] } })]</code> from <code>@openelement/app</code></li>\n<li><code>lessjs()</code> uses static imports, not dynamic <code>import()</code> + try/catch</li>\n<li>Build pipeline: single <code>viteBuild()</code> → <code>closeBundle()</code> inlines Phase 2/3</li>\n</ul>\n"
  },
  {
    "slug": "adr-0008-0009-implementation-status",
    "frontmatter": {
      "title": "ADR 0008/0009/0010/0011/0012 — Implementation Status",
      "date": "2026-05-11",
      "draft": false,
      "tags": [
        "architecture",
        "decision"
      ],
      "type": "adr",
      "lang": "en"
    },
    "content": "\n## Summary\n\nADR 0008 + 0009 + 0010 + 0011 + 0012 fully implemented on the `dev` branch.\n\n- All `.less/` temp files eliminated (ADR 0010)\n- All `globalThis[Symbol.for()]` bridges eliminated (ADR 0011 + 0012)\n- `lessjs()` unified entry extracted to `@openelement/app` (ADR 0012)\n- Build pipeline: single `viteBuild()` + closeBundle inline Phase 2/3 (ADR 0011)\n\n## Completed Phases\n\n### Phase A: Eliminate `.less/` File IPC → `LessBuildContext` + Virtual Modules + Define Injection\n\n| Step | Status | Commit    | Description                                               |\n| ---- | ------ | --------- | --------------------------------------------------------- |\n| A.1  | ✅     | `85f3131` | Expand `LessBuildContext` with 20+ fields                 |\n| A.2  | ✅     | `a1ab6ef` | `lessContent()` writes to `ctx` instead of `.less/` files |\n| A.3  | ✅     | `605c249` | `lessI18n()` writes to `ctx` instead of `.less/` files    |\n| A.4  | ✅     | `2e5e1aa` | `less:build` writes metadata to `ctx` + fallback          |\n| A.5  | ✅     | `eb965a1` | Unified `build.ts` orchestrator with shared `ctx`         |\n| A.6  | ✅     | `eb965a1` | `buildClient()`/`buildSSG()` read from `ctx` (preferred)  |\n| A.7  | ✅     | `0acd4b7` | `headExtras` via Vite `define` injection                  |\n| A.8  | ✅     | `eb965a1` | Cleaned up `.less/` references (see known limits)         |\n\n### Phase D: Replace `runtime-shim.ts` with `virtual:less-runtime`\n\n| Step    | Status | Commit    | Description                                                   |\n| ------- | ------ | --------- | ------------------------------------------------------------- |\n| D.1+D.3 | ✅     | `1bf6d6a` | `virtual:less-runtime` replaces `.less-runtime.ts` file write |\n| D.2     | ✅     | `52f9e11` | Virtual runtime plugin added to SSR build                     |\n\n### Phase E: Single-Plugin API (`lessjs()`) → Extracted to `@openelement/app`\n\n| Step | Status | Description                                                |\n| ---- | ------ | ---------------------------------------------------------- |\n| E.1  | ✅     | `lessjs()` umbrella function created in `@openelement/app`      |\n| E.2  | ✅     | `less()` accepts optional `externalCtx` parameter          |\n| E.3  | ✅     | Backward compat: split-call mode works with explicit `ctx` |\n| E.4  | ✅     | Unified `build` command via closeBundle (ADR 0011)         |\n\n### ADR 0011: Eliminate Last globalThis Bridge via closeBundle Inline\n\n| Step | Status | Description                                                 |\n| ---- | ------ | ----------------------------------------------------------- |\n| 1    | ✅     | Phase 2/3 inlined in closeBundle(), cli/build.ts simplified |\n| 2    | ✅     | globalThis write removed from less() in index.ts            |\n| 3    | ✅     | clearActiveContext() removed from build.ts                  |\n\n### ADR 0012: Extract lessjs() Umbrella to @openelement/app\n\n| Step | Status | Description                                                                              |\n| ---- | ------ | ---------------------------------------------------------------------------------------- |\n| 1    | ✅     | New `@openelement/app` package with static imports                                            |\n| 2    | ✅     | `lessjs()` removed from core/index.ts                                                    |\n| 3    | ✅     | `getActiveContext`/`setActiveContext`/`clearActiveContext` deleted from build-context.ts |\n| 4    | ✅     | content/i18n: `options.ctx                                                               |\n| 5    | ✅     | docs/vite.config.ts switched to `lessjs()` from `@openelement/app`                            |\n| 6    | ✅     | `LessContentOptions` exported from `@openelement/content`                                     |\n\n### ADR 0010: Eliminate All Remaining `.less/` Temp Files\n\n| Step | Status | Commit    | Description                                                  |\n| ---- | ------ | --------- | ------------------------------------------------------------ |\n| 1    | ✅     | `c9cbe61` | `virtual:less-client-entry` replaces `.less-client-entry.ts` |\n| 2    | ✅     | `c9cbe61` | `virtual:less-ssg-entry` replaces `.less-ssg-entry.ts`       |\n| 3    | ✅     | `c9cbe61` | Remove `build-metadata.json` file write from `build.ts`      |\n| 4    | ✅     | `c9cbe61` | Remove all `.less/` fallback reads from `build-ssg.ts`       |\n| 5    | ✅     | `c9cbe61` | Clean up unused imports, fix lint                            |\n\n## Key Results\n\n- **`.less/` files reduced**: 10 → 3 → **0** (zero filesystem IPC)\n- **`globalThis` bridges**: 4 → 1 → **0** (all deleted, ctx via explicit parameter only)\n- **Virtual modules**: `virtual:less-runtime`, `virtual:less-nav`, `virtual:less-client-entry`, `virtual:less-ssg-entry`\n- **`lessjs({ content, i18n })`**: Single-call API in `@openelement/app`, static imports, type-safe\n- **`buildClient()`/`buildSSG()` require `ctx`**: No standalone split-phase execution\n- **Build pipeline**: Single `viteBuild()` → `closeBundle()` inlines Phase 2/3\n- **All pre-commit checks pass** (deno fmt, deno lint, deno check)\n\n## No Remaining Work\n\nAll ADR 0008/0009/0010/0011/0012 tasks are complete.\n",
    "html": "<h2>Summary</h2>\n<p>ADR 0008 + 0009 + 0010 + 0011 + 0012 fully implemented on the <code>dev</code> branch.</p>\n<ul>\n<li>All <code>.less/</code> temp files eliminated (ADR 0010)</li>\n<li>All <code>globalThis[Symbol.for()]</code> bridges eliminated (ADR 0011 + 0012)</li>\n<li><code>lessjs()</code> unified entry extracted to <code>@openelement/app</code> (ADR 0012)</li>\n<li>Build pipeline: single <code>viteBuild()</code> + closeBundle inline Phase 2/3 (ADR 0011)</li>\n</ul>\n<h2>Completed Phases</h2>\n<h3>Phase A: Eliminate <code>.less/</code> File IPC → <code>LessBuildContext</code> + Virtual Modules + Define Injection</h3>\n<table>\n<thead>\n<tr>\n<th>Step</th>\n<th>Status</th>\n<th>Commit</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>A.1</td>\n<td>✅</td>\n<td><code>85f3131</code></td>\n<td>Expand <code>LessBuildContext</code> with 20+ fields</td>\n</tr>\n<tr>\n<td>A.2</td>\n<td>✅</td>\n<td><code>a1ab6ef</code></td>\n<td><code>lessContent()</code> writes to <code>ctx</code> instead of <code>.less/</code> files</td>\n</tr>\n<tr>\n<td>A.3</td>\n<td>✅</td>\n<td><code>605c249</code></td>\n<td><code>lessI18n()</code> writes to <code>ctx</code> instead of <code>.less/</code> files</td>\n</tr>\n<tr>\n<td>A.4</td>\n<td>✅</td>\n<td><code>2e5e1aa</code></td>\n<td><code>less:build</code> writes metadata to <code>ctx</code> + fallback</td>\n</tr>\n<tr>\n<td>A.5</td>\n<td>✅</td>\n<td><code>eb965a1</code></td>\n<td>Unified <code>build.ts</code> orchestrator with shared <code>ctx</code></td>\n</tr>\n<tr>\n<td>A.6</td>\n<td>✅</td>\n<td><code>eb965a1</code></td>\n<td><code>buildClient()</code>/<code>buildSSG()</code> read from <code>ctx</code> (preferred)</td>\n</tr>\n<tr>\n<td>A.7</td>\n<td>✅</td>\n<td><code>0acd4b7</code></td>\n<td><code>headExtras</code> via Vite <code>define</code> injection</td>\n</tr>\n<tr>\n<td>A.8</td>\n<td>✅</td>\n<td><code>eb965a1</code></td>\n<td>Cleaned up <code>.less/</code> references (see known limits)</td>\n</tr>\n</tbody></table>\n<h3>Phase D: Replace <code>runtime-shim.ts</code> with <code>virtual:less-runtime</code></h3>\n<table>\n<thead>\n<tr>\n<th>Step</th>\n<th>Status</th>\n<th>Commit</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>D.1+D.3</td>\n<td>✅</td>\n<td><code>1bf6d6a</code></td>\n<td><code>virtual:less-runtime</code> replaces <code>.less-runtime.ts</code> file write</td>\n</tr>\n<tr>\n<td>D.2</td>\n<td>✅</td>\n<td><code>52f9e11</code></td>\n<td>Virtual runtime plugin added to SSR build</td>\n</tr>\n</tbody></table>\n<h3>Phase E: Single-Plugin API (<code>lessjs()</code>) → Extracted to <code>@openelement/app</code></h3>\n<table>\n<thead>\n<tr>\n<th>Step</th>\n<th>Status</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>E.1</td>\n<td>✅</td>\n<td><code>lessjs()</code> umbrella function created in <code>@openelement/app</code></td>\n</tr>\n<tr>\n<td>E.2</td>\n<td>✅</td>\n<td><code>less()</code> accepts optional <code>externalCtx</code> parameter</td>\n</tr>\n<tr>\n<td>E.3</td>\n<td>✅</td>\n<td>Backward compat: split-call mode works with explicit <code>ctx</code></td>\n</tr>\n<tr>\n<td>E.4</td>\n<td>✅</td>\n<td>Unified <code>build</code> command via closeBundle (ADR 0011)</td>\n</tr>\n</tbody></table>\n<h3>ADR 0011: Eliminate Last globalThis Bridge via closeBundle Inline</h3>\n<table>\n<thead>\n<tr>\n<th>Step</th>\n<th>Status</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>1</td>\n<td>✅</td>\n<td>Phase 2/3 inlined in closeBundle(), cli/build.ts simplified</td>\n</tr>\n<tr>\n<td>2</td>\n<td>✅</td>\n<td>globalThis write removed from less() in index.ts</td>\n</tr>\n<tr>\n<td>3</td>\n<td>✅</td>\n<td>clearActiveContext() removed from build.ts</td>\n</tr>\n</tbody></table>\n<h3>ADR 0012: Extract lessjs() Umbrella to @openelement/app</h3>\n<table>\n<thead>\n<tr>\n<th>Step</th>\n<th>Status</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>1</td>\n<td>✅</td>\n<td>New <code>@openelement/app</code> package with static imports</td>\n</tr>\n<tr>\n<td>2</td>\n<td>✅</td>\n<td><code>lessjs()</code> removed from core/index.ts</td>\n</tr>\n<tr>\n<td>3</td>\n<td>✅</td>\n<td><code>getActiveContext</code>/<code>setActiveContext</code>/<code>clearActiveContext</code> deleted from build-context.ts</td>\n</tr>\n<tr>\n<td>4</td>\n<td>✅</td>\n<td>content/i18n: `options.ctx</td>\n</tr>\n<tr>\n<td>5</td>\n<td>✅</td>\n<td>docs/vite.config.ts switched to <code>lessjs()</code> from <code>@openelement/app</code></td>\n</tr>\n<tr>\n<td>6</td>\n<td>✅</td>\n<td><code>LessContentOptions</code> exported from <code>@openelement/content</code></td>\n</tr>\n</tbody></table>\n<h3>ADR 0010: Eliminate All Remaining <code>.less/</code> Temp Files</h3>\n<table>\n<thead>\n<tr>\n<th>Step</th>\n<th>Status</th>\n<th>Commit</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>1</td>\n<td>✅</td>\n<td><code>c9cbe61</code></td>\n<td><code>virtual:less-client-entry</code> replaces <code>.less-client-entry.ts</code></td>\n</tr>\n<tr>\n<td>2</td>\n<td>✅</td>\n<td><code>c9cbe61</code></td>\n<td><code>virtual:less-ssg-entry</code> replaces <code>.less-ssg-entry.ts</code></td>\n</tr>\n<tr>\n<td>3</td>\n<td>✅</td>\n<td><code>c9cbe61</code></td>\n<td>Remove <code>build-metadata.json</code> file write from <code>build.ts</code></td>\n</tr>\n<tr>\n<td>4</td>\n<td>✅</td>\n<td><code>c9cbe61</code></td>\n<td>Remove all <code>.less/</code> fallback reads from <code>build-ssg.ts</code></td>\n</tr>\n<tr>\n<td>5</td>\n<td>✅</td>\n<td><code>c9cbe61</code></td>\n<td>Clean up unused imports, fix lint</td>\n</tr>\n</tbody></table>\n<h2>Key Results</h2>\n<ul>\n<li><strong><code>.less/</code> files reduced</strong>: 10 → 3 → <strong>0</strong> (zero filesystem IPC)</li>\n<li><strong><code>globalThis</code> bridges</strong>: 4 → 1 → <strong>0</strong> (all deleted, ctx via explicit parameter only)</li>\n<li><strong>Virtual modules</strong>: <code>virtual:less-runtime</code>, <code>virtual:less-nav</code>, <code>virtual:less-client-entry</code>, <code>virtual:less-ssg-entry</code></li>\n<li><strong><code>lessjs({ content, i18n })</code></strong>: Single-call API in <code>@openelement/app</code>, static imports, type-safe</li>\n<li><strong><code>buildClient()</code>/<code>buildSSG()</code> require <code>ctx</code></strong>: No standalone split-phase execution</li>\n<li><strong>Build pipeline</strong>: Single <code>viteBuild()</code> → <code>closeBundle()</code> inlines Phase 2/3</li>\n<li><strong>All pre-commit checks pass</strong> (deno fmt, deno lint, deno check)</li>\n</ul>\n<h2>No Remaining Work</h2>\n<p>All ADR 0008/0009/0010/0011/0012 tasks are complete.</p>\n"
  },
  {
    "slug": "the-0-42-architecture",
    "frontmatter": {
      "title": "为什么是 0.42 这个样子：一个 WC 全栈框架的目标、参照与取舍",
      "date": "2026-08-02",
      "draft": false,
      "tags": [
        "architecture",
        "decision",
        "release-truth"
      ],
      "type": "dispatch",
      "lang": "zh"
    },
    "content": "\n每隔几年，前端就要换一轮框架，而上一轮写的组件跟着陪葬。0.42 立项时我们想回答的不是「怎么再造一个 Next」，而是一个更朴素的问题：**能不能造一个全栈框架，让组件沉淀在浏览器标准上，让页面协议轻到可以被证明？** 这篇文章讲这个目标如何从一连串参照与拒绝中长出现在的形状。\n\n## 总体目标\n\n三件事，按优先级排。\n\n第一，**组件必须是 Web Components**。不是编译目标，不是互操作层，是组件本身就是自定义元素——标准不过期、框架死了组件还在、任何宿主页都能直接用。这一条是不可谈判的，它是整个项目存在的理由。\n\n第二，**页面协议要轻到可证伪**。「支持 SSR」「支持渐进增强」这类话在任何框架的官网都能找到，且都无法验证。我们要的是每一条协议承诺都附带一个能跑给你看的证明——最好是 `javaScriptEnabled: false` 也能走通的那种。\n\n第三，**先赢一个滩头，而不是宣称一个帝国**。内容站加轻交互（文档、博客、营销页、几个表单）是真实且庞大的需求，但今天服务它的工具要么太老（纯 SSG，没有服务端能力），要么太重（为 todo list 付出应用框架的全部复杂度）。这个空隙就是 0.42 的目标场景，我们给它起的名字叫 light fullstack。\n\n## 参照一：React / RSC 路线——最成熟，所以最先被拒绝\n\n把 vdom、hydration、客户端数据层、RSC 搬过来，是人才储备和生态上最舒服的选择。但它的组件模型是框架私有的：vdom 是内部表示，浏览器永远不认识你的组件，认识的是框架运行时替你操作的结果。走这条路，Web Components 会退化成「可选的编译输出格式」——那直接违背总体目标第一条。这不是技术判断，是目标判断：**我们要的是组件活在标准里，不是活在某个运行时里。**\n\n## 参照二：Astro / Fresh——方向最近，所以只抄了一半\n\n静态优先、岛屿架构、服务端端点——这个方向和我们想要的几乎完全重合，0.42 的应用壳（app + adapter-vite）坦然承认欠它们的债。但有两样东西不能抄：Astro 的岛屿是 React/Vue/Svelte 组件，hydration 要在浏览器里重新执行整棵组件树；Fresh 的岛屿是 Preact，同样如此。我们要的岛屿是 WC，SSR 输出就是标准 DSD——浏览器原生解析 shadow tree，hydration 只是把 signal 绑定「接上」已有的 DOM，而不是把组件「重跑」一遍。所以是**取其形（静态优先 + 岛屿 + 端点），换其核（WC + DSD 原生）**。\n\n## 参照三：SolidJS——最心动的一集，也是我们自己反复争吵的一集\n\n既然 element 有 signals，为什么不干脆让 signal 驱动整个页面、做 real DOM 的节点级更新？JSX + signals + 无 vdom，这不就是 SolidJS 吗，一套机制，多优雅。这个方案在内部被认真推进过，最后被三个论证否决。\n\n**状态所有权。** 组件内部的状态（开关、草稿、播放进度）生在浏览器、死在浏览器，signal 管它是完美的。但页面级的数据，主人是服务端——loader 的数据在浏览器看不见的数据库里。让 signal 管页面，客户端就必须持有服务端数据的副本；而一旦有了副本，你就欠下缓存、失效策略、乐观更新与回滚、并发竞态、前进后退的状态恢复——「一套机制」是个幻觉，那套机制的名字叫客户端数据层，React 生态一半的复杂度住在里面。\n\n**总体目标第二条。** 页面更新如果依赖 signal 求值，无 JS 时页面就是死的，「可证伪的渐进增强」从根上就不成立。\n\n**缝的材质。** 这一点最隐蔽也最致命：如果页面级更新说 signal 的方言，那么页面里每个组件都必须会 signal——第三方 WC（不带 signal、不用我们 JSX 的那些）立刻出局。而「任何 WC 都能当页面组件」恰恰是洋葱缝上最值钱的性质。\n\n所以 signal 的疆域被刻意限定在组件内部——在那里它是全胜的。页面数据的更新交给了另一个血统的答案。\n\n## 参照四：htmx / Turbo / LiveView——页面更新的答案在这里，但零件要自研\n\n「HTML-over-the-wire」这一脉的核心洞察是：**服务器永远渲染完整 HTML，客户端只负责聪明地落地**。不需要客户端数据层，因为客户端从不持有数据。这条血脉从 morphdom 流经 idiomorph 流到 Turbo 的 morphing，思想直接可用——但零件一个都不能用，因为现成实现都不认识 shadow root，而我们的页面内容恰恰住在 DSD 的 shadow tree 里。所以 morph 客户端是自研的：按 `id` 对齐新旧两棵 DOM 树，下降进 shadow root，把 `<template shadowrootmode>` 当作传入的 shadow 内容，未被触及的岛屿连滚动位置都不动。它是 0.42 里最特别的零件，也是让「缝」在更新时依然成立的东西——它只认 DOM 和 id，不认任何组件的内部机制。\n\n顺带一提 Qwik 的 resumability：那是「signal 管全部」路线里做得最极致的，代价是复杂度全部转移到了序列化协议上，而且同样解决不了 DSD 场景。我们判断那条路的复杂度预算比 morph 高一个数量级。\n\n## 参照五：Lit / Enhance——血缘最近的两家，各自缺一半\n\nLit 证明了 WC 组件模型有人要，但它是组件库，SSR 靠实验性外挂，页面层不存在。Enhance 的哲学几乎和我们一样——WC SSR + 渐进增强 + 标准优先——但它的组件 authoring 不是 JSX + signals，也没有把「静态冻结线 + 零成本升级」做成一等契约的纪律。我们的赌注就是这个交集存在且空着：Astro 验证过的市场，Lit 验证过的组件模型，加上一层 Remix/htmx 血统的表单协议，用 DSD 原生和可证伪性做差异化。\n\n## 目标如何长出形状\n\n把总体目标和这些取舍放在一起，架构几乎是必然的结果：\n\n组件模型和页面协议必须分层，因为它们变化的原因不同（交互复杂度 vs 数据与部署），于是有了洋葱——`element` 不知道 `app` 存在，`app` 不知道 `adapter-vite` 存在。层间必须说所有组件都懂的普通话，于是缝是 W3C 标准而不是私有 API。页面数据的真理必须在服务端，于是 signal 守组件内、morph 管页面级。承诺必须可证伪，于是无 JS 基线是硬要求，每条协议规则必须挂在机器门禁上——这条规矩的来源并不光彩：alpha.3 的 morph 增强从来没工作过，当时的测试一直在无意识地测量无 JS 兜底路径，两轮审计才挖出来。从那以后，「任何协议规则不得只靠文字存在」成了这个仓库的第一纪律。\n\n## 代价与边界\n\n这套形状是有明确价格的：高频实时数据（行情、协作、聊天）不是它的菜，那种场景请用岛内 signal + WebSocket 这条逃逸舱；页面很大而改动很小时，整页 HTML 过线是一笔带宽税，这是 htmx 路线共同的税。0.42 也刻意没有 session/flash、cache/ISR、streaming、性能 SLO——登录应用今天走 better-auth recipe，框架级原语是 0.44 的事。「light」的含义就是这个冻结边界（ADR-0122）：不是做不到，是在地基被门禁证明之前，不许往上盖。\n\n## 记分牌，以及目标还没完成的部分\n\n六轮全仓审计、850+ 个 issue 全部关闭之后：每条协议规则有机器证明，CSRF 地板有真 deny/allow e2e，0.41.x 静态项目升级零改动。但总体目标其实只完成了前两件半——组件在标准上了，协议可证伪了，滩头打赢没有还无法知道，因为**唯一造不出来的证据是一个外部用户**。如果这个框架听起来像你的下一个站，alpha 线在 npm 的 `alpha` dist-tag 上。我们最想读到的报告，是你上手时卡住的那个瞬间。\n",
    "html": "<p>每隔几年，前端就要换一轮框架，而上一轮写的组件跟着陪葬。0.42 立项时我们想回答的不是「怎么再造一个 Next」，而是一个更朴素的问题：<strong>能不能造一个全栈框架，让组件沉淀在浏览器标准上，让页面协议轻到可以被证明？</strong> 这篇文章讲这个目标如何从一连串参照与拒绝中长出现在的形状。</p>\n<h2>总体目标</h2>\n<p>三件事，按优先级排。</p>\n<p>第一，<strong>组件必须是 Web Components</strong>。不是编译目标，不是互操作层，是组件本身就是自定义元素——标准不过期、框架死了组件还在、任何宿主页都能直接用。这一条是不可谈判的，它是整个项目存在的理由。</p>\n<p>第二，<strong>页面协议要轻到可证伪</strong>。「支持 SSR」「支持渐进增强」这类话在任何框架的官网都能找到，且都无法验证。我们要的是每一条协议承诺都附带一个能跑给你看的证明——最好是 <code>javaScriptEnabled: false</code> 也能走通的那种。</p>\n<p>第三，<strong>先赢一个滩头，而不是宣称一个帝国</strong>。内容站加轻交互（文档、博客、营销页、几个表单）是真实且庞大的需求，但今天服务它的工具要么太老（纯 SSG，没有服务端能力），要么太重（为 todo list 付出应用框架的全部复杂度）。这个空隙就是 0.42 的目标场景，我们给它起的名字叫 light fullstack。</p>\n<h2>参照一：React / RSC 路线——最成熟，所以最先被拒绝</h2>\n<p>把 vdom、hydration、客户端数据层、RSC 搬过来，是人才储备和生态上最舒服的选择。但它的组件模型是框架私有的：vdom 是内部表示，浏览器永远不认识你的组件，认识的是框架运行时替你操作的结果。走这条路，Web Components 会退化成「可选的编译输出格式」——那直接违背总体目标第一条。这不是技术判断，是目标判断：<strong>我们要的是组件活在标准里，不是活在某个运行时里。</strong></p>\n<h2>参照二：Astro / Fresh——方向最近，所以只抄了一半</h2>\n<p>静态优先、岛屿架构、服务端端点——这个方向和我们想要的几乎完全重合，0.42 的应用壳（app + adapter-vite）坦然承认欠它们的债。但有两样东西不能抄：Astro 的岛屿是 React/Vue/Svelte 组件，hydration 要在浏览器里重新执行整棵组件树；Fresh 的岛屿是 Preact，同样如此。我们要的岛屿是 WC，SSR 输出就是标准 DSD——浏览器原生解析 shadow tree，hydration 只是把 signal 绑定「接上」已有的 DOM，而不是把组件「重跑」一遍。所以是<strong>取其形（静态优先 + 岛屿 + 端点），换其核（WC + DSD 原生）</strong>。</p>\n<h2>参照三：SolidJS——最心动的一集，也是我们自己反复争吵的一集</h2>\n<p>既然 element 有 signals，为什么不干脆让 signal 驱动整个页面、做 real DOM 的节点级更新？JSX + signals + 无 vdom，这不就是 SolidJS 吗，一套机制，多优雅。这个方案在内部被认真推进过，最后被三个论证否决。</p>\n<p><strong>状态所有权。</strong> 组件内部的状态（开关、草稿、播放进度）生在浏览器、死在浏览器，signal 管它是完美的。但页面级的数据，主人是服务端——loader 的数据在浏览器看不见的数据库里。让 signal 管页面，客户端就必须持有服务端数据的副本；而一旦有了副本，你就欠下缓存、失效策略、乐观更新与回滚、并发竞态、前进后退的状态恢复——「一套机制」是个幻觉，那套机制的名字叫客户端数据层，React 生态一半的复杂度住在里面。</p>\n<p><strong>总体目标第二条。</strong> 页面更新如果依赖 signal 求值，无 JS 时页面就是死的，「可证伪的渐进增强」从根上就不成立。</p>\n<p><strong>缝的材质。</strong> 这一点最隐蔽也最致命：如果页面级更新说 signal 的方言，那么页面里每个组件都必须会 signal——第三方 WC（不带 signal、不用我们 JSX 的那些）立刻出局。而「任何 WC 都能当页面组件」恰恰是洋葱缝上最值钱的性质。</p>\n<p>所以 signal 的疆域被刻意限定在组件内部——在那里它是全胜的。页面数据的更新交给了另一个血统的答案。</p>\n<h2>参照四：htmx / Turbo / LiveView——页面更新的答案在这里，但零件要自研</h2>\n<p>「HTML-over-the-wire」这一脉的核心洞察是：<strong>服务器永远渲染完整 HTML，客户端只负责聪明地落地</strong>。不需要客户端数据层，因为客户端从不持有数据。这条血脉从 morphdom 流经 idiomorph 流到 Turbo 的 morphing，思想直接可用——但零件一个都不能用，因为现成实现都不认识 shadow root，而我们的页面内容恰恰住在 DSD 的 shadow tree 里。所以 morph 客户端是自研的：按 <code>id</code> 对齐新旧两棵 DOM 树，下降进 shadow root，把 <code>&lt;template shadowrootmode&gt;</code> 当作传入的 shadow 内容，未被触及的岛屿连滚动位置都不动。它是 0.42 里最特别的零件，也是让「缝」在更新时依然成立的东西——它只认 DOM 和 id，不认任何组件的内部机制。</p>\n<p>顺带一提 Qwik 的 resumability：那是「signal 管全部」路线里做得最极致的，代价是复杂度全部转移到了序列化协议上，而且同样解决不了 DSD 场景。我们判断那条路的复杂度预算比 morph 高一个数量级。</p>\n<h2>参照五：Lit / Enhance——血缘最近的两家，各自缺一半</h2>\n<p>Lit 证明了 WC 组件模型有人要，但它是组件库，SSR 靠实验性外挂，页面层不存在。Enhance 的哲学几乎和我们一样——WC SSR + 渐进增强 + 标准优先——但它的组件 authoring 不是 JSX + signals，也没有把「静态冻结线 + 零成本升级」做成一等契约的纪律。我们的赌注就是这个交集存在且空着：Astro 验证过的市场，Lit 验证过的组件模型，加上一层 Remix/htmx 血统的表单协议，用 DSD 原生和可证伪性做差异化。</p>\n<h2>目标如何长出形状</h2>\n<p>把总体目标和这些取舍放在一起，架构几乎是必然的结果：</p>\n<p>组件模型和页面协议必须分层，因为它们变化的原因不同（交互复杂度 vs 数据与部署），于是有了洋葱——<code>element</code> 不知道 <code>app</code> 存在，<code>app</code> 不知道 <code>adapter-vite</code> 存在。层间必须说所有组件都懂的普通话，于是缝是 W3C 标准而不是私有 API。页面数据的真理必须在服务端，于是 signal 守组件内、morph 管页面级。承诺必须可证伪，于是无 JS 基线是硬要求，每条协议规则必须挂在机器门禁上——这条规矩的来源并不光彩：alpha.3 的 morph 增强从来没工作过，当时的测试一直在无意识地测量无 JS 兜底路径，两轮审计才挖出来。从那以后，「任何协议规则不得只靠文字存在」成了这个仓库的第一纪律。</p>\n<h2>代价与边界</h2>\n<p>这套形状是有明确价格的：高频实时数据（行情、协作、聊天）不是它的菜，那种场景请用岛内 signal + WebSocket 这条逃逸舱；页面很大而改动很小时，整页 HTML 过线是一笔带宽税，这是 htmx 路线共同的税。0.42 也刻意没有 session/flash、cache/ISR、streaming、性能 SLO——登录应用今天走 better-auth recipe，框架级原语是 0.44 的事。「light」的含义就是这个冻结边界（ADR-0122）：不是做不到，是在地基被门禁证明之前，不许往上盖。</p>\n<h2>记分牌，以及目标还没完成的部分</h2>\n<p>六轮全仓审计、850+ 个 issue 全部关闭之后：每条协议规则有机器证明，CSRF 地板有真 deny/allow e2e，0.41.x 静态项目升级零改动。但总体目标其实只完成了前两件半——组件在标准上了，协议可证伪了，滩头打赢没有还无法知道，因为<strong>唯一造不出来的证据是一个外部用户</strong>。如果这个框架听起来像你的下一个站，alpha 线在 npm 的 <code>alpha</code> dist-tag 上。我们最想读到的报告，是你上手时卡住的那个瞬间。</p>\n"
  },
  {
    "slug": "v0-14-2",
    "frontmatter": {
      "title": "v0.14.2 - Standards & Safety Patch",
      "date": "2026-05-14",
      "draft": false,
      "tags": [
        "release",
        "standards",
        "security",
        "dsd",
        "ssg"
      ],
      "type": "release",
      "lang": "en"
    },
    "content": "\n# v0.14.2 - Standards & Safety Patch\n\nv0.14.2 ships the first implementation slice from ADR 0024. It keeps the scope\nnarrow: standards correctness, raw head-injection safety, dynamic SSG route path\nsafety, and stronger self-hosted docs build coverage.\n\n## What changed\n\n- DSD output now includes `shadowrootclonable` and serializes\n  `shadowrootcustomelementregistry` as a boolean content attribute.\n- `headExtras` and `inject.headFragments` now reject `<script>` tags. Scripts\n  should go through structured `inject.scripts`, where LessJS validates and\n  escapes script URLs.\n- Dynamic SSG route params are resolved as single URL path segments and encoded\n  with `encodeURIComponent()`. Unsafe path traversal values are skipped.\n- The docs smoke test now imports the real SSR server bundle, calls\n  `renderRoute('/roadmap')`, and verifies roadmap, i18n, content, ADR, UI island,\n  PWA, and DSD output.\n- The roadmap and package metadata now identify v0.14.2 as the current release.\n\n## Verification\n\n- `deno test --allow-read --allow-write --allow-env --allow-net --allow-run packages/core/__tests__/render-dsd.test.ts packages/adapter-vite/__tests__/index-plugin.test.ts packages/adapter-vite/__tests__/ssg-render.test.ts packages/adapter-vite/__tests__/ssg-smoke.test.ts`\n- `deno task typecheck`\n- `deno task build`\n",
    "html": "<h1>v0.14.2 - Standards &amp; Safety Patch</h1>\n<p>v0.14.2 ships the first implementation slice from ADR 0024. It keeps the scope\nnarrow: standards correctness, raw head-injection safety, dynamic SSG route path\nsafety, and stronger self-hosted docs build coverage.</p>\n<h2>What changed</h2>\n<ul>\n<li>DSD output now includes <code>shadowrootclonable</code> and serializes\n<code>shadowrootcustomelementregistry</code> as a boolean content attribute.</li>\n<li><code>headExtras</code> and <code>inject.headFragments</code> now reject <code>&lt;script&gt;</code> tags. Scripts\nshould go through structured <code>inject.scripts</code>, where LessJS validates and\nescapes script URLs.</li>\n<li>Dynamic SSG route params are resolved as single URL path segments and encoded\nwith <code>encodeURIComponent()</code>. Unsafe path traversal values are skipped.</li>\n<li>The docs smoke test now imports the real SSR server bundle, calls\n<code>renderRoute(&#39;/roadmap&#39;)</code>, and verifies roadmap, i18n, content, ADR, UI island,\nPWA, and DSD output.</li>\n<li>The roadmap and package metadata now identify v0.14.2 as the current release.</li>\n</ul>\n<h2>Verification</h2>\n<ul>\n<li><code>deno test --allow-read --allow-write --allow-env --allow-net --allow-run packages/core/__tests__/render-dsd.test.ts packages/adapter-vite/__tests__/index-plugin.test.ts packages/adapter-vite/__tests__/ssg-render.test.ts packages/adapter-vite/__tests__/ssg-smoke.test.ts</code></li>\n<li><code>deno task typecheck</code></li>\n<li><code>deno task build</code></li>\n</ul>\n"
  },
  {
    "slug": "v0-14-1",
    "frontmatter": {
      "title": "v0.14.1 — Release Hardening",
      "date": "2026-05-14",
      "draft": false,
      "tags": [
        "release",
        "hardening",
        "v0.14"
      ],
      "type": "post",
      "lang": "en"
    },
    "content": "\n# v0.14.1 — Release Hardening\n\nv0.14.1 is a focused hardening release. It does not add a new architectural direction or ADR; it tightens the path from scaffold to publish.\n\n## What changed\n\n- All packages now publish as `0.14.1`.\n- The signals package name is unified as `@openelement/signal`.\n- `@openelement/adapter-vite` exposes CLI subpaths for `build`, `build-client`, and `build-ssg`.\n- `@openelement/create` generates versioned build tasks that target the public adapter-vite CLI exports.\n- JSR publish workflows reject dirty worktrees and no longer use `--allow-dirty`.\n- Local quality tasks and CI now run the same lint, format, typecheck, test, build, and E2E paths.\n- Playwright E2E uses an isolated server port instead of reusing any existing local server.\n- SSG artifacts are more stable: locale root paths are normalized, the service worker cache name is deterministic, and sidecar import maps use current package metadata.\n\n## Safety boundary\n\nLessJS content plugins and head injection APIs assume developer-controlled repository content. Markdown HTML, `headExtras`, and `headFragments` are not sanitizers. If an application accepts user-generated HTML or remote CMS content, sanitize that content before passing it to LessJS.\n\n## Stable API candidates\n\nThe current stable API candidates are `lessjs()` options, public `@openelement/core` exports, `@openelement/adapter-vite` CLI subpaths, `@openelement/rpc`, `@openelement/signal`, and `@openelement/ui` component subpaths.\n",
    "html": "<h1>v0.14.1 — Release Hardening</h1>\n<p>v0.14.1 is a focused hardening release. It does not add a new architectural direction or ADR; it tightens the path from scaffold to publish.</p>\n<h2>What changed</h2>\n<ul>\n<li>All packages now publish as <code>0.14.1</code>.</li>\n<li>The signals package name is unified as <code>@openelement/signal</code>.</li>\n<li><code>@openelement/adapter-vite</code> exposes CLI subpaths for <code>build</code>, <code>build-client</code>, and <code>build-ssg</code>.</li>\n<li><code>@openelement/create</code> generates versioned build tasks that target the public adapter-vite CLI exports.</li>\n<li>JSR publish workflows reject dirty worktrees and no longer use <code>--allow-dirty</code>.</li>\n<li>Local quality tasks and CI now run the same lint, format, typecheck, test, build, and E2E paths.</li>\n<li>Playwright E2E uses an isolated server port instead of reusing any existing local server.</li>\n<li>SSG artifacts are more stable: locale root paths are normalized, the service worker cache name is deterministic, and sidecar import maps use current package metadata.</li>\n</ul>\n<h2>Safety boundary</h2>\n<p>LessJS content plugins and head injection APIs assume developer-controlled repository content. Markdown HTML, <code>headExtras</code>, and <code>headFragments</code> are not sanitizers. If an application accepts user-generated HTML or remote CMS content, sanitize that content before passing it to LessJS.</p>\n<h2>Stable API candidates</h2>\n<p>The current stable API candidates are <code>lessjs()</code> options, public <code>@openelement/core</code> exports, <code>@openelement/adapter-vite</code> CLI subpaths, <code>@openelement/rpc</code>, <code>@openelement/signal</code>, and <code>@openelement/ui</code> component subpaths.</p>\n"
  },
  {
    "slug": "v0.14-audit-report",
    "frontmatter": {
      "title": "v0.14.0 — Release Readiness Audit Report",
      "date": "2026-05-13",
      "draft": false,
      "tags": [
        "release",
        "audit",
        "v0.14"
      ],
      "type": "post",
      "lang": "zh"
    },
    "content": "\n# v0.14.0 Release Readiness Audit Report\n\n> 全团队审计结果 · 2026-05-13\n\n## Summary\n\n| Metric | Value |\n|--------|-------|\n| Source files | 121 `.ts` across 10 packages |\n| Test files | 44 (36% src/test ratio) |\n| Lint errors | **0** ✅ |\n| CI workflows | 5 (lint, test, publish x2, deploy-api) |\n| README coverage | 10/10 packages ✅ |\n| Stale root files | Cleaned ✅ |\n\n---\n\n## 1. 产品经理 · 文档审计\n\n### ✅ 通过\n\n- 25 ADRs 全部迁入 blog 管线，功能正常\n- 10 个包 README 全部存在且已更新到 v0.14\n- 根目录已清理（删除 v0.11-release-notes.md, overview.md, deliverables/）\n- `CHANGELOG-v0.14.0.md` 中英双语已放在 Claw 根目录\n\n### ⚠️ 待改进\n\n| 问题 | 文件 | 建议 |\n|------|------|------|\n| 新版博客博文无 frontmatter date 验证 | `2026-05-13-architecture-analysis.md` | 添加 type 和 tags |\n| ADR 列表页/decisions 路由仍有冗余 | `www/app/routes/decisions/` | 考虑统一到 `/blog/` 路径（向后兼容可选） |\n\n---\n\n## 2. 架构师 · 架构审计\n\n### ✅ 通过\n\n- Phase 1→3→2→Inject 新顺序已实现并推送到 dev\n- `ssg-render.ts` 零 Vite 依赖 ✅\n- `importmap.json` 随 SSR bundle 产出 ✅\n- 独立 SSG CLI (`cli/ssg.ts`) ✅\n- 10 包版本统一 0.14.0 ✅\n- 跨包依赖引用全部更新为 `@^0.14` ✅\n\n### ⚠️ 观察\n\n| 问题 | 位置 | 建议 |\n|------|------|------|\n| Phase 1b 仍用 viteBuild | `build-ssg.ts` | ADR 22 提到换 esbuild，优先级 P2 |\n| `@hono/vite-dev-server` 非 optional | `adapter-vite/deno.json` | 标记为 optional dep（P1） |\n| `BuildSSGOptions` 未统一 | `build-ssg.ts` | ADR 22 P1 项 |\n| `html-escape.ts` 中 `wrapInDocument` 和 `renderSsrError` 有部分重复 | `core/src/html-escape.ts` | 考虑合并 |\n\n---\n\n## 3. 工程师 · 代码质量审计\n\n### ✅ 通过\n\n- Lint: **0 errors** ✅\n- TypeScript strict mode 兼容\n- Deno workspace 配置正确（10 成员匹配）\n- 无 `.bak`、`.old`、TODO/FIXME 残留文件\n\n### ⚠️ 待改进\n\n| 问题 | 位置 | 建议 |\n|------|------|------|\n| i18n 包测试覆盖率低 | `packages/i18n/` | 5 src / 1 test — 需增加 |\n| ui 包测试覆盖率低 | `packages/ui/` | 15 src / 2 test — 需增加 |\n| content 包测试覆盖率低 | `packages/content/` | 11 src / 4 test — 需增加 |\n| `rpc` 包无 src 导出函数 | `packages/rpc/src/` | 只有 index.ts — 确认是否需要 |\n| 部分 ssg-render.ts 中的 `node:*` 导入 | `ssg-render.ts` | 允许（SSG 非浏览器端运行） |\n\n---\n\n## 4. QA · 测试审计\n\n### ✅ 通过\n\n- Core: 61 tests ✅ (13 src / 8 test)\n- Adapter-vite: 关键测试通过 (build-context, entry-generators)\n- Adapter-lit: 测试存在\n- Signals: 8 test files (覆盖较好)\n- CI: `test.yml` 存在，运行 `deno test`\n\n### ⚠️ 待改进\n\n| 测试套件 | 状态 | 详情 |\n|---------|------|------|\n| `build-manifest.test.ts` | ❌ 部分失败 | 需 build 产物预置 — 环境依赖 |\n| `ssg-smoke.test.ts` | ❌ 部分失败 | 同上 |\n| `index-plugin.test.ts` | ❌ 部分失败 | 同上 |\n| 新版 ssg-render.ts | ⬜ 0 test | 需添加单元测试 |\n| 新版 ssg.ts (CLI) | ⬜ 0 test | 需添加集成测试 |\n\n---\n\n## 5. CI/CD 审计\n\n| Workflow | 状态 |\n|----------|------|\n| `lint.yml` | ✅ Deno lint + fmt |\n| `test.yml` | ✅ deno test (all) |\n| `publish.yml` | ✅ 自动发布到 JSR |\n| `publish-manual.yml` | ✅ 手动触发发布 |\n| `deploy-api.yml` | ✅ Cloudflare Pages 部署 |\n\n### 发现\n\n- CI workflows 完整，覆盖 lint → test → publish → deploy\n- 无过时 workflow 文件\n- JSR publish 配置正确（每个包有 deno.json publish.include）\n\n---\n\n## 审计结论\n\n### 可发布（v0.14.0）\n\n当前仓库状态**可发布**。无阻塞性问题。\n\n### 建议立即做\n\n- 为 `ssg-render.ts` 和 `ssg.ts` 添加单元测试（~1d）\n- 补 i18n/ui/content 测试（~2d）\n- 清理 decisions 路由冗余（~0.5d）\n\n### 建议推迟\n\n- esbuild 替换 viteBuild（P2）\n- BuildSSGOptions 统一（P1）\n- `@hono/vite-dev-server` optional dep（P1）\n- html-escape 函数合并（P3）\n",
    "html": "<h1>v0.14.0 Release Readiness Audit Report</h1>\n<blockquote>\n<p>全团队审计结果 · 2026-05-13</p>\n</blockquote>\n<h2>Summary</h2>\n<table>\n<thead>\n<tr>\n<th>Metric</th>\n<th>Value</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Source files</td>\n<td>121 <code>.ts</code> across 10 packages</td>\n</tr>\n<tr>\n<td>Test files</td>\n<td>44 (36% src/test ratio)</td>\n</tr>\n<tr>\n<td>Lint errors</td>\n<td><strong>0</strong> ✅</td>\n</tr>\n<tr>\n<td>CI workflows</td>\n<td>5 (lint, test, publish x2, deploy-api)</td>\n</tr>\n<tr>\n<td>README coverage</td>\n<td>10/10 packages ✅</td>\n</tr>\n<tr>\n<td>Stale root files</td>\n<td>Cleaned ✅</td>\n</tr>\n</tbody></table>\n<hr />\n<h2>1. 产品经理 · 文档审计</h2>\n<h3>✅ 通过</h3>\n<ul>\n<li>25 ADRs 全部迁入 blog 管线，功能正常</li>\n<li>10 个包 README 全部存在且已更新到 v0.14</li>\n<li>根目录已清理（删除 v0.11-release-notes.md, overview.md, deliverables/）</li>\n<li><code>CHANGELOG-v0.14.0.md</code> 中英双语已放在 Claw 根目录</li>\n</ul>\n<h3>⚠️ 待改进</h3>\n<table>\n<thead>\n<tr>\n<th>问题</th>\n<th>文件</th>\n<th>建议</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>新版博客博文无 frontmatter date 验证</td>\n<td><code>2026-05-13-architecture-analysis.md</code></td>\n<td>添加 type 和 tags</td>\n</tr>\n<tr>\n<td>ADR 列表页/decisions 路由仍有冗余</td>\n<td><code>www/app/routes/decisions/</code></td>\n<td>考虑统一到 <code>/blog/</code> 路径（向后兼容可选）</td>\n</tr>\n</tbody></table>\n<hr />\n<h2>2. 架构师 · 架构审计</h2>\n<h3>✅ 通过</h3>\n<ul>\n<li>Phase 1→3→2→Inject 新顺序已实现并推送到 dev</li>\n<li><code>ssg-render.ts</code> 零 Vite 依赖 ✅</li>\n<li><code>importmap.json</code> 随 SSR bundle 产出 ✅</li>\n<li>独立 SSG CLI (<code>cli/ssg.ts</code>) ✅</li>\n<li>10 包版本统一 0.14.0 ✅</li>\n<li>跨包依赖引用全部更新为 <code>@^0.14</code> ✅</li>\n</ul>\n<h3>⚠️ 观察</h3>\n<table>\n<thead>\n<tr>\n<th>问题</th>\n<th>位置</th>\n<th>建议</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Phase 1b 仍用 viteBuild</td>\n<td><code>build-ssg.ts</code></td>\n<td>ADR 22 提到换 esbuild，优先级 P2</td>\n</tr>\n<tr>\n<td><code>@hono/vite-dev-server</code> 非 optional</td>\n<td><code>adapter-vite/deno.json</code></td>\n<td>标记为 optional dep（P1）</td>\n</tr>\n<tr>\n<td><code>BuildSSGOptions</code> 未统一</td>\n<td><code>build-ssg.ts</code></td>\n<td>ADR 22 P1 项</td>\n</tr>\n<tr>\n<td><code>html-escape.ts</code> 中 <code>wrapInDocument</code> 和 <code>renderSsrError</code> 有部分重复</td>\n<td><code>core/src/html-escape.ts</code></td>\n<td>考虑合并</td>\n</tr>\n</tbody></table>\n<hr />\n<h2>3. 工程师 · 代码质量审计</h2>\n<h3>✅ 通过</h3>\n<ul>\n<li>Lint: <strong>0 errors</strong> ✅</li>\n<li>TypeScript strict mode 兼容</li>\n<li>Deno workspace 配置正确（10 成员匹配）</li>\n<li>无 <code>.bak</code>、<code>.old</code>、TODO/FIXME 残留文件</li>\n</ul>\n<h3>⚠️ 待改进</h3>\n<table>\n<thead>\n<tr>\n<th>问题</th>\n<th>位置</th>\n<th>建议</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>i18n 包测试覆盖率低</td>\n<td><code>packages/i18n/</code></td>\n<td>5 src / 1 test — 需增加</td>\n</tr>\n<tr>\n<td>ui 包测试覆盖率低</td>\n<td><code>packages/ui/</code></td>\n<td>15 src / 2 test — 需增加</td>\n</tr>\n<tr>\n<td>content 包测试覆盖率低</td>\n<td><code>packages/content/</code></td>\n<td>11 src / 4 test — 需增加</td>\n</tr>\n<tr>\n<td><code>rpc</code> 包无 src 导出函数</td>\n<td><code>packages/rpc/src/</code></td>\n<td>只有 index.ts — 确认是否需要</td>\n</tr>\n<tr>\n<td>部分 ssg-render.ts 中的 <code>node:*</code> 导入</td>\n<td><code>ssg-render.ts</code></td>\n<td>允许（SSG 非浏览器端运行）</td>\n</tr>\n</tbody></table>\n<hr />\n<h2>4. QA · 测试审计</h2>\n<h3>✅ 通过</h3>\n<ul>\n<li>Core: 61 tests ✅ (13 src / 8 test)</li>\n<li>Adapter-vite: 关键测试通过 (build-context, entry-generators)</li>\n<li>Adapter-lit: 测试存在</li>\n<li>Signals: 8 test files (覆盖较好)</li>\n<li>CI: <code>test.yml</code> 存在，运行 <code>deno test</code></li>\n</ul>\n<h3>⚠️ 待改进</h3>\n<table>\n<thead>\n<tr>\n<th>测试套件</th>\n<th>状态</th>\n<th>详情</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>build-manifest.test.ts</code></td>\n<td>❌ 部分失败</td>\n<td>需 build 产物预置 — 环境依赖</td>\n</tr>\n<tr>\n<td><code>ssg-smoke.test.ts</code></td>\n<td>❌ 部分失败</td>\n<td>同上</td>\n</tr>\n<tr>\n<td><code>index-plugin.test.ts</code></td>\n<td>❌ 部分失败</td>\n<td>同上</td>\n</tr>\n<tr>\n<td>新版 ssg-render.ts</td>\n<td>⬜ 0 test</td>\n<td>需添加单元测试</td>\n</tr>\n<tr>\n<td>新版 ssg.ts (CLI)</td>\n<td>⬜ 0 test</td>\n<td>需添加集成测试</td>\n</tr>\n</tbody></table>\n<hr />\n<h2>5. CI/CD 审计</h2>\n<table>\n<thead>\n<tr>\n<th>Workflow</th>\n<th>状态</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>lint.yml</code></td>\n<td>✅ Deno lint + fmt</td>\n</tr>\n<tr>\n<td><code>test.yml</code></td>\n<td>✅ deno test (all)</td>\n</tr>\n<tr>\n<td><code>publish.yml</code></td>\n<td>✅ 自动发布到 JSR</td>\n</tr>\n<tr>\n<td><code>publish-manual.yml</code></td>\n<td>✅ 手动触发发布</td>\n</tr>\n<tr>\n<td><code>deploy-api.yml</code></td>\n<td>✅ Cloudflare Pages 部署</td>\n</tr>\n</tbody></table>\n<h3>发现</h3>\n<ul>\n<li>CI workflows 完整，覆盖 lint → test → publish → deploy</li>\n<li>无过时 workflow 文件</li>\n<li>JSR publish 配置正确（每个包有 deno.json publish.include）</li>\n</ul>\n<hr />\n<h2>审计结论</h2>\n<h3>可发布（v0.14.0）</h3>\n<p>当前仓库状态<strong>可发布</strong>。无阻塞性问题。</p>\n<h3>建议立即做</h3>\n<ul>\n<li>为 <code>ssg-render.ts</code> 和 <code>ssg.ts</code> 添加单元测试（~1d）</li>\n<li>补 i18n/ui/content 测试（~2d）</li>\n<li>清理 decisions 路由冗余（~0.5d）</li>\n</ul>\n<h3>建议推迟</h3>\n<ul>\n<li>esbuild 替换 viteBuild（P2）</li>\n<li>BuildSSGOptions 统一（P1）</li>\n<li><code>@hono/vite-dev-server</code> optional dep（P1）</li>\n<li>html-escape 函数合并（P3）</li>\n</ul>\n"
  },
  {
    "slug": "architecture-analysis",
    "frontmatter": {
      "title": "LessJS 架构分析报告",
      "date": "2026-05-13",
      "draft": false,
      "tags": [
        "architecture",
        "analysis"
      ],
      "type": "post",
      "lang": "zh"
    },
    "content": "\n# LessJS 架构分析报告\n\n> 日期：2026-05-13\n> 目标：E S M 原生 + DSD 驱动 + Vite 可选的 SSG 框架可行性分析\n\n---\n\n## 一、核心三问：DSD 能去依赖吗？ESM SSG 可行吗？项目能更干净吗？\n\n### 1.1 DSD 已经零依赖\n\n`packages/core/src/render-dsd.ts` 的依赖链：\n\n```\nrender-dsd.ts\n  ├── html-escape.ts           ← core 内部（纯函数，零依赖）\n  ├── types.ts                 ← core 内部（interface 定义）\n  ├── adapter-registry.ts      ← core 内部（单例 Map）\n  ├── render-nested.ts         ← core 内部（递归 DSD）\n  └── logger.ts                ← core 内部（console wrapper）\n```\n\n**没有 Lit，没有 Vite，没有 node:*，没有 npm: 前缀。**  \n所有 DSD 渲染是纯字符串拼接 + `async/await`。用户组件只要写 `render(): string`，就不需要 Lit。\n\nLit 只通过 `@openelement/adapter-lit` 的 `installLitAdapter()` 注入——用户主动调用才激活。如果用原生 Web Components（`render(): string`）或者 Lit 之外的框架，整套管线不需要 Lit。\n\n**结论：DSD 已经做到了你想要的——核心依赖为零，Lit 可选。**\n\n### 1.2 ESM SSG 可行，且解决真实摩擦\n\n当前 Phase 3 的流程：\n\n```\ncloseBundle (Vite plugin)\n  → viteBuild(ssr:true, noExternal:true)     ← 用 Vite 产出一个自包含的 SSR bundle\n  → register SSR bundle from disk             ← 用 import() 加载\n  → render all pages                           ← 纯 DSD 渲染\n  → write HTML files                           ← 纯文件写入\n```\n\n**第 1 步和第 2 步耦合了 Vite**。改成 ESM 模式的关键变化：\n\n```\n当前（Vite 强耦合）                      ESM 模式（Vite 可选）\n─────────────────                      ─────────────────\nPhase 1:                                  Phase 1:\n  scanRoutes()  ← 纯 TS，无 Vite             scanRoutes()  ← 不变\n  genHonoEntry() ← 纯字符串拼接              genHonoEntry() ← 不变\n  viteBuild(ssr, noExternal) ↓              esbuild --format=esm ↓\n  → ssr/server/entry.js                      → ssr/server/entry.mjs\n                                            + ssr/importmap.json   ← NEW\n\nPhase 2:                                  Phase 2:\n  viteBuild(client islands)                  viteBuild(client islands)\n  ← 保留 Vite/esbuild/rolldown               ← 保留，仅打包时用\n\nPhase 3:                                  Phase 3:\n  register('./dist/server/entry.js')         register('./dist/server/entry.mjs')\n  renderAll(routes)                          renderAll(routes)  \n  ← 在 closeBundle 里跑                       ← 独立 CLI，不依赖 Vite\n```\n\n**新增的 `importmap.json`**：\n\n```json\n// ssr/importmap.json — Phase 1 生成 SSR bundle 时自动产出\n{\n  \"imports\": {\n    \"hono\": \"https://jsr.io/@hono/hono/4.7.0/mod.ts\",\n    \"lit\": \"npm:lit@3.3.2\",\n    \"@openelement/core\": \"npm:@jsr/lessjs__core@0.13.0\",\n    \"@openelement/core/logger\": \"npm:@jsr/lessjs__core@0.13.0/logger\",\n    \"@openelement/adapter-lit\": \"npm:@jsr/lessjs__adapter-lit@0.8.0\"\n  }\n}\n```\n\n加载方式（任意 runtime）：\n\n```js\n// Node.js / Deno / Bun — 都行\nconst ssrBundle = await import('./dist/server/entry.mjs');\n// 配合 importmap 解析\nconst routes = ssrBundle.getRoutes();\nfor (const route of routes) {\n  const html = await ssrBundle.render(route);\n  writeFileSync(`dist/${route.path}.html`, html);\n}\n```\n\n**为什么这解决了 `npm:` vs `jsr:` 的坑？**  \n因为 SSR bundle 不再依赖\"构建环境的隐式 import map\"，而是**自己带一份明文 import map**。不管用户用 `jsr:@openelement/app` 还是 `npm:@jsr/lessjs__app` 还是 workspace，Phase 3 都读这份 sidecar import map 来解析 bare specifier。\n\n### 1.3 项目能更干净吗？能，但不碰伤筋动骨的东西\n\n当前项目的脏点：\n\n| 脏点 | 描述 | 改的成本 |\n|------|------|---------|\n| `closeBundle` 里塞了整个 Phase 3 | SSG 逻辑不该是 Vite 插件的一个 hook handler | 中（1d 抽出 CLI） |\n| `@hono/vite-dev-server` 作为生产依赖 | dev-only 的包卡了安装流程 | 小（0.5d 改为 optional dep） |\n| `BuildSSGOptions` 的默认值从 ctx 和 options 两处读取 | 同个值有两个源，优先级靠\"如果 CLI 传了就覆盖\" | 中（1d 统一为单一 options 对象） |\n| `allNoExternal` 列表硬编码 lit/parse5 等 | 用户想加自定义 adapter 时不知道往哪加 | 小（0.5d 改为可外部配置） |\n\n**不碰的：** BuildStep 模式、branded token、LessBuildContext 结构。它们不脏，只是\"内部细节多\"，内部项目不需要改。\n\n---\n\n## 二、同类产品对比\n\n### 对比矩阵\n\n| 维度 | LessJS 当前 | LessJS ESM 改造后 | Astro | Fresh (Deno) | Eleventy |\n|------|------------|------------------|-------|-------------|----------|\n| **SSG 是否依赖构建工具** | 🔴 依赖 Vite | 🟢 独立 CLI | 🟢 独立 CLI | 🟢 独立 | 🟢 独立 |\n| **SSR bundle 格式** | ESM（noExternal 内联） | ESM + sidecar importmap | 无（无传统 SSR bundle） | JSX → Preact | 无 |\n| **运行时要求** | Vite 进程内 | 任意支持 ESM import() 的 runtime | Node.js | Deno | Node.js |\n| **核心渲染** | DSD 字符串拼接 | DSD 字符串拼接（不变） | 模板字符串 | JSX → VNode | 模板引擎 |\n| **Islands** | ✅ DSD + Lit | ✅ 不变 | ✅ Astro Islands | ✅ Fresh Islands | ❌ 无 |\n| **HMR** | ✅ Vite HMR | ✅ Vite HMR（dev 时） | ✅ Vite HMR | ✅ Fresh HMR | ❌ 无 |\n| **技术选型自由** | Lit 优先，无 adapter 可自行扩展 | 不变 | React/Vue/Svelte/Lit | Preact | 任意模板引擎 |\n| **外部用户安装摩擦** | ⚠️ npm: vs jsr: 坑 | 🟢 零摩擦 | 🟢 零摩擦 | 🟢 零摩擦 | 🟢 零摩擦 |\n\n### 关键差异\n\n**Astro vs LessJS：**\n- Astro 的 `astro build` 本身就是一个 CLI，Vite 作为打包器被调用，不控制执行流程\n- Astro 没有\"SSR bundle\"这个概念——渲染在 build 进程里直接做\n- LessJS 的 DSD + Lit SSR 管线比 Astro 的模板渲染更灵活（组件可以用 Lit 装饰器、生命周期、reactive properties）\n- **改造后**：LessJS 的 SSG 独立性和 Astro 同级，但内容渲染能力（组件模型）更强\n\n**Fresh vs LessJS：**\n- Fresh 锁死在 Deno 上，LessJS 可以跑在任何 runtime\n- Fresh 的 islands 是 Preact 的，LessJS 是 Web Components 的（更标准）\n- Fresh 没有 DSD，客户端重建整个 shadow tree\n\n**Eleventy vs LessJS：**\n- Eleventy 不碰客户端 JS，LessJS 有完整的 Islands/DSD 管线\n- Eleventy 插件生态成熟，LessJS 还小\n\n### 改造后的差异化定位\n\n```\nLessJS = DSD + ESM-native SSG + Islands + 任意 runtime\n         ↑ Astro 没有 DSD/任意 runtime\n         ↑ Fresh 锁 Deno\n         ↑ Eleventy 没有互动能力\n```\n\n---\n\n## 三、工作量和优先级\n\n### 改造范围（分三批）\n\n**P0（解决痛点，必须做）**\n\n| 任务 | 文件影响 | 估算 |\n|------|---------|------|\n| Phase 1 产出 SSR bundle 时附带 `importmap.json` | `build-ssg.ts` 追加写文件 | 0.5d |\n| 独立 SSG CLI 入口 | 新增 `packages/adapter-vite/src/cli/ssg.ts` | 1d |\n| 将 `@hono/vite-dev-server` 移为 optional dep | `packages/adapter-vite/deno.json` + `package.json` | 0.5d |\n| 文档：写明 `jsr:` vs `npm:` 差异 | README | 0.5d |\n| **小计** | | **2.5d** |\n\n**P1（值得做）**\n\n| 任务 | 文件影响 | 估算 |\n|------|---------|------|\n| 统一 `BuildSSGOptions` 单一数据源（不再从 ctx 和 options 两处读） | `build-ssg.ts` | 1d |\n| `allNoExternal` 列表改为可外部配置 | `build-ssg.ts` + `types.ts` | 0.5d |\n| 测试：SSG CLI 独立运行测试（不经过 Vite closeBundle） | 新增 test | 1d |\n| **小计** | | **2.5d** |\n\n**P2（后面再说）**\n\n| 任务 | 文件影响 | 估算 |\n|------|---------|------|\n| 将 route-scanner.ts / hono-entry.ts 独立成纯函数（无 Vite 类型依赖） | `route-scanner.ts`, `hono-entry.ts` | 1.5d |\n| dev 模式支持纯 ESM 热更新（不需要 Vite HMR 的轻量路径） | 新增 | 3d+ |\n| 全面 Typescript化 `BuildStep`（去掉 `any` 和隐式类型） | `build.ts`, `build-context.ts` | 1d |\n\n### 总计\n\n| 批次 | 工作量 | 效果 |\n|------|--------|------|\n| P0 | 2.5d | ✅ 解决 `npm:` vs `jsr:` 摩擦，SSG 可独立运行 |\n| P0+P1 | 5d | ✅ 架构清晰、可维护性提升、外部用户零阻碍 |\n| P0+P1+P2 | ~10d | ✅ 全链路纯函数化、dev 模式可完全脱离 Vite |\n\n---\n\n## 四、最终效果（用户视角）\n\n### 安装\n\n```bash\n# 方法 1: npm（干净，推荐外部用户）\nnpm install @openelement/app @openelement/adapter-lit\n\n# 方法 2: JSR（干净）\ndeno add jsr:@openelement/app jsr:@openelement/adapter-lit\n\n# 方法 3: Workspace（开发 LessJS 本身用）\ngit clone https://github.com/open-element/open-element.git\n```\n\n**三种方式都能跑，没有 `npm:` vs `jsr:` 的差异。** 因为 importmap.json 是 SSR bundle 自带的。\n\n### 开发\n\n```bash\ndeno task dev    # Vite dev server，和现在一样\n```\n\n### 构建\n\n```bash\ndeno task build  # Vite build + SSG，和现在一样\n\n# 或者独立 SSG（不需要 Vite 环境）\nnpx lessjs-ssg    # 读 dist/server/entry.mjs + importmap.json\n# 或者\ndeno run -A npm:@openelement/adapter-vite/ssg\n# 或者\nnode ssg.mjs      # 只要 Node 18+\n```\n\n### 部署\n\n```bash\n# Cloudflare Pages / Vercel / Netlify / 纯静态托管\n# dist/ 就是你的站点\n# 不需要服务端，不需要函数\n```\n\n---\n\n## 五、实事求是的判断\n\n| 问题 | 答案 |\n|------|------|\n| **DSD 能零依赖吗？** | ✅ **已经做到了。** `render-dsd.ts` 零 Vite、零 Lit、零 node:* |\n| **ESM SSG 可行吗？** | ✅ **可行。** 只加一个 `importmap.json` 和一个独立 CLI，Phase 3 逻辑不需要动 |\n| **能解决 `npm:` vs `jsr:` 摩擦吗？** | ✅ **能。** SSR bundle 自带 import map，加载时不依赖环境解析器 |\n| **项目能更干净吗？** | ⚠️ **能，但 P0 改动不大。** 核心脏点只有 `closeBundle` 的耦合 + 一个 optional dep |\n| **HMR 会断吗？** | ✅ **不会。** dev 模式继续用 Vite，不受影响 |\n| **和 Astro 比呢？** | 🟢 **改造后同级，且 DSD + Lit 组件模型更强。** Astro 不能做到零 Vite 依赖的 SSG |\n| **工作量值不值？** | ✅ **P0 的 2.5d 值。** 解决了一个真实摩擦，外部用户引导文档也不用写\"注意 jsr: 和 npm: 区别\"这种尴尬话 |\n",
    "html": "<h1>LessJS 架构分析报告</h1>\n<blockquote>\n<p>日期：2026-05-13\n目标：E S M 原生 + DSD 驱动 + Vite 可选的 SSG 框架可行性分析</p>\n</blockquote>\n<hr />\n<h2>一、核心三问：DSD 能去依赖吗？ESM SSG 可行吗？项目能更干净吗？</h2>\n<h3>1.1 DSD 已经零依赖</h3>\n<p><code>packages/core/src/render-dsd.ts</code> 的依赖链：</p>\n<pre><code>render-dsd.ts\n  ├── html-escape.ts           ← core 内部（纯函数，零依赖）\n  ├── types.ts                 ← core 内部（interface 定义）\n  ├── adapter-registry.ts      ← core 内部（单例 Map）\n  ├── render-nested.ts         ← core 内部（递归 DSD）\n  └── logger.ts                ← core 内部（console wrapper）\n</code></pre>\n<p><strong>没有 Lit，没有 Vite，没有 node:*，没有 npm: 前缀。</strong><br />所有 DSD 渲染是纯字符串拼接 + <code>async/await</code>。用户组件只要写 <code>render(): string</code>，就不需要 Lit。</p>\n<p>Lit 只通过 <code>@openelement/adapter-lit</code> 的 <code>installLitAdapter()</code> 注入——用户主动调用才激活。如果用原生 Web Components（<code>render(): string</code>）或者 Lit 之外的框架，整套管线不需要 Lit。</p>\n<p><strong>结论：DSD 已经做到了你想要的——核心依赖为零，Lit 可选。</strong></p>\n<h3>1.2 ESM SSG 可行，且解决真实摩擦</h3>\n<p>当前 Phase 3 的流程：</p>\n<pre><code>closeBundle (Vite plugin)\n  → viteBuild(ssr:true, noExternal:true)     ← 用 Vite 产出一个自包含的 SSR bundle\n  → register SSR bundle from disk             ← 用 import() 加载\n  → render all pages                           ← 纯 DSD 渲染\n  → write HTML files                           ← 纯文件写入\n</code></pre>\n<p><strong>第 1 步和第 2 步耦合了 Vite</strong>。改成 ESM 模式的关键变化：</p>\n<pre><code>当前（Vite 强耦合）                      ESM 模式（Vite 可选）\n─────────────────                      ─────────────────\nPhase 1:                                  Phase 1:\n  scanRoutes()  ← 纯 TS，无 Vite             scanRoutes()  ← 不变\n  genHonoEntry() ← 纯字符串拼接              genHonoEntry() ← 不变\n  viteBuild(ssr, noExternal) ↓              esbuild --format=esm ↓\n  → ssr/server/entry.js                      → ssr/server/entry.mjs\n                                            + ssr/importmap.json   ← NEW\n\nPhase 2:                                  Phase 2:\n  viteBuild(client islands)                  viteBuild(client islands)\n  ← 保留 Vite/esbuild/rolldown               ← 保留，仅打包时用\n\nPhase 3:                                  Phase 3:\n  register(&#39;./dist/server/entry.js&#39;)         register(&#39;./dist/server/entry.mjs&#39;)\n  renderAll(routes)                          renderAll(routes)  \n  ← 在 closeBundle 里跑                       ← 独立 CLI，不依赖 Vite\n</code></pre>\n<p><strong>新增的 <code>importmap.json</code></strong>：</p>\n<pre><code class=\"language-json\">// ssr/importmap.json — Phase 1 生成 SSR bundle 时自动产出\n{\n  &quot;imports&quot;: {\n    &quot;hono&quot;: &quot;https://jsr.io/@hono/hono/4.7.0/mod.ts&quot;,\n    &quot;lit&quot;: &quot;npm:lit@3.3.2&quot;,\n    &quot;@openelement/core&quot;: &quot;npm:@jsr/lessjs__core@0.13.0&quot;,\n    &quot;@openelement/core/logger&quot;: &quot;npm:@jsr/lessjs__core@0.13.0/logger&quot;,\n    &quot;@openelement/adapter-lit&quot;: &quot;npm:@jsr/lessjs__adapter-lit@0.8.0&quot;\n  }\n}\n</code></pre>\n<p>加载方式（任意 runtime）：</p>\n<pre><code class=\"language-js\">// Node.js / Deno / Bun — 都行\nconst ssrBundle = await import(&#39;./dist/server/entry.mjs&#39;);\n// 配合 importmap 解析\nconst routes = ssrBundle.getRoutes();\nfor (const route of routes) {\n  const html = await ssrBundle.render(route);\n  writeFileSync(`dist/${route.path}.html`, html);\n}\n</code></pre>\n<p><strong>为什么这解决了 <code>npm:</code> vs <code>jsr:</code> 的坑？</strong><br />因为 SSR bundle 不再依赖&quot;构建环境的隐式 import map&quot;，而是<strong>自己带一份明文 import map</strong>。不管用户用 <code>jsr:@openelement/app</code> 还是 <code>npm:@jsr/lessjs__app</code> 还是 workspace，Phase 3 都读这份 sidecar import map 来解析 bare specifier。</p>\n<h3>1.3 项目能更干净吗？能，但不碰伤筋动骨的东西</h3>\n<p>当前项目的脏点：</p>\n<table>\n<thead>\n<tr>\n<th>脏点</th>\n<th>描述</th>\n<th>改的成本</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>closeBundle</code> 里塞了整个 Phase 3</td>\n<td>SSG 逻辑不该是 Vite 插件的一个 hook handler</td>\n<td>中（1d 抽出 CLI）</td>\n</tr>\n<tr>\n<td><code>@hono/vite-dev-server</code> 作为生产依赖</td>\n<td>dev-only 的包卡了安装流程</td>\n<td>小（0.5d 改为 optional dep）</td>\n</tr>\n<tr>\n<td><code>BuildSSGOptions</code> 的默认值从 ctx 和 options 两处读取</td>\n<td>同个值有两个源，优先级靠&quot;如果 CLI 传了就覆盖&quot;</td>\n<td>中（1d 统一为单一 options 对象）</td>\n</tr>\n<tr>\n<td><code>allNoExternal</code> 列表硬编码 lit/parse5 等</td>\n<td>用户想加自定义 adapter 时不知道往哪加</td>\n<td>小（0.5d 改为可外部配置）</td>\n</tr>\n</tbody></table>\n<p><strong>不碰的：</strong> BuildStep 模式、branded token、LessBuildContext 结构。它们不脏，只是&quot;内部细节多&quot;，内部项目不需要改。</p>\n<hr />\n<h2>二、同类产品对比</h2>\n<h3>对比矩阵</h3>\n<table>\n<thead>\n<tr>\n<th>维度</th>\n<th>LessJS 当前</th>\n<th>LessJS ESM 改造后</th>\n<th>Astro</th>\n<th>Fresh (Deno)</th>\n<th>Eleventy</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>SSG 是否依赖构建工具</strong></td>\n<td>🔴 依赖 Vite</td>\n<td>🟢 独立 CLI</td>\n<td>🟢 独立 CLI</td>\n<td>🟢 独立</td>\n<td>🟢 独立</td>\n</tr>\n<tr>\n<td><strong>SSR bundle 格式</strong></td>\n<td>ESM（noExternal 内联）</td>\n<td>ESM + sidecar importmap</td>\n<td>无（无传统 SSR bundle）</td>\n<td>JSX → Preact</td>\n<td>无</td>\n</tr>\n<tr>\n<td><strong>运行时要求</strong></td>\n<td>Vite 进程内</td>\n<td>任意支持 ESM import() 的 runtime</td>\n<td>Node.js</td>\n<td>Deno</td>\n<td>Node.js</td>\n</tr>\n<tr>\n<td><strong>核心渲染</strong></td>\n<td>DSD 字符串拼接</td>\n<td>DSD 字符串拼接（不变）</td>\n<td>模板字符串</td>\n<td>JSX → VNode</td>\n<td>模板引擎</td>\n</tr>\n<tr>\n<td><strong>Islands</strong></td>\n<td>✅ DSD + Lit</td>\n<td>✅ 不变</td>\n<td>✅ Astro Islands</td>\n<td>✅ Fresh Islands</td>\n<td>❌ 无</td>\n</tr>\n<tr>\n<td><strong>HMR</strong></td>\n<td>✅ Vite HMR</td>\n<td>✅ Vite HMR（dev 时）</td>\n<td>✅ Vite HMR</td>\n<td>✅ Fresh HMR</td>\n<td>❌ 无</td>\n</tr>\n<tr>\n<td><strong>技术选型自由</strong></td>\n<td>Lit 优先，无 adapter 可自行扩展</td>\n<td>不变</td>\n<td>React/Vue/Svelte/Lit</td>\n<td>Preact</td>\n<td>任意模板引擎</td>\n</tr>\n<tr>\n<td><strong>外部用户安装摩擦</strong></td>\n<td>⚠️ npm: vs jsr: 坑</td>\n<td>🟢 零摩擦</td>\n<td>🟢 零摩擦</td>\n<td>🟢 零摩擦</td>\n<td>🟢 零摩擦</td>\n</tr>\n</tbody></table>\n<h3>关键差异</h3>\n<p><strong>Astro vs LessJS：</strong></p>\n<ul>\n<li>Astro 的 <code>astro build</code> 本身就是一个 CLI，Vite 作为打包器被调用，不控制执行流程</li>\n<li>Astro 没有&quot;SSR bundle&quot;这个概念——渲染在 build 进程里直接做</li>\n<li>LessJS 的 DSD + Lit SSR 管线比 Astro 的模板渲染更灵活（组件可以用 Lit 装饰器、生命周期、reactive properties）</li>\n<li><strong>改造后</strong>：LessJS 的 SSG 独立性和 Astro 同级，但内容渲染能力（组件模型）更强</li>\n</ul>\n<p><strong>Fresh vs LessJS：</strong></p>\n<ul>\n<li>Fresh 锁死在 Deno 上，LessJS 可以跑在任何 runtime</li>\n<li>Fresh 的 islands 是 Preact 的，LessJS 是 Web Components 的（更标准）</li>\n<li>Fresh 没有 DSD，客户端重建整个 shadow tree</li>\n</ul>\n<p><strong>Eleventy vs LessJS：</strong></p>\n<ul>\n<li>Eleventy 不碰客户端 JS，LessJS 有完整的 Islands/DSD 管线</li>\n<li>Eleventy 插件生态成熟，LessJS 还小</li>\n</ul>\n<h3>改造后的差异化定位</h3>\n<pre><code>LessJS = DSD + ESM-native SSG + Islands + 任意 runtime\n         ↑ Astro 没有 DSD/任意 runtime\n         ↑ Fresh 锁 Deno\n         ↑ Eleventy 没有互动能力\n</code></pre>\n<hr />\n<h2>三、工作量和优先级</h2>\n<h3>改造范围（分三批）</h3>\n<p><strong>P0（解决痛点，必须做）</strong></p>\n<table>\n<thead>\n<tr>\n<th>任务</th>\n<th>文件影响</th>\n<th>估算</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Phase 1 产出 SSR bundle 时附带 <code>importmap.json</code></td>\n<td><code>build-ssg.ts</code> 追加写文件</td>\n<td>0.5d</td>\n</tr>\n<tr>\n<td>独立 SSG CLI 入口</td>\n<td>新增 <code>packages/adapter-vite/src/cli/ssg.ts</code></td>\n<td>1d</td>\n</tr>\n<tr>\n<td>将 <code>@hono/vite-dev-server</code> 移为 optional dep</td>\n<td><code>packages/adapter-vite/deno.json</code> + <code>package.json</code></td>\n<td>0.5d</td>\n</tr>\n<tr>\n<td>文档：写明 <code>jsr:</code> vs <code>npm:</code> 差异</td>\n<td>README</td>\n<td>0.5d</td>\n</tr>\n<tr>\n<td><strong>小计</strong></td>\n<td></td>\n<td><strong>2.5d</strong></td>\n</tr>\n</tbody></table>\n<p><strong>P1（值得做）</strong></p>\n<table>\n<thead>\n<tr>\n<th>任务</th>\n<th>文件影响</th>\n<th>估算</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>统一 <code>BuildSSGOptions</code> 单一数据源（不再从 ctx 和 options 两处读）</td>\n<td><code>build-ssg.ts</code></td>\n<td>1d</td>\n</tr>\n<tr>\n<td><code>allNoExternal</code> 列表改为可外部配置</td>\n<td><code>build-ssg.ts</code> + <code>types.ts</code></td>\n<td>0.5d</td>\n</tr>\n<tr>\n<td>测试：SSG CLI 独立运行测试（不经过 Vite closeBundle）</td>\n<td>新增 test</td>\n<td>1d</td>\n</tr>\n<tr>\n<td><strong>小计</strong></td>\n<td></td>\n<td><strong>2.5d</strong></td>\n</tr>\n</tbody></table>\n<p><strong>P2（后面再说）</strong></p>\n<table>\n<thead>\n<tr>\n<th>任务</th>\n<th>文件影响</th>\n<th>估算</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>将 route-scanner.ts / hono-entry.ts 独立成纯函数（无 Vite 类型依赖）</td>\n<td><code>route-scanner.ts</code>, <code>hono-entry.ts</code></td>\n<td>1.5d</td>\n</tr>\n<tr>\n<td>dev 模式支持纯 ESM 热更新（不需要 Vite HMR 的轻量路径）</td>\n<td>新增</td>\n<td>3d+</td>\n</tr>\n<tr>\n<td>全面 Typescript化 <code>BuildStep</code>（去掉 <code>any</code> 和隐式类型）</td>\n<td><code>build.ts</code>, <code>build-context.ts</code></td>\n<td>1d</td>\n</tr>\n</tbody></table>\n<h3>总计</h3>\n<table>\n<thead>\n<tr>\n<th>批次</th>\n<th>工作量</th>\n<th>效果</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>P0</td>\n<td>2.5d</td>\n<td>✅ 解决 <code>npm:</code> vs <code>jsr:</code> 摩擦，SSG 可独立运行</td>\n</tr>\n<tr>\n<td>P0+P1</td>\n<td>5d</td>\n<td>✅ 架构清晰、可维护性提升、外部用户零阻碍</td>\n</tr>\n<tr>\n<td>P0+P1+P2</td>\n<td>~10d</td>\n<td>✅ 全链路纯函数化、dev 模式可完全脱离 Vite</td>\n</tr>\n</tbody></table>\n<hr />\n<h2>四、最终效果（用户视角）</h2>\n<h3>安装</h3>\n<pre><code class=\"language-bash\"># 方法 1: npm（干净，推荐外部用户）\nnpm install @openelement/app @openelement/adapter-lit\n\n# 方法 2: JSR（干净）\ndeno add jsr:@openelement/app jsr:@openelement/adapter-lit\n\n# 方法 3: Workspace（开发 LessJS 本身用）\ngit clone https://github.com/open-element/open-element.git\n</code></pre>\n<p><strong>三种方式都能跑，没有 <code>npm:</code> vs <code>jsr:</code> 的差异。</strong> 因为 importmap.json 是 SSR bundle 自带的。</p>\n<h3>开发</h3>\n<pre><code class=\"language-bash\">deno task dev    # Vite dev server，和现在一样\n</code></pre>\n<h3>构建</h3>\n<pre><code class=\"language-bash\">deno task build  # Vite build + SSG，和现在一样\n\n# 或者独立 SSG（不需要 Vite 环境）\nnpx lessjs-ssg    # 读 dist/server/entry.mjs + importmap.json\n# 或者\ndeno run -A npm:@openelement/adapter-vite/ssg\n# 或者\nnode ssg.mjs      # 只要 Node 18+\n</code></pre>\n<h3>部署</h3>\n<pre><code class=\"language-bash\"># Cloudflare Pages / Vercel / Netlify / 纯静态托管\n# dist/ 就是你的站点\n# 不需要服务端，不需要函数\n</code></pre>\n<hr />\n<h2>五、实事求是的判断</h2>\n<table>\n<thead>\n<tr>\n<th>问题</th>\n<th>答案</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>DSD 能零依赖吗？</strong></td>\n<td>✅ <strong>已经做到了。</strong> <code>render-dsd.ts</code> 零 Vite、零 Lit、零 node:*</td>\n</tr>\n<tr>\n<td><strong>ESM SSG 可行吗？</strong></td>\n<td>✅ <strong>可行。</strong> 只加一个 <code>importmap.json</code> 和一个独立 CLI，Phase 3 逻辑不需要动</td>\n</tr>\n<tr>\n<td><strong>能解决 <code>npm:</code> vs <code>jsr:</code> 摩擦吗？</strong></td>\n<td>✅ <strong>能。</strong> SSR bundle 自带 import map，加载时不依赖环境解析器</td>\n</tr>\n<tr>\n<td><strong>项目能更干净吗？</strong></td>\n<td>⚠️ <strong>能，但 P0 改动不大。</strong> 核心脏点只有 <code>closeBundle</code> 的耦合 + 一个 optional dep</td>\n</tr>\n<tr>\n<td><strong>HMR 会断吗？</strong></td>\n<td>✅ <strong>不会。</strong> dev 模式继续用 Vite，不受影响</td>\n</tr>\n<tr>\n<td><strong>和 Astro 比呢？</strong></td>\n<td>🟢 <strong>改造后同级，且 DSD + Lit 组件模型更强。</strong> Astro 不能做到零 Vite 依赖的 SSG</td>\n</tr>\n<tr>\n<td><strong>工作量值不值？</strong></td>\n<td>✅ <strong>P0 的 2.5d 值。</strong> 解决了一个真实摩擦，外部用户引导文档也不用写&quot;注意 jsr: 和 npm: 区别&quot;这种尴尬话</td>\n</tr>\n</tbody></table>\n"
  },
  {
    "slug": "v0-13-0",
    "frontmatter": {
      "title": "v0.13.0 — API Surface Convergence + Phase Branded Types",
      "date": "2026-05-12",
      "draft": false,
      "tags": [
        "release",
        "architecture"
      ],
      "excerpt": "Core API surface reduced from 18 to 6 exports, compile-time phase ordering via branded types, virtual module IDs migrated from core to adapter-vite, and CI coverage collection.",
      "type": "post",
      "lang": "en"
    },
    "content": "\n## Overview\n\nv0.13.0 is an **architecture hardening** release (ADR 0021). The focus is on narrowing the public API surface, adding compiler-enforced phase ordering, and eliminating the last cross-package coupling points.\n\nNo new features. Just tighter contracts.\n\n---\n\n## Breaking Changes\n\n### Core exports: 18 → 6\n\n`@openelement/core` now exports only 6 public subpaths:\n\n```ts\n// Remaining (public)\nimport { ... } from '@openelement/core';              // runtime utilities\nimport { LessError } from '@openelement/core/errors';\nimport { createSsrContext } from '@openelement/core/context';\nimport { createLogger } from '@openelement/core/logger';\nimport { navigate } from '@openelement/core/navigation';\n// ./constants kept for cross-package shared constants\n```\n\nRemoved (internal implementation details, no longer exposed):\n\n- `./render-dsd` — internal, not user-facing\n- `./html-escape` — internal\n- `./adapter-registry` — internal\n- `./ssr-handler` — deleted in v0.12, lingering export removed\n\nGenerated code resolves through `CORE_SUBPATHS` mapping in `@openelement/adapter-vite`.\n\n### ssr-handler.ts deleted\n\nThe pure re-export facade `ssr-handler.ts` is finally gone. Consumers import directly from `@openelement/core`.\n\n### Virtual module IDs moved\n\nAll Vite virtual module constants (`VIRTUAL_NAV_ID`, `RESOLVED_BLOG_DATA_ID`, etc.) moved from `@openelement/core/constants` to `@openelement/adapter-vite/virtual-ids`. Core no longer knows about Vite internals.\n\n---\n\n## New (Internal)\n\n### Compile-time Phase ordering\n\nBranded Type state machine ensures build phases execute in the correct order:\n\n```typescript\ntype Phase1Ready = { __phase: 'phase1' };\ntype Phase2Ready = { __phase: 'phase2' };\ntype Phase3Ready = { __phase: 'phase3' };\n\nfunction buildPhase1(ctx): Phase1Ready & { ... }\nfunction buildPhase2(state: Phase1Ready): Phase2Ready & { ... }\nfunction buildPhase3(state: Phase2Ready): void\n```\n\n`buildPhase2` only accepts `Phase1Ready` output. `buildPhase3` only accepts `Phase2Ready`. The compiler catches out-of-order calls.\n\n### CI coverage\n\nAll test jobs now collect `--coverage`. Coverage reports generated on every CI run.\n\n---\n\n## By-Product Cleanup\n\n| Item                  | Change                                                   |\n| --------------------- | -------------------------------------------------------- |\n| Zero barrel files     | `content/src/nav/index.ts`, `sitemap/types.ts` — inlined |\n| islandEffect interval | 5000ms → 30000ms (reduced polling)                       |\n| @openelement/app tests     | 0 → 16 tests                                             |\n\n---\n\n## Package Versions\n\n| Package                | v0.12.x | v0.13.0    |\n| ---------------------- | ------- | ---------- |\n| `@openelement/core`         | 0.12.1  | **0.13.0** |\n| `@openelement/adapter-vite` | 0.2.0   | **0.3.0**  |\n| `@openelement/content`      | 0.3.2   | 0.3.3      |\n| `@openelement/signal`       | 0.6.2   | 0.6.3      |\n| `@openelement/app`          | 0.3.0   | 0.3.1      |\n| `@openelement/i18n`         | 0.1.0   | 0.1.1      |\n| `@openelement/create`       | 0.8.0   | 0.8.1      |\n\nNo changes to adapter-lit, ui, rpc.\n\n---\n\n## Migration Guide\n\n### If you use `@openelement/app` (recommended)\n\n**No changes needed.** `lessjs()` abstracts all internal restructuring.\n\n### If you import from `@openelement/core` subpaths\n\nRemove imports from removed subpaths:\n\n```ts\n// Delete these:\nimport { ... } from '@openelement/core/render-dsd';    // ❌ removed\nimport { ... } from '@openelement/core/html-escape';    // ❌ removed\nimport { ... } from '@openelement/core/adapter-registry'; // ❌ removed\nimport { ... } from '@openelement/core/ssr-handler';    // ❌ removed in v0.12\n```\n\nAll runtime exports are available from the main entry:\n\n```ts\nimport { escapeHtml, registerAdapter, renderDSD } from '@openelement/core'; // ✅\n```\n\n---\n\n## Stats\n\n- **86 files changed**, +5,427 / −1,506 lines\n- **268 tests passing** (41 new)\n- **CI coverage**: all jobs collect `--coverage`\n- **Zero globalThis bridges**: verified deleted in codebase\n- **Zero barrel files**: verified\n\n---\n\n_Release date: 2026-05-12_\n",
    "html": "<h2>Overview</h2>\n<p>v0.13.0 is an <strong>architecture hardening</strong> release (ADR 0021). The focus is on narrowing the public API surface, adding compiler-enforced phase ordering, and eliminating the last cross-package coupling points.</p>\n<p>No new features. Just tighter contracts.</p>\n<hr />\n<h2>Breaking Changes</h2>\n<h3>Core exports: 18 → 6</h3>\n<p><code>@openelement/core</code> now exports only 6 public subpaths:</p>\n<pre><code class=\"language-ts\">// Remaining (public)\nimport { ... } from &#39;@openelement/core&#39;;              // runtime utilities\nimport { LessError } from &#39;@openelement/core/errors&#39;;\nimport { createSsrContext } from &#39;@openelement/core/context&#39;;\nimport { createLogger } from &#39;@openelement/core/logger&#39;;\nimport { navigate } from &#39;@openelement/core/navigation&#39;;\n// ./constants kept for cross-package shared constants\n</code></pre>\n<p>Removed (internal implementation details, no longer exposed):</p>\n<ul>\n<li><code>./render-dsd</code> — internal, not user-facing</li>\n<li><code>./html-escape</code> — internal</li>\n<li><code>./adapter-registry</code> — internal</li>\n<li><code>./ssr-handler</code> — deleted in v0.12, lingering export removed</li>\n</ul>\n<p>Generated code resolves through <code>CORE_SUBPATHS</code> mapping in <code>@openelement/adapter-vite</code>.</p>\n<h3>ssr-handler.ts deleted</h3>\n<p>The pure re-export facade <code>ssr-handler.ts</code> is finally gone. Consumers import directly from <code>@openelement/core</code>.</p>\n<h3>Virtual module IDs moved</h3>\n<p>All Vite virtual module constants (<code>VIRTUAL_NAV_ID</code>, <code>RESOLVED_BLOG_DATA_ID</code>, etc.) moved from <code>@openelement/core/constants</code> to <code>@openelement/adapter-vite/virtual-ids</code>. Core no longer knows about Vite internals.</p>\n<hr />\n<h2>New (Internal)</h2>\n<h3>Compile-time Phase ordering</h3>\n<p>Branded Type state machine ensures build phases execute in the correct order:</p>\n<pre><code class=\"language-typescript\">type Phase1Ready = { __phase: &#39;phase1&#39; };\ntype Phase2Ready = { __phase: &#39;phase2&#39; };\ntype Phase3Ready = { __phase: &#39;phase3&#39; };\n\nfunction buildPhase1(ctx): Phase1Ready &amp; { ... }\nfunction buildPhase2(state: Phase1Ready): Phase2Ready &amp; { ... }\nfunction buildPhase3(state: Phase2Ready): void\n</code></pre>\n<p><code>buildPhase2</code> only accepts <code>Phase1Ready</code> output. <code>buildPhase3</code> only accepts <code>Phase2Ready</code>. The compiler catches out-of-order calls.</p>\n<h3>CI coverage</h3>\n<p>All test jobs now collect <code>--coverage</code>. Coverage reports generated on every CI run.</p>\n<hr />\n<h2>By-Product Cleanup</h2>\n<table>\n<thead>\n<tr>\n<th>Item</th>\n<th>Change</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Zero barrel files</td>\n<td><code>content/src/nav/index.ts</code>, <code>sitemap/types.ts</code> — inlined</td>\n</tr>\n<tr>\n<td>islandEffect interval</td>\n<td>5000ms → 30000ms (reduced polling)</td>\n</tr>\n<tr>\n<td>@openelement/app tests</td>\n<td>0 → 16 tests</td>\n</tr>\n</tbody></table>\n<hr />\n<h2>Package Versions</h2>\n<table>\n<thead>\n<tr>\n<th>Package</th>\n<th>v0.12.x</th>\n<th>v0.13.0</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>@openelement/core</code></td>\n<td>0.12.1</td>\n<td><strong>0.13.0</strong></td>\n</tr>\n<tr>\n<td><code>@openelement/adapter-vite</code></td>\n<td>0.2.0</td>\n<td><strong>0.3.0</strong></td>\n</tr>\n<tr>\n<td><code>@openelement/content</code></td>\n<td>0.3.2</td>\n<td>0.3.3</td>\n</tr>\n<tr>\n<td><code>@openelement/signal</code></td>\n<td>0.6.2</td>\n<td>0.6.3</td>\n</tr>\n<tr>\n<td><code>@openelement/app</code></td>\n<td>0.3.0</td>\n<td>0.3.1</td>\n</tr>\n<tr>\n<td><code>@openelement/i18n</code></td>\n<td>0.1.0</td>\n<td>0.1.1</td>\n</tr>\n<tr>\n<td><code>@openelement/create</code></td>\n<td>0.8.0</td>\n<td>0.8.1</td>\n</tr>\n</tbody></table>\n<p>No changes to adapter-lit, ui, rpc.</p>\n<hr />\n<h2>Migration Guide</h2>\n<h3>If you use <code>@openelement/app</code> (recommended)</h3>\n<p><strong>No changes needed.</strong> <code>lessjs()</code> abstracts all internal restructuring.</p>\n<h3>If you import from <code>@openelement/core</code> subpaths</h3>\n<p>Remove imports from removed subpaths:</p>\n<pre><code class=\"language-ts\">// Delete these:\nimport { ... } from &#39;@openelement/core/render-dsd&#39;;    // ❌ removed\nimport { ... } from &#39;@openelement/core/html-escape&#39;;    // ❌ removed\nimport { ... } from &#39;@openelement/core/adapter-registry&#39;; // ❌ removed\nimport { ... } from &#39;@openelement/core/ssr-handler&#39;;    // ❌ removed in v0.12\n</code></pre>\n<p>All runtime exports are available from the main entry:</p>\n<pre><code class=\"language-ts\">import { escapeHtml, registerAdapter, renderDSD } from &#39;@openelement/core&#39;; // ✅\n</code></pre>\n<hr />\n<h2>Stats</h2>\n<ul>\n<li><strong>86 files changed</strong>, +5,427 / −1,506 lines</li>\n<li><strong>268 tests passing</strong> (41 new)</li>\n<li><strong>CI coverage</strong>: all jobs collect <code>--coverage</code></li>\n<li><strong>Zero globalThis bridges</strong>: verified deleted in codebase</li>\n<li><strong>Zero barrel files</strong>: verified</li>\n</ul>\n<hr />\n<p><em>Release date: 2026-05-12</em></p>\n"
  },
  {
    "slug": "v0-12-0",
    "frontmatter": {
      "title": "v0.12.0 — Virtual Data Modules + Zero Module State",
      "date": "2026-05-12",
      "draft": false,
      "tags": [
        "release",
        "architecture"
      ],
      "excerpt": "Eliminated all module-level state from @openelement/content and @openelement/i18n. Pure functions replace stateful init/getter patterns. Virtual modules become the only data bridge for SSR routes.",
      "type": "post",
      "lang": "en"
    },
    "content": "\n## Overview\n\nv0.12.0 implements [ADR 0018](/blog/0018-dev-build-data-consistency-plugin-data-bridge): **Eliminate Plugin Module State**.\n\nThe core idea: `@openelement/content` and `@openelement/i18n` must not hold module-level state (`_posts`, `_options`). Instead, data flows through Vite virtual modules that read from `LessBuildContext` at `load()` time.\n\n---\n\n## Breaking Changes\n\n### initBlogData() / getPosts() / getPostBySlug() — DELETED\n\nThese stateful APIs on `@openelement/content` are gone. No backward compatibility — v0.12.0 is a structured breaking change.\n\n```ts\n// Before (v0.11.x) — module state\nimport { getPosts, initBlogData } from '@openelement/content';\nawait initBlogData({ contentDir: 'posts' });\nconst posts = getPosts();\n\n// After (v0.12.0) — virtual module\nimport { posts } from 'virtual:less-blog-data';\n// posts is available during SSR, loaded by the virtual module\n```\n\n### initI18nData() / getLocale() / getI18nOptions() — DELETED\n\nSame pattern for i18n. Route components import from `virtual:less-i18n-data` instead.\n\n### buildCoreSubpathAliases() — DELETED\n\nThis function was the bridge for local Deno workspace resolution. It's replaced by `@deno/vite-plugin@2` which resolves bare specifiers through Deno's native `import.meta.resolve()`.\n\n**20 resolve.alias entries** deleted from `www/vite.config.ts`.\n\n### EntryDescriptor.blog field — DELETED\n\nThe `blog` field on route entries was an artifact of the old stateful pattern. Removed entirely.\n\n---\n\n## New Architecture: Virtual Data Modules\n\n```\n            ┌─ less() builds ctx ─────────────────────┐\n            │  ctx.plugins.blogOptions = { ... }       │\n            │  ctx.plugins.i18nOptions = { ... }       │\n            └──────────────────────────────────────────┘\n                           │\nlessContent().buildStart() │ lessI18n().buildStart()\n       writes to ctx       │      writes to ctx\n           ▼                      ▼\n┌──────────────────┐  ┌──────────────────┐\n│ virtual:less-    │  │ virtual:less-    │\n│ blog-data        │  │ i18n-data        │\n│ load() reads ctx │  │ load() reads ctx │\n└──────────────────┘  └──────────────────┘\n           │                      │\n           └──────┬───────────────┘\n                  ▼\n         Route components import\n         from virtual modules\n```\n\nKey properties:\n\n- **Zero module state**: all data in ctx, ctx in closure scope\n- **Single path**: dev and build both use virtual module `load()` — no dual init paths, no `onSSRInit`\n- **HMR**: content directory changes invalidate virtual module, browser reloads\n- **Static content pages**: `lessContent({ pages: { contentDir, basePath? } })` via `virtual:less-page-data`\n\n### Data flows via ctx, not dynamic imports\n\nThe old architecture had two bridges:\n\n1. **On init**: `initBlogData()` stored posts in module state\n2. **On SSR**: route components imported through a second bridge (`onSSRInit`)\n\nv0.12.0 eliminates this: `lessContent().buildStart()` writes to `ctx.plugins`, and the virtual module's `load()` hook reads from `ctx` at call time. One bridge, zero sync risk.\n\n---\n\n## Pure Functions\n\nNew pure functions replace all stateful patterns:\n\n```ts\n// @openelement/content\nloadBlogData(dir) → BlogPost[]\nloadPageData(opts) → ContentPage[]\n\n// @openelement/i18n\nloadI18nData(opts) → I18nConfig\n```\n\nThese have no side effects, no module state, and are individually testable.\n\n---\n\n## @deno/vite-plugin Integration\n\n`@deno/vite-plugin@2.0.2` handles local Deno workspace resolution:\n\n- `import.meta.resolve('@openelement/core')` → `file:///path/to/packages/core/index.ts`\n- Subpath support via `deno.json` exports map\n- npm: passthrough → Vite handles natively through node_modules\n\nThis eliminates the recurring \"Not a directory\" prefix-matching bug (3 occurrences in project history).\n\n---\n\n## Migration Guide\n\n### If you use `@openelement/app` (recommended)\n\n**No changes needed.** `lessjs()` handles ctx passing automatically.\n\n### If you call initBlogData() / getPosts() directly\n\nReplace with virtual module imports in your route components:\n\n```ts\n// Route component\nimport { getPostBySlug, posts } from 'virtual:less-blog-data';\n```\n\n### If you call initI18nData() / getLocale() directly\n\n```ts\n// Route component\nimport { getDefaultLocale, locales } from 'virtual:less-i18n-data';\n```\n\n### If you use lessContent() standalone\n\nPass ctx explicitly:\n\n```ts\nlessContent({ blog: {...}, ctx });  // ctx is now required\nlessI18n({ locales: [...], ctx });\n```\n\n---\n\n_Release date: 2026-05-12_\n",
    "html": "<h2>Overview</h2>\n<p>v0.12.0 implements <a href=\"/blog/0018-dev-build-data-consistency-plugin-data-bridge\" rel=\"noopener noreferrer\">ADR 0018</a>: <strong>Eliminate Plugin Module State</strong>.</p>\n<p>The core idea: <code>@openelement/content</code> and <code>@openelement/i18n</code> must not hold module-level state (<code>_posts</code>, <code>_options</code>). Instead, data flows through Vite virtual modules that read from <code>LessBuildContext</code> at <code>load()</code> time.</p>\n<hr />\n<h2>Breaking Changes</h2>\n<h3>initBlogData() / getPosts() / getPostBySlug() — DELETED</h3>\n<p>These stateful APIs on <code>@openelement/content</code> are gone. No backward compatibility — v0.12.0 is a structured breaking change.</p>\n<pre><code class=\"language-ts\">// Before (v0.11.x) — module state\nimport { getPosts, initBlogData } from &#39;@openelement/content&#39;;\nawait initBlogData({ contentDir: &#39;posts&#39; });\nconst posts = getPosts();\n\n// After (v0.12.0) — virtual module\nimport { posts } from &#39;virtual:less-blog-data&#39;;\n// posts is available during SSR, loaded by the virtual module\n</code></pre>\n<h3>initI18nData() / getLocale() / getI18nOptions() — DELETED</h3>\n<p>Same pattern for i18n. Route components import from <code>virtual:less-i18n-data</code> instead.</p>\n<h3>buildCoreSubpathAliases() — DELETED</h3>\n<p>This function was the bridge for local Deno workspace resolution. It&#39;s replaced by <code>@deno/vite-plugin@2</code> which resolves bare specifiers through Deno&#39;s native <code>import.meta.resolve()</code>.</p>\n<p><strong>20 resolve.alias entries</strong> deleted from <code>www/vite.config.ts</code>.</p>\n<h3>EntryDescriptor.blog field — DELETED</h3>\n<p>The <code>blog</code> field on route entries was an artifact of the old stateful pattern. Removed entirely.</p>\n<hr />\n<h2>New Architecture: Virtual Data Modules</h2>\n<pre><code>            ┌─ less() builds ctx ─────────────────────┐\n            │  ctx.plugins.blogOptions = { ... }       │\n            │  ctx.plugins.i18nOptions = { ... }       │\n            └──────────────────────────────────────────┘\n                           │\nlessContent().buildStart() │ lessI18n().buildStart()\n       writes to ctx       │      writes to ctx\n           ▼                      ▼\n┌──────────────────┐  ┌──────────────────┐\n│ virtual:less-    │  │ virtual:less-    │\n│ blog-data        │  │ i18n-data        │\n│ load() reads ctx │  │ load() reads ctx │\n└──────────────────┘  └──────────────────┘\n           │                      │\n           └──────┬───────────────┘\n                  ▼\n         Route components import\n         from virtual modules\n</code></pre>\n<p>Key properties:</p>\n<ul>\n<li><strong>Zero module state</strong>: all data in ctx, ctx in closure scope</li>\n<li><strong>Single path</strong>: dev and build both use virtual module <code>load()</code> — no dual init paths, no <code>onSSRInit</code></li>\n<li><strong>HMR</strong>: content directory changes invalidate virtual module, browser reloads</li>\n<li><strong>Static content pages</strong>: <code>lessContent({ pages: { contentDir, basePath? } })</code> via <code>virtual:less-page-data</code></li>\n</ul>\n<h3>Data flows via ctx, not dynamic imports</h3>\n<p>The old architecture had two bridges:</p>\n<ol>\n<li><strong>On init</strong>: <code>initBlogData()</code> stored posts in module state</li>\n<li><strong>On SSR</strong>: route components imported through a second bridge (<code>onSSRInit</code>)</li>\n</ol>\n<p>v0.12.0 eliminates this: <code>lessContent().buildStart()</code> writes to <code>ctx.plugins</code>, and the virtual module&#39;s <code>load()</code> hook reads from <code>ctx</code> at call time. One bridge, zero sync risk.</p>\n<hr />\n<h2>Pure Functions</h2>\n<p>New pure functions replace all stateful patterns:</p>\n<pre><code class=\"language-ts\">// @openelement/content\nloadBlogData(dir) → BlogPost[]\nloadPageData(opts) → ContentPage[]\n\n// @openelement/i18n\nloadI18nData(opts) → I18nConfig\n</code></pre>\n<p>These have no side effects, no module state, and are individually testable.</p>\n<hr />\n<h2>@deno/vite-plugin Integration</h2>\n<p><code>@deno/vite-plugin@2.0.2</code> handles local Deno workspace resolution:</p>\n<ul>\n<li><code>import.meta.resolve(&#39;@openelement/core&#39;)</code> → <code>file:///path/to/packages/core/index.ts</code></li>\n<li>Subpath support via <code>deno.json</code> exports map</li>\n<li>npm: passthrough → Vite handles natively through node_modules</li>\n</ul>\n<p>This eliminates the recurring &quot;Not a directory&quot; prefix-matching bug (3 occurrences in project history).</p>\n<hr />\n<h2>Migration Guide</h2>\n<h3>If you use <code>@openelement/app</code> (recommended)</h3>\n<p><strong>No changes needed.</strong> <code>lessjs()</code> handles ctx passing automatically.</p>\n<h3>If you call initBlogData() / getPosts() directly</h3>\n<p>Replace with virtual module imports in your route components:</p>\n<pre><code class=\"language-ts\">// Route component\nimport { getPostBySlug, posts } from &#39;virtual:less-blog-data&#39;;\n</code></pre>\n<h3>If you call initI18nData() / getLocale() directly</h3>\n<pre><code class=\"language-ts\">// Route component\nimport { getDefaultLocale, locales } from &#39;virtual:less-i18n-data&#39;;\n</code></pre>\n<h3>If you use lessContent() standalone</h3>\n<p>Pass ctx explicitly:</p>\n<pre><code class=\"language-ts\">lessContent({ blog: {...}, ctx });  // ctx is now required\nlessI18n({ locales: [...], ctx });\n</code></pre>\n<hr />\n<p><em>Release date: 2026-05-12</em></p>\n"
  },
  {
    "slug": "v0-8-0",
    "frontmatter": {
      "title": "v0.8.0 — Audit Resolution + Structured Logging + Blog Plugin",
      "date": "2026-05-08",
      "draft": false,
      "tags": [
        "release"
      ],
      "excerpt": "Five audit items resolved: structured logging, runtime-shim auto-generation, parse5 nested DSD optimization, advanced feature docs, and Playwright E2E tests. @openelement/blog ships and dogfoods.",
      "lang": "en"
    },
    "content": "\nv0.8.0 resolves all five audit items, introduces structured logging, auto-generates the runtime shim, optimizes nested DSD rendering, and ships the @openelement/blog plugin with full dogfooding.\n\n## Audit Item Resolution\n\n| # | Priority | Item                                       | Status  |\n| - | -------- | ------------------------------------------ | ------- |\n| 1 | P0       | Eliminate runtime-shim.ts manual sync risk | ✅ Done |\n| 2 | P0       | Clarify error classification guide         | ✅ Done |\n| 3 | P1       | Optimize nested DSD rendering O(n²)        | ✅ Done |\n| 4 | P1       | Improve advanced feature docs              | ✅ Done |\n| 5 | P1       | Add integration tests                      | ✅ Done |\n\n### P0-1: Runtime Shim Auto-Generation\n\n`packages/core/scripts/generate-runtime-shim.ts` uses TypeScript AST to extract functions from source files and auto-generate `runtime-shim.ts`. No more manual sync between source and shim — run `deno task generate:runtime-shim` to regenerate, verify with `git diff --exit-code`.\n\n### P0-2: Structured Logging + Error Classification\n\nNew `@openelement/core/logger` module with `createLogger(scope)`:\n\n```ts\nimport { createLogger } from '@openelement/core/logger';\n\nconst log = createLogger('ssg');\nlog.warn('Client build failed:', err.message);\nlog.info('Routes: 5 page(s), 2 API route(s), 8 island(s)');\nlog.debug('customElements.define(\"my-counter\") skipped: already defined');\n```\n\nAll framework internals migrated from raw `console.*` to structured logging. Scopes: `core` → `[LessJS]`, `ssg` → `[LessJS/SSG]`, `blog` → `[LessJS/Blog]`, `signal` → `[LessJS/Signal]`. Four log levels (DEBUG/INFO/WARN/ERROR) plus SILENT. The runtime shim includes a lightweight `log` stub that maps to `console.*` with the `[LessJS]` prefix.\n\n### P1-3: parse5 Nested DSD Optimization\n\nReplaced regex-based O(n²) nested custom element rendering with a parse5 AST O(n×d) approach in `render-nested.ts`. Bottom-up traversal, proper DSD template detection, and attribute inference. Supports complex nesting scenarios the regex approach couldn't handle.\n\n### P1-4: Advanced Feature Docs\n\nDSD guide (`/guide/dsd`), deep Islands guide (`/guide/islands-deep`), and RPC guide (`/guide/rpc`) — covering the three-layer DSD model, four island strategies, and cross-island communication patterns.\n\n### P1-5: Playwright E2E Tests\n\n10 end-to-end tests covering DSD layers and nested custom elements against the built docs site:\n\n- `e2e/dsd-layers.spec.ts` — 5 tests: HTML structure, shadow roots, styles, no raw DSD text, custom element discovery\n- `e2e/nested-ce.spec.ts` — 5 tests: custom elements in DOM, no raw text, LessJS components, shadow root upgrade, navigation\n\n## Core Features\n\n### Signal Native Switch\n\n`@openelement/signal` adds `isNativeSignal()` detection. When the browser supports TC39 Signals, it uses `globalThis.Signal` and falls back to polyfill automatically.\n\n### Island Upgrade Manifest\n\n`@openelement/core/island-manifest` generates per-page island manifests, replacing the global island entry. Each SSG page only loads the islands it actually uses.\n\n### @openelement/blog Package\n\nNew Vite plugin package for blog/content sites. `lessBlog()` integrates into Vite build, `parseMarkdownFile()` handles Markdown + frontmatter, `scanPosts()` + `generateBlogRoutes()` auto-generate routes. The docs site itself dogfoods this plugin — 5 blog posts served from `/blog`.\n\nv0.8 scope: `.md → routes → list/post pages`. No MDX, comments, or tags system.\n\n## Architecture Improvements\n\n- **render-dsd.ts split**: 770-line monolith → 4 focused modules (core, escape, nested, barrel export). Backward-compatible.\n- **UI unified to DsdLitElement**: 3 components migrated to the Mixin pattern. All UI components now share the same DSD hydration mechanism.\n- **insertAfterHead dedup**: Moved from @openelement/ui to @openelement/core. Shared utility functions belong in core.\n\n## Bug Fixes\n\n- **Runtime shim `log` undefined**: Generated shim code referenced `log` but had no definition. Added `var log = {...}` stub to SHIM_BOILERPLATE.\n- **island-effect cleanup leak**: islandEffect() didn't properly clean up Signal effects in disconnectedCallback.\n- **buildIslandChunkMap double prefix**: Re-prepended `islands/` prefix, causing 404s in SSG HTML.\n- __Full console._ migration_*: All raw `console.warn/error/debug` calls replaced with `createLogger()` across core, adapter-lit, and signals packages.\n\n## Test Coverage\n\n393 unit tests passing. 10 Playwright E2E tests passing. Zero lint warnings. Zero type errors.\n\n## What's Next\n\nv0.8 audit resolution is complete. Next focus areas:\n\n- Interactive Playground for live component demos\n- Speculative Loading observability (prefetch metrics)\n- Continued @openelement/blog dogfooding and feature expansion\n\n---\n\n## v0.8.0 — Audit Resolution + Structured Logging + Blog Plugin\n\nAll five audit items resolved. Structured logging unifies framework diagnostics. Runtime shim is auto-generated. @openelement/blog ships with full dogfooding.\n\n### Highlights\n\n- **Structured Logging**: `createLogger(scope)` with unified prefixes, four levels, SILENT mode. All internals migrated.\n- **Runtime Shim Auto-Gen**: TypeScript AST-based generation eliminates manual sync risk.\n- **parse5 Nested DSD**: O(n²) → O(n×d) for complex nesting scenarios.\n- **Playwright E2E**: 10 browser-level tests against SSG output.\n- **@openelement/blog**: Vite plugin for markdown-driven blogs, dogfooded on the docs site.\n\n### Bug Fixes\n\n- **Runtime shim `log` undefined**: Added log stub to generated shim boilerplate.\n- **island-effect cleanup leak**: Proper Signal effect cleanup on disconnect.\n- **buildIslandChunkMap double prefix**: Fixed 404s in SSG HTML island scripts.\n- __Full console._ migration_*: No raw console calls remain in framework internals.\n\n### Test Coverage\n\n393 unit + 10 E2E tests passing. Zero lint warnings. Zero type errors.\n",
    "html": "<p>v0.8.0 resolves all five audit items, introduces structured logging, auto-generates the runtime shim, optimizes nested DSD rendering, and ships the @openelement/blog plugin with full dogfooding.</p>\n<h2>Audit Item Resolution</h2>\n<table>\n<thead>\n<tr>\n<th>#</th>\n<th>Priority</th>\n<th>Item</th>\n<th>Status</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>1</td>\n<td>P0</td>\n<td>Eliminate runtime-shim.ts manual sync risk</td>\n<td>✅ Done</td>\n</tr>\n<tr>\n<td>2</td>\n<td>P0</td>\n<td>Clarify error classification guide</td>\n<td>✅ Done</td>\n</tr>\n<tr>\n<td>3</td>\n<td>P1</td>\n<td>Optimize nested DSD rendering O(n²)</td>\n<td>✅ Done</td>\n</tr>\n<tr>\n<td>4</td>\n<td>P1</td>\n<td>Improve advanced feature docs</td>\n<td>✅ Done</td>\n</tr>\n<tr>\n<td>5</td>\n<td>P1</td>\n<td>Add integration tests</td>\n<td>✅ Done</td>\n</tr>\n</tbody></table>\n<h3>P0-1: Runtime Shim Auto-Generation</h3>\n<p><code>packages/core/scripts/generate-runtime-shim.ts</code> uses TypeScript AST to extract functions from source files and auto-generate <code>runtime-shim.ts</code>. No more manual sync between source and shim — run <code>deno task generate:runtime-shim</code> to regenerate, verify with <code>git diff --exit-code</code>.</p>\n<h3>P0-2: Structured Logging + Error Classification</h3>\n<p>New <code>@openelement/core/logger</code> module with <code>createLogger(scope)</code>:</p>\n<pre><code class=\"language-ts\">import { createLogger } from &#39;@openelement/core/logger&#39;;\n\nconst log = createLogger(&#39;ssg&#39;);\nlog.warn(&#39;Client build failed:&#39;, err.message);\nlog.info(&#39;Routes: 5 page(s), 2 API route(s), 8 island(s)&#39;);\nlog.debug(&#39;customElements.define(&quot;my-counter&quot;) skipped: already defined&#39;);\n</code></pre>\n<p>All framework internals migrated from raw <code>console.*</code> to structured logging. Scopes: <code>core</code> → <code>[LessJS]</code>, <code>ssg</code> → <code>[LessJS/SSG]</code>, <code>blog</code> → <code>[LessJS/Blog]</code>, <code>signal</code> → <code>[LessJS/Signal]</code>. Four log levels (DEBUG/INFO/WARN/ERROR) plus SILENT. The runtime shim includes a lightweight <code>log</code> stub that maps to <code>console.*</code> with the <code>[LessJS]</code> prefix.</p>\n<h3>P1-3: parse5 Nested DSD Optimization</h3>\n<p>Replaced regex-based O(n²) nested custom element rendering with a parse5 AST O(n×d) approach in <code>render-nested.ts</code>. Bottom-up traversal, proper DSD template detection, and attribute inference. Supports complex nesting scenarios the regex approach couldn&#39;t handle.</p>\n<h3>P1-4: Advanced Feature Docs</h3>\n<p>DSD guide (<code>/guide/dsd</code>), deep Islands guide (<code>/guide/islands-deep</code>), and RPC guide (<code>/guide/rpc</code>) — covering the three-layer DSD model, four island strategies, and cross-island communication patterns.</p>\n<h3>P1-5: Playwright E2E Tests</h3>\n<p>10 end-to-end tests covering DSD layers and nested custom elements against the built docs site:</p>\n<ul>\n<li><code>e2e/dsd-layers.spec.ts</code> — 5 tests: HTML structure, shadow roots, styles, no raw DSD text, custom element discovery</li>\n<li><code>e2e/nested-ce.spec.ts</code> — 5 tests: custom elements in DOM, no raw text, LessJS components, shadow root upgrade, navigation</li>\n</ul>\n<h2>Core Features</h2>\n<h3>Signal Native Switch</h3>\n<p><code>@openelement/signal</code> adds <code>isNativeSignal()</code> detection. When the browser supports TC39 Signals, it uses <code>globalThis.Signal</code> and falls back to polyfill automatically.</p>\n<h3>Island Upgrade Manifest</h3>\n<p><code>@openelement/core/island-manifest</code> generates per-page island manifests, replacing the global island entry. Each SSG page only loads the islands it actually uses.</p>\n<h3>@openelement/blog Package</h3>\n<p>New Vite plugin package for blog/content sites. <code>lessBlog()</code> integrates into Vite build, <code>parseMarkdownFile()</code> handles Markdown + frontmatter, <code>scanPosts()</code> + <code>generateBlogRoutes()</code> auto-generate routes. The docs site itself dogfoods this plugin — 5 blog posts served from <code>/blog</code>.</p>\n<p>v0.8 scope: <code>.md → routes → list/post pages</code>. No MDX, comments, or tags system.</p>\n<h2>Architecture Improvements</h2>\n<ul>\n<li><strong>render-dsd.ts split</strong>: 770-line monolith → 4 focused modules (core, escape, nested, barrel export). Backward-compatible.</li>\n<li><strong>UI unified to DsdLitElement</strong>: 3 components migrated to the Mixin pattern. All UI components now share the same DSD hydration mechanism.</li>\n<li><strong>insertAfterHead dedup</strong>: Moved from @openelement/ui to @openelement/core. Shared utility functions belong in core.</li>\n</ul>\n<h2>Bug Fixes</h2>\n<ul>\n<li><strong>Runtime shim <code>log</code> undefined</strong>: Generated shim code referenced <code>log</code> but had no definition. Added <code>var log = {...}</code> stub to SHIM_BOILERPLATE.</li>\n<li><strong>island-effect cleanup leak</strong>: islandEffect() didn&#39;t properly clean up Signal effects in disconnectedCallback.</li>\n<li><strong>buildIslandChunkMap double prefix</strong>: Re-prepended <code>islands/</code> prefix, causing 404s in SSG HTML.</li>\n<li><em><em>Full console.</em> migration</em>*: All raw <code>console.warn/error/debug</code> calls replaced with <code>createLogger()</code> across core, adapter-lit, and signals packages.</li>\n</ul>\n<h2>Test Coverage</h2>\n<p>393 unit tests passing. 10 Playwright E2E tests passing. Zero lint warnings. Zero type errors.</p>\n<h2>What&#39;s Next</h2>\n<p>v0.8 audit resolution is complete. Next focus areas:</p>\n<ul>\n<li>Interactive Playground for live component demos</li>\n<li>Speculative Loading observability (prefetch metrics)</li>\n<li>Continued @openelement/blog dogfooding and feature expansion</li>\n</ul>\n<hr />\n<h2>v0.8.0 — Audit Resolution + Structured Logging + Blog Plugin</h2>\n<p>All five audit items resolved. Structured logging unifies framework diagnostics. Runtime shim is auto-generated. @openelement/blog ships with full dogfooding.</p>\n<h3>Highlights</h3>\n<ul>\n<li><strong>Structured Logging</strong>: <code>createLogger(scope)</code> with unified prefixes, four levels, SILENT mode. All internals migrated.</li>\n<li><strong>Runtime Shim Auto-Gen</strong>: TypeScript AST-based generation eliminates manual sync risk.</li>\n<li><strong>parse5 Nested DSD</strong>: O(n²) → O(n×d) for complex nesting scenarios.</li>\n<li><strong>Playwright E2E</strong>: 10 browser-level tests against SSG output.</li>\n<li><strong>@openelement/blog</strong>: Vite plugin for markdown-driven blogs, dogfooded on the docs site.</li>\n</ul>\n<h3>Bug Fixes</h3>\n<ul>\n<li><strong>Runtime shim <code>log</code> undefined</strong>: Added log stub to generated shim boilerplate.</li>\n<li><strong>island-effect cleanup leak</strong>: Proper Signal effect cleanup on disconnect.</li>\n<li><strong>buildIslandChunkMap double prefix</strong>: Fixed 404s in SSG HTML island scripts.</li>\n<li><em><em>Full console.</em> migration</em>*: No raw console calls remain in framework internals.</li>\n</ul>\n<h3>Test Coverage</h3>\n<p>393 unit + 10 E2E tests passing. Zero lint warnings. Zero type errors.</p>\n"
  },
  {
    "slug": "v0-7-0",
    "frontmatter": {
      "title": "LessJS v0.7.0 — 审计修复 + 测试补全 + 部署迁移",
      "date": "2026-05-07",
      "draft": false,
      "tags": [
        "release",
        "audit"
      ],
      "excerpt": "P0 审计修复：runtime-shim 一致性、XSS 警告、静默 catch 消除。73 个新测试覆盖 render-dsd.ts 和 island.ts。部署从 GitHub Pages 迁移到 Cloudflare Pages。",
      "lang": "zh"
    },
    "content": "\nv0.7.0 是 v0.6.0 审计的直接成果。核心变化：零覆盖模块补齐测试，安全标注补全，部署流程现代化。\n\n## 测试补全\n\n### render-dsd.ts：44 个新测试\n\n770 行的核心 DSD 渲染器此前零覆盖。新增测试涵盖：\n\n- `escapeHtml`/`escapeAttr`/`escapeAttrValue` 安全函数\n- `serializeAttributes` 属性序列化\n- `renderDSD` 全路径（static/interactive/pure-island 三层）\n- L2 Nested DSD 渲染\n- XSS 安全（恶意属性值、JavaScript URI）\n- DSD options（delegatesFocus、serializable、slotAssignment）\n- pure-island layer 不输出 DSD 模板\n- Adapter protocol 和 edge cases\n\n### island.ts：29 个新测试\n\n321 行的 Island 系统此前零覆盖。新增测试涵盖：\n\n- tagName 验证（必须含连字符）\n- 元数据标记（`__island`/`__tagName`/`__layer`）\n- DSD opt-out（`dsd: false` → pure island）\n- 四种策略实现（eager/lazy/idle/visible）\n- 幂等注册（重复 define 不报错）\n- connectedCallback 包装\n- getSSRProps 属性传递\n- lessBind 框架无关绑定\n\n## 安全修复\n\n### runtime-shim 一致性\n\n`serializeAttributes()` 改用 `escapeAttrValue()`，与 `render-dsd.ts` 保持一致。此前 runtime-shim 的 `serializeAttributes` 直接调用 `escapeAttr`，跳过了 null/undefined 处理逻辑。\n\n### headExtras/headFragments XSS 警告\n\n`headExtras` 和 `headFragments` 添加 `@security` / `@dangerous` JSDoc 标注。当注入内容包含 `<script>` 标签时，运行时打印 `console.warn` 提醒开发者注意 XSS 风险。\n\n### 静默 catch 消除\n\n修复 6 处残余静默 catch 块，改为 `console.debug` 或 `console.warn`，使错误可观测。涉及文件：`island.ts`、`render-dsd.ts`、`cli/build-ssg.ts`、`cli/build-client.ts`。\n\n## 工程改进\n\n### Pre-commit Hooks\n\n`.githooks/pre-commit` 自动运行 `deno fmt --check` + `deno lint` + `deno check`，阻止格式/lint/类型错误进入仓库。通过 `deno task hooks:install` 启用。\n\n### CI 增强\n\n- `test.yml` 新增 `test-adapter-lit` job\n- `publish.yml` 添加 `needs: [test]` 依赖，确保测试通过后才能发布\n\n### 部署迁移至 Cloudflare Pages\n\n从 GitHub Pages 迁移到 Cloudflare Pages Connect GitHub 模式。main → Production（lessjs.com），dev → Preview（自动分配 URL）。\n\n## 版本更新\n\n- @openelement/core: 0.7.0\n- @openelement/adapter-lit: 0.6.3\n\n## 测试覆盖\n\n354 个单元测试全部通过（+73 新增）。零 P0/P1 级已知 Bug。\n",
    "html": "<p>v0.7.0 是 v0.6.0 审计的直接成果。核心变化：零覆盖模块补齐测试，安全标注补全，部署流程现代化。</p>\n<h2>测试补全</h2>\n<h3>render-dsd.ts：44 个新测试</h3>\n<p>770 行的核心 DSD 渲染器此前零覆盖。新增测试涵盖：</p>\n<ul>\n<li><code>escapeHtml</code>/<code>escapeAttr</code>/<code>escapeAttrValue</code> 安全函数</li>\n<li><code>serializeAttributes</code> 属性序列化</li>\n<li><code>renderDSD</code> 全路径（static/interactive/pure-island 三层）</li>\n<li>L2 Nested DSD 渲染</li>\n<li>XSS 安全（恶意属性值、JavaScript URI）</li>\n<li>DSD options（delegatesFocus、serializable、slotAssignment）</li>\n<li>pure-island layer 不输出 DSD 模板</li>\n<li>Adapter protocol 和 edge cases</li>\n</ul>\n<h3>island.ts：29 个新测试</h3>\n<p>321 行的 Island 系统此前零覆盖。新增测试涵盖：</p>\n<ul>\n<li>tagName 验证（必须含连字符）</li>\n<li>元数据标记（<code>__island</code>/<code>__tagName</code>/<code>__layer</code>）</li>\n<li>DSD opt-out（<code>dsd: false</code> → pure island）</li>\n<li>四种策略实现（eager/lazy/idle/visible）</li>\n<li>幂等注册（重复 define 不报错）</li>\n<li>connectedCallback 包装</li>\n<li>getSSRProps 属性传递</li>\n<li>lessBind 框架无关绑定</li>\n</ul>\n<h2>安全修复</h2>\n<h3>runtime-shim 一致性</h3>\n<p><code>serializeAttributes()</code> 改用 <code>escapeAttrValue()</code>，与 <code>render-dsd.ts</code> 保持一致。此前 runtime-shim 的 <code>serializeAttributes</code> 直接调用 <code>escapeAttr</code>，跳过了 null/undefined 处理逻辑。</p>\n<h3>headExtras/headFragments XSS 警告</h3>\n<p><code>headExtras</code> 和 <code>headFragments</code> 添加 <code>@security</code> / <code>@dangerous</code> JSDoc 标注。当注入内容包含 <code>&lt;script&gt;</code> 标签时，运行时打印 <code>console.warn</code> 提醒开发者注意 XSS 风险。</p>\n<h3>静默 catch 消除</h3>\n<p>修复 6 处残余静默 catch 块，改为 <code>console.debug</code> 或 <code>console.warn</code>，使错误可观测。涉及文件：<code>island.ts</code>、<code>render-dsd.ts</code>、<code>cli/build-ssg.ts</code>、<code>cli/build-client.ts</code>。</p>\n<h2>工程改进</h2>\n<h3>Pre-commit Hooks</h3>\n<p><code>.githooks/pre-commit</code> 自动运行 <code>deno fmt --check</code> + <code>deno lint</code> + <code>deno check</code>，阻止格式/lint/类型错误进入仓库。通过 <code>deno task hooks:install</code> 启用。</p>\n<h3>CI 增强</h3>\n<ul>\n<li><code>test.yml</code> 新增 <code>test-adapter-lit</code> job</li>\n<li><code>publish.yml</code> 添加 <code>needs: [test]</code> 依赖，确保测试通过后才能发布</li>\n</ul>\n<h3>部署迁移至 Cloudflare Pages</h3>\n<p>从 GitHub Pages 迁移到 Cloudflare Pages Connect GitHub 模式。main → Production（lessjs.com），dev → Preview（自动分配 URL）。</p>\n<h2>版本更新</h2>\n<ul>\n<li>@openelement/core: 0.7.0</li>\n<li>@openelement/adapter-lit: 0.6.3</li>\n</ul>\n<h2>测试覆盖</h2>\n<p>354 个单元测试全部通过（+73 新增）。零 P0/P1 级已知 Bug。</p>\n"
  },
  {
    "slug": "v0-6-x",
    "frontmatter": {
      "title": "LessJS v0.6.x — DSD 渲染模型 + Island 架构 + 三层组件模型",
      "date": "2026-05-05",
      "draft": false,
      "tags": [
        "release",
        "architecture"
      ],
      "excerpt": "v0.6.0 确立 DSD 为核心渲染模型，引入 island() Mixin 替代 monkey-patch，定义三层组件模型（dsd-static / dsd-interactive / pure-island）。v0.6.1 移除 npm 发布，全面转向 JSR。v0.6.2 统一 WithDsdHydration Mixin，消除 DsdLitElement 继承。",
      "lang": "zh"
    },
    "content": "\nv0.6 系列是 LessJS 架构的里程碑：确立了 DSD 渲染模型、Island 三层组件模型、registerAdapter 协议，并完成了从 npm 到 JSR 的迁移。\n\n## v0.6.0 — DSD + Island Mixin + registerAdapter\n\n### DSD 作为核心渲染模型\n\nLessJS 选择 Declarative Shadow DOM (DSD) 作为核心渲染模型，而非 Lit SSR 或 FAST SSR。三个原因：\n\n1. **Deno/Edge 不能运行 Node.js 依赖** — @lit-labs/ssr 依赖 Node.js stream API\n2. **Lit SSR 不输出标准 DSD** — 它使用 `<!--lit-part-->` 标记，需要客户端 cleanup\n3. **框架锁定违反 core 的 agnostic 承诺** — core 不能依赖特定框架的 SSR 方案\n\nSelf-built SSR 使用纯字符串拼接，输出标准 DSD：\n\n```html\n<my-counter>\n  <template shadowrootmode=\"open\">\n    <style>:host { display: block; }</style>\n    <button>Count: 0</button>\n  </template>\n</my-counter>\n```\n\n### island() Mixin 替代 monkey-patch\n\nv0.5 使用 `globalThis` monkey-patch 来注册 island 组件。v0.6 引入 `island()` Mixin：\n\n```ts\nimport { island } from '@openelement/core';\n\n@island({ strategy: 'lazy' })\nclass MyCounter extends LitElement { ... }\n```\n\n支持四种策略：`eager`、`lazy`（默认）、`idle`、`visible`。\n\n### registerAdapter() 替代 globalThis hooks\n\nv0.6 用显式的 `registerAdapter()` 替代所有 `globalThis` 钩子：\n\n```ts\nimport { registerAdapter } from '@openelement/core';\nimport { installLitAdapter } from '@openelement/adapter-lit';\n\nregisterAdapter({\n  isTemplate: (v) => v instanceof TemplateResult,\n  render: (v, tag) => renderLitTemplate(v, tag),\n  extractStyles: (cls) => extractLitStyles(cls),\n});\n```\n\n### CSS Custom Properties 主题系统\n\n主题系统从 `_propagateTheme()` 迁移到 CSS Custom Properties。`color-values.ts` 作为颜色 token 的 single source of truth。\n\n## v0.6.1 — JSR-only 发布\n\n移除 npm 发布，全面转向 JSR (jsr.io)。所有 6 个包现在只发布到 JSR。\n\n发布顺序（按依赖关系）：core → rpc → signal → adapter-lit → ui → create\n\n### 跨包 JSR 导入\n\n必须使用显式版本 + 子路径格式：\n\n```ts\nimport { renderDSD } from 'jsr:@openelement/core@^0.6.0/render-dsd';\n```\n\n## v0.6.2 — 三层组件模型 + WithDsdHydration Mixin\n\n### 三层组件模型\n\nv0.6.2 定义了清晰的组件分层：\n\n| Layer | 类型 | DSD 模板 | Hydration | 框架依赖 |\n|-------|------|----------|-----------|---------|\n| 1 | dsd-static | 有 | 不需要 | 无 |\n| 2 | dsd-interactive | 有 | 事件绑定 | 需要适配器 |\n| 3 | pure-island | 无 | 完整框架 | 框架拥有 shadow root |\n\n### WithDsdHydration Mixin\n\n替代继承式的 `DsdLitElement`，用组合模式：\n\n```ts\nimport { WithDsdHydration } from '@openelement/adapter-lit/dsd-hydration';\n\nclass MyComponent extends WithDsdHydration(LitElement) {\n  static hydrateEvents = [\n    { selector: 'button', event: 'click', method: '_handleClick' },\n  ];\n}\n```\n\n- `_dsdHydrated` 标志检测 DSD 场景\n- `createRenderRoot()` 检测已有 shadow root\n- `_hydrateEvents()` 绑定声明式事件\n- 直接 DOM 操作绕过 Lit reactive cycle\n\n### HydrateEventDescriptor\n\n声明式事件绑定，替代手动 `addEventListener`：\n\n```ts\ninterface HydrateEventDescriptor {\n  selector: string;  // shadow root 内的 CSS 选择器\n  event: string;     // DOM 事件名\n  method: string;    // 组件实例方法名\n}\n```\n\n### 清理：移除死代码\n\n- `LitDsdElement` — 无组件继承，替换为 Mixin\n- `RenderAdapter.hydrate()` — 注册但从未调用\n- `Hydratable` 接口 — 无组件实现\n- `_extractCeTag()` — 从未被调用\n\n## 版本演进\n\n| 版本 | 核心变化 |\n|------|---------|\n| v0.6.0 | DSD + island() + registerAdapter |\n| v0.6.1 | JSR-only，移除 npm |\n| v0.6.2 | 三层模型 + WithDsdHydration + 死代码清理 |\n",
    "html": "<p>v0.6 系列是 LessJS 架构的里程碑：确立了 DSD 渲染模型、Island 三层组件模型、registerAdapter 协议，并完成了从 npm 到 JSR 的迁移。</p>\n<h2>v0.6.0 — DSD + Island Mixin + registerAdapter</h2>\n<h3>DSD 作为核心渲染模型</h3>\n<p>LessJS 选择 Declarative Shadow DOM (DSD) 作为核心渲染模型，而非 Lit SSR 或 FAST SSR。三个原因：</p>\n<ol>\n<li><strong>Deno/Edge 不能运行 Node.js 依赖</strong> — @lit-labs/ssr 依赖 Node.js stream API</li>\n<li><strong>Lit SSR 不输出标准 DSD</strong> — 它使用 <code>&lt;!--lit-part--&gt;</code> 标记，需要客户端 cleanup</li>\n<li><strong>框架锁定违反 core 的 agnostic 承诺</strong> — core 不能依赖特定框架的 SSR 方案</li>\n</ol>\n<p>Self-built SSR 使用纯字符串拼接，输出标准 DSD：</p>\n<pre><code class=\"language-html\">&lt;my-counter&gt;\n  &lt;template shadowrootmode=&quot;open&quot;&gt;\n    &lt;style&gt;:host { display: block; }&lt;/style&gt;\n    &lt;button&gt;Count: 0&lt;/button&gt;\n  &lt;/template&gt;\n&lt;/my-counter&gt;\n</code></pre>\n<h3>island() Mixin 替代 monkey-patch</h3>\n<p>v0.5 使用 <code>globalThis</code> monkey-patch 来注册 island 组件。v0.6 引入 <code>island()</code> Mixin：</p>\n<pre><code class=\"language-ts\">import { island } from &#39;@openelement/core&#39;;\n\n@island({ strategy: &#39;lazy&#39; })\nclass MyCounter extends LitElement { ... }\n</code></pre>\n<p>支持四种策略：<code>eager</code>、<code>lazy</code>（默认）、<code>idle</code>、<code>visible</code>。</p>\n<h3>registerAdapter() 替代 globalThis hooks</h3>\n<p>v0.6 用显式的 <code>registerAdapter()</code> 替代所有 <code>globalThis</code> 钩子：</p>\n<pre><code class=\"language-ts\">import { registerAdapter } from &#39;@openelement/core&#39;;\nimport { installLitAdapter } from &#39;@openelement/adapter-lit&#39;;\n\nregisterAdapter({\n  isTemplate: (v) =&gt; v instanceof TemplateResult,\n  render: (v, tag) =&gt; renderLitTemplate(v, tag),\n  extractStyles: (cls) =&gt; extractLitStyles(cls),\n});\n</code></pre>\n<h3>CSS Custom Properties 主题系统</h3>\n<p>主题系统从 <code>_propagateTheme()</code> 迁移到 CSS Custom Properties。<code>color-values.ts</code> 作为颜色 token 的 single source of truth。</p>\n<h2>v0.6.1 — JSR-only 发布</h2>\n<p>移除 npm 发布，全面转向 JSR (jsr.io)。所有 6 个包现在只发布到 JSR。</p>\n<p>发布顺序（按依赖关系）：core → rpc → signal → adapter-lit → ui → create</p>\n<h3>跨包 JSR 导入</h3>\n<p>必须使用显式版本 + 子路径格式：</p>\n<pre><code class=\"language-ts\">import { renderDSD } from &#39;jsr:@openelement/core@^0.6.0/render-dsd&#39;;\n</code></pre>\n<h2>v0.6.2 — 三层组件模型 + WithDsdHydration Mixin</h2>\n<h3>三层组件模型</h3>\n<p>v0.6.2 定义了清晰的组件分层：</p>\n<table>\n<thead>\n<tr>\n<th>Layer</th>\n<th>类型</th>\n<th>DSD 模板</th>\n<th>Hydration</th>\n<th>框架依赖</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>1</td>\n<td>dsd-static</td>\n<td>有</td>\n<td>不需要</td>\n<td>无</td>\n</tr>\n<tr>\n<td>2</td>\n<td>dsd-interactive</td>\n<td>有</td>\n<td>事件绑定</td>\n<td>需要适配器</td>\n</tr>\n<tr>\n<td>3</td>\n<td>pure-island</td>\n<td>无</td>\n<td>完整框架</td>\n<td>框架拥有 shadow root</td>\n</tr>\n</tbody></table>\n<h3>WithDsdHydration Mixin</h3>\n<p>替代继承式的 <code>DsdLitElement</code>，用组合模式：</p>\n<pre><code class=\"language-ts\">import { WithDsdHydration } from &#39;@openelement/adapter-lit/dsd-hydration&#39;;\n\nclass MyComponent extends WithDsdHydration(LitElement) {\n  static hydrateEvents = [\n    { selector: &#39;button&#39;, event: &#39;click&#39;, method: &#39;_handleClick&#39; },\n  ];\n}\n</code></pre>\n<ul>\n<li><code>_dsdHydrated</code> 标志检测 DSD 场景</li>\n<li><code>createRenderRoot()</code> 检测已有 shadow root</li>\n<li><code>_hydrateEvents()</code> 绑定声明式事件</li>\n<li>直接 DOM 操作绕过 Lit reactive cycle</li>\n</ul>\n<h3>HydrateEventDescriptor</h3>\n<p>声明式事件绑定，替代手动 <code>addEventListener</code>：</p>\n<pre><code class=\"language-ts\">interface HydrateEventDescriptor {\n  selector: string;  // shadow root 内的 CSS 选择器\n  event: string;     // DOM 事件名\n  method: string;    // 组件实例方法名\n}\n</code></pre>\n<h3>清理：移除死代码</h3>\n<ul>\n<li><code>LitDsdElement</code> — 无组件继承，替换为 Mixin</li>\n<li><code>RenderAdapter.hydrate()</code> — 注册但从未调用</li>\n<li><code>Hydratable</code> 接口 — 无组件实现</li>\n<li><code>_extractCeTag()</code> — 从未被调用</li>\n</ul>\n<h2>版本演进</h2>\n<table>\n<thead>\n<tr>\n<th>版本</th>\n<th>核心变化</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>v0.6.0</td>\n<td>DSD + island() + registerAdapter</td>\n</tr>\n<tr>\n<td>v0.6.1</td>\n<td>JSR-only，移除 npm</td>\n</tr>\n<tr>\n<td>v0.6.2</td>\n<td>三层模型 + WithDsdHydration + 死代码清理</td>\n</tr>\n</tbody></table>\n"
  },
  {
    "slug": "v0-5-alpha1",
    "frontmatter": {
      "title": "v0.5-alpha1 — 全量架构审计与精准修复",
      "date": "2026-05-03",
      "draft": false,
      "tags": [
        "release",
        "audit"
      ],
      "excerpt": "3 agent 扫描 13k 行源码 · CSS 注入修复 · Island 升级修复 · 6 条新设计原则 · 配置精简",
      "lang": "zh"
    },
    "content": "\n3 agent 扫描 13k 行源码 · CSS 注入修复 · Island 升级修复 · 6 条新设计原则 · 配置精简\n\n## 发现的问题\n\n### Bug 1: CSS 注入顺序错乱\n\nCSS Custom Properties 主题系统替代了旧的 `_propagateTheme` DOM 遍历，但注入顺序不正确导致暗色模式下部分组件闪烁。修复：确保 theme CSS 在 component CSS 之前注入。\n\n### Bug 2: Island 升级路径断裂\n\n`idle` 策略的 `requestIdleCallback` 降级在部分浏览器上没有正确回退到 `setTimeout`，导致 island 组件永远不加载。修复：添加 `setTimeout` 兜底。\n\n### Bug 3: 配置冗余\n\n`kiss.config.ts` 中有多个已废弃的配置项仍然被读取，增加了维护负担和出错概率。修复：移除废弃配置，精简为最小配置集。\n\n## 6 条新设计原则\n\n1. **Web Standards first** — 优先使用平台原语，不造框架抽象层\n2. **Static first** — 动态运行时行为必须显式声明\n3. **DSD first** — JavaScript 运行前 HTML 已可读\n4. **Island upgrade** — 客户端 JavaScript 升级交互组件，而非整页\n5. **TC39 Signals first** — 跟标准走，框架造自己的 effect/islandEffect API\n6. **Docs are falsifiable** — 当前指南描述当前行为，未来工作留在 roadmap\n\n## 配置精简\n\n移除了 `kiss.config.ts` 中已废弃的 `legacyMode`、`experimentalDSD`、`customIslandStrategy` 等配置项。当前配置仅保留：\n\n- `routesDir` — 路由目录\n- `islandsDir` — Island 目录\n- `componentsDir` — 共享组件目录\n- `packageIslands` — 包级 Island 声明\n\n## 修复统计\n\n- CSS 注入修复：3 处\n- Island 升级修复：2 处\n- 配置精简：5 个废弃项移除\n- 新增测试：12 个\n",
    "html": "<p>3 agent 扫描 13k 行源码 · CSS 注入修复 · Island 升级修复 · 6 条新设计原则 · 配置精简</p>\n<h2>发现的问题</h2>\n<h3>Bug 1: CSS 注入顺序错乱</h3>\n<p>CSS Custom Properties 主题系统替代了旧的 <code>_propagateTheme</code> DOM 遍历，但注入顺序不正确导致暗色模式下部分组件闪烁。修复：确保 theme CSS 在 component CSS 之前注入。</p>\n<h3>Bug 2: Island 升级路径断裂</h3>\n<p><code>idle</code> 策略的 <code>requestIdleCallback</code> 降级在部分浏览器上没有正确回退到 <code>setTimeout</code>，导致 island 组件永远不加载。修复：添加 <code>setTimeout</code> 兜底。</p>\n<h3>Bug 3: 配置冗余</h3>\n<p><code>kiss.config.ts</code> 中有多个已废弃的配置项仍然被读取，增加了维护负担和出错概率。修复：移除废弃配置，精简为最小配置集。</p>\n<h2>6 条新设计原则</h2>\n<ol>\n<li><strong>Web Standards first</strong> — 优先使用平台原语，不造框架抽象层</li>\n<li><strong>Static first</strong> — 动态运行时行为必须显式声明</li>\n<li><strong>DSD first</strong> — JavaScript 运行前 HTML 已可读</li>\n<li><strong>Island upgrade</strong> — 客户端 JavaScript 升级交互组件，而非整页</li>\n<li><strong>TC39 Signals first</strong> — 跟标准走，框架造自己的 effect/islandEffect API</li>\n<li><strong>Docs are falsifiable</strong> — 当前指南描述当前行为，未来工作留在 roadmap</li>\n</ol>\n<h2>配置精简</h2>\n<p>移除了 <code>kiss.config.ts</code> 中已废弃的 <code>legacyMode</code>、<code>experimentalDSD</code>、<code>customIslandStrategy</code> 等配置项。当前配置仅保留：</p>\n<ul>\n<li><code>routesDir</code> — 路由目录</li>\n<li><code>islandsDir</code> — Island 目录</li>\n<li><code>componentsDir</code> — 共享组件目录</li>\n<li><code>packageIslands</code> — 包级 Island 声明</li>\n</ul>\n<h2>修复统计</h2>\n<ul>\n<li>CSS 注入修复：3 处</li>\n<li>Island 升级修复：2 处</li>\n<li>配置精简：5 个废弃项移除</li>\n<li>新增测试：12 个</li>\n</ul>\n"
  },
  {
    "slug": "v0-5-0",
    "frontmatter": {
      "title": "LessJS v0.5-alpha-0 — 架构精简",
      "date": "2026-05-02",
      "draft": false,
      "tags": [
        "release",
        "architecture"
      ],
      "excerpt": "零框架运行时 Core · 原生 RPC · OpenProps + Lit · 单 deno.json",
      "lang": "zh"
    },
    "content": "\n零框架运行时 Core · 原生 RPC · OpenProps + Lit · 单 deno.json\n\n## 核心变更\n\n### 零框架运行时 Core\n\n将 `@openelement/core` 拆分为构建时工具 + 零运行时。Core 在 SSG 构建后不进入客户端 bundle。页面只携带：\n\n- 主题初始化脚本（~200 bytes）\n- Service Worker 注册（~200 bytes）\n- Island chunk（按需，每个 ~2-6 KB gzip）\n\n对比之前 Core 运行时 ~15KB，这是数量级的减小。\n\n### 原生 RPC\n\n`@openelement/rpc` 基于 Hono RPC 的类型安全客户端。不再使用代码生成，直接从 Hono 路由定义推断类型：\n\n```typescript\n// server\napp.post('/api/ping', (c) => c.json({ ok: true }));\n\n// client — 类型自动推断\nconst res = await rpc.ping.$post();\n```\n\n### OpenProps + Lit\n\nUI 包从硬编码 CSS 值迁移到 OpenProps 设计令牌。所有颜色、间距、字体大小使用 CSS Custom Properties，支持暗色模式零成本切换。\n\n### 单 deno.json\n\n项目配置从 `kiss.config.ts` + `deno.json` + `tsconfig.json` 三个文件合并为单一 `deno.json`。Deno workspace 管理 monorepo 依赖，不再需要 npm workarounds。\n\n## 包结构\n\n| 包                  | 版本  | 说明                       |\n| ------------------- | ----- | -------------------------- |\n| @openelement/core        | 0.5.0 | Vite 插件 + SSG + DSD 渲染 |\n| @openelement/rpc         | 0.5.0 | 类型安全 RPC 客户端        |\n| @openelement/ui          | 0.5.0 | Lit Web Component 库       |\n| @openelement/adapter-lit | 0.5.0 | Lit SSR 适配器             |\n| @openelement/create      | 0.5.0 | 项目脚手架 CLI             |\n",
    "html": "<p>零框架运行时 Core · 原生 RPC · OpenProps + Lit · 单 deno.json</p>\n<h2>核心变更</h2>\n<h3>零框架运行时 Core</h3>\n<p>将 <code>@openelement/core</code> 拆分为构建时工具 + 零运行时。Core 在 SSG 构建后不进入客户端 bundle。页面只携带：</p>\n<ul>\n<li>主题初始化脚本（~200 bytes）</li>\n<li>Service Worker 注册（~200 bytes）</li>\n<li>Island chunk（按需，每个 ~2-6 KB gzip）</li>\n</ul>\n<p>对比之前 Core 运行时 ~15KB，这是数量级的减小。</p>\n<h3>原生 RPC</h3>\n<p><code>@openelement/rpc</code> 基于 Hono RPC 的类型安全客户端。不再使用代码生成，直接从 Hono 路由定义推断类型：</p>\n<pre><code class=\"language-typescript\">// server\napp.post(&#39;/api/ping&#39;, (c) =&gt; c.json({ ok: true }));\n\n// client — 类型自动推断\nconst res = await rpc.ping.$post();\n</code></pre>\n<h3>OpenProps + Lit</h3>\n<p>UI 包从硬编码 CSS 值迁移到 OpenProps 设计令牌。所有颜色、间距、字体大小使用 CSS Custom Properties，支持暗色模式零成本切换。</p>\n<h3>单 deno.json</h3>\n<p>项目配置从 <code>kiss.config.ts</code> + <code>deno.json</code> + <code>tsconfig.json</code> 三个文件合并为单一 <code>deno.json</code>。Deno workspace 管理 monorepo 依赖，不再需要 npm workarounds。</p>\n<h2>包结构</h2>\n<table>\n<thead>\n<tr>\n<th>包</th>\n<th>版本</th>\n<th>说明</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>@openelement/core</td>\n<td>0.5.0</td>\n<td>Vite 插件 + SSG + DSD 渲染</td>\n</tr>\n<tr>\n<td>@openelement/rpc</td>\n<td>0.5.0</td>\n<td>类型安全 RPC 客户端</td>\n</tr>\n<tr>\n<td>@openelement/ui</td>\n<td>0.5.0</td>\n<td>Lit Web Component 库</td>\n</tr>\n<tr>\n<td>@openelement/adapter-lit</td>\n<td>0.5.0</td>\n<td>Lit SSR 适配器</td>\n</tr>\n<tr>\n<td>@openelement/create</td>\n<td>0.5.0</td>\n<td>项目脚手架 CLI</td>\n</tr>\n</tbody></table>\n"
  },
  {
    "slug": "v0-4-0",
    "frontmatter": {
      "title": "LessJS v0.4.0 — Serverless Integration Milestone",
      "date": "2026-04-30",
      "draft": false,
      "tags": [
        "release",
        "serverless"
      ],
      "excerpt": "Serverless API 部署成功、全站统一 0.5px 视觉风格、零 lint 零 type errors。从\"能跑起来\"到\"真正能用\"的里程碑。",
      "lang": "zh"
    },
    "content": "\nServerless API 部署成功、全站统一 0.5px 视觉风格、零 lint 零 type errors。从\"能跑起来\"到\"真正能用\"的里程碑。\n\nLessJS v0.4.0 只有一个核心主题：**Serverless 集成跑通了**。\n\n从 v0.3.2 开始，LessJS 的架构文档里就写着 \"API Routes 可以部署为 Serverless\"，但直到这个版本，它才真正 CI 自动化、真正跑在生产环境上。\n\n**v0.3.2 → v0.4.0：90 commits · 103 files · +3904 / −1810**\n\n## Serverless API 的落地之路\n\n| 阶段         | 问题                                      | 解决方案                                    |\n| ------------ | ----------------------------------------- | ------------------------------------------- |\n| 部署平台迁移 | `deployctl`（旧平台 Classic）废弃         | 迁移到 `deno deploy`（新平台 v2）           |\n| CORS         | `hono/cors` 中间件在 Deno Deploy 上不兼容 | 手动设置 Access-Control-Allow-Origin        |\n| 入口路径     | deployctl 和 deno deploy 的入口解析不一致 | 用 `--entrypoint=serverless.ts` 明确指定    |\n| Import Map   | Deno Deploy 读不到 workspace 配置         | 从 clean temp dir 部署，自带 deno.json      |\n| CI 触发      | 只 push main + demo/** 变更               | `deploy-api.yml` 自动化 + workflow_dispatch |\n\n最终成果：`less-demo-api.sisyphuszheng.deno.net` 生产在线，CI 自动部署。这是 LessJS Jamstack 承诺的 \"J\"（Markup）+ \"A\"（API）+ \"M\"（Markup）全链路闭环。\n\n## 代码质量\n\n| 指标                | v0.3.2 | v0.4.0             |\n| ------------------- | ------ | ------------------ |\n| Lint 错误           | 有残留 | 0                  |\n| TypeScript 类型     | 有残留 | 0 errors           |\n| UI 测试通过         | 部分   | 84/84              |\n| 全站 1px 残留       | 40+ 处 | 0                  |\n| 硬编码色（非 Hero） | 大量   | 0（全部 CSS 变量） |\n\n## 关于\"零 JS\"的实话\n\n**LessJS 不是零 JS，而是零框架 JS。** 每个页面有约 400 字节的内联基础设施脚本（主题初始化 + Service Worker 注册）。这是不可消除的——它们是 L2 层（平台 API），在 I 约束（Isolated）的豁免范围内。但 Lit / Hono / 框架核心代码的确是 0 字节。零交互页面只加载这 ~400 字节。对比之下 Fresh 每页 ~1KB，Nuxt ~60KB，Next.js ~70KB。有交互的页面按需加载 Island chunk，每个 ~2-6 KB gzip，懒加载 + 独立 Shadow DOM。\n",
    "html": "<p>Serverless API 部署成功、全站统一 0.5px 视觉风格、零 lint 零 type errors。从&quot;能跑起来&quot;到&quot;真正能用&quot;的里程碑。</p>\n<p>LessJS v0.4.0 只有一个核心主题：<strong>Serverless 集成跑通了</strong>。</p>\n<p>从 v0.3.2 开始，LessJS 的架构文档里就写着 &quot;API Routes 可以部署为 Serverless&quot;，但直到这个版本，它才真正 CI 自动化、真正跑在生产环境上。</p>\n<p><strong>v0.3.2 → v0.4.0：90 commits · 103 files · +3904 / −1810</strong></p>\n<h2>Serverless API 的落地之路</h2>\n<table>\n<thead>\n<tr>\n<th>阶段</th>\n<th>问题</th>\n<th>解决方案</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>部署平台迁移</td>\n<td><code>deployctl</code>（旧平台 Classic）废弃</td>\n<td>迁移到 <code>deno deploy</code>（新平台 v2）</td>\n</tr>\n<tr>\n<td>CORS</td>\n<td><code>hono/cors</code> 中间件在 Deno Deploy 上不兼容</td>\n<td>手动设置 Access-Control-Allow-Origin</td>\n</tr>\n<tr>\n<td>入口路径</td>\n<td>deployctl 和 deno deploy 的入口解析不一致</td>\n<td>用 <code>--entrypoint=serverless.ts</code> 明确指定</td>\n</tr>\n<tr>\n<td>Import Map</td>\n<td>Deno Deploy 读不到 workspace 配置</td>\n<td>从 clean temp dir 部署，自带 deno.json</td>\n</tr>\n<tr>\n<td>CI 触发</td>\n<td>只 push main + demo/** 变更</td>\n<td><code>deploy-api.yml</code> 自动化 + workflow_dispatch</td>\n</tr>\n</tbody></table>\n<p>最终成果：<code>less-demo-api.sisyphuszheng.deno.net</code> 生产在线，CI 自动部署。这是 LessJS Jamstack 承诺的 &quot;J&quot;（Markup）+ &quot;A&quot;（API）+ &quot;M&quot;（Markup）全链路闭环。</p>\n<h2>代码质量</h2>\n<table>\n<thead>\n<tr>\n<th>指标</th>\n<th>v0.3.2</th>\n<th>v0.4.0</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Lint 错误</td>\n<td>有残留</td>\n<td>0</td>\n</tr>\n<tr>\n<td>TypeScript 类型</td>\n<td>有残留</td>\n<td>0 errors</td>\n</tr>\n<tr>\n<td>UI 测试通过</td>\n<td>部分</td>\n<td>84/84</td>\n</tr>\n<tr>\n<td>全站 1px 残留</td>\n<td>40+ 处</td>\n<td>0</td>\n</tr>\n<tr>\n<td>硬编码色（非 Hero）</td>\n<td>大量</td>\n<td>0（全部 CSS 变量）</td>\n</tr>\n</tbody></table>\n<h2>关于&quot;零 JS&quot;的实话</h2>\n<p><strong>LessJS 不是零 JS，而是零框架 JS。</strong> 每个页面有约 400 字节的内联基础设施脚本（主题初始化 + Service Worker 注册）。这是不可消除的——它们是 L2 层（平台 API），在 I 约束（Isolated）的豁免范围内。但 Lit / Hono / 框架核心代码的确是 0 字节。零交互页面只加载这 ~400 字节。对比之下 Fresh 每页 ~1KB，Nuxt ~60KB，Next.js ~70KB。有交互的页面按需加载 Island chunk，每个 ~2-6 KB gzip，懒加载 + 独立 Shadow DOM。</p>\n"
  },
  {
    "slug": "less-compiler",
    "frontmatter": {
      "title": ".less Compiler — 可选零框架运行时组件",
      "date": "2026-04-30",
      "draft": false,
      "tags": [
        "architecture",
        "compiler"
      ],
      "excerpt": "一个可选编译器将声明式 .less 文件编译为原生 Custom Elements，让 Lit 从必选路线变成 adapter。",
      "lang": "zh"
    },
    "content": "\n一个可选编译器将声明式 .less 文件编译为原生 Custom Elements，让 Lit 从必选路线变成 adapter。\n\nLessJS 框架从第一天起就选择了 Lit 作为组件基础。这个选择是对的——Lit 是 Web Components 生态中最成熟的库，让我们快速验证了架构的可行性。但经过后续架构审查，我们也更清楚地看到：core 的长期合同不能绑定到某个组件库。Lit 应该保留为 adapter，而不是成为用户必须接受的唯一组件模型。\n\n## 今天的代价\n\n依赖 Lit 编写的 island 会携带 Lit 运行时；SSR/style extraction 需要 adapter 维护；旧 Lit SSR 路线留下的 hydration 术语又容易和现在的 DSD + Custom Element upgrade 模型混淆。结论不是\"消灭 Lit\"，而是把 Lit 放回正确的位置：一个好 adapter，而不是 LessJS 的定义本身。\n\n## .less 文件格式\n\n一个组件一个文件。没有 class 声明，没有 decorator，没有 import：\n\n```html\n<!-- my-counter.less -->\n<template>\n  <button @click=\"decrement\">−</button>\n  <span>{count}</span>\n  <button @click=\"increment\">+</button>\n</template>\n<script>\n  count = 0\n  increment() { this.count++ }\n  decrement() { this.count-- }\n</script>\n<style>\n  :host { display: inline-flex; gap: 0.5rem; align-items: center; }\n</style>\n```\n\n## 编译器产出\n\n零依赖的原生 Custom Element：\n\n```javascript\nclass MyCounter extends HTMLElement {\n  #count = 0;\n  #root = this.attachShadow({ mode: 'open' });\n  get count() {\n    return this.#count;\n  }\n  set count(v) {\n    this.#count = v;\n    this.#update();\n  }\n  connectedCallback() {\n    this.#root.append(tpl.content.cloneNode(true));\n    this.#root.querySelector('button:first-child').onclick = () => this.count--;\n    this.#root.querySelector('button:last-child').onclick = () => this.count++;\n  }\n}\n```\n\n## 消除清单\n\n- Lit-authored islands 的框架运行时代价 → 编译产物 0 KB framework runtime\n- adapter-mediated SSR → LessJS DSD renderer / template strings\n- hydration 术语漂移 → 明确的 Custom Element upgrade\n- decorator / tagged template 生态复杂度 → 标准 JS 输出\n- 复杂的类型层次 → 简单的 getter/setter\n\n## 路线\n\n这项工作不应该阻塞 v0.7–v0.10。当前路线是：先修可信度、安全、DSD renderer、Island Upgrade、Serverless Fullstack 与 SSG/ISR，再在 v0.11.0 引入 `.less` compiler alpha。Lit 兼容模式在 v0.x 生命周期中保留。版本策略详见 [ADR 0006](/blog/0006-version-strategy)。\n\n详细技术设计见 [ADR 0002](/blog/0002-less-compiler-eliminate-lit)。\n",
    "html": "<p>一个可选编译器将声明式 .less 文件编译为原生 Custom Elements，让 Lit 从必选路线变成 adapter。</p>\n<p>LessJS 框架从第一天起就选择了 Lit 作为组件基础。这个选择是对的——Lit 是 Web Components 生态中最成熟的库，让我们快速验证了架构的可行性。但经过后续架构审查，我们也更清楚地看到：core 的长期合同不能绑定到某个组件库。Lit 应该保留为 adapter，而不是成为用户必须接受的唯一组件模型。</p>\n<h2>今天的代价</h2>\n<p>依赖 Lit 编写的 island 会携带 Lit 运行时；SSR/style extraction 需要 adapter 维护；旧 Lit SSR 路线留下的 hydration 术语又容易和现在的 DSD + Custom Element upgrade 模型混淆。结论不是&quot;消灭 Lit&quot;，而是把 Lit 放回正确的位置：一个好 adapter，而不是 LessJS 的定义本身。</p>\n<h2>.less 文件格式</h2>\n<p>一个组件一个文件。没有 class 声明，没有 decorator，没有 import：</p>\n<pre><code class=\"language-html\">&lt;!-- my-counter.less --&gt;\n&lt;template&gt;\n  &lt;button @click=&quot;decrement&quot;&gt;−&lt;/button&gt;\n  &lt;span&gt;{count}&lt;/span&gt;\n  &lt;button @click=&quot;increment&quot;&gt;+&lt;/button&gt;\n&lt;/template&gt;\n&lt;script&gt;\n  count = 0\n  increment() { this.count++ }\n  decrement() { this.count-- }\n&lt;/script&gt;\n&lt;style&gt;\n  :host { display: inline-flex; gap: 0.5rem; align-items: center; }\n&lt;/style&gt;\n</code></pre>\n<h2>编译器产出</h2>\n<p>零依赖的原生 Custom Element：</p>\n<pre><code class=\"language-javascript\">class MyCounter extends HTMLElement {\n  #count = 0;\n  #root = this.attachShadow({ mode: &#39;open&#39; });\n  get count() {\n    return this.#count;\n  }\n  set count(v) {\n    this.#count = v;\n    this.#update();\n  }\n  connectedCallback() {\n    this.#root.append(tpl.content.cloneNode(true));\n    this.#root.querySelector(&#39;button:first-child&#39;).onclick = () =&gt; this.count--;\n    this.#root.querySelector(&#39;button:last-child&#39;).onclick = () =&gt; this.count++;\n  }\n}\n</code></pre>\n<h2>消除清单</h2>\n<ul>\n<li>Lit-authored islands 的框架运行时代价 → 编译产物 0 KB framework runtime</li>\n<li>adapter-mediated SSR → LessJS DSD renderer / template strings</li>\n<li>hydration 术语漂移 → 明确的 Custom Element upgrade</li>\n<li>decorator / tagged template 生态复杂度 → 标准 JS 输出</li>\n<li>复杂的类型层次 → 简单的 getter/setter</li>\n</ul>\n<h2>路线</h2>\n<p>这项工作不应该阻塞 v0.7–v0.10。当前路线是：先修可信度、安全、DSD renderer、Island Upgrade、Serverless Fullstack 与 SSG/ISR，再在 v0.11.0 引入 <code>.less</code> compiler alpha。Lit 兼容模式在 v0.x 生命周期中保留。版本策略详见 <a href=\"/blog/0006-version-strategy\" rel=\"noopener noreferrer\">ADR 0006</a>。</p>\n<p>详细技术设计见 <a href=\"/blog/0002-less-compiler-eliminate-lit\" rel=\"noopener noreferrer\">ADR 0002</a>。</p>\n"
  },
  {
    "slug": "0100-three-audits-later-the-stable-line",
    "frontmatter": {
      "title": "Three audits later, the stable line",
      "date": "2026-07-26",
      "draft": false,
      "tags": [
        "release-truth",
        "architecture",
        "audit"
      ],
      "type": "dispatch",
      "lang": "en"
    },
    "content": "\nIn July we audited the whole repository three times. Not a vendor, not a\nchecklist — a line-by-line adversarial read of the renderer, the router, the\nrelease tooling, and every document that claims something about them. This is\nwhat three rounds found in our own framework, and why 0.41.0 freezes only\nwhat the evidence proves.\n\n> The markup is the application — so the markup gets audited.\n\n## Round one: correctness was not what the gates said\n\nThe first audit (ADR-0116) found defects no qualification gate had caught:\n`observedAttributes` registered at `connectedCallback` time, which real\nbrowsers read once at `customElements.define` — attribute-to-signal\nsynchronization never fired outside our DOM shim. SSR-to-hydration event\nmarkers relied on an implicit identical traversal order on both sides, and\nruntime-dependent `<Show>`/`<For>` branches could deterministically misalign\nhandler binding. The island chunk matcher forgot the base64url alphabet and\nsilently dropped islands. The published `latest` dist-tag pointed six alphas\nback.\n\nEvery one of those shipped behind green CI. The lesson that stuck: a gate\nthat cannot see a class of failure is evidence for nothing.\n\n## Round two: the half-fix is the default outcome\n\nThe second audit (ADR-0117) verified the first round's repairs and found the\nmeta-pattern: fixes closed one path and left siblings open. The anchor gate\nasserted new version claims but never rejected stale ones. The missing-closure\nCI failure was fixed on the CI path but not on local `patch-release`. And\n`reflect: true` static props — a publicly documented feature — was broken in\nall three browser engines with zero test coverage: write-loop storms and SSR\nattributes overwritten at connect.\n\nAlpha.18 therefore shipped with a rule we now treat as doctrine: every fix\nenumerates its sibling paths, or it does not count as a fix.\n\n## Round three: the bugs moved to the combinations\n\nThe third round (ADR-0118, issues #481–#506) found no correctness bugs on the\nmain paths at all. What remained was the combinations: the reflect short\ncircuit over-suppressing on `removeAttribute` when the value equals the\ndefault, a popstate redirect whose target guard then blocks — the browser URL\nand router state silently forking — a `For` drift token colliding on\nseparator smuggling, and the gates' own coverage edges (header zones, regex\nprefixes, four-file whitelists).\n\nThat is what a framework approaching a freeze looks like: the defects migrate\nfrom the mechanism to the edges of the mechanism's evidence.\n\n## What we froze, and what we did not\n\nADR-0119 records the decision. Frozen in 0.41.0: `defineElement`,\n`definePage`, `buildApp`, the static and SPA semantics of `defineApp` as\nshipped, the five-package graph, and the supported subpaths of\n`PACKAGE_SURFACE.md`. Explicitly not frozen: request-time data, forms,\nsessions and cache — those belong to the Application Loop (0.42) and the\nProduction Runtime (0.44), and pretending otherwise would be the same kind of\noverclaim the audits kept finding.\n\nThe external adopter pilot #390 was retired by maintainer decision after zero\nrecruitment across three release cycles. We did not substitute internal CI\nfor external evidence; we recorded the exception and narrowed the freeze to\nwhat three audits and the consumer matrix actually prove.\n\nThe stable line is not a claim that nothing will break. It is a claim about\nwhich interfaces we will not break — written down, gated, and small enough to\nkeep.\n",
    "html": "<p>In July we audited the whole repository three times. Not a vendor, not a\nchecklist — a line-by-line adversarial read of the renderer, the router, the\nrelease tooling, and every document that claims something about them. This is\nwhat three rounds found in our own framework, and why 0.41.0 freezes only\nwhat the evidence proves.</p>\n<blockquote>\n<p>The markup is the application — so the markup gets audited.</p>\n</blockquote>\n<h2>Round one: correctness was not what the gates said</h2>\n<p>The first audit (ADR-0116) found defects no qualification gate had caught:\n<code>observedAttributes</code> registered at <code>connectedCallback</code> time, which real\nbrowsers read once at <code>customElements.define</code> — attribute-to-signal\nsynchronization never fired outside our DOM shim. SSR-to-hydration event\nmarkers relied on an implicit identical traversal order on both sides, and\nruntime-dependent <code>&lt;Show&gt;</code>/<code>&lt;For&gt;</code> branches could deterministically misalign\nhandler binding. The island chunk matcher forgot the base64url alphabet and\nsilently dropped islands. The published <code>latest</code> dist-tag pointed six alphas\nback.</p>\n<p>Every one of those shipped behind green CI. The lesson that stuck: a gate\nthat cannot see a class of failure is evidence for nothing.</p>\n<h2>Round two: the half-fix is the default outcome</h2>\n<p>The second audit (ADR-0117) verified the first round&#39;s repairs and found the\nmeta-pattern: fixes closed one path and left siblings open. The anchor gate\nasserted new version claims but never rejected stale ones. The missing-closure\nCI failure was fixed on the CI path but not on local <code>patch-release</code>. And\n<code>reflect: true</code> static props — a publicly documented feature — was broken in\nall three browser engines with zero test coverage: write-loop storms and SSR\nattributes overwritten at connect.</p>\n<p>Alpha.18 therefore shipped with a rule we now treat as doctrine: every fix\nenumerates its sibling paths, or it does not count as a fix.</p>\n<h2>Round three: the bugs moved to the combinations</h2>\n<p>The third round (ADR-0118, issues #481–#506) found no correctness bugs on the\nmain paths at all. What remained was the combinations: the reflect short\ncircuit over-suppressing on <code>removeAttribute</code> when the value equals the\ndefault, a popstate redirect whose target guard then blocks — the browser URL\nand router state silently forking — a <code>For</code> drift token colliding on\nseparator smuggling, and the gates&#39; own coverage edges (header zones, regex\nprefixes, four-file whitelists).</p>\n<p>That is what a framework approaching a freeze looks like: the defects migrate\nfrom the mechanism to the edges of the mechanism&#39;s evidence.</p>\n<h2>What we froze, and what we did not</h2>\n<p>ADR-0119 records the decision. Frozen in 0.41.0: <code>defineElement</code>,\n<code>definePage</code>, <code>buildApp</code>, the static and SPA semantics of <code>defineApp</code> as\nshipped, the five-package graph, and the supported subpaths of\n<code>PACKAGE_SURFACE.md</code>. Explicitly not frozen: request-time data, forms,\nsessions and cache — those belong to the Application Loop (0.42) and the\nProduction Runtime (0.44), and pretending otherwise would be the same kind of\noverclaim the audits kept finding.</p>\n<p>The external adopter pilot #390 was retired by maintainer decision after zero\nrecruitment across three release cycles. We did not substitute internal CI\nfor external evidence; we recorded the exception and narrowed the freeze to\nwhat three audits and the consumer matrix actually prove.</p>\n<p>The stable line is not a claim that nothing will break. It is a claim about\nwhich interfaces we will not break — written down, gated, and small enough to\nkeep.</p>\n"
  },
  {
    "slug": "0024-standards-first-wc-renderer-roadmap",
    "frontmatter": {
      "title": "ADR 0024: Standards-first Web Components Renderer Roadmap",
      "date": "2026-05-14",
      "draft": false,
      "tags": [
        "architecture",
        "roadmap",
        "standards",
        "web-components",
        "dsd",
        "ssg"
      ],
      "type": "adr",
      "lang": "en"
    },
    "content": "\n# ADR 0024: Standards-first Web Components Renderer Roadmap\n\n## Status\n\n**ACCEPTED** - 2026-05-14 strategic reset.\n\n**Updated** - 2026-05-16 to align the public roadmap with the WC SSR/SSG\nrenderer-kernel and registry-hub feasibility review.\n\n## Context\n\nLessJS has reached a point where the next roadmap cannot be a checklist of\nexternal ecosystem ideas. The project already has its own strongest axis:\n\n- Declarative Shadow DOM rendering through `renderDSD()` and nested custom\n  element rendering.\n- SSR bundle ownership through generated `renderRoute()`, `routeInfo`, and\n  `getStaticPaths()`.\n- A Vite-free SSG rendering path through `ssgRender()`.\n- Package island discovery through `PackageIslandMeta`.\n- Deno-first validation, self-hosted documentation, and Playwright browser\n  verification.\n\nThe strategic risk is to dilute this position by chasing older Web Components\ntooling. OpenWC remains valuable as ecosystem history and component-contract\nexperience, but LessJS should not adopt OpenWC's project template or legacy test\nstack as its roadmap spine. Webpack and Rollup/OpenWC build presets are also not\naligned with the current LessJS direction: Vite is already narrowed to dev/client\nbundling, and Phase 3 is moving toward Vite-free ESM SSG.\n\nOpen UI remains useful, but as a contract vocabulary: parts, states, events,\naccessibility behavior, form behavior, and design tokens. It should inform how\nLessJS describes components, not turn LessJS into an OpenWC clone.\n\nThe 2026-05-16 feasibility review clarifies one more boundary: \"universal\"\nmeans protocol-universal, not automatic support for every Web Component package.\nArbitrary custom elements can depend on browser-only APIs, layout state, timers,\nglobal side effects, or imperative `connectedCallback()` behavior that cannot be\nreliably reproduced during SSR. The registry direction is therefore only\ncredible when packages self-describe and validation can prove which components\nare SSR-renderable, DSD-safe, upgradeable, and hydratable.\n\n## Decision\n\nLessJS will position itself as a **Web Standards-first DSD/Web Components\napplication framework**.\n\nThe roadmap is organized around three workstreams:\n\n1. **Engine** - productize the existing DSD renderer, SSR bundle, route\n   rendering, and SSG pipeline into a reusable Web Components rendering kernel.\n2. **Protocol** - define how component packages describe tags, modules,\n   SSR renderability, upgrade strategy, parts, states, events, and tokens.\n3. **Ecosystem** - improve discovery, docs indexing, benchmarks, interactive\n   scaffolds, and npm reach before committing to a centralized registry hub.\n\nThe public product boundary is:\n\n> LessJS is a Web Components SSR/SSG rendering kernel and package protocol. A\n> registry hub is a discovery layer over manifests and validation artifacts, not\n> a magic runtime for arbitrary npm packages.\n\n### Version sequence\n\n**v0.14.2: Standards & Safety Patch**\n\n- Correct DSD template attributes against the WHATWG template element:\n  `shadowrootmode`, `shadowrootdelegatesfocus`, `shadowrootslotassignment`,\n  `shadowrootclonable`, `shadowrootserializable`, and boolean\n  `shadowrootcustomelementregistry`.\n- Tighten raw head injection boundaries:\n  `headExtras` remains developer-controlled raw HTML, but scripts should use\n  structured `inject.scripts`; `inject.headFragments` must not contain script\n  tags.\n- Fix dynamic SSG route parameter expansion so generated paths cannot escape the\n  output directory or accidentally broaden route matching.\n- Add real SSR bundle, i18n, content, and UI/docs output tests around the actual\n  self-hosted site.\n\n**v0.15: Renderer Kernel**\n\n- Define a public Web Component render protocol around adapter inputs, outputs,\n  errors, hydration hints, DSD constraints, metrics, and nested rendering.\n- Keep Playwright as the browser-validation mainline. Do not introduce\n  `@open-wc/testing`, `@open-wc/semantic-dom-diff`, or similar old presets as a\n  framework dependency.\n- Leave room for non-Lit adapters, but do not promise a large adapter matrix\n  before the protocol is stable.\n\n**v0.16: WC Package Protocol**\n\n- Extend `PackageIslandMeta` into a component package manifest.\n- Use Custom Elements Manifest-compatible metadata as the base for tag, module,\n  export, attributes, properties, events, slots, CSS parts, CSS custom\n  properties, custom states, and docs metadata.\n- Add LessJS-specific fields for `ssr`, `dsd`, `hydrate`, `strategy`,\n  diagnostics, validation status, and fallback behavior.\n- Support a local registry index that can be scanned and indexed by the docs\n  site.\n\n**v0.17: Ecosystem Entry**\n\n- Improve npm reach, docs search, benchmark visibility, interactive scaffolds,\n  and a registry page prototype.\n- Demonstrate component discoverability through local manifests and docs\n  indexing. Do not promise a centralized marketplace yet.\n- Add `less validate-manifest` before `less add`; install automation must reject\n  packages that cannot explain their SSR, registration, and hydration behavior.\n- Generate registration only from manifest data. Guard against duplicate\n  `customElements.define()` calls, version conflicts, and packages that require\n  browser-only side effects during SSR.\n\n**v0.18-v1.0: API Freeze**\n\n- Freeze `lessjs()` configuration, renderer protocol, adapter contract, package\n  manifest, migration policy, and release gates.\n\n**v1.0+ Bets**\n\n- Edge SSR, ISR, compiler pipeline, WC registry hub, and multi-adapter support\n  remain long-term bets. They should not become the main promise before the\n  renderer/protocol layer is stable.\n\n## Explicit Rejections\n\n### No webpack\n\nLessJS will not route its roadmap back through webpack or legacy bundler\npresets. The project direction is ESM-first: Vite remains useful for dev mode and\nclient bundling, while SSG/rendering boundaries continue to be abstracted away\nfrom bundler-specific assumptions.\n\n### No OpenWC toolchain adoption\n\nOpenWC can inform naming, testing ideas, and component-contract lessons, but\nLessJS will not adopt the OpenWC project template, `@open-wc/testing`, or\nsemantic DOM diff tooling as its main path. LessJS already has a Deno-first test\nstack, real SSG output, and Playwright browser verification.\n\n### No generic full-stack promise before renderer protocol stability\n\nLessJS should not present itself as a generic full-stack framework until the\nDSD/WC rendering kernel and protocol are stable. API routes and RPC are useful,\nbut they are not the differentiating core.\n\n### No centralized WC hub before manifest/protocol\n\nA Web Components registry hub is worth exploring, but only after component\npackages can self-describe, be locally scanned, be indexed by docs, and be\nchecked against security and governance rules.\n\n### No automatic hydration without explicit metadata\n\nHydration is not guessed from DOM shape. A package must declare the relevant\nstrategy, selectors, events, cleanup rules, and fallback behavior before LessJS\ncan bind interactivity automatically.\n\n## Consequences\n\n### Positive\n\n- The roadmap now follows LessJS's actual strengths instead of copying older\n  Web Components ecosystems.\n- Standards work becomes concrete: DSD attributes, package manifests, adapter\n  contracts, and browser validation.\n- Package discovery can start locally and incrementally before introducing\n  centralized governance.\n- The roadmap creates crisp release gates for v1.0 instead of broad product\n  promises.\n- The registry story is grounded in validation artifacts: manifests, SSR output,\n  SSG output, bundle cost, accessibility notes, and Playwright checks.\n\n### Negative\n\n- Some ecosystem expectations are explicitly deferred. Users who expect OpenWC\n  presets, webpack support, or a generic full-stack framework may need a\n  different tool.\n- v0.15-v0.16 require disciplined API design work before adding visible new\n  features.\n- A registry hub becomes a later governance problem, not a short-term marketing\n  feature.\n- One-click install and automatic rendering are intentionally limited to\n  manifest-bearing packages, which makes the pitch less magical but much safer.\n\n### Neutral\n\n- Open UI remains relevant, but only as a component-contract reference.\n- Vite remains useful, but as a dev/client bundling tool rather than the\n  framework's identity.\n- The current Lit adapter remains important, but the renderer protocol must not\n  hard-code Lit concepts.\n- FAST, Lit, and other authoring libraries remain adapter inputs. LessJS should\n  not treat any one authoring model as the definition of Web Components.\n- Scoped Custom Element Registries and CSS Houdini are tracked as standards\n  adjacency. They can inform future conflict isolation and styling extension, but\n  they are not required for the current stable path.\n\n## Validation\n\nEach version must ship with tests that prove the relevant boundary:\n\n- v0.14.2: DSD attribute tests, head injection rejection tests, route parameter\n  safety tests, actual docs SSR bundle test, and content/i18n/UI output checks.\n- v0.15: adapter contract tests, DSD metrics/report tests, nested CE regression\n  tests, and Playwright verification.\n- v0.16: manifest schema validation, local registry index generation, and docs\n  indexing tests.\n- v0.17: manifest validation CLI, `less add` dry-run, scaffold smoke test,\n  benchmark output shape, search/indexing checks, and registry page prototype\n  tests.\n- v0.18-v1.0: public API snapshot tests and migration policy checks.\n\n## References\n\n- [WHATWG HTML Living Standard](https://html.spec.whatwg.org/multipage/scripting.html#the-template-element):\n  `template` element and declarative shadow root attributes.\n- [MDN Shadow DOM guide](https://developer.mozilla.org/en-US/docs/Web/API/Web_components/Using_shadow_DOM):\n  server-rendered UI can use declarative shadow roots.\n- [Custom Elements Manifest](https://custom-elements-manifest.open-wc.org/):\n  metadata for tags, attributes, properties, slots, CSS parts, CSS custom\n  properties, custom states, and events.\n- [Open UI](https://open-ui.org/): component contracts for parts, states,\n  behaviors, accessibility requirements, form behavior, and tokens.\n- [OpenWC](https://open-wc.org/docs/): useful historical experience for testing,\n  linting, demoing, building, and publishing, not the adopted LessJS toolchain.\n- [Lit SSR](https://lit.dev/docs/v2/ssr/overview/): proof that Web Components SSR\n  is useful, with important library-specific limitations.\n- [FAST](https://fast.design/docs/getting-started/quick-start): example of a\n  Web Components authoring model that should integrate through protocols rather\n  than become a LessJS dependency.\n- [Scoped Custom Element Registries proposal](https://wicg.github.io/webcomponents/proposals/Scoped-Custom-Element-Registries.html):\n  relevant to future duplicate-tag and multi-version isolation.\n- [CSS Houdini drafts](https://drafts.css-houdini.org/): future styling and\n  worklet adjacency, not current core scope.\n- Playwright: real-browser validation for DSD parsing, custom element upgrade,\n  and layout behavior.\n",
    "html": "<h1>ADR 0024: Standards-first Web Components Renderer Roadmap</h1>\n<h2>Status</h2>\n<p><strong>ACCEPTED</strong> - 2026-05-14 strategic reset.</p>\n<p><strong>Updated</strong> - 2026-05-16 to align the public roadmap with the WC SSR/SSG\nrenderer-kernel and registry-hub feasibility review.</p>\n<h2>Context</h2>\n<p>LessJS has reached a point where the next roadmap cannot be a checklist of\nexternal ecosystem ideas. The project already has its own strongest axis:</p>\n<ul>\n<li>Declarative Shadow DOM rendering through <code>renderDSD()</code> and nested custom\nelement rendering.</li>\n<li>SSR bundle ownership through generated <code>renderRoute()</code>, <code>routeInfo</code>, and\n<code>getStaticPaths()</code>.</li>\n<li>A Vite-free SSG rendering path through <code>ssgRender()</code>.</li>\n<li>Package island discovery through <code>PackageIslandMeta</code>.</li>\n<li>Deno-first validation, self-hosted documentation, and Playwright browser\nverification.</li>\n</ul>\n<p>The strategic risk is to dilute this position by chasing older Web Components\ntooling. OpenWC remains valuable as ecosystem history and component-contract\nexperience, but LessJS should not adopt OpenWC&#39;s project template or legacy test\nstack as its roadmap spine. Webpack and Rollup/OpenWC build presets are also not\naligned with the current LessJS direction: Vite is already narrowed to dev/client\nbundling, and Phase 3 is moving toward Vite-free ESM SSG.</p>\n<p>Open UI remains useful, but as a contract vocabulary: parts, states, events,\naccessibility behavior, form behavior, and design tokens. It should inform how\nLessJS describes components, not turn LessJS into an OpenWC clone.</p>\n<p>The 2026-05-16 feasibility review clarifies one more boundary: &quot;universal&quot;\nmeans protocol-universal, not automatic support for every Web Component package.\nArbitrary custom elements can depend on browser-only APIs, layout state, timers,\nglobal side effects, or imperative <code>connectedCallback()</code> behavior that cannot be\nreliably reproduced during SSR. The registry direction is therefore only\ncredible when packages self-describe and validation can prove which components\nare SSR-renderable, DSD-safe, upgradeable, and hydratable.</p>\n<h2>Decision</h2>\n<p>LessJS will position itself as a <strong>Web Standards-first DSD/Web Components\napplication framework</strong>.</p>\n<p>The roadmap is organized around three workstreams:</p>\n<ol>\n<li><strong>Engine</strong> - productize the existing DSD renderer, SSR bundle, route\nrendering, and SSG pipeline into a reusable Web Components rendering kernel.</li>\n<li><strong>Protocol</strong> - define how component packages describe tags, modules,\nSSR renderability, upgrade strategy, parts, states, events, and tokens.</li>\n<li><strong>Ecosystem</strong> - improve discovery, docs indexing, benchmarks, interactive\nscaffolds, and npm reach before committing to a centralized registry hub.</li>\n</ol>\n<p>The public product boundary is:</p>\n<blockquote>\n<p>LessJS is a Web Components SSR/SSG rendering kernel and package protocol. A\nregistry hub is a discovery layer over manifests and validation artifacts, not\na magic runtime for arbitrary npm packages.</p>\n</blockquote>\n<h3>Version sequence</h3>\n<p><strong>v0.14.2: Standards &amp; Safety Patch</strong></p>\n<ul>\n<li>Correct DSD template attributes against the WHATWG template element:\n<code>shadowrootmode</code>, <code>shadowrootdelegatesfocus</code>, <code>shadowrootslotassignment</code>,\n<code>shadowrootclonable</code>, <code>shadowrootserializable</code>, and boolean\n<code>shadowrootcustomelementregistry</code>.</li>\n<li>Tighten raw head injection boundaries:\n<code>headExtras</code> remains developer-controlled raw HTML, but scripts should use\nstructured <code>inject.scripts</code>; <code>inject.headFragments</code> must not contain script\ntags.</li>\n<li>Fix dynamic SSG route parameter expansion so generated paths cannot escape the\noutput directory or accidentally broaden route matching.</li>\n<li>Add real SSR bundle, i18n, content, and UI/docs output tests around the actual\nself-hosted site.</li>\n</ul>\n<p><strong>v0.15: Renderer Kernel</strong></p>\n<ul>\n<li>Define a public Web Component render protocol around adapter inputs, outputs,\nerrors, hydration hints, DSD constraints, metrics, and nested rendering.</li>\n<li>Keep Playwright as the browser-validation mainline. Do not introduce\n<code>@open-wc/testing</code>, <code>@open-wc/semantic-dom-diff</code>, or similar old presets as a\nframework dependency.</li>\n<li>Leave room for non-Lit adapters, but do not promise a large adapter matrix\nbefore the protocol is stable.</li>\n</ul>\n<p><strong>v0.16: WC Package Protocol</strong></p>\n<ul>\n<li>Extend <code>PackageIslandMeta</code> into a component package manifest.</li>\n<li>Use Custom Elements Manifest-compatible metadata as the base for tag, module,\nexport, attributes, properties, events, slots, CSS parts, CSS custom\nproperties, custom states, and docs metadata.</li>\n<li>Add LessJS-specific fields for <code>ssr</code>, <code>dsd</code>, <code>hydrate</code>, <code>strategy</code>,\ndiagnostics, validation status, and fallback behavior.</li>\n<li>Support a local registry index that can be scanned and indexed by the docs\nsite.</li>\n</ul>\n<p><strong>v0.17: Ecosystem Entry</strong></p>\n<ul>\n<li>Improve npm reach, docs search, benchmark visibility, interactive scaffolds,\nand a registry page prototype.</li>\n<li>Demonstrate component discoverability through local manifests and docs\nindexing. Do not promise a centralized marketplace yet.</li>\n<li>Add <code>less validate-manifest</code> before <code>less add</code>; install automation must reject\npackages that cannot explain their SSR, registration, and hydration behavior.</li>\n<li>Generate registration only from manifest data. Guard against duplicate\n<code>customElements.define()</code> calls, version conflicts, and packages that require\nbrowser-only side effects during SSR.</li>\n</ul>\n<p><strong>v0.18-v1.0: API Freeze</strong></p>\n<ul>\n<li>Freeze <code>lessjs()</code> configuration, renderer protocol, adapter contract, package\nmanifest, migration policy, and release gates.</li>\n</ul>\n<p><strong>v1.0+ Bets</strong></p>\n<ul>\n<li>Edge SSR, ISR, compiler pipeline, WC registry hub, and multi-adapter support\nremain long-term bets. They should not become the main promise before the\nrenderer/protocol layer is stable.</li>\n</ul>\n<h2>Explicit Rejections</h2>\n<h3>No webpack</h3>\n<p>LessJS will not route its roadmap back through webpack or legacy bundler\npresets. The project direction is ESM-first: Vite remains useful for dev mode and\nclient bundling, while SSG/rendering boundaries continue to be abstracted away\nfrom bundler-specific assumptions.</p>\n<h3>No OpenWC toolchain adoption</h3>\n<p>OpenWC can inform naming, testing ideas, and component-contract lessons, but\nLessJS will not adopt the OpenWC project template, <code>@open-wc/testing</code>, or\nsemantic DOM diff tooling as its main path. LessJS already has a Deno-first test\nstack, real SSG output, and Playwright browser verification.</p>\n<h3>No generic full-stack promise before renderer protocol stability</h3>\n<p>LessJS should not present itself as a generic full-stack framework until the\nDSD/WC rendering kernel and protocol are stable. API routes and RPC are useful,\nbut they are not the differentiating core.</p>\n<h3>No centralized WC hub before manifest/protocol</h3>\n<p>A Web Components registry hub is worth exploring, but only after component\npackages can self-describe, be locally scanned, be indexed by docs, and be\nchecked against security and governance rules.</p>\n<h3>No automatic hydration without explicit metadata</h3>\n<p>Hydration is not guessed from DOM shape. A package must declare the relevant\nstrategy, selectors, events, cleanup rules, and fallback behavior before LessJS\ncan bind interactivity automatically.</p>\n<h2>Consequences</h2>\n<h3>Positive</h3>\n<ul>\n<li>The roadmap now follows LessJS&#39;s actual strengths instead of copying older\nWeb Components ecosystems.</li>\n<li>Standards work becomes concrete: DSD attributes, package manifests, adapter\ncontracts, and browser validation.</li>\n<li>Package discovery can start locally and incrementally before introducing\ncentralized governance.</li>\n<li>The roadmap creates crisp release gates for v1.0 instead of broad product\npromises.</li>\n<li>The registry story is grounded in validation artifacts: manifests, SSR output,\nSSG output, bundle cost, accessibility notes, and Playwright checks.</li>\n</ul>\n<h3>Negative</h3>\n<ul>\n<li>Some ecosystem expectations are explicitly deferred. Users who expect OpenWC\npresets, webpack support, or a generic full-stack framework may need a\ndifferent tool.</li>\n<li>v0.15-v0.16 require disciplined API design work before adding visible new\nfeatures.</li>\n<li>A registry hub becomes a later governance problem, not a short-term marketing\nfeature.</li>\n<li>One-click install and automatic rendering are intentionally limited to\nmanifest-bearing packages, which makes the pitch less magical but much safer.</li>\n</ul>\n<h3>Neutral</h3>\n<ul>\n<li>Open UI remains relevant, but only as a component-contract reference.</li>\n<li>Vite remains useful, but as a dev/client bundling tool rather than the\nframework&#39;s identity.</li>\n<li>The current Lit adapter remains important, but the renderer protocol must not\nhard-code Lit concepts.</li>\n<li>FAST, Lit, and other authoring libraries remain adapter inputs. LessJS should\nnot treat any one authoring model as the definition of Web Components.</li>\n<li>Scoped Custom Element Registries and CSS Houdini are tracked as standards\nadjacency. They can inform future conflict isolation and styling extension, but\nthey are not required for the current stable path.</li>\n</ul>\n<h2>Validation</h2>\n<p>Each version must ship with tests that prove the relevant boundary:</p>\n<ul>\n<li>v0.14.2: DSD attribute tests, head injection rejection tests, route parameter\nsafety tests, actual docs SSR bundle test, and content/i18n/UI output checks.</li>\n<li>v0.15: adapter contract tests, DSD metrics/report tests, nested CE regression\ntests, and Playwright verification.</li>\n<li>v0.16: manifest schema validation, local registry index generation, and docs\nindexing tests.</li>\n<li>v0.17: manifest validation CLI, <code>less add</code> dry-run, scaffold smoke test,\nbenchmark output shape, search/indexing checks, and registry page prototype\ntests.</li>\n<li>v0.18-v1.0: public API snapshot tests and migration policy checks.</li>\n</ul>\n<h2>References</h2>\n<ul>\n<li><a href=\"https://html.spec.whatwg.org/multipage/scripting.html#the-template-element\" rel=\"noopener noreferrer\">WHATWG HTML Living Standard</a>:\n<code>template</code> element and declarative shadow root attributes.</li>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/API/Web_components/Using_shadow_DOM\" rel=\"noopener noreferrer\">MDN Shadow DOM guide</a>:\nserver-rendered UI can use declarative shadow roots.</li>\n<li><a href=\"https://custom-elements-manifest.open-wc.org/\" rel=\"noopener noreferrer\">Custom Elements Manifest</a>:\nmetadata for tags, attributes, properties, slots, CSS parts, CSS custom\nproperties, custom states, and events.</li>\n<li><a href=\"https://open-ui.org/\" rel=\"noopener noreferrer\">Open UI</a>: component contracts for parts, states,\nbehaviors, accessibility requirements, form behavior, and tokens.</li>\n<li><a href=\"https://open-wc.org/docs/\" rel=\"noopener noreferrer\">OpenWC</a>: useful historical experience for testing,\nlinting, demoing, building, and publishing, not the adopted LessJS toolchain.</li>\n<li><a href=\"https://lit.dev/docs/v2/ssr/overview/\" rel=\"noopener noreferrer\">Lit SSR</a>: proof that Web Components SSR\nis useful, with important library-specific limitations.</li>\n<li><a href=\"https://fast.design/docs/getting-started/quick-start\" rel=\"noopener noreferrer\">FAST</a>: example of a\nWeb Components authoring model that should integrate through protocols rather\nthan become a LessJS dependency.</li>\n<li><a href=\"https://wicg.github.io/webcomponents/proposals/Scoped-Custom-Element-Registries.html\" rel=\"noopener noreferrer\">Scoped Custom Element Registries proposal</a>:\nrelevant to future duplicate-tag and multi-version isolation.</li>\n<li><a href=\"https://drafts.css-houdini.org/\" rel=\"noopener noreferrer\">CSS Houdini drafts</a>: future styling and\nworklet adjacency, not current core scope.</li>\n<li>Playwright: real-browser validation for DSD parsing, custom element upgrade,\nand layout behavior.</li>\n</ul>\n"
  },
  {
    "slug": "0023-phase-reordering-bundler-agnostic",
    "frontmatter": {
      "title": "ADR 0023: Phase Reordering — Client Bundle as Final Layer",
      "date": "2026-05-13",
      "draft": false,
      "tags": [
        "architecture",
        "build",
        "phases",
        "bundler"
      ],
      "type": "adr",
      "lang": "en"
    },
    "content": "\n# ADR 0023: Phase Reordering — Client Bundle as Final Layer\n\n## Status\n\n✅ Proposed\n\n## Context\n\nADR 0022 established that LessJS phases can operate independently of Vite. Phase 3 (SSG rendering) is now Vite-independent via `ssg-render.ts`. Phase 1a (scanRoutes, genHonoEntry) is pure TS. Phase 1b (SSR bundle building) can use esbuild.\n\nThis ADR addresses the remaining architectural inconsistency: **Phase ordering**.\n\n### Current order\n\n```\nPhase 1 (SSR bundle)\n  → Phase 2 (client island build + manifest.json)\n  → Phase 3 (SSG render all pages + inject client script from manifest)\n```\n\nPhase 2 runs before Phase 3 because the manifest it produces is needed for client script injection. But the client script is only a `<script>` tag appended to already-rendered HTML — it has no effect on the HTML content itself. Phase 3 does not need Phase 2 to run.\n\n### Problem\n\nThe current ordering creates an unnecessary dependency chain:\n\n- Phase 2 blocks Phase 3 for no functional reason\n- Content hashes in client chunk filenames are determined before HTML is rendered (should be the other way around)\n- Users who only want SSG (no islands) must still go through Phase 2\n- The build pipeline looks like a linear chain when it's actually two independent outputs (SSR HTML + client JS)\n\n### Proposed order\n\n```\nPhase 1 (SSR bundle — any bundler or esbuild)\n  → Phase 3 (SSG render all pages)\n  → Phase 2 (bundle client islands) — FINAL step\n  → Inject client script URLs into rendered HTML\n```\n\nThis makes Phase 2 the last step — the only phase that depends on a bundler. Everything else runs on pure ESM.\n\n## Decision\n\n### Phase reordering\n\nChange the build pipeline to:\n\n1. **Phase 1**: SSR bundle (content-independent, generates routes + rendering code)\n2. **Phase 3**: SSG render (uses SSR bundle to produce all HTML files)\n3. **Phase 2**: Client island bundle (produces ESM chunks + manifest.json)\n4. **Inject**: Read manifest, inject `<script>` tags into HTML\n\nThe injection step is decoupled from Phase 2 — it reads the already-written manifest.json and patches HTML files. This means Phase 2 can be skipped entirely if the project has no islands.\n\n### Phase 2 bundler options\n\nPhase 2's requirements:\n- Input: virtual client entry module + island source files\n- Output: ESM chunks in `dist/client/`\n- Manifest: Vite-style `manifest.json` mapping entry → chunk URL\n- Minification: production builds\n- Code splitting: per-island chunks\n\n| Option | Speed | Code splitting | Manifest | Maturity | Notes |\n|--------|-------|---------------|----------|----------|-------|\n| **Vite** (current) | 🟡 moderate | ✅ full | ✅ built-in | ✅ battle-tested | Only used for client build, not dev |\n| **esbuild** | 🟢 fastest | ⚠️ limited | ❌ custom needed | ✅ mature | No code splitting per-island |\n| **rolldown** (Rust) | 🟢 fast | ✅ full | ⚠️ partial | 🟡 new (v1 in 2026) | Rollup-compatible API, still maturing |\n| **rollup** | 🟡 moderate | ✅ full | ✅ via plugin | ✅ battle-tested | Slower than alternatives |\n\n**Recommendation: Keep Vite for Phase 2.**\n\nRationale:\n- Phase 2 already uses Vite and works correctly — replacing it gains nothing for the user\n- Vite's manifest output is already consumed by `ssgRender()` — esbuild would need custom manifest generation\n- Dev mode still needs Vite for HMR — keeping Vite in Phase 2 means shared config between build and dev\n- The \"one bundler config\" story is simpler for users\n- esbuild's lack of per-island code splitting means all islands go in one file — defeats the purpose of islands\n\n**Phase 2 does not need to be swapped. It only needs to be reordered to run last.**\n\n## Consequences\n\n### Positive\n\n1. **Phase 3 runs immediately after Phase 1** — no waiting for client bundle\n2. **SSG works without any bundler** — Phase 2 is optional (zero-island projects)\n3. **Content hashes are stable** — client chunk hashes don't affect HTML content\n4. **Clearer architecture** — Phase 2 is \"add client JS to already-rendered HTML\", not \"prerequisite for rendering\"\n\n### Negative\n\n1. **Injection step becomes explicit** — currently `ssgRender()` handles injection internally. Must decouple: `ssgRender()` writes HTML, then injection reads manifest and modifies HTML.\n2. **`dist/` is incomplete after Phase 3** — users running only Phase 1+3 get HTML without client JS. Must document this.\n3. **Build pipeline rework** — `build.ts` in adapter-vite must call phases in new order. ~1 day migration.\n\n### Implementation sketch\n\n```typescript\n// build.ts — new order\nasync function build(ctx) {\n  // Phase 1: SSR bundle\n  await buildSSRBundle(ctx);\n  ctx.markComplete(1);\n\n  // Phase 3: SSG render (no manifest needed — just render)\n  const module = await loadSsrBundle(ctx);\n  await ssgRender(module, ctx.options, ctx);  // writes HTML, no script injection\n  ctx.markComplete(3);\n\n  // Phase 2: Client islands (any bundler)\n  await buildClientBundle(ctx);\n  ctx.markComplete(2);\n\n  // Inject: read manifest, patch HTML\n  if (manifestExists()) {\n    const scriptUrls = parseManifest(manifestPath);\n    injectClientScripts(distDir, scriptUrls);\n  }\n}\n```\n\nNo changes to `ssgRender()` internals — it simply stops doing client script injection. The caller (`build.ts` or `cli/ssg.ts`) decides whether and when to run Phase 2 + injection.\n\n## Migration\n\nSplit across two minor versions:\n\n**v0.14** (next):\n- Decouple injection from `ssgRender()` — remove client script injection from render pipeline\n- `build.ts` calls phases in new order (Phase 1 → Phase 3 → Phase 2 → injection)\n- Inject becomes a standalone function in `ssg-postprocess.js`\n\n**v0.15**:\n- Phase 2 becomes optional — `build.ts` skips it if `totalIslands === 0`\n- `cli/ssg.ts` supports `--skip-client` flag\n- API stable: `ssgRender()` no longer requires client manifest\n",
    "html": "<h1>ADR 0023: Phase Reordering — Client Bundle as Final Layer</h1>\n<h2>Status</h2>\n<p>✅ Proposed</p>\n<h2>Context</h2>\n<p>ADR 0022 established that LessJS phases can operate independently of Vite. Phase 3 (SSG rendering) is now Vite-independent via <code>ssg-render.ts</code>. Phase 1a (scanRoutes, genHonoEntry) is pure TS. Phase 1b (SSR bundle building) can use esbuild.</p>\n<p>This ADR addresses the remaining architectural inconsistency: <strong>Phase ordering</strong>.</p>\n<h3>Current order</h3>\n<pre><code>Phase 1 (SSR bundle)\n  → Phase 2 (client island build + manifest.json)\n  → Phase 3 (SSG render all pages + inject client script from manifest)\n</code></pre>\n<p>Phase 2 runs before Phase 3 because the manifest it produces is needed for client script injection. But the client script is only a <code>&lt;script&gt;</code> tag appended to already-rendered HTML — it has no effect on the HTML content itself. Phase 3 does not need Phase 2 to run.</p>\n<h3>Problem</h3>\n<p>The current ordering creates an unnecessary dependency chain:</p>\n<ul>\n<li>Phase 2 blocks Phase 3 for no functional reason</li>\n<li>Content hashes in client chunk filenames are determined before HTML is rendered (should be the other way around)</li>\n<li>Users who only want SSG (no islands) must still go through Phase 2</li>\n<li>The build pipeline looks like a linear chain when it&#39;s actually two independent outputs (SSR HTML + client JS)</li>\n</ul>\n<h3>Proposed order</h3>\n<pre><code>Phase 1 (SSR bundle — any bundler or esbuild)\n  → Phase 3 (SSG render all pages)\n  → Phase 2 (bundle client islands) — FINAL step\n  → Inject client script URLs into rendered HTML\n</code></pre>\n<p>This makes Phase 2 the last step — the only phase that depends on a bundler. Everything else runs on pure ESM.</p>\n<h2>Decision</h2>\n<h3>Phase reordering</h3>\n<p>Change the build pipeline to:</p>\n<ol>\n<li><strong>Phase 1</strong>: SSR bundle (content-independent, generates routes + rendering code)</li>\n<li><strong>Phase 3</strong>: SSG render (uses SSR bundle to produce all HTML files)</li>\n<li><strong>Phase 2</strong>: Client island bundle (produces ESM chunks + manifest.json)</li>\n<li><strong>Inject</strong>: Read manifest, inject <code>&lt;script&gt;</code> tags into HTML</li>\n</ol>\n<p>The injection step is decoupled from Phase 2 — it reads the already-written manifest.json and patches HTML files. This means Phase 2 can be skipped entirely if the project has no islands.</p>\n<h3>Phase 2 bundler options</h3>\n<p>Phase 2&#39;s requirements:</p>\n<ul>\n<li>Input: virtual client entry module + island source files</li>\n<li>Output: ESM chunks in <code>dist/client/</code></li>\n<li>Manifest: Vite-style <code>manifest.json</code> mapping entry → chunk URL</li>\n<li>Minification: production builds</li>\n<li>Code splitting: per-island chunks</li>\n</ul>\n<table>\n<thead>\n<tr>\n<th>Option</th>\n<th>Speed</th>\n<th>Code splitting</th>\n<th>Manifest</th>\n<th>Maturity</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><strong>Vite</strong> (current)</td>\n<td>🟡 moderate</td>\n<td>✅ full</td>\n<td>✅ built-in</td>\n<td>✅ battle-tested</td>\n<td>Only used for client build, not dev</td>\n</tr>\n<tr>\n<td><strong>esbuild</strong></td>\n<td>🟢 fastest</td>\n<td>⚠️ limited</td>\n<td>❌ custom needed</td>\n<td>✅ mature</td>\n<td>No code splitting per-island</td>\n</tr>\n<tr>\n<td><strong>rolldown</strong> (Rust)</td>\n<td>🟢 fast</td>\n<td>✅ full</td>\n<td>⚠️ partial</td>\n<td>🟡 new (v1 in 2026)</td>\n<td>Rollup-compatible API, still maturing</td>\n</tr>\n<tr>\n<td><strong>rollup</strong></td>\n<td>🟡 moderate</td>\n<td>✅ full</td>\n<td>✅ via plugin</td>\n<td>✅ battle-tested</td>\n<td>Slower than alternatives</td>\n</tr>\n</tbody></table>\n<p><strong>Recommendation: Keep Vite for Phase 2.</strong></p>\n<p>Rationale:</p>\n<ul>\n<li>Phase 2 already uses Vite and works correctly — replacing it gains nothing for the user</li>\n<li>Vite&#39;s manifest output is already consumed by <code>ssgRender()</code> — esbuild would need custom manifest generation</li>\n<li>Dev mode still needs Vite for HMR — keeping Vite in Phase 2 means shared config between build and dev</li>\n<li>The &quot;one bundler config&quot; story is simpler for users</li>\n<li>esbuild&#39;s lack of per-island code splitting means all islands go in one file — defeats the purpose of islands</li>\n</ul>\n<p><strong>Phase 2 does not need to be swapped. It only needs to be reordered to run last.</strong></p>\n<h2>Consequences</h2>\n<h3>Positive</h3>\n<ol>\n<li><strong>Phase 3 runs immediately after Phase 1</strong> — no waiting for client bundle</li>\n<li><strong>SSG works without any bundler</strong> — Phase 2 is optional (zero-island projects)</li>\n<li><strong>Content hashes are stable</strong> — client chunk hashes don&#39;t affect HTML content</li>\n<li><strong>Clearer architecture</strong> — Phase 2 is &quot;add client JS to already-rendered HTML&quot;, not &quot;prerequisite for rendering&quot;</li>\n</ol>\n<h3>Negative</h3>\n<ol>\n<li><strong>Injection step becomes explicit</strong> — currently <code>ssgRender()</code> handles injection internally. Must decouple: <code>ssgRender()</code> writes HTML, then injection reads manifest and modifies HTML.</li>\n<li><strong><code>dist/</code> is incomplete after Phase 3</strong> — users running only Phase 1+3 get HTML without client JS. Must document this.</li>\n<li><strong>Build pipeline rework</strong> — <code>build.ts</code> in adapter-vite must call phases in new order. ~1 day migration.</li>\n</ol>\n<h3>Implementation sketch</h3>\n<pre><code class=\"language-typescript\">// build.ts — new order\nasync function build(ctx) {\n  // Phase 1: SSR bundle\n  await buildSSRBundle(ctx);\n  ctx.markComplete(1);\n\n  // Phase 3: SSG render (no manifest needed — just render)\n  const module = await loadSsrBundle(ctx);\n  await ssgRender(module, ctx.options, ctx);  // writes HTML, no script injection\n  ctx.markComplete(3);\n\n  // Phase 2: Client islands (any bundler)\n  await buildClientBundle(ctx);\n  ctx.markComplete(2);\n\n  // Inject: read manifest, patch HTML\n  if (manifestExists()) {\n    const scriptUrls = parseManifest(manifestPath);\n    injectClientScripts(distDir, scriptUrls);\n  }\n}\n</code></pre>\n<p>No changes to <code>ssgRender()</code> internals — it simply stops doing client script injection. The caller (<code>build.ts</code> or <code>cli/ssg.ts</code>) decides whether and when to run Phase 2 + injection.</p>\n<h2>Migration</h2>\n<p>Split across two minor versions:</p>\n<p><strong>v0.14</strong> (next):</p>\n<ul>\n<li>Decouple injection from <code>ssgRender()</code> — remove client script injection from render pipeline</li>\n<li><code>build.ts</code> calls phases in new order (Phase 1 → Phase 3 → Phase 2 → injection)</li>\n<li>Inject becomes a standalone function in <code>ssg-postprocess.js</code></li>\n</ul>\n<p><strong>v0.15</strong>:</p>\n<ul>\n<li>Phase 2 becomes optional — <code>build.ts</code> skips it if <code>totalIslands === 0</code></li>\n<li><code>cli/ssg.ts</code> supports <code>--skip-client</code> flag</li>\n<li>API stable: <code>ssgRender()</code> no longer requires client manifest</li>\n</ul>\n"
  },
  {
    "slug": "0022-esm-self-sustaining-ssg-pipeline",
    "frontmatter": {
      "title": "ADR 0022: ESM-Native LessJS — Pure ESM Imports Across Browser/Deno/Bun/CF Workers",
      "date": "2026-05-13",
      "draft": false,
      "tags": [
        "architecture",
        "esm",
        "ssg",
        "core",
        "build",
        "standards",
        "portability"
      ],
      "type": "adr",
      "lang": "en"
    },
    "content": "\n# ADR 0022: ESM-Native LessJS — Pure ESM Imports Across Browser/Deno/Bun/CF Workers\n\n## Status\n\n✅ Proposed\n\n## Core Thesis\n\n**LessJS should work as pure native ESM imports — runnable in browser, Deno, Bun, Node.js, and Cloudflare Workers — with Vite being an optional accelerator for dev mode and client bundling, not a hard requirement for the SSG pipeline.**\n\nThis means:\n- Phase 1 (route scanning + SSR bundle generation) must produce **standard ESM modules**, not Vite-coupled bundles\n- Phase 3 (SSG rendering) must be a **standalone process** that loads SSR bundles via `import()` — no Vite plugin required\n- The SSR bundle must carry its own dependency map (`importmap.json`) so it resolves bare specifiers in **any runtime**\n- `@openelement/core` must use only **Web Standard APIs** — zero `node:*` imports, zero platform-specific assumptions\n\n## Context\n\n### Problem 1: SSR bundle is Vite-coupled, breaks outside Vite process\n\nLessJS Phase 3 (SSG) currently runs inside `closeBundle()` of the `less:build` Vite plugin. It builds a self-contained SSR bundle via `viteBuild(ssr: true, noExternal: [...])`, then imports it in the same process.\n\nThis bundle contains bare specifiers (`import 'hono'`, `import '@openelement/core'`) that rely on the **ambient process import map** to resolve. When a user installs LessJS from JSR (`jsr:@openelement/app`), Deno's workspace import map covers these. But with `npm:@jsr/lessjs__app`, the npm resolver doesn't know about them, and Phase 3 fails.\n\n**Root cause**: The SSR bundle has no self-declared dependency map — it assumes the loading environment will resolve its bare specifiers.\n\n### Problem 2: Target runtime diversity demands portable ESM\n\nLessJS users deploy to:\n- **Cloudflare Workers** — no filesystem, no Node APIs, pure Service Worker scope\n- **Deno** — native ESM, import maps\n- **Bun** — native ESM, Node compat\n- **Node.js** — ESM with `--experimental-json-modules`\n- **Browser** — native `<script type=\"module\"` with importmap\n\nEach has different resolution semantics. A Vite-bundled SSR bundle with inlined dependencies works in none of them consistently. A native ESM module with an explicit importmap works in all of them.\n\n### Problem 3: Hand-rolled URL pattern matching in core\n\n`core/src/context.ts` has a hand-rolled `extractParams()` function that parses route patterns (`/posts/:id` → pathname `/posts/123` → `{id: '123'}`). This duplicates WHATWG's **URLPattern** standard API, which is now available across all target runtimes (Deno native, Node 19+, Bun native, all browsers).\n\n### What was considered and rejected\n\n**parse5 replacement**: `parse5` is the WHATWG-standard HTML parser, used for AST-based nested DSD rendering in `render-nested.ts`. Considered replacing it with an inline HTML walker for \"zero-dependency core\", but rejected because:\n\n- parse5 has zero functional overhead for our use case — it IS the standard\n- A hand-rolled walker would lost WHATWG compliance (edge cases like nested `<template>`, HTML entity decoding inside attributes, quoted attribute values containing `>`)\n- parse5 is only used in SSR build, not shipped to browsers (tree-shaken)\n- The \"zero dependency\" benefit does not solve any real user problem\n\n**`parseQuery()` replacement**: Already uses `URLSearchParams` internally. No change needed.\n\n### Scope\n\nThis ADR covers changes to align LessJS with its core thesis:\n\n1. **Phase 1 + Phase 3 → Pure ESM pipeline** — SSR bundle carries its own `importmap.json`, Phase 3 runs standalone in any runtime. Vite retained for dev HMR + client bundling only.\n2. **`@openelement/core` → Web Standards only** — Already zero `node:*`, already zero Vite. Replace hand-rolled route matching with WHATWG `URLPattern`. Retain `parse5` (WHATWG-standard HTML parser, necessary for AST-based nested DSD).\n\n## Decision\n\n### 0. Every Artifact is Pure ESM — Runtime Agnostic\n\nAll LessJS build artifacts must be **standard ESM modules** that can be loaded via `import()` in any runtime. No artifact may assume the presence of Vite, Node.js APIs (`node:fs`, `node:path`), Deno-specific APIs, or any specific runtime's module resolution semantics.\n\nThis applies to:\n\n| Artifact | Format | Loaded by |\n|----------|--------|-----------|\n| SSR bundle (`dist/server/entry.mjs`) | Pure ESM with bare specifiers | Phase 3 SSG in any runtime via `import()` |\n| Client islands (`dist/client/islands/*.js`) | ESM chunks | Browser via `<script type=\"module\">` |\n| HTML pages (`dist/**/*.html`) | Static HTML + DSD | Browser natively |\n| `@openelement/core` exports | Pure ESM with only relative/web-standard imports | SSR bundle, client islands, user code |\n\n**Enforcement rules:**\n- No `node:*` imports in `@openelement/core` (already done ✓)\n- No Vite types or APIs referenced in core (already done ✓)\n- SSR bundle must carry `importmap.json` so it resolves bare specifiers in any runtime\n- Phase 3 SSG CLI must work in Deno, Bun, Node.js, and Cloudflare Workers without modification\n- `parse5` is acceptable (WHATWG standard, available on npm/JSR, no runtime lock-in)\n- `URLSearchParams` replaces hand-rolled query parsing (already done ✓)\n- `URLPattern` replaces hand-rolled route param extraction (P1)\n\n### 1. Phase 1 + 3: Native ESM SSG Pipeline\n\n#### Current flow (Vite-coupled)\n\n```\nPhase 1 (Vite closeBundle):\n  scanRoutes() → genHonoEntry() → viteBuild(ssr: true, noExternal: all)\n  → dist/server/entry.js          ← bare specifiers, no self-declared deps\n  → Phase 2 (viteBuild client)\n  → Phase 3 (import('./dist/server/entry.js'), render all)\n```\n\nCannot run outside Vite process because bare specifiers rely on ambient import map.\n\n#### Proposed flow (ESM-native)\n\n```\nPhase 1 (Vite closeBundle or standalone):\n  scanRoutes() → genHonoEntry()\n  → esbuild --format=esm --packages=external    ← preserves bare specifiers\n  → dist/server/entry.mjs + dist/server/importmap.json\n\nPhase 2 (client bundling — Vite only):\n  viteBuild(client islands)                     ← unchanged\n\nPhase 3 (SSG — any runtime):\n  read dist/server/importmap.json\n  → load dist/server/entry.mjs with importmap\n  → renderAll(routes)\n  → write HTML files\n\nTarget runtimes:\n  ✅ Deno:   deno run --import-map=importmap.json ssg.mjs\n  ✅ Bun:    bun ssg.mjs                          ← natively supports importmap\n  ✅ Node:   node --experimental-network-imports ssg.mjs\n  ✅ CF:     wrangler ssg.mjs                     ← Workers runtime supports ESM\n  ✅ Browser: not applicable (SSG runs at build time)\n```\n\n#### Sidecar importmap.json format\n\n```json\n{\n  \"imports\": {\n    \"hono\": \"npm:hono@4.7.0\",\n    \"lit\": \"npm:lit@3.3.2\",\n    \"@lit/reactive-element\": \"npm:@lit/reactive-element@2.1.0\",\n    \"@openelement/core\": \"npm:@jsr/lessjs__core@0.13.0\",\n    \"@openelement/core/logger\": \"npm:@jsr/lessjs__core@0.13.0/logger\",\n    \"@openelement/adapter-lit\": \"npm:@jsr/lessjs__adapter-lit@0.8.0\",\n    \"parse5\": \"npm:parse5@7.0.0\"\n  }\n}\n```\n\nGenerated by Phase 1 from:\n- The project's `deno.json` / `package.json` imports\n- Resolved versions of `@openelement/*` packages\n- User-configured `ssr.noExternal` patterns\n\n#### Phase 3 loading strategy\n\n```typescript\n// ssg-cli.ts — standalone, no Vite dependency\nasync function loadSsrBundle(ssrDir: string) {\n  const importmapPath = resolve(ssrDir, 'importmap.json');\n  const entryPath = resolve(ssrDir, 'entry.mjs');\n\n  const importmap = JSON.parse(readFileSync(importmapPath, 'utf-8'));\n\n  // In Deno: deno run --import-map=./dist/server/importmap.json\n  // In Bun: natively supports importmap.json\n  // In Node: resolve bare specifiers manually via the importmap before import()\n  const entryUrl = pathToFileURL(entryPath).href;\n  return await import(entryUrl);\n}\n```\n\n**Key insight**: Switch from `viteBuild(ssr: true, noExternal: all)` (everything inlined) to **esbuild `--format=esm --packages=external`** (bare specifiers preserved). The importmap provides the resolution map.\n\n#### Dev mode impact\n\n**None**. Vite dev server continues to handle HMR, dev proxy, and dev module resolution unchanged. The importmap is only produced during `build`.\n\n---\n\n### 2. URLPattern Adoption\n\n#### Current code\n\n```typescript\n// core/src/context.ts — hand-rolled, ~30 lines\nexport function extractParams(\n  pattern: string,\n  pathname: string,\n): Record<string, string> {\n  const params: Record<string, string> = {};\n  const patternParts = pattern.split('/');\n  const pathParts = pathname.split('/');\n  for (let i = 0; i < patternParts.length; i++) {\n    const part = patternParts[i];\n    if (part.startsWith(':')) {\n      const paramName = part.slice(1);\n      if (i < pathParts.length) {\n        params[paramName] = decodeURIComponent(pathParts[i]);\n      }\n    }\n  }\n  return params;\n}\n```\n\nDoes not handle:\n- Optional params (`/posts/:id?`)\n- Wildcards (`/posts/*`)\n- Regex constraints (`/posts/:id(\\\\d+)`)\n\n#### Replacement\n\n```typescript\n// core/src/context.ts — using WHATWG URLPattern\nexport function extractParams(\n  pattern: string,\n  pathname: string,\n): Record<string, string> {\n  const urlPattern = new URLPattern({ pathname: pattern });\n  const match = urlPattern.exec({ pathname, protocol: 'https', hostname: 'localhost' });\n  return match?.pathname?.groups ?? {};\n}\n```\n\nURLPattern supports:\n- `:id` → named groups\n- `:id?` → optional\n- `*` → wildcard\n- `(\\\\d+)` → regex constraints\n- All per WHATWG spec\n\n#### Runtime availability\n\n| Runtime | URLPattern support | Notes |\n|---------|-------------------|-------|\n| Deno 1.33+ | ✅ Native | No flags |\n| Node.js 19+ | ✅ | Under `--experimental-url-pattern` flag |\n| Bun 1.0+ | ✅ Native | No flags |\n| Chrome/Edge 95+ | ✅ Native | |\n| Firefox 113+ | ✅ Native | |\n| Safari 16.4+ | ✅ Native | |\n\nLessJS targets Deno + Node. Use with runtime guard:\n\n```typescript\nexport function extractParams(pattern: string, pathname: string): Record<string, string> {\n  if (typeof URLPattern !== 'undefined') {\n    const urlPattern = new URLPattern({ pathname: pattern });\n    const match = urlPattern.exec({ pathname, protocol: 'https', hostname: 'localhost' });\n    return match?.pathname?.groups ?? {};\n  }\n  // Fallback: existing hand-rolled implementation\n  return extractParamsFallback(pattern, pathname);\n}\n```\n\n---\n\n## Consequences\n\n### Positive\n\n1. **True ESM-native architecture** — LessJS SSG runs in any runtime that supports ESM `import()`. No Vite process needed for production builds.\n2. **Zero-friction install** — No `npm:` vs `jsr:` distinction. All install paths (npm, JSR, workspace) converge on the same Phase 3 behaviour.\n3. **Platform-agnostic deployment** — `dist/` is pure static HTML + ESM islands. SSG runs equally well in CI on Deno, Node, or Bun.\n4. **Standard-compliant route matching** — URLPattern supports optional params, wildcards, regex constraints. Future-proof.\n5. **parse5 retained** — WHATWG-standard HTML parsing for correct AST-based DSD rendering.\n6. **Vite stays where it shines** — Dev HMR and client bundling remain Vite's job. No reinvention needed.\n\n### Negative\n\n1. **Importmap generation adds logic to Phase 1** — Must scan project config for version info (~50 lines)\n2. **Two SSR bundle modes** — Old `viteBuild(noExternal)` inline mode retained for backward compat; new importmap mode as default\n3. **URLPattern needs a fallback** — Node.js <19 users get the hand-rolled version\n4. **No change to `parseQuery()`** — Already uses `URLSearchParams`, correctly handles multi-value keys\n\n### Migration\n\n**Phase 3 importmap (v0.14)**:\n- Phase 1 additionally produces `dist/server/entry.mjs` + `importmap.json`\n- Both old (Vite inline) and new (ESM + importmap) modes coexist\n- Phase 3 CLI defaults to importmap mode, falls back to Vite inline\n\n**URLPattern (v0.14)**:\n- Drop-in replacement for `extractParams()` with runtime guard\n- Remove fallback in v0.15 after Node <19 support window ends\n\n---\n\n## Implementation Plan\n\n### P0 (2.5d) — Solve the friction\n\n| # | Task | Files | Est. |\n|---|------|-------|------|\n| 1 | Phase 1: emit `importmap.json` alongside SSR bundle | `cli/build-ssg.ts` | 0.5d |\n| 2 | Phase 3: standalone SSG CLI using importmap | new `cli/ssg.ts` | 1d |\n| 3 | Move `@hono/vite-dev-server` to optional dep | `adapter-vite/deno.json` | 0.5d |\n| 4 | Document importmap approach in README | README.md | 0.5d |\n\n### P1 (1.5d) — Standard API alignment\n\n| # | Task | Files | Est. |\n|---|------|-------|------|\n| 5 | Replace `extractParams()` with `URLPattern` + fallback | `core/src/context.ts` | 0.5d |\n| 6 | Test URLPattern route matching for all patterns | `__tests__/context.test.ts` | 0.5d |\n| 7 | Remove the \"parse5 replacement\" section from all docs | - | 0.1d |\n| 8 | Unify `BuildSSGOptions` single source of truth | `cli/build-ssg.ts`, `build-context.ts` | 0.5d |\n\n### Total: 4d\n\n---\n\n## Technical Notes\n\n### Why esbuild instead of viteBuild for SSR bundle?\n\n`esbuild --format=esm --packages=external` preserves bare specifiers so the importmap can resolve them. It's also ~40× faster than Vite SSR build for this task (~50ms vs ~2s).\n\nThe virtual modules (`virtual:less-ssg-entry`, `virtual:less-blog-data`) are handled by **string inlining** via esbuild's `--define:` instead of Vite plugin resolvers.\n\n### URLPattern fallback strategy\n\nKeep the hand-rolled `extractParams()` as `extractParamsFallback()` until Node.js 19+ becomes the minimum supported version (~v0.15). The guard adds <10 lines and zero dependencies.\n\n### What we keep\n\n- `parse5` in `@openelement/core` — WHATWG-standard HTML parser, right tool for AST-based DSD rendering\n- `URLSearchParams` in `parseQuery()` — already standard, no change\n- Vite for dev mode and client bundle — unchanged, best-in-class HMR\n\n### What we replace\n\n- `import('./dist/server/entry.js')` without importmap → with importmap\n- Hand-rolled route parameter parser → `URLPattern` (WHATWG standard)\n",
    "html": "<h1>ADR 0022: ESM-Native LessJS — Pure ESM Imports Across Browser/Deno/Bun/CF Workers</h1>\n<h2>Status</h2>\n<p>✅ Proposed</p>\n<h2>Core Thesis</h2>\n<p><strong>LessJS should work as pure native ESM imports — runnable in browser, Deno, Bun, Node.js, and Cloudflare Workers — with Vite being an optional accelerator for dev mode and client bundling, not a hard requirement for the SSG pipeline.</strong></p>\n<p>This means:</p>\n<ul>\n<li>Phase 1 (route scanning + SSR bundle generation) must produce <strong>standard ESM modules</strong>, not Vite-coupled bundles</li>\n<li>Phase 3 (SSG rendering) must be a <strong>standalone process</strong> that loads SSR bundles via <code>import()</code> — no Vite plugin required</li>\n<li>The SSR bundle must carry its own dependency map (<code>importmap.json</code>) so it resolves bare specifiers in <strong>any runtime</strong></li>\n<li><code>@openelement/core</code> must use only <strong>Web Standard APIs</strong> — zero <code>node:*</code> imports, zero platform-specific assumptions</li>\n</ul>\n<h2>Context</h2>\n<h3>Problem 1: SSR bundle is Vite-coupled, breaks outside Vite process</h3>\n<p>LessJS Phase 3 (SSG) currently runs inside <code>closeBundle()</code> of the <code>less:build</code> Vite plugin. It builds a self-contained SSR bundle via <code>viteBuild(ssr: true, noExternal: [...])</code>, then imports it in the same process.</p>\n<p>This bundle contains bare specifiers (<code>import &#39;hono&#39;</code>, <code>import &#39;@openelement/core&#39;</code>) that rely on the <strong>ambient process import map</strong> to resolve. When a user installs LessJS from JSR (<code>jsr:@openelement/app</code>), Deno&#39;s workspace import map covers these. But with <code>npm:@jsr/lessjs__app</code>, the npm resolver doesn&#39;t know about them, and Phase 3 fails.</p>\n<p><strong>Root cause</strong>: The SSR bundle has no self-declared dependency map — it assumes the loading environment will resolve its bare specifiers.</p>\n<h3>Problem 2: Target runtime diversity demands portable ESM</h3>\n<p>LessJS users deploy to:</p>\n<ul>\n<li><strong>Cloudflare Workers</strong> — no filesystem, no Node APIs, pure Service Worker scope</li>\n<li><strong>Deno</strong> — native ESM, import maps</li>\n<li><strong>Bun</strong> — native ESM, Node compat</li>\n<li><strong>Node.js</strong> — ESM with <code>--experimental-json-modules</code></li>\n<li><strong>Browser</strong> — native <code>&lt;script type=&quot;module&quot;</code> with importmap</li>\n</ul>\n<p>Each has different resolution semantics. A Vite-bundled SSR bundle with inlined dependencies works in none of them consistently. A native ESM module with an explicit importmap works in all of them.</p>\n<h3>Problem 3: Hand-rolled URL pattern matching in core</h3>\n<p><code>core/src/context.ts</code> has a hand-rolled <code>extractParams()</code> function that parses route patterns (<code>/posts/:id</code> → pathname <code>/posts/123</code> → <code>{id: &#39;123&#39;}</code>). This duplicates WHATWG&#39;s <strong>URLPattern</strong> standard API, which is now available across all target runtimes (Deno native, Node 19+, Bun native, all browsers).</p>\n<h3>What was considered and rejected</h3>\n<p><strong>parse5 replacement</strong>: <code>parse5</code> is the WHATWG-standard HTML parser, used for AST-based nested DSD rendering in <code>render-nested.ts</code>. Considered replacing it with an inline HTML walker for &quot;zero-dependency core&quot;, but rejected because:</p>\n<ul>\n<li>parse5 has zero functional overhead for our use case — it IS the standard</li>\n<li>A hand-rolled walker would lost WHATWG compliance (edge cases like nested <code>&lt;template&gt;</code>, HTML entity decoding inside attributes, quoted attribute values containing <code>&gt;</code>)</li>\n<li>parse5 is only used in SSR build, not shipped to browsers (tree-shaken)</li>\n<li>The &quot;zero dependency&quot; benefit does not solve any real user problem</li>\n</ul>\n<p><strong><code>parseQuery()</code> replacement</strong>: Already uses <code>URLSearchParams</code> internally. No change needed.</p>\n<h3>Scope</h3>\n<p>This ADR covers changes to align LessJS with its core thesis:</p>\n<ol>\n<li><strong>Phase 1 + Phase 3 → Pure ESM pipeline</strong> — SSR bundle carries its own <code>importmap.json</code>, Phase 3 runs standalone in any runtime. Vite retained for dev HMR + client bundling only.</li>\n<li><strong><code>@openelement/core</code> → Web Standards only</strong> — Already zero <code>node:*</code>, already zero Vite. Replace hand-rolled route matching with WHATWG <code>URLPattern</code>. Retain <code>parse5</code> (WHATWG-standard HTML parser, necessary for AST-based nested DSD).</li>\n</ol>\n<h2>Decision</h2>\n<h3>0. Every Artifact is Pure ESM — Runtime Agnostic</h3>\n<p>All LessJS build artifacts must be <strong>standard ESM modules</strong> that can be loaded via <code>import()</code> in any runtime. No artifact may assume the presence of Vite, Node.js APIs (<code>node:fs</code>, <code>node:path</code>), Deno-specific APIs, or any specific runtime&#39;s module resolution semantics.</p>\n<p>This applies to:</p>\n<table>\n<thead>\n<tr>\n<th>Artifact</th>\n<th>Format</th>\n<th>Loaded by</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>SSR bundle (<code>dist/server/entry.mjs</code>)</td>\n<td>Pure ESM with bare specifiers</td>\n<td>Phase 3 SSG in any runtime via <code>import()</code></td>\n</tr>\n<tr>\n<td>Client islands (<code>dist/client/islands/*.js</code>)</td>\n<td>ESM chunks</td>\n<td>Browser via <code>&lt;script type=&quot;module&quot;&gt;</code></td>\n</tr>\n<tr>\n<td>HTML pages (<code>dist/**/*.html</code>)</td>\n<td>Static HTML + DSD</td>\n<td>Browser natively</td>\n</tr>\n<tr>\n<td><code>@openelement/core</code> exports</td>\n<td>Pure ESM with only relative/web-standard imports</td>\n<td>SSR bundle, client islands, user code</td>\n</tr>\n</tbody></table>\n<p><strong>Enforcement rules:</strong></p>\n<ul>\n<li>No <code>node:*</code> imports in <code>@openelement/core</code> (already done ✓)</li>\n<li>No Vite types or APIs referenced in core (already done ✓)</li>\n<li>SSR bundle must carry <code>importmap.json</code> so it resolves bare specifiers in any runtime</li>\n<li>Phase 3 SSG CLI must work in Deno, Bun, Node.js, and Cloudflare Workers without modification</li>\n<li><code>parse5</code> is acceptable (WHATWG standard, available on npm/JSR, no runtime lock-in)</li>\n<li><code>URLSearchParams</code> replaces hand-rolled query parsing (already done ✓)</li>\n<li><code>URLPattern</code> replaces hand-rolled route param extraction (P1)</li>\n</ul>\n<h3>1. Phase 1 + 3: Native ESM SSG Pipeline</h3>\n<h4>Current flow (Vite-coupled)</h4>\n<pre><code>Phase 1 (Vite closeBundle):\n  scanRoutes() → genHonoEntry() → viteBuild(ssr: true, noExternal: all)\n  → dist/server/entry.js          ← bare specifiers, no self-declared deps\n  → Phase 2 (viteBuild client)\n  → Phase 3 (import(&#39;./dist/server/entry.js&#39;), render all)\n</code></pre>\n<p>Cannot run outside Vite process because bare specifiers rely on ambient import map.</p>\n<h4>Proposed flow (ESM-native)</h4>\n<pre><code>Phase 1 (Vite closeBundle or standalone):\n  scanRoutes() → genHonoEntry()\n  → esbuild --format=esm --packages=external    ← preserves bare specifiers\n  → dist/server/entry.mjs + dist/server/importmap.json\n\nPhase 2 (client bundling — Vite only):\n  viteBuild(client islands)                     ← unchanged\n\nPhase 3 (SSG — any runtime):\n  read dist/server/importmap.json\n  → load dist/server/entry.mjs with importmap\n  → renderAll(routes)\n  → write HTML files\n\nTarget runtimes:\n  ✅ Deno:   deno run --import-map=importmap.json ssg.mjs\n  ✅ Bun:    bun ssg.mjs                          ← natively supports importmap\n  ✅ Node:   node --experimental-network-imports ssg.mjs\n  ✅ CF:     wrangler ssg.mjs                     ← Workers runtime supports ESM\n  ✅ Browser: not applicable (SSG runs at build time)\n</code></pre>\n<h4>Sidecar importmap.json format</h4>\n<pre><code class=\"language-json\">{\n  &quot;imports&quot;: {\n    &quot;hono&quot;: &quot;npm:hono@4.7.0&quot;,\n    &quot;lit&quot;: &quot;npm:lit@3.3.2&quot;,\n    &quot;@lit/reactive-element&quot;: &quot;npm:@lit/reactive-element@2.1.0&quot;,\n    &quot;@openelement/core&quot;: &quot;npm:@jsr/lessjs__core@0.13.0&quot;,\n    &quot;@openelement/core/logger&quot;: &quot;npm:@jsr/lessjs__core@0.13.0/logger&quot;,\n    &quot;@openelement/adapter-lit&quot;: &quot;npm:@jsr/lessjs__adapter-lit@0.8.0&quot;,\n    &quot;parse5&quot;: &quot;npm:parse5@7.0.0&quot;\n  }\n}\n</code></pre>\n<p>Generated by Phase 1 from:</p>\n<ul>\n<li>The project&#39;s <code>deno.json</code> / <code>package.json</code> imports</li>\n<li>Resolved versions of <code>@openelement/*</code> packages</li>\n<li>User-configured <code>ssr.noExternal</code> patterns</li>\n</ul>\n<h4>Phase 3 loading strategy</h4>\n<pre><code class=\"language-typescript\">// ssg-cli.ts — standalone, no Vite dependency\nasync function loadSsrBundle(ssrDir: string) {\n  const importmapPath = resolve(ssrDir, &#39;importmap.json&#39;);\n  const entryPath = resolve(ssrDir, &#39;entry.mjs&#39;);\n\n  const importmap = JSON.parse(readFileSync(importmapPath, &#39;utf-8&#39;));\n\n  // In Deno: deno run --import-map=./dist/server/importmap.json\n  // In Bun: natively supports importmap.json\n  // In Node: resolve bare specifiers manually via the importmap before import()\n  const entryUrl = pathToFileURL(entryPath).href;\n  return await import(entryUrl);\n}\n</code></pre>\n<p><strong>Key insight</strong>: Switch from <code>viteBuild(ssr: true, noExternal: all)</code> (everything inlined) to <strong>esbuild <code>--format=esm --packages=external</code></strong> (bare specifiers preserved). The importmap provides the resolution map.</p>\n<h4>Dev mode impact</h4>\n<p><strong>None</strong>. Vite dev server continues to handle HMR, dev proxy, and dev module resolution unchanged. The importmap is only produced during <code>build</code>.</p>\n<hr />\n<h3>2. URLPattern Adoption</h3>\n<h4>Current code</h4>\n<pre><code class=\"language-typescript\">// core/src/context.ts — hand-rolled, ~30 lines\nexport function extractParams(\n  pattern: string,\n  pathname: string,\n): Record&lt;string, string&gt; {\n  const params: Record&lt;string, string&gt; = {};\n  const patternParts = pattern.split(&#39;/&#39;);\n  const pathParts = pathname.split(&#39;/&#39;);\n  for (let i = 0; i &lt; patternParts.length; i++) {\n    const part = patternParts[i];\n    if (part.startsWith(&#39;:&#39;)) {\n      const paramName = part.slice(1);\n      if (i &lt; pathParts.length) {\n        params[paramName] = decodeURIComponent(pathParts[i]);\n      }\n    }\n  }\n  return params;\n}\n</code></pre>\n<p>Does not handle:</p>\n<ul>\n<li>Optional params (<code>/posts/:id?</code>)</li>\n<li>Wildcards (<code>/posts/*</code>)</li>\n<li>Regex constraints (<code>/posts/:id(\\\\d+)</code>)</li>\n</ul>\n<h4>Replacement</h4>\n<pre><code class=\"language-typescript\">// core/src/context.ts — using WHATWG URLPattern\nexport function extractParams(\n  pattern: string,\n  pathname: string,\n): Record&lt;string, string&gt; {\n  const urlPattern = new URLPattern({ pathname: pattern });\n  const match = urlPattern.exec({ pathname, protocol: &#39;https&#39;, hostname: &#39;localhost&#39; });\n  return match?.pathname?.groups ?? {};\n}\n</code></pre>\n<p>URLPattern supports:</p>\n<ul>\n<li><code>:id</code> → named groups</li>\n<li><code>:id?</code> → optional</li>\n<li><code>*</code> → wildcard</li>\n<li><code>(\\\\d+)</code> → regex constraints</li>\n<li>All per WHATWG spec</li>\n</ul>\n<h4>Runtime availability</h4>\n<table>\n<thead>\n<tr>\n<th>Runtime</th>\n<th>URLPattern support</th>\n<th>Notes</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Deno 1.33+</td>\n<td>✅ Native</td>\n<td>No flags</td>\n</tr>\n<tr>\n<td>Node.js 19+</td>\n<td>✅</td>\n<td>Under <code>--experimental-url-pattern</code> flag</td>\n</tr>\n<tr>\n<td>Bun 1.0+</td>\n<td>✅ Native</td>\n<td>No flags</td>\n</tr>\n<tr>\n<td>Chrome/Edge 95+</td>\n<td>✅ Native</td>\n<td></td>\n</tr>\n<tr>\n<td>Firefox 113+</td>\n<td>✅ Native</td>\n<td></td>\n</tr>\n<tr>\n<td>Safari 16.4+</td>\n<td>✅ Native</td>\n<td></td>\n</tr>\n</tbody></table>\n<p>LessJS targets Deno + Node. Use with runtime guard:</p>\n<pre><code class=\"language-typescript\">export function extractParams(pattern: string, pathname: string): Record&lt;string, string&gt; {\n  if (typeof URLPattern !== &#39;undefined&#39;) {\n    const urlPattern = new URLPattern({ pathname: pattern });\n    const match = urlPattern.exec({ pathname, protocol: &#39;https&#39;, hostname: &#39;localhost&#39; });\n    return match?.pathname?.groups ?? {};\n  }\n  // Fallback: existing hand-rolled implementation\n  return extractParamsFallback(pattern, pathname);\n}\n</code></pre>\n<hr />\n<h2>Consequences</h2>\n<h3>Positive</h3>\n<ol>\n<li><strong>True ESM-native architecture</strong> — LessJS SSG runs in any runtime that supports ESM <code>import()</code>. No Vite process needed for production builds.</li>\n<li><strong>Zero-friction install</strong> — No <code>npm:</code> vs <code>jsr:</code> distinction. All install paths (npm, JSR, workspace) converge on the same Phase 3 behaviour.</li>\n<li><strong>Platform-agnostic deployment</strong> — <code>dist/</code> is pure static HTML + ESM islands. SSG runs equally well in CI on Deno, Node, or Bun.</li>\n<li><strong>Standard-compliant route matching</strong> — URLPattern supports optional params, wildcards, regex constraints. Future-proof.</li>\n<li><strong>parse5 retained</strong> — WHATWG-standard HTML parsing for correct AST-based DSD rendering.</li>\n<li><strong>Vite stays where it shines</strong> — Dev HMR and client bundling remain Vite&#39;s job. No reinvention needed.</li>\n</ol>\n<h3>Negative</h3>\n<ol>\n<li><strong>Importmap generation adds logic to Phase 1</strong> — Must scan project config for version info (~50 lines)</li>\n<li><strong>Two SSR bundle modes</strong> — Old <code>viteBuild(noExternal)</code> inline mode retained for backward compat; new importmap mode as default</li>\n<li><strong>URLPattern needs a fallback</strong> — Node.js &lt;19 users get the hand-rolled version</li>\n<li><strong>No change to <code>parseQuery()</code></strong> — Already uses <code>URLSearchParams</code>, correctly handles multi-value keys</li>\n</ol>\n<h3>Migration</h3>\n<p><strong>Phase 3 importmap (v0.14)</strong>:</p>\n<ul>\n<li>Phase 1 additionally produces <code>dist/server/entry.mjs</code> + <code>importmap.json</code></li>\n<li>Both old (Vite inline) and new (ESM + importmap) modes coexist</li>\n<li>Phase 3 CLI defaults to importmap mode, falls back to Vite inline</li>\n</ul>\n<p><strong>URLPattern (v0.14)</strong>:</p>\n<ul>\n<li>Drop-in replacement for <code>extractParams()</code> with runtime guard</li>\n<li>Remove fallback in v0.15 after Node &lt;19 support window ends</li>\n</ul>\n<hr />\n<h2>Implementation Plan</h2>\n<h3>P0 (2.5d) — Solve the friction</h3>\n<table>\n<thead>\n<tr>\n<th>#</th>\n<th>Task</th>\n<th>Files</th>\n<th>Est.</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>1</td>\n<td>Phase 1: emit <code>importmap.json</code> alongside SSR bundle</td>\n<td><code>cli/build-ssg.ts</code></td>\n<td>0.5d</td>\n</tr>\n<tr>\n<td>2</td>\n<td>Phase 3: standalone SSG CLI using importmap</td>\n<td>new <code>cli/ssg.ts</code></td>\n<td>1d</td>\n</tr>\n<tr>\n<td>3</td>\n<td>Move <code>@hono/vite-dev-server</code> to optional dep</td>\n<td><code>adapter-vite/deno.json</code></td>\n<td>0.5d</td>\n</tr>\n<tr>\n<td>4</td>\n<td>Document importmap approach in README</td>\n<td>README.md</td>\n<td>0.5d</td>\n</tr>\n</tbody></table>\n<h3>P1 (1.5d) — Standard API alignment</h3>\n<table>\n<thead>\n<tr>\n<th>#</th>\n<th>Task</th>\n<th>Files</th>\n<th>Est.</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>5</td>\n<td>Replace <code>extractParams()</code> with <code>URLPattern</code> + fallback</td>\n<td><code>core/src/context.ts</code></td>\n<td>0.5d</td>\n</tr>\n<tr>\n<td>6</td>\n<td>Test URLPattern route matching for all patterns</td>\n<td><code>__tests__/context.test.ts</code></td>\n<td>0.5d</td>\n</tr>\n<tr>\n<td>7</td>\n<td>Remove the &quot;parse5 replacement&quot; section from all docs</td>\n<td>-</td>\n<td>0.1d</td>\n</tr>\n<tr>\n<td>8</td>\n<td>Unify <code>BuildSSGOptions</code> single source of truth</td>\n<td><code>cli/build-ssg.ts</code>, <code>build-context.ts</code></td>\n<td>0.5d</td>\n</tr>\n</tbody></table>\n<h3>Total: 4d</h3>\n<hr />\n<h2>Technical Notes</h2>\n<h3>Why esbuild instead of viteBuild for SSR bundle?</h3>\n<p><code>esbuild --format=esm --packages=external</code> preserves bare specifiers so the importmap can resolve them. It&#39;s also <del>40× faster than Vite SSR build for this task (</del>50ms vs ~2s).</p>\n<p>The virtual modules (<code>virtual:less-ssg-entry</code>, <code>virtual:less-blog-data</code>) are handled by <strong>string inlining</strong> via esbuild&#39;s <code>--define:</code> instead of Vite plugin resolvers.</p>\n<h3>URLPattern fallback strategy</h3>\n<p>Keep the hand-rolled <code>extractParams()</code> as <code>extractParamsFallback()</code> until Node.js 19+ becomes the minimum supported version (~v0.15). The guard adds &lt;10 lines and zero dependencies.</p>\n<h3>What we keep</h3>\n<ul>\n<li><code>parse5</code> in <code>@openelement/core</code> — WHATWG-standard HTML parser, right tool for AST-based DSD rendering</li>\n<li><code>URLSearchParams</code> in <code>parseQuery()</code> — already standard, no change</li>\n<li>Vite for dev mode and client bundle — unchanged, best-in-class HMR</li>\n</ul>\n<h3>What we replace</h3>\n<ul>\n<li><code>import(&#39;./dist/server/entry.js&#39;)</code> without importmap → with importmap</li>\n<li>Hand-rolled route parameter parser → <code>URLPattern</code> (WHATWG standard)</li>\n</ul>\n"
  },
  {
    "slug": "0021-api-surface-pipeline-phase-check",
    "frontmatter": {
      "title": "ADR 0021: A+ 架构冲刺 — API 表面收敛 + 管道化 + 参数校验",
      "date": "2026-05-12",
      "draft": false,
      "tags": [
        "architecture",
        "decision"
      ],
      "type": "adr",
      "lang": "zh"
    },
    "content": "\n## 状态\n\n**IMPLEMENTED** (v0.13.0)\n\n> 注意：`build-context.ts` 和 `build.ts` 实际于 v0.11 迁移至 `@openelement/adapter-vite`，ADR 撰写时 core 尚未拆分完毕。最终实现中 virtual-ids 也位于 `@openelement/adapter-vite/virtual-ids` 而非 `@openelement/core/constants`。\n\n## 日期\n\n2026-05-12\n\n## 修改文件\n\n- `packages/core/deno.json` — exports 裁剪\n- `packages/core/src/constants.ts` — 迁出 Vite 相关 ID\n- `packages/core/src/build-context.ts` — Phase branded types\n- `packages/core/src/build.ts` — closeBundle 替代为 chain\n- `packages/adapter-vite/deno.json`\n- `packages/adapter-vite/src/index.ts`\n- `packages/adapter-vite/src/cli/build-ssg.ts`\n- `.github/workflows/test.yml` — 加 coverage flag\n- `deno.json` — 加 coverage task\n- 各 barrel 文件 inline（5 处）\n\n## 背景\n\n2026-05-12 全面架构审查得出评级 A-。核心问题：\n\n1. **core 公共 API 表面过大**：10 个 subpath exports 中 4 个是内部实现细节，因生成代码需要而被迫公开\n2. **无测试覆盖率度量**：CI 有 452 测试但无 `--coverage`\n3. **3 个 barrel 文件仅聚合导出**：`content/src/nav/index.ts`、`content/src/sitemap/index.ts`、`content/src/pages/index.ts`\n4. **`@openelement/core/constants` 包含 Vite 虚拟模块 ID**：core 不应知道 Vite 的内部机制\n5. **Phase 顺序无编译期保证**：`build()` 能在 Phase1 就绪前被调用\n\n## 决策\n\n### 1. core 导出收敛到 6 个公共 API\n\n保留：\n\n- `.`（主入口）\n- `./errors` — 用户需要 `LessError`\n- `./context` — 用户需要 `SsrContext`\n- `./logger` — 用户需要 `createLogger`\n- `./navigation` — 用户需要 `navigate`/`onNavigate`\n- `./constants` — 公共常量（仅跨包共享的配置常量）\n\n移除（移到 adapter-vite 的 `CORE_SUBPATHS` 映射中）：\n\n- `./render-dsd` — 生成代码导入，非用户 API\n- `./html-escape` — 内部 HTML 转义，非用户 API\n- `./adapter-registry` — 内部适配器注册，非用户 API\n- `./ssr-handler` — 已删除文件，残留导出\n\n**影响**：生成代码通过 `CORE_SUBPATHS` + `JSR remote resolution` 解析，不受影响。\n\n### 2. CI 添加覆盖率收集\n\n`deno test --coverage` 生成覆盖率报告，在 test.yml 中每个 job 末尾运行。\n\n### 3. 零 barrel\n\n`content/src/nav/index.ts`、`content/src/sitemap/index.ts` 两个文件的导出直接 inline 到 `content/src/index.ts`，删除 barrel 文件。\n\n### 4. core-Vite 分离\n\n`@openelement/core/constants` 只保留纯配置常量。Vite 虚拟模块 ID（`VIRTUAL_BLOG_DATA_ID`、`RESOLVED_NAV_ID` 等）迁到 `@openelement/adapter-vite` 的 `virtual-ids.ts`。\n\n### 5. 编译期 Phase 顺序校验 (Branded Type 状态机)\n\n```typescript\ntype Phase1Ready = { __phase: 'phase1' };\ntype Phase2Ready = { __phase: 'phase2' };\ntype Phase3Ready = { __phase: 'phase3' };\n\nfunction buildPhase1(ctx: LessBuildContext): Phase1Ready & { ... }\nfunction buildPhase2(state: Phase1Ready): Phase2Ready & { ... }\nfunction buildPhase3(state: Phase2Ready): void\n```\n\n`buildPhase2` 只接受 `Phase1Ready` 的返回值，`buildPhase3` 只接受 `Phase2Ready`。编译器阻止乱序调用。\n\n## 后果\n\n- core 公共 API 从 10 个降到 6 个\n- CI 每次运行产生覆盖率报告\n- barrel 文件清零\n- Vite 知识不在 core 中存在\n- Phase 顺序错误被编译器捕获而不是运行时 500\n- 项目从 A- 前进到 A\n\n## 未完成（A+ 剩余项）\n\n- LessBuildContext → 纯函数管线：当前已是中间态（ctx 显式传递），完全纯函数化需 3-5 天专门工作\n- 正式插件生命周期：`registerAdapter()` 是唯一入口，完整的 pre/post hooks 需 2 天\n",
    "html": "<h2>状态</h2>\n<p><strong>IMPLEMENTED</strong> (v0.13.0)</p>\n<blockquote>\n<p>注意：<code>build-context.ts</code> 和 <code>build.ts</code> 实际于 v0.11 迁移至 <code>@openelement/adapter-vite</code>，ADR 撰写时 core 尚未拆分完毕。最终实现中 virtual-ids 也位于 <code>@openelement/adapter-vite/virtual-ids</code> 而非 <code>@openelement/core/constants</code>。</p>\n</blockquote>\n<h2>日期</h2>\n<p>2026-05-12</p>\n<h2>修改文件</h2>\n<ul>\n<li><code>packages/core/deno.json</code> — exports 裁剪</li>\n<li><code>packages/core/src/constants.ts</code> — 迁出 Vite 相关 ID</li>\n<li><code>packages/core/src/build-context.ts</code> — Phase branded types</li>\n<li><code>packages/core/src/build.ts</code> — closeBundle 替代为 chain</li>\n<li><code>packages/adapter-vite/deno.json</code></li>\n<li><code>packages/adapter-vite/src/index.ts</code></li>\n<li><code>packages/adapter-vite/src/cli/build-ssg.ts</code></li>\n<li><code>.github/workflows/test.yml</code> — 加 coverage flag</li>\n<li><code>deno.json</code> — 加 coverage task</li>\n<li>各 barrel 文件 inline（5 处）</li>\n</ul>\n<h2>背景</h2>\n<p>2026-05-12 全面架构审查得出评级 A-。核心问题：</p>\n<ol>\n<li><strong>core 公共 API 表面过大</strong>：10 个 subpath exports 中 4 个是内部实现细节，因生成代码需要而被迫公开</li>\n<li><strong>无测试覆盖率度量</strong>：CI 有 452 测试但无 <code>--coverage</code></li>\n<li><strong>3 个 barrel 文件仅聚合导出</strong>：<code>content/src/nav/index.ts</code>、<code>content/src/sitemap/index.ts</code>、<code>content/src/pages/index.ts</code></li>\n<li><strong><code>@openelement/core/constants</code> 包含 Vite 虚拟模块 ID</strong>：core 不应知道 Vite 的内部机制</li>\n<li><strong>Phase 顺序无编译期保证</strong>：<code>build()</code> 能在 Phase1 就绪前被调用</li>\n</ol>\n<h2>决策</h2>\n<h3>1. core 导出收敛到 6 个公共 API</h3>\n<p>保留：</p>\n<ul>\n<li><code>.</code>（主入口）</li>\n<li><code>./errors</code> — 用户需要 <code>LessError</code></li>\n<li><code>./context</code> — 用户需要 <code>SsrContext</code></li>\n<li><code>./logger</code> — 用户需要 <code>createLogger</code></li>\n<li><code>./navigation</code> — 用户需要 <code>navigate</code>/<code>onNavigate</code></li>\n<li><code>./constants</code> — 公共常量（仅跨包共享的配置常量）</li>\n</ul>\n<p>移除（移到 adapter-vite 的 <code>CORE_SUBPATHS</code> 映射中）：</p>\n<ul>\n<li><code>./render-dsd</code> — 生成代码导入，非用户 API</li>\n<li><code>./html-escape</code> — 内部 HTML 转义，非用户 API</li>\n<li><code>./adapter-registry</code> — 内部适配器注册，非用户 API</li>\n<li><code>./ssr-handler</code> — 已删除文件，残留导出</li>\n</ul>\n<p><strong>影响</strong>：生成代码通过 <code>CORE_SUBPATHS</code> + <code>JSR remote resolution</code> 解析，不受影响。</p>\n<h3>2. CI 添加覆盖率收集</h3>\n<p><code>deno test --coverage</code> 生成覆盖率报告，在 test.yml 中每个 job 末尾运行。</p>\n<h3>3. 零 barrel</h3>\n<p><code>content/src/nav/index.ts</code>、<code>content/src/sitemap/index.ts</code> 两个文件的导出直接 inline 到 <code>content/src/index.ts</code>，删除 barrel 文件。</p>\n<h3>4. core-Vite 分离</h3>\n<p><code>@openelement/core/constants</code> 只保留纯配置常量。Vite 虚拟模块 ID（<code>VIRTUAL_BLOG_DATA_ID</code>、<code>RESOLVED_NAV_ID</code> 等）迁到 <code>@openelement/adapter-vite</code> 的 <code>virtual-ids.ts</code>。</p>\n<h3>5. 编译期 Phase 顺序校验 (Branded Type 状态机)</h3>\n<pre><code class=\"language-typescript\">type Phase1Ready = { __phase: &#39;phase1&#39; };\ntype Phase2Ready = { __phase: &#39;phase2&#39; };\ntype Phase3Ready = { __phase: &#39;phase3&#39; };\n\nfunction buildPhase1(ctx: LessBuildContext): Phase1Ready &amp; { ... }\nfunction buildPhase2(state: Phase1Ready): Phase2Ready &amp; { ... }\nfunction buildPhase3(state: Phase2Ready): void\n</code></pre>\n<p><code>buildPhase2</code> 只接受 <code>Phase1Ready</code> 的返回值，<code>buildPhase3</code> 只接受 <code>Phase2Ready</code>。编译器阻止乱序调用。</p>\n<h2>后果</h2>\n<ul>\n<li>core 公共 API 从 10 个降到 6 个</li>\n<li>CI 每次运行产生覆盖率报告</li>\n<li>barrel 文件清零</li>\n<li>Vite 知识不在 core 中存在</li>\n<li>Phase 顺序错误被编译器捕获而不是运行时 500</li>\n<li>项目从 A- 前进到 A</li>\n</ul>\n<h2>未完成（A+ 剩余项）</h2>\n<ul>\n<li>LessBuildContext → 纯函数管线：当前已是中间态（ctx 显式传递），完全纯函数化需 3-5 天专门工作</li>\n<li>正式插件生命周期：<code>registerAdapter()</code> 是唯一入口，完整的 pre/post hooks 需 2 天</li>\n</ul>\n"
  },
  {
    "slug": "0020-dsd-engine-islands-enhancement",
    "frontmatter": {
      "title": "ADR 0020: DSD 渲染引擎与 Islands 策略增强 — 核心护城河建设",
      "date": "2026-05-12",
      "draft": false,
      "tags": [
        "architecture",
        "decision"
      ],
      "type": "adr",
      "lang": "zh"
    },
    "content": "\n## Status\n\n**PROPOSED** — v0.14.0+ 目标\n\n## Context\n\n### 战略背景\n\nADR 0019（综合改进计划）确立了\"先固本，后拓疆\"的策略。Phase 0/1 解决的是质量缺口的补救。**但护城河不是修出来的——护城河是在别人没去过的地方挖出来的。**\n\n四份审查报告的共识结论：LessJS 最大的差异化在于 **DSD-first**。然而当前这个差异化是理念层面的，到了开发者体验层面，DSD 的独特价值并未充分展现——开发者看到的依然是\"Lit + Vite 构建的又一个 SSG\"。\n\nAstro 6.x、Fresh 2.0 beta、Qwik、Lume 3 都没有以 DSD 为核心模型。这意味着 **DSD 相关的能力建设是 LessJS 唯一不可被快速复制的竞争壁垒**。如果 LessJS 能在 DSD 领域做到\"这个体验只有这里有\"，就获得了真正的时间窗口。\n\n### 问题分析\n\n#### 问题 1：DSD 对开发者不可见\n\nDSD 的核心价值（原生 CSS 封装、零 JS 组件边界、浏览器级渲染）在构建产物中是隐形的。开发者在 dev 工具中看不到 DSD 的影子：\n\n```\nDSD 在浏览器中:  DOM 树上看到一个 <template shadowrootmode=\"open\"> \n                  → 开发者需要手动打开 Elements 面板去确认\n                  → 无法直观看到\"哪些组件用了 DSD、哪些没用到\"\n                  → 无法验证 DSD 嵌套是否正确\n                  → 无法区分 DSD Interactive 和 DSD Static\n```\n\n**对比**：Vue 开发者能看到 Vue DevTools 面板里的组件树；React 开发者能看到 Components 面板。DSD 开发者——什么都没有。\n\n#### 问题 2：Island 策略缺乏智能\n\n当前 4 种策略（eager/lazy/visible/idle）依赖开发者手动指定。但在实践中：\n\n- 开发者经常不清楚某个组件应该用什么策略\n- 策略选择缺乏客观标准（\"这个组件交互复杂吗？\"是主观判断）\n- 同一组件的策略在页面的不同实例中可能不同\n- 没有运行时反馈来验证策略是否合理\n\n#### 问题 3：DSD 渲染引擎能力边界\n\n当前 DSD 渲染引擎支持 L2 嵌套（通过 parse5 AST O(n·d)），但：\n\n- 深层嵌套（L3+）的性能未见公开基准\n- 无流式输出能力（大页面需全部渲染完成才输出）\n- 无 DSD 错误定位机制（渲染失败时，开发者不知哪个组件的哪个属性出了问题）\n- 无 DSD 渲染报告（构建后无法查看页面 DSD 的\"健康度\"）\n\n### 竞品对比：DSD 体验差距\n\n| 能力                         | Astro | Fresh | Qwik | LessJS 当前 | LessJS 目标 |\n| ---------------------------- | ----- | ----- | ---- | ----------- | ----------- |\n| DSD DevTools 面板            | ❌    | ❌    | ❌   | ❌          | ✅ **独家** |\n| DSD Tree View                | ❌    | ❌    | ❌   | ❌          | ✅ **独家** |\n| DSD Hydration 监控           | ❌    | ❌    | ❌   | ❌          | ✅ **独家** |\n| DSD 渲染报告                 | ❌    | ❌    | ❌   | ❌          | ✅ **独家** |\n| Island 策略推荐              | ❌    | ❌    | ❌   | ❌          | ✅ **独家** |\n| Speculative Loading 深度集成 | 基础  | ❌    | ❌   | 基础        | ✅ **领先** |\n| View Transitions 零配置      | ❌    | ❌    | ❌   | 基础        | ✅ **领先** |\n| DSD Streaming                | ❌    | ❌    | ❌   | ❌          | ✅ **独家** |\n\n**核心结论**：LessJS 站在 DSD 的天然位置，但当前的能力建设程度与这个位置不匹配。DSD-first 不能只是 README 中的一句话——它应该是开发者每一次交互中都能感受到的体验。\n\n## Decision\n\n### 核心原则\n\n**\"让 DSD 好到无法被忽视，而不是让框架小到没有依赖。\"**\n\n### Proposal A: DSD DevTools Panel（v0.14.0）\n\n#### 目标\n\n参考 Astro Dev Toolbar 的模式，在开发环境中注入一个 DSD 调试面板，让 DSD 变得**可见、可审查、可调试**。\n\n#### 实现方案\n\n**注入方式**：Vite Dev Server 在 HTML 响应末尾注入一个 `<script>` 标签，加载 DevTool Panel。\n\n```\nDev 模式 HTML 响应:\n  <html>\n    <head>...</head>\n    <body>\n      ... 页面内容 ...\n      <less-devtool>                                    ← 注入的自定义元素\n        #shadow-root:\n          <panel-toggle>  (右下角浮动按钮)                ← 点击打开面板\n          <panel-overlay> (全屏覆盖)\n             ├── DSD Tree View     → 组件树 + DSD 层级\n             ├── Hydration Status  → 哪些跳过渲染、哪些做了\n             ├── Island Explorer → 策略分布与实际行为\n             └── Performance Tab   → 渲染时间 / DSD 数量\n      </less-devtool>\n      <script type=\"module\" src=\"/@openelement/devtool\"></script>  ← 虚拟模块入口\n    </body>\n  </html>\n```\n\n**DSD Tree View 数据结构**：\n\n```ts\ninterface DsdTreeNode {\n  tagName: string; // 'less-button', 'home-page', etc.\n  layer: 'dsd-static' | 'dsd-interactive' | 'pure-island';\n  shadowRootAttached: boolean; // 浏览器是否已附加 DSD\n  hydrationSkipped: boolean; // Lit 是否跳过了重渲染\n  childNodes: DsdTreeNode[]; // 嵌套的 DSD 子节点\n  renderTime: number; // SSR 渲染时间 (ms)\n  templateSize: number; // DSD 模板字节数\n}\n\ninterface DsdReport {\n  totalComponents: number;\n  dsdComponents: number; // 有 DSD 模板的\n  hydratedComponents: number; // 跳过了渲染的\n  pureIslands: number;\n  totalDsdSize: number; // 所有 DSD 模板的总 KB\n  maxNestingDepth: number;\n}\n```\n\n**采集方式**：\n\n```\nSSR 渲染阶段:\n  renderDSD() 收集：组件名 → 渲染时间 → DSD 模板大小\n  通过构建上下文传递到 dev 阶段\n\n浏览器运行时:\n  MutationObserver 观察 Custom Elements 升级\n  Lit connectedCallback() 检测 _dsdHydrated 标志\n  汇总数据发送到 DevTool Panel\n```\n\n**运行时校验**：\n\nDevTool Panel 在运行时执行自动检查：\n\n```ts\n// 自动检查清单（面板中显示 4 个状态灯）\nconst checks = {\n  dsdNestingValid: checkNoShadowDOMInjection(), // ✅ DSD 嵌套完整\n  ssrClientMatch: compareSSRandClient(), // ✅ SSR 客户端一致\n  noDuplicateContent: checkBlankBoxes(), // ✅ 无重复渲染\n  islandStrategiesOptimal: evalIslandStrategies(), // ✅ Island 策略合理\n};\n```\n\n#### 为什么是护城河\n\n这个面板 Astro 不可能做——它没有 DSD 为核心。Fresh 不可能做——Preact 不支持 DSD。Qwik 不需要做——它的渲染模型是完全不同的。**只有 LessJS 能以原生 DSD-first 的视角呈现这些信息。**\n\n一旦开发者体验了这个面板，他们就会理解\"为什么 LessJS 选择 DSD\"。这不是营销话术，是一种\"亲眼看到\"的体验。\n\n---\n\n### Proposal B: Island 策略推荐系统（v0.14.0）\n\n#### 目标\n\n消除\"开发者不知道该用哪种策略\"的问题，在构建时自动推荐最优策略。\n\n#### 实现方案\n\n**分析维度**（构建时静态分析）：\n\n```ts\ninterface IslandProfile {\n  // 交互维度\n  hasEventListeners: boolean; // 有 click/input 等事件绑定\n  hasDomMutations: boolean; // 修改了 DOM 结构\n  usesExternalState: boolean; // 依赖外部信号/状态\n  renderComplexity: 'simple' | 'moderate' | 'complex'; // render() 复杂度判断\n\n  // 可见性维度\n  isAboveFold: boolean; // 是否在首屏\n  isInViewport: boolean; // 初始可见（非懒加载区域）\n\n  // 性能维度\n  estimatedChunkSize: number; // 估算的 JS chunk 大小\n  dependencyCount: number; // 依赖图大小\n}\n\ntype RecommendedStrategy = 'eager' | 'lazy' | 'visible' | 'idle';\n```\n\n**决策规则**：\n\n```\nif (isAboveFold || hasEventListeners):        → eager      (首屏交互)\nelse if (isInViewport && hasDomMutations):    → lazy       (中低优先级)\nelse if (renderComplexity === 'complex'):      → visible    (IntersectionObserver)\nelse:                                          → idle       (requestIdleCallback)\n```\n\n**开发者交互**：\n\n- 推荐策略输出到构建日志：`[LessJS] less-theme-toggle: recommended = idle (no event listeners, below fold, 0.8KB)`\n- DevTool Panel 中显示当前策略 vs 推荐策略对比\n- 开发者可手动覆盖策略（显式指定的优先级高于推荐）\n\n#### 落地方式\n\n不需要复杂的 AST 全量分析。通过在 build 阶段的 SSR 上下文中收集元数据：\n\n```ts\n// SSR 渲染时，renderDSD() 收集 island 元数据\nfunction renderDSD(tagName, cls, props, source, options) {\n  // ... existing rendering logic ...\n\n  // NEW: collect island profile\n  if (options?.layer === 'pure-island' || options?.layer === 'dsd-interactive') {\n    ctx.collectIslandProfile({\n      tagName,\n      componentSource: source?.source,\n      chunkSize: estimatedSize,\n      hasEvents: detectEventBindings(cls),\n      strategy: options.strategy || 'eager',\n      recommended: recommendStrategy(profile),\n    });\n  }\n\n  // ... continue rendering ...\n}\n```\n\n---\n\n### Proposal C: 无限制 DSD 嵌套 + 渲染报告（v0.15.0）\n\n#### 目标\n\n将 DSD 嵌套从 L2 扩展到无限制，并提供构建时的 DSD 渲染报告。\n\n#### L3+ 嵌套的技术方案\n\n当前 `renderNestedCustomElements()` 使用 parse5 AST 进行自底向上的 DSD 包装。对于 L2 嵌套，一次 AST 遍历就够了。对于 L3+，需要**多次 AST 遍历**：\n\n```\n输入:  <outer-ce>\n         <template shadowrootmode=\"open\">\n           <inner-ce>\n             <template shadowrootmode=\"open\">\n               <p>Hello</p>\n             </template>\n           </inner-ce>\n         </template>\n       </outer-ce>\n\nL3+ 扩展:\n  <outer-ce>                                    ← DSD 由 renderDSD() 包装\n    <template shadowrootmode=\"open\">\n      <inner-ce>                                ← DSD 由 renderNested() 包装\n        <template shadowrootmode=\"open\">\n          <p>Hello</p>\n        </template>\n      </inner-ce>\n    </template>\n  </outer-ce>\n```\n\n**策略**：递归 AST 遍历 + 深度缓存\n\n```ts\n/**\n * 无限制 DSD 嵌套渲染\n *\n * 策略：自底向上递归 AST 遍历\n * 1. parse5 parseFragment(html) → AST\n * 2. 自底向上遍历，每遇到一个 Custom Element\n * 3. 递归渲染其内容中的嵌套 CE\n * 4. 用 <template shadowrootmode> 包裹渲染结果\n * 5. 替换 AST 中的原始 CE 节点\n * 6. 序列化 AST → HTML\n *\n * 性能保障：通过 maxDepth 参数防止栈溢出\n */\nexport async function renderNestedCustomElements(\n  html: string,\n  ctx: LessBuildContext,\n  maxDepth = 10, // 防止栈溢出\n): Promise<string> {\n  // ... parse5 AST 遍历 + 递归渲染 ...\n}\n```\n\n#### DSD 渲染报告\n\n构建完成后输出一份可读的 DSD 健康报告：\n\n```\n=== DSD 渲染报告: 页面 index.html ===\n\nTotal Components:      24\nDSD Components:        18  (75%)\n  ├── DSD Static:      12  (no JS needed)\n  ├── DSD Interactive: 4   (event hydration)\n  └── Pure Island:     2   (client JS chunk)\nNon-DSD Components:    6   (void elements)\n\nDSD Template Size:     12.4 KB\nMax Nesting Depth:     3   (home-page → less-layout → less-button)\nDSD Coverage:          92% (22/24 components have shadow DOM)\n\nIsland Distribution:\n  eager:  1  (less-theme-toggle)\n  idle:   1  (less-hero-ping)\n\nHydration Efficiency:\n  Skipped: 4  (DSD interactive, ready from server)\n  Active:  2  (Pure islands, client JS)\n\n=== Summary: 92% DSD coverage, 12.4KB templates, 2 client chunks ===\n```\n\n---\n\n### Proposal D: Speculative Loading 与 View Transitions 深度集成（v0.14.0 并行）\n\n#### 现状\n\nLessJS 已经为 Island 的资源提示做了策略化注入（eager → modulepreload，其他 → prefetch）。但这是\"浅集成\"——它只做了资源提示，没有利用浏览器的更高级能力。\n\n#### Speculation Rules API 深度集成\n\nWHATWG Speculation Rules API 允许浏览器**预渲染**页面（不仅仅是预取资源）。LessJS 可以利用 SSG 的内置信息生成最优的 Speculation Rules：\n\n```html\n<!-- 自动生成的预渲染规则（基于 SSG 路由图） -->\n<script type=\"speculationrules\">\n{\n  \"prerender\": [\n    {\n      \"source\": \"list\",\n      \"urls\": [\"/\", \"/blog\", \"/about\", \"/docs\"],\n      \"eagerness\": \"moderate\",\n      \"referrer_policy\": \"strict-origin-when-cross-origin\"\n    }\n  ],\n  \"prefetch\": [\n    {\n      \"source\": \"list\",\n      \"urls\": [\"/blog/hello-world\", \"/changelog\", \"/roadmap\"],\n      \"eagerness\": \"conservative\"\n    }\n  ]\n}\n</script>\n```\n\n**智能规则生成逻辑**：\n\n```\nSSG 构建完成后，获取路由表：\n  首页        → prerender (moderate)   → 用户大概率访问\n  博客列表    → prerender (moderate)   → 导航栏链接\n  关于页      → prerender (conservative) → 不太可能但可能\n  博客文章    → prefetch (conservative) → 数量多，只预取不预渲染\n  404页       → 不处理\n\n规则基于路由深度 + 链接在页面中的位置自动生成。\n```\n\n#### View Transitions API 零配置集成\n\n目前 LessJS 已支持 View Transitions 的 SSR meta tag 注入。但开发者仍需手动配置过渡动画。目标：**零配置的同站过渡**。\n\n```\nSSG 构建时分析页面间 DOM 结构差异：\n  如果两个页面共享相同的 <header> <nav> <open-layout> 结构\n  自动添加 view-transition-name 以保持过渡一致性\n\n用户不需要写任何 CSS，框架自动处理：\n  document.startViewTransition(() => { navigate(url); });\n```\n\n---\n\n## Consequences\n\n### Positive\n\n- **DSD DevTools Panel（Proposal A）是最大的差异化武器**。一旦这个面板存在，Astro/Fresh 用户试用 LessJS 后的第一个感受就是\"这个框架把那些我看不见的东西变得可见了\"。这是一个体验层面的护城河\n- **Island 策略推荐（Proposal B）降低认知负担**。策略从\"开发者决策\"变为\"框架建议，开发者选择\"，这是 DX 层面的实质性提升\n- **DSD 渲染报告（Proposal C）把构建产物变成可量化、可优化、可炫耀的东西**。\"我的站点 92% DSD 覆盖，零 JS 渲染\"是一个强有力的产品叙事\n- **Speculative Rules 深度集成（Proposal D）天然只有 SSG 框架能做**。Astro 也能做，但 LessJS 有 Island 策略信息，能生成更精准的预渲染规则\n\n### Negative\n\n- **Proposal A（DevTools Panel）的开发成本最高**（估计 3-4 周），需要 Vite 虚拟模块、Custom Element 面板、DSD 元数据采集三个子系统配合\n- **Proposal B（策略推荐）精度有限**。静态分析无法准确判断\"这个 Island 是否需要交互\"——一些条件绑定的交互在 render() 之前不可见\n- **Proposal C（L3+ 嵌套）可能引入性能回归**。parse5 的多次 AST 遍历加递归渲染需要严格的性能基准测试\n\n### Neutral\n\n- **护城河是时间积累的结果**，不是某个版本一次性建成的。ADR 0020 的四个 Proposal 分布在 v0.14.0-v0.15.0 两个版本周期中\n- DSD DevTools Panel 的注入机制（`<less-devtool>` CE + virtual module）本身就是对 LessJS 自身框架能力的一次验证（dogfooding）\n- 这些能力中的大部分在 Astro/Fresh 的 roadmaps 中不出现——不是因为它们不重要，而是因为它们的架构不支持。这正是护城河的逻辑\n\n## Implementation Roadmap\n\n```\nv0.14.0（2026-07）\n  ├── Proposal B: Island 策略推荐系统       (2 周)\n  │   ├── SSR 渲染时收集 Island profile\n  │   ├── 决策规则引擎\n  │   └── 构建日志输出推荐\n  │\n  ├── Proposal D: Speculative Loading 深度集成  (1 周)\n  │   ├── SSG 路由表 → 规则生成\n  │   └── 优化现有 modulepreload/prefetch\n  │\n  └── View Transitions 零配置                (1 周)\n      └── 自动 view-transition-name 注入\n\nv0.15.0（2026-08）\n  ├── Proposal A: DSD DevTools Panel         (3-4 周)\n  │   ├── Vite Dev Server 注入\n  │   ├── DSD Tree View 组件\n  │   ├── Hydration / Island 监控面板\n  │   ├── 性能 Tab\n  │   └── 运行时校验检查\n  │\n  └── Proposal C: 无限制 DSD 嵌套 + 渲染报告   (2 周)\n      ├── 递归 AST 遍历实现 L3+\n      ├── 构建时 DSD 报告生成\n      └── 性能基准测试\n```\n\n## 参考\n\n- [ADR 0019: 综合改进计划](/blog/0019-post-review-improvement-plan) — 本 ADR 的上层战略框架\n- [Astro Dev Toolbar](https://docs.astro.build/en/reference/dev-toolbar/) — 启发 LessJS DevTools Panel 的模式\n- [Speculation Rules API](https://developer.mozilla.org/en-US/docs/Web/API/Speculation_Rules_API) — 浏览器预渲染标准\n- [View Transitions API](https://developer.mozilla.org/en-US/docs/Web/API/View_Transitions_API) — 过渡动画标准\n- [WhatWG Navigation API](https://developer.mozilla.org/en-US/docs/Web/API/Navigation_API) — 客户端路由标准（已在 core 中使用）\n- [ADR 0018: 虚拟模块数据模式](/blog/0018-dev-build-data-consistency-plugin-data-bridge) — DevTools Panel 的虚拟模块注入可复用此模式\n- 四份审查报告：`deliverables/review-architecture.md`、`deliverables/review-code-quality.md`\n\n---\n\n_提出日期：2026-05-12 | 状态：PROPOSED | 目标版本：v0.14.0+ | 提出人：齐活林（Qi）_\n",
    "html": "<h2>Status</h2>\n<p><strong>PROPOSED</strong> — v0.14.0+ 目标</p>\n<h2>Context</h2>\n<h3>战略背景</h3>\n<p>ADR 0019（综合改进计划）确立了&quot;先固本，后拓疆&quot;的策略。Phase 0/1 解决的是质量缺口的补救。<strong>但护城河不是修出来的——护城河是在别人没去过的地方挖出来的。</strong></p>\n<p>四份审查报告的共识结论：LessJS 最大的差异化在于 <strong>DSD-first</strong>。然而当前这个差异化是理念层面的，到了开发者体验层面，DSD 的独特价值并未充分展现——开发者看到的依然是&quot;Lit + Vite 构建的又一个 SSG&quot;。</p>\n<p>Astro 6.x、Fresh 2.0 beta、Qwik、Lume 3 都没有以 DSD 为核心模型。这意味着 <strong>DSD 相关的能力建设是 LessJS 唯一不可被快速复制的竞争壁垒</strong>。如果 LessJS 能在 DSD 领域做到&quot;这个体验只有这里有&quot;，就获得了真正的时间窗口。</p>\n<h3>问题分析</h3>\n<h4>问题 1：DSD 对开发者不可见</h4>\n<p>DSD 的核心价值（原生 CSS 封装、零 JS 组件边界、浏览器级渲染）在构建产物中是隐形的。开发者在 dev 工具中看不到 DSD 的影子：</p>\n<pre><code>DSD 在浏览器中:  DOM 树上看到一个 &lt;template shadowrootmode=&quot;open&quot;&gt; \n                  → 开发者需要手动打开 Elements 面板去确认\n                  → 无法直观看到&quot;哪些组件用了 DSD、哪些没用到&quot;\n                  → 无法验证 DSD 嵌套是否正确\n                  → 无法区分 DSD Interactive 和 DSD Static\n</code></pre>\n<p><strong>对比</strong>：Vue 开发者能看到 Vue DevTools 面板里的组件树；React 开发者能看到 Components 面板。DSD 开发者——什么都没有。</p>\n<h4>问题 2：Island 策略缺乏智能</h4>\n<p>当前 4 种策略（eager/lazy/visible/idle）依赖开发者手动指定。但在实践中：</p>\n<ul>\n<li>开发者经常不清楚某个组件应该用什么策略</li>\n<li>策略选择缺乏客观标准（&quot;这个组件交互复杂吗？&quot;是主观判断）</li>\n<li>同一组件的策略在页面的不同实例中可能不同</li>\n<li>没有运行时反馈来验证策略是否合理</li>\n</ul>\n<h4>问题 3：DSD 渲染引擎能力边界</h4>\n<p>当前 DSD 渲染引擎支持 L2 嵌套（通过 parse5 AST O(n·d)），但：</p>\n<ul>\n<li>深层嵌套（L3+）的性能未见公开基准</li>\n<li>无流式输出能力（大页面需全部渲染完成才输出）</li>\n<li>无 DSD 错误定位机制（渲染失败时，开发者不知哪个组件的哪个属性出了问题）</li>\n<li>无 DSD 渲染报告（构建后无法查看页面 DSD 的&quot;健康度&quot;）</li>\n</ul>\n<h3>竞品对比：DSD 体验差距</h3>\n<table>\n<thead>\n<tr>\n<th>能力</th>\n<th>Astro</th>\n<th>Fresh</th>\n<th>Qwik</th>\n<th>LessJS 当前</th>\n<th>LessJS 目标</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>DSD DevTools 面板</td>\n<td>❌</td>\n<td>❌</td>\n<td>❌</td>\n<td>❌</td>\n<td>✅ <strong>独家</strong></td>\n</tr>\n<tr>\n<td>DSD Tree View</td>\n<td>❌</td>\n<td>❌</td>\n<td>❌</td>\n<td>❌</td>\n<td>✅ <strong>独家</strong></td>\n</tr>\n<tr>\n<td>DSD Hydration 监控</td>\n<td>❌</td>\n<td>❌</td>\n<td>❌</td>\n<td>❌</td>\n<td>✅ <strong>独家</strong></td>\n</tr>\n<tr>\n<td>DSD 渲染报告</td>\n<td>❌</td>\n<td>❌</td>\n<td>❌</td>\n<td>❌</td>\n<td>✅ <strong>独家</strong></td>\n</tr>\n<tr>\n<td>Island 策略推荐</td>\n<td>❌</td>\n<td>❌</td>\n<td>❌</td>\n<td>❌</td>\n<td>✅ <strong>独家</strong></td>\n</tr>\n<tr>\n<td>Speculative Loading 深度集成</td>\n<td>基础</td>\n<td>❌</td>\n<td>❌</td>\n<td>基础</td>\n<td>✅ <strong>领先</strong></td>\n</tr>\n<tr>\n<td>View Transitions 零配置</td>\n<td>❌</td>\n<td>❌</td>\n<td>❌</td>\n<td>基础</td>\n<td>✅ <strong>领先</strong></td>\n</tr>\n<tr>\n<td>DSD Streaming</td>\n<td>❌</td>\n<td>❌</td>\n<td>❌</td>\n<td>❌</td>\n<td>✅ <strong>独家</strong></td>\n</tr>\n</tbody></table>\n<p><strong>核心结论</strong>：LessJS 站在 DSD 的天然位置，但当前的能力建设程度与这个位置不匹配。DSD-first 不能只是 README 中的一句话——它应该是开发者每一次交互中都能感受到的体验。</p>\n<h2>Decision</h2>\n<h3>核心原则</h3>\n<p><strong>&quot;让 DSD 好到无法被忽视，而不是让框架小到没有依赖。&quot;</strong></p>\n<h3>Proposal A: DSD DevTools Panel（v0.14.0）</h3>\n<h4>目标</h4>\n<p>参考 Astro Dev Toolbar 的模式，在开发环境中注入一个 DSD 调试面板，让 DSD 变得<strong>可见、可审查、可调试</strong>。</p>\n<h4>实现方案</h4>\n<p><strong>注入方式</strong>：Vite Dev Server 在 HTML 响应末尾注入一个 <code>&lt;script&gt;</code> 标签，加载 DevTool Panel。</p>\n<pre><code>Dev 模式 HTML 响应:\n  &lt;html&gt;\n    &lt;head&gt;...&lt;/head&gt;\n    &lt;body&gt;\n      ... 页面内容 ...\n      &lt;less-devtool&gt;                                    ← 注入的自定义元素\n        #shadow-root:\n          &lt;panel-toggle&gt;  (右下角浮动按钮)                ← 点击打开面板\n          &lt;panel-overlay&gt; (全屏覆盖)\n             ├── DSD Tree View     → 组件树 + DSD 层级\n             ├── Hydration Status  → 哪些跳过渲染、哪些做了\n             ├── Island Explorer → 策略分布与实际行为\n             └── Performance Tab   → 渲染时间 / DSD 数量\n      &lt;/less-devtool&gt;\n      &lt;script type=&quot;module&quot; src=&quot;/@openelement/devtool&quot;&gt;&lt;/script&gt;  ← 虚拟模块入口\n    &lt;/body&gt;\n  &lt;/html&gt;\n</code></pre>\n<p><strong>DSD Tree View 数据结构</strong>：</p>\n<pre><code class=\"language-ts\">interface DsdTreeNode {\n  tagName: string; // &#39;less-button&#39;, &#39;home-page&#39;, etc.\n  layer: &#39;dsd-static&#39; | &#39;dsd-interactive&#39; | &#39;pure-island&#39;;\n  shadowRootAttached: boolean; // 浏览器是否已附加 DSD\n  hydrationSkipped: boolean; // Lit 是否跳过了重渲染\n  childNodes: DsdTreeNode[]; // 嵌套的 DSD 子节点\n  renderTime: number; // SSR 渲染时间 (ms)\n  templateSize: number; // DSD 模板字节数\n}\n\ninterface DsdReport {\n  totalComponents: number;\n  dsdComponents: number; // 有 DSD 模板的\n  hydratedComponents: number; // 跳过了渲染的\n  pureIslands: number;\n  totalDsdSize: number; // 所有 DSD 模板的总 KB\n  maxNestingDepth: number;\n}\n</code></pre>\n<p><strong>采集方式</strong>：</p>\n<pre><code>SSR 渲染阶段:\n  renderDSD() 收集：组件名 → 渲染时间 → DSD 模板大小\n  通过构建上下文传递到 dev 阶段\n\n浏览器运行时:\n  MutationObserver 观察 Custom Elements 升级\n  Lit connectedCallback() 检测 _dsdHydrated 标志\n  汇总数据发送到 DevTool Panel\n</code></pre>\n<p><strong>运行时校验</strong>：</p>\n<p>DevTool Panel 在运行时执行自动检查：</p>\n<pre><code class=\"language-ts\">// 自动检查清单（面板中显示 4 个状态灯）\nconst checks = {\n  dsdNestingValid: checkNoShadowDOMInjection(), // ✅ DSD 嵌套完整\n  ssrClientMatch: compareSSRandClient(), // ✅ SSR 客户端一致\n  noDuplicateContent: checkBlankBoxes(), // ✅ 无重复渲染\n  islandStrategiesOptimal: evalIslandStrategies(), // ✅ Island 策略合理\n};\n</code></pre>\n<h4>为什么是护城河</h4>\n<p>这个面板 Astro 不可能做——它没有 DSD 为核心。Fresh 不可能做——Preact 不支持 DSD。Qwik 不需要做——它的渲染模型是完全不同的。<strong>只有 LessJS 能以原生 DSD-first 的视角呈现这些信息。</strong></p>\n<p>一旦开发者体验了这个面板，他们就会理解&quot;为什么 LessJS 选择 DSD&quot;。这不是营销话术，是一种&quot;亲眼看到&quot;的体验。</p>\n<hr />\n<h3>Proposal B: Island 策略推荐系统（v0.14.0）</h3>\n<h4>目标</h4>\n<p>消除&quot;开发者不知道该用哪种策略&quot;的问题，在构建时自动推荐最优策略。</p>\n<h4>实现方案</h4>\n<p><strong>分析维度</strong>（构建时静态分析）：</p>\n<pre><code class=\"language-ts\">interface IslandProfile {\n  // 交互维度\n  hasEventListeners: boolean; // 有 click/input 等事件绑定\n  hasDomMutations: boolean; // 修改了 DOM 结构\n  usesExternalState: boolean; // 依赖外部信号/状态\n  renderComplexity: &#39;simple&#39; | &#39;moderate&#39; | &#39;complex&#39;; // render() 复杂度判断\n\n  // 可见性维度\n  isAboveFold: boolean; // 是否在首屏\n  isInViewport: boolean; // 初始可见（非懒加载区域）\n\n  // 性能维度\n  estimatedChunkSize: number; // 估算的 JS chunk 大小\n  dependencyCount: number; // 依赖图大小\n}\n\ntype RecommendedStrategy = &#39;eager&#39; | &#39;lazy&#39; | &#39;visible&#39; | &#39;idle&#39;;\n</code></pre>\n<p><strong>决策规则</strong>：</p>\n<pre><code>if (isAboveFold || hasEventListeners):        → eager      (首屏交互)\nelse if (isInViewport &amp;&amp; hasDomMutations):    → lazy       (中低优先级)\nelse if (renderComplexity === &#39;complex&#39;):      → visible    (IntersectionObserver)\nelse:                                          → idle       (requestIdleCallback)\n</code></pre>\n<p><strong>开发者交互</strong>：</p>\n<ul>\n<li>推荐策略输出到构建日志：<code>[LessJS] less-theme-toggle: recommended = idle (no event listeners, below fold, 0.8KB)</code></li>\n<li>DevTool Panel 中显示当前策略 vs 推荐策略对比</li>\n<li>开发者可手动覆盖策略（显式指定的优先级高于推荐）</li>\n</ul>\n<h4>落地方式</h4>\n<p>不需要复杂的 AST 全量分析。通过在 build 阶段的 SSR 上下文中收集元数据：</p>\n<pre><code class=\"language-ts\">// SSR 渲染时，renderDSD() 收集 island 元数据\nfunction renderDSD(tagName, cls, props, source, options) {\n  // ... existing rendering logic ...\n\n  // NEW: collect island profile\n  if (options?.layer === &#39;pure-island&#39; || options?.layer === &#39;dsd-interactive&#39;) {\n    ctx.collectIslandProfile({\n      tagName,\n      componentSource: source?.source,\n      chunkSize: estimatedSize,\n      hasEvents: detectEventBindings(cls),\n      strategy: options.strategy || &#39;eager&#39;,\n      recommended: recommendStrategy(profile),\n    });\n  }\n\n  // ... continue rendering ...\n}\n</code></pre>\n<hr />\n<h3>Proposal C: 无限制 DSD 嵌套 + 渲染报告（v0.15.0）</h3>\n<h4>目标</h4>\n<p>将 DSD 嵌套从 L2 扩展到无限制，并提供构建时的 DSD 渲染报告。</p>\n<h4>L3+ 嵌套的技术方案</h4>\n<p>当前 <code>renderNestedCustomElements()</code> 使用 parse5 AST 进行自底向上的 DSD 包装。对于 L2 嵌套，一次 AST 遍历就够了。对于 L3+，需要<strong>多次 AST 遍历</strong>：</p>\n<pre><code>输入:  &lt;outer-ce&gt;\n         &lt;template shadowrootmode=&quot;open&quot;&gt;\n           &lt;inner-ce&gt;\n             &lt;template shadowrootmode=&quot;open&quot;&gt;\n               &lt;p&gt;Hello&lt;/p&gt;\n             &lt;/template&gt;\n           &lt;/inner-ce&gt;\n         &lt;/template&gt;\n       &lt;/outer-ce&gt;\n\nL3+ 扩展:\n  &lt;outer-ce&gt;                                    ← DSD 由 renderDSD() 包装\n    &lt;template shadowrootmode=&quot;open&quot;&gt;\n      &lt;inner-ce&gt;                                ← DSD 由 renderNested() 包装\n        &lt;template shadowrootmode=&quot;open&quot;&gt;\n          &lt;p&gt;Hello&lt;/p&gt;\n        &lt;/template&gt;\n      &lt;/inner-ce&gt;\n    &lt;/template&gt;\n  &lt;/outer-ce&gt;\n</code></pre>\n<p><strong>策略</strong>：递归 AST 遍历 + 深度缓存</p>\n<pre><code class=\"language-ts\">/**\n * 无限制 DSD 嵌套渲染\n *\n * 策略：自底向上递归 AST 遍历\n * 1. parse5 parseFragment(html) → AST\n * 2. 自底向上遍历，每遇到一个 Custom Element\n * 3. 递归渲染其内容中的嵌套 CE\n * 4. 用 &lt;template shadowrootmode&gt; 包裹渲染结果\n * 5. 替换 AST 中的原始 CE 节点\n * 6. 序列化 AST → HTML\n *\n * 性能保障：通过 maxDepth 参数防止栈溢出\n */\nexport async function renderNestedCustomElements(\n  html: string,\n  ctx: LessBuildContext,\n  maxDepth = 10, // 防止栈溢出\n): Promise&lt;string&gt; {\n  // ... parse5 AST 遍历 + 递归渲染 ...\n}\n</code></pre>\n<h4>DSD 渲染报告</h4>\n<p>构建完成后输出一份可读的 DSD 健康报告：</p>\n<pre><code>=== DSD 渲染报告: 页面 index.html ===\n\nTotal Components:      24\nDSD Components:        18  (75%)\n  ├── DSD Static:      12  (no JS needed)\n  ├── DSD Interactive: 4   (event hydration)\n  └── Pure Island:     2   (client JS chunk)\nNon-DSD Components:    6   (void elements)\n\nDSD Template Size:     12.4 KB\nMax Nesting Depth:     3   (home-page → less-layout → less-button)\nDSD Coverage:          92% (22/24 components have shadow DOM)\n\nIsland Distribution:\n  eager:  1  (less-theme-toggle)\n  idle:   1  (less-hero-ping)\n\nHydration Efficiency:\n  Skipped: 4  (DSD interactive, ready from server)\n  Active:  2  (Pure islands, client JS)\n\n=== Summary: 92% DSD coverage, 12.4KB templates, 2 client chunks ===\n</code></pre>\n<hr />\n<h3>Proposal D: Speculative Loading 与 View Transitions 深度集成（v0.14.0 并行）</h3>\n<h4>现状</h4>\n<p>LessJS 已经为 Island 的资源提示做了策略化注入（eager → modulepreload，其他 → prefetch）。但这是&quot;浅集成&quot;——它只做了资源提示，没有利用浏览器的更高级能力。</p>\n<h4>Speculation Rules API 深度集成</h4>\n<p>WHATWG Speculation Rules API 允许浏览器<strong>预渲染</strong>页面（不仅仅是预取资源）。LessJS 可以利用 SSG 的内置信息生成最优的 Speculation Rules：</p>\n<pre><code class=\"language-html\">&lt;!-- 自动生成的预渲染规则（基于 SSG 路由图） --&gt;\n&lt;script type=&quot;speculationrules&quot;&gt;\n{\n  &quot;prerender&quot;: [\n    {\n      &quot;source&quot;: &quot;list&quot;,\n      &quot;urls&quot;: [&quot;/&quot;, &quot;/blog&quot;, &quot;/about&quot;, &quot;/docs&quot;],\n      &quot;eagerness&quot;: &quot;moderate&quot;,\n      &quot;referrer_policy&quot;: &quot;strict-origin-when-cross-origin&quot;\n    }\n  ],\n  &quot;prefetch&quot;: [\n    {\n      &quot;source&quot;: &quot;list&quot;,\n      &quot;urls&quot;: [&quot;/blog/hello-world&quot;, &quot;/changelog&quot;, &quot;/roadmap&quot;],\n      &quot;eagerness&quot;: &quot;conservative&quot;\n    }\n  ]\n}\n&lt;/script&gt;\n</code></pre>\n<p><strong>智能规则生成逻辑</strong>：</p>\n<pre><code>SSG 构建完成后，获取路由表：\n  首页        → prerender (moderate)   → 用户大概率访问\n  博客列表    → prerender (moderate)   → 导航栏链接\n  关于页      → prerender (conservative) → 不太可能但可能\n  博客文章    → prefetch (conservative) → 数量多，只预取不预渲染\n  404页       → 不处理\n\n规则基于路由深度 + 链接在页面中的位置自动生成。\n</code></pre>\n<h4>View Transitions API 零配置集成</h4>\n<p>目前 LessJS 已支持 View Transitions 的 SSR meta tag 注入。但开发者仍需手动配置过渡动画。目标：<strong>零配置的同站过渡</strong>。</p>\n<pre><code>SSG 构建时分析页面间 DOM 结构差异：\n  如果两个页面共享相同的 &lt;header&gt; &lt;nav&gt; &lt;open-layout&gt; 结构\n  自动添加 view-transition-name 以保持过渡一致性\n\n用户不需要写任何 CSS，框架自动处理：\n  document.startViewTransition(() =&gt; { navigate(url); });\n</code></pre>\n<hr />\n<h2>Consequences</h2>\n<h3>Positive</h3>\n<ul>\n<li><strong>DSD DevTools Panel（Proposal A）是最大的差异化武器</strong>。一旦这个面板存在，Astro/Fresh 用户试用 LessJS 后的第一个感受就是&quot;这个框架把那些我看不见的东西变得可见了&quot;。这是一个体验层面的护城河</li>\n<li><strong>Island 策略推荐（Proposal B）降低认知负担</strong>。策略从&quot;开发者决策&quot;变为&quot;框架建议，开发者选择&quot;，这是 DX 层面的实质性提升</li>\n<li><strong>DSD 渲染报告（Proposal C）把构建产物变成可量化、可优化、可炫耀的东西</strong>。&quot;我的站点 92% DSD 覆盖，零 JS 渲染&quot;是一个强有力的产品叙事</li>\n<li><strong>Speculative Rules 深度集成（Proposal D）天然只有 SSG 框架能做</strong>。Astro 也能做，但 LessJS 有 Island 策略信息，能生成更精准的预渲染规则</li>\n</ul>\n<h3>Negative</h3>\n<ul>\n<li><strong>Proposal A（DevTools Panel）的开发成本最高</strong>（估计 3-4 周），需要 Vite 虚拟模块、Custom Element 面板、DSD 元数据采集三个子系统配合</li>\n<li><strong>Proposal B（策略推荐）精度有限</strong>。静态分析无法准确判断&quot;这个 Island 是否需要交互&quot;——一些条件绑定的交互在 render() 之前不可见</li>\n<li><strong>Proposal C（L3+ 嵌套）可能引入性能回归</strong>。parse5 的多次 AST 遍历加递归渲染需要严格的性能基准测试</li>\n</ul>\n<h3>Neutral</h3>\n<ul>\n<li><strong>护城河是时间积累的结果</strong>，不是某个版本一次性建成的。ADR 0020 的四个 Proposal 分布在 v0.14.0-v0.15.0 两个版本周期中</li>\n<li>DSD DevTools Panel 的注入机制（<code>&lt;less-devtool&gt;</code> CE + virtual module）本身就是对 LessJS 自身框架能力的一次验证（dogfooding）</li>\n<li>这些能力中的大部分在 Astro/Fresh 的 roadmaps 中不出现——不是因为它们不重要，而是因为它们的架构不支持。这正是护城河的逻辑</li>\n</ul>\n<h2>Implementation Roadmap</h2>\n<pre><code>v0.14.0（2026-07）\n  ├── Proposal B: Island 策略推荐系统       (2 周)\n  │   ├── SSR 渲染时收集 Island profile\n  │   ├── 决策规则引擎\n  │   └── 构建日志输出推荐\n  │\n  ├── Proposal D: Speculative Loading 深度集成  (1 周)\n  │   ├── SSG 路由表 → 规则生成\n  │   └── 优化现有 modulepreload/prefetch\n  │\n  └── View Transitions 零配置                (1 周)\n      └── 自动 view-transition-name 注入\n\nv0.15.0（2026-08）\n  ├── Proposal A: DSD DevTools Panel         (3-4 周)\n  │   ├── Vite Dev Server 注入\n  │   ├── DSD Tree View 组件\n  │   ├── Hydration / Island 监控面板\n  │   ├── 性能 Tab\n  │   └── 运行时校验检查\n  │\n  └── Proposal C: 无限制 DSD 嵌套 + 渲染报告   (2 周)\n      ├── 递归 AST 遍历实现 L3+\n      ├── 构建时 DSD 报告生成\n      └── 性能基准测试\n</code></pre>\n<h2>参考</h2>\n<ul>\n<li><a href=\"/blog/0019-post-review-improvement-plan\" rel=\"noopener noreferrer\">ADR 0019: 综合改进计划</a> — 本 ADR 的上层战略框架</li>\n<li><a href=\"https://docs.astro.build/en/reference/dev-toolbar/\" rel=\"noopener noreferrer\">Astro Dev Toolbar</a> — 启发 LessJS DevTools Panel 的模式</li>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/API/Speculation_Rules_API\" rel=\"noopener noreferrer\">Speculation Rules API</a> — 浏览器预渲染标准</li>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/API/View_Transitions_API\" rel=\"noopener noreferrer\">View Transitions API</a> — 过渡动画标准</li>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/API/Navigation_API\" rel=\"noopener noreferrer\">WhatWG Navigation API</a> — 客户端路由标准（已在 core 中使用）</li>\n<li><a href=\"/blog/0018-dev-build-data-consistency-plugin-data-bridge\" rel=\"noopener noreferrer\">ADR 0018: 虚拟模块数据模式</a> — DevTools Panel 的虚拟模块注入可复用此模式</li>\n<li>四份审查报告：<code>deliverables/review-architecture.md</code>、<code>deliverables/review-code-quality.md</code></li>\n</ul>\n<hr />\n<p><em>提出日期：2026-05-12 | 状态：PROPOSED | 目标版本：v0.14.0+ | 提出人：齐活林（Qi）</em></p>\n"
  },
  {
    "slug": "0019-post-review-improvement-plan",
    "frontmatter": {
      "title": "ADR 0019: Post-Review Comprehensive Improvement Plan",
      "date": "2026-05-12",
      "draft": false,
      "tags": [
        "architecture",
        "decision"
      ],
      "type": "adr",
      "lang": "zh"
    },
    "content": "\n## Status\n\n**PARTIALLY IMPLEMENTED** (v0.12.0–v0.14.0) — See implementation notes below.\n\n> 2026-05-13 更新：ADR 0022 (ESM-Native SSG pipeline) + ADR 0023 (Phase reordering) 在 v0.14.0 中落地。Phase 3 从 Vite closeBundle 中抽出为独立模块，URLPattern 替换手写路由解析。\n>\n> 2026-05-12 更新：ADR 0021 (API surface convergence) 在 v0.13.0 中落地，解决了 `ssr-handler.ts` 删除、导出收敛等问题。`@openelement/app` 测试从 0 增加到 16 个。\n\n## Context\n\n### 触发条件\n\n2026-05-12，主理人齐活林（Qi）启动团队分工审查，对 LessJS 进行了四个维度的全面评估：\n\n| 维度           | 负责人              | 报告                                        |\n| -------------- | ------------------- | ------------------------------------------- |\n| 产品定位与市场 | 许清楚（产品经理）  | `deliverables/review-positioning-market.md` |\n| 系统架构       | 高见远（架构师）    | `deliverables/review-architecture.md`       |\n| 源代码质量     | 寇豆码（工程师）    | `deliverables/review-code-quality.md`       |\n| 测试质量       | 严过关（QA 工程师） | `deliverables/review-test-quality.md`       |\n\n四份报告共发现 **4 个 Critical 问题、16 个 Major 问题、30+ Minor/Info 问题**，覆盖了从产品战略到底层实现的完整质量视图。\n\n### 问题聚合与优先级映射\n\n将四份报告的问题按**紧急度 × 影响力**聚合后，呈现三个层级：\n\n```\n     高\n      │\n影    │  P0 (需立即行动)\n响    │    ├── @openelement/app 零测试              [测试]\n力    │    ├── DSD 差异化不突出                 [产品]\n      │    └── 社区生态真空                     [产品]\n      │\n中    │  P1 (本轮迭代必须解决)\n      │    ├── LessBuildContext 巨型对象重构      [架构]\n      │    ├── closeBundle 职责超载              [架构]\n      │    ├── @openelement/rpc 测试严重不足          [测试]\n      │    ├── @openelement/ui 组件测试不足           [测试]\n      │    └── 4 项代码质量 Major 问题           [代码]\n      │\n低    │  P2 (下轮迭代)\n      │    ├── parse5 分离                       [架构]\n      │    ├── Lit 内部 API 依赖治理             [架构]\n      │    ├── 12 项代码 Minor 问题               [代码]\n      │    └── E2E 浏览器矩阵扩展                [测试]\n      │\n          低 ─────────── 紧急度 ──────────→ 高\n```\n\n### 根本问题识别\n\n透过四份报告的具体问题，根因集中在三个核心矛盾：\n\n1. **v0.x 快速迭代 vs 质量沉淀** — 从 v0.1 到 v0.11 仅用 6 周，核心引擎推进极快，但外围（测试、文档、生态）未能同步\n2. **理念领先 vs 体验粗糙** — DSD-first 理念独特，但开发者在实际使用中的\"第一印象\"尚不流畅\n3. **工程执行力强 vs 产品叙事弱** — 代码和架构质量高（8.5/10），但市场讲述\"为什么选 LessJS\"的能力薄弱\n\n## Decision\n\n### 决策原则\n\n**\"先固本，后拓疆\"** — v0.12.0 阶段的核心任务是巩固工程质量基础，而非追求功能数量的增长。\n\n具体而言：\n\n1. 当前不追求 .less Compiler（远景目标），不做大规模功能扩展\n2. 优先弥补现有的质量缺口，让核心功能\"经得起审查\"\n3. 在产品叙事上确立 \"DSD-first\" 的明确标签，而非定位为\"又一个 Deno 框架\"\n\n### Phase 0: 工程质量加固（v0.12.0，立即执行）\n\n解决审查中发现的最迫切质量缺口：\n\n| #      | 行动                                                                     | 参考来源       | 工作量估计 |\n| ------ | ------------------------------------------------------------------------ | -------------- | ---------- |\n| FIX-01 | 补齐 `@openelement/app` 整合测试（lessjs() 组合 core+content+i18n）           | 测试审查-C1    | 1-2 天     |\n| FIX-02 | 修复 `less-layout.ts` innerHTML 使用，改用 `DOMParser.parseFromString()` | 代码审查-C1    | 0.5 天     |\n| FIX-03 | 补充 `@openelement/rpc` 基本测试（请求/响应/错误处理）                        | 测试审查-C2    | 1 天       |\n| FIX-04 | 治理 signals 模块 `no-explicit-any`（50+ 处，按模块分治）                | 代码审查-M1    | 2 天       |\n| FIX-05 | 删除 `ssr-handler.ts` 冗余重导出层                                       | 架构审查-T1    | 0.5 天     |\n| FIX-06 | 修复空 catch block 和未使用变量                                          | 代码审查-M2~M4 | 1 天       |\n| FIX-07 | 补充 `@openelement/create` CLI 测试                                           | 测试审查-M1    | 1 天       |\n\n**总计工作量：约 7-9 天**（单人全职）\n\n### Phase 1: 架构债务清理（v0.13.0）\n\n解决审查中发现的三项高风险架构问题：\n\n| #       | 行动                                                               | 参考来源    | 工作量估计 |\n| ------- | ------------------------------------------------------------------ | ----------- | ---------- |\n| ARCH-01 | LessBuildContext 字段按 Phase 分组为类型安全的子对象               | 架构审查-R1 | 3 天       |\n| ARCH-02 | closeBundle Phase 2/3 引入显式 BuildStep 编排 + try-catch 隔离     | 架构审查-R2 | 2 天       |\n| ARCH-03 | `signals/src/index.ts` 单文件拆分为 engine/polyfill/framework 三层 | 架构审查-T4 | 1 天       |\n| ARCH-04 | 修复 `build-manifest.ts` 包标注不一致                              | 架构审查-T2 | 0.5 天     |\n\n**总计工作量：约 6-7 天**\n\n### Phase 2: 护城河建设（v0.14.0+）\n\n基于审查结论确立的长期竞争优势方向。详见 [ADR 0020: DSD 渲染引擎与 Islands 策略增强](/blog/0020-dsd-engine-islands-enhancement)。\n\n## Consequences\n\n### Positive\n\n- **质量地基夯实**：P0/P1 问题全部消除后，LessJS 的工程质量将处于同类框架的头部水平\n- **降低贡献门槛**：测试覆盖充分 + 架构债务清理后，社区贡献者更容易理解代码、安全修改\n- **产品叙事聚焦**：不再分散精力在多个方向，集中资源打\"DSD-first\"一张牌\n\n### Negative\n\n- **功能增长暂停**：v0.12.0-v0.13.0 两个版本以清理为主，新功能推进速度放缓\n- **Breaking changes**：架构债务清理（如信号包拆分、ssr-handler 删除）可能造成用户迁移成本\n- **无法满足所有审查建议**：共 50+ 项建议，当前规划仅覆盖约 60%，剩余项需后续版本持续消化\n\n### Neutral\n\n- Phase 0 和 Phase 1 的改动大部分是内部重构，对用户可见的 API 变更有限\n- 审查中发现的一些\"亮点\"（如 ADR 0018 虚拟模块模式、Branded Type 安全体系）应作为最佳实践推广到更多代码位置\n\n## 参考\n\n- 四份审查报告：`deliverables/review-positioning-market.md`、`deliverables/review-architecture.md`、`deliverables/review-code-quality.md`、`deliverables/review-test-quality.md`\n- [ADR 0020: DSD 渲染引擎与 Islands 策略增强](/blog/0020-dsd-engine-islands-enhancement) — 护城河建设的具体技术方案\n- [ADR 0011: 消除最后一个 globalThis 桥接](/blog/0011-eliminate-last-globalthis-via-closebundle) — 类似的架构清理模式\n- [ADR 0018: 消除插件模块状态](/blog/0018-dev-build-data-consistency-plugin-data-bridge) — 虚拟模块模式最佳实践\n\n---\n\n_提出日期：2026-05-12 | 状态：PARTIALLY IMPLEMENTED (v0.14.0) | 目标版本：v0.12.0+ | 提出人：齐活林（Qi）_\n",
    "html": "<h2>Status</h2>\n<p><strong>PARTIALLY IMPLEMENTED</strong> (v0.12.0–v0.14.0) — See implementation notes below.</p>\n<blockquote>\n<p>2026-05-13 更新：ADR 0022 (ESM-Native SSG pipeline) + ADR 0023 (Phase reordering) 在 v0.14.0 中落地。Phase 3 从 Vite closeBundle 中抽出为独立模块，URLPattern 替换手写路由解析。</p>\n<p>2026-05-12 更新：ADR 0021 (API surface convergence) 在 v0.13.0 中落地，解决了 <code>ssr-handler.ts</code> 删除、导出收敛等问题。<code>@openelement/app</code> 测试从 0 增加到 16 个。</p>\n</blockquote>\n<h2>Context</h2>\n<h3>触发条件</h3>\n<p>2026-05-12，主理人齐活林（Qi）启动团队分工审查，对 LessJS 进行了四个维度的全面评估：</p>\n<table>\n<thead>\n<tr>\n<th>维度</th>\n<th>负责人</th>\n<th>报告</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>产品定位与市场</td>\n<td>许清楚（产品经理）</td>\n<td><code>deliverables/review-positioning-market.md</code></td>\n</tr>\n<tr>\n<td>系统架构</td>\n<td>高见远（架构师）</td>\n<td><code>deliverables/review-architecture.md</code></td>\n</tr>\n<tr>\n<td>源代码质量</td>\n<td>寇豆码（工程师）</td>\n<td><code>deliverables/review-code-quality.md</code></td>\n</tr>\n<tr>\n<td>测试质量</td>\n<td>严过关（QA 工程师）</td>\n<td><code>deliverables/review-test-quality.md</code></td>\n</tr>\n</tbody></table>\n<p>四份报告共发现 <strong>4 个 Critical 问题、16 个 Major 问题、30+ Minor/Info 问题</strong>，覆盖了从产品战略到底层实现的完整质量视图。</p>\n<h3>问题聚合与优先级映射</h3>\n<p>将四份报告的问题按<strong>紧急度 × 影响力</strong>聚合后，呈现三个层级：</p>\n<pre><code>     高\n      │\n影    │  P0 (需立即行动)\n响    │    ├── @openelement/app 零测试              [测试]\n力    │    ├── DSD 差异化不突出                 [产品]\n      │    └── 社区生态真空                     [产品]\n      │\n中    │  P1 (本轮迭代必须解决)\n      │    ├── LessBuildContext 巨型对象重构      [架构]\n      │    ├── closeBundle 职责超载              [架构]\n      │    ├── @openelement/rpc 测试严重不足          [测试]\n      │    ├── @openelement/ui 组件测试不足           [测试]\n      │    └── 4 项代码质量 Major 问题           [代码]\n      │\n低    │  P2 (下轮迭代)\n      │    ├── parse5 分离                       [架构]\n      │    ├── Lit 内部 API 依赖治理             [架构]\n      │    ├── 12 项代码 Minor 问题               [代码]\n      │    └── E2E 浏览器矩阵扩展                [测试]\n      │\n          低 ─────────── 紧急度 ──────────→ 高\n</code></pre>\n<h3>根本问题识别</h3>\n<p>透过四份报告的具体问题，根因集中在三个核心矛盾：</p>\n<ol>\n<li><strong>v0.x 快速迭代 vs 质量沉淀</strong> — 从 v0.1 到 v0.11 仅用 6 周，核心引擎推进极快，但外围（测试、文档、生态）未能同步</li>\n<li><strong>理念领先 vs 体验粗糙</strong> — DSD-first 理念独特，但开发者在实际使用中的&quot;第一印象&quot;尚不流畅</li>\n<li><strong>工程执行力强 vs 产品叙事弱</strong> — 代码和架构质量高（8.5/10），但市场讲述&quot;为什么选 LessJS&quot;的能力薄弱</li>\n</ol>\n<h2>Decision</h2>\n<h3>决策原则</h3>\n<p><strong>&quot;先固本，后拓疆&quot;</strong> — v0.12.0 阶段的核心任务是巩固工程质量基础，而非追求功能数量的增长。</p>\n<p>具体而言：</p>\n<ol>\n<li>当前不追求 .less Compiler（远景目标），不做大规模功能扩展</li>\n<li>优先弥补现有的质量缺口，让核心功能&quot;经得起审查&quot;</li>\n<li>在产品叙事上确立 &quot;DSD-first&quot; 的明确标签，而非定位为&quot;又一个 Deno 框架&quot;</li>\n</ol>\n<h3>Phase 0: 工程质量加固（v0.12.0，立即执行）</h3>\n<p>解决审查中发现的最迫切质量缺口：</p>\n<table>\n<thead>\n<tr>\n<th>#</th>\n<th>行动</th>\n<th>参考来源</th>\n<th>工作量估计</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>FIX-01</td>\n<td>补齐 <code>@openelement/app</code> 整合测试（lessjs() 组合 core+content+i18n）</td>\n<td>测试审查-C1</td>\n<td>1-2 天</td>\n</tr>\n<tr>\n<td>FIX-02</td>\n<td>修复 <code>less-layout.ts</code> innerHTML 使用，改用 <code>DOMParser.parseFromString()</code></td>\n<td>代码审查-C1</td>\n<td>0.5 天</td>\n</tr>\n<tr>\n<td>FIX-03</td>\n<td>补充 <code>@openelement/rpc</code> 基本测试（请求/响应/错误处理）</td>\n<td>测试审查-C2</td>\n<td>1 天</td>\n</tr>\n<tr>\n<td>FIX-04</td>\n<td>治理 signals 模块 <code>no-explicit-any</code>（50+ 处，按模块分治）</td>\n<td>代码审查-M1</td>\n<td>2 天</td>\n</tr>\n<tr>\n<td>FIX-05</td>\n<td>删除 <code>ssr-handler.ts</code> 冗余重导出层</td>\n<td>架构审查-T1</td>\n<td>0.5 天</td>\n</tr>\n<tr>\n<td>FIX-06</td>\n<td>修复空 catch block 和未使用变量</td>\n<td>代码审查-M2~M4</td>\n<td>1 天</td>\n</tr>\n<tr>\n<td>FIX-07</td>\n<td>补充 <code>@openelement/create</code> CLI 测试</td>\n<td>测试审查-M1</td>\n<td>1 天</td>\n</tr>\n</tbody></table>\n<p><strong>总计工作量：约 7-9 天</strong>（单人全职）</p>\n<h3>Phase 1: 架构债务清理（v0.13.0）</h3>\n<p>解决审查中发现的三项高风险架构问题：</p>\n<table>\n<thead>\n<tr>\n<th>#</th>\n<th>行动</th>\n<th>参考来源</th>\n<th>工作量估计</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>ARCH-01</td>\n<td>LessBuildContext 字段按 Phase 分组为类型安全的子对象</td>\n<td>架构审查-R1</td>\n<td>3 天</td>\n</tr>\n<tr>\n<td>ARCH-02</td>\n<td>closeBundle Phase 2/3 引入显式 BuildStep 编排 + try-catch 隔离</td>\n<td>架构审查-R2</td>\n<td>2 天</td>\n</tr>\n<tr>\n<td>ARCH-03</td>\n<td><code>signals/src/index.ts</code> 单文件拆分为 engine/polyfill/framework 三层</td>\n<td>架构审查-T4</td>\n<td>1 天</td>\n</tr>\n<tr>\n<td>ARCH-04</td>\n<td>修复 <code>build-manifest.ts</code> 包标注不一致</td>\n<td>架构审查-T2</td>\n<td>0.5 天</td>\n</tr>\n</tbody></table>\n<p><strong>总计工作量：约 6-7 天</strong></p>\n<h3>Phase 2: 护城河建设（v0.14.0+）</h3>\n<p>基于审查结论确立的长期竞争优势方向。详见 <a href=\"/blog/0020-dsd-engine-islands-enhancement\" rel=\"noopener noreferrer\">ADR 0020: DSD 渲染引擎与 Islands 策略增强</a>。</p>\n<h2>Consequences</h2>\n<h3>Positive</h3>\n<ul>\n<li><strong>质量地基夯实</strong>：P0/P1 问题全部消除后，LessJS 的工程质量将处于同类框架的头部水平</li>\n<li><strong>降低贡献门槛</strong>：测试覆盖充分 + 架构债务清理后，社区贡献者更容易理解代码、安全修改</li>\n<li><strong>产品叙事聚焦</strong>：不再分散精力在多个方向，集中资源打&quot;DSD-first&quot;一张牌</li>\n</ul>\n<h3>Negative</h3>\n<ul>\n<li><strong>功能增长暂停</strong>：v0.12.0-v0.13.0 两个版本以清理为主，新功能推进速度放缓</li>\n<li><strong>Breaking changes</strong>：架构债务清理（如信号包拆分、ssr-handler 删除）可能造成用户迁移成本</li>\n<li><strong>无法满足所有审查建议</strong>：共 50+ 项建议，当前规划仅覆盖约 60%，剩余项需后续版本持续消化</li>\n</ul>\n<h3>Neutral</h3>\n<ul>\n<li>Phase 0 和 Phase 1 的改动大部分是内部重构，对用户可见的 API 变更有限</li>\n<li>审查中发现的一些&quot;亮点&quot;（如 ADR 0018 虚拟模块模式、Branded Type 安全体系）应作为最佳实践推广到更多代码位置</li>\n</ul>\n<h2>参考</h2>\n<ul>\n<li>四份审查报告：<code>deliverables/review-positioning-market.md</code>、<code>deliverables/review-architecture.md</code>、<code>deliverables/review-code-quality.md</code>、<code>deliverables/review-test-quality.md</code></li>\n<li><a href=\"/blog/0020-dsd-engine-islands-enhancement\" rel=\"noopener noreferrer\">ADR 0020: DSD 渲染引擎与 Islands 策略增强</a> — 护城河建设的具体技术方案</li>\n<li><a href=\"/blog/0011-eliminate-last-globalthis-via-closebundle\" rel=\"noopener noreferrer\">ADR 0011: 消除最后一个 globalThis 桥接</a> — 类似的架构清理模式</li>\n<li><a href=\"/blog/0018-dev-build-data-consistency-plugin-data-bridge\" rel=\"noopener noreferrer\">ADR 0018: 消除插件模块状态</a> — 虚拟模块模式最佳实践</li>\n</ul>\n<hr />\n<p><em>提出日期：2026-05-12 | 状态：PARTIALLY IMPLEMENTED (v0.14.0) | 目标版本：v0.12.0+ | 提出人：齐活林（Qi）</em></p>\n"
  },
  {
    "slug": "0018-dev-build-data-consistency-plugin-data-bridge",
    "frontmatter": {
      "title": "ADR 0018: Eliminate Plugin Module State — Virtual Data Modules + @deno/vite-plugin",
      "date": "2026-05-11",
      "draft": false,
      "tags": [
        "architecture",
        "decision"
      ],
      "type": "adr",
      "lang": "zh"
    },
    "content": "\n## Status\n\n**ACCEPTED** — v0.12.0 架构改进\n\n## Context\n\n### 问题本质\n\nLessJS 的 `@openelement/content` 和 `@openelement/i18n` 使用**模块级状态**存储数据：\n\n```\ncontent/blog-data.ts:    let _posts: BlogPost[] = [];    // 模块级变量\ni18n/i18n-data.ts:       let _options: LessI18nOptions;  // 模块级变量\n```\n\n插件在 `buildStart()` 中调用 `initBlogData()` / `initI18nData()` 填充状态。路由组件调用 `getPosts()` / `getI18nLocales()` 读取状态。\n\n**Build 模式**正常：`viteBuild(ssr:true, noExternal)` 产出自包含 bundle，所有代码在同一个模块实例。\n\n**Dev 模式**崩溃：Vite 插件进程和 SSR runner 是隔离的 JavaScript 领域。`initBlogData()` 在插件进程运行，`getPosts()` 在 SSR runner 运行——它们看到的是**不同的 `_posts`**。结果是 `_posts = []`，博客数据为空。\n\n### 当前 ad-hoc 修复及其问题\n\n在虚拟入口代码中注入 `initBlogData()` 调用（`entry-renderer.ts` 第 306-313 行），让初始化代码在 SSR runner 模块空间中执行。\n\n问题：\n\n1. `EntryDescriptor.blog` 字段——每个插件都要加字段，`EntryDescriptor` 侵入性增长\n2. `entry-renderer.ts` 承载业务逻辑——不再是纯函数\n3. `@openelement/adapter-vite` 对 content 包的硬依赖知识——需要知道导出名、参数格式\n4. `@openelement/i18n` 还没修——同样的问题，还没加 ad-hoc 修复\n5. 内容 HMR 不工作——`_posts` 变量停留在首次加载时的状态\n\n### 根因不是\"需要桥\"，而是\"不该有状态\"\n\n模块级 `_posts` 变量是 bug source。如果有状态，就需要桥；如果没状态，就不需要桥。\n\n**正确思路：砍掉模块级状态，用虚拟模块的数据导出替代。**\n\n### CYBER::BLOG 暴露的完整问题清单\n\n| # | 问题 | 根因 | 优先级 |\n|---|------|------|--------|\n| 1 | Dev 模式 blog 数据空 | 模块级状态 + Vite 模块隔离 | P0 |\n| 2 | 17 行 resolve.alias 样板代码 | Deno workspace 包不在 node_modules | P1 |\n| 3 | 内容 HMR 不工作 | 缺 configureServer + watcher | P1 |\n| 4 | @openelement/i18n dev 数据空 | 同 #1 模式 | P2 |\n| 5 | About 页使用 node:fs | content 只管 blog，缺静态内容方案 | P2 |\n| 6 | About 页手写 md 转换器 | 同 #5，用户被迫 workaround | P2 |\n\n## Decision\n\n### 核心原则\n\n**Zero module state. Zero bridges. Virtual modules are the data source.**\n\n1. 插件提供**纯函数**（`loadBlogData()` → `BlogPost[]`），不持有状态\n2. `adapter-vite` 提供虚拟数据模块（`virtual:less-blog-data`），`load()` 钩子调用纯函数并生成导出代码\n3. 路由组件从虚拟模块 import 数据，不从 `@openelement/content` 的模块状态读取\n4. `@deno/vite-plugin` 处理 bare specifier 解析，消除用户 `vite.config.ts` 中的 alias\n\n**不需要 `onSSRInit`，不需要 Data Bridge 协议，不需要双重初始化路径。**\n\n### 架构对比\n\n```\nBefore (stateful):\n  @openelement/content:  initBlogData() → _posts       (module state, needs bridge)\n                    getPosts()     → _posts          (reads state)\n  adapter-vite:     inject initBlogData() into entry (ad-hoc bridge)\n  route component:  import { getPosts } from '@openelement/content'\n  问题：_posts 在两个模块空间各一份 → 需要桥\n\nAfter (stateless):\n  @openelement/content:  loadBlogData(dir) → BlogPost[]  (pure function, zero state)\n  adapter-vite:     virtual:less-blog-data           (resolveId + load)\n                    load() calls loadBlogData() → generates export code\n  route component:  import { posts, getPostBySlug } from 'virtual:less-blog-data'\n  零桥，零状态，零双路径\n```\n\n### 为什么这样足够\n\n**Dev 模式**：`@hono/vite-dev-server` 调 `ssrLoadModule('virtual:less-blog-data')` → 触发 `load()` → 生成含数据的代码 → SSR runner 执行 → 数据在。没有初始化步骤，没有空变量。\n\n**Build 模式**：`viteBuild(ssr:true, noExternal)` 打包时 `load()` 执行 → 数据内联进 bundle。和 dev 走同一条路。\n\n**HMR**：`configureServer` watch 内容目录 → `invalidateModule(virtualBlogData)` → 下次请求自动触发 `load()` 重新生成 → 无缓存问题，无 flag 需重置。\n\n**Dev 和 Build 走同一条路。** 不存在\"build 模式走 buildStart，dev 模式走 onSSRInit\"的双路径问题。\n\n---\n\n## Implementation SOP\n\n### Phase 依赖关系\n\n```\nPhase 0: @deno/vite-plugin → 独立，可先行\nPhase 1: content 纯函数拆分 → Phase 2 的前置\nPhase 2: adapter-vite 虚拟数据模块 → Phase 1 + Phase 3 的核心\nPhase 3: content HMR → Phase 2 的后续\nPhase 4: i18n 同模式改造 → Phase 2 的验证\nPhase 5: 静态内容页 → Phase 2 的扩展\n```\n\n### Phase 0: @deno/vite-plugin 集成\n\n**目标**：消除用户 `vite.config.ts` 中的 resolve.alias，让 Deno import map 处理 `@openelement/*` 解析。\n\n**原理**：`@deno/vite-plugin` 使用 `import.meta.resolve()` 将 bare specifier 解析为 Deno workspace 中的实际路径。用户的 `deno.json` 已经定义了 workspace imports，Vite 不需要知道这些映射。\n\n**变更文件**：\n\n| 文件 | 变更 |\n|------|------|\n| `packages/adapter-vite/src/index.ts` | 在 `less()` 返回的插件数组中注入 `denoVitePlugin()` |\n| `packages/adapter-vite/deno.json` | 添加 `@deno/vite-plugin` 依赖 |\n| `my-tech-blog/vite.config.ts` | 删除所有 resolve.alias |\n\n**实现步骤**：\n\n1. 在 adapter-vite 中添加 `@deno/vite-plugin` 依赖\n2. 在 `less()` 的 `config()` 钩子中检测是否可用 `@deno/vite-plugin`，可用则注入\n3. 删除 `buildCoreSubpathAliases()` 整个函数及所有调用点\n4. 更新 `my-tech-blog/vite.config.ts` 删除 17 行 alias\n\n**验证**：\n\n```bash\ncd my-tech-blog && deno task dev\n# 访问 http://localhost:5173 → 页面正常渲染\n# 确认 vite.config.ts 无 resolve.alias\n```\n\n**时序图**：\n\n```mermaid\nsequenceDiagram\n    participant User as vite.config.ts\n    participant Deno as @deno/vite-plugin\n    participant Vite as Vite Dev Server\n    participant FS as Deno Workspace FS\n\n    User->>Vite: plugins: [lessjs()]\n    Note over User: Zero resolve.alias\n    Vite->>Deno: resolveId('@openelement/content')\n    Deno->>FS: import.meta.resolve('@openelement/content')\n    FS-->>Deno: file:///.../packages/content/src/index.ts\n    Deno-->>Vite: resolved path\n    Vite->>Deno: resolveId('@openelement/core/ssr-handler')\n    Deno->>FS: import.meta.resolve('@openelement/core/ssr-handler')\n    FS-->>Deno: file:///.../packages/core/src/ssr-handler.ts\n    Deno-->>Vite: resolved path\n```\n\n**回退方案**：如果 `@deno/vite-plugin` 不支持子路径解析或出现兼容问题，直接修 `@deno/vite-plugin` 提 PR，不走 `buildCoreSubpathAliases()` 回头路。\n\n---\n\n### Phase 1: @openelement/content 纯函数拆分\n\n**目标**：将 `blog-data.ts` 的模块级状态 + init/getter 模式，重构为纯函数 + 虚拟模块导出模式。\n\n**变更文件**：\n\n| 文件 | 变更 |\n|------|------|\n| `packages/content/src/blog/blog-data.ts` | 删除 `_posts`/`_options`/`initBlogData()`/`getPosts()`/`getPostBySlug()`/`getBlogOptions()`，替换为 `loadBlogData()` 纯函数 |\n| `packages/content/src/index.ts` | 导出 `loadBlogData()`，删除 `initBlogData`/`getPosts`/`getPostBySlug`/`getBlogOptions` 导出，`buildStart()` 改用 `loadBlogData()` |\n| `packages/content/src/blog/markdown.ts` | 确保 `parseMarkdownFile()` 无文件系统副作用（纯函数） |\n| `packages/content/src/blog/routes.ts` | 确保 `generateBlogRoutes()` 无副作用 |\n\n**新增接口**：\n\n```ts\n// packages/content/src/blog/blog-data.ts\n\n/**\n * Pure function: load blog data from file system.\n * No module-level state. No side effects beyond reading files.\n * Can be called from any runtime context.\n *\n * This replaces the stateful initBlogData() + getPosts() pattern.\n * For virtual module consumers, use virtual:less-blog-data instead.\n */\nexport async function loadBlogData(options?: LessBlogOptions): Promise<{\n  posts: BlogPost[];\n  basePath: string;\n}> {\n  const routes = await generateBlogRoutes(options);\n  return {\n    posts: routes.posts,\n    basePath: routes.basePath,\n  };\n}\n```\n\n**删除的接口**（不保留，不 deprecated，直接砍）：\n\n```ts\n// 全部删除 — 不向后兼容\n// let _posts: BlogPost[] = [];           ← 删除\n// let _options: LessBlogOptions = {};    ← 删除\n// export function initBlogData() {}      ← 删除\n// export function getPosts() {}          ← 删除\n// export function getPostBySlug() {}     ← 删除\n// export function getBlogOptions() {}    ← 删除\n```\n\n`blog-data.ts` 重写后只剩 `loadBlogData()` 纯函数，零状态。\n\n```ts\n// packages/content/src/blog/blog-data.ts — 重写后\n\nimport type { BlogPost, LessBlogOptions } from './types.ts';\nimport { generateBlogRoutes } from './routes.ts';\n\n/**\n * Pure function: load blog data from file system.\n * No module-level state. No side effects beyond reading files.\n * Can be called from any runtime context.\n */\nexport async function loadBlogData(options?: LessBlogOptions): Promise<{\n  posts: BlogPost[];\n  basePath: string;\n}> {\n  const routes = await generateBlogRoutes(options);\n  return {\n    posts: routes.posts,\n    basePath: routes.basePath,\n  };\n}\n```\n\n**验证**：\n\n```bash\ncd packages/content && deno test\n# loadBlogData() 单元测试通过\n# initBlogData/getPosts/getPostBySlug/getBlogOptions 已删除，不再存在\n```\n\n**时序图**：\n\n```mermaid\nsequenceDiagram\n    participant Dev as Developer\n    participant Content as @openelement/content\n    participant FS as File System\n\n    Note over Dev: Phase 1: Pure function only\n    Dev->>Content: loadBlogData(opts)\n    Content->>FS: read markdown files\n    FS-->>Content: raw content\n    Content->>Content: parse + sort\n    Content-->>Dev: { posts, basePath }\n    Note over Content: Zero module state\n```\n\n---\n\n### Phase 2: adapter-vite 虚拟数据模块\n\n**目标**：新增 `virtual:less-blog-data` 虚拟模块插件，替代 `entry-renderer.ts` 中的 ad-hoc `initBlogData()` 注入。\n\n**这是整个方案的核心。** 虚拟模块是唯一的数据桥，dev/build 走同一条路。\n\n**变更文件**：\n\n| 文件 | 变更 |\n|------|------|\n| `packages/adapter-vite/src/virtual-data.ts` | **新建** — 虚拟数据模块插件 |\n| `packages/adapter-vite/src/index.ts` | 引入 virtual data plugin，删除 `EntryDescriptor.blog` 相关逻辑 |\n| `packages/adapter-vite/src/entry-renderer.ts` | 删除 `initBlogData()` 注入代码 |\n| `packages/adapter-vite/src/entry-descriptor.ts` | 删除 `blog` 字段 |\n| `packages/adapter-vite/src/build-context.ts` | `blogOptions` 字段改为内部配置，不传给 entry descriptor |\n| `my-tech-blog/app/routes/` | 路由组件改为从 `virtual:less-blog-data` import |\n\n**新增文件 `virtual-data.ts`**：\n\n```ts\n/**\n * @openelement/adapter-vite - Virtual data module plugins\n *\n * Replaces module-level state in @openelement/content and @openelement/i18n\n * with virtual module data exports. The load() hook runs in the\n * Vite plugin process and generates code that executes in the\n * SSR runner — this IS the data bridge.\n *\n * Dev mode:  ssrLoadModule('virtual:less-blog-data') → load() → fresh data\n * Build mode: viteBuild bundles the virtual module → data inlined\n * HMR:       invalidateModule → load() re-runs → fresh data\n *\n * Zero module state. Zero init functions. Zero dual paths.\n */\n\nimport type { Plugin } from 'vite';\nimport type { LessBuildContext } from './build-context.js';\nimport { loadBlogData } from '@openelement/content/blog-data';\nimport type { LessBlogOptions } from '@openelement/content/types';\n\n// ─── Virtual module IDs ────────────────────────────────────────\n\nconst VIRTUAL_BLOG_DATA_ID = 'virtual:less-blog-data';\nconst RESOLVED_BLOG_DATA_ID = '\\0' + VIRTUAL_BLOG_DATA_ID;\n\n// ─── Blog data virtual module ──────────────────────────────────\n\nexport function createBlogDataPlugin(ctx: LessBuildContext): Plugin {\n  return {\n    name: 'less:blog-data',\n    enforce: 'pre',\n\n    resolveId(id) {\n      if (id === VIRTUAL_BLOG_DATA_ID) return RESOLVED_BLOG_DATA_ID;\n    },\n\n    async load(id) {\n      if (id !== RESOLVED_BLOG_DATA_ID) return;\n\n      const blogOpts = ctx.blogOptions;\n      if (!blogOpts) {\n        // Blog not configured — export empty data\n        return [\n          'export const posts = [];',\n          'export function getPostBySlug(slug) { return undefined; }',\n          'export function getBlogOptions() { return {}; }',\n        ].join('\\n');\n      }\n\n      const options: LessBlogOptions = {\n        contentDir: blogOpts.contentDir,\n        basePath: blogOpts.basePath,\n      };\n\n      const { posts } = await loadBlogData(options);\n\n      // Generate code that exports data + derived functions\n      // The posts array is serialized as JSON — this is the bridge.\n      // In build mode, this gets inlined into the SSR bundle.\n      // In dev mode, this runs fresh on each ssrLoadModule() call.\n      return [\n        `const _posts = ${JSON.stringify(posts)};`,\n        '',\n        `/** All published blog posts, sorted newest first */`,\n        `export const posts = _posts;`,\n        '',\n        `/** Get a single post by slug */`,\n        `export function getPostBySlug(slug) {`,\n        `  return _posts.find(p => p.slug === slug);`,\n        `}`,\n        '',\n        `/** Get blog configuration */`,\n        `export function getBlogOptions() {`,\n        `  return ${JSON.stringify(options)};`,\n        `}`,\n      ].join('\\n');\n    },\n  };\n}\n```\n\n**从 `entry-renderer.ts` 删除的代码**：\n\n```diff\n-  // --- Dev mode: Initialize blog data ---\n-  // In SSG mode, initBlogData() is called by build-ssg.ts after importing\n-  // the SSR bundle (same module instance). In dev mode, the Vite SSR runner\n-  // loads route components (e.g., cyber-home) that call getPosts(), but the\n-  // blog-data module's _posts variable is empty because initBlogData() only\n-  // ran in the plugin process (different module space). We must call\n-  // initBlogData() inside the virtual entry so it runs in the same module\n-  // instance as the route components.\n-  if (!desc.isSSG && desc.blog) {\n-    lines.push('// Dev mode: Initialize blog data in SSR runner module space');\n-    lines.push('try {');\n-    lines.push(\"  const { initBlogData } = await import('@openelement/content');\");\n-    lines.push(`  await initBlogData(${JSON.stringify(desc.blog)});`);\n-    lines.push('} catch { /* @openelement/content not available */ }');\n-    lines.push('');\n-  }\n```\n\n**从 `entry-descriptor.ts` 删除的字段**：\n\n```diff\n  export interface EntryDescriptor {\n    // ...\n-   /** Blog options for dev-mode initBlogData() call */\n-   blog?: { contentDir?: string; basePath?: string } | null;\n  }\n```\n\n**SSG bundle re-exports 更新**：\n\n`entry-renderer.ts` 中 SSG 模式的 re-exports 需要更新。因为 `virtual:less-blog-data` 在 build 模式下也是通过 `load()` 钩子解析的，Vite 会将其内联进 bundle。所以 SSG bundle 中的 re-export 从 `@openelement/content` 改为 `virtual:less-blog-data`：\n\n```diff\n- export { initBlogData, getPosts, getPostBySlug, getBlogOptions } from \"@openelement/content\"\n+ export { posts, getPostBySlug, getBlogOptions } from \"virtual:less-blog-data\"\n```\n\n同样，SSG 的 `getStaticPaths()` 对于 blog 动态路由，原来调用 `getPosts()` 获取所有 slugs，现在从 bundle 的 `posts` export 获取：\n\n```diff\n  // In the generated SSR bundle\n  export async function getStaticPaths(routePath) {\n    // ...\n    if (routePath === '/blog/:slug') {\n-     const { getPosts } = await import('@openelement/content');\n-     return getPosts().map(p => ({ slug: p.slug }));\n+     return posts.map(p => ({ slug: p.slug }));\n    }\n  }\n```\n\n**路由组件迁移**：\n\n```diff\n  // my-tech-blog/app/routes/blog/[slug].ts\n- import { getPostBySlug } from '@openelement/content';\n+ import { getPostBySlug } from 'virtual:less-blog-data';\n```\n\n**TypeScript 类型声明**（`adapter-vite` 提供）：\n\n```ts\n// packages/adapter-vite/src/virtual-data.d.ts\ndeclare module 'virtual:less-blog-data' {\n  import type { BlogPost, LessBlogOptions } from '@openelement/content';\n  export const posts: BlogPost[];\n  export function getPostBySlug(slug: string): BlogPost | undefined;\n  export function getBlogOptions(): LessBlogOptions;\n}\n```\n\n**验证**：\n\n```bash\n# Dev 模式\ncd my-tech-blog && deno task dev\n# 访问 http://localhost:5173 → 博客首页显示文章列表\n# 访问 http://localhost:5173/blog/some-post → 文章内容正常\n\n# Build 模式\ncd my-tech-blog && deno task build\n# dist/ 目录生成完整 SSG HTML\n# 博客文章页面内容正确\n```\n\n**时序图 — Dev 模式**：\n\n```mermaid\nsequenceDiagram\n    participant Browser\n    participant HonoVite as @hono/vite-dev-server\n    participant Vite as Vite SSR Runner\n    participant Plugin as less:blog-data (load)\n    participant Content as @openelement/content\n    participant FS as File System\n\n    Browser->>HonoVite: GET /blog\n    HonoVite->>Vite: ssrLoadModule('virtual:less-hono-entry')\n    Vite->>Vite: ssrLoadModule('virtual:less-blog-data')\n    Vite->>Plugin: load('\\0virtual:open-blog-data')\n    Plugin->>Content: loadBlogData(opts)\n    Content->>FS: read markdown files\n    FS-->>Content: raw content\n    Content->>Content: parse + sort\n    Content-->>Plugin: { posts: [3 items] }\n    Plugin-->>Vite: \"const _posts = [...]; export const posts = _posts; ...\"\n    Vite->>Vite: execute virtual module code\n    Note over Vite: posts = [3 items] in SSR runner\n    Vite-->>HonoVite: app module loaded\n    HonoVite->>Vite: route component imports from virtual:less-blog-data\n    Vite-->>HonoVite: posts (already loaded)\n    HonoVite-->>Browser: HTML with blog posts\n```\n\n**时序图 — Build 模式**：\n\n```mermaid\nsequenceDiagram\n    participant CLI as deno task build\n    participant Vite as viteBuild(ssr:true)\n    participant Plugin as less:blog-data (load)\n    participant Content as @openelement/content\n    participant FS as File System\n\n    CLI->>Vite: start SSR build\n    Vite->>Plugin: load('\\0virtual:open-blog-data')\n    Plugin->>Content: loadBlogData(opts)\n    Content->>FS: read markdown files\n    FS-->>Content: raw content\n    Content-->>Plugin: { posts: [3 items] }\n    Plugin-->>Vite: virtual module code with inlined data\n    Note over Vite: noExternal → virtual module inlined into bundle\n    Vite-->>CLI: self-contained SSR bundle\n    CLI->>CLI: import bundle, iterate routes\n    CLI->>CLI: renderRoute('/blog/hello') → HTML\n    CLI->>CLI: write dist/blog/hello/index.html\n```\n\n---\n\n### Phase 3: 内容 HMR\n\n**目标**：在 dev 模式下编辑 `.md` 文件后，刷新页面即可看到新内容，无需重启 dev server。\n\n**原理**：`virtual:less-blog-data` 的 `load()` 钩子是无状态的纯函数调用。只需 `invalidateModule` 让 Vite 丢弃缓存的虚拟模块，下次 `ssrLoadModule()` 就会重新调用 `load()` 获取新数据。不需要 flag，不需要重置，不需要手动重新初始化。\n\n**变更文件**：\n\n| 文件 | 变更 |\n|------|------|\n| `packages/content/src/index.ts` | 新增 `configureServer` 钩子，watch 内容目录 |\n| `packages/adapter-vite/src/virtual-data.ts` | 添加 HMR invalidate 辅助函数 |\n\n**实现**：\n\n```ts\n// packages/content/src/index.ts — 添加到 contentPlugin\n\nconfigureServer(server) {\n  const contentDir = blogOpts?.contentDir;\n  if (!contentDir) return;\n\n  const absoluteContentDir = resolve(server.config.root, contentDir);\n\n  // Watch the content directory for changes\n  server.watcher.add(absoluteContentDir);\n\n  const invalidateBlogData = (file: string) => {\n    if (!file.startsWith(absoluteContentDir)) return;\n    if (!file.endsWith('.md') && !file.endsWith('.mdx')) return;\n\n    // Invalidate the virtual blog data module\n    const mod = server.moduleGraph.getModuleById(RESOLVED_BLOG_DATA_ID);\n    if (mod) {\n      server.moduleGraph.invalidateModule(mod);\n      log.info(`Content changed: ${relative(server.config.root, file)} — reloading`);\n      server.hot.send({ type: 'full-reload' });\n    }\n  };\n\n  server.watcher.on('change', invalidateBlogData);\n  server.watcher.on('add', invalidateBlogData);\n  server.watcher.on('unlink', invalidateBlogData);\n},\n```\n\n**验证**：\n\n```bash\ncd my-tech-blog && deno task dev\n# 1. 访问 http://localhost:5173 → 博客正常\n# 2. 编辑 content/blog/some-post.md → 修改标题\n# 3. 刷新浏览器 → 看到新标题（无需重启 dev server）\n# 4. 新增 content/blog/new-post.md\n# 5. 刷新浏览器 → 新文章出现在列表中\n```\n\n**时序图 — HMR**：\n\n```mermaid\nsequenceDiagram\n    participant Dev as Developer\n    participant Watcher as Vite Watcher\n    participant Graph as Module Graph\n    participant Vite as Vite SSR Runner\n    participant Plugin as less:blog-data (load)\n    participant Browser\n\n    Dev->>Watcher: edit content/blog/post.md\n    Watcher->>Graph: invalidateModule(virtual:less-blog-data)\n    Graph-->>Watcher: module invalidated\n    Watcher->>Browser: hot.send({ type: 'full-reload' })\n    Browser->>Vite: GET /blog (new request)\n    Vite->>Plugin: load('\\0virtual:open-blog-data')\n    Note over Plugin: load() re-reads all .md files\n    Plugin-->>Vite: fresh data exports\n    Vite-->>Browser: HTML with updated content\n```\n\n---\n\n### Phase 4: @openelement/i18n 同模式改造\n\n**目标**：将 i18n 的模块级状态模式改为与 content 相同的虚拟数据模块模式。\n\n**变更文件**：\n\n| 文件 | 变更 |\n|------|------|\n| `packages/i18n/src/i18n-data.ts` | 删除 `_options`/`initI18nData()`/`getI18nLocales()`/`getI18nOptions()`/`getDefaultLocale()`，替换为 `loadI18nData()` 纯函数 |\n| `packages/i18n/src/index.ts` | 导出 `loadI18nData()`，`buildStart()` 改用纯函数 + ctx |\n| `packages/adapter-vite/src/virtual-data.ts` | 新增 `virtual:less-i18n-data` 虚拟模块 |\n\n**i18n 数据量小**（只是 locale 配置），虚拟模块的序列化成本可以忽略。实现模式与 blog 完全相同。\n\n**新增虚拟模块**：\n\n```ts\n// virtual:less-i18n-data\nconst VIRTUAL_I18N_DATA_ID = 'virtual:less-i18n-data';\nconst RESOLVED_I18N_DATA_ID = '\\0' + VIRTUAL_I18N_DATA_ID;\n\nexport function createI18nDataPlugin(ctx: LessBuildContext): Plugin {\n  return {\n    name: 'less:i18n-data',\n    enforce: 'pre',\n\n    resolveId(id) {\n      if (id === VIRTUAL_I18N_DATA_ID) return RESOLVED_I18N_DATA_ID;\n    },\n\n    load(id) {\n      if (id !== RESOLVED_I18N_DATA_ID) return;\n\n      const i18nOpts = ctx.i18nOptions;\n      if (!i18nOpts) {\n        return [\n          'export const locales = [];',\n          'export function getDefaultLocale() { return \"en\"; }',\n          'export function getI18nOptions() { return null; }',\n        ].join('\\n');\n      }\n\n      return [\n        `const _options = ${JSON.stringify(i18nOpts)};`,\n        'export const locales = _options.locales;',\n        'export function getDefaultLocale() { return _options.defaultLocale || \"en\"; }',\n        'export function getI18nOptions() { return _options; }',\n      ].join('\\n');\n    },\n  };\n}\n```\n\n**TypeScript 类型声明**：\n\n```ts\ndeclare module 'virtual:less-i18n-data' {\n  import type { LessI18nOptions } from '@openelement/i18n';\n  export const locales: string[];\n  export function getDefaultLocale(): string;\n  export function getI18nOptions(): LessI18nOptions | null;\n}\n```\n\n**验证**：\n\n```bash\n# 使用 i18n 的项目（如有）\ndeno task dev\n# 访问页面 → locale 切换正常\n# 无 i18n 的项目 → 编译不报错（虚拟模块返回空数据）\n```\n\n---\n\n### Phase 5: 静态内容页\n\n**目标**：`lessContent()` 增加 `pages` 选项，框架自动解析 markdown 内容页，通过虚拟模块提供数据。消除用户代码中的 `node:fs` + 手写 md 转换器。\n\n**变更文件**：\n\n| 文件 | 变更 |\n|------|------|\n| `packages/content/src/types.ts` | 新增 `LessPagesOptions` 类型 |\n| `packages/content/src/pages/page-loader.ts` | **新建** — 静态页加载器 |\n| `packages/content/src/index.ts` | `lessContent()` 新增 `pages` 选项处理 |\n| `packages/adapter-vite/src/virtual-data.ts` | 新增 `virtual:less-page-data` 虚拟模块 |\n\n**接口设计**：\n\n```ts\n// types.ts\nexport interface LessPagesOptions {\n  /** Map of page key → content file path (relative to project root) */\n  [key: string]: string;\n  // e.g., { about: 'content/about/index.md', credits: 'content/credits.md' }\n}\n\nexport interface LessContentOptions {\n  blog?: LessBlogOptions | false;\n  nav?: LessNavOptions | false;\n  sitemap?: SitemapOptions | false;\n  pages?: LessPagesOptions;  // NEW\n  ctx?: LessBuildContext;\n}\n```\n\n**虚拟模块导出**：\n\n```ts\n// virtual:less-page-data\n// load() generates:\nconst _pages = {\n  about: { title: \"About\", html: \"<p>...</p>\", frontmatter: {...} },\n  credits: { title: \"Credits\", html: \"<p>...</p>\", frontmatter: {...} },\n};\nexport function getPage(key) { return _pages[key]; }\nexport const pageKeys = Object.keys(_pages);\n```\n\n**路由组件使用**：\n\n```ts\n// app/routes/about.ts\nimport { getPage } from 'virtual:less-page-data';\n\nexport default class AboutPage extends LitElement {\n  connectedCallback() {\n    super.connectedCallback();\n    const page = getPage('about');\n    this.innerHTML = page.html;\n  }\n}\n```\n\n**验证**：\n\n```bash\n# my-tech-blog About 页面\n# 1. 创建 content/about/index.md\n# 2. 配置 lessContent({ pages: { about: 'content/about/index.md' } })\n# 3. 路由组件 import { getPage } from 'virtual:less-page-data'\n# 4. 访问 /about → 渲染 markdown 内容\n# 5. 无 node:fs 依赖\n```\n\n---\n\n## 清理清单\n\n所有 Phase 完成后，需要清理的遗留代码：\n\n| 项 | 位置 | 操作 |\n|----|------|------|\n| `EntryDescriptor.blog` | `entry-descriptor.ts` | 删除 |\n| `blog` 参数 | `buildEntryDescriptor()` | 删除 |\n| `blog` 参数 | `generateHonoEntryCode()` | 删除 |\n| `blog` 传递 | `index.ts` generateEntry() | 删除 `blog: ctx.blogOptions` |\n| initBlogData 注入 | `entry-renderer.ts` 306-313 行 | 删除 |\n| `_posts` 模块状态 | `blog-data.ts` | 删除 |\n| `initBlogData()` / `getPosts()` / `getPostBySlug()` / `getBlogOptions()` | `blog-data.ts` + `index.ts` re-exports | 删除 |\n| `initBlogData` re-export | SSG bundle | 改为 `virtual:less-blog-data` export |\n| `buildCoreSubpathAliases()` | `adapter-vite` | 删除整个函数 |\n| 17 行 resolve.alias | `my-tech-blog/vite.config.ts` | 删除 |\n\n---\n\n## Consequences\n\n### Positive\n\n- **单路径**：dev/build 走同一条路（虚拟模块 `load()`），不存在双初始化路径\n- **零桥**：没有 `onSSRInit`，没有 Data Bridge，没有 ad-hoc 代码注入\n- **零状态**：content/i18n 不持有模块级状态，不存在\"状态在错误的模块空间\"的问题\n- **HMR 天然支持**：`invalidateModule` → `load()` 重新执行 → 新数据，无 flag 重置\n- **用户 vite.config.ts 清零**：`@deno/vite-plugin` 消除所有 resolve.alias\n- **`EntryDescriptor` 纯净化**：只描述 Hono entry 结构，不承载插件初始化逻辑\n- **`entry-renderer.ts` 纯净化**：恢复为纯函数（描述符 → 字符串）\n- **可扩展**：未来新插件只需 `loadBlogData()` 纯函数 + 虚拟模块，零框架改动\n\n### Negative\n\n- **大数据集序列化**：100 篇博文含 HTML body 时，`JSON.stringify(posts)` 可能达到 MB 级。但 SSG build 模式同样需要处理这个量级（bundle 大小），且 dev 模式每次请求重新生成，不会有内存累积\n- **虚拟模块 ID 不是标准路径**：IDE 可能不识别 `virtual:less-blog-data` 的 import，需要 TypeScript `.d.ts` 声明文件\n- **迁移成本**：路由组件需要将 `import { getPosts } from '@openelement/content'` 改为 `import { posts } from 'virtual:less-blog-data'`\n\n### Neutral\n\n- 虚拟模块模式与 Astro 的 `import.meta.glob()` / collection 模式类似，开发者认知成本低\n- `initBlogData()`/`getPosts()`/`buildCoreSubpathAliases()` 直接删除，不向后兼容 — v0.12.0 是 breaking change\n\n---\n\n## 参考\n\n- [ADR 0011: 消除最后一个 globalThis 桥接](/blog/0011-eliminate-last-globalthis-via-closebundle) — 类似的跨模块空间数据传递问题\n- [ADR 0016: 双模式子路径解析](/blog/0016-dual-mode-subpath-resolution) — coreResolvePlugin 的当前实现\n- [ADR 0017: 框架运行时与构建编排解耦](/blog/0017-separate-runtime-from-build-orchestration) — core/adapter-vite 分离\n- [Vite SSR](https://vite.dev/guide/ssr) — SSR runner 的模块隔离机制\n- [@deno/vite-plugin](https://github.com/denoland/deno/tree/main/packages/vite) — Deno import map 集成\n- [Astro Content Collections](https://docs.astro.build/en/guides/content-collections/) — 类似的虚拟模块数据模式\n\n---\n\n_提出日期: 2026-05-11 | 状态: ACCEPTED | 目标版本: v0.12.0_\n",
    "html": "<h2>Status</h2>\n<p><strong>ACCEPTED</strong> — v0.12.0 架构改进</p>\n<h2>Context</h2>\n<h3>问题本质</h3>\n<p>LessJS 的 <code>@openelement/content</code> 和 <code>@openelement/i18n</code> 使用<strong>模块级状态</strong>存储数据：</p>\n<pre><code>content/blog-data.ts:    let _posts: BlogPost[] = [];    // 模块级变量\ni18n/i18n-data.ts:       let _options: LessI18nOptions;  // 模块级变量\n</code></pre>\n<p>插件在 <code>buildStart()</code> 中调用 <code>initBlogData()</code> / <code>initI18nData()</code> 填充状态。路由组件调用 <code>getPosts()</code> / <code>getI18nLocales()</code> 读取状态。</p>\n<p><strong>Build 模式</strong>正常：<code>viteBuild(ssr:true, noExternal)</code> 产出自包含 bundle，所有代码在同一个模块实例。</p>\n<p><strong>Dev 模式</strong>崩溃：Vite 插件进程和 SSR runner 是隔离的 JavaScript 领域。<code>initBlogData()</code> 在插件进程运行，<code>getPosts()</code> 在 SSR runner 运行——它们看到的是<strong>不同的 <code>_posts</code></strong>。结果是 <code>_posts = []</code>，博客数据为空。</p>\n<h3>当前 ad-hoc 修复及其问题</h3>\n<p>在虚拟入口代码中注入 <code>initBlogData()</code> 调用（<code>entry-renderer.ts</code> 第 306-313 行），让初始化代码在 SSR runner 模块空间中执行。</p>\n<p>问题：</p>\n<ol>\n<li><code>EntryDescriptor.blog</code> 字段——每个插件都要加字段，<code>EntryDescriptor</code> 侵入性增长</li>\n<li><code>entry-renderer.ts</code> 承载业务逻辑——不再是纯函数</li>\n<li><code>@openelement/adapter-vite</code> 对 content 包的硬依赖知识——需要知道导出名、参数格式</li>\n<li><code>@openelement/i18n</code> 还没修——同样的问题，还没加 ad-hoc 修复</li>\n<li>内容 HMR 不工作——<code>_posts</code> 变量停留在首次加载时的状态</li>\n</ol>\n<h3>根因不是&quot;需要桥&quot;，而是&quot;不该有状态&quot;</h3>\n<p>模块级 <code>_posts</code> 变量是 bug source。如果有状态，就需要桥；如果没状态，就不需要桥。</p>\n<p><strong>正确思路：砍掉模块级状态，用虚拟模块的数据导出替代。</strong></p>\n<h3>CYBER::BLOG 暴露的完整问题清单</h3>\n<table>\n<thead>\n<tr>\n<th>#</th>\n<th>问题</th>\n<th>根因</th>\n<th>优先级</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>1</td>\n<td>Dev 模式 blog 数据空</td>\n<td>模块级状态 + Vite 模块隔离</td>\n<td>P0</td>\n</tr>\n<tr>\n<td>2</td>\n<td>17 行 resolve.alias 样板代码</td>\n<td>Deno workspace 包不在 node_modules</td>\n<td>P1</td>\n</tr>\n<tr>\n<td>3</td>\n<td>内容 HMR 不工作</td>\n<td>缺 configureServer + watcher</td>\n<td>P1</td>\n</tr>\n<tr>\n<td>4</td>\n<td>@openelement/i18n dev 数据空</td>\n<td>同 #1 模式</td>\n<td>P2</td>\n</tr>\n<tr>\n<td>5</td>\n<td>About 页使用 node:fs</td>\n<td>content 只管 blog，缺静态内容方案</td>\n<td>P2</td>\n</tr>\n<tr>\n<td>6</td>\n<td>About 页手写 md 转换器</td>\n<td>同 #5，用户被迫 workaround</td>\n<td>P2</td>\n</tr>\n</tbody></table>\n<h2>Decision</h2>\n<h3>核心原则</h3>\n<p><strong>Zero module state. Zero bridges. Virtual modules are the data source.</strong></p>\n<ol>\n<li>插件提供<strong>纯函数</strong>（<code>loadBlogData()</code> → <code>BlogPost[]</code>），不持有状态</li>\n<li><code>adapter-vite</code> 提供虚拟数据模块（<code>virtual:less-blog-data</code>），<code>load()</code> 钩子调用纯函数并生成导出代码</li>\n<li>路由组件从虚拟模块 import 数据，不从 <code>@openelement/content</code> 的模块状态读取</li>\n<li><code>@deno/vite-plugin</code> 处理 bare specifier 解析，消除用户 <code>vite.config.ts</code> 中的 alias</li>\n</ol>\n<p><strong>不需要 <code>onSSRInit</code>，不需要 Data Bridge 协议，不需要双重初始化路径。</strong></p>\n<h3>架构对比</h3>\n<pre><code>Before (stateful):\n  @openelement/content:  initBlogData() → _posts       (module state, needs bridge)\n                    getPosts()     → _posts          (reads state)\n  adapter-vite:     inject initBlogData() into entry (ad-hoc bridge)\n  route component:  import { getPosts } from &#39;@openelement/content&#39;\n  问题：_posts 在两个模块空间各一份 → 需要桥\n\nAfter (stateless):\n  @openelement/content:  loadBlogData(dir) → BlogPost[]  (pure function, zero state)\n  adapter-vite:     virtual:less-blog-data           (resolveId + load)\n                    load() calls loadBlogData() → generates export code\n  route component:  import { posts, getPostBySlug } from &#39;virtual:less-blog-data&#39;\n  零桥，零状态，零双路径\n</code></pre>\n<h3>为什么这样足够</h3>\n<p><strong>Dev 模式</strong>：<code>@hono/vite-dev-server</code> 调 <code>ssrLoadModule(&#39;virtual:less-blog-data&#39;)</code> → 触发 <code>load()</code> → 生成含数据的代码 → SSR runner 执行 → 数据在。没有初始化步骤，没有空变量。</p>\n<p><strong>Build 模式</strong>：<code>viteBuild(ssr:true, noExternal)</code> 打包时 <code>load()</code> 执行 → 数据内联进 bundle。和 dev 走同一条路。</p>\n<p><strong>HMR</strong>：<code>configureServer</code> watch 内容目录 → <code>invalidateModule(virtualBlogData)</code> → 下次请求自动触发 <code>load()</code> 重新生成 → 无缓存问题，无 flag 需重置。</p>\n<p><strong>Dev 和 Build 走同一条路。</strong> 不存在&quot;build 模式走 buildStart，dev 模式走 onSSRInit&quot;的双路径问题。</p>\n<hr />\n<h2>Implementation SOP</h2>\n<h3>Phase 依赖关系</h3>\n<pre><code>Phase 0: @deno/vite-plugin → 独立，可先行\nPhase 1: content 纯函数拆分 → Phase 2 的前置\nPhase 2: adapter-vite 虚拟数据模块 → Phase 1 + Phase 3 的核心\nPhase 3: content HMR → Phase 2 的后续\nPhase 4: i18n 同模式改造 → Phase 2 的验证\nPhase 5: 静态内容页 → Phase 2 的扩展\n</code></pre>\n<h3>Phase 0: @deno/vite-plugin 集成</h3>\n<p><strong>目标</strong>：消除用户 <code>vite.config.ts</code> 中的 resolve.alias，让 Deno import map 处理 <code>@openelement/*</code> 解析。</p>\n<p><strong>原理</strong>：<code>@deno/vite-plugin</code> 使用 <code>import.meta.resolve()</code> 将 bare specifier 解析为 Deno workspace 中的实际路径。用户的 <code>deno.json</code> 已经定义了 workspace imports，Vite 不需要知道这些映射。</p>\n<p><strong>变更文件</strong>：</p>\n<table>\n<thead>\n<tr>\n<th>文件</th>\n<th>变更</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>packages/adapter-vite/src/index.ts</code></td>\n<td>在 <code>less()</code> 返回的插件数组中注入 <code>denoVitePlugin()</code></td>\n</tr>\n<tr>\n<td><code>packages/adapter-vite/deno.json</code></td>\n<td>添加 <code>@deno/vite-plugin</code> 依赖</td>\n</tr>\n<tr>\n<td><code>my-tech-blog/vite.config.ts</code></td>\n<td>删除所有 resolve.alias</td>\n</tr>\n</tbody></table>\n<p><strong>实现步骤</strong>：</p>\n<ol>\n<li>在 adapter-vite 中添加 <code>@deno/vite-plugin</code> 依赖</li>\n<li>在 <code>less()</code> 的 <code>config()</code> 钩子中检测是否可用 <code>@deno/vite-plugin</code>，可用则注入</li>\n<li>删除 <code>buildCoreSubpathAliases()</code> 整个函数及所有调用点</li>\n<li>更新 <code>my-tech-blog/vite.config.ts</code> 删除 17 行 alias</li>\n</ol>\n<p><strong>验证</strong>：</p>\n<pre><code class=\"language-bash\">cd my-tech-blog &amp;&amp; deno task dev\n# 访问 http://localhost:5173 → 页面正常渲染\n# 确认 vite.config.ts 无 resolve.alias\n</code></pre>\n<p><strong>时序图</strong>：</p>\n<pre><code class=\"language-mermaid\">sequenceDiagram\n    participant User as vite.config.ts\n    participant Deno as @deno/vite-plugin\n    participant Vite as Vite Dev Server\n    participant FS as Deno Workspace FS\n\n    User-&gt;&gt;Vite: plugins: [lessjs()]\n    Note over User: Zero resolve.alias\n    Vite-&gt;&gt;Deno: resolveId(&#39;@openelement/content&#39;)\n    Deno-&gt;&gt;FS: import.meta.resolve(&#39;@openelement/content&#39;)\n    FS--&gt;&gt;Deno: file:///.../packages/content/src/index.ts\n    Deno--&gt;&gt;Vite: resolved path\n    Vite-&gt;&gt;Deno: resolveId(&#39;@openelement/core/ssr-handler&#39;)\n    Deno-&gt;&gt;FS: import.meta.resolve(&#39;@openelement/core/ssr-handler&#39;)\n    FS--&gt;&gt;Deno: file:///.../packages/core/src/ssr-handler.ts\n    Deno--&gt;&gt;Vite: resolved path\n</code></pre>\n<p><strong>回退方案</strong>：如果 <code>@deno/vite-plugin</code> 不支持子路径解析或出现兼容问题，直接修 <code>@deno/vite-plugin</code> 提 PR，不走 <code>buildCoreSubpathAliases()</code> 回头路。</p>\n<hr />\n<h3>Phase 1: @openelement/content 纯函数拆分</h3>\n<p><strong>目标</strong>：将 <code>blog-data.ts</code> 的模块级状态 + init/getter 模式，重构为纯函数 + 虚拟模块导出模式。</p>\n<p><strong>变更文件</strong>：</p>\n<table>\n<thead>\n<tr>\n<th>文件</th>\n<th>变更</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>packages/content/src/blog/blog-data.ts</code></td>\n<td>删除 <code>_posts</code>/<code>_options</code>/<code>initBlogData()</code>/<code>getPosts()</code>/<code>getPostBySlug()</code>/<code>getBlogOptions()</code>，替换为 <code>loadBlogData()</code> 纯函数</td>\n</tr>\n<tr>\n<td><code>packages/content/src/index.ts</code></td>\n<td>导出 <code>loadBlogData()</code>，删除 <code>initBlogData</code>/<code>getPosts</code>/<code>getPostBySlug</code>/<code>getBlogOptions</code> 导出，<code>buildStart()</code> 改用 <code>loadBlogData()</code></td>\n</tr>\n<tr>\n<td><code>packages/content/src/blog/markdown.ts</code></td>\n<td>确保 <code>parseMarkdownFile()</code> 无文件系统副作用（纯函数）</td>\n</tr>\n<tr>\n<td><code>packages/content/src/blog/routes.ts</code></td>\n<td>确保 <code>generateBlogRoutes()</code> 无副作用</td>\n</tr>\n</tbody></table>\n<p><strong>新增接口</strong>：</p>\n<pre><code class=\"language-ts\">// packages/content/src/blog/blog-data.ts\n\n/**\n * Pure function: load blog data from file system.\n * No module-level state. No side effects beyond reading files.\n * Can be called from any runtime context.\n *\n * This replaces the stateful initBlogData() + getPosts() pattern.\n * For virtual module consumers, use virtual:less-blog-data instead.\n */\nexport async function loadBlogData(options?: LessBlogOptions): Promise&lt;{\n  posts: BlogPost[];\n  basePath: string;\n}&gt; {\n  const routes = await generateBlogRoutes(options);\n  return {\n    posts: routes.posts,\n    basePath: routes.basePath,\n  };\n}\n</code></pre>\n<p><strong>删除的接口</strong>（不保留，不 deprecated，直接砍）：</p>\n<pre><code class=\"language-ts\">// 全部删除 — 不向后兼容\n// let _posts: BlogPost[] = [];           ← 删除\n// let _options: LessBlogOptions = {};    ← 删除\n// export function initBlogData() {}      ← 删除\n// export function getPosts() {}          ← 删除\n// export function getPostBySlug() {}     ← 删除\n// export function getBlogOptions() {}    ← 删除\n</code></pre>\n<p><code>blog-data.ts</code> 重写后只剩 <code>loadBlogData()</code> 纯函数，零状态。</p>\n<pre><code class=\"language-ts\">// packages/content/src/blog/blog-data.ts — 重写后\n\nimport type { BlogPost, LessBlogOptions } from &#39;./types.ts&#39;;\nimport { generateBlogRoutes } from &#39;./routes.ts&#39;;\n\n/**\n * Pure function: load blog data from file system.\n * No module-level state. No side effects beyond reading files.\n * Can be called from any runtime context.\n */\nexport async function loadBlogData(options?: LessBlogOptions): Promise&lt;{\n  posts: BlogPost[];\n  basePath: string;\n}&gt; {\n  const routes = await generateBlogRoutes(options);\n  return {\n    posts: routes.posts,\n    basePath: routes.basePath,\n  };\n}\n</code></pre>\n<p><strong>验证</strong>：</p>\n<pre><code class=\"language-bash\">cd packages/content &amp;&amp; deno test\n# loadBlogData() 单元测试通过\n# initBlogData/getPosts/getPostBySlug/getBlogOptions 已删除，不再存在\n</code></pre>\n<p><strong>时序图</strong>：</p>\n<pre><code class=\"language-mermaid\">sequenceDiagram\n    participant Dev as Developer\n    participant Content as @openelement/content\n    participant FS as File System\n\n    Note over Dev: Phase 1: Pure function only\n    Dev-&gt;&gt;Content: loadBlogData(opts)\n    Content-&gt;&gt;FS: read markdown files\n    FS--&gt;&gt;Content: raw content\n    Content-&gt;&gt;Content: parse + sort\n    Content--&gt;&gt;Dev: { posts, basePath }\n    Note over Content: Zero module state\n</code></pre>\n<hr />\n<h3>Phase 2: adapter-vite 虚拟数据模块</h3>\n<p><strong>目标</strong>：新增 <code>virtual:less-blog-data</code> 虚拟模块插件，替代 <code>entry-renderer.ts</code> 中的 ad-hoc <code>initBlogData()</code> 注入。</p>\n<p><strong>这是整个方案的核心。</strong> 虚拟模块是唯一的数据桥，dev/build 走同一条路。</p>\n<p><strong>变更文件</strong>：</p>\n<table>\n<thead>\n<tr>\n<th>文件</th>\n<th>变更</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>packages/adapter-vite/src/virtual-data.ts</code></td>\n<td><strong>新建</strong> — 虚拟数据模块插件</td>\n</tr>\n<tr>\n<td><code>packages/adapter-vite/src/index.ts</code></td>\n<td>引入 virtual data plugin，删除 <code>EntryDescriptor.blog</code> 相关逻辑</td>\n</tr>\n<tr>\n<td><code>packages/adapter-vite/src/entry-renderer.ts</code></td>\n<td>删除 <code>initBlogData()</code> 注入代码</td>\n</tr>\n<tr>\n<td><code>packages/adapter-vite/src/entry-descriptor.ts</code></td>\n<td>删除 <code>blog</code> 字段</td>\n</tr>\n<tr>\n<td><code>packages/adapter-vite/src/build-context.ts</code></td>\n<td><code>blogOptions</code> 字段改为内部配置，不传给 entry descriptor</td>\n</tr>\n<tr>\n<td><code>my-tech-blog/app/routes/</code></td>\n<td>路由组件改为从 <code>virtual:less-blog-data</code> import</td>\n</tr>\n</tbody></table>\n<p><strong>新增文件 <code>virtual-data.ts</code></strong>：</p>\n<pre><code class=\"language-ts\">/**\n * @openelement/adapter-vite - Virtual data module plugins\n *\n * Replaces module-level state in @openelement/content and @openelement/i18n\n * with virtual module data exports. The load() hook runs in the\n * Vite plugin process and generates code that executes in the\n * SSR runner — this IS the data bridge.\n *\n * Dev mode:  ssrLoadModule(&#39;virtual:less-blog-data&#39;) → load() → fresh data\n * Build mode: viteBuild bundles the virtual module → data inlined\n * HMR:       invalidateModule → load() re-runs → fresh data\n *\n * Zero module state. Zero init functions. Zero dual paths.\n */\n\nimport type { Plugin } from &#39;vite&#39;;\nimport type { LessBuildContext } from &#39;./build-context.js&#39;;\nimport { loadBlogData } from &#39;@openelement/content/blog-data&#39;;\nimport type { LessBlogOptions } from &#39;@openelement/content/types&#39;;\n\n// ─── Virtual module IDs ────────────────────────────────────────\n\nconst VIRTUAL_BLOG_DATA_ID = &#39;virtual:less-blog-data&#39;;\nconst RESOLVED_BLOG_DATA_ID = &#39;\\0&#39; + VIRTUAL_BLOG_DATA_ID;\n\n// ─── Blog data virtual module ──────────────────────────────────\n\nexport function createBlogDataPlugin(ctx: LessBuildContext): Plugin {\n  return {\n    name: &#39;less:blog-data&#39;,\n    enforce: &#39;pre&#39;,\n\n    resolveId(id) {\n      if (id === VIRTUAL_BLOG_DATA_ID) return RESOLVED_BLOG_DATA_ID;\n    },\n\n    async load(id) {\n      if (id !== RESOLVED_BLOG_DATA_ID) return;\n\n      const blogOpts = ctx.blogOptions;\n      if (!blogOpts) {\n        // Blog not configured — export empty data\n        return [\n          &#39;export const posts = [];&#39;,\n          &#39;export function getPostBySlug(slug) { return undefined; }&#39;,\n          &#39;export function getBlogOptions() { return {}; }&#39;,\n        ].join(&#39;\\n&#39;);\n      }\n\n      const options: LessBlogOptions = {\n        contentDir: blogOpts.contentDir,\n        basePath: blogOpts.basePath,\n      };\n\n      const { posts } = await loadBlogData(options);\n\n      // Generate code that exports data + derived functions\n      // The posts array is serialized as JSON — this is the bridge.\n      // In build mode, this gets inlined into the SSR bundle.\n      // In dev mode, this runs fresh on each ssrLoadModule() call.\n      return [\n        `const _posts = ${JSON.stringify(posts)};`,\n        &#39;&#39;,\n        `/** All published blog posts, sorted newest first */`,\n        `export const posts = _posts;`,\n        &#39;&#39;,\n        `/** Get a single post by slug */`,\n        `export function getPostBySlug(slug) {`,\n        `  return _posts.find(p =&gt; p.slug === slug);`,\n        `}`,\n        &#39;&#39;,\n        `/** Get blog configuration */`,\n        `export function getBlogOptions() {`,\n        `  return ${JSON.stringify(options)};`,\n        `}`,\n      ].join(&#39;\\n&#39;);\n    },\n  };\n}\n</code></pre>\n<p><strong>从 <code>entry-renderer.ts</code> 删除的代码</strong>：</p>\n<pre><code class=\"language-diff\">-  // --- Dev mode: Initialize blog data ---\n-  // In SSG mode, initBlogData() is called by build-ssg.ts after importing\n-  // the SSR bundle (same module instance). In dev mode, the Vite SSR runner\n-  // loads route components (e.g., cyber-home) that call getPosts(), but the\n-  // blog-data module&#39;s _posts variable is empty because initBlogData() only\n-  // ran in the plugin process (different module space). We must call\n-  // initBlogData() inside the virtual entry so it runs in the same module\n-  // instance as the route components.\n-  if (!desc.isSSG &amp;&amp; desc.blog) {\n-    lines.push(&#39;// Dev mode: Initialize blog data in SSR runner module space&#39;);\n-    lines.push(&#39;try {&#39;);\n-    lines.push(&quot;  const { initBlogData } = await import(&#39;@openelement/content&#39;);&quot;);\n-    lines.push(`  await initBlogData(${JSON.stringify(desc.blog)});`);\n-    lines.push(&#39;} catch { /* @openelement/content not available */ }&#39;);\n-    lines.push(&#39;&#39;);\n-  }\n</code></pre>\n<p><strong>从 <code>entry-descriptor.ts</code> 删除的字段</strong>：</p>\n<pre><code class=\"language-diff\">  export interface EntryDescriptor {\n    // ...\n-   /** Blog options for dev-mode initBlogData() call */\n-   blog?: { contentDir?: string; basePath?: string } | null;\n  }\n</code></pre>\n<p><strong>SSG bundle re-exports 更新</strong>：</p>\n<p><code>entry-renderer.ts</code> 中 SSG 模式的 re-exports 需要更新。因为 <code>virtual:less-blog-data</code> 在 build 模式下也是通过 <code>load()</code> 钩子解析的，Vite 会将其内联进 bundle。所以 SSG bundle 中的 re-export 从 <code>@openelement/content</code> 改为 <code>virtual:less-blog-data</code>：</p>\n<pre><code class=\"language-diff\">- export { initBlogData, getPosts, getPostBySlug, getBlogOptions } from &quot;@openelement/content&quot;\n+ export { posts, getPostBySlug, getBlogOptions } from &quot;virtual:less-blog-data&quot;\n</code></pre>\n<p>同样，SSG 的 <code>getStaticPaths()</code> 对于 blog 动态路由，原来调用 <code>getPosts()</code> 获取所有 slugs，现在从 bundle 的 <code>posts</code> export 获取：</p>\n<pre><code class=\"language-diff\">  // In the generated SSR bundle\n  export async function getStaticPaths(routePath) {\n    // ...\n    if (routePath === &#39;/blog/:slug&#39;) {\n-     const { getPosts } = await import(&#39;@openelement/content&#39;);\n-     return getPosts().map(p =&gt; ({ slug: p.slug }));\n+     return posts.map(p =&gt; ({ slug: p.slug }));\n    }\n  }\n</code></pre>\n<p><strong>路由组件迁移</strong>：</p>\n<pre><code class=\"language-diff\">  // my-tech-blog/app/routes/blog/[slug].ts\n- import { getPostBySlug } from &#39;@openelement/content&#39;;\n+ import { getPostBySlug } from &#39;virtual:less-blog-data&#39;;\n</code></pre>\n<p><strong>TypeScript 类型声明</strong>（<code>adapter-vite</code> 提供）：</p>\n<pre><code class=\"language-ts\">// packages/adapter-vite/src/virtual-data.d.ts\ndeclare module &#39;virtual:less-blog-data&#39; {\n  import type { BlogPost, LessBlogOptions } from &#39;@openelement/content&#39;;\n  export const posts: BlogPost[];\n  export function getPostBySlug(slug: string): BlogPost | undefined;\n  export function getBlogOptions(): LessBlogOptions;\n}\n</code></pre>\n<p><strong>验证</strong>：</p>\n<pre><code class=\"language-bash\"># Dev 模式\ncd my-tech-blog &amp;&amp; deno task dev\n# 访问 http://localhost:5173 → 博客首页显示文章列表\n# 访问 http://localhost:5173/blog/some-post → 文章内容正常\n\n# Build 模式\ncd my-tech-blog &amp;&amp; deno task build\n# dist/ 目录生成完整 SSG HTML\n# 博客文章页面内容正确\n</code></pre>\n<p><strong>时序图 — Dev 模式</strong>：</p>\n<pre><code class=\"language-mermaid\">sequenceDiagram\n    participant Browser\n    participant HonoVite as @hono/vite-dev-server\n    participant Vite as Vite SSR Runner\n    participant Plugin as less:blog-data (load)\n    participant Content as @openelement/content\n    participant FS as File System\n\n    Browser-&gt;&gt;HonoVite: GET /blog\n    HonoVite-&gt;&gt;Vite: ssrLoadModule(&#39;virtual:less-hono-entry&#39;)\n    Vite-&gt;&gt;Vite: ssrLoadModule(&#39;virtual:less-blog-data&#39;)\n    Vite-&gt;&gt;Plugin: load(&#39;\\0virtual:open-blog-data&#39;)\n    Plugin-&gt;&gt;Content: loadBlogData(opts)\n    Content-&gt;&gt;FS: read markdown files\n    FS--&gt;&gt;Content: raw content\n    Content-&gt;&gt;Content: parse + sort\n    Content--&gt;&gt;Plugin: { posts: [3 items] }\n    Plugin--&gt;&gt;Vite: &quot;const _posts = [...]; export const posts = _posts; ...&quot;\n    Vite-&gt;&gt;Vite: execute virtual module code\n    Note over Vite: posts = [3 items] in SSR runner\n    Vite--&gt;&gt;HonoVite: app module loaded\n    HonoVite-&gt;&gt;Vite: route component imports from virtual:less-blog-data\n    Vite--&gt;&gt;HonoVite: posts (already loaded)\n    HonoVite--&gt;&gt;Browser: HTML with blog posts\n</code></pre>\n<p><strong>时序图 — Build 模式</strong>：</p>\n<pre><code class=\"language-mermaid\">sequenceDiagram\n    participant CLI as deno task build\n    participant Vite as viteBuild(ssr:true)\n    participant Plugin as less:blog-data (load)\n    participant Content as @openelement/content\n    participant FS as File System\n\n    CLI-&gt;&gt;Vite: start SSR build\n    Vite-&gt;&gt;Plugin: load(&#39;\\0virtual:open-blog-data&#39;)\n    Plugin-&gt;&gt;Content: loadBlogData(opts)\n    Content-&gt;&gt;FS: read markdown files\n    FS--&gt;&gt;Content: raw content\n    Content--&gt;&gt;Plugin: { posts: [3 items] }\n    Plugin--&gt;&gt;Vite: virtual module code with inlined data\n    Note over Vite: noExternal → virtual module inlined into bundle\n    Vite--&gt;&gt;CLI: self-contained SSR bundle\n    CLI-&gt;&gt;CLI: import bundle, iterate routes\n    CLI-&gt;&gt;CLI: renderRoute(&#39;/blog/hello&#39;) → HTML\n    CLI-&gt;&gt;CLI: write dist/blog/hello/index.html\n</code></pre>\n<hr />\n<h3>Phase 3: 内容 HMR</h3>\n<p><strong>目标</strong>：在 dev 模式下编辑 <code>.md</code> 文件后，刷新页面即可看到新内容，无需重启 dev server。</p>\n<p><strong>原理</strong>：<code>virtual:less-blog-data</code> 的 <code>load()</code> 钩子是无状态的纯函数调用。只需 <code>invalidateModule</code> 让 Vite 丢弃缓存的虚拟模块，下次 <code>ssrLoadModule()</code> 就会重新调用 <code>load()</code> 获取新数据。不需要 flag，不需要重置，不需要手动重新初始化。</p>\n<p><strong>变更文件</strong>：</p>\n<table>\n<thead>\n<tr>\n<th>文件</th>\n<th>变更</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>packages/content/src/index.ts</code></td>\n<td>新增 <code>configureServer</code> 钩子，watch 内容目录</td>\n</tr>\n<tr>\n<td><code>packages/adapter-vite/src/virtual-data.ts</code></td>\n<td>添加 HMR invalidate 辅助函数</td>\n</tr>\n</tbody></table>\n<p><strong>实现</strong>：</p>\n<pre><code class=\"language-ts\">// packages/content/src/index.ts — 添加到 contentPlugin\n\nconfigureServer(server) {\n  const contentDir = blogOpts?.contentDir;\n  if (!contentDir) return;\n\n  const absoluteContentDir = resolve(server.config.root, contentDir);\n\n  // Watch the content directory for changes\n  server.watcher.add(absoluteContentDir);\n\n  const invalidateBlogData = (file: string) =&gt; {\n    if (!file.startsWith(absoluteContentDir)) return;\n    if (!file.endsWith(&#39;.md&#39;) &amp;&amp; !file.endsWith(&#39;.mdx&#39;)) return;\n\n    // Invalidate the virtual blog data module\n    const mod = server.moduleGraph.getModuleById(RESOLVED_BLOG_DATA_ID);\n    if (mod) {\n      server.moduleGraph.invalidateModule(mod);\n      log.info(`Content changed: ${relative(server.config.root, file)} — reloading`);\n      server.hot.send({ type: &#39;full-reload&#39; });\n    }\n  };\n\n  server.watcher.on(&#39;change&#39;, invalidateBlogData);\n  server.watcher.on(&#39;add&#39;, invalidateBlogData);\n  server.watcher.on(&#39;unlink&#39;, invalidateBlogData);\n},\n</code></pre>\n<p><strong>验证</strong>：</p>\n<pre><code class=\"language-bash\">cd my-tech-blog &amp;&amp; deno task dev\n# 1. 访问 http://localhost:5173 → 博客正常\n# 2. 编辑 content/blog/some-post.md → 修改标题\n# 3. 刷新浏览器 → 看到新标题（无需重启 dev server）\n# 4. 新增 content/blog/new-post.md\n# 5. 刷新浏览器 → 新文章出现在列表中\n</code></pre>\n<p><strong>时序图 — HMR</strong>：</p>\n<pre><code class=\"language-mermaid\">sequenceDiagram\n    participant Dev as Developer\n    participant Watcher as Vite Watcher\n    participant Graph as Module Graph\n    participant Vite as Vite SSR Runner\n    participant Plugin as less:blog-data (load)\n    participant Browser\n\n    Dev-&gt;&gt;Watcher: edit content/blog/post.md\n    Watcher-&gt;&gt;Graph: invalidateModule(virtual:less-blog-data)\n    Graph--&gt;&gt;Watcher: module invalidated\n    Watcher-&gt;&gt;Browser: hot.send({ type: &#39;full-reload&#39; })\n    Browser-&gt;&gt;Vite: GET /blog (new request)\n    Vite-&gt;&gt;Plugin: load(&#39;\\0virtual:open-blog-data&#39;)\n    Note over Plugin: load() re-reads all .md files\n    Plugin--&gt;&gt;Vite: fresh data exports\n    Vite--&gt;&gt;Browser: HTML with updated content\n</code></pre>\n<hr />\n<h3>Phase 4: @openelement/i18n 同模式改造</h3>\n<p><strong>目标</strong>：将 i18n 的模块级状态模式改为与 content 相同的虚拟数据模块模式。</p>\n<p><strong>变更文件</strong>：</p>\n<table>\n<thead>\n<tr>\n<th>文件</th>\n<th>变更</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>packages/i18n/src/i18n-data.ts</code></td>\n<td>删除 <code>_options</code>/<code>initI18nData()</code>/<code>getI18nLocales()</code>/<code>getI18nOptions()</code>/<code>getDefaultLocale()</code>，替换为 <code>loadI18nData()</code> 纯函数</td>\n</tr>\n<tr>\n<td><code>packages/i18n/src/index.ts</code></td>\n<td>导出 <code>loadI18nData()</code>，<code>buildStart()</code> 改用纯函数 + ctx</td>\n</tr>\n<tr>\n<td><code>packages/adapter-vite/src/virtual-data.ts</code></td>\n<td>新增 <code>virtual:less-i18n-data</code> 虚拟模块</td>\n</tr>\n</tbody></table>\n<p><strong>i18n 数据量小</strong>（只是 locale 配置），虚拟模块的序列化成本可以忽略。实现模式与 blog 完全相同。</p>\n<p><strong>新增虚拟模块</strong>：</p>\n<pre><code class=\"language-ts\">// virtual:less-i18n-data\nconst VIRTUAL_I18N_DATA_ID = &#39;virtual:less-i18n-data&#39;;\nconst RESOLVED_I18N_DATA_ID = &#39;\\0&#39; + VIRTUAL_I18N_DATA_ID;\n\nexport function createI18nDataPlugin(ctx: LessBuildContext): Plugin {\n  return {\n    name: &#39;less:i18n-data&#39;,\n    enforce: &#39;pre&#39;,\n\n    resolveId(id) {\n      if (id === VIRTUAL_I18N_DATA_ID) return RESOLVED_I18N_DATA_ID;\n    },\n\n    load(id) {\n      if (id !== RESOLVED_I18N_DATA_ID) return;\n\n      const i18nOpts = ctx.i18nOptions;\n      if (!i18nOpts) {\n        return [\n          &#39;export const locales = [];&#39;,\n          &#39;export function getDefaultLocale() { return &quot;en&quot;; }&#39;,\n          &#39;export function getI18nOptions() { return null; }&#39;,\n        ].join(&#39;\\n&#39;);\n      }\n\n      return [\n        `const _options = ${JSON.stringify(i18nOpts)};`,\n        &#39;export const locales = _options.locales;&#39;,\n        &#39;export function getDefaultLocale() { return _options.defaultLocale || &quot;en&quot;; }&#39;,\n        &#39;export function getI18nOptions() { return _options; }&#39;,\n      ].join(&#39;\\n&#39;);\n    },\n  };\n}\n</code></pre>\n<p><strong>TypeScript 类型声明</strong>：</p>\n<pre><code class=\"language-ts\">declare module &#39;virtual:less-i18n-data&#39; {\n  import type { LessI18nOptions } from &#39;@openelement/i18n&#39;;\n  export const locales: string[];\n  export function getDefaultLocale(): string;\n  export function getI18nOptions(): LessI18nOptions | null;\n}\n</code></pre>\n<p><strong>验证</strong>：</p>\n<pre><code class=\"language-bash\"># 使用 i18n 的项目（如有）\ndeno task dev\n# 访问页面 → locale 切换正常\n# 无 i18n 的项目 → 编译不报错（虚拟模块返回空数据）\n</code></pre>\n<hr />\n<h3>Phase 5: 静态内容页</h3>\n<p><strong>目标</strong>：<code>lessContent()</code> 增加 <code>pages</code> 选项，框架自动解析 markdown 内容页，通过虚拟模块提供数据。消除用户代码中的 <code>node:fs</code> + 手写 md 转换器。</p>\n<p><strong>变更文件</strong>：</p>\n<table>\n<thead>\n<tr>\n<th>文件</th>\n<th>变更</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>packages/content/src/types.ts</code></td>\n<td>新增 <code>LessPagesOptions</code> 类型</td>\n</tr>\n<tr>\n<td><code>packages/content/src/pages/page-loader.ts</code></td>\n<td><strong>新建</strong> — 静态页加载器</td>\n</tr>\n<tr>\n<td><code>packages/content/src/index.ts</code></td>\n<td><code>lessContent()</code> 新增 <code>pages</code> 选项处理</td>\n</tr>\n<tr>\n<td><code>packages/adapter-vite/src/virtual-data.ts</code></td>\n<td>新增 <code>virtual:less-page-data</code> 虚拟模块</td>\n</tr>\n</tbody></table>\n<p><strong>接口设计</strong>：</p>\n<pre><code class=\"language-ts\">// types.ts\nexport interface LessPagesOptions {\n  /** Map of page key → content file path (relative to project root) */\n  [key: string]: string;\n  // e.g., { about: &#39;content/about/index.md&#39;, credits: &#39;content/credits.md&#39; }\n}\n\nexport interface LessContentOptions {\n  blog?: LessBlogOptions | false;\n  nav?: LessNavOptions | false;\n  sitemap?: SitemapOptions | false;\n  pages?: LessPagesOptions;  // NEW\n  ctx?: LessBuildContext;\n}\n</code></pre>\n<p><strong>虚拟模块导出</strong>：</p>\n<pre><code class=\"language-ts\">// virtual:less-page-data\n// load() generates:\nconst _pages = {\n  about: { title: &quot;About&quot;, html: &quot;&lt;p&gt;...&lt;/p&gt;&quot;, frontmatter: {...} },\n  credits: { title: &quot;Credits&quot;, html: &quot;&lt;p&gt;...&lt;/p&gt;&quot;, frontmatter: {...} },\n};\nexport function getPage(key) { return _pages[key]; }\nexport const pageKeys = Object.keys(_pages);\n</code></pre>\n<p><strong>路由组件使用</strong>：</p>\n<pre><code class=\"language-ts\">// app/routes/about.ts\nimport { getPage } from &#39;virtual:less-page-data&#39;;\n\nexport default class AboutPage extends LitElement {\n  connectedCallback() {\n    super.connectedCallback();\n    const page = getPage(&#39;about&#39;);\n    this.innerHTML = page.html;\n  }\n}\n</code></pre>\n<p><strong>验证</strong>：</p>\n<pre><code class=\"language-bash\"># my-tech-blog About 页面\n# 1. 创建 content/about/index.md\n# 2. 配置 lessContent({ pages: { about: &#39;content/about/index.md&#39; } })\n# 3. 路由组件 import { getPage } from &#39;virtual:less-page-data&#39;\n# 4. 访问 /about → 渲染 markdown 内容\n# 5. 无 node:fs 依赖\n</code></pre>\n<hr />\n<h2>清理清单</h2>\n<p>所有 Phase 完成后，需要清理的遗留代码：</p>\n<table>\n<thead>\n<tr>\n<th>项</th>\n<th>位置</th>\n<th>操作</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>EntryDescriptor.blog</code></td>\n<td><code>entry-descriptor.ts</code></td>\n<td>删除</td>\n</tr>\n<tr>\n<td><code>blog</code> 参数</td>\n<td><code>buildEntryDescriptor()</code></td>\n<td>删除</td>\n</tr>\n<tr>\n<td><code>blog</code> 参数</td>\n<td><code>generateHonoEntryCode()</code></td>\n<td>删除</td>\n</tr>\n<tr>\n<td><code>blog</code> 传递</td>\n<td><code>index.ts</code> generateEntry()</td>\n<td>删除 <code>blog: ctx.blogOptions</code></td>\n</tr>\n<tr>\n<td>initBlogData 注入</td>\n<td><code>entry-renderer.ts</code> 306-313 行</td>\n<td>删除</td>\n</tr>\n<tr>\n<td><code>_posts</code> 模块状态</td>\n<td><code>blog-data.ts</code></td>\n<td>删除</td>\n</tr>\n<tr>\n<td><code>initBlogData()</code> / <code>getPosts()</code> / <code>getPostBySlug()</code> / <code>getBlogOptions()</code></td>\n<td><code>blog-data.ts</code> + <code>index.ts</code> re-exports</td>\n<td>删除</td>\n</tr>\n<tr>\n<td><code>initBlogData</code> re-export</td>\n<td>SSG bundle</td>\n<td>改为 <code>virtual:less-blog-data</code> export</td>\n</tr>\n<tr>\n<td><code>buildCoreSubpathAliases()</code></td>\n<td><code>adapter-vite</code></td>\n<td>删除整个函数</td>\n</tr>\n<tr>\n<td>17 行 resolve.alias</td>\n<td><code>my-tech-blog/vite.config.ts</code></td>\n<td>删除</td>\n</tr>\n</tbody></table>\n<hr />\n<h2>Consequences</h2>\n<h3>Positive</h3>\n<ul>\n<li><strong>单路径</strong>：dev/build 走同一条路（虚拟模块 <code>load()</code>），不存在双初始化路径</li>\n<li><strong>零桥</strong>：没有 <code>onSSRInit</code>，没有 Data Bridge，没有 ad-hoc 代码注入</li>\n<li><strong>零状态</strong>：content/i18n 不持有模块级状态，不存在&quot;状态在错误的模块空间&quot;的问题</li>\n<li><strong>HMR 天然支持</strong>：<code>invalidateModule</code> → <code>load()</code> 重新执行 → 新数据，无 flag 重置</li>\n<li><strong>用户 vite.config.ts 清零</strong>：<code>@deno/vite-plugin</code> 消除所有 resolve.alias</li>\n<li><strong><code>EntryDescriptor</code> 纯净化</strong>：只描述 Hono entry 结构，不承载插件初始化逻辑</li>\n<li><strong><code>entry-renderer.ts</code> 纯净化</strong>：恢复为纯函数（描述符 → 字符串）</li>\n<li><strong>可扩展</strong>：未来新插件只需 <code>loadBlogData()</code> 纯函数 + 虚拟模块，零框架改动</li>\n</ul>\n<h3>Negative</h3>\n<ul>\n<li><strong>大数据集序列化</strong>：100 篇博文含 HTML body 时，<code>JSON.stringify(posts)</code> 可能达到 MB 级。但 SSG build 模式同样需要处理这个量级（bundle 大小），且 dev 模式每次请求重新生成，不会有内存累积</li>\n<li><strong>虚拟模块 ID 不是标准路径</strong>：IDE 可能不识别 <code>virtual:less-blog-data</code> 的 import，需要 TypeScript <code>.d.ts</code> 声明文件</li>\n<li><strong>迁移成本</strong>：路由组件需要将 <code>import { getPosts } from &#39;@openelement/content&#39;</code> 改为 <code>import { posts } from &#39;virtual:less-blog-data&#39;</code></li>\n</ul>\n<h3>Neutral</h3>\n<ul>\n<li>虚拟模块模式与 Astro 的 <code>import.meta.glob()</code> / collection 模式类似，开发者认知成本低</li>\n<li><code>initBlogData()</code>/<code>getPosts()</code>/<code>buildCoreSubpathAliases()</code> 直接删除，不向后兼容 — v0.12.0 是 breaking change</li>\n</ul>\n<hr />\n<h2>参考</h2>\n<ul>\n<li><a href=\"/blog/0011-eliminate-last-globalthis-via-closebundle\" rel=\"noopener noreferrer\">ADR 0011: 消除最后一个 globalThis 桥接</a> — 类似的跨模块空间数据传递问题</li>\n<li><a href=\"/blog/0016-dual-mode-subpath-resolution\" rel=\"noopener noreferrer\">ADR 0016: 双模式子路径解析</a> — coreResolvePlugin 的当前实现</li>\n<li><a href=\"/blog/0017-separate-runtime-from-build-orchestration\" rel=\"noopener noreferrer\">ADR 0017: 框架运行时与构建编排解耦</a> — core/adapter-vite 分离</li>\n<li><a href=\"https://vite.dev/guide/ssr\" rel=\"noopener noreferrer\">Vite SSR</a> — SSR runner 的模块隔离机制</li>\n<li><a href=\"https://github.com/denoland/deno/tree/main/packages/vite\" rel=\"noopener noreferrer\">@deno/vite-plugin</a> — Deno import map 集成</li>\n<li><a href=\"https://docs.astro.build/en/guides/content-collections/\" rel=\"noopener noreferrer\">Astro Content Collections</a> — 类似的虚拟模块数据模式</li>\n</ul>\n<hr />\n<p><em>提出日期: 2026-05-11 | 状态: ACCEPTED | 目标版本: v0.12.0</em></p>\n"
  },
  {
    "slug": "0017-separate-runtime-from-build-orchestration",
    "frontmatter": {
      "title": "ADR 0017: 框架运行时与构建编排解耦 — 拆分 @openelement/adapter-vite",
      "date": "2026-05-11",
      "draft": false,
      "tags": [
        "architecture",
        "decision"
      ],
      "type": "adr",
      "lang": "zh"
    },
    "content": "\n## Status\n\n**ACCEPTED** — v0.11.0 架构重构\n\n## Context\n\n### 问题本质\n\nBug #11（`npm:` specifier 无法解析）暴露了一个更深层的架构问题：`@openelement/core` 同时承担了两个职责——\n\n1. **框架运行时**：`renderDSD()`、`island()`、`escapeHtml()`、`adapterRegistry` 等纯逻辑\n2. **构建编排**：Vite 插件集合、路由扫描、HMR、SSG 三阶段流水线\n\n当 Vite 的 SSR runner 需要加载 `@openelement/core` 的源码时，它实际上只需要运行时部分。但因为是同一个包，Vite 不得不加载整个包——包括 Vite 插件 API 本身。这导致了一系列连锁问题：\n\n```\n@openelement/core 是一个 Vite Plugin\n    ↓\nJSR 发布后源码含 npm: specifier (Deno 生态语义)\n    ↓\nVite SSR runner 不认识 npm: 协议\n    ↓\n需要 5 个补丁翻译 specifier + 路由解析\n```\n\n### 补丁清单（截至 v0.10.7）\n\n| # | 补丁                          | 存在原因                                | 如果 core 不含 Vite 插件 |\n| - | ----------------------------- | --------------------------------------- | ------------------------ |\n| 1 | `less:core-resolve` resolveId | Vite 不认识 JSR 包的 `@openelement/core`     | 不需要                   |\n| 2 | `load()` 从 JSR fetch 源码    | Vite SSR runner 不认识 `https://` URL   | 不需要                   |\n| 3 | esbuild 编译 TS→JS            | 虚拟模块绕过了 Vite 的 transform 管线   | 不需要                   |\n| 4 | npm: → bare 正则替换          | Vite 不认识 Deno 的 `npm:` specifier    | 不需要                   |\n| 5 | resolveId Case 3 委托解析     | 虚拟模块无真实路径，找不到 node_modules | 不需要                   |\n\n**如果 `@openelement/core` 只包含纯 Web Standard 运行时代码，这 5 个补丁全部不需要。** 因为运行时代码不会被 Vite 的 SSR runner 当作\"源码\"加载——它就是一段普通 ESM，Deno / Node / Bun / Edge 都能直接 `import`。\n\n### 与已有架构模式的一致性\n\nLessJS 已经有 `adapter-lit` 的先例：core 定义 `RenderAdapter` 接口，`adapter-lit` 实现 `LitRenderer`。构建编排层做同样的事情是自然的延伸——core 定义 `BuildAdapter` 接口，`adapter-vite` 实现 Vite 构建编排。\n\n### 与 Fresh 的对比\n\n| 维度       | Fresh       | LessJS（现状）      | LessJS（解耦后）         |\n| ---------- | ----------- | ------------------- | ------------------------ |\n| 运行时     | 纯 Deno     | 混在 Vite 插件里    | 纯 Web Std ESM           |\n| 构建编排   | Deno native | Vite（绑定）        | 可插拔 adapter           |\n| npm: 问题  | 不存在      | 需要 5 个补丁       | 不存在                   |\n| 运行时兼容 | Deno only   | Deno + Node（勉强） | Deno / Node / Bun / Edge |\n\n## Decision\n\n### 方案：将 core 拆为运行时 + 构建适配器\n\n```\n@openelement/core          纯运行时，零 node:*，零 npm:，零 Vite 依赖\n                      renderDSD / island / escape / adapter-registry / ...\n                      标准 Web API：URL, fetch, import.meta.url, crypto\n                      任何 ESM 运行时都能直接 import\n\n@openelement/adapter-vite  Vite 构建编排适配器\n                      less() → Plugin[]，路由扫描，HMR，SSG 三阶段\n                      coreResolvePlugin（npm: 翻译，虚拟模块解析）\n                      所有 node:* 和 Vite API 依赖都在这里\n```\n\n### 依赖图变化\n\n```\nBefore:\n  用户项目 → @openelement/app → @openelement/core (运行时 + Vite 编排混在一起)\n                                       → @openelement/content\n                                       → @openelement/i18n\n\nAfter:\n  用户项目 → @openelement/app → @openelement/core (纯运行时)\n                          → @openelement/adapter-vite (构建编排)\n                          → @openelement/content\n                          → @openelement/i18n\n```\n\n### 代码拆分边界\n\n#### @openelement/core（保留，纯化）\n\n```\npackages/core/src/\n  ├── render-dsd.ts          ← 纯字符串拼接\n  ├── render-nested.ts       ← 纯字符串 + parse5\n  ├── html-escape.ts         ← 纯字符串处理\n  ├── island.ts              ← 纯逻辑\n  ├── adapter-registry.ts    ← 纯 Map\n  ├── navigation.ts          ← 纯 URL + popstate\n  ├── logger.ts              ← 纯 console\n  ├── errors.ts              ← 纯 Error 类\n  ├── context.ts             ← 纯逻辑\n  ├── types.ts               ← 纯类型定义\n  └── index.ts               ← 导出汇总，零 Vite 依赖\n```\n\n- **零** `node:*` import（`node:path`, `node:process`, `node:url` 全移走）\n- **零** `import type { Plugin } from 'vite'`\n- **零** `npm:` / `deno:` 依赖\n- `parse5` 是唯一的外部依赖（纯 JS HTML parser，所有运行时都支持）\n\n#### @openelement/adapter-vite（新包）\n\n```\npackages/adapter-vite/src/\n  ├── index.ts               ← less() 入口，返回 Plugin[]\n  ├── build.ts               ← SSG 三阶段流水线\n  ├── build-context.ts       ← LessBuildContext（构建元数据）\n  ├── build-manifest.ts      ← 构建产物扫描\n  ├── hono-entry.ts          ← 虚拟入口代码生成\n  ├── island-transform.ts    ← island 标记转换\n  ├── island-manifest.ts     ← island 清单生成\n  ├── route-scanner.ts       ← 文件系统路由扫描\n  ├── ssg-postprocess.ts     ← SSG 后处理\n  └── core-resolve.ts        ← npm: 翻译 + 虚拟模块解析（原 5 个补丁）\n```\n\n- 所有 `node:*` import 在这里\n- Vite Plugin API 在这里\n- `coreResolvePlugin` 在这里（只处理用户项目代码的 specifier，不再需要处理框架自身的）\n\n### BuildAdapter 接口（可选）\n\n如果未来要支持非 Vite 构建工具（Deno native build、esbuild），core 可以定义一个最小接口：\n\n```ts\n// @openelement/core — 可选，按需引入\nexport interface BuildAdapter {\n  name: string;\n  build(options: BuildOptions): Promise<BuildResult>;\n  dev(options: DevOptions): Promise<DevServer>;\n}\n```\n\n但这是**远期目标**，不是这次拆分必须做的。第一次拆分只需要把 Vite 代码搬出 core，不改变用户 API。\n\n### 用户迁移\n\n```ts\n// Before\nimport { less } from '@openelement/core';\nimport { lessjs } from '@openelement/app';\n\n// After（方案 A：最小变化）\nimport { less } from '@openelement/adapter-vite'; // 构建编排\nimport { island, renderDSD } from '@openelement/core'; // 运行时（不变）\nimport { lessjs } from '@openelement/app'; // 不变\n\n// After（方案 B：app 内部重导出，用户零改动）\nimport { lessjs } from '@openelement/app'; // app 内部改为 import adapter-vite\n```\n\n**推荐方案 B**：`@openelement/app` 内部重导出 `less()`，用户代码零改动。只有直接 `import { less } from '@openelement/core'` 的用户需要改路径。\n\n### 包数量变化\n\n9 个包 → 10 个包。新增 `@openelement/adapter-vite`。\n\n发布顺序更新：\n\n```\nrpc → ui → adapter-lit → signal → content → i18n → core → adapter-vite → app → create\n```\n\n## 替代方案\n\n### A. 不拆，继续打补丁\n\n现状维持。每次遇到新的 Deno/Vite 语义冲突就加补丁。\n\n- 优点：零改动，零迁移成本\n- 缺点：补丁会持续增长（Bug #11 是第 6 次复发），core 越来越难维护，运行时被锁在 Vite 生态\n\n### B. 拆分（本 ADR 提案）\n\n- 优点：5 个补丁不再需要碰到框架本身，core 纯净可移植，未来可支持非 Vite 构建工具\n- 缺点：多一个包，用户 import 路径可能变化，需要一次性的拆分工作\n\n### C. 彻底脱离 Vite，Deno native 全栈（Fresh 模式）\n\n- 优点：架构最干净，零补丁，与 Deno 生态零摩擦\n- 缺点：放弃 Vite HMR + 插件生态，目标用户缩小到 Deno 开发者，工作量巨大\n\n## Consequences\n\n### Positive\n\n- **5 个补丁自然失效**：core 不含 Vite 插件，不需要被 SSR runner 当源码加载，specifier 翻译问题消失\n- **core 可移植**：纯 Web Std ESM，Deno / Node 18+ / Bun / Cloudflare Workers / Vercel Edge 都能直接 import\n- **架构与 adapter-lit 一致**：渲染有 `RenderAdapter`，构建有 `BuildAdapter`，模式统一\n- **未来可扩展**：`@openelement/adapter-deno`、`@openelement/adapter-esbuild` 可按需实现\n- **职责清晰**：core 只关心\"怎么渲染\"，adapter 只关心\"怎么构建\"\n\n### Negative\n\n- **多一个包**：9 → 10，发布流程多一步\n- **迁移成本**：直接 `import { less } from '@openelement/core'` 的用户需改为 `@openelement/adapter-vite`\n- **core-resolve 补丁仍在 adapter-vite 中**：但管辖范围缩小——只处理用户项目代码的 specifier，不处理框架自身\n- **parse5 仍是外部依赖**：core 的 render-nested.ts 依赖 parse5，这是纯 JS 库但仍是 npm 包。如果追求零 npm 依赖，需要自行实现一个最小 HTML parser 或将 render-nested 也拆出\n\n### Neutral\n\n- `@openelement/app` 可以重导出 `less()`，让大多数用户无感迁移\n- `BuildAdapter` 接口是可选的远期目标，不影响本次拆分\n- 包名 `adapter-vite` 遵循已有的 `adapter-lit` 命名惯例\n\n## 参考\n\n- [ADR 0016: 双模式子路径解析](/blog/0016-dual-mode-subpath-resolution) — 当前补丁方案的完整描述\n- [ADR 0012: 拆分 @openelement/app](/blog/0012-extract-lessjs-umbrella-to-app-package) — 类似的\"拆分以消除依赖倒挂\"决策\n- [Fresh 2.0](https://github.com/denoland/fresh) — Deno native 构建框架的参考实现\n\n---\n\n_提出日期: 2026-05-11 | 状态: ACCEPTED | 实施版本: v0.11.0_\n",
    "html": "<h2>Status</h2>\n<p><strong>ACCEPTED</strong> — v0.11.0 架构重构</p>\n<h2>Context</h2>\n<h3>问题本质</h3>\n<p>Bug #11（<code>npm:</code> specifier 无法解析）暴露了一个更深层的架构问题：<code>@openelement/core</code> 同时承担了两个职责——</p>\n<ol>\n<li><strong>框架运行时</strong>：<code>renderDSD()</code>、<code>island()</code>、<code>escapeHtml()</code>、<code>adapterRegistry</code> 等纯逻辑</li>\n<li><strong>构建编排</strong>：Vite 插件集合、路由扫描、HMR、SSG 三阶段流水线</li>\n</ol>\n<p>当 Vite 的 SSR runner 需要加载 <code>@openelement/core</code> 的源码时，它实际上只需要运行时部分。但因为是同一个包，Vite 不得不加载整个包——包括 Vite 插件 API 本身。这导致了一系列连锁问题：</p>\n<pre><code>@openelement/core 是一个 Vite Plugin\n    ↓\nJSR 发布后源码含 npm: specifier (Deno 生态语义)\n    ↓\nVite SSR runner 不认识 npm: 协议\n    ↓\n需要 5 个补丁翻译 specifier + 路由解析\n</code></pre>\n<h3>补丁清单（截至 v0.10.7）</h3>\n<table>\n<thead>\n<tr>\n<th>#</th>\n<th>补丁</th>\n<th>存在原因</th>\n<th>如果 core 不含 Vite 插件</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>1</td>\n<td><code>less:core-resolve</code> resolveId</td>\n<td>Vite 不认识 JSR 包的 <code>@openelement/core</code></td>\n<td>不需要</td>\n</tr>\n<tr>\n<td>2</td>\n<td><code>load()</code> 从 JSR fetch 源码</td>\n<td>Vite SSR runner 不认识 <code>https://</code> URL</td>\n<td>不需要</td>\n</tr>\n<tr>\n<td>3</td>\n<td>esbuild 编译 TS→JS</td>\n<td>虚拟模块绕过了 Vite 的 transform 管线</td>\n<td>不需要</td>\n</tr>\n<tr>\n<td>4</td>\n<td>npm: → bare 正则替换</td>\n<td>Vite 不认识 Deno 的 <code>npm:</code> specifier</td>\n<td>不需要</td>\n</tr>\n<tr>\n<td>5</td>\n<td>resolveId Case 3 委托解析</td>\n<td>虚拟模块无真实路径，找不到 node_modules</td>\n<td>不需要</td>\n</tr>\n</tbody></table>\n<p><strong>如果 <code>@openelement/core</code> 只包含纯 Web Standard 运行时代码，这 5 个补丁全部不需要。</strong> 因为运行时代码不会被 Vite 的 SSR runner 当作&quot;源码&quot;加载——它就是一段普通 ESM，Deno / Node / Bun / Edge 都能直接 <code>import</code>。</p>\n<h3>与已有架构模式的一致性</h3>\n<p>LessJS 已经有 <code>adapter-lit</code> 的先例：core 定义 <code>RenderAdapter</code> 接口，<code>adapter-lit</code> 实现 <code>LitRenderer</code>。构建编排层做同样的事情是自然的延伸——core 定义 <code>BuildAdapter</code> 接口，<code>adapter-vite</code> 实现 Vite 构建编排。</p>\n<h3>与 Fresh 的对比</h3>\n<table>\n<thead>\n<tr>\n<th>维度</th>\n<th>Fresh</th>\n<th>LessJS（现状）</th>\n<th>LessJS（解耦后）</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>运行时</td>\n<td>纯 Deno</td>\n<td>混在 Vite 插件里</td>\n<td>纯 Web Std ESM</td>\n</tr>\n<tr>\n<td>构建编排</td>\n<td>Deno native</td>\n<td>Vite（绑定）</td>\n<td>可插拔 adapter</td>\n</tr>\n<tr>\n<td>npm: 问题</td>\n<td>不存在</td>\n<td>需要 5 个补丁</td>\n<td>不存在</td>\n</tr>\n<tr>\n<td>运行时兼容</td>\n<td>Deno only</td>\n<td>Deno + Node（勉强）</td>\n<td>Deno / Node / Bun / Edge</td>\n</tr>\n</tbody></table>\n<h2>Decision</h2>\n<h3>方案：将 core 拆为运行时 + 构建适配器</h3>\n<pre><code>@openelement/core          纯运行时，零 node:*，零 npm:，零 Vite 依赖\n                      renderDSD / island / escape / adapter-registry / ...\n                      标准 Web API：URL, fetch, import.meta.url, crypto\n                      任何 ESM 运行时都能直接 import\n\n@openelement/adapter-vite  Vite 构建编排适配器\n                      less() → Plugin[]，路由扫描，HMR，SSG 三阶段\n                      coreResolvePlugin（npm: 翻译，虚拟模块解析）\n                      所有 node:* 和 Vite API 依赖都在这里\n</code></pre>\n<h3>依赖图变化</h3>\n<pre><code>Before:\n  用户项目 → @openelement/app → @openelement/core (运行时 + Vite 编排混在一起)\n                                       → @openelement/content\n                                       → @openelement/i18n\n\nAfter:\n  用户项目 → @openelement/app → @openelement/core (纯运行时)\n                          → @openelement/adapter-vite (构建编排)\n                          → @openelement/content\n                          → @openelement/i18n\n</code></pre>\n<h3>代码拆分边界</h3>\n<h4>@openelement/core（保留，纯化）</h4>\n<pre><code>packages/core/src/\n  ├── render-dsd.ts          ← 纯字符串拼接\n  ├── render-nested.ts       ← 纯字符串 + parse5\n  ├── html-escape.ts         ← 纯字符串处理\n  ├── island.ts              ← 纯逻辑\n  ├── adapter-registry.ts    ← 纯 Map\n  ├── navigation.ts          ← 纯 URL + popstate\n  ├── logger.ts              ← 纯 console\n  ├── errors.ts              ← 纯 Error 类\n  ├── context.ts             ← 纯逻辑\n  ├── types.ts               ← 纯类型定义\n  └── index.ts               ← 导出汇总，零 Vite 依赖\n</code></pre>\n<ul>\n<li><strong>零</strong> <code>node:*</code> import（<code>node:path</code>, <code>node:process</code>, <code>node:url</code> 全移走）</li>\n<li><strong>零</strong> <code>import type { Plugin } from &#39;vite&#39;</code></li>\n<li><strong>零</strong> <code>npm:</code> / <code>deno:</code> 依赖</li>\n<li><code>parse5</code> 是唯一的外部依赖（纯 JS HTML parser，所有运行时都支持）</li>\n</ul>\n<h4>@openelement/adapter-vite（新包）</h4>\n<pre><code>packages/adapter-vite/src/\n  ├── index.ts               ← less() 入口，返回 Plugin[]\n  ├── build.ts               ← SSG 三阶段流水线\n  ├── build-context.ts       ← LessBuildContext（构建元数据）\n  ├── build-manifest.ts      ← 构建产物扫描\n  ├── hono-entry.ts          ← 虚拟入口代码生成\n  ├── island-transform.ts    ← island 标记转换\n  ├── island-manifest.ts     ← island 清单生成\n  ├── route-scanner.ts       ← 文件系统路由扫描\n  ├── ssg-postprocess.ts     ← SSG 后处理\n  └── core-resolve.ts        ← npm: 翻译 + 虚拟模块解析（原 5 个补丁）\n</code></pre>\n<ul>\n<li>所有 <code>node:*</code> import 在这里</li>\n<li>Vite Plugin API 在这里</li>\n<li><code>coreResolvePlugin</code> 在这里（只处理用户项目代码的 specifier，不再需要处理框架自身的）</li>\n</ul>\n<h3>BuildAdapter 接口（可选）</h3>\n<p>如果未来要支持非 Vite 构建工具（Deno native build、esbuild），core 可以定义一个最小接口：</p>\n<pre><code class=\"language-ts\">// @openelement/core — 可选，按需引入\nexport interface BuildAdapter {\n  name: string;\n  build(options: BuildOptions): Promise&lt;BuildResult&gt;;\n  dev(options: DevOptions): Promise&lt;DevServer&gt;;\n}\n</code></pre>\n<p>但这是<strong>远期目标</strong>，不是这次拆分必须做的。第一次拆分只需要把 Vite 代码搬出 core，不改变用户 API。</p>\n<h3>用户迁移</h3>\n<pre><code class=\"language-ts\">// Before\nimport { less } from &#39;@openelement/core&#39;;\nimport { lessjs } from &#39;@openelement/app&#39;;\n\n// After（方案 A：最小变化）\nimport { less } from &#39;@openelement/adapter-vite&#39;; // 构建编排\nimport { island, renderDSD } from &#39;@openelement/core&#39;; // 运行时（不变）\nimport { lessjs } from &#39;@openelement/app&#39;; // 不变\n\n// After（方案 B：app 内部重导出，用户零改动）\nimport { lessjs } from &#39;@openelement/app&#39;; // app 内部改为 import adapter-vite\n</code></pre>\n<p><strong>推荐方案 B</strong>：<code>@openelement/app</code> 内部重导出 <code>less()</code>，用户代码零改动。只有直接 <code>import { less } from &#39;@openelement/core&#39;</code> 的用户需要改路径。</p>\n<h3>包数量变化</h3>\n<p>9 个包 → 10 个包。新增 <code>@openelement/adapter-vite</code>。</p>\n<p>发布顺序更新：</p>\n<pre><code>rpc → ui → adapter-lit → signal → content → i18n → core → adapter-vite → app → create\n</code></pre>\n<h2>替代方案</h2>\n<h3>A. 不拆，继续打补丁</h3>\n<p>现状维持。每次遇到新的 Deno/Vite 语义冲突就加补丁。</p>\n<ul>\n<li>优点：零改动，零迁移成本</li>\n<li>缺点：补丁会持续增长（Bug #11 是第 6 次复发），core 越来越难维护，运行时被锁在 Vite 生态</li>\n</ul>\n<h3>B. 拆分（本 ADR 提案）</h3>\n<ul>\n<li>优点：5 个补丁不再需要碰到框架本身，core 纯净可移植，未来可支持非 Vite 构建工具</li>\n<li>缺点：多一个包，用户 import 路径可能变化，需要一次性的拆分工作</li>\n</ul>\n<h3>C. 彻底脱离 Vite，Deno native 全栈（Fresh 模式）</h3>\n<ul>\n<li>优点：架构最干净，零补丁，与 Deno 生态零摩擦</li>\n<li>缺点：放弃 Vite HMR + 插件生态，目标用户缩小到 Deno 开发者，工作量巨大</li>\n</ul>\n<h2>Consequences</h2>\n<h3>Positive</h3>\n<ul>\n<li><strong>5 个补丁自然失效</strong>：core 不含 Vite 插件，不需要被 SSR runner 当源码加载，specifier 翻译问题消失</li>\n<li><strong>core 可移植</strong>：纯 Web Std ESM，Deno / Node 18+ / Bun / Cloudflare Workers / Vercel Edge 都能直接 import</li>\n<li><strong>架构与 adapter-lit 一致</strong>：渲染有 <code>RenderAdapter</code>，构建有 <code>BuildAdapter</code>，模式统一</li>\n<li><strong>未来可扩展</strong>：<code>@openelement/adapter-deno</code>、<code>@openelement/adapter-esbuild</code> 可按需实现</li>\n<li><strong>职责清晰</strong>：core 只关心&quot;怎么渲染&quot;，adapter 只关心&quot;怎么构建&quot;</li>\n</ul>\n<h3>Negative</h3>\n<ul>\n<li><strong>多一个包</strong>：9 → 10，发布流程多一步</li>\n<li><strong>迁移成本</strong>：直接 <code>import { less } from &#39;@openelement/core&#39;</code> 的用户需改为 <code>@openelement/adapter-vite</code></li>\n<li><strong>core-resolve 补丁仍在 adapter-vite 中</strong>：但管辖范围缩小——只处理用户项目代码的 specifier，不处理框架自身</li>\n<li><strong>parse5 仍是外部依赖</strong>：core 的 render-nested.ts 依赖 parse5，这是纯 JS 库但仍是 npm 包。如果追求零 npm 依赖，需要自行实现一个最小 HTML parser 或将 render-nested 也拆出</li>\n</ul>\n<h3>Neutral</h3>\n<ul>\n<li><code>@openelement/app</code> 可以重导出 <code>less()</code>，让大多数用户无感迁移</li>\n<li><code>BuildAdapter</code> 接口是可选的远期目标，不影响本次拆分</li>\n<li>包名 <code>adapter-vite</code> 遵循已有的 <code>adapter-lit</code> 命名惯例</li>\n</ul>\n<h2>参考</h2>\n<ul>\n<li><a href=\"/blog/0016-dual-mode-subpath-resolution\" rel=\"noopener noreferrer\">ADR 0016: 双模式子路径解析</a> — 当前补丁方案的完整描述</li>\n<li><a href=\"/blog/0012-extract-lessjs-umbrella-to-app-package\" rel=\"noopener noreferrer\">ADR 0012: 拆分 @openelement/app</a> — 类似的&quot;拆分以消除依赖倒挂&quot;决策</li>\n<li><a href=\"https://github.com/denoland/fresh\" rel=\"noopener noreferrer\">Fresh 2.0</a> — Deno native 构建框架的参考实现</li>\n</ul>\n<hr />\n<p><em>提出日期: 2026-05-11 | 状态: ACCEPTED | 实施版本: v0.11.0</em></p>\n"
  },
  {
    "slug": "0016-dual-mode-subpath-resolution",
    "frontmatter": {
      "title": "ADR 0016: 双模式子路径解析 — resolve.alias 本地 + resolveId/load 虚拟模块远程",
      "date": "2026-05-11",
      "draft": false,
      "tags": [
        "architecture",
        "decision"
      ],
      "type": "adr",
      "lang": "zh"
    },
    "content": "\n## Status\n\n**ACCEPTED** — v0.10.3 Bug #9 修复\n\n## Context\n\n### 问题描述\n\nLessJS 的虚拟入口模块 `virtual:less-hono-entry` 会从 `@openelement/core` 的子路径导入模块（如 `@openelement/core/ssr-handler`、`@openelement/core/logger`）。当用户通过 JSR 使用 LessJS 时（即 `import.meta.url` 为 `https://jsr.io/...`），Vite 的 SSR runner 无法解析这些裸标识符（bare specifier），原因如下：\n\n1. **JSR 包不在 `node_modules/` 中**：Deno 使用内容寻址缓存（content-addressable cache），文件名是不透明的哈希值，无法从 `https://` URL 反推本地文件路径\n2. **Deno 的 import map（`deno.json`）不被 Vite SSR runner 使用**：Vite 的 SSR runner 底层是 Node.js ESM loader\n3. **Node.js ESM loader 仅支持 `file://` 和 `data:` URL scheme**：不支持 `https://` scheme\n\n### Bug 历史\n\n此问题已反复出现 4 次：\n\n| 提交      | 时间    | 尝试方案                                             | 失败原因                                                                    |\n| --------- | ------- | ---------------------------------------------------- | --------------------------------------------------------------------------- |\n| `b6a6b41` | v0.9.x  | 手动添加 Vite alias（硬编码路径）                    | 新增子路径时遗漏，DX 差                                                     |\n| `f223bef` | v0.9.x  | 扩展 alias 覆盖面                                    | 仍有遗漏 + 路径不对                                                         |\n| `6c5a992` | v0.9.x  | 补充缺失的 alias                                     | 同上，仍然不完整                                                            |\n| `70d3704` | v0.10.2 | `resolve.alias` 使用 `https://` URL 作为 replacement | `ERR_UNSUPPORTED_ESM_URL_SCHEME`：Node.js ESM loader 拒绝 `https://` scheme |\n| `caaca34` | v0.10.3 | **本 ADR**：双模式 resolve.alias + resolveId/load    | ✅                                                                          |\n\n### ADR 0015 的贡献\n\nADR 0015（`c576c74`）引入了 `buildCoreSubpathAliases()` 自动生成所有子路径的 alias，消除了手动遗漏的问题。但 ADR 0015 仅解决了**本地模式**（`file://` import.meta.url）的问题。对于 JSR 远程模式，仍需要一个不同的策略。\n\n### 早期尝试的失败\n\nv0.10.2（`70d3704`）尝试让 `buildCoreSubpathAliases()` 在远程模式下返回 `https://` URL 作为 alias replacement：\n\n```ts\n// 失败的方案\naliases.push({\n  find: '@openelement/core/ssr-handler',\n  replacement: 'https://jsr.io/@openelement/core@0.10.2/src/ssr-handler.ts', // ❌\n});\n```\n\n失败原因：Vite 的 `resolve.alias` 最终会通过 Node.js ESM loader 解析模块。当 replacement 值是 `https://` URL 时，Node.js ESM loader 抛出 `ERR_UNSUPPORTED_ESM_URL_SCHEME` 错误——它只接受 `file://` 和 `data:` scheme。\n\n## Decision\n\n### 双模式解析策略\n\n根据 `import.meta.url` 的 scheme 自动选择解析策略：\n\n| 执行上下文 | `import.meta.url` scheme | 解析策略                      | 原因                           |\n| ---------- | ------------------------ | ----------------------------- | ------------------------------ |\n| 本地开发   | `file://`                | `resolve.alias`               | 快速、HMR 兼容、无虚拟模块开销 |\n| JSR 远程   | `https://`               | `resolveId` + `load` 虚拟模块 | 绕过 Node.js ESM loader        |\n\n### 模式 A：本地模式（`resolve.alias`）\n\n```ts\n// 本地模式：直接映射到文件系统路径\naliases.push({\n  find: '@openelement/core/ssr-handler',\n  replacement: '/path/to/packages/core/src/ssr-handler.ts',\n});\n```\n\n- 速度快：Vite 内部路径替换，不经过网络\n- HMR 兼容：文件变更直接触发热更新\n- 零开销：无虚拟模块的 load 钩子开销\n\n### 模式 B：远程模式（`resolveId` + `load` 虚拟模块）\n\n使用 Rollup/Vite 的虚拟模块模式（`resolveId` + `load` hooks），通过 `\\0` 前缀的虚拟 ID 完全绕过 Node.js ESM loader：\n\n```\n用户代码 import '@openelement/core/ssr-handler'\n    ↓ resolveId hook\n虚拟 ID '\\0lessjs:core/src/ssr-handler.ts'\n    ↓ load hook\n从 JSR fetch 源码 → 返回字符串代码\n    ↓\nVite/Rollup 内部处理（不经过 Node.js ESM loader）\n```\n\n#### 为什么 `\\0` 前缀有效\n\nRollup/Vite 约定：以 `\\0` 开头的模块 ID 是虚拟模块，不会被写入文件系统，也不会通过 Node.js ESM loader 解析。它们的生命周期完全在 Rollup/Vite 的 plugin pipeline 内：\n\n- `resolveId` 返回 `\\0xxx` → Vite 知道这是虚拟模块\n- `load` 返回源码字符串 → Vite 直接编译，不涉及文件系统 I/O\n- 后续 `transform` 等钩子正常执行\n\n#### 三种 resolveId 拦截场景\n\n```ts\nresolveId(source, importer) {\n  // Case 1: 裸标识符 @openelement/core/* → 虚拟 ID\n  //   '@openelement/core/ssr-handler' → '\\0lessjs:core/src/ssr-handler.ts'\n  if (source.startsWith('@openelement/core/')) { ... }\n\n  // Case 2: 虚拟模块内部的相对导入 → 也拦截\n  //   './errors.js' from '\\0lessjs:core/src/ssr-handler.ts' → '\\0lessjs:core/src/errors.js'\n  if (importer?.startsWith(VIRTUAL_CORE_PREFIX) && source.startsWith('./')) { ... }\n\n  // Case 3: 已解析的虚拟 ID → 直接通过（防重入）\n  if (source.startsWith(VIRTUAL_CORE_PREFIX)) { ... }\n}\n```\n\nCase 2 是关键的细节：`ssr-handler.ts` 内部 `import { LessError } from './errors.js'`，这个相对导入也需要被拦截——否则 Vite 会尝试通过 Node.js ESM loader 解析它，而此时 importer 是虚拟 ID（不在文件系统上），解析会失败。\n\n#### `.js` → `.ts` 扩展名规范化\n\nDeno 约定：导入语句使用 `.js` 扩展名，但实际文件是 `.ts`。虚拟模块的 load 钩子在构建 JSR URL 时需要将 `.js` 转换为 `.ts`：\n\n```ts\n// Deno 源码中的导入\nimport { LessError } from './errors.js'; // .js 扩展名\n\n// JSR 上实际存储的文件\n// https://jsr.io/@openelement/core@0.10.3/src/errors.ts  // .ts 扩展名\n```\n\n#### 源码缓存\n\n`jsrSourceCache` Map 缓存已 fetch 的源码，避免重复网络请求。在 dev 模式下，同一模块可能被多次 load（HMR、依赖图重建等），缓存确保只 fetch 一次。\n\n### 代码实现\n\n#### `CORE_SUBPATHS` 映射表\n\n所有支持的子路径集中定义在一个 Record 中，本地模式和远程模式共用：\n\n```ts\nconst CORE_SUBPATHS: Record<string, string> = {\n  'html-escape': 'html-escape.ts',\n  'render-dsd': 'render-dsd.ts',\n  'render-nested': 'render-nested.ts',\n  'island-manifest': 'island-manifest.ts',\n  'adapter-registry': 'adapter-registry.ts',\n  'ssr-handler': 'ssr-handler.ts',\n  'logger': 'logger.ts',\n  'build-context': 'build-context.ts',\n  'navigation': 'navigation.ts',\n};\n```\n\n#### `createCoreResolvePlugin(metaUrl: string): Plugin`\n\n工厂函数，根据 `import.meta.url` 创建虚拟模块插件。在本地模式下，所有钩子提前返回（`if (!isRemote) return`），开销接近零。\n\n#### `buildCoreSubpathAliases()` — 仅本地模式\n\nADR 0015 引入的自动 alias 生成函数保持不变，但现在只在本地模式下使用。远程模式返回空数组。\n\n#### `less()` 插件数组\n\n从 5 个插件增加到 6 个，新增 `less:core-resolve` 插件：\n\n```ts\nreturn [\n  corePlugin,                          // less:core\n  createCoreResolvePlugin(metaUrl),     // less:core-resolve (NEW)\n  virtualEntryPlugin,                  // less:virtual-entry\n  devServerPlugin,                     // @hono/vite-dev-server\n  islandTransformPlugin(...),          // less:island-transform\n  buildPlugin(...),                     // less:build\n];\n```\n\n### 与 Fresh 2.0 的对比\n\nDeno 的 Fresh 2.0 框架面临类似问题，使用 `@deno/loader` 包来处理远程模块加载。我们的方案差异：\n\n| 维度       | Fresh 2.0                  | LessJS（本 ADR）                    |\n| ---------- | -------------------------- | ----------------------------------- |\n| 远程加载器 | `@deno/loader`（外部依赖） | 内置 `resolveId` + `load`（零依赖） |\n| 范围       | 通用 Deno 模块加载         | 仅 `@openelement/core/*` 子路径          |\n| 缓存       | 依赖 Deno 的模块缓存       | 内置 `jsrSourceCache` Map           |\n| 复杂度     | 高（通用方案）             | 低（最小化，仅处理核心子路径）      |\n\n选择零依赖内置方案的原因：\n\n- `@openelement/core` 的子路径数量有限（~10 个），不需要通用方案\n- 避免引入外部依赖带来的版本兼容和维护风险\n- 内置方案可以精确控制缓存策略和错误处理\n\n## Consequences\n\n### Positive\n\n- **彻底解决 JSR 远程 SSR 崩溃**：虚拟模块通过 `\\0` 前缀完全绕过 Node.js ESM loader\n- **本地模式零影响**：`resolve.alias` 仍然是本地模式的首选方案，快速且 HMR 兼容\n- **远程模式零依赖**：不需要引入 `@deno/loader` 等外部包\n- **Bug 不再复发**：集中定义 `CORE_SUBPATHS`，新增子路径只需在一处更新\n- **相对导入链完整支持**：`resolveId` 同时拦截虚拟模块内部的相对导入（Case 2）\n- **源码缓存**：`jsrSourceCache` 避免重复网络请求\n\n### Negative\n\n- **运行时网络依赖**：远程模式下首次 load 需要 fetch JSR 源码，增加冷启动延迟\n  - **缓解**：`jsrSourceCache` 确保每个模块只 fetch 一次；JSR CDN 速度快（< 100ms）\n- **源码需可 fetch**：假设 JSR 上的源码 URL 可公开访问；私有包可能需要认证\n  - **缓解**：LessJS 是开源框架，所有包都是公开的\n- **`.js` → `.ts` 规范化假设**：依赖 Deno 的约定（导入用 `.js`，文件是 `.ts`）\n  - **缓解**：这是 Deno 生态的标准做法，不太可能改变\n- **插件数量增加**：从 5 个增加到 6 个\n  - **缓解**：在本地模式下，`less:core-resolve` 的所有钩子提前返回，开销接近零\n\n### Neutral\n\n- `CORE_SUBPATHS` 映射表是两个模式的共享真相源（single source of truth）\n- 虚拟模块 ID 格式 `\\0lessjs:core/src/` 是内部约定，不对外暴露\n- `createCoreResolvePlugin` 是工厂函数而非单例，因为需要捕获 `import.meta.url` 的值\n\n## 参考\n\n- [ADR 0015: 自动注入 @openelement/core 子路径 alias](.) — 自动 alias 生成本地模式的基础\n- [Rollup Virtual Modules 约定](https://rollupjs.org/plugin-development/#conventions-for-plugin-hooks) — `\\0` 前缀约定\n- [Node.js ESM Loader](https://nodejs.org/api/esm.html) — 仅支持 `file://` 和 `data:` scheme\n- [JSR Registry](https://jsr.io/) — Deno 的包注册中心\n- [Fresh 2.0 `@deno/loader`](https://github.com/denoland/fresh) — 通用 Deno 模块加载器（参考方案）\n\n---\n\n_决策日期: 2026-05-11 | 版本: v0.10.3 | Bug #9 修复_\n",
    "html": "<h2>Status</h2>\n<p><strong>ACCEPTED</strong> — v0.10.3 Bug #9 修复</p>\n<h2>Context</h2>\n<h3>问题描述</h3>\n<p>LessJS 的虚拟入口模块 <code>virtual:less-hono-entry</code> 会从 <code>@openelement/core</code> 的子路径导入模块（如 <code>@openelement/core/ssr-handler</code>、<code>@openelement/core/logger</code>）。当用户通过 JSR 使用 LessJS 时（即 <code>import.meta.url</code> 为 <code>https://jsr.io/...</code>），Vite 的 SSR runner 无法解析这些裸标识符（bare specifier），原因如下：</p>\n<ol>\n<li><strong>JSR 包不在 <code>node_modules/</code> 中</strong>：Deno 使用内容寻址缓存（content-addressable cache），文件名是不透明的哈希值，无法从 <code>https://</code> URL 反推本地文件路径</li>\n<li><strong>Deno 的 import map（<code>deno.json</code>）不被 Vite SSR runner 使用</strong>：Vite 的 SSR runner 底层是 Node.js ESM loader</li>\n<li><strong>Node.js ESM loader 仅支持 <code>file://</code> 和 <code>data:</code> URL scheme</strong>：不支持 <code>https://</code> scheme</li>\n</ol>\n<h3>Bug 历史</h3>\n<p>此问题已反复出现 4 次：</p>\n<table>\n<thead>\n<tr>\n<th>提交</th>\n<th>时间</th>\n<th>尝试方案</th>\n<th>失败原因</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>b6a6b41</code></td>\n<td>v0.9.x</td>\n<td>手动添加 Vite alias（硬编码路径）</td>\n<td>新增子路径时遗漏，DX 差</td>\n</tr>\n<tr>\n<td><code>f223bef</code></td>\n<td>v0.9.x</td>\n<td>扩展 alias 覆盖面</td>\n<td>仍有遗漏 + 路径不对</td>\n</tr>\n<tr>\n<td><code>6c5a992</code></td>\n<td>v0.9.x</td>\n<td>补充缺失的 alias</td>\n<td>同上，仍然不完整</td>\n</tr>\n<tr>\n<td><code>70d3704</code></td>\n<td>v0.10.2</td>\n<td><code>resolve.alias</code> 使用 <code>https://</code> URL 作为 replacement</td>\n<td><code>ERR_UNSUPPORTED_ESM_URL_SCHEME</code>：Node.js ESM loader 拒绝 <code>https://</code> scheme</td>\n</tr>\n<tr>\n<td><code>caaca34</code></td>\n<td>v0.10.3</td>\n<td><strong>本 ADR</strong>：双模式 resolve.alias + resolveId/load</td>\n<td>✅</td>\n</tr>\n</tbody></table>\n<h3>ADR 0015 的贡献</h3>\n<p>ADR 0015（<code>c576c74</code>）引入了 <code>buildCoreSubpathAliases()</code> 自动生成所有子路径的 alias，消除了手动遗漏的问题。但 ADR 0015 仅解决了<strong>本地模式</strong>（<code>file://</code> import.meta.url）的问题。对于 JSR 远程模式，仍需要一个不同的策略。</p>\n<h3>早期尝试的失败</h3>\n<p>v0.10.2（<code>70d3704</code>）尝试让 <code>buildCoreSubpathAliases()</code> 在远程模式下返回 <code>https://</code> URL 作为 alias replacement：</p>\n<pre><code class=\"language-ts\">// 失败的方案\naliases.push({\n  find: &#39;@openelement/core/ssr-handler&#39;,\n  replacement: &#39;https://jsr.io/@openelement/core@0.10.2/src/ssr-handler.ts&#39;, // ❌\n});\n</code></pre>\n<p>失败原因：Vite 的 <code>resolve.alias</code> 最终会通过 Node.js ESM loader 解析模块。当 replacement 值是 <code>https://</code> URL 时，Node.js ESM loader 抛出 <code>ERR_UNSUPPORTED_ESM_URL_SCHEME</code> 错误——它只接受 <code>file://</code> 和 <code>data:</code> scheme。</p>\n<h2>Decision</h2>\n<h3>双模式解析策略</h3>\n<p>根据 <code>import.meta.url</code> 的 scheme 自动选择解析策略：</p>\n<table>\n<thead>\n<tr>\n<th>执行上下文</th>\n<th><code>import.meta.url</code> scheme</th>\n<th>解析策略</th>\n<th>原因</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>本地开发</td>\n<td><code>file://</code></td>\n<td><code>resolve.alias</code></td>\n<td>快速、HMR 兼容、无虚拟模块开销</td>\n</tr>\n<tr>\n<td>JSR 远程</td>\n<td><code>https://</code></td>\n<td><code>resolveId</code> + <code>load</code> 虚拟模块</td>\n<td>绕过 Node.js ESM loader</td>\n</tr>\n</tbody></table>\n<h3>模式 A：本地模式（<code>resolve.alias</code>）</h3>\n<pre><code class=\"language-ts\">// 本地模式：直接映射到文件系统路径\naliases.push({\n  find: &#39;@openelement/core/ssr-handler&#39;,\n  replacement: &#39;/path/to/packages/core/src/ssr-handler.ts&#39;,\n});\n</code></pre>\n<ul>\n<li>速度快：Vite 内部路径替换，不经过网络</li>\n<li>HMR 兼容：文件变更直接触发热更新</li>\n<li>零开销：无虚拟模块的 load 钩子开销</li>\n</ul>\n<h3>模式 B：远程模式（<code>resolveId</code> + <code>load</code> 虚拟模块）</h3>\n<p>使用 Rollup/Vite 的虚拟模块模式（<code>resolveId</code> + <code>load</code> hooks），通过 <code>\\0</code> 前缀的虚拟 ID 完全绕过 Node.js ESM loader：</p>\n<pre><code>用户代码 import &#39;@openelement/core/ssr-handler&#39;\n    ↓ resolveId hook\n虚拟 ID &#39;\\0lessjs:core/src/ssr-handler.ts&#39;\n    ↓ load hook\n从 JSR fetch 源码 → 返回字符串代码\n    ↓\nVite/Rollup 内部处理（不经过 Node.js ESM loader）\n</code></pre>\n<h4>为什么 <code>\\0</code> 前缀有效</h4>\n<p>Rollup/Vite 约定：以 <code>\\0</code> 开头的模块 ID 是虚拟模块，不会被写入文件系统，也不会通过 Node.js ESM loader 解析。它们的生命周期完全在 Rollup/Vite 的 plugin pipeline 内：</p>\n<ul>\n<li><code>resolveId</code> 返回 <code>\\0xxx</code> → Vite 知道这是虚拟模块</li>\n<li><code>load</code> 返回源码字符串 → Vite 直接编译，不涉及文件系统 I/O</li>\n<li>后续 <code>transform</code> 等钩子正常执行</li>\n</ul>\n<h4>三种 resolveId 拦截场景</h4>\n<pre><code class=\"language-ts\">resolveId(source, importer) {\n  // Case 1: 裸标识符 @openelement/core/* → 虚拟 ID\n  //   &#39;@openelement/core/ssr-handler&#39; → &#39;\\0lessjs:core/src/ssr-handler.ts&#39;\n  if (source.startsWith(&#39;@openelement/core/&#39;)) { ... }\n\n  // Case 2: 虚拟模块内部的相对导入 → 也拦截\n  //   &#39;./errors.js&#39; from &#39;\\0lessjs:core/src/ssr-handler.ts&#39; → &#39;\\0lessjs:core/src/errors.js&#39;\n  if (importer?.startsWith(VIRTUAL_CORE_PREFIX) &amp;&amp; source.startsWith(&#39;./&#39;)) { ... }\n\n  // Case 3: 已解析的虚拟 ID → 直接通过（防重入）\n  if (source.startsWith(VIRTUAL_CORE_PREFIX)) { ... }\n}\n</code></pre>\n<p>Case 2 是关键的细节：<code>ssr-handler.ts</code> 内部 <code>import { LessError } from &#39;./errors.js&#39;</code>，这个相对导入也需要被拦截——否则 Vite 会尝试通过 Node.js ESM loader 解析它，而此时 importer 是虚拟 ID（不在文件系统上），解析会失败。</p>\n<h4><code>.js</code> → <code>.ts</code> 扩展名规范化</h4>\n<p>Deno 约定：导入语句使用 <code>.js</code> 扩展名，但实际文件是 <code>.ts</code>。虚拟模块的 load 钩子在构建 JSR URL 时需要将 <code>.js</code> 转换为 <code>.ts</code>：</p>\n<pre><code class=\"language-ts\">// Deno 源码中的导入\nimport { LessError } from &#39;./errors.js&#39;; // .js 扩展名\n\n// JSR 上实际存储的文件\n// https://jsr.io/@openelement/core@0.10.3/src/errors.ts  // .ts 扩展名\n</code></pre>\n<h4>源码缓存</h4>\n<p><code>jsrSourceCache</code> Map 缓存已 fetch 的源码，避免重复网络请求。在 dev 模式下，同一模块可能被多次 load（HMR、依赖图重建等），缓存确保只 fetch 一次。</p>\n<h3>代码实现</h3>\n<h4><code>CORE_SUBPATHS</code> 映射表</h4>\n<p>所有支持的子路径集中定义在一个 Record 中，本地模式和远程模式共用：</p>\n<pre><code class=\"language-ts\">const CORE_SUBPATHS: Record&lt;string, string&gt; = {\n  &#39;html-escape&#39;: &#39;html-escape.ts&#39;,\n  &#39;render-dsd&#39;: &#39;render-dsd.ts&#39;,\n  &#39;render-nested&#39;: &#39;render-nested.ts&#39;,\n  &#39;island-manifest&#39;: &#39;island-manifest.ts&#39;,\n  &#39;adapter-registry&#39;: &#39;adapter-registry.ts&#39;,\n  &#39;ssr-handler&#39;: &#39;ssr-handler.ts&#39;,\n  &#39;logger&#39;: &#39;logger.ts&#39;,\n  &#39;build-context&#39;: &#39;build-context.ts&#39;,\n  &#39;navigation&#39;: &#39;navigation.ts&#39;,\n};\n</code></pre>\n<h4><code>createCoreResolvePlugin(metaUrl: string): Plugin</code></h4>\n<p>工厂函数，根据 <code>import.meta.url</code> 创建虚拟模块插件。在本地模式下，所有钩子提前返回（<code>if (!isRemote) return</code>），开销接近零。</p>\n<h4><code>buildCoreSubpathAliases()</code> — 仅本地模式</h4>\n<p>ADR 0015 引入的自动 alias 生成函数保持不变，但现在只在本地模式下使用。远程模式返回空数组。</p>\n<h4><code>less()</code> 插件数组</h4>\n<p>从 5 个插件增加到 6 个，新增 <code>less:core-resolve</code> 插件：</p>\n<pre><code class=\"language-ts\">return [\n  corePlugin,                          // less:core\n  createCoreResolvePlugin(metaUrl),     // less:core-resolve (NEW)\n  virtualEntryPlugin,                  // less:virtual-entry\n  devServerPlugin,                     // @hono/vite-dev-server\n  islandTransformPlugin(...),          // less:island-transform\n  buildPlugin(...),                     // less:build\n];\n</code></pre>\n<h3>与 Fresh 2.0 的对比</h3>\n<p>Deno 的 Fresh 2.0 框架面临类似问题，使用 <code>@deno/loader</code> 包来处理远程模块加载。我们的方案差异：</p>\n<table>\n<thead>\n<tr>\n<th>维度</th>\n<th>Fresh 2.0</th>\n<th>LessJS（本 ADR）</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>远程加载器</td>\n<td><code>@deno/loader</code>（外部依赖）</td>\n<td>内置 <code>resolveId</code> + <code>load</code>（零依赖）</td>\n</tr>\n<tr>\n<td>范围</td>\n<td>通用 Deno 模块加载</td>\n<td>仅 <code>@openelement/core/*</code> 子路径</td>\n</tr>\n<tr>\n<td>缓存</td>\n<td>依赖 Deno 的模块缓存</td>\n<td>内置 <code>jsrSourceCache</code> Map</td>\n</tr>\n<tr>\n<td>复杂度</td>\n<td>高（通用方案）</td>\n<td>低（最小化，仅处理核心子路径）</td>\n</tr>\n</tbody></table>\n<p>选择零依赖内置方案的原因：</p>\n<ul>\n<li><code>@openelement/core</code> 的子路径数量有限（~10 个），不需要通用方案</li>\n<li>避免引入外部依赖带来的版本兼容和维护风险</li>\n<li>内置方案可以精确控制缓存策略和错误处理</li>\n</ul>\n<h2>Consequences</h2>\n<h3>Positive</h3>\n<ul>\n<li><strong>彻底解决 JSR 远程 SSR 崩溃</strong>：虚拟模块通过 <code>\\0</code> 前缀完全绕过 Node.js ESM loader</li>\n<li><strong>本地模式零影响</strong>：<code>resolve.alias</code> 仍然是本地模式的首选方案，快速且 HMR 兼容</li>\n<li><strong>远程模式零依赖</strong>：不需要引入 <code>@deno/loader</code> 等外部包</li>\n<li><strong>Bug 不再复发</strong>：集中定义 <code>CORE_SUBPATHS</code>，新增子路径只需在一处更新</li>\n<li><strong>相对导入链完整支持</strong>：<code>resolveId</code> 同时拦截虚拟模块内部的相对导入（Case 2）</li>\n<li><strong>源码缓存</strong>：<code>jsrSourceCache</code> 避免重复网络请求</li>\n</ul>\n<h3>Negative</h3>\n<ul>\n<li><strong>运行时网络依赖</strong>：远程模式下首次 load 需要 fetch JSR 源码，增加冷启动延迟<ul>\n<li><strong>缓解</strong>：<code>jsrSourceCache</code> 确保每个模块只 fetch 一次；JSR CDN 速度快（&lt; 100ms）</li>\n</ul>\n</li>\n<li><strong>源码需可 fetch</strong>：假设 JSR 上的源码 URL 可公开访问；私有包可能需要认证<ul>\n<li><strong>缓解</strong>：LessJS 是开源框架，所有包都是公开的</li>\n</ul>\n</li>\n<li><strong><code>.js</code> → <code>.ts</code> 规范化假设</strong>：依赖 Deno 的约定（导入用 <code>.js</code>，文件是 <code>.ts</code>）<ul>\n<li><strong>缓解</strong>：这是 Deno 生态的标准做法，不太可能改变</li>\n</ul>\n</li>\n<li><strong>插件数量增加</strong>：从 5 个增加到 6 个<ul>\n<li><strong>缓解</strong>：在本地模式下，<code>less:core-resolve</code> 的所有钩子提前返回，开销接近零</li>\n</ul>\n</li>\n</ul>\n<h3>Neutral</h3>\n<ul>\n<li><code>CORE_SUBPATHS</code> 映射表是两个模式的共享真相源（single source of truth）</li>\n<li>虚拟模块 ID 格式 <code>\\0lessjs:core/src/</code> 是内部约定，不对外暴露</li>\n<li><code>createCoreResolvePlugin</code> 是工厂函数而非单例，因为需要捕获 <code>import.meta.url</code> 的值</li>\n</ul>\n<h2>参考</h2>\n<ul>\n<li><a href=\".\" rel=\"noopener noreferrer\">ADR 0015: 自动注入 @openelement/core 子路径 alias</a> — 自动 alias 生成本地模式的基础</li>\n<li><a href=\"https://rollupjs.org/plugin-development/#conventions-for-plugin-hooks\" rel=\"noopener noreferrer\">Rollup Virtual Modules 约定</a> — <code>\\0</code> 前缀约定</li>\n<li><a href=\"https://nodejs.org/api/esm.html\" rel=\"noopener noreferrer\">Node.js ESM Loader</a> — 仅支持 <code>file://</code> 和 <code>data:</code> scheme</li>\n<li><a href=\"https://jsr.io/\" rel=\"noopener noreferrer\">JSR Registry</a> — Deno 的包注册中心</li>\n<li><a href=\"https://github.com/denoland/fresh\" rel=\"noopener noreferrer\">Fresh 2.0 <code>@deno/loader</code></a> — 通用 Deno 模块加载器（参考方案）</li>\n</ul>\n<hr />\n<p><em>决策日期: 2026-05-11 | 版本: v0.10.3 | Bug #9 修复</em></p>\n"
  },
  {
    "slug": "0014-ssr-bundle-export-renderroute",
    "frontmatter": {
      "title": "ADR 0014: SSR Bundle 导出 renderRoute() — 消除 build-ssg.ts 的越权访问",
      "date": "2026-05-10",
      "draft": false,
      "tags": [
        "architecture",
        "decision"
      ],
      "type": "adr",
      "lang": "zh"
    },
    "content": "\n## Status\n\n**ACCEPTED** — v0.10.x 架构简化\n\n## Context\n\nADR 0010/0011 将 SSG 构建从 `createServer() + ssrLoadModule()` 迁移到 `viteBuild(ssr:true, noExternal) + import()`，产出自包含 SSR bundle。`build-ssg.ts` 通过 `import(ssrBundlePath)` 加载 bundle，然后：\n\n1. 调用 Hono `toSSG(app, ...)` 渲染默认 locale 的静态路由 ✅\n2. 手动遍历路由，为每个 locale 和动态路由渲染额外页面 ❌\n\n### 当前 i18n + 动态路由渲染的问题\n\n`build-ssg.ts` 在渲染 locale 扩展和动态路由页面时，需要获取每个路由的 `tagName` 和 `ComponentClass`。但这两个信息的获取方式都存在问题：\n\n#### 问题 1：正则提取 tagName（脆弱、侵入源码）\n\n```ts\n// build-ssg.ts L556-564\nconst src = readFileSync(routeSourcePath, 'utf-8');\nconst tagMatch = src.match(/export\\s+const\\s+tagName\\s*=\\s*['\"]([^'\"]+)['\"]/);\n```\n\n- 读源码文件 + 正则解析 — 不是 API，是实现细节的逆向工程\n- 如果用户用不同格式导出 tagName（间接引用、计算值），正则失败\n- 构建工具不应该解析自己的输入源码来提取运行时信息\n\n#### 问题 2：globalThis.customElements.get()（越权访问）\n\n```ts\n// build-ssg.ts L567-569\nconst ComponentClass = globalThis.customElements.get(tagName);\n```\n\n- `build-ssg.ts` 从外部读取 SSR bundle 内部通过副作用注册的全局数据\n- 虽然这是 Web 标准 API，但调用位置在抽象边界之外——相当于\"翻墙\"进入 bundle 的内部状态\n- 违背 ADR 0008/0011 \"显式优于隐式\"的原则\n\n#### 问题 3：直接调用 renderDSD() + wrapInDocument()（越权操作）\n\n```ts\n// build-ssg.ts L619-631\nconst html = await renderDSDFn(tagName, ComponentClass, { ...params, locale }, { ... });\nconst fullHtml = wrapInDocumentFn(html, { title, lang, headExtras });\n```\n\n- `build-ssg.ts` 自己查找组件类、自己调渲染、自己包装 HTML — 全部是 SSR bundle 内部该做的事\n- 每次 build-ssg.ts 需要新的渲染能力（如 renderer chain），都必须再\"翻墙\"获取\n\n#### 问题 4：customElements.define 幂等补丁（外部修改全局状态）\n\n```ts\n// build-ssg.ts L304-321\nconst origDefine = globalThis.customElements?.define?.bind(globalThis.customElements);\nglobalThis.customElements.define = (name, ctor, options) => { ... };\n```\n\n- 从外部修改 Web 标准 API 的行为，影响整个 realm\n- 这个补丁应该在 bundle 内部执行——只有 bundle 知道何时需要幂等\n\n#### 问题 5：双作用域初始化（博客/i18n 数据）\n\n```ts\n// build-ssg.ts L349-368\n// 先在 bundle scope 初始化\nawait module.initBlogData(blogOptions);\n// 再在 file system scope 初始化（因为 await import() 创建了新的模块实例）\nconst fsContentModule = await import('@openelement/content');\nawait fsContentModule.initBlogData(blogOptions);\n```\n\n- `await import('@openelement/content')` 会创建独立的模块实例（不在 SSR bundle 内）\n- 需要手动在两个作用域中同步状态 — ADR 0008 要消除的正是这类多实例问题\n- 如果路由模块依赖虚拟模块（如 `virtual:less-nav`），`native import()` 直接失败\n\n### 根本原因：抽象边界错误\n\n```\n┌─────────────────────────────────────┐\n│        SSR Bundle (entry.js)       │\n│  ┌───────────────────────────────┐  │\n│  │  customElements registry      │  │\n│  │  renderDSD()                   │  │\n│  │  wrapInDocument()              │  │\n│  │  所有路由模块 + 组件类           │  │\n│  └───────────────────────────────┘  │\n│         ↑ 被外部越权读取/调用        │\n└─────────────────────────────────────┘\n          ↑\n┌─────────────────────────────────────┐\n│       build-ssg.ts (外部)           │\n│  globalThis.customElements.get()    │\n│  readFileSync + regex (提取 tagName) │\n│  renderDSDFn() + wrapInDocumentFn() │\n│  customElements.define 幂等补丁     │\n└─────────────────────────────────────┘\n```\n\n`build-ssg.ts` 扮演了两个角色：\n\n1. **编排者**：决定渲染什么路径、写到哪个文件 ✅\n2. **渲染执行者**：查找组件、调用渲染、包装 HTML ❌\n\n角色 2 应该由 SSR bundle 内部承担。\n\n## Decision\n\n**将渲染能力封装为 SSR bundle 的显式导出**，让 `build-ssg.ts` 只做编排（路径枚举 + 文件写入），不再进入 bundle 内部。\n\n### 新的抽象边界\n\n```\n┌─────────────────────────────────────┐\n│        SSR Bundle (entry.js)       │\n│                                     │\n│  内部：                              │\n│    customElements registry (Web API)│\n│    renderDSD()                       │\n│    wrapInDocument()                  │\n│    所有路由模块 + 组件类               │\n│    getStaticPaths() per route        │\n│                                     │\n│  导出（显式 API）：                    │\n│    renderRoute(path, opts) → HTML   │\n│    getStaticPaths(path) → params[]   │\n│    routeInfo[]                       │\n└─────────────────────────────────────┘\n          ↓\n┌─────────────────────────────────────┐\n│       build-ssg.ts (外部)           │\n│                                     │\n│  只做编排：                            │\n│    1. 哪些路径需要渲染？               │\n│    2. 哪些 locale 需要扩展？          │\n│    3. 结果写到哪个文件？               │\n│                                     │\n│  调用 bundle API：                    │\n│    ssrBundle.renderRoute(path, opts) │\n│    ssrBundle.getStaticPaths(path)    │\n└─────────────────────────────────────┘\n```\n\n### 导出的 API 设计\n\n```typescript\n// SSR bundle 导出\n\n/** 路由信息 — bundle 知道自己包含的所有路由 */\nexport interface RouteInfo {\n  /** URL 模式，如 '/blog/:slug' 或 '/guide/dsd' */\n  path: string;\n  /** 注册的 Custom Element tag name */\n  tagName: string;\n  /** 是否为动态路由 */\n  isDynamic: boolean;\n  /** 动态路由的参数名 */\n  paramNames: string[];\n}\n\nexport const routeInfo: RouteInfo[];\n\n/**\n * 渲染一个路由页面为完整 HTML。\n *\n * 内部使用 customElements.get() + renderDSD() + wrapInDocument()。\n * 调用者不需要知道组件类、tagName、或渲染细节。\n *\n * @param routePath - 路由路径模式（如 '/blog/:slug'）\n * @param options - 渲染选项\n * @returns 完整的 HTML 文档字符串\n */\nexport async function renderRoute(\n  routePath: string,\n  options?: {\n    /** 动态路由参数（如 { slug: 'v0-8-0' }） */\n    params?: Record<string, string>;\n    /** i18n locale（如 'zh'） */\n    locale?: string;\n    /** HTML <title> */\n    title?: string;\n    /** HTML lang attribute */\n    lang?: string;\n    /** 额外 <head> 内容 */\n    headExtras?: string;\n  },\n): Promise<string>;\n\n/**\n * 获取动态路由的静态路径列表。\n *\n * 内部调用路由模块的 getStaticPaths()。\n * 只对 isDynamic: true 的路由有效。\n *\n * @param routePath - 路由路径模式（如 '/blog/:slug'）\n * @returns 参数组合列表\n */\nexport async function getStaticPaths(\n  routePath: string,\n): Promise<Array<Record<string, string>>>;\n```\n\n### 关键设计决策\n\n#### 为什么导出 `renderRoute(path)` 而不是 `renderRoute(tagName)`\n\n`build-ssg.ts` 工作的粒度是**路径**（path），不是 tagName。它遍历路由列表，对每个路径+locale 组合生成页面。tagName 是 bundle 内部的实现细节——调用者不应该需要知道它。\n\n这也使得未来可以改变内部注册机制（比如不用 customElements 而用其他注册方式），而不影响调用方。\n\n#### 为什么 `customElements` 是 Web 标准 API 的正确用法\n\n在 ADR 讨论中，我们反复问\"能不能干掉 `customElements`？\"。答案是：\n\n- **在 bundle 外部**：不应该直接使用。这是 ADR 0014 要消除的。\n- **在 bundle 内部**：应该正常使用。Custom Elements Registry 是 Web Components 的标准基础设施，就如同 `document.createElement` 或 `fetch` 一样。\n\n类比：你不会因为\"用了 `document.createElement`\"就觉得需要干掉它。`customElements` 在 bundle 内部的角色完全相同——它是 Web 平台提供的服务定位器（Service Locator），不是我们自己的全局变量。\n\n#### 为什么不导出 N 个组件\n\n导出 `routeRegistry = { 'page-dsd-guide': DsdGuidePage, ... }` 的问题是：\n\n- N 随路由增长，接口不稳定\n- 暴露了内部实现（组件类）\n- 调用者仍需自己做 tagName → ComponentClass 映射 + 渲染调用\n\n`renderRoute()` 封装了所有这些——接口大小为 1，永远不变。\n\n#### 动态路由的 getStaticPaths() 如何处理\n\n当前 `build-ssg.ts` 用 `await import(routeImportPath)` 加载动态路由模块来调用 `getStaticPaths()`。但路由模块可能依赖虚拟模块（如 `virtual:less-nav`），导致 native import 失败。\n\n在新设计中，`getStaticPaths()` 从 bundle 内部获取——bundle 已经内联了所有路由模块，包括 `getStaticPaths` 函数。bundle 的 `getStaticPaths(routePath)` 导出在内部查找对应的路由模块并调用它。\n\n这也解决了博客数据双作用域初始化问题：`getStaticPaths()` 在 bundle 内部执行时，自然使用 bundle 内的博客数据（同一模块作用域），无需外部手动初始化。\n\n### 与已有 API 的关系\n\nLessJS 已有 `renderDSDByName(tagName, props)` —— 它通过 `customElements.get(tagName)` 查找组件并渲染。`renderRoute(path, opts)` 是更高层的封装：\n\n```\nrenderDSD(tagName, Cls, props)          ← 底层：已知组件类\nrenderDSDByName(tagName, props)         ← 中层：通过 registry 查组件类\nrenderRoute(path, opts)                 ← 高层：通过路径找组件 + 渲染 + 包装\n```\n\n三层共存在 bundle 内，各司其职。外部只调用高层 API。\n\n### 代码变更\n\n#### 1. `entry-renderer.ts` — 生成 routeInfo + renderRoute + getStaticPaths\n\n在 SSG 模式下，在现有的 \"SSG Utility Re-exports\" 之后追加：\n\n```ts\n// --- Route info (build-ssg.ts needs this for path enumeration) ---\nexport const routeInfo = ${JSON.stringify(pageRoutes.map(r => ({\n  path: r.path,\n  tagName: r.tagName || r.defaultTagName,  // 编译时已知\n  isDynamic: r.isDynamic,\n  paramNames: r.paramNames,\n})))};\n\n// --- renderRoute: path → HTML (DSD-first) ---\nexport async function renderRoute(routePath, options = {}) {\n  const info = routeInfo.find(r => r.path === routePath);\n  if (!info) throw new Error('Route not found: ' + routePath);\n\n  const { params = {}, locale, title = 'LessJS', lang = locale || 'en', headExtras = __headExtras || '' } = options;\n\n  const html = await renderDSDByName(info.tagName, { ...params, locale }, { route: routePath, source: info.tagName });\n  return wrapInDocument(html, { title, lang, headExtras });\n}\n\n// --- getStaticPaths: dynamic route → params[] ---\nexport async function getStaticPaths(routePath) {\n  const info = routeInfo.find(r => r.path === routePath);\n  if (!info || !info.isDynamic) return [];\n\n  // 动态路由模块的 getStaticPaths 在 bundle 内已被内联\n  // 直接查找并调用\n  ${dynamicRoutes.map(r => {\n    return `if (routePath === '${r.path}') return await ${r.varName}.getStaticPaths();`;\n  }).join('\\n  ')}\n  return [];\n}\n```\n\n#### 2. `build-ssg.ts` — 移除所有越权代码\n\n**删除**：\n\n- 正则提取 tagName（L556-566）\n- `globalThis.customElements.get(tagName)` (L567-569)\n- 直接调用 `renderDSDFn()` + `wrapInDocumentFn()` (L619-631, L433-442)\n- `customElements.define` 幂等补丁 (L304-321)\n- 博客数据双作用域初始化 (L346-368)\n- `await import('@openelement/i18n')` 初始化 (L535-542)\n- 动态路由的 `await import(routeImportPath)` (L387-388)\n\n**替换为**：\n\n```ts\nconst { renderRoute, getStaticPaths, routeInfo } = module;\n\n// i18n locale expansion\nfor (const locale of locales) {\n  for (const route of routeInfo) {\n    if (route.isDynamic) {\n      const paramsList = await getStaticPaths(route.path);\n      for (const params of paramsList) {\n        const html = await renderRoute(route.path, { params, locale, ... });\n        // write to disk\n      }\n    } else {\n      const html = await renderRoute(route.path, { locale, ... });\n      // write to disk\n    }\n  }\n}\n```\n\n#### 3. `customElements.define` 幂等补丁 — 移入 bundle 内部\n\n在 `entry-renderer.ts` 生成的代码中，在 `customElements.define` 循环之前插入幂等补丁：\n\n```ts\n// Patch CustomElementRegistry.define to be idempotent in SSR\nconst _origDefine = customElements.define.bind(customElements);\ncustomElements.define = (name, ctor, options) => {\n  if (customElements.get(name)) return;\n  try {\n    _origDefine(name, ctor, options);\n  } catch { /* already defined */ }\n};\n```\n\n这样补丁在 bundle 内部执行，只影响 bundle 自己的注册行为，不污染外部 realm。\n\n## Consequences\n\n### Positive\n\n- **彻底消除 globalThis 越权访问**：`build-ssg.ts` 不再直接读取 `customElements` registry\n- **消除正则源码解析**：tagName 从 bundle 的显式导出获取，而非逆向工程\n- **抽象边界清晰**：`build-ssg.ts` 只做编排（路径 + 文件），bundle 只做渲染（组件 + HTML）\n- **customElements 在正确位置使用**：bundle 内部正常使用 Web 标准 API，外部不需要碰它\n- **消除双作用域初始化**：博客/i18n 数据只在 bundle 作用域初始化一次\n- **renderRoute 接口稳定**：1 个函数，不随路由数量增长\n- **未来可扩展**：renderer chain、head 管理等渲染逻辑变更只影响 bundle 内部\n- **DSD-first 对齐**：\"给我路径，给我 locale，我给你 HTML\" — 这才是 DSD-first 框架该有的接口\n\n### Negative\n\n- **SSR bundle 体积微增**：新增 `routeInfo` 常量 + `renderRoute` + `getStaticPaths` 函数。预计增加 < 2KB，可忽略\n- **renderRoute 丢失细粒度控制**：调用者不能自定义 `renderDSD` 的 `dsdOptions` 参数\n  - **缓解**：可在 `renderRoute` 的 options 中增加 `dsdOptions` 透传；当前无使用场景\n- **getStaticPaths 对动态路由硬编码 if/else**：每个动态路由生成一个 `if (routePath === ...)` 分支\n  - **缓解**：动态路由数量通常 < 10，分支可接受；也可改为 routePath → module 的 map\n\n### Neutral\n\n- `toSSG(app, ...)` 仍然用于默认 locale 的静态路由渲染 — 这个用法正确且不变\n- Hono app 对象仍然从 bundle 导出 — `build-ssg.ts` 用它调 `toSSG()`\n- `renderDSD` / `renderDSDByName` / `wrapInDocument` 仍从 bundle 导出 — 供高级用例使用，但 `renderRoute` 是推荐入口\n\n## 类似问题审计\n\n在审查 `build-ssg.ts` 的越权模式时，发现以下同类问题：\n\n### 已在本 ADR 中解决\n\n| # | 问题                                     | 当前位置              | 修复方式                         |\n| - | ---------------------------------------- | --------------------- | -------------------------------- |\n| 1 | `globalThis.customElements.get()`        | build-ssg.ts L567     | → bundle 内部的 renderRoute()    |\n| 2 | `readFileSync` + 正则提取 tagName        | build-ssg.ts L558-564 | → bundle 导出 routeInfo          |\n| 3 | 直接调用 renderDSD + wrapInDocument      | build-ssg.ts L619-631 | → bundle 内部的 renderRoute()    |\n| 4 | customElements.define 幂等补丁           | build-ssg.ts L304-321 | → 移入 bundle 内部               |\n| 5 | 博客数据双作用域初始化                   | build-ssg.ts L346-368 | → bundle 内部的 getStaticPaths() |\n| 6 | `await import('@openelement/i18n')` 初始化    | build-ssg.ts L535-542 | → bundle 内部处理                |\n| 7 | 动态路由 `await import(routeImportPath)` | build-ssg.ts L387-388 | → bundle 内部的 getStaticPaths() |\n\n### 值得关注但不在本 ADR 范围\n\n| #  | 问题                                               | 位置                  | 建议                                    |\n| -- | -------------------------------------------------- | --------------------- | --------------------------------------- |\n| 8  | `await import('@openelement/content/sitemap')` 独立导入 | build-ssg.ts L851     | 可改为 bundle 导出，与 renderRoute 同源 |\n| 9  | 客户端 manifest 读取 + 脚本注入                    | build-ssg.ts L676-698 | 纯文件 I/O，无越权，暂不处理            |\n| 10 | PWA manifest + service worker 生成                 | build-ssg.ts L769-845 | 纯文件生成，无越权，暂不处理            |\n\n## 参考\n\n- [ADR 0008: 消除 createServer()、.less/ 临时文件与 globalThis 桥接](/blog/0008-eliminate-createserver-globalthis-bridges)\n- [ADR 0010: 消除所有 .less/ 临时文件](/blog/0010-eliminate-all-dot-less-temp-files)\n- [ADR 0011: 消除最后一个 globalThis 桥接](/blog/0011-eliminate-last-globalthis-via-closebundle)\n- `packages/core/src/render-dsd.ts` — `renderDSDByName()` 已有的 registry 查找模式\n- `packages/core/src/entry-renderer.ts` — SSR bundle 代码生成\n\n---\n\n_决策日期: 2026-05-10 | 版本: v0.10.0_\n",
    "html": "<h2>Status</h2>\n<p><strong>ACCEPTED</strong> — v0.10.x 架构简化</p>\n<h2>Context</h2>\n<p>ADR 0010/0011 将 SSG 构建从 <code>createServer() + ssrLoadModule()</code> 迁移到 <code>viteBuild(ssr:true, noExternal) + import()</code>，产出自包含 SSR bundle。<code>build-ssg.ts</code> 通过 <code>import(ssrBundlePath)</code> 加载 bundle，然后：</p>\n<ol>\n<li>调用 Hono <code>toSSG(app, ...)</code> 渲染默认 locale 的静态路由 ✅</li>\n<li>手动遍历路由，为每个 locale 和动态路由渲染额外页面 ❌</li>\n</ol>\n<h3>当前 i18n + 动态路由渲染的问题</h3>\n<p><code>build-ssg.ts</code> 在渲染 locale 扩展和动态路由页面时，需要获取每个路由的 <code>tagName</code> 和 <code>ComponentClass</code>。但这两个信息的获取方式都存在问题：</p>\n<h4>问题 1：正则提取 tagName（脆弱、侵入源码）</h4>\n<pre><code class=\"language-ts\">// build-ssg.ts L556-564\nconst src = readFileSync(routeSourcePath, &#39;utf-8&#39;);\nconst tagMatch = src.match(/export\\s+const\\s+tagName\\s*=\\s*[&#39;&quot;]([^&#39;&quot;]+)[&#39;&quot;]/);\n</code></pre>\n<ul>\n<li>读源码文件 + 正则解析 — 不是 API，是实现细节的逆向工程</li>\n<li>如果用户用不同格式导出 tagName（间接引用、计算值），正则失败</li>\n<li>构建工具不应该解析自己的输入源码来提取运行时信息</li>\n</ul>\n<h4>问题 2：globalThis.customElements.get()（越权访问）</h4>\n<pre><code class=\"language-ts\">// build-ssg.ts L567-569\nconst ComponentClass = globalThis.customElements.get(tagName);\n</code></pre>\n<ul>\n<li><code>build-ssg.ts</code> 从外部读取 SSR bundle 内部通过副作用注册的全局数据</li>\n<li>虽然这是 Web 标准 API，但调用位置在抽象边界之外——相当于&quot;翻墙&quot;进入 bundle 的内部状态</li>\n<li>违背 ADR 0008/0011 &quot;显式优于隐式&quot;的原则</li>\n</ul>\n<h4>问题 3：直接调用 renderDSD() + wrapInDocument()（越权操作）</h4>\n<pre><code class=\"language-ts\">// build-ssg.ts L619-631\nconst html = await renderDSDFn(tagName, ComponentClass, { ...params, locale }, { ... });\nconst fullHtml = wrapInDocumentFn(html, { title, lang, headExtras });\n</code></pre>\n<ul>\n<li><code>build-ssg.ts</code> 自己查找组件类、自己调渲染、自己包装 HTML — 全部是 SSR bundle 内部该做的事</li>\n<li>每次 build-ssg.ts 需要新的渲染能力（如 renderer chain），都必须再&quot;翻墙&quot;获取</li>\n</ul>\n<h4>问题 4：customElements.define 幂等补丁（外部修改全局状态）</h4>\n<pre><code class=\"language-ts\">// build-ssg.ts L304-321\nconst origDefine = globalThis.customElements?.define?.bind(globalThis.customElements);\nglobalThis.customElements.define = (name, ctor, options) =&gt; { ... };\n</code></pre>\n<ul>\n<li>从外部修改 Web 标准 API 的行为，影响整个 realm</li>\n<li>这个补丁应该在 bundle 内部执行——只有 bundle 知道何时需要幂等</li>\n</ul>\n<h4>问题 5：双作用域初始化（博客/i18n 数据）</h4>\n<pre><code class=\"language-ts\">// build-ssg.ts L349-368\n// 先在 bundle scope 初始化\nawait module.initBlogData(blogOptions);\n// 再在 file system scope 初始化（因为 await import() 创建了新的模块实例）\nconst fsContentModule = await import(&#39;@openelement/content&#39;);\nawait fsContentModule.initBlogData(blogOptions);\n</code></pre>\n<ul>\n<li><code>await import(&#39;@openelement/content&#39;)</code> 会创建独立的模块实例（不在 SSR bundle 内）</li>\n<li>需要手动在两个作用域中同步状态 — ADR 0008 要消除的正是这类多实例问题</li>\n<li>如果路由模块依赖虚拟模块（如 <code>virtual:less-nav</code>），<code>native import()</code> 直接失败</li>\n</ul>\n<h3>根本原因：抽象边界错误</h3>\n<pre><code>┌─────────────────────────────────────┐\n│        SSR Bundle (entry.js)       │\n│  ┌───────────────────────────────┐  │\n│  │  customElements registry      │  │\n│  │  renderDSD()                   │  │\n│  │  wrapInDocument()              │  │\n│  │  所有路由模块 + 组件类           │  │\n│  └───────────────────────────────┘  │\n│         ↑ 被外部越权读取/调用        │\n└─────────────────────────────────────┘\n          ↑\n┌─────────────────────────────────────┐\n│       build-ssg.ts (外部)           │\n│  globalThis.customElements.get()    │\n│  readFileSync + regex (提取 tagName) │\n│  renderDSDFn() + wrapInDocumentFn() │\n│  customElements.define 幂等补丁     │\n└─────────────────────────────────────┘\n</code></pre>\n<p><code>build-ssg.ts</code> 扮演了两个角色：</p>\n<ol>\n<li><strong>编排者</strong>：决定渲染什么路径、写到哪个文件 ✅</li>\n<li><strong>渲染执行者</strong>：查找组件、调用渲染、包装 HTML ❌</li>\n</ol>\n<p>角色 2 应该由 SSR bundle 内部承担。</p>\n<h2>Decision</h2>\n<p><strong>将渲染能力封装为 SSR bundle 的显式导出</strong>，让 <code>build-ssg.ts</code> 只做编排（路径枚举 + 文件写入），不再进入 bundle 内部。</p>\n<h3>新的抽象边界</h3>\n<pre><code>┌─────────────────────────────────────┐\n│        SSR Bundle (entry.js)       │\n│                                     │\n│  内部：                              │\n│    customElements registry (Web API)│\n│    renderDSD()                       │\n│    wrapInDocument()                  │\n│    所有路由模块 + 组件类               │\n│    getStaticPaths() per route        │\n│                                     │\n│  导出（显式 API）：                    │\n│    renderRoute(path, opts) → HTML   │\n│    getStaticPaths(path) → params[]   │\n│    routeInfo[]                       │\n└─────────────────────────────────────┘\n          ↓\n┌─────────────────────────────────────┐\n│       build-ssg.ts (外部)           │\n│                                     │\n│  只做编排：                            │\n│    1. 哪些路径需要渲染？               │\n│    2. 哪些 locale 需要扩展？          │\n│    3. 结果写到哪个文件？               │\n│                                     │\n│  调用 bundle API：                    │\n│    ssrBundle.renderRoute(path, opts) │\n│    ssrBundle.getStaticPaths(path)    │\n└─────────────────────────────────────┘\n</code></pre>\n<h3>导出的 API 设计</h3>\n<pre><code class=\"language-typescript\">// SSR bundle 导出\n\n/** 路由信息 — bundle 知道自己包含的所有路由 */\nexport interface RouteInfo {\n  /** URL 模式，如 &#39;/blog/:slug&#39; 或 &#39;/guide/dsd&#39; */\n  path: string;\n  /** 注册的 Custom Element tag name */\n  tagName: string;\n  /** 是否为动态路由 */\n  isDynamic: boolean;\n  /** 动态路由的参数名 */\n  paramNames: string[];\n}\n\nexport const routeInfo: RouteInfo[];\n\n/**\n * 渲染一个路由页面为完整 HTML。\n *\n * 内部使用 customElements.get() + renderDSD() + wrapInDocument()。\n * 调用者不需要知道组件类、tagName、或渲染细节。\n *\n * @param routePath - 路由路径模式（如 &#39;/blog/:slug&#39;）\n * @param options - 渲染选项\n * @returns 完整的 HTML 文档字符串\n */\nexport async function renderRoute(\n  routePath: string,\n  options?: {\n    /** 动态路由参数（如 { slug: &#39;v0-8-0&#39; }） */\n    params?: Record&lt;string, string&gt;;\n    /** i18n locale（如 &#39;zh&#39;） */\n    locale?: string;\n    /** HTML &lt;title&gt; */\n    title?: string;\n    /** HTML lang attribute */\n    lang?: string;\n    /** 额外 &lt;head&gt; 内容 */\n    headExtras?: string;\n  },\n): Promise&lt;string&gt;;\n\n/**\n * 获取动态路由的静态路径列表。\n *\n * 内部调用路由模块的 getStaticPaths()。\n * 只对 isDynamic: true 的路由有效。\n *\n * @param routePath - 路由路径模式（如 &#39;/blog/:slug&#39;）\n * @returns 参数组合列表\n */\nexport async function getStaticPaths(\n  routePath: string,\n): Promise&lt;Array&lt;Record&lt;string, string&gt;&gt;&gt;;\n</code></pre>\n<h3>关键设计决策</h3>\n<h4>为什么导出 <code>renderRoute(path)</code> 而不是 <code>renderRoute(tagName)</code></h4>\n<p><code>build-ssg.ts</code> 工作的粒度是<strong>路径</strong>（path），不是 tagName。它遍历路由列表，对每个路径+locale 组合生成页面。tagName 是 bundle 内部的实现细节——调用者不应该需要知道它。</p>\n<p>这也使得未来可以改变内部注册机制（比如不用 customElements 而用其他注册方式），而不影响调用方。</p>\n<h4>为什么 <code>customElements</code> 是 Web 标准 API 的正确用法</h4>\n<p>在 ADR 讨论中，我们反复问&quot;能不能干掉 <code>customElements</code>？&quot;。答案是：</p>\n<ul>\n<li><strong>在 bundle 外部</strong>：不应该直接使用。这是 ADR 0014 要消除的。</li>\n<li><strong>在 bundle 内部</strong>：应该正常使用。Custom Elements Registry 是 Web Components 的标准基础设施，就如同 <code>document.createElement</code> 或 <code>fetch</code> 一样。</li>\n</ul>\n<p>类比：你不会因为&quot;用了 <code>document.createElement</code>&quot;就觉得需要干掉它。<code>customElements</code> 在 bundle 内部的角色完全相同——它是 Web 平台提供的服务定位器（Service Locator），不是我们自己的全局变量。</p>\n<h4>为什么不导出 N 个组件</h4>\n<p>导出 <code>routeRegistry = { &#39;page-dsd-guide&#39;: DsdGuidePage, ... }</code> 的问题是：</p>\n<ul>\n<li>N 随路由增长，接口不稳定</li>\n<li>暴露了内部实现（组件类）</li>\n<li>调用者仍需自己做 tagName → ComponentClass 映射 + 渲染调用</li>\n</ul>\n<p><code>renderRoute()</code> 封装了所有这些——接口大小为 1，永远不变。</p>\n<h4>动态路由的 getStaticPaths() 如何处理</h4>\n<p>当前 <code>build-ssg.ts</code> 用 <code>await import(routeImportPath)</code> 加载动态路由模块来调用 <code>getStaticPaths()</code>。但路由模块可能依赖虚拟模块（如 <code>virtual:less-nav</code>），导致 native import 失败。</p>\n<p>在新设计中，<code>getStaticPaths()</code> 从 bundle 内部获取——bundle 已经内联了所有路由模块，包括 <code>getStaticPaths</code> 函数。bundle 的 <code>getStaticPaths(routePath)</code> 导出在内部查找对应的路由模块并调用它。</p>\n<p>这也解决了博客数据双作用域初始化问题：<code>getStaticPaths()</code> 在 bundle 内部执行时，自然使用 bundle 内的博客数据（同一模块作用域），无需外部手动初始化。</p>\n<h3>与已有 API 的关系</h3>\n<p>LessJS 已有 <code>renderDSDByName(tagName, props)</code> —— 它通过 <code>customElements.get(tagName)</code> 查找组件并渲染。<code>renderRoute(path, opts)</code> 是更高层的封装：</p>\n<pre><code>renderDSD(tagName, Cls, props)          ← 底层：已知组件类\nrenderDSDByName(tagName, props)         ← 中层：通过 registry 查组件类\nrenderRoute(path, opts)                 ← 高层：通过路径找组件 + 渲染 + 包装\n</code></pre>\n<p>三层共存在 bundle 内，各司其职。外部只调用高层 API。</p>\n<h3>代码变更</h3>\n<h4>1. <code>entry-renderer.ts</code> — 生成 routeInfo + renderRoute + getStaticPaths</h4>\n<p>在 SSG 模式下，在现有的 &quot;SSG Utility Re-exports&quot; 之后追加：</p>\n<pre><code class=\"language-ts\">// --- Route info (build-ssg.ts needs this for path enumeration) ---\nexport const routeInfo = ${JSON.stringify(pageRoutes.map(r =&gt; ({\n  path: r.path,\n  tagName: r.tagName || r.defaultTagName,  // 编译时已知\n  isDynamic: r.isDynamic,\n  paramNames: r.paramNames,\n})))};\n\n// --- renderRoute: path → HTML (DSD-first) ---\nexport async function renderRoute(routePath, options = {}) {\n  const info = routeInfo.find(r =&gt; r.path === routePath);\n  if (!info) throw new Error(&#39;Route not found: &#39; + routePath);\n\n  const { params = {}, locale, title = &#39;LessJS&#39;, lang = locale || &#39;en&#39;, headExtras = __headExtras || &#39;&#39; } = options;\n\n  const html = await renderDSDByName(info.tagName, { ...params, locale }, { route: routePath, source: info.tagName });\n  return wrapInDocument(html, { title, lang, headExtras });\n}\n\n// --- getStaticPaths: dynamic route → params[] ---\nexport async function getStaticPaths(routePath) {\n  const info = routeInfo.find(r =&gt; r.path === routePath);\n  if (!info || !info.isDynamic) return [];\n\n  // 动态路由模块的 getStaticPaths 在 bundle 内已被内联\n  // 直接查找并调用\n  ${dynamicRoutes.map(r =&gt; {\n    return `if (routePath === &#39;${r.path}&#39;) return await ${r.varName}.getStaticPaths();`;\n  }).join(&#39;\\n  &#39;)}\n  return [];\n}\n</code></pre>\n<h4>2. <code>build-ssg.ts</code> — 移除所有越权代码</h4>\n<p><strong>删除</strong>：</p>\n<ul>\n<li>正则提取 tagName（L556-566）</li>\n<li><code>globalThis.customElements.get(tagName)</code> (L567-569)</li>\n<li>直接调用 <code>renderDSDFn()</code> + <code>wrapInDocumentFn()</code> (L619-631, L433-442)</li>\n<li><code>customElements.define</code> 幂等补丁 (L304-321)</li>\n<li>博客数据双作用域初始化 (L346-368)</li>\n<li><code>await import(&#39;@openelement/i18n&#39;)</code> 初始化 (L535-542)</li>\n<li>动态路由的 <code>await import(routeImportPath)</code> (L387-388)</li>\n</ul>\n<p><strong>替换为</strong>：</p>\n<pre><code class=\"language-ts\">const { renderRoute, getStaticPaths, routeInfo } = module;\n\n// i18n locale expansion\nfor (const locale of locales) {\n  for (const route of routeInfo) {\n    if (route.isDynamic) {\n      const paramsList = await getStaticPaths(route.path);\n      for (const params of paramsList) {\n        const html = await renderRoute(route.path, { params, locale, ... });\n        // write to disk\n      }\n    } else {\n      const html = await renderRoute(route.path, { locale, ... });\n      // write to disk\n    }\n  }\n}\n</code></pre>\n<h4>3. <code>customElements.define</code> 幂等补丁 — 移入 bundle 内部</h4>\n<p>在 <code>entry-renderer.ts</code> 生成的代码中，在 <code>customElements.define</code> 循环之前插入幂等补丁：</p>\n<pre><code class=\"language-ts\">// Patch CustomElementRegistry.define to be idempotent in SSR\nconst _origDefine = customElements.define.bind(customElements);\ncustomElements.define = (name, ctor, options) =&gt; {\n  if (customElements.get(name)) return;\n  try {\n    _origDefine(name, ctor, options);\n  } catch { /* already defined */ }\n};\n</code></pre>\n<p>这样补丁在 bundle 内部执行，只影响 bundle 自己的注册行为，不污染外部 realm。</p>\n<h2>Consequences</h2>\n<h3>Positive</h3>\n<ul>\n<li><strong>彻底消除 globalThis 越权访问</strong>：<code>build-ssg.ts</code> 不再直接读取 <code>customElements</code> registry</li>\n<li><strong>消除正则源码解析</strong>：tagName 从 bundle 的显式导出获取，而非逆向工程</li>\n<li><strong>抽象边界清晰</strong>：<code>build-ssg.ts</code> 只做编排（路径 + 文件），bundle 只做渲染（组件 + HTML）</li>\n<li><strong>customElements 在正确位置使用</strong>：bundle 内部正常使用 Web 标准 API，外部不需要碰它</li>\n<li><strong>消除双作用域初始化</strong>：博客/i18n 数据只在 bundle 作用域初始化一次</li>\n<li><strong>renderRoute 接口稳定</strong>：1 个函数，不随路由数量增长</li>\n<li><strong>未来可扩展</strong>：renderer chain、head 管理等渲染逻辑变更只影响 bundle 内部</li>\n<li><strong>DSD-first 对齐</strong>：&quot;给我路径，给我 locale，我给你 HTML&quot; — 这才是 DSD-first 框架该有的接口</li>\n</ul>\n<h3>Negative</h3>\n<ul>\n<li><strong>SSR bundle 体积微增</strong>：新增 <code>routeInfo</code> 常量 + <code>renderRoute</code> + <code>getStaticPaths</code> 函数。预计增加 &lt; 2KB，可忽略</li>\n<li><strong>renderRoute 丢失细粒度控制</strong>：调用者不能自定义 <code>renderDSD</code> 的 <code>dsdOptions</code> 参数<ul>\n<li><strong>缓解</strong>：可在 <code>renderRoute</code> 的 options 中增加 <code>dsdOptions</code> 透传；当前无使用场景</li>\n</ul>\n</li>\n<li><strong>getStaticPaths 对动态路由硬编码 if/else</strong>：每个动态路由生成一个 <code>if (routePath === ...)</code> 分支<ul>\n<li><strong>缓解</strong>：动态路由数量通常 &lt; 10，分支可接受；也可改为 routePath → module 的 map</li>\n</ul>\n</li>\n</ul>\n<h3>Neutral</h3>\n<ul>\n<li><code>toSSG(app, ...)</code> 仍然用于默认 locale 的静态路由渲染 — 这个用法正确且不变</li>\n<li>Hono app 对象仍然从 bundle 导出 — <code>build-ssg.ts</code> 用它调 <code>toSSG()</code></li>\n<li><code>renderDSD</code> / <code>renderDSDByName</code> / <code>wrapInDocument</code> 仍从 bundle 导出 — 供高级用例使用，但 <code>renderRoute</code> 是推荐入口</li>\n</ul>\n<h2>类似问题审计</h2>\n<p>在审查 <code>build-ssg.ts</code> 的越权模式时，发现以下同类问题：</p>\n<h3>已在本 ADR 中解决</h3>\n<table>\n<thead>\n<tr>\n<th>#</th>\n<th>问题</th>\n<th>当前位置</th>\n<th>修复方式</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>1</td>\n<td><code>globalThis.customElements.get()</code></td>\n<td>build-ssg.ts L567</td>\n<td>→ bundle 内部的 renderRoute()</td>\n</tr>\n<tr>\n<td>2</td>\n<td><code>readFileSync</code> + 正则提取 tagName</td>\n<td>build-ssg.ts L558-564</td>\n<td>→ bundle 导出 routeInfo</td>\n</tr>\n<tr>\n<td>3</td>\n<td>直接调用 renderDSD + wrapInDocument</td>\n<td>build-ssg.ts L619-631</td>\n<td>→ bundle 内部的 renderRoute()</td>\n</tr>\n<tr>\n<td>4</td>\n<td>customElements.define 幂等补丁</td>\n<td>build-ssg.ts L304-321</td>\n<td>→ 移入 bundle 内部</td>\n</tr>\n<tr>\n<td>5</td>\n<td>博客数据双作用域初始化</td>\n<td>build-ssg.ts L346-368</td>\n<td>→ bundle 内部的 getStaticPaths()</td>\n</tr>\n<tr>\n<td>6</td>\n<td><code>await import(&#39;@openelement/i18n&#39;)</code> 初始化</td>\n<td>build-ssg.ts L535-542</td>\n<td>→ bundle 内部处理</td>\n</tr>\n<tr>\n<td>7</td>\n<td>动态路由 <code>await import(routeImportPath)</code></td>\n<td>build-ssg.ts L387-388</td>\n<td>→ bundle 内部的 getStaticPaths()</td>\n</tr>\n</tbody></table>\n<h3>值得关注但不在本 ADR 范围</h3>\n<table>\n<thead>\n<tr>\n<th>#</th>\n<th>问题</th>\n<th>位置</th>\n<th>建议</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>8</td>\n<td><code>await import(&#39;@openelement/content/sitemap&#39;)</code> 独立导入</td>\n<td>build-ssg.ts L851</td>\n<td>可改为 bundle 导出，与 renderRoute 同源</td>\n</tr>\n<tr>\n<td>9</td>\n<td>客户端 manifest 读取 + 脚本注入</td>\n<td>build-ssg.ts L676-698</td>\n<td>纯文件 I/O，无越权，暂不处理</td>\n</tr>\n<tr>\n<td>10</td>\n<td>PWA manifest + service worker 生成</td>\n<td>build-ssg.ts L769-845</td>\n<td>纯文件生成，无越权，暂不处理</td>\n</tr>\n</tbody></table>\n<h2>参考</h2>\n<ul>\n<li><a href=\"/blog/0008-eliminate-createserver-globalthis-bridges\" rel=\"noopener noreferrer\">ADR 0008: 消除 createServer()、.less/ 临时文件与 globalThis 桥接</a></li>\n<li><a href=\"/blog/0010-eliminate-all-dot-less-temp-files\" rel=\"noopener noreferrer\">ADR 0010: 消除所有 .less/ 临时文件</a></li>\n<li><a href=\"/blog/0011-eliminate-last-globalthis-via-closebundle\" rel=\"noopener noreferrer\">ADR 0011: 消除最后一个 globalThis 桥接</a></li>\n<li><code>packages/core/src/render-dsd.ts</code> — <code>renderDSDByName()</code> 已有的 registry 查找模式</li>\n<li><code>packages/core/src/entry-renderer.ts</code> — SSR bundle 代码生成</li>\n</ul>\n<hr />\n<p><em>决策日期: 2026-05-10 | 版本: v0.10.0</em></p>\n"
  },
  {
    "slug": "0013-eliminate-less-runtime-barrel-inline-shim",
    "frontmatter": {
      "title": "ADR 0013: 消除 less-runtime barrel、删除 virtual:less-runtime、移除 .less/ 残留",
      "date": "2026-05-10",
      "draft": false,
      "tags": [
        "architecture",
        "decision"
      ],
      "type": "adr",
      "lang": "zh"
    },
    "content": "\n## Status\n\n**IMPLEMENTED** — v0.9.x 架构清理\n\n## Context\n\n### 四项决策\n\n1. **D4 修正**：`.less/` 在脚手架 `.gitignore` 模板和仓库 `.gitignore` 中的残留应**直接删除**，不做向后兼容。ADR 0010 已消除所有 `.less/` 写入，新项目不应该知道它曾经存在。\n\n2. **彻底删除 `virtual:less-runtime`**：消费者已全部改为从源文件直接导入后，没有任何代码 import `@openelement/core/less-runtime`。`virtual:less-runtime` 的 Vite 别名、resolveId、load 钩子全部成为死代码。`registerAdapter()` 和 `renderDSD()` 在 SSR 构建中通过同一个 Vite bundle 天然共享模块作用域，不再需要虚拟模块来保证。\n\n3. **删除 `runtime-shim.ts` 和生成器**：`virtual:less-runtime` 删除后，`runtime-shim.ts` 和 `generate-runtime-shim.ts` 也不再被任何代码引用，一并删除。\n\n4. **less-runtime.ts barrel 删除，消费者直连源文件**：`less-runtime.ts` 是 19 行的 barrel re-export 文件，仅转发 `adapter-registry`、`render-dsd`、`ssr-handler`、`logger` 的导出。删除后所有消费者改为从源文件直接导入。\n\n### 之前架构（已消除）\n\n```\ngenerate-runtime-shim.ts (229行脚本)\n  → 输出 runtime-shim.ts (3行包装, 含 JS 字符串)\n  → index.ts import { createRuntimeShimCode } from './runtime-shim.js'\n  → virtual:less-runtime 的 load() 返回 createRuntimeShimCode()\n\nless-runtime.ts (19行 barrel)\n  → re-export from adapter-registry, render-dsd, ssr-handler, logger\n  → 被消费者 import { xxx } from '@openelement/core/less-runtime'\n\nvirtual:less-runtime (Vite 虚拟模块)\n  → config() 别名: '@openelement/core/less-runtime' → 'virtual:less-runtime'\n  → resolveId() 解析虚拟 ID\n  → load() SSR 分支: re-export from @openelement/core\n  → load() 客户端分支: 返回 createRuntimeShimCode()\n\nbuild-ssg.ts 中的 less:ssg-virtual-runtime 插件\n  → 复制了 index.ts 中的 virtual:less-runtime 逻辑\n```\n\n### 当前架构（ADR 0013 后）\n\n```\n消费者直接 import from '@openelement/core/render-dsd' 等\n  → Vite 自然解析，SSR 构建打包进同一个 bundle\n  → 模块作用域共享通过 bundle 机制保证，无需虚拟模块\n```\n\n## Decision\n\n### 1. 移除所有 `.less/` 引用\n\n- `packages/create/cli.ts`：脚手架 `.gitignore` 模板删除 `.less/` 行\n- `.gitignore`（仓库级）：删除 `.less/` 行\n- `deno.json` clean task：删除 `docs/.less`\n\n### 2. 彻底删除 `virtual:less-runtime`\n\n**`packages/core/src/index.ts`**：\n\n- 删除 `'@openelement/core/less-runtime': VIRTUAL_RUNTIME_ID` 别名\n- 删除 `VIRTUAL_RUNTIME_ID` 和 `RESOLVED_RUNTIME_ID` 常量\n- 删除 `resolveId()` 中对 `VIRTUAL_RUNTIME_ID` 的解析\n- 删除 `load()` 中对 `RESOLVED_RUNTIME_ID` 的处理\n- 删除 `readFileSync`、`dirname` 等不再需要的 import\n\n**`packages/core/src/cli/build-ssg.ts`**：\n\n- 删除整个 `less:ssg-virtual-runtime` 插件\n\n### 3. 删除 `runtime-shim.ts` 和生成器\n\n- 删除 `packages/core/src/runtime-shim.ts`\n- 删除 `packages/core/scripts/generate-runtime-shim.ts`\n- 删除 `deno.json` 中的 `generate:runtime-shim` task\n- 不需要替代的 `generate:client-shim`（virtual module 删除后无消费者）\n\n### 4. less-runtime.ts → 删除，消费者直连源文件\n\n- 删除 `packages/core/src/less-runtime.ts`\n- 删除 `packages/core/deno.json` 中的 `\"./less-runtime\"` 和 `\"./less-runtime.js\"` subpath\n- 新增 `\"./adapter-registry\"` 和 `\"./ssr-handler\"` subpath exports\n- 更新所有消费者：\n\n| 消费者                              | 之前                                                              | 之后                                                                                                                                                    |\n| ----------------------------------- | ----------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |\n| `entry-descriptor.ts`               | `from '@openelement/core/less-runtime'`                                | `from '@openelement/core/render-dsd'`                                                                                                                        |\n| `entry-renderer.ts` (log+wrap)      | `import { log, wrapInDocument } from '@openelement/core/less-runtime'` | `import { wrapInDocument } from '@openelement/core/ssr-handler'` + `import { createLogger } from '@openelement/core/logger'` + `const log = createLogger('core')` |\n| `entry-renderer.ts` (SSG re-export) | `from \"@openelement/core/less-runtime\"`                                | 拆为 `from \"@openelement/core/ssr-handler\"` + `from \"@openelement/core/adapter-registry\"`                                                                         |\n| `adapter-lit/ssr.ts`                | `from '@openelement/core/less-runtime'`                                | `from '@openelement/core/adapter-registry'`                                                                                                                  |\n| `create/cli.ts`                     | `\"@openelement/core/less-runtime\": \"jsr:...\"`                          | `\"@openelement/core/adapter-registry\": \"jsr:...\"`                                                                                                            |\n\n### 5. types.ts 值导出清理（D2，与 ADR 合并执行）\n\n- 删除 `types.ts` 的 `export { getAdapter, registerAdapter } from './adapter-registry.js'`\n- 修改 `index.ts` 改为从 `adapter-registry.js` 直接导入\n\n## Consequences\n\n### Positive\n\n- 消除 4 个文件（`less-runtime.ts`、`runtime-shim.ts`、`generate-runtime-shim.ts`、`generated/client-shim.js`）\n- 消除 `virtual:less-runtime` 整套机制（Vite 别名 + resolveId + load + SSG 插件）\n- 消除 `@openelement/core/less-runtime` 这个模糊的 import 路径\n- `.less/` 完全从项目记忆中抹除\n- types.ts 回归纯类型定义的职责\n- `index.ts` 不再需要 `readFileSync`、`dirname` 等 Node API import\n- 构建管道更简单：少一个 Vite 插件，少一个生成步骤\n\n### Negative\n\n- `adapter-registry` 和 `ssr-handler` 需新增为 public subpath export\n- `entry-renderer.ts` 的 import 从 1 行变 3 行（拆分 log 和 wrapInDocument）\n- 客户端构建不再有 lightweight shim——但由于客户端构建不使用 `renderDSD`/`registerAdapter`（仅 SSR 构建使用），无实际影响\n\n### Neutral\n\n- SSR 构建中 `registerAdapter()` 与 `renderDSD()` 的模块作用域共享通过 Vite bundle 机制自然保证，无需虚拟模块\n\n## 参考\n\n- [ADR 0008: 消除 createServer()、.less/ 临时文件与 globalThis 桥接](/blog/0008-eliminate-createserver-globalthis-bridges)\n- [ADR 0010: 消除所有 .less/ 临时文件](/blog/0010-eliminate-all-dot-less-temp-files)\n\n---\n\n_决策日期: 2026-05-10 | 版本: v0.9.x | 实施日期: 2026-05-10_\n",
    "html": "<h2>Status</h2>\n<p><strong>IMPLEMENTED</strong> — v0.9.x 架构清理</p>\n<h2>Context</h2>\n<h3>四项决策</h3>\n<ol>\n<li><p><strong>D4 修正</strong>：<code>.less/</code> 在脚手架 <code>.gitignore</code> 模板和仓库 <code>.gitignore</code> 中的残留应<strong>直接删除</strong>，不做向后兼容。ADR 0010 已消除所有 <code>.less/</code> 写入，新项目不应该知道它曾经存在。</p>\n</li>\n<li><p><strong>彻底删除 <code>virtual:less-runtime</code></strong>：消费者已全部改为从源文件直接导入后，没有任何代码 import <code>@openelement/core/less-runtime</code>。<code>virtual:less-runtime</code> 的 Vite 别名、resolveId、load 钩子全部成为死代码。<code>registerAdapter()</code> 和 <code>renderDSD()</code> 在 SSR 构建中通过同一个 Vite bundle 天然共享模块作用域，不再需要虚拟模块来保证。</p>\n</li>\n<li><p><strong>删除 <code>runtime-shim.ts</code> 和生成器</strong>：<code>virtual:less-runtime</code> 删除后，<code>runtime-shim.ts</code> 和 <code>generate-runtime-shim.ts</code> 也不再被任何代码引用，一并删除。</p>\n</li>\n<li><p><strong>less-runtime.ts barrel 删除，消费者直连源文件</strong>：<code>less-runtime.ts</code> 是 19 行的 barrel re-export 文件，仅转发 <code>adapter-registry</code>、<code>render-dsd</code>、<code>ssr-handler</code>、<code>logger</code> 的导出。删除后所有消费者改为从源文件直接导入。</p>\n</li>\n</ol>\n<h3>之前架构（已消除）</h3>\n<pre><code>generate-runtime-shim.ts (229行脚本)\n  → 输出 runtime-shim.ts (3行包装, 含 JS 字符串)\n  → index.ts import { createRuntimeShimCode } from &#39;./runtime-shim.js&#39;\n  → virtual:less-runtime 的 load() 返回 createRuntimeShimCode()\n\nless-runtime.ts (19行 barrel)\n  → re-export from adapter-registry, render-dsd, ssr-handler, logger\n  → 被消费者 import { xxx } from &#39;@openelement/core/less-runtime&#39;\n\nvirtual:less-runtime (Vite 虚拟模块)\n  → config() 别名: &#39;@openelement/core/less-runtime&#39; → &#39;virtual:less-runtime&#39;\n  → resolveId() 解析虚拟 ID\n  → load() SSR 分支: re-export from @openelement/core\n  → load() 客户端分支: 返回 createRuntimeShimCode()\n\nbuild-ssg.ts 中的 less:ssg-virtual-runtime 插件\n  → 复制了 index.ts 中的 virtual:less-runtime 逻辑\n</code></pre>\n<h3>当前架构（ADR 0013 后）</h3>\n<pre><code>消费者直接 import from &#39;@openelement/core/render-dsd&#39; 等\n  → Vite 自然解析，SSR 构建打包进同一个 bundle\n  → 模块作用域共享通过 bundle 机制保证，无需虚拟模块\n</code></pre>\n<h2>Decision</h2>\n<h3>1. 移除所有 <code>.less/</code> 引用</h3>\n<ul>\n<li><code>packages/create/cli.ts</code>：脚手架 <code>.gitignore</code> 模板删除 <code>.less/</code> 行</li>\n<li><code>.gitignore</code>（仓库级）：删除 <code>.less/</code> 行</li>\n<li><code>deno.json</code> clean task：删除 <code>docs/.less</code></li>\n</ul>\n<h3>2. 彻底删除 <code>virtual:less-runtime</code></h3>\n<p><strong><code>packages/core/src/index.ts</code></strong>：</p>\n<ul>\n<li>删除 <code>&#39;@openelement/core/less-runtime&#39;: VIRTUAL_RUNTIME_ID</code> 别名</li>\n<li>删除 <code>VIRTUAL_RUNTIME_ID</code> 和 <code>RESOLVED_RUNTIME_ID</code> 常量</li>\n<li>删除 <code>resolveId()</code> 中对 <code>VIRTUAL_RUNTIME_ID</code> 的解析</li>\n<li>删除 <code>load()</code> 中对 <code>RESOLVED_RUNTIME_ID</code> 的处理</li>\n<li>删除 <code>readFileSync</code>、<code>dirname</code> 等不再需要的 import</li>\n</ul>\n<p><strong><code>packages/core/src/cli/build-ssg.ts</code></strong>：</p>\n<ul>\n<li>删除整个 <code>less:ssg-virtual-runtime</code> 插件</li>\n</ul>\n<h3>3. 删除 <code>runtime-shim.ts</code> 和生成器</h3>\n<ul>\n<li>删除 <code>packages/core/src/runtime-shim.ts</code></li>\n<li>删除 <code>packages/core/scripts/generate-runtime-shim.ts</code></li>\n<li>删除 <code>deno.json</code> 中的 <code>generate:runtime-shim</code> task</li>\n<li>不需要替代的 <code>generate:client-shim</code>（virtual module 删除后无消费者）</li>\n</ul>\n<h3>4. less-runtime.ts → 删除，消费者直连源文件</h3>\n<ul>\n<li>删除 <code>packages/core/src/less-runtime.ts</code></li>\n<li>删除 <code>packages/core/deno.json</code> 中的 <code>&quot;./less-runtime&quot;</code> 和 <code>&quot;./less-runtime.js&quot;</code> subpath</li>\n<li>新增 <code>&quot;./adapter-registry&quot;</code> 和 <code>&quot;./ssr-handler&quot;</code> subpath exports</li>\n<li>更新所有消费者：</li>\n</ul>\n<table>\n<thead>\n<tr>\n<th>消费者</th>\n<th>之前</th>\n<th>之后</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>entry-descriptor.ts</code></td>\n<td><code>from &#39;@openelement/core/less-runtime&#39;</code></td>\n<td><code>from &#39;@openelement/core/render-dsd&#39;</code></td>\n</tr>\n<tr>\n<td><code>entry-renderer.ts</code> (log+wrap)</td>\n<td><code>import { log, wrapInDocument } from &#39;@openelement/core/less-runtime&#39;</code></td>\n<td><code>import { wrapInDocument } from &#39;@openelement/core/ssr-handler&#39;</code> + <code>import { createLogger } from &#39;@openelement/core/logger&#39;</code> + <code>const log = createLogger(&#39;core&#39;)</code></td>\n</tr>\n<tr>\n<td><code>entry-renderer.ts</code> (SSG re-export)</td>\n<td><code>from &quot;@openelement/core/less-runtime&quot;</code></td>\n<td>拆为 <code>from &quot;@openelement/core/ssr-handler&quot;</code> + <code>from &quot;@openelement/core/adapter-registry&quot;</code></td>\n</tr>\n<tr>\n<td><code>adapter-lit/ssr.ts</code></td>\n<td><code>from &#39;@openelement/core/less-runtime&#39;</code></td>\n<td><code>from &#39;@openelement/core/adapter-registry&#39;</code></td>\n</tr>\n<tr>\n<td><code>create/cli.ts</code></td>\n<td><code>&quot;@openelement/core/less-runtime&quot;: &quot;jsr:...&quot;</code></td>\n<td><code>&quot;@openelement/core/adapter-registry&quot;: &quot;jsr:...&quot;</code></td>\n</tr>\n</tbody></table>\n<h3>5. types.ts 值导出清理（D2，与 ADR 合并执行）</h3>\n<ul>\n<li>删除 <code>types.ts</code> 的 <code>export { getAdapter, registerAdapter } from &#39;./adapter-registry.js&#39;</code></li>\n<li>修改 <code>index.ts</code> 改为从 <code>adapter-registry.js</code> 直接导入</li>\n</ul>\n<h2>Consequences</h2>\n<h3>Positive</h3>\n<ul>\n<li>消除 4 个文件（<code>less-runtime.ts</code>、<code>runtime-shim.ts</code>、<code>generate-runtime-shim.ts</code>、<code>generated/client-shim.js</code>）</li>\n<li>消除 <code>virtual:less-runtime</code> 整套机制（Vite 别名 + resolveId + load + SSG 插件）</li>\n<li>消除 <code>@openelement/core/less-runtime</code> 这个模糊的 import 路径</li>\n<li><code>.less/</code> 完全从项目记忆中抹除</li>\n<li>types.ts 回归纯类型定义的职责</li>\n<li><code>index.ts</code> 不再需要 <code>readFileSync</code>、<code>dirname</code> 等 Node API import</li>\n<li>构建管道更简单：少一个 Vite 插件，少一个生成步骤</li>\n</ul>\n<h3>Negative</h3>\n<ul>\n<li><code>adapter-registry</code> 和 <code>ssr-handler</code> 需新增为 public subpath export</li>\n<li><code>entry-renderer.ts</code> 的 import 从 1 行变 3 行（拆分 log 和 wrapInDocument）</li>\n<li>客户端构建不再有 lightweight shim——但由于客户端构建不使用 <code>renderDSD</code>/<code>registerAdapter</code>（仅 SSR 构建使用），无实际影响</li>\n</ul>\n<h3>Neutral</h3>\n<ul>\n<li>SSR 构建中 <code>registerAdapter()</code> 与 <code>renderDSD()</code> 的模块作用域共享通过 Vite bundle 机制自然保证，无需虚拟模块</li>\n</ul>\n<h2>参考</h2>\n<ul>\n<li><a href=\"/blog/0008-eliminate-createserver-globalthis-bridges\" rel=\"noopener noreferrer\">ADR 0008: 消除 createServer()、.less/ 临时文件与 globalThis 桥接</a></li>\n<li><a href=\"/blog/0010-eliminate-all-dot-less-temp-files\" rel=\"noopener noreferrer\">ADR 0010: 消除所有 .less/ 临时文件</a></li>\n</ul>\n<hr />\n<p><em>决策日期: 2026-05-10 | 版本: v0.9.x | 实施日期: 2026-05-10</em></p>\n"
  },
  {
    "slug": "0012-extract-lessjs-umbrella-to-app-package",
    "frontmatter": {
      "title": "ADR 0012: 将 lessjs() 统一入口从 @openelement/core 拆到 @openelement/app",
      "date": "2026-05-10",
      "draft": false,
      "tags": [
        "architecture",
        "decision"
      ],
      "type": "adr",
      "lang": "zh"
    },
    "content": "\n## Status\n\n**ACCEPTED** — v0.9.x 架构简化\n\n## Context\n\n`lessjs()` 是 LessJS 的统一 Vite 插件入口，将 `less()` + `lessContent()` + `lessI18n()` 组合在一个调用中，通过显式传递 `LessBuildContext` 避免了 globalThis 桥接。\n\n当前 `lessjs()` 放在 `@openelement/core` 中（`packages/core/src/index.ts` L349-396），使用动态 `import()` 加载 `@openelement/content` 和 `@openelement/i18n`：\n\n```ts\n// 当前：core 里动态 import 上层包\nexport async function lessjs(options = {}) {\n  const ctx = new LessBuildContext({...});\n  const plugins = [...less(coreOpts, ctx)];\n\n  if (contentOpts) {\n    const contentMod = await import('@openelement/content');  // 动态 import\n    plugins.push(...contentMod.lessContent({...contentOpts, ctx}));\n  }\n\n  if (i18nOpts) {\n    const i18nMod = await import('@openelement/i18n');  // 动态 import\n    plugins.push(i18nMod.lessI18n({...i18nOpts, ctx}));\n  }\n\n  return plugins;\n}\n```\n\n### 问题：依赖方向倒挂\n\n`@openelement/core` 是基础层——`content` 和 `i18n` 都依赖 core。但 `lessjs()` 在 core 里 import content 和 i18n，形成概念上的循环依赖：\n\n```\ncontent → core → content   ← 概念倒挂（动态 import 打断了物理循环）\ni18n    → core → i18n       ← 同上\n```\n\n动态 `import()` 虽然避免了运行时循环依赖错误，但：\n\n- **语义不清**：core 不应该知道 content/i18n 的存在\n- **try/catch 降级**：必须用 try/catch 处理\"未安装\"的情况，而这是一个正常的包管理问题\n- **类型丢失**：动态 import 导致类型断言 `as Record<string, unknown>`，丧失编译时类型安全\n\n### 替代方案评估\n\n| 方案                          | 做法                       | 优点                                | 缺点                 |\n| ----------------------------- | -------------------------- | ----------------------------------- | -------------------- |\n| A. 留在 core，保持动态 import | 现状                       | 零改动                              | 概念倒挂，类型不安全 |\n| B. 拆到 @openelement/app           | 新包依赖 core+content+i18n | 依赖方向干净，静态 import，类型安全 | 多一个包             |\n| C. 留在 core，注册表模式      | core 定义 pluginRegistry   | core 不需知道子插件                 | 多一层间接，过度设计 |\n\n## Decision\n\n**选方案 B**：将 `lessjs()` 从 `@openelement/core` 拆到新包 `@openelement/app`。\n\n### 新依赖图\n\n```\n@openelement/app\n  ├── @openelement/core       (静态 import)\n  ├── @openelement/content    (静态 import)\n  └── @openelement/i18n       (静态 import)\n```\n\n依赖方向清晰：app 是\"组装层\"，core 是\"基础层\"，content/i18n 是\"功能层\"。无循环，无倒挂。\n\n### 新发布顺序\n\n```\nrpc → ui → adapter-lit → signal → content → i18n → core → app → create\n```\n\n`@openelement/app` 排在 core 之后、create 之前。\n\n### 代码变更\n\n#### 1. 新建 `packages/app/`\n\n```\npackages/app/\n  ├── deno.json\n  └── src/\n      └── index.ts      ← lessjs() 函数\n```\n\n#### 2. `packages/app/src/index.ts`\n\n```ts\nimport type { Plugin } from 'vite';\nimport type { FrameworkOptions } from '@openelement/core';\nimport type { LessContentOptions } from '@openelement/content';\nimport type { LessI18nOptions } from '@openelement/i18n';\nimport type { LessBuildContext } from '@openelement/core/build-context';\n\nimport { less } from '@openelement/core';\nimport { LessBuildContext as LessBuildContextClass } from '@openelement/core/build-context';\nimport { lessContent } from '@openelement/content';\nimport { lessI18n } from '@openelement/i18n';\nimport { createLogger } from '@openelement/core/logger';\n\nconst log = createLogger('app');\n\nexport interface LessjsOptions extends FrameworkOptions {\n  content?: LessContentOptions;\n  i18n?: LessI18nOptions;\n}\n\nexport async function lessjs(options: LessjsOptions = {}): Promise<Plugin[]> {\n  const { content: contentOpts, i18n: i18nOpts, ...coreOpts } = options;\n  const ctx: LessBuildContext = new LessBuildContextClass({\n    ...coreOpts,\n    routesDir: coreOpts.routesDir || 'app/routes',\n    islandsDir: coreOpts.islandsDir || 'app/islands',\n    componentsDir: coreOpts.componentsDir || 'app/components',\n  });\n\n  const plugins: Plugin[] = [...less(coreOpts, ctx)];\n\n  if (contentOpts) {\n    plugins.push(...lessContent({ ...contentOpts, ctx }));\n  }\n\n  if (i18nOpts) {\n    plugins.push(lessI18n({ ...i18nOpts, ctx }));\n  }\n\n  return plugins;\n}\n\nexport default lessjs;\n```\n\n关键变化：\n\n- **静态 import**：不再需要 `await import()` + `try/catch`\n- **类型安全**：直接 import `lessContent` / `lessI18n`，无需 `as Record<string, unknown>` 断言\n- **安装即用**：如果 `@openelement/content` 未安装，Deno 会在启动时报错，而非运行时静默降级\n\n#### 3. `packages/core/src/index.ts`\n\n- 删除 `lessjs()` 函数（L349-396）\n- 删除 `LessBuildContext` 的顶层 export（保留 `build-context` 子路径导出）\n- `less()` 函数保留 `externalCtx` 参数不变\n\n#### 4. 用户迁移\n\n```ts\n// Before (v0.9.x)\nimport { lessjs } from '@openelement/core';\n\n// After (v0.10.x)\nimport { lessjs } from '@openelement/app';\n```\n\n`less()` 不受影响——仍在 `@openelement/core`。\n\n### 为什么安装失败是正确的行为\n\n当前 `lessjs()` 用 try/catch 静默降级未安装的子插件。这看似友好，但隐藏了配置错误：\n\n- 用户在 `lessjs()` 中传了 `content: { blog: {...} }` 但忘了安装 `@openelement/content`\n- 当前行为：**静默跳过**，不报错，用户困惑\"博客功能为什么没生效\"\n- 新行为：**启动报错** `Cannot find module '@openelement/content'`，问题一目了然\n\n如果用户不想用 content/i18n，**不传对应选项即可**——这是 opt-in 的正确姿势，不需要 try/catch。\n\n## Consequences\n\n### Positive\n\n- **依赖方向正确**：app → core/content/i18n，无倒挂\n- **类型安全**：静态 import 替代动态 import + 类型断言\n- **core 保持纯粹**：core 只关心构建上下文、DSD 渲染、island 机制\n- **错误更早暴露**：配置错误在启动时而非运行时发现\n- **globalThis 彻底删除**：`getActiveContext`/`setActiveContext`/`clearActiveContext` 全部移除，不再有 `globalThis[Symbol.for()]` 桥接。ctx 只通过显式参数传递。\n- **ADR 0008 最终完成**：从 4 个 globalThis 桥接到 0 个，ADR 0008 的 globalThis 消除目标彻底达成\n\n### Negative\n\n- **多一个包**：8 个包变为 9 个，维护成本略增\n- **用户 import 路径变化**：`lessjs()` 从 `@openelement/core` 改为 `@openelement/app`（有迁移成本）\n- **app 强依赖 content 和 i18n**：即使不用，也必须安装（node_modules 里有）\n- **split-call 模式需要显式传 ctx**：`lessContent()` 和 `lessI18n()` 不再自动发现 ctx，必须通过 `ctx` 参数传入\n\n### Neutral\n\n- 分开调用模式 `less() + lessContent({ ctx }) + lessI18n({ ctx })` 仍然可用，但需显式传 ctx\n- `lessjs()` 的 API 签名不变，只是 import 来源变了\n- `@openelement/app` 的版本号从 0.1.0 开始，独立于 core 版本\n\n## 参考\n\n- [ADR 0008: 消除 createServer()、.less/ 临时文件与 globalThis 桥接](/blog/0008-eliminate-createserver-globalthis-bridges)\n- [ADR 0011: 消除最后一个 globalThis 桥接](/blog/0011-eliminate-last-globalthis-via-closebundle)\n\n---\n\n_决策日期: 2026-05-10 | 版本: v0.9.0_\n",
    "html": "<h2>Status</h2>\n<p><strong>ACCEPTED</strong> — v0.9.x 架构简化</p>\n<h2>Context</h2>\n<p><code>lessjs()</code> 是 LessJS 的统一 Vite 插件入口，将 <code>less()</code> + <code>lessContent()</code> + <code>lessI18n()</code> 组合在一个调用中，通过显式传递 <code>LessBuildContext</code> 避免了 globalThis 桥接。</p>\n<p>当前 <code>lessjs()</code> 放在 <code>@openelement/core</code> 中（<code>packages/core/src/index.ts</code> L349-396），使用动态 <code>import()</code> 加载 <code>@openelement/content</code> 和 <code>@openelement/i18n</code>：</p>\n<pre><code class=\"language-ts\">// 当前：core 里动态 import 上层包\nexport async function lessjs(options = {}) {\n  const ctx = new LessBuildContext({...});\n  const plugins = [...less(coreOpts, ctx)];\n\n  if (contentOpts) {\n    const contentMod = await import(&#39;@openelement/content&#39;);  // 动态 import\n    plugins.push(...contentMod.lessContent({...contentOpts, ctx}));\n  }\n\n  if (i18nOpts) {\n    const i18nMod = await import(&#39;@openelement/i18n&#39;);  // 动态 import\n    plugins.push(i18nMod.lessI18n({...i18nOpts, ctx}));\n  }\n\n  return plugins;\n}\n</code></pre>\n<h3>问题：依赖方向倒挂</h3>\n<p><code>@openelement/core</code> 是基础层——<code>content</code> 和 <code>i18n</code> 都依赖 core。但 <code>lessjs()</code> 在 core 里 import content 和 i18n，形成概念上的循环依赖：</p>\n<pre><code>content → core → content   ← 概念倒挂（动态 import 打断了物理循环）\ni18n    → core → i18n       ← 同上\n</code></pre>\n<p>动态 <code>import()</code> 虽然避免了运行时循环依赖错误，但：</p>\n<ul>\n<li><strong>语义不清</strong>：core 不应该知道 content/i18n 的存在</li>\n<li><strong>try/catch 降级</strong>：必须用 try/catch 处理&quot;未安装&quot;的情况，而这是一个正常的包管理问题</li>\n<li><strong>类型丢失</strong>：动态 import 导致类型断言 <code>as Record&lt;string, unknown&gt;</code>，丧失编译时类型安全</li>\n</ul>\n<h3>替代方案评估</h3>\n<table>\n<thead>\n<tr>\n<th>方案</th>\n<th>做法</th>\n<th>优点</th>\n<th>缺点</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>A. 留在 core，保持动态 import</td>\n<td>现状</td>\n<td>零改动</td>\n<td>概念倒挂，类型不安全</td>\n</tr>\n<tr>\n<td>B. 拆到 @openelement/app</td>\n<td>新包依赖 core+content+i18n</td>\n<td>依赖方向干净，静态 import，类型安全</td>\n<td>多一个包</td>\n</tr>\n<tr>\n<td>C. 留在 core，注册表模式</td>\n<td>core 定义 pluginRegistry</td>\n<td>core 不需知道子插件</td>\n<td>多一层间接，过度设计</td>\n</tr>\n</tbody></table>\n<h2>Decision</h2>\n<p><strong>选方案 B</strong>：将 <code>lessjs()</code> 从 <code>@openelement/core</code> 拆到新包 <code>@openelement/app</code>。</p>\n<h3>新依赖图</h3>\n<pre><code>@openelement/app\n  ├── @openelement/core       (静态 import)\n  ├── @openelement/content    (静态 import)\n  └── @openelement/i18n       (静态 import)\n</code></pre>\n<p>依赖方向清晰：app 是&quot;组装层&quot;，core 是&quot;基础层&quot;，content/i18n 是&quot;功能层&quot;。无循环，无倒挂。</p>\n<h3>新发布顺序</h3>\n<pre><code>rpc → ui → adapter-lit → signal → content → i18n → core → app → create\n</code></pre>\n<p><code>@openelement/app</code> 排在 core 之后、create 之前。</p>\n<h3>代码变更</h3>\n<h4>1. 新建 <code>packages/app/</code></h4>\n<pre><code>packages/app/\n  ├── deno.json\n  └── src/\n      └── index.ts      ← lessjs() 函数\n</code></pre>\n<h4>2. <code>packages/app/src/index.ts</code></h4>\n<pre><code class=\"language-ts\">import type { Plugin } from &#39;vite&#39;;\nimport type { FrameworkOptions } from &#39;@openelement/core&#39;;\nimport type { LessContentOptions } from &#39;@openelement/content&#39;;\nimport type { LessI18nOptions } from &#39;@openelement/i18n&#39;;\nimport type { LessBuildContext } from &#39;@openelement/core/build-context&#39;;\n\nimport { less } from &#39;@openelement/core&#39;;\nimport { LessBuildContext as LessBuildContextClass } from &#39;@openelement/core/build-context&#39;;\nimport { lessContent } from &#39;@openelement/content&#39;;\nimport { lessI18n } from &#39;@openelement/i18n&#39;;\nimport { createLogger } from &#39;@openelement/core/logger&#39;;\n\nconst log = createLogger(&#39;app&#39;);\n\nexport interface LessjsOptions extends FrameworkOptions {\n  content?: LessContentOptions;\n  i18n?: LessI18nOptions;\n}\n\nexport async function lessjs(options: LessjsOptions = {}): Promise&lt;Plugin[]&gt; {\n  const { content: contentOpts, i18n: i18nOpts, ...coreOpts } = options;\n  const ctx: LessBuildContext = new LessBuildContextClass({\n    ...coreOpts,\n    routesDir: coreOpts.routesDir || &#39;app/routes&#39;,\n    islandsDir: coreOpts.islandsDir || &#39;app/islands&#39;,\n    componentsDir: coreOpts.componentsDir || &#39;app/components&#39;,\n  });\n\n  const plugins: Plugin[] = [...less(coreOpts, ctx)];\n\n  if (contentOpts) {\n    plugins.push(...lessContent({ ...contentOpts, ctx }));\n  }\n\n  if (i18nOpts) {\n    plugins.push(lessI18n({ ...i18nOpts, ctx }));\n  }\n\n  return plugins;\n}\n\nexport default lessjs;\n</code></pre>\n<p>关键变化：</p>\n<ul>\n<li><strong>静态 import</strong>：不再需要 <code>await import()</code> + <code>try/catch</code></li>\n<li><strong>类型安全</strong>：直接 import <code>lessContent</code> / <code>lessI18n</code>，无需 <code>as Record&lt;string, unknown&gt;</code> 断言</li>\n<li><strong>安装即用</strong>：如果 <code>@openelement/content</code> 未安装，Deno 会在启动时报错，而非运行时静默降级</li>\n</ul>\n<h4>3. <code>packages/core/src/index.ts</code></h4>\n<ul>\n<li>删除 <code>lessjs()</code> 函数（L349-396）</li>\n<li>删除 <code>LessBuildContext</code> 的顶层 export（保留 <code>build-context</code> 子路径导出）</li>\n<li><code>less()</code> 函数保留 <code>externalCtx</code> 参数不变</li>\n</ul>\n<h4>4. 用户迁移</h4>\n<pre><code class=\"language-ts\">// Before (v0.9.x)\nimport { lessjs } from &#39;@openelement/core&#39;;\n\n// After (v0.10.x)\nimport { lessjs } from &#39;@openelement/app&#39;;\n</code></pre>\n<p><code>less()</code> 不受影响——仍在 <code>@openelement/core</code>。</p>\n<h3>为什么安装失败是正确的行为</h3>\n<p>当前 <code>lessjs()</code> 用 try/catch 静默降级未安装的子插件。这看似友好，但隐藏了配置错误：</p>\n<ul>\n<li>用户在 <code>lessjs()</code> 中传了 <code>content: { blog: {...} }</code> 但忘了安装 <code>@openelement/content</code></li>\n<li>当前行为：<strong>静默跳过</strong>，不报错，用户困惑&quot;博客功能为什么没生效&quot;</li>\n<li>新行为：<strong>启动报错</strong> <code>Cannot find module &#39;@openelement/content&#39;</code>，问题一目了然</li>\n</ul>\n<p>如果用户不想用 content/i18n，<strong>不传对应选项即可</strong>——这是 opt-in 的正确姿势，不需要 try/catch。</p>\n<h2>Consequences</h2>\n<h3>Positive</h3>\n<ul>\n<li><strong>依赖方向正确</strong>：app → core/content/i18n，无倒挂</li>\n<li><strong>类型安全</strong>：静态 import 替代动态 import + 类型断言</li>\n<li><strong>core 保持纯粹</strong>：core 只关心构建上下文、DSD 渲染、island 机制</li>\n<li><strong>错误更早暴露</strong>：配置错误在启动时而非运行时发现</li>\n<li><strong>globalThis 彻底删除</strong>：<code>getActiveContext</code>/<code>setActiveContext</code>/<code>clearActiveContext</code> 全部移除，不再有 <code>globalThis[Symbol.for()]</code> 桥接。ctx 只通过显式参数传递。</li>\n<li><strong>ADR 0008 最终完成</strong>：从 4 个 globalThis 桥接到 0 个，ADR 0008 的 globalThis 消除目标彻底达成</li>\n</ul>\n<h3>Negative</h3>\n<ul>\n<li><strong>多一个包</strong>：8 个包变为 9 个，维护成本略增</li>\n<li><strong>用户 import 路径变化</strong>：<code>lessjs()</code> 从 <code>@openelement/core</code> 改为 <code>@openelement/app</code>（有迁移成本）</li>\n<li><strong>app 强依赖 content 和 i18n</strong>：即使不用，也必须安装（node_modules 里有）</li>\n<li><strong>split-call 模式需要显式传 ctx</strong>：<code>lessContent()</code> 和 <code>lessI18n()</code> 不再自动发现 ctx，必须通过 <code>ctx</code> 参数传入</li>\n</ul>\n<h3>Neutral</h3>\n<ul>\n<li>分开调用模式 <code>less() + lessContent({ ctx }) + lessI18n({ ctx })</code> 仍然可用，但需显式传 ctx</li>\n<li><code>lessjs()</code> 的 API 签名不变，只是 import 来源变了</li>\n<li><code>@openelement/app</code> 的版本号从 0.1.0 开始，独立于 core 版本</li>\n</ul>\n<h2>参考</h2>\n<ul>\n<li><a href=\"/blog/0008-eliminate-createserver-globalthis-bridges\" rel=\"noopener noreferrer\">ADR 0008: 消除 createServer()、.less/ 临时文件与 globalThis 桥接</a></li>\n<li><a href=\"/blog/0011-eliminate-last-globalthis-via-closebundle\" rel=\"noopener noreferrer\">ADR 0011: 消除最后一个 globalThis 桥接</a></li>\n</ul>\n<hr />\n<p><em>决策日期: 2026-05-10 | 版本: v0.9.0</em></p>\n"
  },
  {
    "slug": "0011-eliminate-last-globalthis-via-closebundle",
    "frontmatter": {
      "title": "ADR 0011: 消除最后一个 globalThis 桥接 — closeBundle 内联 Phase 2/3",
      "date": "2026-05-10",
      "draft": false,
      "tags": [
        "architecture",
        "decision"
      ],
      "type": "adr",
      "lang": "zh"
    },
    "content": "\n## Status\n\n**IMPLEMENTED** — v0.9.x 架构简化（globalThis 桥接已彻底删除，见 ADR 0012）\n\n## Context\n\nADR 0008 将 `globalThis[Symbol.for()]` 桥接从 4 个减到 1 个。ADR 0010 消除了所有 `.less/` 临时文件。当前仅剩一个 globalThis 桥接：\n\n```ts\n// index.ts — less() 插件创建 ctx 后写入 globalThis\nconst CTX_KEY = Symbol.for('lessjs:build-context');\n(globalThis as Record<symbol, unknown>)[CTX_KEY] = ctx;\n\n// cli/build.ts — orchestrator 从 globalThis 读回\nconst CTX_KEY = Symbol.for('lessjs:build-context');\nconst existingCtx = (globalThis as Record<symbol, unknown>)[CTX_KEY];\n```\n\n### 为什么需要这个桥接\n\n`cli/build.ts`（orchestrator）调用 `viteBuild()` 触发 Phase 1。Vite 内部加载 `vite.config.ts` → 调用 `less()` → 创建 `LessBuildContext`。但 `viteBuild()` 是黑盒，不返回任何用户数据，orchestrator 无法直接拿到 ctx 引用。\n\n因此用 `globalThis` 做桥梁：`less()` 写入，orchestrator 读回，传递给 Phase 2/3。\n\n### 现状数据流\n\n```\ncli/build.ts (orchestrator)\n  │\n  ├─ viteBuild()                           ← 黑盒\n  │     └─ less() 创建 ctx\n  │        └─ globalThis[CTX_KEY] = ctx    ← 写入\n  │        └─ buildPlugin(opts, ctx)       ← closeBundle 写 metadata\n  │\n  ├─ globalThis[CTX_KEY] 读取 ctx          ← 问题所在\n  ├─ buildClient(ctx)\n  ├─ buildSSG({}, ctx)\n  └─ delete globalThis[CTX_KEY]\n```\n\n### 关键观察\n\n`closeBundle()` **已经有 ctx 的直接引用**——它是 `buildPlugin(opts, ctx)` 的参数，在 `less()` 的闭包作用域内。而 Phase 2 和 3 都是 `configFile: false` 的独立 Vite 构建，不依赖 `vite.config.ts`，不会触发 `less:build` 的 `closeBundle()` 递归。\n\n这意味着 Phase 2/3 可以直接在 `closeBundle()` 内执行，ctx 全程在闭包内传递，不需要任何跨作用域桥接。\n\n## Decision\n\n将 Phase 2（buildClient）和 Phase 3（buildSSG）从 `cli/build.ts` orchestrator 搬入 `build.ts` 的 `closeBundle()` 钩子。\n\n### 新数据流\n\n```\nviteBuild()                                ← 用户或 CLI 调用\n  └─ less() 创建 ctx（闭包内）\n     └─ buildPlugin(opts, ctx)\n        └─ closeBundle()\n           ├─ 写 metadata 到 ctx\n           ├─ buildClient(ctx)              ← Phase 2\n           └─ buildSSG({}, ctx)            ← Phase 3\n```\n\nctx 从创建到消费，全程在 `less()` 的闭包作用域内，零跨作用域传递。\n\n### 代码变更\n\n#### 1. `build.ts` — closeBundle 内联 Phase 2/3\n\n```ts\nasync closeBundle() {\n  if (config.command !== 'build') return;\n\n  // 现有逻辑：写 metadata 到 ctx\n  ctx.root = root;\n  // ...\n\n  // 新增：直接触发 Phase 2 和 3\n  const { buildClient } = await import('./cli/build-client.js');\n  await buildClient(ctx);\n\n  const { buildSSG } = await import('./cli/build-ssg.js');\n  await buildSSG({}, ctx);\n\n  log.info('Build complete.');\n}\n```\n\n#### 2. `index.ts` — 删除 globalThis 写入\n\n移除：\n\n```ts\nconst CTX_KEY = Symbol.for('lessjs:build-context');\nif (!(globalThis as Record<symbol, unknown>)[CTX_KEY]) {\n  (globalThis as Record<symbol, unknown>)[CTX_KEY] = ctx;\n}\n```\n\n#### 3. `cli/build.ts` — 简化为单行 viteBuild()\n\n```ts\nimport { build as viteBuild } from 'vite';\nimport process from 'node:process';\n\nif (import.meta.main) {\n  viteBuild().catch((error) => {\n    console.error('Build failed:', error);\n    process.exit(1);\n  });\n}\n```\n\n不再需要 `runPhase()`、`LessBuildContext` 导入、globalThis 读写、Phase 2/3 调度。\n\n#### 4. `build-client.ts` / `build-ssg.ts` — ctx 参数为 required\n\n已有的变更，无额外工作。\n\n### 为什么不会递归\n\nPhase 2 和 3 的 Vite 配置都是 `configFile: false`，不会加载 `vite.config.ts`，不会触发 `less()` 插件，不会再次进入 `closeBundle()`。\n\nFresh 的 `writeBundle` 钩子中触发后续构建也是同样模式——在 Vite 钩子内调用 `viteBuild()` 是已验证的实践。\n\n## Consequences\n\n### Positive\n\n- **globalThis 彻底清零**：从 4→1→**0**，ADR 0008 的 globalThis 消除目标完成\n- **零共享可变状态**：ctx 全程在闭包内，无跨作用域传递\n- **代码更简单**：`cli/build.ts` 从 ~80 行降至 ~10 行，`index.ts` 删除 globalThis 相关代码\n- **架构更清晰**：`viteBuild()` 一个入口完成全部 3 个 phase，与 Fresh 模式对齐\n- **消除 Symbol.for() 碰撞风险**：不再有全局 Symbol 注册\n- **ADR 0012 进一步删除**：`getActiveContext()`/`setActiveContext()`/`clearActiveContext()` 函数本身也从 `build-context.ts` 中删除，ctx 只通过显式参数传递\n\n### Negative\n\n- **closeBundle 变重**：在钩子内调两次 `viteBuild()`，但 Phase 2/3 本就是独立 Vite 实例，实际影响为零\n- **失去独立的 Phase 2/3 命令**：`deno task build:client` / `build:ssg` 不再可用\n  - **缓解**：ADR 0010 已确定统一入口是唯一路径，standalone CLI 不支持\n\n### Neutral\n\n- `cli/build.ts` 不再是\"orchestrator\"——它只是一个 `viteBuild()` 的 CLI 入口。真正的编排逻辑在 `closeBundle()` 中\n- 用户构建命令不变：仍然是 `deno task build`\n\n## 参考\n\n- [ADR 0008: 消除 createServer()、.less/ 临时文件与 globalThis 桥接](/blog/0008-eliminate-createserver-globalthis-bridges)\n- [ADR 0010: 消除所有 .less/ 临时文件](/blog/0010-eliminate-all-dot-less-temp-files)\n- Fresh `plugin-vite/server_entry.ts` — `writeBundle` 钩子内触发后续构建的先例\n\n---\n\n_决策日期: 2026-05-10 | 版本: v0.9.0_\n",
    "html": "<h2>Status</h2>\n<p><strong>IMPLEMENTED</strong> — v0.9.x 架构简化（globalThis 桥接已彻底删除，见 ADR 0012）</p>\n<h2>Context</h2>\n<p>ADR 0008 将 <code>globalThis[Symbol.for()]</code> 桥接从 4 个减到 1 个。ADR 0010 消除了所有 <code>.less/</code> 临时文件。当前仅剩一个 globalThis 桥接：</p>\n<pre><code class=\"language-ts\">// index.ts — less() 插件创建 ctx 后写入 globalThis\nconst CTX_KEY = Symbol.for(&#39;lessjs:build-context&#39;);\n(globalThis as Record&lt;symbol, unknown&gt;)[CTX_KEY] = ctx;\n\n// cli/build.ts — orchestrator 从 globalThis 读回\nconst CTX_KEY = Symbol.for(&#39;lessjs:build-context&#39;);\nconst existingCtx = (globalThis as Record&lt;symbol, unknown&gt;)[CTX_KEY];\n</code></pre>\n<h3>为什么需要这个桥接</h3>\n<p><code>cli/build.ts</code>（orchestrator）调用 <code>viteBuild()</code> 触发 Phase 1。Vite 内部加载 <code>vite.config.ts</code> → 调用 <code>less()</code> → 创建 <code>LessBuildContext</code>。但 <code>viteBuild()</code> 是黑盒，不返回任何用户数据，orchestrator 无法直接拿到 ctx 引用。</p>\n<p>因此用 <code>globalThis</code> 做桥梁：<code>less()</code> 写入，orchestrator 读回，传递给 Phase 2/3。</p>\n<h3>现状数据流</h3>\n<pre><code>cli/build.ts (orchestrator)\n  │\n  ├─ viteBuild()                           ← 黑盒\n  │     └─ less() 创建 ctx\n  │        └─ globalThis[CTX_KEY] = ctx    ← 写入\n  │        └─ buildPlugin(opts, ctx)       ← closeBundle 写 metadata\n  │\n  ├─ globalThis[CTX_KEY] 读取 ctx          ← 问题所在\n  ├─ buildClient(ctx)\n  ├─ buildSSG({}, ctx)\n  └─ delete globalThis[CTX_KEY]\n</code></pre>\n<h3>关键观察</h3>\n<p><code>closeBundle()</code> <strong>已经有 ctx 的直接引用</strong>——它是 <code>buildPlugin(opts, ctx)</code> 的参数，在 <code>less()</code> 的闭包作用域内。而 Phase 2 和 3 都是 <code>configFile: false</code> 的独立 Vite 构建，不依赖 <code>vite.config.ts</code>，不会触发 <code>less:build</code> 的 <code>closeBundle()</code> 递归。</p>\n<p>这意味着 Phase 2/3 可以直接在 <code>closeBundle()</code> 内执行，ctx 全程在闭包内传递，不需要任何跨作用域桥接。</p>\n<h2>Decision</h2>\n<p>将 Phase 2（buildClient）和 Phase 3（buildSSG）从 <code>cli/build.ts</code> orchestrator 搬入 <code>build.ts</code> 的 <code>closeBundle()</code> 钩子。</p>\n<h3>新数据流</h3>\n<pre><code>viteBuild()                                ← 用户或 CLI 调用\n  └─ less() 创建 ctx（闭包内）\n     └─ buildPlugin(opts, ctx)\n        └─ closeBundle()\n           ├─ 写 metadata 到 ctx\n           ├─ buildClient(ctx)              ← Phase 2\n           └─ buildSSG({}, ctx)            ← Phase 3\n</code></pre>\n<p>ctx 从创建到消费，全程在 <code>less()</code> 的闭包作用域内，零跨作用域传递。</p>\n<h3>代码变更</h3>\n<h4>1. <code>build.ts</code> — closeBundle 内联 Phase 2/3</h4>\n<pre><code class=\"language-ts\">async closeBundle() {\n  if (config.command !== &#39;build&#39;) return;\n\n  // 现有逻辑：写 metadata 到 ctx\n  ctx.root = root;\n  // ...\n\n  // 新增：直接触发 Phase 2 和 3\n  const { buildClient } = await import(&#39;./cli/build-client.js&#39;);\n  await buildClient(ctx);\n\n  const { buildSSG } = await import(&#39;./cli/build-ssg.js&#39;);\n  await buildSSG({}, ctx);\n\n  log.info(&#39;Build complete.&#39;);\n}\n</code></pre>\n<h4>2. <code>index.ts</code> — 删除 globalThis 写入</h4>\n<p>移除：</p>\n<pre><code class=\"language-ts\">const CTX_KEY = Symbol.for(&#39;lessjs:build-context&#39;);\nif (!(globalThis as Record&lt;symbol, unknown&gt;)[CTX_KEY]) {\n  (globalThis as Record&lt;symbol, unknown&gt;)[CTX_KEY] = ctx;\n}\n</code></pre>\n<h4>3. <code>cli/build.ts</code> — 简化为单行 viteBuild()</h4>\n<pre><code class=\"language-ts\">import { build as viteBuild } from &#39;vite&#39;;\nimport process from &#39;node:process&#39;;\n\nif (import.meta.main) {\n  viteBuild().catch((error) =&gt; {\n    console.error(&#39;Build failed:&#39;, error);\n    process.exit(1);\n  });\n}\n</code></pre>\n<p>不再需要 <code>runPhase()</code>、<code>LessBuildContext</code> 导入、globalThis 读写、Phase 2/3 调度。</p>\n<h4>4. <code>build-client.ts</code> / <code>build-ssg.ts</code> — ctx 参数为 required</h4>\n<p>已有的变更，无额外工作。</p>\n<h3>为什么不会递归</h3>\n<p>Phase 2 和 3 的 Vite 配置都是 <code>configFile: false</code>，不会加载 <code>vite.config.ts</code>，不会触发 <code>less()</code> 插件，不会再次进入 <code>closeBundle()</code>。</p>\n<p>Fresh 的 <code>writeBundle</code> 钩子中触发后续构建也是同样模式——在 Vite 钩子内调用 <code>viteBuild()</code> 是已验证的实践。</p>\n<h2>Consequences</h2>\n<h3>Positive</h3>\n<ul>\n<li><strong>globalThis 彻底清零</strong>：从 4→1→<strong>0</strong>，ADR 0008 的 globalThis 消除目标完成</li>\n<li><strong>零共享可变状态</strong>：ctx 全程在闭包内，无跨作用域传递</li>\n<li><strong>代码更简单</strong>：<code>cli/build.ts</code> 从 ~80 行降至 ~10 行，<code>index.ts</code> 删除 globalThis 相关代码</li>\n<li><strong>架构更清晰</strong>：<code>viteBuild()</code> 一个入口完成全部 3 个 phase，与 Fresh 模式对齐</li>\n<li><strong>消除 Symbol.for() 碰撞风险</strong>：不再有全局 Symbol 注册</li>\n<li><strong>ADR 0012 进一步删除</strong>：<code>getActiveContext()</code>/<code>setActiveContext()</code>/<code>clearActiveContext()</code> 函数本身也从 <code>build-context.ts</code> 中删除，ctx 只通过显式参数传递</li>\n</ul>\n<h3>Negative</h3>\n<ul>\n<li><strong>closeBundle 变重</strong>：在钩子内调两次 <code>viteBuild()</code>，但 Phase 2/3 本就是独立 Vite 实例，实际影响为零</li>\n<li><strong>失去独立的 Phase 2/3 命令</strong>：<code>deno task build:client</code> / <code>build:ssg</code> 不再可用<ul>\n<li><strong>缓解</strong>：ADR 0010 已确定统一入口是唯一路径，standalone CLI 不支持</li>\n</ul>\n</li>\n</ul>\n<h3>Neutral</h3>\n<ul>\n<li><code>cli/build.ts</code> 不再是&quot;orchestrator&quot;——它只是一个 <code>viteBuild()</code> 的 CLI 入口。真正的编排逻辑在 <code>closeBundle()</code> 中</li>\n<li>用户构建命令不变：仍然是 <code>deno task build</code></li>\n</ul>\n<h2>参考</h2>\n<ul>\n<li><a href=\"/blog/0008-eliminate-createserver-globalthis-bridges\" rel=\"noopener noreferrer\">ADR 0008: 消除 createServer()、.less/ 临时文件与 globalThis 桥接</a></li>\n<li><a href=\"/blog/0010-eliminate-all-dot-less-temp-files\" rel=\"noopener noreferrer\">ADR 0010: 消除所有 .less/ 临时文件</a></li>\n<li>Fresh <code>plugin-vite/server_entry.ts</code> — <code>writeBundle</code> 钩子内触发后续构建的先例</li>\n</ul>\n<hr />\n<p><em>决策日期: 2026-05-10 | 版本: v0.9.0</em></p>\n"
  },
  {
    "slug": "0010-eliminate-all-dot-less-temp-files",
    "frontmatter": {
      "title": "ADR 0010: 消除所有 .less/ 临时文件，统一单进程构建",
      "date": "2026-05-11",
      "draft": false,
      "tags": [
        "architecture",
        "decision"
      ],
      "type": "adr",
      "lang": "zh"
    },
    "content": "\n## Status\n\n**IMPLEMENTED** — v0.9.x 架构简化（所有 5 个 Step 已完成）\n\n## Context\n\nADR 0008 将 `.less/` 临时文件从 10 个减少到 3 个，建立了 `LessBuildContext` 内存传递机制和 `virtual:less-runtime`/`virtual:less-nav` 虚拟模块模式。但仍有 3 个文件残留：\n\n| 文件                    | 写入位置               | 读取位置                          | 残留原因                |\n| ----------------------- | ---------------------- | --------------------------------- | ----------------------- |\n| `build-metadata.json`   | `build.ts` closeBundle | `build-client.ts`、`build-ssg.ts` | 跨进程 IPC fallback     |\n| `.less-client-entry.ts` | `build-client.ts`      | Vite `rollupOptions.input`        | Vite 入口需文件路径     |\n| `.less-ssg-entry.ts`    | `build-ssg.ts`         | Vite `build.ssr`                  | Vite SSR 入口需文件路径 |\n\n此外，`build-ssg.ts` 仍有多处 `.less/` 文件的 fallback 读取：\n\n- `nav-data.json` / `header-nav.json`（virtual:less-nav 插件内）\n- `blog-options.json`（动态路由博客初始化）\n- `i18n-options.json`（i18n locale 扩展）\n- `sitemap-options.json`（sitemap 生成）\n\n### 为什么分步独立运行没有意义\n\n当前 standalone CLI 的设计假设 Phase 1/2/3 分别由不同进程运行，需要文件做跨进程 IPC。但实际上：\n\n- Phase 2 依赖 Phase 1 的 island 元数据（串行）\n- Phase 3 依赖 Phase 2 的 island chunk 路径（串行）\n- 最终输出是同一个 `dist/` 目录\n\n分步运行不存在任何可并行、可增量、可独立部署的优势。`cli/build.ts` 已经是单进程全流程编排——standalone CLI 只是它的退化版本。\n\n### 技术可行性\n\n`virtual:less-hono-entry` 在 dev 模式已证明 Vite/Rollup 的 `input` 和 `build.ssr` 都走 `resolveId → load` 流程，虚拟模块 ID 可以作为入口。具体证据：\n\n- `index.ts` L203: `rollupOptions: { input: [VIRTUAL_ENTRY_ID] }` — 虚拟 ID 作为 client build 入口\n- `index.ts` L275-288: `resolveId()` + `load()` 提供 `VIRTUAL_ENTRY_ID` 的内容\n- `build-ssg.ts` L316-327: `virtual:less-runtime` 已在 SSR build 中工作\n\n## Decision\n\n### 1. 消除 `build-metadata.json`\n\n**方案**：从 `build.ts` 的 `closeBundle()` 中移除文件写入。所有数据通过 `LessBuildContext` 传递。`build-client.ts` 和 `build-ssg.ts` 不再有\"无 ctx 时从文件读取\"的 fallback 路径。\n\n**前提**：standalone `deno run -A jsr:@openelement/core/cli/build-client` 不再支持独立运行——用户应使用 `deno run -A jsr:@openelement/core/cli/build` 统一入口。\n\n### 2. `.less-client-entry.ts` → `virtual:less-client-entry`\n\n**方案**：在 `build-client.ts` 的 Vite 配置中添加 `resolveId`/`load` 插件，将生成的客户端入口代码作为虚拟模块提供。`rollupOptions.input` 使用 `virtual:less-client-entry`。\n\n**代码变更**：\n\n- 移除 `mkdirSync` + `writeFileSync` 写入\n- 添加内联 Vite plugin（同 `virtual:less-runtime` 模式）\n- `input: { client: 'virtual:less-client-entry' }` 替代文件路径\n\n### 3. `.less-ssg-entry.ts` → `virtual:less-ssg-entry`\n\n**方案**：在 `build-ssg.ts` 的 SSR Vite 配置中添加 `resolveId`/`load` 插件。`build.ssr` 使用 `virtual:less-ssg-entry`。\n\n**代码变更**：\n\n- 移除 `mkdirSync` + `writeFileSync` 写入\n- 添加内联 Vite plugin\n- `build: { ssr: 'virtual:less-ssg-entry' }` 替代文件路径\n\n### 4. 移除所有 `.less/` fallback 读取\n\n**方案**：`build-ssg.ts` 中所有 `if (!ctx || !ctx.root) { readFileSync(...) }` 分支全部移除。当 ctx 不可用时，直接报错退出，而不是静默降级到文件读取。\n\n**影响的 fallback**：\n\n- `build-metadata.json`（island 元数据、alias、middleware 等）\n- `nav-data.json` / `header-nav.json`（virtual:less-nav 插件内）\n- `blog-options.json`（动态路由博客初始化）\n- `i18n-options.json`（i18n locale 扩展）\n- `sitemap-options.json`（sitemap 生成）\n\n### 5. `build.ts` 清理\n\n**方案**：\n\n- 移除 `serializeAlias()` 函数（仅用于 JSON 序列化写入文件）\n- 移除 `mkdirSync` + `writeFileSync` 写入 `build-metadata.json`\n- 移除 `import { mkdirSync, writeFileSync } from 'node:fs'`\n- `closeBundle()` 只写 ctx\n\n## Consequences\n\n### Positive\n\n- **`.less/` 目录完全消失**：10→3→0，构建管线零文件 IPC\n- **代码大幅简化**：移除 6+ 处 `if (ctx) { } else { readFileSync }` 双路径分支、`serializeAlias()` 函数、`BuildMetadata` 接口、`getBuildMetadata()` 函数\n- **调试体验改善**：`DEBUG=lessjs*` 日志覆盖虚拟模块内容查看，无需 `cat .less/` 文件\n- **消除 stale data 风险**：不再存在 Phase 1 写文件、Phase 2/3 读旧文件的不一致问题\n- **构建更健壮**：不存在文件缺失、编码错误、JSON 格式错误等文件系统异常\n\n### Negative\n\n- **Standalone CLI 不再支持分步运行**：`deno run -A jsr:@openelement/core/cli/build-client` 独立运行会报错。用户必须使用 `deno run -A jsr:@openelement/core/cli/build` 或 `lessjs()` 编排器。\n  - **缓解**：`cli/build.ts` 已提供统一入口，分步运行无实际用例。\n- **无法直接查看生成的入口代码**：之前可以 `cat .less/.less-ssg-entry.ts` 查看生成的 Hono 入口。\n  - **缓解**：`DEBUG=lessjs*` 日志会输出虚拟模块内容；也可在 Vite config 中添加 `debug: true` 查看。\n\n### Neutral\n\n- `build-client.ts` 和 `build-ssg.ts` 的 `ctx` 参数从 optional 变为 required。这是正确的约束——强制所有构建走统一编排。\n- `LessBuildContext` 已有 `ssgEntryCode` 和 `clientEntryCode` 字段（ADR 0008 Phase A 预留），本 ADR 正式启用它们。\n\n## Implementation Plan\n\n按依赖顺序，每个步骤独立可提交：\n\n### Step 1: `.less-client-entry.ts` → `virtual:less-client-entry`\n\n- `build-client.ts`：添加虚拟模块插件，移除文件写入\n- `build-client.ts`：`ctx` 参数变为 required，移除 `BuildMetadata` 接口和 `getBuildMetadata()`\n\n### Step 2: `.less-ssg-entry.ts` → `virtual:less-ssg-entry`\n\n- `build-ssg.ts`：添加虚拟模块插件，移除文件写入\n- `build-ssg.ts`：移除 `build-metadata.json` fallback 读取块\n\n### Step 3: 移除 `build-metadata.json` 文件写入\n\n- `build.ts`：移除 `serializeAlias()`、移除文件写入、只写 ctx\n\n### Step 4: 移除 `build-ssg.ts` 中所有 `.less/` fallback 读取\n\n- `virtual:less-nav`：移除 `.less/nav-data.json` / `.less/header-nav.json` fallback\n- 博客初始化：移除 `.less/blog-options.json` fallback\n- i18n：移除 `.less/i18n-options.json` fallback\n- sitemap：移除 `.less/sitemap-options.json` fallback\n\n### Step 5: 清理和验证\n\n- 移除 `build-client.ts` 和 `build-ssg.ts` 中未使用的 `import { ... from 'node:fs' }`\n- 运行 `deno task build` 端到端验证\n- 确认 `.less/` 目录不再被创建\n\n## Expected Impact\n\n- **文件写入点**：3→0\n- **文件读取点**：6→0\n- **代码行数**：净减 ~120 行（双路径分支、序列化逻辑、fallback 读取）\n- **功能变更**：零——所有构建产物不变\n",
    "html": "<h2>Status</h2>\n<p><strong>IMPLEMENTED</strong> — v0.9.x 架构简化（所有 5 个 Step 已完成）</p>\n<h2>Context</h2>\n<p>ADR 0008 将 <code>.less/</code> 临时文件从 10 个减少到 3 个，建立了 <code>LessBuildContext</code> 内存传递机制和 <code>virtual:less-runtime</code>/<code>virtual:less-nav</code> 虚拟模块模式。但仍有 3 个文件残留：</p>\n<table>\n<thead>\n<tr>\n<th>文件</th>\n<th>写入位置</th>\n<th>读取位置</th>\n<th>残留原因</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>build-metadata.json</code></td>\n<td><code>build.ts</code> closeBundle</td>\n<td><code>build-client.ts</code>、<code>build-ssg.ts</code></td>\n<td>跨进程 IPC fallback</td>\n</tr>\n<tr>\n<td><code>.less-client-entry.ts</code></td>\n<td><code>build-client.ts</code></td>\n<td>Vite <code>rollupOptions.input</code></td>\n<td>Vite 入口需文件路径</td>\n</tr>\n<tr>\n<td><code>.less-ssg-entry.ts</code></td>\n<td><code>build-ssg.ts</code></td>\n<td>Vite <code>build.ssr</code></td>\n<td>Vite SSR 入口需文件路径</td>\n</tr>\n</tbody></table>\n<p>此外，<code>build-ssg.ts</code> 仍有多处 <code>.less/</code> 文件的 fallback 读取：</p>\n<ul>\n<li><code>nav-data.json</code> / <code>header-nav.json</code>（virtual:less-nav 插件内）</li>\n<li><code>blog-options.json</code>（动态路由博客初始化）</li>\n<li><code>i18n-options.json</code>（i18n locale 扩展）</li>\n<li><code>sitemap-options.json</code>（sitemap 生成）</li>\n</ul>\n<h3>为什么分步独立运行没有意义</h3>\n<p>当前 standalone CLI 的设计假设 Phase 1/2/3 分别由不同进程运行，需要文件做跨进程 IPC。但实际上：</p>\n<ul>\n<li>Phase 2 依赖 Phase 1 的 island 元数据（串行）</li>\n<li>Phase 3 依赖 Phase 2 的 island chunk 路径（串行）</li>\n<li>最终输出是同一个 <code>dist/</code> 目录</li>\n</ul>\n<p>分步运行不存在任何可并行、可增量、可独立部署的优势。<code>cli/build.ts</code> 已经是单进程全流程编排——standalone CLI 只是它的退化版本。</p>\n<h3>技术可行性</h3>\n<p><code>virtual:less-hono-entry</code> 在 dev 模式已证明 Vite/Rollup 的 <code>input</code> 和 <code>build.ssr</code> 都走 <code>resolveId → load</code> 流程，虚拟模块 ID 可以作为入口。具体证据：</p>\n<ul>\n<li><code>index.ts</code> L203: <code>rollupOptions: { input: [VIRTUAL_ENTRY_ID] }</code> — 虚拟 ID 作为 client build 入口</li>\n<li><code>index.ts</code> L275-288: <code>resolveId()</code> + <code>load()</code> 提供 <code>VIRTUAL_ENTRY_ID</code> 的内容</li>\n<li><code>build-ssg.ts</code> L316-327: <code>virtual:less-runtime</code> 已在 SSR build 中工作</li>\n</ul>\n<h2>Decision</h2>\n<h3>1. 消除 <code>build-metadata.json</code></h3>\n<p><strong>方案</strong>：从 <code>build.ts</code> 的 <code>closeBundle()</code> 中移除文件写入。所有数据通过 <code>LessBuildContext</code> 传递。<code>build-client.ts</code> 和 <code>build-ssg.ts</code> 不再有&quot;无 ctx 时从文件读取&quot;的 fallback 路径。</p>\n<p><strong>前提</strong>：standalone <code>deno run -A jsr:@openelement/core/cli/build-client</code> 不再支持独立运行——用户应使用 <code>deno run -A jsr:@openelement/core/cli/build</code> 统一入口。</p>\n<h3>2. <code>.less-client-entry.ts</code> → <code>virtual:less-client-entry</code></h3>\n<p><strong>方案</strong>：在 <code>build-client.ts</code> 的 Vite 配置中添加 <code>resolveId</code>/<code>load</code> 插件，将生成的客户端入口代码作为虚拟模块提供。<code>rollupOptions.input</code> 使用 <code>virtual:less-client-entry</code>。</p>\n<p><strong>代码变更</strong>：</p>\n<ul>\n<li>移除 <code>mkdirSync</code> + <code>writeFileSync</code> 写入</li>\n<li>添加内联 Vite plugin（同 <code>virtual:less-runtime</code> 模式）</li>\n<li><code>input: { client: &#39;virtual:less-client-entry&#39; }</code> 替代文件路径</li>\n</ul>\n<h3>3. <code>.less-ssg-entry.ts</code> → <code>virtual:less-ssg-entry</code></h3>\n<p><strong>方案</strong>：在 <code>build-ssg.ts</code> 的 SSR Vite 配置中添加 <code>resolveId</code>/<code>load</code> 插件。<code>build.ssr</code> 使用 <code>virtual:less-ssg-entry</code>。</p>\n<p><strong>代码变更</strong>：</p>\n<ul>\n<li>移除 <code>mkdirSync</code> + <code>writeFileSync</code> 写入</li>\n<li>添加内联 Vite plugin</li>\n<li><code>build: { ssr: &#39;virtual:less-ssg-entry&#39; }</code> 替代文件路径</li>\n</ul>\n<h3>4. 移除所有 <code>.less/</code> fallback 读取</h3>\n<p><strong>方案</strong>：<code>build-ssg.ts</code> 中所有 <code>if (!ctx || !ctx.root) { readFileSync(...) }</code> 分支全部移除。当 ctx 不可用时，直接报错退出，而不是静默降级到文件读取。</p>\n<p><strong>影响的 fallback</strong>：</p>\n<ul>\n<li><code>build-metadata.json</code>（island 元数据、alias、middleware 等）</li>\n<li><code>nav-data.json</code> / <code>header-nav.json</code>（virtual:less-nav 插件内）</li>\n<li><code>blog-options.json</code>（动态路由博客初始化）</li>\n<li><code>i18n-options.json</code>（i18n locale 扩展）</li>\n<li><code>sitemap-options.json</code>（sitemap 生成）</li>\n</ul>\n<h3>5. <code>build.ts</code> 清理</h3>\n<p><strong>方案</strong>：</p>\n<ul>\n<li>移除 <code>serializeAlias()</code> 函数（仅用于 JSON 序列化写入文件）</li>\n<li>移除 <code>mkdirSync</code> + <code>writeFileSync</code> 写入 <code>build-metadata.json</code></li>\n<li>移除 <code>import { mkdirSync, writeFileSync } from &#39;node:fs&#39;</code></li>\n<li><code>closeBundle()</code> 只写 ctx</li>\n</ul>\n<h2>Consequences</h2>\n<h3>Positive</h3>\n<ul>\n<li><strong><code>.less/</code> 目录完全消失</strong>：10→3→0，构建管线零文件 IPC</li>\n<li><strong>代码大幅简化</strong>：移除 6+ 处 <code>if (ctx) { } else { readFileSync }</code> 双路径分支、<code>serializeAlias()</code> 函数、<code>BuildMetadata</code> 接口、<code>getBuildMetadata()</code> 函数</li>\n<li><strong>调试体验改善</strong>：<code>DEBUG=lessjs*</code> 日志覆盖虚拟模块内容查看，无需 <code>cat .less/</code> 文件</li>\n<li><strong>消除 stale data 风险</strong>：不再存在 Phase 1 写文件、Phase 2/3 读旧文件的不一致问题</li>\n<li><strong>构建更健壮</strong>：不存在文件缺失、编码错误、JSON 格式错误等文件系统异常</li>\n</ul>\n<h3>Negative</h3>\n<ul>\n<li><strong>Standalone CLI 不再支持分步运行</strong>：<code>deno run -A jsr:@openelement/core/cli/build-client</code> 独立运行会报错。用户必须使用 <code>deno run -A jsr:@openelement/core/cli/build</code> 或 <code>lessjs()</code> 编排器。<ul>\n<li><strong>缓解</strong>：<code>cli/build.ts</code> 已提供统一入口，分步运行无实际用例。</li>\n</ul>\n</li>\n<li><strong>无法直接查看生成的入口代码</strong>：之前可以 <code>cat .less/.less-ssg-entry.ts</code> 查看生成的 Hono 入口。<ul>\n<li><strong>缓解</strong>：<code>DEBUG=lessjs*</code> 日志会输出虚拟模块内容；也可在 Vite config 中添加 <code>debug: true</code> 查看。</li>\n</ul>\n</li>\n</ul>\n<h3>Neutral</h3>\n<ul>\n<li><code>build-client.ts</code> 和 <code>build-ssg.ts</code> 的 <code>ctx</code> 参数从 optional 变为 required。这是正确的约束——强制所有构建走统一编排。</li>\n<li><code>LessBuildContext</code> 已有 <code>ssgEntryCode</code> 和 <code>clientEntryCode</code> 字段（ADR 0008 Phase A 预留），本 ADR 正式启用它们。</li>\n</ul>\n<h2>Implementation Plan</h2>\n<p>按依赖顺序，每个步骤独立可提交：</p>\n<h3>Step 1: <code>.less-client-entry.ts</code> → <code>virtual:less-client-entry</code></h3>\n<ul>\n<li><code>build-client.ts</code>：添加虚拟模块插件，移除文件写入</li>\n<li><code>build-client.ts</code>：<code>ctx</code> 参数变为 required，移除 <code>BuildMetadata</code> 接口和 <code>getBuildMetadata()</code></li>\n</ul>\n<h3>Step 2: <code>.less-ssg-entry.ts</code> → <code>virtual:less-ssg-entry</code></h3>\n<ul>\n<li><code>build-ssg.ts</code>：添加虚拟模块插件，移除文件写入</li>\n<li><code>build-ssg.ts</code>：移除 <code>build-metadata.json</code> fallback 读取块</li>\n</ul>\n<h3>Step 3: 移除 <code>build-metadata.json</code> 文件写入</h3>\n<ul>\n<li><code>build.ts</code>：移除 <code>serializeAlias()</code>、移除文件写入、只写 ctx</li>\n</ul>\n<h3>Step 4: 移除 <code>build-ssg.ts</code> 中所有 <code>.less/</code> fallback 读取</h3>\n<ul>\n<li><code>virtual:less-nav</code>：移除 <code>.less/nav-data.json</code> / <code>.less/header-nav.json</code> fallback</li>\n<li>博客初始化：移除 <code>.less/blog-options.json</code> fallback</li>\n<li>i18n：移除 <code>.less/i18n-options.json</code> fallback</li>\n<li>sitemap：移除 <code>.less/sitemap-options.json</code> fallback</li>\n</ul>\n<h3>Step 5: 清理和验证</h3>\n<ul>\n<li>移除 <code>build-client.ts</code> 和 <code>build-ssg.ts</code> 中未使用的 <code>import { ... from &#39;node:fs&#39; }</code></li>\n<li>运行 <code>deno task build</code> 端到端验证</li>\n<li>确认 <code>.less/</code> 目录不再被创建</li>\n</ul>\n<h2>Expected Impact</h2>\n<ul>\n<li><strong>文件写入点</strong>：3→0</li>\n<li><strong>文件读取点</strong>：6→0</li>\n<li><strong>代码行数</strong>：净减 ~120 行（双路径分支、序列化逻辑、fallback 读取）</li>\n<li><strong>功能变更</strong>：零——所有构建产物不变</li>\n</ul>\n"
  },
  {
    "slug": "0009-repo-simplification-issue",
    "frontmatter": {
      "title": "Repo-wide Simplification Roadmap: ADR 0008 + ADR 0009",
      "date": "2026-05-11",
      "draft": false,
      "tags": [
        "architecture",
        "decision"
      ],
      "type": "adr",
      "lang": "en"
    },
    "content": "\n## Overview\n\nTwo ADRs driving a comprehensive simplification of the LessJS codebase — preserving all functionality while eliminating coupling, duplication, and unnecessary abstraction layers.\n\n---\n\n### ADR 0008 — Pipeline-level Restructuring\n\nEliminate `createServer()`, `globalThis[Symbol.for()]` bridges, `.less/` temp file IPC, and `runtime-shim`.\n\n**Phases (execution order)**:\n\n- **Phase C+B** (merge): Remove `createServer()`, replace `ssrLoadModule` with `import()`, replace `globalThis` bridges with module variables\n- **Phase A**: Replace `.less/` temp file IPC with `define` injection into SSR bundle\n- **Phase D**: Delete `runtime-shim.ts` + `generate-runtime-shim.ts`, replace `less-runtime.ts` with `virtual:less-runtime`\n\n**Key result**: 3→2 Vite calls, 8+→0 temp files, 4→0 globalThis keys, SSR bundle actually consumed\n\n**Phase D detail — `virtual:less-runtime`**:\nDelete physical `less-runtime.ts` and the entire alias infrastructure (`writeFileSync` → `userResolveAlias` → `resolve.alias`). Replace with a `virtual:less-runtime` module implemented in the `less()` Vite plugin's `resolveId`/`load` hooks. Consumers keep the same import path (`@openelement/core/less-runtime`), zero code changes needed. Also removes `export { Hono } from 'hono'` — consumers import `Hono` directly.\n\n---\n\n### ADR 0009 — Repo-wide Simplification (on top of ADR 0008)\n\nLayered simplification across `@openelement/core` and cross-package boundaries.\n\n**Layer 1 — Core internal simplification** (6 independent + 2 dependent items):\n\n- [ ] 1.1 Delete `CodeBuilder` class → `string[]`\n- [ ] 1.2 Merge `hono-entry.ts` into `entry-renderer.ts`\n- [ ] 1.3 Eliminate inline HTML escape → use `escapeHtml()`\n- [ ] 1.4 Merge `renderPageRoute` wrapInDocument branches\n- [ ] 1.5 Merge `renderCorsOrigin` duplicate branches\n- [ ] 1.6 Merge `ssr-handler.ts` into `html-escape.ts`\n- [ ] 1.7 Extract `__ssr` to `ssr-helper.ts` _(needs Phase C+B)_\n- [ ] 1.8 Eliminate `__less_get_default_export` helper\n- [ ] 1.9 Remove `LessBuildContext.userResolveAlias` _(needs Phase C)_\n\n**Layer 2 — Cross-package decoupling**:\n\n- [ ] 2.1 Mark `render-dsd.ts` re-exports of `html-escape` as `@deprecated`\n- [ ] 2.2 Delete `runtime-shim.ts` _(ADR 0008 Phase D)_\n- [ ] 2.3 Align `blog-data.ts` with `i18n-data.ts` pattern _(ADR 0008 Phase B)_\n- [ ] 2.5 Extract `walkHtmlFiles()` from `ssg-postprocess.ts` (eliminate 5× duplication)\n\n**Layer 3 — Export cleanup & file deletion**:\n\n- [ ] 3.1 `render-dsd.ts` re-export chain cleanup\n- [ ] 3.2 **Delete `less-runtime.ts`** → replace with `virtual:less-runtime` in Vite plugin _(needs Phase C+B, zero consumer changes)_\n- [ ] 3.3 `index.ts` export audit (mark CLI internals as `@internal`)\n- [ ] 3.4 Extract `registerAdapter`/`getAdapter` to `adapter-registry.ts`\n\n---\n\n### Execution Order\n\n```\nPhase C+B (ADR 0008)\n  → Layer 1.1–1.6, 1.8 (all independent, can parallel)\n  → Layer 1.7, 1.9 (depend on C+B)\n  → less-runtime.ts → virtual:less-runtime + adapter-registry.ts extraction\nPhase A → D (ADR 0008)\n  → Layer 2 remaining\n  → Layer 3 export audit\n```\n\n### Expected Impact\n\n- **~350 lines reduced** in core (5846 → ~5495)\n- **5 files deleted**: `hono-entry.ts`, `ssr-handler.ts`, `less-runtime.ts`, `runtime-shim.ts`, `generate-runtime-shim.ts`\n- **2 files created**: `ssr-helper.ts`, `adapter-registry.ts` (clear single-responsibility modules)\n- **1 virtual module added**: `virtual:less-runtime` (replaces physical file + 35 lines of alias logic, zero consumer changes)\n- **Zero functionality changes** — all simplifications are behavior-preserving\n\n---\n\n_Full details: [ADR 0008](/blog/0008-eliminate-createserver-globalthis-bridges) and ADR 0009 artifact_\n",
    "html": "<h2>Overview</h2>\n<p>Two ADRs driving a comprehensive simplification of the LessJS codebase — preserving all functionality while eliminating coupling, duplication, and unnecessary abstraction layers.</p>\n<hr />\n<h3>ADR 0008 — Pipeline-level Restructuring</h3>\n<p>Eliminate <code>createServer()</code>, <code>globalThis[Symbol.for()]</code> bridges, <code>.less/</code> temp file IPC, and <code>runtime-shim</code>.</p>\n<p><strong>Phases (execution order)</strong>:</p>\n<ul>\n<li><strong>Phase C+B</strong> (merge): Remove <code>createServer()</code>, replace <code>ssrLoadModule</code> with <code>import()</code>, replace <code>globalThis</code> bridges with module variables</li>\n<li><strong>Phase A</strong>: Replace <code>.less/</code> temp file IPC with <code>define</code> injection into SSR bundle</li>\n<li><strong>Phase D</strong>: Delete <code>runtime-shim.ts</code> + <code>generate-runtime-shim.ts</code>, replace <code>less-runtime.ts</code> with <code>virtual:less-runtime</code></li>\n</ul>\n<p><strong>Key result</strong>: 3→2 Vite calls, 8+→0 temp files, 4→0 globalThis keys, SSR bundle actually consumed</p>\n<p><strong>Phase D detail — <code>virtual:less-runtime</code></strong>:\nDelete physical <code>less-runtime.ts</code> and the entire alias infrastructure (<code>writeFileSync</code> → <code>userResolveAlias</code> → <code>resolve.alias</code>). Replace with a <code>virtual:less-runtime</code> module implemented in the <code>less()</code> Vite plugin&#39;s <code>resolveId</code>/<code>load</code> hooks. Consumers keep the same import path (<code>@openelement/core/less-runtime</code>), zero code changes needed. Also removes <code>export { Hono } from &#39;hono&#39;</code> — consumers import <code>Hono</code> directly.</p>\n<hr />\n<h3>ADR 0009 — Repo-wide Simplification (on top of ADR 0008)</h3>\n<p>Layered simplification across <code>@openelement/core</code> and cross-package boundaries.</p>\n<p><strong>Layer 1 — Core internal simplification</strong> (6 independent + 2 dependent items):</p>\n<ul>\n<li><input disabled=\"\" type=\"checkbox\" /> 1.1 Delete <code>CodeBuilder</code> class → <code>string[]</code></li>\n<li><input disabled=\"\" type=\"checkbox\" /> 1.2 Merge <code>hono-entry.ts</code> into <code>entry-renderer.ts</code></li>\n<li><input disabled=\"\" type=\"checkbox\" /> 1.3 Eliminate inline HTML escape → use <code>escapeHtml()</code></li>\n<li><input disabled=\"\" type=\"checkbox\" /> 1.4 Merge <code>renderPageRoute</code> wrapInDocument branches</li>\n<li><input disabled=\"\" type=\"checkbox\" /> 1.5 Merge <code>renderCorsOrigin</code> duplicate branches</li>\n<li><input disabled=\"\" type=\"checkbox\" /> 1.6 Merge <code>ssr-handler.ts</code> into <code>html-escape.ts</code></li>\n<li><input disabled=\"\" type=\"checkbox\" /> 1.7 Extract <code>__ssr</code> to <code>ssr-helper.ts</code> <em>(needs Phase C+B)</em></li>\n<li><input disabled=\"\" type=\"checkbox\" /> 1.8 Eliminate <code>__less_get_default_export</code> helper</li>\n<li><input disabled=\"\" type=\"checkbox\" /> 1.9 Remove <code>LessBuildContext.userResolveAlias</code> <em>(needs Phase C)</em></li>\n</ul>\n<p><strong>Layer 2 — Cross-package decoupling</strong>:</p>\n<ul>\n<li><input disabled=\"\" type=\"checkbox\" /> 2.1 Mark <code>render-dsd.ts</code> re-exports of <code>html-escape</code> as <code>@deprecated</code></li>\n<li><input disabled=\"\" type=\"checkbox\" /> 2.2 Delete <code>runtime-shim.ts</code> <em>(ADR 0008 Phase D)</em></li>\n<li><input disabled=\"\" type=\"checkbox\" /> 2.3 Align <code>blog-data.ts</code> with <code>i18n-data.ts</code> pattern <em>(ADR 0008 Phase B)</em></li>\n<li><input disabled=\"\" type=\"checkbox\" /> 2.5 Extract <code>walkHtmlFiles()</code> from <code>ssg-postprocess.ts</code> (eliminate 5× duplication)</li>\n</ul>\n<p><strong>Layer 3 — Export cleanup &amp; file deletion</strong>:</p>\n<ul>\n<li><input disabled=\"\" type=\"checkbox\" /> 3.1 <code>render-dsd.ts</code> re-export chain cleanup</li>\n<li><input disabled=\"\" type=\"checkbox\" /> 3.2 <strong>Delete <code>less-runtime.ts</code></strong> → replace with <code>virtual:less-runtime</code> in Vite plugin <em>(needs Phase C+B, zero consumer changes)</em></li>\n<li><input disabled=\"\" type=\"checkbox\" /> 3.3 <code>index.ts</code> export audit (mark CLI internals as <code>@internal</code>)</li>\n<li><input disabled=\"\" type=\"checkbox\" /> 3.4 Extract <code>registerAdapter</code>/<code>getAdapter</code> to <code>adapter-registry.ts</code></li>\n</ul>\n<hr />\n<h3>Execution Order</h3>\n<pre><code>Phase C+B (ADR 0008)\n  → Layer 1.1–1.6, 1.8 (all independent, can parallel)\n  → Layer 1.7, 1.9 (depend on C+B)\n  → less-runtime.ts → virtual:less-runtime + adapter-registry.ts extraction\nPhase A → D (ADR 0008)\n  → Layer 2 remaining\n  → Layer 3 export audit\n</code></pre>\n<h3>Expected Impact</h3>\n<ul>\n<li><strong>~350 lines reduced</strong> in core (5846 → ~5495)</li>\n<li><strong>5 files deleted</strong>: <code>hono-entry.ts</code>, <code>ssr-handler.ts</code>, <code>less-runtime.ts</code>, <code>runtime-shim.ts</code>, <code>generate-runtime-shim.ts</code></li>\n<li><strong>2 files created</strong>: <code>ssr-helper.ts</code>, <code>adapter-registry.ts</code> (clear single-responsibility modules)</li>\n<li><strong>1 virtual module added</strong>: <code>virtual:less-runtime</code> (replaces physical file + 35 lines of alias logic, zero consumer changes)</li>\n<li><strong>Zero functionality changes</strong> — all simplifications are behavior-preserving</li>\n</ul>\n<hr />\n<p><em>Full details: <a href=\"/blog/0008-eliminate-createserver-globalthis-bridges\" rel=\"noopener noreferrer\">ADR 0008</a> and ADR 0009 artifact</em></p>\n"
  },
  {
    "slug": "0008-eliminate-createserver-globalthis-bridges",
    "frontmatter": {
      "title": "ADR 0008: 消除 createServer()、.less/ 临时文件与 globalThis 桥接",
      "date": "2026-05-10",
      "draft": false,
      "tags": [
        "architecture",
        "decision"
      ],
      "type": "adr",
      "lang": "zh"
    },
    "content": "\n## Status\n\n**IMPLEMENTED** — v0.9.x 架构重构（通过 ADR 0010/0011/0012 分步实施完成）\n\n## Context\n\nLessJS 当前的 SSG 构建管线存在三类耦合机制，它们互相依赖，形成了一个\"临时文件 + 全局状态桥接 + Vite Dev Server 中转\"的三角耦合：\n\n### 1. 构建管线：3 次 Vite 调用\n\n```\nPhase 1: viteBuild(client)      → 产出客户端 JS/CSS\nPhase 2: viteBuild(ssr:true)    → 产出 SSR bundle（但未实际消费）\nPhase 3: createServer(ssr)      → 用 Vite Dev Server 的 ssrLoadModule() 消费 SSR 代码\n```\n\n`createServer()` 的存在是为了解决 **Vite 模块多实例问题**：当 `viteBuild(ssr:true)` 产出 SSR bundle 后，如果用 Deno 的 `import()` 加载，会产生与 Phase 1 不同的模块实例，导致 `registerAdapter()` 注册的 adapter 对 `renderDSD()` 不可见。`createServer()` 通过 Vite 自身的 `ssrLoadModule()` 共享模块图，绕过了这个问题。\n\n**代价**：`createServer()` 启动一个完整的 Vite Dev Server，仅用于调用 `ssrLoadModule()`，资源浪费严重；且引入了 `globalThis.module` / `globalThis.exports` 的 CJS 兼容 shim（Phase 3 L221-223）。\n\n### 2. .less/ 临时文件：8+ 文件的 IPC 管道\n\nVite 插件的各个 Phase 运行在不同的 Vite 实例中，无法直接共享 JS 对象。当前用 `.less/` 目录作为文件系统的 IPC 管道：\n\n| 文件                          | 写入方                         | 读取方                          | 用途                 |\n| ----------------------------- | ------------------------------ | ------------------------------- | -------------------- |\n| `.less/blog-options.json`     | `@openelement/content` buildStart() | `build-ssg.ts` Phase 3          | 传递博客配置         |\n| `.less/nav-data.json`         | `@openelement/content` buildStart() | `build-ssg.ts` Phase 3 虚拟模块 | 传递导航数据         |\n| `.less/header-nav.json`       | `@openelement/content` buildStart() | `build-ssg.ts` Phase 3 虚拟模块 | 传递顶部导航         |\n| `.less/sitemap-options.json`  | `@openelement/content` buildStart() | `build-ssg.ts` Phase 3          | 传递站点地图配置     |\n| `.less/i18n-options.json`     | `@openelement/i18n` buildStart()    | `build-ssg.ts` Phase 3          | 传递国际化配置       |\n| `.less/head-extras.html`      | `build-ssg.ts` Phase 前置      | 生成的 entry 代码运行时         | 传递 headExtras HTML |\n| `.less/.less-runtime.ts`      | `less()` configResolved()      | Vite resolve alias              | 运行时 shim          |\n| `.less/.less-ssg-entry.ts`    | `build-ssg.ts` Phase 前置      | Vite 入口                       | SSG 入口代码         |\n| `.less/.less-client-entry.ts` | `build-ssg.ts`                 | Vite 入口                       | 客户端入口代码       |\n| `.less/build-metadata.json`   | `less()` closeBundle()         | `build-ssg.ts` Phase 前置       | 岛屿清单             |\n\n**问题**：文件系统 IPC 脆弱（文件可能缺失、格式错误、编码问题），且这些数据本可以在构建时内联到 SSR bundle 中。\n\n### 3. globalThis[Symbol.for()] 桥接：3 个跨实例桥\n\n| Symbol Key                                   | 所在文件                   | 用途                           |\n| -------------------------------------------- | -------------------------- | ------------------------------ |\n| `Symbol.for('lessjs:adapter')`               | `core/types.ts` L306       | 跨实例共享注册的 RenderAdapter |\n| `Symbol.for('lessjs:content:posts')`         | `content/blog-data.ts` L22 | 跨实例共享博客文章数据         |\n| `Symbol.for('lessjs:content:options')`       | `content/blog-data.ts` L23 | 跨实例共享博客配置             |\n| `Symbol.for('lessjs:lit-adapter-installed')` | `adapter-lit/ssr.ts` L465  | 跨实例幂等性守卫               |\n\n**为什么需要**：Vite SSR 可能将同一个包解析为不同的模块实例（通过 resolveAlias、symlinks、或 `ssr.noExternal` 不完整），导致模块作用域变量不共享。`Symbol.for()` 保证同一 realm 内返回相同的 Symbol，使 `globalThis[KEY]` 成为可靠的跨实例桥。\n\n**对比**：`@openelement/i18n` 的 `i18n-data.ts` 使用纯模块变量 `_options`，**没有 globalThis 耦合**——这是更干净的模式。\n\n### 4. 额外发现的耦合模式\n\n审计中发现的超出原始范围的耦合：\n\n#### 4a. runtime-shim.ts 机制 — 字符串化的代码重复\n\n`packages/core/scripts/generate-runtime-shim.ts`（223 行）使用 TypeScript AST 从源文件中提取函数，编译为 JS，拼成一个巨型字符串 `createRuntimeShimCode()`。这个字符串被写入 `.less/.less-runtime.ts`，通过 Vite resolve alias 指向。\n\n**问题**：这是源代码的手工同步副本。当 `render-dsd.ts`、`html-escape.ts`、`render-nested.ts` 的签名或行为变化时，必须运行 generator 脚本重新生成，否则运行时行为与源码不一致。ADR 0006 将此列为 P2 技术债。\n\n#### 4b. entry-renderer.ts 的 headExtras 文件读取\n\n`packages/core/src/entry-renderer.ts` L348-362：SSG 模式下，生成的 entry 代码在运行时 `readFileSync('.less/head-extras.html')`，而不是在构建时内联。原因注释说是为了避免大字符串破坏 Vite SSR 的 `AsyncFunction` 求值器。\n\n**问题**：这个限制仅在 `createServer()` + `ssrLoadModule()` 路径下存在。如果改为 `viteBuild(ssr:true)` + `import()` 路径，headExtras 可以作为构建时常量内联到 bundle 中。\n\n#### 4c. Symbol.for('lit-nothing') — 跨实例 Lit 值检测\n\n在 `adapter-lit/ssr.ts` 中，检测 Lit 的 `nothing` 值时使用 `Symbol.for('lit-nothing')`，同样是跨实例桥。bundle 化后只有一个 Lit 实例，此桥不再必要。\n\n## Decision\n\n### 核心洞察\n\n当 `viteBuild(ssr:true)` 配合 `noExternal` 产出 **自包含 ESM bundle** 时，上述所有耦合机制变得不必要：\n\n- **所有虚拟模块在编译时解析** → 不再需要 `createServer()` 运行时解析\n- **bundle 内只有一个模块实例** → `globalThis[Symbol.for()]` 桥接退化为普通模块变量\n- **所有数据在编译时内联** → `.less/` 临时文件 IPC 消除\n- **bundle 直接 `import()` 消费** → runtime-shim 字符串化机制不再需要\n\n### 新构建管线\n\n```\nPhase 1: viteBuild(ssr:true, noExternal) → 产出自包含 SSR bundle (dist/server/entry.js)\nPhase 2: import('./dist/server/entry.js')  → 直接消费 SSR bundle 渲染页面\nPhase 3: viteBuild(client)                → 产出客户端 JS/CSS\n```\n\n**关键变化**：\n\n- 3 次 Vite 调用 → 2 次（SSR bundle 真正被消费）\n- `createServer()` → 消除\n- `.less/` 临时文件 → 0（数据内联到 bundle）\n- `globalThis[Symbol.for()]` → 0（模块作用域变量）\n- `runtime-shim` 字符串 → 直接导入 `less-runtime`\n\n### 耦合消除映射表\n\n| 耦合机制                                                 | 当前用途                     | 重构后为何不必要                            |\n| -------------------------------------------------------- | ---------------------------- | ------------------------------------------- |\n| `createServer()`                                         | 运行时虚拟模块解析           | 虚拟模块在 `viteBuild` 编译时解析           |\n| `globalThis.module` / `globalThis.exports` shim          | createServer 的 CJS 兼容     | createServer 消除                           |\n| `globalThis[Symbol.for('lessjs:adapter')]`               | 跨实例共享 adapter           | bundle 单实例，模块变量即可                 |\n| `globalThis[Symbol.for('lessjs:content:posts')]`         | 跨实例共享博客数据           | bundle 单实例，模块变量即可                 |\n| `globalThis[Symbol.for('lessjs:content:options')]`       | 跨实例共享博客配置           | bundle 单实例，模块变量即可                 |\n| `globalThis[Symbol.for('lessjs:lit-adapter-installed')]` | 跨实例幂等守卫               | bundle 单实例，单次执行                     |\n| `.less/blog-options.json`                                | IPC：content 插件 → SSG      | 数据内联到 SSR bundle                       |\n| `.less/nav-data.json`                                    | IPC：content 插件 → 虚拟模块 | `virtual:less-nav` 编译时解析               |\n| `.less/header-nav.json`                                  | IPC：content 插件 → 虚拟模块 | 编译时解析                                  |\n| `.less/sitemap-options.json`                             | IPC：content 插件 → SSG      | 数据内联到 SSR bundle                       |\n| `.less/i18n-options.json`                                | IPC：i18n 插件 → SSG         | 数据内联到 SSR bundle                       |\n| `.less/head-extras.html`                                 | IPC：entry 代码 → 运行时读取 | 构建时常量内联到 bundle                     |\n| `.less/.less-runtime.ts`                                 | 运行时 shim 文件             | `virtual:less-runtime` 替代物理文件 + alias |\n| `.less/.less-ssg-entry.ts`                               | SSG 入口文件                 | 虚拟模块或 inline                           |\n| `.less/.less-client-entry.ts`                            | 客户端入口文件               | 虚拟模块或 inline                           |\n| `.less/build-metadata.json`                              | 岛屿清单 IPC                 | 编译时内联                                  |\n| `createRuntimeShimCode()`                                | 避免加载 Vite 插件依赖图     | bundle 直接引用 virtual:less-runtime        |\n| `less:ssg-virtual-nav` 插件                              | createServer 的虚拟模块桥接  | createServer 消除                           |\n| `Symbol.for('lit-nothing')`                              | 跨实例 Lit nothing 检测      | bundle 单实例                               |\n\n### 重构步骤（4 个 Phase）\n\n#### Phase A: 将 .less/ 临时文件 IPC 替换为编译时内联\n\n**目标**：消除所有 `.less/*.json` 和 `.less/head-extras.html`。\n\n1. **重构 `entry-renderer.ts`**：将 headExtras 作为 `define` 常量注入到 SSR bundle，而非运行时读取文件。在 `viteBuild` 配置中通过 `define: { '__LESS_HEAD_EXTRAS__': JSON.stringify(headExtras) }` 注入。\n\n2. **重构 `@openelement/content` 插件**：不再写 `.less/blog-options.json`、`.less/nav-data.json`、`.less/header-nav.json`、`.less/sitemap-options.json`。改为在 Vite `config()` hook 中通过 `define` 注入构建时常量：\n   ```ts\n   // 之前：写入 .less/blog-options.json\n   // 之后：通过 Vite define 注入\n   config() {\n     return {\n       define: {\n         '__LESS_BLOG_OPTIONS__': JSON.stringify(blogOptions),\n         '__LESS_NAV_DATA__': JSON.stringify(navSections),\n         '__LESS_HEADER_NAV__': JSON.stringify(headerNav),\n         '__LESS_SITEMAP_OPTIONS__': JSON.stringify(sitemapOptions),\n       }\n     };\n   }\n   ```\n\n3. **重构 `@openelement/i18n` 插件**：同样改为 `define` 注入 `__LESS_I18N_OPTIONS__`。\n\n4. **重构 `virtual:less-nav`**：改为从 `__LESS_NAV_DATA__` / `__LESS_HEADER_NAV__` 构建时常量导出，而非从 `.less/nav-data.json` 读取。\n\n5. **重构 `build-ssg.ts`**：移除所有 `readFileSync('.less/*.json')` 调用，改为从构建时注入的常量或 bundle 导出获取数据。\n\n#### Phase B: 将 globalThis[Symbol.for()] 桥接替换为模块变量\n\n**目标**：所有 `globalThis` 桥接退化为普通模块作用域变量，与 `i18n-data.ts` 的干净模式对齐。\n\n1. **`core/types.ts`**：\n   ```ts\n   // 之前\n   const ADAPTER_KEY = Symbol.for('lessjs:adapter');\n   export function registerAdapter(adapter) {\n     globalThis[ADAPTER_KEY] = adapter;\n   }\n   export function getAdapter() {\n     return globalThis[ADAPTER_KEY];\n   }\n\n   // 之后\n   let _adapter: RenderAdapter | undefined;\n   export function registerAdapter(adapter) {\n     _adapter = adapter;\n   }\n   export function getAdapter() {\n     return _adapter;\n   }\n   ```\n\n2. **`content/blog-data.ts`**：\n   ```ts\n   // 之前\n   const BLOG_POSTS_KEY = Symbol.for('lessjs:content:posts');\n   const BLOG_OPTIONS_KEY = Symbol.for('lessjs:content:options');\n   // ... globalThis 桥接 ...\n\n   // 之后\n   let _posts: BlogPost[] = [];\n   let _options: LessBlogOptions = {};\n   // 直接读写模块变量\n   ```\n\n3. **`adapter-lit/ssr.ts`**：\n   ```ts\n   // 之前\n   const INSTALLED_KEY = Symbol.for('lessjs:lit-adapter-installed');\n   if (globalThis[INSTALLED_KEY]) return;\n\n   // 之后\n   let _installed = false;\n   if (_installed) return;\n   _installed = true;\n   ```\n\n#### Phase C: 消除 createServer()，改为 viteBuild + import()\n\n**目标**：SSR bundle 真正被消费，而非仅产出后丢弃。\n\n1. **`build-ssg.ts` 核心变更**：\n   ```ts\n   // 之前（3 阶段）\n   await viteBuild(clientConfig); // Phase 1\n   await viteBuild(ssrConfig); // Phase 2（产出但未消费）\n   const server = await createServer(ssrServerOpts); // Phase 3（消费）\n   await server.ssrLoadModule('...');\n   await server.close();\n\n   // 之后（2 阶段）\n   await viteBuild(ssrConfig); // Phase 1：产出 SSR bundle\n   const ssrBundle = await import('./dist/server/entry.js'); // Phase 2：直接消费\n   await viteBuild(clientConfig); // Phase 3：产出客户端\n   ```\n\n2. **移除 `less:ssg-virtual-nav` 插件**：虚拟模块在 `viteBuild` 编译时已解析，不再需要运行时桥接。\n\n3. **移除 `globalThis.module` / `globalThis.exports` CJS shim**：不再使用 `createServer()`，无需 CJS 兼容。\n\n4. **移除 `customElements.define` 拦截的 `server` 依赖**：`build-ssg.ts` L295-307 的 `customElements.define` 幂等补丁改为在 bundle 入口代码中执行。\n\n5. **移除 `server.ssrLoadModule()` 的所有调用**：改用 `import()` 加载 bundle 导出的函数。这包括：\n   - L285: `server.ssrLoadModule('@openelement/adapter-lit')` → bundle 入口自动执行 `installLitAdapter()`\n   - L345-350: `server.ssrLoadModule('@openelement/core/render-dsd')` → 从 bundle 导入\n   - L371: `server.ssrLoadModule('@openelement/content')` → 从 bundle 导入\n   - L537-542: `server.ssrLoadModule('@openelement/core/render-dsd')` → 从 bundle 导入\n   - L557: `server.ssrLoadModule('@openelement/i18n')` → 从 bundle 导入\n   - L868: `server.ssrLoadModule('@openelement/content/sitemap')` → 从 bundle 导入\n\n6. **调整 SSR 构建顺序**：SSR 构建移到客户端构建之前（先产出 bundle → 消费渲染 → 再产出客户端资源）。\n\n#### Phase D: 消除 runtime-shim 机制\n\n**目标**：`.less/.less-runtime.ts` 和 `createRuntimeShimCode()` 不再需要。\n\n1. **SSR bundle 直接 import `virtual:less-runtime`**：当 bundle 自包含时，`@openelement/core/less-runtime` 路径由 Vite 插件的 `resolveId` hook 拦截并指向 `virtual:less-runtime`，该虚拟模块从各源文件 re-export（`registerAdapter` from `adapter-registry.ts`、`renderDSD` from `render-dsd.ts`、`wrapInDocument` from `html-escape.ts`、`log` from `logger.ts`）。\n\n2. **移除 `generate-runtime-shim.ts` 脚本**：AST 提取 + 编译 + 字符串化的机制不再需要。\n\n3. **移除 `runtime-shim.ts`**：巨型字符串常量不再需要。\n\n4. **移除 `less()` 插件中的 `.less/.less-runtime.ts` 写入逻辑**（`index.ts` L199-208）及相关的 `writeFileSync` + `userResolveAlias` + `resolve.alias` 配置（L183-217），全部由 `virtual:less-runtime` 的 `resolveId`/`load` hook 替代。\n\n5. **删除物理 `less-runtime.ts` 文件**：不再需要物理 re-export 文件，`virtual:less-runtime` 在插件中集中声明 re-export 映射。消费者 import 路径 `@openelement/core/less-runtime` 保持不变，由插件自动拦截。\n\n6. **移除 `export { Hono } from 'hono'`**：第三方包不应通过 runtime 中间层 re-export，消费者直接 `import { Hono } from 'hono'`。\n\n### 风险与缓解\n\n| 风险                                           | 影响                                         | 缓解                                                     |\n| ---------------------------------------------- | -------------------------------------------- | -------------------------------------------------------- |\n| SSR bundle 体积过大                            | `noExternal` 将 lit/parse5/entities 全部打包 | 当前 `createServer()` 也是全量加载，体积不减             |\n| `import()` 在 Deno 中路径解析                  | Deno 的 `import()` 不支持所有 Node 路径      | 使用绝对路径 `import(resolve('./dist/server/entry.js'))` |\n| `define` 注入大字符串（headExtras）            | JSON.stringify 大字符串可能影响构建性能      | headExtras 通常 < 10KB，可接受；若超限可 split chunk     |\n| Sitemap 生成依赖 `ssrLoadModule`               | 改为从 bundle 导入 `generateSitemap`         | bundle 已包含 `@openelement/content/sitemap`                  |\n| `viteBuild(ssr:true)` 不解析虚拟模块           | 需要确保 SSR 构建配置包含所有插件            | SSR 构建应复用用户的 Vite 配置 + 所有插件                |\n| Phase B 单独执行时可能破坏 `createServer` 路径 | 模块变量在 `ssrLoadModule` 下仍可能多实例    | Phase B 应与 Phase C 同步执行，或先做 Phase C            |\n\n### 建议执行顺序\n\n**推荐**：先 C 后 B，再 A，最后 D。理由：\n\n1. **Phase C 优先**：消除 `createServer()` 是所有其他变更的前提。只有 bundle 被直接消费后，globalThis 桥接和文件 IPC 才能安全移除。\n2. **Phase B 与 C 同步**：`createServer()` 消除后，globalThis 桥接自动变得不必要，应同步清理。\n3. **Phase A 紧随**：`.less/` 临时文件可以改为 `define` 注入，但需要确认 SSR 构建配置能正确传递这些 `define`。\n4. **Phase D 最后**：runtime-shim 消除依赖前面所有 Phase 完成，是纯清理工作。\n\n### 预期收益\n\n| 指标                        | 当前                    | 重构后                  |\n| --------------------------- | ----------------------- | ----------------------- |\n| Vite 调用次数               | 3                       | 2                       |\n| `createServer()`            | 有                      | 无                      |\n| `.less/` 临时文件           | 8+                      | 0                       |\n| `globalThis` 桥接键         | 4                       | 0                       |\n| SSR bundle 是否被消费       | 否（产出后丢弃）        | 是（直接 import）       |\n| `runtime-shim` 字符串化     | 有（手工同步）          | 无                      |\n| `less:ssg-virtual-nav` 插件 | 有                      | 无                      |\n| CJS 兼容 shim               | 有                      | 无                      |\n| 构建管线复杂度              | 高（3 阶段 + 文件 IPC） | 低（2 阶段 + 内存传递） |\n\n## Consequences\n\n**正面：**\n\n- 构建管线从 3 阶段简化到 2 阶段，概念复杂度大幅降低\n- 消除文件系统 IPC，消除\"文件缺失/格式错误/编码问题\"类的间歇性 bug\n- 消除 `globalThis` 污染，SSR 运行更加干净\n- 消除 runtime-shim 手工同步问题，SSR 输出与源码一致\n- SSR bundle 真正被消费，不再\"产出后丢弃\"\n- 为后续增量构建（ISR）奠定基础：bundle 可缓存\n\n**负面：**\n\n- 一次性重构量大，需谨慎分 Phase 执行\n- SSR bundle 的 `import()` 路径在不同运行时（Deno/Node）下行为可能不同\n- `define` 注入的方式需要在 SSR 构建配置中正确传递所有插件定义\n\n**缓解：**\n\n- 严格按 Phase 执行，每个 Phase 完成后运行全量测试\n- SSR bundle 使用绝对路径 `import()`，兼容 Deno 和 Node\n- 保留 `.less/` 目录作为构建缓存（如增量构建 hash），但不作为 IPC\n\n## 参考\n\n- [ADR 0005: WithDsdHydration Mixin](/blog/0005-with-dsd-hydration-mixin)\n- [ADR 0006: 版本号策略](/blog/0006-version-strategy)\n- `packages/core/src/cli/build-ssg.ts` — 当前 SSG 构建主文件\n- `packages/core/src/types.ts` — adapter 全局桥定义\n- `packages/content/src/blog/blog-data.ts` — blog 全局桥定义\n- `packages/adapter-lit/src/ssr.ts` — Lit adapter 幂等守卫\n- `packages/i18n/src/i18n-data.ts` — 干净模式参考（无 globalThis）\n\n---\n\n_决策日期: 2026-05-10 | 版本: v0.9.0_\n\n## Implementation Summary\n\n本 ADR 的各 Phase 通过后续 ADR 分步实施完成：\n\n| Phase | 描述                                              | 实施 ADR                   | 状态    |\n| ----- | ------------------------------------------------- | -------------------------- | ------- |\n| A     | .less/ 临时文件 IPC → LessBuildContext + 虚拟模块 | ADR 0010                   | ✅ 完成 |\n| B     | globalThis[Symbol.for()] → 模块变量               | ADR 0011 (部分) + ADR 0012 | ✅ 完成 |\n| C     | 消除 createServer()，改为 closeBundle 内联        | ADR 0011                   | ✅ 完成 |\n| D     | runtime-shim → virtual:less-runtime               | ADR 0010 (Step 1)          | ✅ 完成 |\n| E     | lessjs() 统一入口 → 拆到 @openelement/app              | ADR 0012                   | ✅ 完成 |\n\n### 最终指标\n\n| 指标                    | ADR 0008 提出时         | 当前                              |\n| ----------------------- | ----------------------- | --------------------------------- |\n| `createServer()`        | 有                      | **无**                            |\n| `.less/` 临时文件       | 8+                      | **0**                             |\n| `globalThis` 桥接键     | 4                       | **0**                             |\n| `runtime-shim` 字符串化 | 有                      | 无（使用 virtual:less-runtime）   |\n| 构建管线                | 3 阶段 + 文件 IPC       | 1 次 viteBuild + closeBundle 内联 |\n| ctx 传递                | globalThis 隐式共享     | 显式参数传递                      |\n| `lessjs()` 入口         | 在 core 中，动态 import | 在 @openelement/app 中，静态 import    |\n",
    "html": "<h2>Status</h2>\n<p><strong>IMPLEMENTED</strong> — v0.9.x 架构重构（通过 ADR 0010/0011/0012 分步实施完成）</p>\n<h2>Context</h2>\n<p>LessJS 当前的 SSG 构建管线存在三类耦合机制，它们互相依赖，形成了一个&quot;临时文件 + 全局状态桥接 + Vite Dev Server 中转&quot;的三角耦合：</p>\n<h3>1. 构建管线：3 次 Vite 调用</h3>\n<pre><code>Phase 1: viteBuild(client)      → 产出客户端 JS/CSS\nPhase 2: viteBuild(ssr:true)    → 产出 SSR bundle（但未实际消费）\nPhase 3: createServer(ssr)      → 用 Vite Dev Server 的 ssrLoadModule() 消费 SSR 代码\n</code></pre>\n<p><code>createServer()</code> 的存在是为了解决 <strong>Vite 模块多实例问题</strong>：当 <code>viteBuild(ssr:true)</code> 产出 SSR bundle 后，如果用 Deno 的 <code>import()</code> 加载，会产生与 Phase 1 不同的模块实例，导致 <code>registerAdapter()</code> 注册的 adapter 对 <code>renderDSD()</code> 不可见。<code>createServer()</code> 通过 Vite 自身的 <code>ssrLoadModule()</code> 共享模块图，绕过了这个问题。</p>\n<p><strong>代价</strong>：<code>createServer()</code> 启动一个完整的 Vite Dev Server，仅用于调用 <code>ssrLoadModule()</code>，资源浪费严重；且引入了 <code>globalThis.module</code> / <code>globalThis.exports</code> 的 CJS 兼容 shim（Phase 3 L221-223）。</p>\n<h3>2. .less/ 临时文件：8+ 文件的 IPC 管道</h3>\n<p>Vite 插件的各个 Phase 运行在不同的 Vite 实例中，无法直接共享 JS 对象。当前用 <code>.less/</code> 目录作为文件系统的 IPC 管道：</p>\n<table>\n<thead>\n<tr>\n<th>文件</th>\n<th>写入方</th>\n<th>读取方</th>\n<th>用途</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>.less/blog-options.json</code></td>\n<td><code>@openelement/content</code> buildStart()</td>\n<td><code>build-ssg.ts</code> Phase 3</td>\n<td>传递博客配置</td>\n</tr>\n<tr>\n<td><code>.less/nav-data.json</code></td>\n<td><code>@openelement/content</code> buildStart()</td>\n<td><code>build-ssg.ts</code> Phase 3 虚拟模块</td>\n<td>传递导航数据</td>\n</tr>\n<tr>\n<td><code>.less/header-nav.json</code></td>\n<td><code>@openelement/content</code> buildStart()</td>\n<td><code>build-ssg.ts</code> Phase 3 虚拟模块</td>\n<td>传递顶部导航</td>\n</tr>\n<tr>\n<td><code>.less/sitemap-options.json</code></td>\n<td><code>@openelement/content</code> buildStart()</td>\n<td><code>build-ssg.ts</code> Phase 3</td>\n<td>传递站点地图配置</td>\n</tr>\n<tr>\n<td><code>.less/i18n-options.json</code></td>\n<td><code>@openelement/i18n</code> buildStart()</td>\n<td><code>build-ssg.ts</code> Phase 3</td>\n<td>传递国际化配置</td>\n</tr>\n<tr>\n<td><code>.less/head-extras.html</code></td>\n<td><code>build-ssg.ts</code> Phase 前置</td>\n<td>生成的 entry 代码运行时</td>\n<td>传递 headExtras HTML</td>\n</tr>\n<tr>\n<td><code>.less/.less-runtime.ts</code></td>\n<td><code>less()</code> configResolved()</td>\n<td>Vite resolve alias</td>\n<td>运行时 shim</td>\n</tr>\n<tr>\n<td><code>.less/.less-ssg-entry.ts</code></td>\n<td><code>build-ssg.ts</code> Phase 前置</td>\n<td>Vite 入口</td>\n<td>SSG 入口代码</td>\n</tr>\n<tr>\n<td><code>.less/.less-client-entry.ts</code></td>\n<td><code>build-ssg.ts</code></td>\n<td>Vite 入口</td>\n<td>客户端入口代码</td>\n</tr>\n<tr>\n<td><code>.less/build-metadata.json</code></td>\n<td><code>less()</code> closeBundle()</td>\n<td><code>build-ssg.ts</code> Phase 前置</td>\n<td>岛屿清单</td>\n</tr>\n</tbody></table>\n<p><strong>问题</strong>：文件系统 IPC 脆弱（文件可能缺失、格式错误、编码问题），且这些数据本可以在构建时内联到 SSR bundle 中。</p>\n<h3>3. globalThis[Symbol.for()] 桥接：3 个跨实例桥</h3>\n<table>\n<thead>\n<tr>\n<th>Symbol Key</th>\n<th>所在文件</th>\n<th>用途</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>Symbol.for(&#39;lessjs:adapter&#39;)</code></td>\n<td><code>core/types.ts</code> L306</td>\n<td>跨实例共享注册的 RenderAdapter</td>\n</tr>\n<tr>\n<td><code>Symbol.for(&#39;lessjs:content:posts&#39;)</code></td>\n<td><code>content/blog-data.ts</code> L22</td>\n<td>跨实例共享博客文章数据</td>\n</tr>\n<tr>\n<td><code>Symbol.for(&#39;lessjs:content:options&#39;)</code></td>\n<td><code>content/blog-data.ts</code> L23</td>\n<td>跨实例共享博客配置</td>\n</tr>\n<tr>\n<td><code>Symbol.for(&#39;lessjs:lit-adapter-installed&#39;)</code></td>\n<td><code>adapter-lit/ssr.ts</code> L465</td>\n<td>跨实例幂等性守卫</td>\n</tr>\n</tbody></table>\n<p><strong>为什么需要</strong>：Vite SSR 可能将同一个包解析为不同的模块实例（通过 resolveAlias、symlinks、或 <code>ssr.noExternal</code> 不完整），导致模块作用域变量不共享。<code>Symbol.for()</code> 保证同一 realm 内返回相同的 Symbol，使 <code>globalThis[KEY]</code> 成为可靠的跨实例桥。</p>\n<p><strong>对比</strong>：<code>@openelement/i18n</code> 的 <code>i18n-data.ts</code> 使用纯模块变量 <code>_options</code>，<strong>没有 globalThis 耦合</strong>——这是更干净的模式。</p>\n<h3>4. 额外发现的耦合模式</h3>\n<p>审计中发现的超出原始范围的耦合：</p>\n<h4>4a. runtime-shim.ts 机制 — 字符串化的代码重复</h4>\n<p><code>packages/core/scripts/generate-runtime-shim.ts</code>（223 行）使用 TypeScript AST 从源文件中提取函数，编译为 JS，拼成一个巨型字符串 <code>createRuntimeShimCode()</code>。这个字符串被写入 <code>.less/.less-runtime.ts</code>，通过 Vite resolve alias 指向。</p>\n<p><strong>问题</strong>：这是源代码的手工同步副本。当 <code>render-dsd.ts</code>、<code>html-escape.ts</code>、<code>render-nested.ts</code> 的签名或行为变化时，必须运行 generator 脚本重新生成，否则运行时行为与源码不一致。ADR 0006 将此列为 P2 技术债。</p>\n<h4>4b. entry-renderer.ts 的 headExtras 文件读取</h4>\n<p><code>packages/core/src/entry-renderer.ts</code> L348-362：SSG 模式下，生成的 entry 代码在运行时 <code>readFileSync(&#39;.less/head-extras.html&#39;)</code>，而不是在构建时内联。原因注释说是为了避免大字符串破坏 Vite SSR 的 <code>AsyncFunction</code> 求值器。</p>\n<p><strong>问题</strong>：这个限制仅在 <code>createServer()</code> + <code>ssrLoadModule()</code> 路径下存在。如果改为 <code>viteBuild(ssr:true)</code> + <code>import()</code> 路径，headExtras 可以作为构建时常量内联到 bundle 中。</p>\n<h4>4c. Symbol.for(&#39;lit-nothing&#39;) — 跨实例 Lit 值检测</h4>\n<p>在 <code>adapter-lit/ssr.ts</code> 中，检测 Lit 的 <code>nothing</code> 值时使用 <code>Symbol.for(&#39;lit-nothing&#39;)</code>，同样是跨实例桥。bundle 化后只有一个 Lit 实例，此桥不再必要。</p>\n<h2>Decision</h2>\n<h3>核心洞察</h3>\n<p>当 <code>viteBuild(ssr:true)</code> 配合 <code>noExternal</code> 产出 <strong>自包含 ESM bundle</strong> 时，上述所有耦合机制变得不必要：</p>\n<ul>\n<li><strong>所有虚拟模块在编译时解析</strong> → 不再需要 <code>createServer()</code> 运行时解析</li>\n<li><strong>bundle 内只有一个模块实例</strong> → <code>globalThis[Symbol.for()]</code> 桥接退化为普通模块变量</li>\n<li><strong>所有数据在编译时内联</strong> → <code>.less/</code> 临时文件 IPC 消除</li>\n<li><strong>bundle 直接 <code>import()</code> 消费</strong> → runtime-shim 字符串化机制不再需要</li>\n</ul>\n<h3>新构建管线</h3>\n<pre><code>Phase 1: viteBuild(ssr:true, noExternal) → 产出自包含 SSR bundle (dist/server/entry.js)\nPhase 2: import(&#39;./dist/server/entry.js&#39;)  → 直接消费 SSR bundle 渲染页面\nPhase 3: viteBuild(client)                → 产出客户端 JS/CSS\n</code></pre>\n<p><strong>关键变化</strong>：</p>\n<ul>\n<li>3 次 Vite 调用 → 2 次（SSR bundle 真正被消费）</li>\n<li><code>createServer()</code> → 消除</li>\n<li><code>.less/</code> 临时文件 → 0（数据内联到 bundle）</li>\n<li><code>globalThis[Symbol.for()]</code> → 0（模块作用域变量）</li>\n<li><code>runtime-shim</code> 字符串 → 直接导入 <code>less-runtime</code></li>\n</ul>\n<h3>耦合消除映射表</h3>\n<table>\n<thead>\n<tr>\n<th>耦合机制</th>\n<th>当前用途</th>\n<th>重构后为何不必要</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>createServer()</code></td>\n<td>运行时虚拟模块解析</td>\n<td>虚拟模块在 <code>viteBuild</code> 编译时解析</td>\n</tr>\n<tr>\n<td><code>globalThis.module</code> / <code>globalThis.exports</code> shim</td>\n<td>createServer 的 CJS 兼容</td>\n<td>createServer 消除</td>\n</tr>\n<tr>\n<td><code>globalThis[Symbol.for(&#39;lessjs:adapter&#39;)]</code></td>\n<td>跨实例共享 adapter</td>\n<td>bundle 单实例，模块变量即可</td>\n</tr>\n<tr>\n<td><code>globalThis[Symbol.for(&#39;lessjs:content:posts&#39;)]</code></td>\n<td>跨实例共享博客数据</td>\n<td>bundle 单实例，模块变量即可</td>\n</tr>\n<tr>\n<td><code>globalThis[Symbol.for(&#39;lessjs:content:options&#39;)]</code></td>\n<td>跨实例共享博客配置</td>\n<td>bundle 单实例，模块变量即可</td>\n</tr>\n<tr>\n<td><code>globalThis[Symbol.for(&#39;lessjs:lit-adapter-installed&#39;)]</code></td>\n<td>跨实例幂等守卫</td>\n<td>bundle 单实例，单次执行</td>\n</tr>\n<tr>\n<td><code>.less/blog-options.json</code></td>\n<td>IPC：content 插件 → SSG</td>\n<td>数据内联到 SSR bundle</td>\n</tr>\n<tr>\n<td><code>.less/nav-data.json</code></td>\n<td>IPC：content 插件 → 虚拟模块</td>\n<td><code>virtual:less-nav</code> 编译时解析</td>\n</tr>\n<tr>\n<td><code>.less/header-nav.json</code></td>\n<td>IPC：content 插件 → 虚拟模块</td>\n<td>编译时解析</td>\n</tr>\n<tr>\n<td><code>.less/sitemap-options.json</code></td>\n<td>IPC：content 插件 → SSG</td>\n<td>数据内联到 SSR bundle</td>\n</tr>\n<tr>\n<td><code>.less/i18n-options.json</code></td>\n<td>IPC：i18n 插件 → SSG</td>\n<td>数据内联到 SSR bundle</td>\n</tr>\n<tr>\n<td><code>.less/head-extras.html</code></td>\n<td>IPC：entry 代码 → 运行时读取</td>\n<td>构建时常量内联到 bundle</td>\n</tr>\n<tr>\n<td><code>.less/.less-runtime.ts</code></td>\n<td>运行时 shim 文件</td>\n<td><code>virtual:less-runtime</code> 替代物理文件 + alias</td>\n</tr>\n<tr>\n<td><code>.less/.less-ssg-entry.ts</code></td>\n<td>SSG 入口文件</td>\n<td>虚拟模块或 inline</td>\n</tr>\n<tr>\n<td><code>.less/.less-client-entry.ts</code></td>\n<td>客户端入口文件</td>\n<td>虚拟模块或 inline</td>\n</tr>\n<tr>\n<td><code>.less/build-metadata.json</code></td>\n<td>岛屿清单 IPC</td>\n<td>编译时内联</td>\n</tr>\n<tr>\n<td><code>createRuntimeShimCode()</code></td>\n<td>避免加载 Vite 插件依赖图</td>\n<td>bundle 直接引用 virtual:less-runtime</td>\n</tr>\n<tr>\n<td><code>less:ssg-virtual-nav</code> 插件</td>\n<td>createServer 的虚拟模块桥接</td>\n<td>createServer 消除</td>\n</tr>\n<tr>\n<td><code>Symbol.for(&#39;lit-nothing&#39;)</code></td>\n<td>跨实例 Lit nothing 检测</td>\n<td>bundle 单实例</td>\n</tr>\n</tbody></table>\n<h3>重构步骤（4 个 Phase）</h3>\n<h4>Phase A: 将 .less/ 临时文件 IPC 替换为编译时内联</h4>\n<p><strong>目标</strong>：消除所有 <code>.less/*.json</code> 和 <code>.less/head-extras.html</code>。</p>\n<ol>\n<li><p><strong>重构 <code>entry-renderer.ts</code></strong>：将 headExtras 作为 <code>define</code> 常量注入到 SSR bundle，而非运行时读取文件。在 <code>viteBuild</code> 配置中通过 <code>define: { &#39;__LESS_HEAD_EXTRAS__&#39;: JSON.stringify(headExtras) }</code> 注入。</p>\n</li>\n<li><p><strong>重构 <code>@openelement/content</code> 插件</strong>：不再写 <code>.less/blog-options.json</code>、<code>.less/nav-data.json</code>、<code>.less/header-nav.json</code>、<code>.less/sitemap-options.json</code>。改为在 Vite <code>config()</code> hook 中通过 <code>define</code> 注入构建时常量：</p>\n<pre><code class=\"language-ts\">// 之前：写入 .less/blog-options.json\n// 之后：通过 Vite define 注入\nconfig() {\n  return {\n    define: {\n      &#39;__LESS_BLOG_OPTIONS__&#39;: JSON.stringify(blogOptions),\n      &#39;__LESS_NAV_DATA__&#39;: JSON.stringify(navSections),\n      &#39;__LESS_HEADER_NAV__&#39;: JSON.stringify(headerNav),\n      &#39;__LESS_SITEMAP_OPTIONS__&#39;: JSON.stringify(sitemapOptions),\n    }\n  };\n}\n</code></pre>\n</li>\n<li><p><strong>重构 <code>@openelement/i18n</code> 插件</strong>：同样改为 <code>define</code> 注入 <code>__LESS_I18N_OPTIONS__</code>。</p>\n</li>\n<li><p><strong>重构 <code>virtual:less-nav</code></strong>：改为从 <code>__LESS_NAV_DATA__</code> / <code>__LESS_HEADER_NAV__</code> 构建时常量导出，而非从 <code>.less/nav-data.json</code> 读取。</p>\n</li>\n<li><p><strong>重构 <code>build-ssg.ts</code></strong>：移除所有 <code>readFileSync(&#39;.less/*.json&#39;)</code> 调用，改为从构建时注入的常量或 bundle 导出获取数据。</p>\n</li>\n</ol>\n<h4>Phase B: 将 globalThis[Symbol.for()] 桥接替换为模块变量</h4>\n<p><strong>目标</strong>：所有 <code>globalThis</code> 桥接退化为普通模块作用域变量，与 <code>i18n-data.ts</code> 的干净模式对齐。</p>\n<ol>\n<li><p><strong><code>core/types.ts</code></strong>：</p>\n<pre><code class=\"language-ts\">// 之前\nconst ADAPTER_KEY = Symbol.for(&#39;lessjs:adapter&#39;);\nexport function registerAdapter(adapter) {\n  globalThis[ADAPTER_KEY] = adapter;\n}\nexport function getAdapter() {\n  return globalThis[ADAPTER_KEY];\n}\n\n// 之后\nlet _adapter: RenderAdapter | undefined;\nexport function registerAdapter(adapter) {\n  _adapter = adapter;\n}\nexport function getAdapter() {\n  return _adapter;\n}\n</code></pre>\n</li>\n<li><p><strong><code>content/blog-data.ts</code></strong>：</p>\n<pre><code class=\"language-ts\">// 之前\nconst BLOG_POSTS_KEY = Symbol.for(&#39;lessjs:content:posts&#39;);\nconst BLOG_OPTIONS_KEY = Symbol.for(&#39;lessjs:content:options&#39;);\n// ... globalThis 桥接 ...\n\n// 之后\nlet _posts: BlogPost[] = [];\nlet _options: LessBlogOptions = {};\n// 直接读写模块变量\n</code></pre>\n</li>\n<li><p><strong><code>adapter-lit/ssr.ts</code></strong>：</p>\n<pre><code class=\"language-ts\">// 之前\nconst INSTALLED_KEY = Symbol.for(&#39;lessjs:lit-adapter-installed&#39;);\nif (globalThis[INSTALLED_KEY]) return;\n\n// 之后\nlet _installed = false;\nif (_installed) return;\n_installed = true;\n</code></pre>\n</li>\n</ol>\n<h4>Phase C: 消除 createServer()，改为 viteBuild + import()</h4>\n<p><strong>目标</strong>：SSR bundle 真正被消费，而非仅产出后丢弃。</p>\n<ol>\n<li><p><strong><code>build-ssg.ts</code> 核心变更</strong>：</p>\n<pre><code class=\"language-ts\">// 之前（3 阶段）\nawait viteBuild(clientConfig); // Phase 1\nawait viteBuild(ssrConfig); // Phase 2（产出但未消费）\nconst server = await createServer(ssrServerOpts); // Phase 3（消费）\nawait server.ssrLoadModule(&#39;...&#39;);\nawait server.close();\n\n// 之后（2 阶段）\nawait viteBuild(ssrConfig); // Phase 1：产出 SSR bundle\nconst ssrBundle = await import(&#39;./dist/server/entry.js&#39;); // Phase 2：直接消费\nawait viteBuild(clientConfig); // Phase 3：产出客户端\n</code></pre>\n</li>\n<li><p><strong>移除 <code>less:ssg-virtual-nav</code> 插件</strong>：虚拟模块在 <code>viteBuild</code> 编译时已解析，不再需要运行时桥接。</p>\n</li>\n<li><p><strong>移除 <code>globalThis.module</code> / <code>globalThis.exports</code> CJS shim</strong>：不再使用 <code>createServer()</code>，无需 CJS 兼容。</p>\n</li>\n<li><p><strong>移除 <code>customElements.define</code> 拦截的 <code>server</code> 依赖</strong>：<code>build-ssg.ts</code> L295-307 的 <code>customElements.define</code> 幂等补丁改为在 bundle 入口代码中执行。</p>\n</li>\n<li><p><strong>移除 <code>server.ssrLoadModule()</code> 的所有调用</strong>：改用 <code>import()</code> 加载 bundle 导出的函数。这包括：</p>\n<ul>\n<li>L285: <code>server.ssrLoadModule(&#39;@openelement/adapter-lit&#39;)</code> → bundle 入口自动执行 <code>installLitAdapter()</code></li>\n<li>L345-350: <code>server.ssrLoadModule(&#39;@openelement/core/render-dsd&#39;)</code> → 从 bundle 导入</li>\n<li>L371: <code>server.ssrLoadModule(&#39;@openelement/content&#39;)</code> → 从 bundle 导入</li>\n<li>L537-542: <code>server.ssrLoadModule(&#39;@openelement/core/render-dsd&#39;)</code> → 从 bundle 导入</li>\n<li>L557: <code>server.ssrLoadModule(&#39;@openelement/i18n&#39;)</code> → 从 bundle 导入</li>\n<li>L868: <code>server.ssrLoadModule(&#39;@openelement/content/sitemap&#39;)</code> → 从 bundle 导入</li>\n</ul>\n</li>\n<li><p><strong>调整 SSR 构建顺序</strong>：SSR 构建移到客户端构建之前（先产出 bundle → 消费渲染 → 再产出客户端资源）。</p>\n</li>\n</ol>\n<h4>Phase D: 消除 runtime-shim 机制</h4>\n<p><strong>目标</strong>：<code>.less/.less-runtime.ts</code> 和 <code>createRuntimeShimCode()</code> 不再需要。</p>\n<ol>\n<li><p><strong>SSR bundle 直接 import <code>virtual:less-runtime</code></strong>：当 bundle 自包含时，<code>@openelement/core/less-runtime</code> 路径由 Vite 插件的 <code>resolveId</code> hook 拦截并指向 <code>virtual:less-runtime</code>，该虚拟模块从各源文件 re-export（<code>registerAdapter</code> from <code>adapter-registry.ts</code>、<code>renderDSD</code> from <code>render-dsd.ts</code>、<code>wrapInDocument</code> from <code>html-escape.ts</code>、<code>log</code> from <code>logger.ts</code>）。</p>\n</li>\n<li><p><strong>移除 <code>generate-runtime-shim.ts</code> 脚本</strong>：AST 提取 + 编译 + 字符串化的机制不再需要。</p>\n</li>\n<li><p><strong>移除 <code>runtime-shim.ts</code></strong>：巨型字符串常量不再需要。</p>\n</li>\n<li><p><strong>移除 <code>less()</code> 插件中的 <code>.less/.less-runtime.ts</code> 写入逻辑</strong>（<code>index.ts</code> L199-208）及相关的 <code>writeFileSync</code> + <code>userResolveAlias</code> + <code>resolve.alias</code> 配置（L183-217），全部由 <code>virtual:less-runtime</code> 的 <code>resolveId</code>/<code>load</code> hook 替代。</p>\n</li>\n<li><p><strong>删除物理 <code>less-runtime.ts</code> 文件</strong>：不再需要物理 re-export 文件，<code>virtual:less-runtime</code> 在插件中集中声明 re-export 映射。消费者 import 路径 <code>@openelement/core/less-runtime</code> 保持不变，由插件自动拦截。</p>\n</li>\n<li><p><strong>移除 <code>export { Hono } from &#39;hono&#39;</code></strong>：第三方包不应通过 runtime 中间层 re-export，消费者直接 <code>import { Hono } from &#39;hono&#39;</code>。</p>\n</li>\n</ol>\n<h3>风险与缓解</h3>\n<table>\n<thead>\n<tr>\n<th>风险</th>\n<th>影响</th>\n<th>缓解</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>SSR bundle 体积过大</td>\n<td><code>noExternal</code> 将 lit/parse5/entities 全部打包</td>\n<td>当前 <code>createServer()</code> 也是全量加载，体积不减</td>\n</tr>\n<tr>\n<td><code>import()</code> 在 Deno 中路径解析</td>\n<td>Deno 的 <code>import()</code> 不支持所有 Node 路径</td>\n<td>使用绝对路径 <code>import(resolve(&#39;./dist/server/entry.js&#39;))</code></td>\n</tr>\n<tr>\n<td><code>define</code> 注入大字符串（headExtras）</td>\n<td>JSON.stringify 大字符串可能影响构建性能</td>\n<td>headExtras 通常 &lt; 10KB，可接受；若超限可 split chunk</td>\n</tr>\n<tr>\n<td>Sitemap 生成依赖 <code>ssrLoadModule</code></td>\n<td>改为从 bundle 导入 <code>generateSitemap</code></td>\n<td>bundle 已包含 <code>@openelement/content/sitemap</code></td>\n</tr>\n<tr>\n<td><code>viteBuild(ssr:true)</code> 不解析虚拟模块</td>\n<td>需要确保 SSR 构建配置包含所有插件</td>\n<td>SSR 构建应复用用户的 Vite 配置 + 所有插件</td>\n</tr>\n<tr>\n<td>Phase B 单独执行时可能破坏 <code>createServer</code> 路径</td>\n<td>模块变量在 <code>ssrLoadModule</code> 下仍可能多实例</td>\n<td>Phase B 应与 Phase C 同步执行，或先做 Phase C</td>\n</tr>\n</tbody></table>\n<h3>建议执行顺序</h3>\n<p><strong>推荐</strong>：先 C 后 B，再 A，最后 D。理由：</p>\n<ol>\n<li><strong>Phase C 优先</strong>：消除 <code>createServer()</code> 是所有其他变更的前提。只有 bundle 被直接消费后，globalThis 桥接和文件 IPC 才能安全移除。</li>\n<li><strong>Phase B 与 C 同步</strong>：<code>createServer()</code> 消除后，globalThis 桥接自动变得不必要，应同步清理。</li>\n<li><strong>Phase A 紧随</strong>：<code>.less/</code> 临时文件可以改为 <code>define</code> 注入，但需要确认 SSR 构建配置能正确传递这些 <code>define</code>。</li>\n<li><strong>Phase D 最后</strong>：runtime-shim 消除依赖前面所有 Phase 完成，是纯清理工作。</li>\n</ol>\n<h3>预期收益</h3>\n<table>\n<thead>\n<tr>\n<th>指标</th>\n<th>当前</th>\n<th>重构后</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Vite 调用次数</td>\n<td>3</td>\n<td>2</td>\n</tr>\n<tr>\n<td><code>createServer()</code></td>\n<td>有</td>\n<td>无</td>\n</tr>\n<tr>\n<td><code>.less/</code> 临时文件</td>\n<td>8+</td>\n<td>0</td>\n</tr>\n<tr>\n<td><code>globalThis</code> 桥接键</td>\n<td>4</td>\n<td>0</td>\n</tr>\n<tr>\n<td>SSR bundle 是否被消费</td>\n<td>否（产出后丢弃）</td>\n<td>是（直接 import）</td>\n</tr>\n<tr>\n<td><code>runtime-shim</code> 字符串化</td>\n<td>有（手工同步）</td>\n<td>无</td>\n</tr>\n<tr>\n<td><code>less:ssg-virtual-nav</code> 插件</td>\n<td>有</td>\n<td>无</td>\n</tr>\n<tr>\n<td>CJS 兼容 shim</td>\n<td>有</td>\n<td>无</td>\n</tr>\n<tr>\n<td>构建管线复杂度</td>\n<td>高（3 阶段 + 文件 IPC）</td>\n<td>低（2 阶段 + 内存传递）</td>\n</tr>\n</tbody></table>\n<h2>Consequences</h2>\n<p><strong>正面：</strong></p>\n<ul>\n<li>构建管线从 3 阶段简化到 2 阶段，概念复杂度大幅降低</li>\n<li>消除文件系统 IPC，消除&quot;文件缺失/格式错误/编码问题&quot;类的间歇性 bug</li>\n<li>消除 <code>globalThis</code> 污染，SSR 运行更加干净</li>\n<li>消除 runtime-shim 手工同步问题，SSR 输出与源码一致</li>\n<li>SSR bundle 真正被消费，不再&quot;产出后丢弃&quot;</li>\n<li>为后续增量构建（ISR）奠定基础：bundle 可缓存</li>\n</ul>\n<p><strong>负面：</strong></p>\n<ul>\n<li>一次性重构量大，需谨慎分 Phase 执行</li>\n<li>SSR bundle 的 <code>import()</code> 路径在不同运行时（Deno/Node）下行为可能不同</li>\n<li><code>define</code> 注入的方式需要在 SSR 构建配置中正确传递所有插件定义</li>\n</ul>\n<p><strong>缓解：</strong></p>\n<ul>\n<li>严格按 Phase 执行，每个 Phase 完成后运行全量测试</li>\n<li>SSR bundle 使用绝对路径 <code>import()</code>，兼容 Deno 和 Node</li>\n<li>保留 <code>.less/</code> 目录作为构建缓存（如增量构建 hash），但不作为 IPC</li>\n</ul>\n<h2>参考</h2>\n<ul>\n<li><a href=\"/blog/0005-with-dsd-hydration-mixin\" rel=\"noopener noreferrer\">ADR 0005: WithDsdHydration Mixin</a></li>\n<li><a href=\"/blog/0006-version-strategy\" rel=\"noopener noreferrer\">ADR 0006: 版本号策略</a></li>\n<li><code>packages/core/src/cli/build-ssg.ts</code> — 当前 SSG 构建主文件</li>\n<li><code>packages/core/src/types.ts</code> — adapter 全局桥定义</li>\n<li><code>packages/content/src/blog/blog-data.ts</code> — blog 全局桥定义</li>\n<li><code>packages/adapter-lit/src/ssr.ts</code> — Lit adapter 幂等守卫</li>\n<li><code>packages/i18n/src/i18n-data.ts</code> — 干净模式参考（无 globalThis）</li>\n</ul>\n<hr />\n<p><em>决策日期: 2026-05-10 | 版本: v0.9.0</em></p>\n<h2>Implementation Summary</h2>\n<p>本 ADR 的各 Phase 通过后续 ADR 分步实施完成：</p>\n<table>\n<thead>\n<tr>\n<th>Phase</th>\n<th>描述</th>\n<th>实施 ADR</th>\n<th>状态</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>A</td>\n<td>.less/ 临时文件 IPC → LessBuildContext + 虚拟模块</td>\n<td>ADR 0010</td>\n<td>✅ 完成</td>\n</tr>\n<tr>\n<td>B</td>\n<td>globalThis[Symbol.for()] → 模块变量</td>\n<td>ADR 0011 (部分) + ADR 0012</td>\n<td>✅ 完成</td>\n</tr>\n<tr>\n<td>C</td>\n<td>消除 createServer()，改为 closeBundle 内联</td>\n<td>ADR 0011</td>\n<td>✅ 完成</td>\n</tr>\n<tr>\n<td>D</td>\n<td>runtime-shim → virtual:less-runtime</td>\n<td>ADR 0010 (Step 1)</td>\n<td>✅ 完成</td>\n</tr>\n<tr>\n<td>E</td>\n<td>lessjs() 统一入口 → 拆到 @openelement/app</td>\n<td>ADR 0012</td>\n<td>✅ 完成</td>\n</tr>\n</tbody></table>\n<h3>最终指标</h3>\n<table>\n<thead>\n<tr>\n<th>指标</th>\n<th>ADR 0008 提出时</th>\n<th>当前</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>createServer()</code></td>\n<td>有</td>\n<td><strong>无</strong></td>\n</tr>\n<tr>\n<td><code>.less/</code> 临时文件</td>\n<td>8+</td>\n<td><strong>0</strong></td>\n</tr>\n<tr>\n<td><code>globalThis</code> 桥接键</td>\n<td>4</td>\n<td><strong>0</strong></td>\n</tr>\n<tr>\n<td><code>runtime-shim</code> 字符串化</td>\n<td>有</td>\n<td>无（使用 virtual:less-runtime）</td>\n</tr>\n<tr>\n<td>构建管线</td>\n<td>3 阶段 + 文件 IPC</td>\n<td>1 次 viteBuild + closeBundle 内联</td>\n</tr>\n<tr>\n<td>ctx 传递</td>\n<td>globalThis 隐式共享</td>\n<td>显式参数传递</td>\n</tr>\n<tr>\n<td><code>lessjs()</code> 入口</td>\n<td>在 core 中，动态 import</td>\n<td>在 @openelement/app 中，静态 import</td>\n</tr>\n</tbody></table>\n"
  },
  {
    "slug": "0007-view-transitions-speculation-rules",
    "frontmatter": {
      "title": "ADR 0007: View Transitions + Speculation Rules",
      "date": "2026-05-09",
      "draft": false,
      "tags": [
        "architecture",
        "decision"
      ],
      "type": "adr",
      "lang": "zh"
    },
    "content": "\n## Status\n\n**ADOPTED** — v0.9.2 架构决策，两个 Web Platform API 纳入 SSG 后处理管线。\n\n## Context\n\nLessJS 的 SSG 产物是纯静态 HTML。用户在页面间导航时，浏览器完全卸载旧页面再加载新页面，体验上有两个痛点：\n\n1. **页面切换闪烁**：全量 DOM 替换导致白屏闪烁，SPA 框架（React/Vue）用客户端路由解决了这个问题，但 MPA 天然不具备。\n2. **导航延迟感知**：用户点击链接后，浏览器才开始网络请求、解析 HTML、执行 JS。用户感知到\"等待\"。\n\nWeb Platform 提供了两个原生 API 来解决这些问题：\n\n- **View Transitions API**（Chrome 111+, Safari 18+, Firefox 129+）：为跨页面导航提供原生动画过渡。\n- **Speculation Rules API**（Chrome 121+）：让浏览器在用户点击前就 prefetch/prerender 目标页面。\n\n这两个 API 都是零 JS 运行时成本（View Transitions 仅需一个 `<meta>` 标签，Speculation Rules 仅需一个 `<script type=\"speculationrules\">` JSON），与 LessJS 的\"零框架运行时\"理念完全一致。\n\n## Decision\n\n### 1. View Transitions — 默认开启\n\n在 SSG 后处理管线中注入 `<meta name=\"view-transition\" content=\"same-origin\">`。\n\n- **默认开启**：单 `<meta>` 标签零成本，不支持浏览器静默降级。\n- **可关闭**：`viewTransition: false`。\n- **实现位置**：`ssg-postprocess.ts` 中的 `injectViewTransitionMeta()`。\n\n### 2. Speculation Rules — 默认关闭\n\n在 SSG 后处理管线中注入 `<script type=\"speculationrules\">` JSON。\n\n- **默认关闭**：prefetch/prerender 消耗带宽和服务器资源，需用户显式启用 `speculation: true`。\n- **配置选项**：\n  - `eagerness`: `'conservative'`（默认）| `'moderate'` | `'eager'`\n  - `prerender`: `boolean`（默认 `false`），启用后浏览器会预渲染目标页面\n- **实现位置**：`ssg-postprocess.ts` 中的 `injectSpeculationRules()`。\n\n### 3. SSG 后处理管线重构\n\n从单一函数拆分为 5 步有序管线：\n\n```\n1. injectClientScript()       — Island 水合脚本\n2. injectViewTransitionMeta() — 跨页面动画\n3. injectSpeculationRules()   — 预取/预渲染\n4. injectCspMeta()           — 内容安全策略\n5. injectDsdPolyfill()        — Firefox DSD 兼容\n```\n\n每步独立、可测试、可配置。39 个测试覆盖全部管线步骤。\n\n### 4. Phase 1→3 配置传递\n\n`viewTransition` 和 `speculation` 配置项从 Phase 1 写入 `build-metadata.json`，Phase 3 读取并传递给 SSG 后处理管线。\n\n## Why Not ISR\n\nISR（Incremental Static Regeneration）是 Next.js 在没有 Islands 架构下的补丁方案。LessJS 的 SSG + Islands + Serverless 部署模式已经覆盖了 ISR 解决的核心问题（按需生成、增量更新），不需要额外引入 ISR 概念。\n\n## Consequences\n\n**正面：**\n- 跨页面导航体验接近 SPA，但保持 MPA 的简单性和可缓存性\n- 零框架运行时成本，仅使用浏览器原生 API\n- SSG 后处理管线可测试性大幅提升\n- Speculation Rules 的 opt-in 设计尊重用户带宽\n\n**负面：**\n- View Transitions 的 CSS 动画定制需要用户学习 `::view-transition-*` 伪元素\n- Speculation Rules 仅 Chrome 121+ 支持，Safari/Firefox 静默降级\n- Speculation Rules 可能导致未访问页面的服务器请求增加\n\n**缓解：**\n- 文档提供 View Transitions CSS 定制示例\n- Speculation Rules 默认关闭，用户明确选择后才启用\n- `eagerness: 'conservative'` 默认值仅在用户 hover 时触发 prefetch\n\n## 参考\n\n- [View Transitions API — MDN](https://developer.mozilla.org/en-US/docs/Web/API/View_Transitions_API)\n- [Speculation Rules API — Chrome Developers](https://developer.chrome.com/docs/web-platform/speculation-rules)\n- [ADR 0005: WithDsdHydration Mixin](/blog/0005-with-dsd-hydration-mixin)\n- [ADR 0006: Version Strategy](/blog/0006-version-strategy)\n\n---\n\n_决策日期: 2026-05-09 | 版本: v0.9.2_\n",
    "html": "<h2>Status</h2>\n<p><strong>ADOPTED</strong> — v0.9.2 架构决策，两个 Web Platform API 纳入 SSG 后处理管线。</p>\n<h2>Context</h2>\n<p>LessJS 的 SSG 产物是纯静态 HTML。用户在页面间导航时，浏览器完全卸载旧页面再加载新页面，体验上有两个痛点：</p>\n<ol>\n<li><strong>页面切换闪烁</strong>：全量 DOM 替换导致白屏闪烁，SPA 框架（React/Vue）用客户端路由解决了这个问题，但 MPA 天然不具备。</li>\n<li><strong>导航延迟感知</strong>：用户点击链接后，浏览器才开始网络请求、解析 HTML、执行 JS。用户感知到&quot;等待&quot;。</li>\n</ol>\n<p>Web Platform 提供了两个原生 API 来解决这些问题：</p>\n<ul>\n<li><strong>View Transitions API</strong>（Chrome 111+, Safari 18+, Firefox 129+）：为跨页面导航提供原生动画过渡。</li>\n<li><strong>Speculation Rules API</strong>（Chrome 121+）：让浏览器在用户点击前就 prefetch/prerender 目标页面。</li>\n</ul>\n<p>这两个 API 都是零 JS 运行时成本（View Transitions 仅需一个 <code>&lt;meta&gt;</code> 标签，Speculation Rules 仅需一个 <code>&lt;script type=&quot;speculationrules&quot;&gt;</code> JSON），与 LessJS 的&quot;零框架运行时&quot;理念完全一致。</p>\n<h2>Decision</h2>\n<h3>1. View Transitions — 默认开启</h3>\n<p>在 SSG 后处理管线中注入 <code>&lt;meta name=&quot;view-transition&quot; content=&quot;same-origin&quot;&gt;</code>。</p>\n<ul>\n<li><strong>默认开启</strong>：单 <code>&lt;meta&gt;</code> 标签零成本，不支持浏览器静默降级。</li>\n<li><strong>可关闭</strong>：<code>viewTransition: false</code>。</li>\n<li><strong>实现位置</strong>：<code>ssg-postprocess.ts</code> 中的 <code>injectViewTransitionMeta()</code>。</li>\n</ul>\n<h3>2. Speculation Rules — 默认关闭</h3>\n<p>在 SSG 后处理管线中注入 <code>&lt;script type=&quot;speculationrules&quot;&gt;</code> JSON。</p>\n<ul>\n<li><strong>默认关闭</strong>：prefetch/prerender 消耗带宽和服务器资源，需用户显式启用 <code>speculation: true</code>。</li>\n<li><strong>配置选项</strong>：<ul>\n<li><code>eagerness</code>: <code>&#39;conservative&#39;</code>（默认）| <code>&#39;moderate&#39;</code> | <code>&#39;eager&#39;</code></li>\n<li><code>prerender</code>: <code>boolean</code>（默认 <code>false</code>），启用后浏览器会预渲染目标页面</li>\n</ul>\n</li>\n<li><strong>实现位置</strong>：<code>ssg-postprocess.ts</code> 中的 <code>injectSpeculationRules()</code>。</li>\n</ul>\n<h3>3. SSG 后处理管线重构</h3>\n<p>从单一函数拆分为 5 步有序管线：</p>\n<pre><code>1. injectClientScript()       — Island 水合脚本\n2. injectViewTransitionMeta() — 跨页面动画\n3. injectSpeculationRules()   — 预取/预渲染\n4. injectCspMeta()           — 内容安全策略\n5. injectDsdPolyfill()        — Firefox DSD 兼容\n</code></pre>\n<p>每步独立、可测试、可配置。39 个测试覆盖全部管线步骤。</p>\n<h3>4. Phase 1→3 配置传递</h3>\n<p><code>viewTransition</code> 和 <code>speculation</code> 配置项从 Phase 1 写入 <code>build-metadata.json</code>，Phase 3 读取并传递给 SSG 后处理管线。</p>\n<h2>Why Not ISR</h2>\n<p>ISR（Incremental Static Regeneration）是 Next.js 在没有 Islands 架构下的补丁方案。LessJS 的 SSG + Islands + Serverless 部署模式已经覆盖了 ISR 解决的核心问题（按需生成、增量更新），不需要额外引入 ISR 概念。</p>\n<h2>Consequences</h2>\n<p><strong>正面：</strong></p>\n<ul>\n<li>跨页面导航体验接近 SPA，但保持 MPA 的简单性和可缓存性</li>\n<li>零框架运行时成本，仅使用浏览器原生 API</li>\n<li>SSG 后处理管线可测试性大幅提升</li>\n<li>Speculation Rules 的 opt-in 设计尊重用户带宽</li>\n</ul>\n<p><strong>负面：</strong></p>\n<ul>\n<li>View Transitions 的 CSS 动画定制需要用户学习 <code>::view-transition-*</code> 伪元素</li>\n<li>Speculation Rules 仅 Chrome 121+ 支持，Safari/Firefox 静默降级</li>\n<li>Speculation Rules 可能导致未访问页面的服务器请求增加</li>\n</ul>\n<p><strong>缓解：</strong></p>\n<ul>\n<li>文档提供 View Transitions CSS 定制示例</li>\n<li>Speculation Rules 默认关闭，用户明确选择后才启用</li>\n<li><code>eagerness: &#39;conservative&#39;</code> 默认值仅在用户 hover 时触发 prefetch</li>\n</ul>\n<h2>参考</h2>\n<ul>\n<li><a href=\"https://developer.mozilla.org/en-US/docs/Web/API/View_Transitions_API\" rel=\"noopener noreferrer\">View Transitions API — MDN</a></li>\n<li><a href=\"https://developer.chrome.com/docs/web-platform/speculation-rules\" rel=\"noopener noreferrer\">Speculation Rules API — Chrome Developers</a></li>\n<li><a href=\"/blog/0005-with-dsd-hydration-mixin\" rel=\"noopener noreferrer\">ADR 0005: WithDsdHydration Mixin</a></li>\n<li><a href=\"/blog/0006-version-strategy\" rel=\"noopener noreferrer\">ADR 0006: Version Strategy</a></li>\n</ul>\n<hr />\n<p><em>决策日期: 2026-05-09 | 版本: v0.9.2</em></p>\n"
  },
  {
    "slug": "0006-version-strategy",
    "frontmatter": {
      "title": "ADR 0006: 版本号策略 — 从 0.6 到 2.0 的递进路线",
      "date": "2026-05-07",
      "draft": false,
      "tags": [
        "architecture",
        "decision"
      ],
      "type": "adr",
      "lang": "zh"
    },
    "content": "\n## Status\n\n**ADOPTED** — v0.6.2 架构决策，替代旧 roadmap 中的 v0.7~v0.10 线性递增方案。\n\n## Context\n\nLessJS 当前的 roadmap 采用 0.7 → 0.8 → 0.9 → 0.10 的线性递增。这个方案有几个问题：\n\n1. **SemVer 语义不精确**：0.7（Island Manifest）到 0.8（Fullstack）是架构能力的质变，不是量变。Patch 级别的递增不能传达这种差异。\n2. **1.0 的门槛模糊**：什么时候可以打 1.0？是 P2 审计修复完成后？还是 Compiler 完成后？没有明确的判定标准。\n3. **Compiler 和 Fullstack 的归属不清**：这两个能力会改变\"LessJS 是什么\"的定义。如果放在 1.0 之后，就是 Breaking Change 需要升 2.0；如果放在 1.0 之前，它们还是 0.x 的正常演化。\n4. **审计发现尚未纳入**：2026-05-07 四维审计的 P0/P1/P2 修复任务没有对应的版本号。\n\n## Decision\n\n采用**三段式版本路线**，以\"能力域\"划分 MAJOR/MINOR，而非线性递增：\n\n### 版本总览\n\n```\nv0.7.0  ← P0 稳定基线（审计修复）\nv0.8.0  ← P1 功能完善 + Island Manifest\nv0.9.0  ← i18n + View Transitions + Speculation Rules  ← 实际交付偏离原计划\nv0.10.0 ← SSR Architecture Purification + API Boundary  ← 实际交付偏离原计划\nv0.11.0 ← ISR + PWA + Compiler Alpha  ← 原 v0.10 ISR/PWA 内容后移至此\nv1.0.0  ← API 稳定承诺\nv1.x    ← 增量演进（不破坏公共 API）\nv2.0.0  ← Compiler 成为默认（如需 Breaking Change）\n```\n\n### 判定 1.0.0 的标准\n\n1.0.0 不是一个\"完成了所有功能\"的版本号，而是一个**公共 API 稳定承诺**的信号。以下条件全部满足时，可以打 1.0.0：\n\n| # | 条件                                                          | 理由                                                     |\n| - | ------------------------------------------------------------- | -------------------------------------------------------- |\n| 1 | 核心模块测试覆盖率 ≥ 80%                                      | 覆盖率是 API 稳定的前提——没有测试的 API 无法保证行为不变 |\n| 2 | CI 全链路门禁就位（test + typecheck + lint + coverage gate）  | 自动化防止退化                                           |\n| 3 | runtime-shim 由 AST 生成，不再手工维护                        | 最大技术债消除，SSR 输出一致性可验证                     |\n| 4 | 公共 API 列表明确文档化（exports、config options、CLI flags） | 用户知道哪些是承诺的，哪些是内部的                       |\n| 5 | 至少 3 个真实项目 dogfooding（含 docs 站）                    | 实际使用验证                                             |\n| 6 | 无 P0/P1 级已知 Bug                                           | 质量基线                                                 |\n\n**1.0.0 不要求**：Compiler 生产可用、ISR 生产可用、Fullstack 示例丰富。这些是 1.x 的增量工作。\n\n### 各版本详细规划\n\n#### v0.7.0 — 稳定基线（P0 审计修复）\n\n**定位**：消除不可信行为，建立工程纪律。\n\n| 任务                              | 优先级 | 说明                                     |\n| --------------------------------- | ------ | ---------------------------------------- |\n| render-dsd.ts 单元测试            | P0     | 770 行核心渲染器零覆盖                   |\n| island.ts 单元测试                | P0     | 321 行 Island 系统零覆盖                 |\n| runtime-shim 一致性测试 + 修复    | P0     | serializeAttributes 缺失 escapeAttrValue |\n| headExtras/headFragments XSS 修复 | P0     | 重命名 + 运行时警告                      |\n| 消除静默 catch → console.warn     | P0     | 8+ 处错误吞没                            |\n| CI 补全                           | P0     | adapter-lit/docs 测试任务、发布门禁      |\n| pre-commit hooks                  | P0     | 格式化/lint/类型检查守卫                 |\n\n**为什么是 0.7.0 而不是 0.6.3**：XSS 修复和 catch 行为变更属于破坏性变更（部分用户可能依赖当前的静默行为）。SemVer 0.x 的 MINOR 升级允许破坏性变更，正好对应。\n\n#### v0.8.0 — 功能完善 + Island Manifest\n\n**定位**：补全测试覆盖 + Island 系统演进。\n\n| 任务                           | 优先级  | 说明                                  |\n| ------------------------------ | ------- | ------------------------------------- |\n| signals 测试套件               | P1      | 749 行零覆盖                          |\n| dsd-hydration.ts 单元测试      | P1      | Mixin 核心逻辑验证                    |\n| Signal 原生切换                | P1      | npm 依赖 + globalThis.Signal 条件回退 |\n| render-dsd.ts 拆分为 4 模块    | P1      | 可维护性                              |\n| UI 组件统一到 DsdLitElement    | P1      | 3 个组件未使用 Mixin                  |\n| insertAfterHead 去重           | P1      | ui → core                             |\n| 包版本统一                     | P1      | 6 包版本不一致                        |\n| 定位重写（避免\"全栈\"过度承诺） | P1      | 诚实营销                              |\n| Interactive Playground         | P1      | StackBlitz 一键体验                   |\n| Playwright E2E 测试            | P1      | 浏览器级集成测试                      |\n| Island Upgrade Manifest        | Roadmap | 页面级 island 清单替代全局入口        |\n| Speculative Loading 可观测     | Roadmap | eager/visible/idle 策略浏览器测试     |\n\n**合并旧 roadmap 的 v0.7 内容**：Island Manifest 和 Speculative Loading 与 P1 测试/组件统一工作属于同一能力域，合并到 0.8.0 合理。\n\n#### v0.9.0 — i18n + View Transitions + Speculation Rules\n\n**定位**：国际化 + 浏览器原生性能 API 集成。\n\n> ⚠️ **实际交付偏离原计划**：原计划为 Serverless Fullstack（FormData actions, Typed RPC, env/secrets），实际因 SSG 稳定性需求和浏览器 API 成熟度而调整。i18n 和 View Transitions 是用户更紧迫的需求。Serverless Fullstack 推迟到 v1.x。\n\n| 能力                  | 说明                                                                       |\n| --------------------- | -------------------------------------------------------------------------- |\n| @openelement/i18n 独立包   | 从 content 拆出，lessI18n() 独立插件，SSG locale 展开                      |\n| 双语文档站            | 25/30 页面英文版 + language switcher + en/zh 路径                          |\n| View Transitions API  | 跨页面 MPA 动画（Chrome 111+, Safari 18+, Firefox 129+）                   |\n| Speculation Rules API | 浏览器 prefetch/prerender（Chrome 121+）                                   |\n| SSG 后处理管线重构    | 5 步管线：clientScript → viewTransition → speculation → CSP → DSD polyfill |\n\n#### v0.10.0 — SSR Architecture Purification + API Boundary\n\n**定位**：消除 SSR 层全部边界违规，建立干净的公共 API。\n\n> ⚠️ **实际交付偏离原计划**：原计划为 SSG + ISR + PWA（路由级 revalidation, Cache lock, Stale fallback），实际因 ADR 0008-0014 架构净化需求优先而调整。ISR/PWA 内容后移至 v0.11。\n\n| 能力                                 | 说明                                         |\n| ------------------------------------ | -------------------------------------------- |\n| 消除 globalThis 桥接（ADR 0008）     | 消除 createServer() + 全部 globalThis 桥接   |\n| 消除 .less/ 临时文件（ADR 0010）     | 构建 ctx 替代文件系统中间态                  |\n| 消除 last globalThis（ADR 0011）     | closeBundle 替代 globalThis 数据传递         |\n| 提取 @openelement/app（ADR 0012）         | umbrella 函数抽到独立包                      |\n| 消除 less-runtime barrel（ADR 0013） | 内联 shim 替代 barrel 重导出                 |\n| SSR Bundle 公共 API（ADR 0014）      | renderRoute() + getStaticPaths() + routeInfo |\n\n#### v0.11.0 — ISR + PWA + Compiler Alpha\n\n**定位**：构建能力成熟 + 编译器实验性能力。\n\n| 任务                              | 来源     | 说明                                  |\n| --------------------------------- | -------- | ------------------------------------- |\n| 路由级 revalidation               | 原 v0.10 | 按需重新生成静态页面                  |\n| Cache lock                        | 原 v0.10 | 并发构建时的缓存锁                    |\n| Stale fallback                    | 原 v0.10 | 新内容构建中返回旧内容                |\n| Service Worker 策略               | 原 v0.10 | NetworkFirst/CacheFirst 可配置        |\n| CDN recipes                       | 原 v0.10 | Cloudflare/Netlify 缓存配置模板       |\n| AST 代码生成替代手工 runtime-shim | P2 #19   | 根治 SSR 输出一致性                   |\n| 增量 SSG 构建                     | P2 #20   | 文件哈希缓存                          |\n| 性能基准                          | P2 #24   | 构建时间/产物体积/Lighthouse/竞品对比 |\n| 覆盖率收集 + 门禁                 | P2 #25   | CI 覆盖率强制                         |\n| 视觉回归测试                      | P2 #26   | 组件截图对比                          |\n| 依赖安全审计自动化                | P2 #27   | CI 集成 npm audit / Snyk              |\n| .less Compiler Alpha              | ADR 0002 | 可选零框架组件编译                    |\n\n**为什么 Compiler 在 P2 之后**：Compiler 需要稳定的 DSD renderer 作为编译目标。如果 renderer 还在手工同步（runtime-shim 问题），Compiler 的输出也无法保证正确。P2 的 AST 替换解决了这个问题。\n\n#### v1.0.0 — API 稳定承诺\n\n**判定标准**：见上文 6 条标准。\n\n**1.0.0 的含义**：\n\n- 公共 API（`less()` 配置、CLI 命令、`@openelement/core` 导出、`@openelement/rpc` 导出）视为稳定\n- Breaking Change 必须升 MAJOR\n- Lit adapter 继续作为一等公民支持\n- Compiler 仍为 opt-in，不是默认\n\n#### v1.x — 增量演进\n\n| 版本 | 方向                   | 说明                                |\n| ---- | ---------------------- | ----------------------------------- |\n| v1.1 | Compiler Beta → Stable | `.less` 文件成为推荐组件编写方式    |\n| v1.2 | Fullstack 示例丰富     | CRM/admin 级 demo                   |\n| v1.x | 更多 adapter           | Vue adapter、React island bridge 等 |\n\n> **注**：`@openelement/blog` 开发提前到 v0.8.0 后启动（SSG 插件形态，不依赖 Fullstack），在 v0.9 期间 dogfooding。v1.0 时 blog 包随主框架一起稳定。\n\n#### v2.0.0 — Compiler 成为默认（如果需要）\n\n只有当以下条件同时满足时才考虑 2.0：\n\n1. `.less` Compiler 生产可用且经过大量项目验证\n2. 社区已自然迁移到 `.less` 组件\n3. 将 Compiler 设为默认会破坏现有 Lit 用户的工作流\n\n**如果 Lit 兼容模式可以无缝共存，则不需要 2.0。**\n\n### Monorepo 包版本策略\n\nLessJS 是 Deno workspace monorepo，7 个包发布到 JSR。仓库 Release tag 和包版本号**不需要一致**。\n\n**规则：谁改了谁升级，仓库 tag 取本次发布中最大的包版本号。**\n\n```\n仓库 tag: v0.9.0\n\n包版本变更：\n  @openelement/core         0.8.1 → 0.9.0   (SSR 属性绑定行为变更)\n  @openelement/adapter-lit  0.6.4 → 0.7.0   (interpolate 属性绑定输出变更)\n  @openelement/content      0.1.0 → 0.2.0   (新增 nav/sitemap 模块)\n  @openelement/ui           0.6.2 → 不变    (无变更)\n  @openelement/rpc          0.6.1 → 不变    (无变更)\n  @openelement/signal        0.6.2 → 不变   (无变更)\n  @openelement/create       0.6.1 → 不变    (无变更)\n```\n\n**具体规则**：\n\n1. **仓库 Release tag ≠ 任何单个包的版本**。tag 是这批变更的整体发布代号。\n2. **包独立发版**：JSR 按包发版，不要求所有包版本一致。\n3. **版本号判断标准**：\n   - 有 Breaking Change 或行为变更 → MINOR bump（0.x 阶段）\n   - 纯 Bug fix / 新增非 Breaking 功能 → PATCH bump\n   - 没改 → 不动\n4. **Release Note 按包分组**，列出每个包的变更版本，末尾列出未变更的包。\n5. **跨包依赖版本引用**：必须引用已发布到 JSR 的版本（`jsr:@scope/package@^version/subpath`），不能引用本地路径。\n\n**为什么不用固定版本（Fixed/Locked）**：\n\n- Babel/Angular 的做法要求所有包版本号相同，但在 JSR 生态中不必要——JSR 天然按包发版。\n- 新增包（如 content 0.1.0）如果被强制跳到 0.9.0，版本号含金量稀释。\n- 没改的包浪费版本号，changelog 虚胖。\n\n**为什么不用完全独立版本（不加仓库 tag）**：\n\n- 用户需要知道\"这一批变更的整体节奏\"——仓库 tag 提供叙事感。\n- GitHub Release 是用户发现更新的入口，不能省略。\n\n### 版本号语义总结\n\n| 版本段   | 含义                                | Breaking Change 规则     |\n| -------- | ----------------------------------- | ------------------------ |\n| 0.7–0.10 | 框架在定义自己                      | MINOR 升级允许破坏性变更 |\n| 0.11     | 过渡版本：基础设施成熟 + 新能力引入 | 仍允许，但应尽量减少     |\n| 1.0      | 公共 API 冻结                       | 绝不允许（必须升 MAJOR） |\n| 1.x      | 增量扩展                            | 公共 API 不破坏          |\n| 2.0      | 范式转换（如果需要）                | 允许，但需迁移指南       |\n\n## Consequences\n\n**正面：**\n\n- 每个版本号有清晰的语义——用户看到版本号就能判断升级风险\n- 1.0.0 有明确的判定标准，不再是\"感觉差不多了\"的主观判断\n- Compiler 和 Fullstack 在 0.x 阶段完成，不用担心 1.0 后的 Breaking Change\n- 审计发现有了正式的版本归属\n\n**负面：**\n\n- 0.x 阶段拉长（0.7 → 0.11 共 5 个 MINOR 版本），可能给人\"不够稳定\"的印象\n- 需要在 1.0 前维护公共 API 列表，增加了文档工作量\n- 如果 0.11 的 Compiler Alpha 发现需要调整 DSD renderer 输出格式，可能影响 1.0 的时间线\n\n**缓解：**\n\n- 每个 0.x 版本都应该有明确的里程碑文档，传递\"渐进成熟\"而非\"还不稳定\"的信号\n- 公共 API 列表在 v0.8 就开始文档化（标记为 \"Stable API Candidate\"），让 1.0 的门槛更具体\n- 如果 0.11 Compiler Alpha 进展顺利，可以合并到 1.0 直接发布\n\n## 参考\n\n- [ADR 0002: .less Compiler](/blog/0002-less-compiler-eliminate-lit)\n- [ADR 0005: WithDsdHydration Mixin](/blog/0005-with-dsd-hydration-mixin)\n- [SemVer 2.0.0](https://semver.org/)\n- LessJS 四维审计报告 (2026-05-07)\n\n---\n\n_决策日期: 2026-05-07 | 版本: v0.6.2_\n",
    "html": "<h2>Status</h2>\n<p><strong>ADOPTED</strong> — v0.6.2 架构决策，替代旧 roadmap 中的 v0.7~v0.10 线性递增方案。</p>\n<h2>Context</h2>\n<p>LessJS 当前的 roadmap 采用 0.7 → 0.8 → 0.9 → 0.10 的线性递增。这个方案有几个问题：</p>\n<ol>\n<li><strong>SemVer 语义不精确</strong>：0.7（Island Manifest）到 0.8（Fullstack）是架构能力的质变，不是量变。Patch 级别的递增不能传达这种差异。</li>\n<li><strong>1.0 的门槛模糊</strong>：什么时候可以打 1.0？是 P2 审计修复完成后？还是 Compiler 完成后？没有明确的判定标准。</li>\n<li><strong>Compiler 和 Fullstack 的归属不清</strong>：这两个能力会改变&quot;LessJS 是什么&quot;的定义。如果放在 1.0 之后，就是 Breaking Change 需要升 2.0；如果放在 1.0 之前，它们还是 0.x 的正常演化。</li>\n<li><strong>审计发现尚未纳入</strong>：2026-05-07 四维审计的 P0/P1/P2 修复任务没有对应的版本号。</li>\n</ol>\n<h2>Decision</h2>\n<p>采用<strong>三段式版本路线</strong>，以&quot;能力域&quot;划分 MAJOR/MINOR，而非线性递增：</p>\n<h3>版本总览</h3>\n<pre><code>v0.7.0  ← P0 稳定基线（审计修复）\nv0.8.0  ← P1 功能完善 + Island Manifest\nv0.9.0  ← i18n + View Transitions + Speculation Rules  ← 实际交付偏离原计划\nv0.10.0 ← SSR Architecture Purification + API Boundary  ← 实际交付偏离原计划\nv0.11.0 ← ISR + PWA + Compiler Alpha  ← 原 v0.10 ISR/PWA 内容后移至此\nv1.0.0  ← API 稳定承诺\nv1.x    ← 增量演进（不破坏公共 API）\nv2.0.0  ← Compiler 成为默认（如需 Breaking Change）\n</code></pre>\n<h3>判定 1.0.0 的标准</h3>\n<p>1.0.0 不是一个&quot;完成了所有功能&quot;的版本号，而是一个<strong>公共 API 稳定承诺</strong>的信号。以下条件全部满足时，可以打 1.0.0：</p>\n<table>\n<thead>\n<tr>\n<th>#</th>\n<th>条件</th>\n<th>理由</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>1</td>\n<td>核心模块测试覆盖率 ≥ 80%</td>\n<td>覆盖率是 API 稳定的前提——没有测试的 API 无法保证行为不变</td>\n</tr>\n<tr>\n<td>2</td>\n<td>CI 全链路门禁就位（test + typecheck + lint + coverage gate）</td>\n<td>自动化防止退化</td>\n</tr>\n<tr>\n<td>3</td>\n<td>runtime-shim 由 AST 生成，不再手工维护</td>\n<td>最大技术债消除，SSR 输出一致性可验证</td>\n</tr>\n<tr>\n<td>4</td>\n<td>公共 API 列表明确文档化（exports、config options、CLI flags）</td>\n<td>用户知道哪些是承诺的，哪些是内部的</td>\n</tr>\n<tr>\n<td>5</td>\n<td>至少 3 个真实项目 dogfooding（含 docs 站）</td>\n<td>实际使用验证</td>\n</tr>\n<tr>\n<td>6</td>\n<td>无 P0/P1 级已知 Bug</td>\n<td>质量基线</td>\n</tr>\n</tbody></table>\n<p><strong>1.0.0 不要求</strong>：Compiler 生产可用、ISR 生产可用、Fullstack 示例丰富。这些是 1.x 的增量工作。</p>\n<h3>各版本详细规划</h3>\n<h4>v0.7.0 — 稳定基线（P0 审计修复）</h4>\n<p><strong>定位</strong>：消除不可信行为，建立工程纪律。</p>\n<table>\n<thead>\n<tr>\n<th>任务</th>\n<th>优先级</th>\n<th>说明</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>render-dsd.ts 单元测试</td>\n<td>P0</td>\n<td>770 行核心渲染器零覆盖</td>\n</tr>\n<tr>\n<td>island.ts 单元测试</td>\n<td>P0</td>\n<td>321 行 Island 系统零覆盖</td>\n</tr>\n<tr>\n<td>runtime-shim 一致性测试 + 修复</td>\n<td>P0</td>\n<td>serializeAttributes 缺失 escapeAttrValue</td>\n</tr>\n<tr>\n<td>headExtras/headFragments XSS 修复</td>\n<td>P0</td>\n<td>重命名 + 运行时警告</td>\n</tr>\n<tr>\n<td>消除静默 catch → console.warn</td>\n<td>P0</td>\n<td>8+ 处错误吞没</td>\n</tr>\n<tr>\n<td>CI 补全</td>\n<td>P0</td>\n<td>adapter-lit/docs 测试任务、发布门禁</td>\n</tr>\n<tr>\n<td>pre-commit hooks</td>\n<td>P0</td>\n<td>格式化/lint/类型检查守卫</td>\n</tr>\n</tbody></table>\n<p><strong>为什么是 0.7.0 而不是 0.6.3</strong>：XSS 修复和 catch 行为变更属于破坏性变更（部分用户可能依赖当前的静默行为）。SemVer 0.x 的 MINOR 升级允许破坏性变更，正好对应。</p>\n<h4>v0.8.0 — 功能完善 + Island Manifest</h4>\n<p><strong>定位</strong>：补全测试覆盖 + Island 系统演进。</p>\n<table>\n<thead>\n<tr>\n<th>任务</th>\n<th>优先级</th>\n<th>说明</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>signals 测试套件</td>\n<td>P1</td>\n<td>749 行零覆盖</td>\n</tr>\n<tr>\n<td>dsd-hydration.ts 单元测试</td>\n<td>P1</td>\n<td>Mixin 核心逻辑验证</td>\n</tr>\n<tr>\n<td>Signal 原生切换</td>\n<td>P1</td>\n<td>npm 依赖 + globalThis.Signal 条件回退</td>\n</tr>\n<tr>\n<td>render-dsd.ts 拆分为 4 模块</td>\n<td>P1</td>\n<td>可维护性</td>\n</tr>\n<tr>\n<td>UI 组件统一到 DsdLitElement</td>\n<td>P1</td>\n<td>3 个组件未使用 Mixin</td>\n</tr>\n<tr>\n<td>insertAfterHead 去重</td>\n<td>P1</td>\n<td>ui → core</td>\n</tr>\n<tr>\n<td>包版本统一</td>\n<td>P1</td>\n<td>6 包版本不一致</td>\n</tr>\n<tr>\n<td>定位重写（避免&quot;全栈&quot;过度承诺）</td>\n<td>P1</td>\n<td>诚实营销</td>\n</tr>\n<tr>\n<td>Interactive Playground</td>\n<td>P1</td>\n<td>StackBlitz 一键体验</td>\n</tr>\n<tr>\n<td>Playwright E2E 测试</td>\n<td>P1</td>\n<td>浏览器级集成测试</td>\n</tr>\n<tr>\n<td>Island Upgrade Manifest</td>\n<td>Roadmap</td>\n<td>页面级 island 清单替代全局入口</td>\n</tr>\n<tr>\n<td>Speculative Loading 可观测</td>\n<td>Roadmap</td>\n<td>eager/visible/idle 策略浏览器测试</td>\n</tr>\n</tbody></table>\n<p><strong>合并旧 roadmap 的 v0.7 内容</strong>：Island Manifest 和 Speculative Loading 与 P1 测试/组件统一工作属于同一能力域，合并到 0.8.0 合理。</p>\n<h4>v0.9.0 — i18n + View Transitions + Speculation Rules</h4>\n<p><strong>定位</strong>：国际化 + 浏览器原生性能 API 集成。</p>\n<blockquote>\n<p>⚠️ <strong>实际交付偏离原计划</strong>：原计划为 Serverless Fullstack（FormData actions, Typed RPC, env/secrets），实际因 SSG 稳定性需求和浏览器 API 成熟度而调整。i18n 和 View Transitions 是用户更紧迫的需求。Serverless Fullstack 推迟到 v1.x。</p>\n</blockquote>\n<table>\n<thead>\n<tr>\n<th>能力</th>\n<th>说明</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>@openelement/i18n 独立包</td>\n<td>从 content 拆出，lessI18n() 独立插件，SSG locale 展开</td>\n</tr>\n<tr>\n<td>双语文档站</td>\n<td>25/30 页面英文版 + language switcher + en/zh 路径</td>\n</tr>\n<tr>\n<td>View Transitions API</td>\n<td>跨页面 MPA 动画（Chrome 111+, Safari 18+, Firefox 129+）</td>\n</tr>\n<tr>\n<td>Speculation Rules API</td>\n<td>浏览器 prefetch/prerender（Chrome 121+）</td>\n</tr>\n<tr>\n<td>SSG 后处理管线重构</td>\n<td>5 步管线：clientScript → viewTransition → speculation → CSP → DSD polyfill</td>\n</tr>\n</tbody></table>\n<h4>v0.10.0 — SSR Architecture Purification + API Boundary</h4>\n<p><strong>定位</strong>：消除 SSR 层全部边界违规，建立干净的公共 API。</p>\n<blockquote>\n<p>⚠️ <strong>实际交付偏离原计划</strong>：原计划为 SSG + ISR + PWA（路由级 revalidation, Cache lock, Stale fallback），实际因 ADR 0008-0014 架构净化需求优先而调整。ISR/PWA 内容后移至 v0.11。</p>\n</blockquote>\n<table>\n<thead>\n<tr>\n<th>能力</th>\n<th>说明</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>消除 globalThis 桥接（ADR 0008）</td>\n<td>消除 createServer() + 全部 globalThis 桥接</td>\n</tr>\n<tr>\n<td>消除 .less/ 临时文件（ADR 0010）</td>\n<td>构建 ctx 替代文件系统中间态</td>\n</tr>\n<tr>\n<td>消除 last globalThis（ADR 0011）</td>\n<td>closeBundle 替代 globalThis 数据传递</td>\n</tr>\n<tr>\n<td>提取 @openelement/app（ADR 0012）</td>\n<td>umbrella 函数抽到独立包</td>\n</tr>\n<tr>\n<td>消除 less-runtime barrel（ADR 0013）</td>\n<td>内联 shim 替代 barrel 重导出</td>\n</tr>\n<tr>\n<td>SSR Bundle 公共 API（ADR 0014）</td>\n<td>renderRoute() + getStaticPaths() + routeInfo</td>\n</tr>\n</tbody></table>\n<h4>v0.11.0 — ISR + PWA + Compiler Alpha</h4>\n<p><strong>定位</strong>：构建能力成熟 + 编译器实验性能力。</p>\n<table>\n<thead>\n<tr>\n<th>任务</th>\n<th>来源</th>\n<th>说明</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>路由级 revalidation</td>\n<td>原 v0.10</td>\n<td>按需重新生成静态页面</td>\n</tr>\n<tr>\n<td>Cache lock</td>\n<td>原 v0.10</td>\n<td>并发构建时的缓存锁</td>\n</tr>\n<tr>\n<td>Stale fallback</td>\n<td>原 v0.10</td>\n<td>新内容构建中返回旧内容</td>\n</tr>\n<tr>\n<td>Service Worker 策略</td>\n<td>原 v0.10</td>\n<td>NetworkFirst/CacheFirst 可配置</td>\n</tr>\n<tr>\n<td>CDN recipes</td>\n<td>原 v0.10</td>\n<td>Cloudflare/Netlify 缓存配置模板</td>\n</tr>\n<tr>\n<td>AST 代码生成替代手工 runtime-shim</td>\n<td>P2 #19</td>\n<td>根治 SSR 输出一致性</td>\n</tr>\n<tr>\n<td>增量 SSG 构建</td>\n<td>P2 #20</td>\n<td>文件哈希缓存</td>\n</tr>\n<tr>\n<td>性能基准</td>\n<td>P2 #24</td>\n<td>构建时间/产物体积/Lighthouse/竞品对比</td>\n</tr>\n<tr>\n<td>覆盖率收集 + 门禁</td>\n<td>P2 #25</td>\n<td>CI 覆盖率强制</td>\n</tr>\n<tr>\n<td>视觉回归测试</td>\n<td>P2 #26</td>\n<td>组件截图对比</td>\n</tr>\n<tr>\n<td>依赖安全审计自动化</td>\n<td>P2 #27</td>\n<td>CI 集成 npm audit / Snyk</td>\n</tr>\n<tr>\n<td>.less Compiler Alpha</td>\n<td>ADR 0002</td>\n<td>可选零框架组件编译</td>\n</tr>\n</tbody></table>\n<p><strong>为什么 Compiler 在 P2 之后</strong>：Compiler 需要稳定的 DSD renderer 作为编译目标。如果 renderer 还在手工同步（runtime-shim 问题），Compiler 的输出也无法保证正确。P2 的 AST 替换解决了这个问题。</p>\n<h4>v1.0.0 — API 稳定承诺</h4>\n<p><strong>判定标准</strong>：见上文 6 条标准。</p>\n<p><strong>1.0.0 的含义</strong>：</p>\n<ul>\n<li>公共 API（<code>less()</code> 配置、CLI 命令、<code>@openelement/core</code> 导出、<code>@openelement/rpc</code> 导出）视为稳定</li>\n<li>Breaking Change 必须升 MAJOR</li>\n<li>Lit adapter 继续作为一等公民支持</li>\n<li>Compiler 仍为 opt-in，不是默认</li>\n</ul>\n<h4>v1.x — 增量演进</h4>\n<table>\n<thead>\n<tr>\n<th>版本</th>\n<th>方向</th>\n<th>说明</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>v1.1</td>\n<td>Compiler Beta → Stable</td>\n<td><code>.less</code> 文件成为推荐组件编写方式</td>\n</tr>\n<tr>\n<td>v1.2</td>\n<td>Fullstack 示例丰富</td>\n<td>CRM/admin 级 demo</td>\n</tr>\n<tr>\n<td>v1.x</td>\n<td>更多 adapter</td>\n<td>Vue adapter、React island bridge 等</td>\n</tr>\n</tbody></table>\n<blockquote>\n<p><strong>注</strong>：<code>@openelement/blog</code> 开发提前到 v0.8.0 后启动（SSG 插件形态，不依赖 Fullstack），在 v0.9 期间 dogfooding。v1.0 时 blog 包随主框架一起稳定。</p>\n</blockquote>\n<h4>v2.0.0 — Compiler 成为默认（如果需要）</h4>\n<p>只有当以下条件同时满足时才考虑 2.0：</p>\n<ol>\n<li><code>.less</code> Compiler 生产可用且经过大量项目验证</li>\n<li>社区已自然迁移到 <code>.less</code> 组件</li>\n<li>将 Compiler 设为默认会破坏现有 Lit 用户的工作流</li>\n</ol>\n<p><strong>如果 Lit 兼容模式可以无缝共存，则不需要 2.0。</strong></p>\n<h3>Monorepo 包版本策略</h3>\n<p>LessJS 是 Deno workspace monorepo，7 个包发布到 JSR。仓库 Release tag 和包版本号<strong>不需要一致</strong>。</p>\n<p><strong>规则：谁改了谁升级，仓库 tag 取本次发布中最大的包版本号。</strong></p>\n<pre><code>仓库 tag: v0.9.0\n\n包版本变更：\n  @openelement/core         0.8.1 → 0.9.0   (SSR 属性绑定行为变更)\n  @openelement/adapter-lit  0.6.4 → 0.7.0   (interpolate 属性绑定输出变更)\n  @openelement/content      0.1.0 → 0.2.0   (新增 nav/sitemap 模块)\n  @openelement/ui           0.6.2 → 不变    (无变更)\n  @openelement/rpc          0.6.1 → 不变    (无变更)\n  @openelement/signal        0.6.2 → 不变   (无变更)\n  @openelement/create       0.6.1 → 不变    (无变更)\n</code></pre>\n<p><strong>具体规则</strong>：</p>\n<ol>\n<li><strong>仓库 Release tag ≠ 任何单个包的版本</strong>。tag 是这批变更的整体发布代号。</li>\n<li><strong>包独立发版</strong>：JSR 按包发版，不要求所有包版本一致。</li>\n<li><strong>版本号判断标准</strong>：<ul>\n<li>有 Breaking Change 或行为变更 → MINOR bump（0.x 阶段）</li>\n<li>纯 Bug fix / 新增非 Breaking 功能 → PATCH bump</li>\n<li>没改 → 不动</li>\n</ul>\n</li>\n<li><strong>Release Note 按包分组</strong>，列出每个包的变更版本，末尾列出未变更的包。</li>\n<li><strong>跨包依赖版本引用</strong>：必须引用已发布到 JSR 的版本（<code>jsr:@scope/package@^version/subpath</code>），不能引用本地路径。</li>\n</ol>\n<p><strong>为什么不用固定版本（Fixed/Locked）</strong>：</p>\n<ul>\n<li>Babel/Angular 的做法要求所有包版本号相同，但在 JSR 生态中不必要——JSR 天然按包发版。</li>\n<li>新增包（如 content 0.1.0）如果被强制跳到 0.9.0，版本号含金量稀释。</li>\n<li>没改的包浪费版本号，changelog 虚胖。</li>\n</ul>\n<p><strong>为什么不用完全独立版本（不加仓库 tag）</strong>：</p>\n<ul>\n<li>用户需要知道&quot;这一批变更的整体节奏&quot;——仓库 tag 提供叙事感。</li>\n<li>GitHub Release 是用户发现更新的入口，不能省略。</li>\n</ul>\n<h3>版本号语义总结</h3>\n<table>\n<thead>\n<tr>\n<th>版本段</th>\n<th>含义</th>\n<th>Breaking Change 规则</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>0.7–0.10</td>\n<td>框架在定义自己</td>\n<td>MINOR 升级允许破坏性变更</td>\n</tr>\n<tr>\n<td>0.11</td>\n<td>过渡版本：基础设施成熟 + 新能力引入</td>\n<td>仍允许，但应尽量减少</td>\n</tr>\n<tr>\n<td>1.0</td>\n<td>公共 API 冻结</td>\n<td>绝不允许（必须升 MAJOR）</td>\n</tr>\n<tr>\n<td>1.x</td>\n<td>增量扩展</td>\n<td>公共 API 不破坏</td>\n</tr>\n<tr>\n<td>2.0</td>\n<td>范式转换（如果需要）</td>\n<td>允许，但需迁移指南</td>\n</tr>\n</tbody></table>\n<h2>Consequences</h2>\n<p><strong>正面：</strong></p>\n<ul>\n<li>每个版本号有清晰的语义——用户看到版本号就能判断升级风险</li>\n<li>1.0.0 有明确的判定标准，不再是&quot;感觉差不多了&quot;的主观判断</li>\n<li>Compiler 和 Fullstack 在 0.x 阶段完成，不用担心 1.0 后的 Breaking Change</li>\n<li>审计发现有了正式的版本归属</li>\n</ul>\n<p><strong>负面：</strong></p>\n<ul>\n<li>0.x 阶段拉长（0.7 → 0.11 共 5 个 MINOR 版本），可能给人&quot;不够稳定&quot;的印象</li>\n<li>需要在 1.0 前维护公共 API 列表，增加了文档工作量</li>\n<li>如果 0.11 的 Compiler Alpha 发现需要调整 DSD renderer 输出格式，可能影响 1.0 的时间线</li>\n</ul>\n<p><strong>缓解：</strong></p>\n<ul>\n<li>每个 0.x 版本都应该有明确的里程碑文档，传递&quot;渐进成熟&quot;而非&quot;还不稳定&quot;的信号</li>\n<li>公共 API 列表在 v0.8 就开始文档化（标记为 &quot;Stable API Candidate&quot;），让 1.0 的门槛更具体</li>\n<li>如果 0.11 Compiler Alpha 进展顺利，可以合并到 1.0 直接发布</li>\n</ul>\n<h2>参考</h2>\n<ul>\n<li><a href=\"/blog/0002-less-compiler-eliminate-lit\" rel=\"noopener noreferrer\">ADR 0002: .less Compiler</a></li>\n<li><a href=\"/blog/0005-with-dsd-hydration-mixin\" rel=\"noopener noreferrer\">ADR 0005: WithDsdHydration Mixin</a></li>\n<li><a href=\"https://semver.org/\" rel=\"noopener noreferrer\">SemVer 2.0.0</a></li>\n<li>LessJS 四维审计报告 (2026-05-07)</li>\n</ul>\n<hr />\n<p><em>决策日期: 2026-05-07 | 版本: v0.6.2</em></p>\n"
  },
  {
    "slug": "0005-with-dsd-hydration-mixin",
    "frontmatter": {
      "title": "WithDsdHydration Mixin + 自建 SSR 决策",
      "date": "2026-05-07",
      "draft": false,
      "tags": [
        "architecture",
        "decision"
      ],
      "type": "adr",
      "lang": "zh"
    },
    "content": "\n## Status: ✅ **ADOPTED** — v0.6.2 架构决策\n\n## 上下文\n\nLessJS 使用 Lit 作为 UI 层框架，但 SSR 渲染器（`renderDSD()`）是自建的，不使用 `@lit-labs/ssr`。这导致 DSD 预渲染的组件存在水合缺口：框架的模板系统没有参与 SSR，所以 `@click` 事件绑定和响应式更新都不生效。\n\n我们需要回答两个问题：\n\n1. 为什么不用 Lit/FAST 的原生 SSR？\n2. 如何弥补自建 SSR 造成的水合缺口？\n\n## 决策\n\n### 1. 自建 SSR，不用框架原生 SSR\n\n| 原因             | Lit SSR (`@lit-labs/ssr`)                         | FAST SSR     | LessJS `renderDSD()`                              |\n| ---------------- | ------------------------------------------------- | ------------ | ------------------------------------------------- |\n| Deno/Edge 可运行 | ❌ 依赖 `node:stream`、`node:buffer`              | ❌ 同上      | ✅ 纯字符串拼接                                   |\n| 输出标准 DSD     | ❌ 私有序列化格式，需 `@lit-labs/ssr-client` 解析 | ❌ 同上      | ✅ WHATWG 标准 `<template shadowrootmode=\"open\">` |\n| 框架无关         | ❌ 绑定 Lit                                       | ❌ 绑定 FAST | ✅ Core 零框架依赖                                |\n\n**权重分析：**\n\n- **Deno/Edge 可运行** — 硬性约束。LessJS 目标运行时是 Deno + Edge Functions，`@lit-labs/ssr` 的 Node.js 依赖使其完全无法使用。单这一条就足够。\n- **标准 DSD** — 用户体验约束。WHATWG 标准 DSD 在 HTML 解析时即挂载 shadow root，零 JS、零闪烁。Lit SSR 的私有序列化格式需要等 JS 加载完才能恢复。\n- **框架无关** — 架构约束。LessJS Core 的核心承诺是 framework-agnostic，绑死某个框架的 SSR 会让这个承诺名存实亡。\n\n### 2. 用 `WithDsdHydration` Mixin 弥补水合缺口\n\n将 `LitDsdElement` 中 **80% 框架无关的逻辑** 提取到 `@openelement/core` 的通用 Mixin：\n\n```\n@openelement/core\n  └── WithDsdHydration<T extends HTMLElement>(Base: T): T\n        // DSD 检测（shadow root 已有内容？）\n        // _hydrateEvents() — HydrateEventDescriptor → addEventListener\n        // updateDsdElement() — querySelectorAll + 回调\n        // AbortController 自动清理\n\n@openelement/adapter-lit\n  └── LitDsdElement = WithDsdHydration(LitElement)\n        // render() → nothing（Lit 模板绕过）\n        // createRenderRoot() 检测已有 shadow root\n\n@openelement/adapter-fast (未来)\n  └── FastDsdElement = WithDsdHydration(FASTElement)\n        // FAST 模板绕过策略\n```\n\n**为什么是 Mixin 而不是基类：**\n\n- Mixin 接受任意基类（`LitElement`、`FASTElement`、`HTMLElement`），返回增强后的类\n- 不需要组件继承特定基类，不与框架继承链冲突\n- `@openelement/ui` 不需要依赖 `@openelement/adapter-lit`，消除了循环依赖风险\n- 符合 LessJS \"framework-agnostic core + pluggable adapters\" 的架构理念\n\n**Mixin 中框架无关 vs 框架特定的边界：**\n\n| 能力                          | 通用？ | 依赖                                                           |\n| ----------------------------- | ------ | -------------------------------------------------------------- |\n| DSD 检测                      | ✅     | 标准 DOM API                                                   |\n| 事件绑定 (`_hydrateEvents`)   | ✅     | 标准 DOM API                                                   |\n| DOM 更新 (`updateDsdElement`) | ✅     | 标准 DOM API                                                   |\n| 清理 (`AbortController`)      | ✅     | 标准 DOM API                                                   |\n| 渲染绕过                      | ❌     | 每个框架不同（Lit→`nothing`, FAST→自己的方式, vanilla→不需要） |\n| 响应式集成                    | ❌     | 每个框架不同（Lit `requestUpdate`, FAST `Observable`）         |\n\n### 3. Stencil 不纳入 Mixin 体系\n\nStencil 是编译器而非运行时库，它把 TSX 编译成标准 Custom Element。适配 Stencil 需要**编译器插件**或**后处理 transform**，跟运行时 Mixin 不是同一路径。当前不纳入规划。\n\n## 三层组件模型\n\nMixin 只服务于 Layer 2（DSD Interactive）。三层各有不同的水合策略：\n\n| 层级    | 名称            | DSD | 水合                     | 适用场景       |\n| ------- | --------------- | --- | ------------------------ | -------------- |\n| Layer 1 | DSD Static      | ✅  | 无需                     | 纯展示         |\n| Layer 2 | DSD Interactive | ✅  | `WithDsdHydration` Mixin | 需要首屏+交互  |\n| Layer 3 | Pure Island     | ❌  | 框架原生                 | 需要完整响应式 |\n\n三体困境（自有 SSR + 框架响应式 + DSD 无闪烁）在**组件级别**消解——不是所有组件都需要 DSD。\n\n## 后果\n\n**正面：**\n\n- Core 保持 framework-agnostic，UI 组件不再依赖特定 adapter\n- 适配新框架只需写一个薄壳（`WithDsdHydration(NewFrameworkElement)`）\n- 手动水合的代价被控制在 `static hydrateEvents` 声明级别\n- 标准 DSD 确保首屏零闪烁、零 JS\n\n**负面：**\n\n- Layer 2 组件仍需手动声明 `hydrateEvents`（编译时无类型检查）\n- Layer 2 组件的 DOM 更新仍需手动 `updateDsdElement()`（无框架响应式）\n- 这些局限需要等待 Lit 原生 hydration 或 `.less` 编译器才能根治\n\n**演进路径：**\n\n1. **v0.6.2**：`WithDsdHydration` Mixin 提取到 Core，组件使用 `extends WithDsdHydration(LitElement)`\n2. **中期**：`@lit-labs/ssr` hydration 模块成熟后，Lit 原生 diff 已有 DOM，Mixin 的水合逻辑自然废弃\n3. **远期**：`.less` 编译器输出 SSR HTML + 客户端水合代码，人类不再写水合代码\n\n## 参考\n\n- [ADR 0002: .less Compiler](/blog/0002-less-compiler-eliminate-lit) — 远期方向\n- [WHATWG HTML §13.4: Declarative Shadow DOM](https://html.spec.whatwg.org/multipage/scripting.html)\n- [@lit-labs/ssr](https://github.com/nicolo-ribaudo/lit/tree/main/packages/labs/ssr) — Lit 官方 SSR（Node.js 绑定）\n\n---\n\n_决策日期: 2026-05-07 | 版本: v0.6.2_\n",
    "html": "<h2>Status: ✅ <strong>ADOPTED</strong> — v0.6.2 架构决策</h2>\n<h2>上下文</h2>\n<p>LessJS 使用 Lit 作为 UI 层框架，但 SSR 渲染器（<code>renderDSD()</code>）是自建的，不使用 <code>@lit-labs/ssr</code>。这导致 DSD 预渲染的组件存在水合缺口：框架的模板系统没有参与 SSR，所以 <code>@click</code> 事件绑定和响应式更新都不生效。</p>\n<p>我们需要回答两个问题：</p>\n<ol>\n<li>为什么不用 Lit/FAST 的原生 SSR？</li>\n<li>如何弥补自建 SSR 造成的水合缺口？</li>\n</ol>\n<h2>决策</h2>\n<h3>1. 自建 SSR，不用框架原生 SSR</h3>\n<table>\n<thead>\n<tr>\n<th>原因</th>\n<th>Lit SSR (<code>@lit-labs/ssr</code>)</th>\n<th>FAST SSR</th>\n<th>LessJS <code>renderDSD()</code></th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Deno/Edge 可运行</td>\n<td>❌ 依赖 <code>node:stream</code>、<code>node:buffer</code></td>\n<td>❌ 同上</td>\n<td>✅ 纯字符串拼接</td>\n</tr>\n<tr>\n<td>输出标准 DSD</td>\n<td>❌ 私有序列化格式，需 <code>@lit-labs/ssr-client</code> 解析</td>\n<td>❌ 同上</td>\n<td>✅ WHATWG 标准 <code>&lt;template shadowrootmode=&quot;open&quot;&gt;</code></td>\n</tr>\n<tr>\n<td>框架无关</td>\n<td>❌ 绑定 Lit</td>\n<td>❌ 绑定 FAST</td>\n<td>✅ Core 零框架依赖</td>\n</tr>\n</tbody></table>\n<p><strong>权重分析：</strong></p>\n<ul>\n<li><strong>Deno/Edge 可运行</strong> — 硬性约束。LessJS 目标运行时是 Deno + Edge Functions，<code>@lit-labs/ssr</code> 的 Node.js 依赖使其完全无法使用。单这一条就足够。</li>\n<li><strong>标准 DSD</strong> — 用户体验约束。WHATWG 标准 DSD 在 HTML 解析时即挂载 shadow root，零 JS、零闪烁。Lit SSR 的私有序列化格式需要等 JS 加载完才能恢复。</li>\n<li><strong>框架无关</strong> — 架构约束。LessJS Core 的核心承诺是 framework-agnostic，绑死某个框架的 SSR 会让这个承诺名存实亡。</li>\n</ul>\n<h3>2. 用 <code>WithDsdHydration</code> Mixin 弥补水合缺口</h3>\n<p>将 <code>LitDsdElement</code> 中 <strong>80% 框架无关的逻辑</strong> 提取到 <code>@openelement/core</code> 的通用 Mixin：</p>\n<pre><code>@openelement/core\n  └── WithDsdHydration&lt;T extends HTMLElement&gt;(Base: T): T\n        // DSD 检测（shadow root 已有内容？）\n        // _hydrateEvents() — HydrateEventDescriptor → addEventListener\n        // updateDsdElement() — querySelectorAll + 回调\n        // AbortController 自动清理\n\n@openelement/adapter-lit\n  └── LitDsdElement = WithDsdHydration(LitElement)\n        // render() → nothing（Lit 模板绕过）\n        // createRenderRoot() 检测已有 shadow root\n\n@openelement/adapter-fast (未来)\n  └── FastDsdElement = WithDsdHydration(FASTElement)\n        // FAST 模板绕过策略\n</code></pre>\n<p><strong>为什么是 Mixin 而不是基类：</strong></p>\n<ul>\n<li>Mixin 接受任意基类（<code>LitElement</code>、<code>FASTElement</code>、<code>HTMLElement</code>），返回增强后的类</li>\n<li>不需要组件继承特定基类，不与框架继承链冲突</li>\n<li><code>@openelement/ui</code> 不需要依赖 <code>@openelement/adapter-lit</code>，消除了循环依赖风险</li>\n<li>符合 LessJS &quot;framework-agnostic core + pluggable adapters&quot; 的架构理念</li>\n</ul>\n<p><strong>Mixin 中框架无关 vs 框架特定的边界：</strong></p>\n<table>\n<thead>\n<tr>\n<th>能力</th>\n<th>通用？</th>\n<th>依赖</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>DSD 检测</td>\n<td>✅</td>\n<td>标准 DOM API</td>\n</tr>\n<tr>\n<td>事件绑定 (<code>_hydrateEvents</code>)</td>\n<td>✅</td>\n<td>标准 DOM API</td>\n</tr>\n<tr>\n<td>DOM 更新 (<code>updateDsdElement</code>)</td>\n<td>✅</td>\n<td>标准 DOM API</td>\n</tr>\n<tr>\n<td>清理 (<code>AbortController</code>)</td>\n<td>✅</td>\n<td>标准 DOM API</td>\n</tr>\n<tr>\n<td>渲染绕过</td>\n<td>❌</td>\n<td>每个框架不同（Lit→<code>nothing</code>, FAST→自己的方式, vanilla→不需要）</td>\n</tr>\n<tr>\n<td>响应式集成</td>\n<td>❌</td>\n<td>每个框架不同（Lit <code>requestUpdate</code>, FAST <code>Observable</code>）</td>\n</tr>\n</tbody></table>\n<h3>3. Stencil 不纳入 Mixin 体系</h3>\n<p>Stencil 是编译器而非运行时库，它把 TSX 编译成标准 Custom Element。适配 Stencil 需要<strong>编译器插件</strong>或<strong>后处理 transform</strong>，跟运行时 Mixin 不是同一路径。当前不纳入规划。</p>\n<h2>三层组件模型</h2>\n<p>Mixin 只服务于 Layer 2（DSD Interactive）。三层各有不同的水合策略：</p>\n<table>\n<thead>\n<tr>\n<th>层级</th>\n<th>名称</th>\n<th>DSD</th>\n<th>水合</th>\n<th>适用场景</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Layer 1</td>\n<td>DSD Static</td>\n<td>✅</td>\n<td>无需</td>\n<td>纯展示</td>\n</tr>\n<tr>\n<td>Layer 2</td>\n<td>DSD Interactive</td>\n<td>✅</td>\n<td><code>WithDsdHydration</code> Mixin</td>\n<td>需要首屏+交互</td>\n</tr>\n<tr>\n<td>Layer 3</td>\n<td>Pure Island</td>\n<td>❌</td>\n<td>框架原生</td>\n<td>需要完整响应式</td>\n</tr>\n</tbody></table>\n<p>三体困境（自有 SSR + 框架响应式 + DSD 无闪烁）在<strong>组件级别</strong>消解——不是所有组件都需要 DSD。</p>\n<h2>后果</h2>\n<p><strong>正面：</strong></p>\n<ul>\n<li>Core 保持 framework-agnostic，UI 组件不再依赖特定 adapter</li>\n<li>适配新框架只需写一个薄壳（<code>WithDsdHydration(NewFrameworkElement)</code>）</li>\n<li>手动水合的代价被控制在 <code>static hydrateEvents</code> 声明级别</li>\n<li>标准 DSD 确保首屏零闪烁、零 JS</li>\n</ul>\n<p><strong>负面：</strong></p>\n<ul>\n<li>Layer 2 组件仍需手动声明 <code>hydrateEvents</code>（编译时无类型检查）</li>\n<li>Layer 2 组件的 DOM 更新仍需手动 <code>updateDsdElement()</code>（无框架响应式）</li>\n<li>这些局限需要等待 Lit 原生 hydration 或 <code>.less</code> 编译器才能根治</li>\n</ul>\n<p><strong>演进路径：</strong></p>\n<ol>\n<li><strong>v0.6.2</strong>：<code>WithDsdHydration</code> Mixin 提取到 Core，组件使用 <code>extends WithDsdHydration(LitElement)</code></li>\n<li><strong>中期</strong>：<code>@lit-labs/ssr</code> hydration 模块成熟后，Lit 原生 diff 已有 DOM，Mixin 的水合逻辑自然废弃</li>\n<li><strong>远期</strong>：<code>.less</code> 编译器输出 SSR HTML + 客户端水合代码，人类不再写水合代码</li>\n</ol>\n<h2>参考</h2>\n<ul>\n<li><a href=\"/blog/0002-less-compiler-eliminate-lit\" rel=\"noopener noreferrer\">ADR 0002: .less Compiler</a> — 远期方向</li>\n<li><a href=\"https://html.spec.whatwg.org/multipage/scripting.html\" rel=\"noopener noreferrer\">WHATWG HTML §13.4: Declarative Shadow DOM</a></li>\n<li><a href=\"https://github.com/nicolo-ribaudo/lit/tree/main/packages/labs/ssr\" rel=\"noopener noreferrer\">@lit-labs/ssr</a> — Lit 官方 SSR（Node.js 绑定）</li>\n</ul>\n<hr />\n<p><em>决策日期: 2026-05-07 | 版本: v0.6.2</em></p>\n"
  },
  {
    "slug": "0004-blog-system",
    "frontmatter": {
      "title": "@openelement/blog - Standalone Blog Package",
      "date": "2026-05-01",
      "draft": false,
      "tags": [
        "architecture",
        "decision"
      ],
      "type": "adr",
      "lang": "en"
    },
    "content": "\n## Status\n\n**SUPERSEDED** — Blog functionality was merged into `@openelement/content` (Blog + Nav + Sitemap unified plugin) instead of shipping as a standalone `@openelement/blog` package. See `packages/content/` for the implementation.\n\n## Context\n\nLessJS currently has two hardcoded blog route pages (`/blog/` +\n`/blog/less-compiler`) on its own docs site. These are hand-written custom\nelement pages, not a reusable system.\n\nUsers need a proper blog: drop in `.md` files, get automatic listing +\npagination + RSS + tags. Like VitePress's blog feature, but as a LessJS plugin.\n\n## Constraints\n\nThe blog system should not require Lit. It should work first as a plain SSG\nplugin, then gain `.less` compiler templates when the compiler exists:\n\n- Post templates should be able to compile to vanilla Custom Elements when\n  `.less` exists\n- SSR must be synchronous string concatenation (`template.innerHTML`)\n- No page-level client runtime for plain blog pages; interactive widgets remain\n  islands\n- The `.less` compiler is an optional future template backend, not a blocker\n  for the first release\n\n## Proposal\n\n### `@openelement/blog` package\n\nA Vite plugin that:\n\n1. **Scans** a user-defined directory for `.md` files with YAML frontmatter\n2. **Generates** route entries at build time (like route-scanner does for `.ts`\n   files)\n3. **Renders** listing pages, post pages, tag pages, and RSS feed during SSG\n\n### User experience\n\n```ts\n// vite.config.ts\nimport { less } from '@openelement/core';\nimport { lessBlog } from '@openelement/blog';\n\nexport default defineConfig({\n  plugins: [\n    less(),\n    lessBlog({\n      dir: 'content/blog', // where your .md files live\n      title: 'My Blog',\n      postsPerPage: 10,\n    }),\n  ],\n});\n```\n\n```md\n# content/blog/hello-world.md\n\n---\ntitle: Hello World\ndate: 2026-05-01\ntags: [lessjs, meta]\n---\n\nThis is my first post.\n```\n\n### Generated routes\n\n| Route               | Content                                |\n| ------------------- | -------------------------------------- |\n| `/blog/`            | Post listing (paginated, newest first) |\n| `/blog/hello-world` | Individual post (rendered from .md)    |\n| `/blog/page/2`      | Page 2 of listing                      |\n| `/blog/tags/lessjs` | Filter by tag                          |\n| `/blog/feed.xml`    | RSS 2.0 / Atom feed                    |\n\n### Plugin architecture\n\nThe `lessBlog()` plugin hooks into the LessJS build pipeline:\n\n```text\nvite.config.ts\n  -> lessBlog() reads content/blog/*.md\n       extracts frontmatter -> metadata.json\n       registers virtual routes\n  -> deno task build\n       Phase 1: Vite SSR build\n       Phase 2: buildClient() (no island chunks needed for blog)\n       Phase 3: buildSSG() renders /blog/* pages\n            generates /feed.xml\n  -> dist/\n       blog/index.html\n       blog/hello-world/index.html\n       feed.xml\n```\n\n### Post rendering\n\nWith the `.less` compiler:\n\n```less\n<!-- blog post template (built into @openelement/blog) -->\n<template>\n  <article>\n    <h1>{post.title}</h1>\n    <time datetime=\"{post.isoDate}\">{post.displayDate}</time>\n    <div class=\"content\">{post.html}</div>\n    <nav class=\"tags\">{post.tags}</nav>\n  </article>\n</template>\n\n<script>\n  // post data injected at compile time by the plugin\n  post = { title: \"\", html: \"\", tags: [], isoDate: \"\", displayDate: \"\" }\n</script>\n\n<style>\n  .content :host { max-width: 720px; margin: 0 auto; }\n  time { font-size: 0.75rem; color: var(--less-text-muted); }\n</style>\n```\n\nWithout the `.less` compiler:\n\nThe same content can be rendered through the LessJS DSD renderer or a safe\nserver-side HTML template helper. It should not depend on Lit for plain Markdown\npages.\n\n### MDX support (future)\n\nThe `.md` parser should support:\n\n- **Basic**: Gray-matter frontmatter + marked/markdown-it\n- **Extended (v2)**: MDX - embed Lit Components or `.less` components inline in\n  markdown\n- **Code blocks**: Syntax highlighting at build time (not client-side)\n\n## Implementation order\n\n1. v0.8.0 stabilizes core API (render-dsd split, component unification)\n2. `@openelement/blog` development starts after v0.8.0 — SSG plugin form only\n3. Dogfood on LessJS docs site during v0.9.0 phase\n4. `.less` compiler support is added when v0.11.0 alpha is available\n5. LessJS docs site eats its own dogfood and replaces the current hardcoded blog\n   routes\n\n## Open Questions\n\n1. Markdown parser: `marked` (lightweight) vs `markdown-it` (plugin ecosystem)\n   vs custom?\n2. Should the blog template be customizable via\n   `lessBlog({ template: \"my-template.less\" })`?\n3. RSS vs Atom vs both?\n4. Comments? (Disqus, utteranc.es, or leave it to the user)\n\n## Consequences\n\n- **Positive**: Users get a one-line blog setup, competitive with VitePress\n- **Positive**: LessJS docs site can dogfood its own blog package\n- **Positive**: Zero framework runtime for plain blog pages (with `.less`\n  compiler)\n- **Negative**: The ideal compiler-backed architecture comes later\n- **Negative**: Markdown parsing at build time adds ~100ms to Phase 1\n",
    "html": "<h2>Status</h2>\n<p><strong>SUPERSEDED</strong> — Blog functionality was merged into <code>@openelement/content</code> (Blog + Nav + Sitemap unified plugin) instead of shipping as a standalone <code>@openelement/blog</code> package. See <code>packages/content/</code> for the implementation.</p>\n<h2>Context</h2>\n<p>LessJS currently has two hardcoded blog route pages (<code>/blog/</code> +\n<code>/blog/less-compiler</code>) on its own docs site. These are hand-written custom\nelement pages, not a reusable system.</p>\n<p>Users need a proper blog: drop in <code>.md</code> files, get automatic listing +\npagination + RSS + tags. Like VitePress&#39;s blog feature, but as a LessJS plugin.</p>\n<h2>Constraints</h2>\n<p>The blog system should not require Lit. It should work first as a plain SSG\nplugin, then gain <code>.less</code> compiler templates when the compiler exists:</p>\n<ul>\n<li>Post templates should be able to compile to vanilla Custom Elements when\n<code>.less</code> exists</li>\n<li>SSR must be synchronous string concatenation (<code>template.innerHTML</code>)</li>\n<li>No page-level client runtime for plain blog pages; interactive widgets remain\nislands</li>\n<li>The <code>.less</code> compiler is an optional future template backend, not a blocker\nfor the first release</li>\n</ul>\n<h2>Proposal</h2>\n<h3><code>@openelement/blog</code> package</h3>\n<p>A Vite plugin that:</p>\n<ol>\n<li><strong>Scans</strong> a user-defined directory for <code>.md</code> files with YAML frontmatter</li>\n<li><strong>Generates</strong> route entries at build time (like route-scanner does for <code>.ts</code>\nfiles)</li>\n<li><strong>Renders</strong> listing pages, post pages, tag pages, and RSS feed during SSG</li>\n</ol>\n<h3>User experience</h3>\n<pre><code class=\"language-ts\">// vite.config.ts\nimport { less } from &#39;@openelement/core&#39;;\nimport { lessBlog } from &#39;@openelement/blog&#39;;\n\nexport default defineConfig({\n  plugins: [\n    less(),\n    lessBlog({\n      dir: &#39;content/blog&#39;, // where your .md files live\n      title: &#39;My Blog&#39;,\n      postsPerPage: 10,\n    }),\n  ],\n});\n</code></pre>\n<pre><code class=\"language-md\"># content/blog/hello-world.md\n\n---\ntitle: Hello World\ndate: 2026-05-01\ntags: [lessjs, meta]\n---\n\nThis is my first post.\n</code></pre>\n<h3>Generated routes</h3>\n<table>\n<thead>\n<tr>\n<th>Route</th>\n<th>Content</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>/blog/</code></td>\n<td>Post listing (paginated, newest first)</td>\n</tr>\n<tr>\n<td><code>/blog/hello-world</code></td>\n<td>Individual post (rendered from .md)</td>\n</tr>\n<tr>\n<td><code>/blog/page/2</code></td>\n<td>Page 2 of listing</td>\n</tr>\n<tr>\n<td><code>/blog/tags/lessjs</code></td>\n<td>Filter by tag</td>\n</tr>\n<tr>\n<td><code>/blog/feed.xml</code></td>\n<td>RSS 2.0 / Atom feed</td>\n</tr>\n</tbody></table>\n<h3>Plugin architecture</h3>\n<p>The <code>lessBlog()</code> plugin hooks into the LessJS build pipeline:</p>\n<pre><code class=\"language-text\">vite.config.ts\n  -&gt; lessBlog() reads content/blog/*.md\n       extracts frontmatter -&gt; metadata.json\n       registers virtual routes\n  -&gt; deno task build\n       Phase 1: Vite SSR build\n       Phase 2: buildClient() (no island chunks needed for blog)\n       Phase 3: buildSSG() renders /blog/* pages\n            generates /feed.xml\n  -&gt; dist/\n       blog/index.html\n       blog/hello-world/index.html\n       feed.xml\n</code></pre>\n<h3>Post rendering</h3>\n<p>With the <code>.less</code> compiler:</p>\n<pre><code class=\"language-less\">&lt;!-- blog post template (built into @openelement/blog) --&gt;\n&lt;template&gt;\n  &lt;article&gt;\n    &lt;h1&gt;{post.title}&lt;/h1&gt;\n    &lt;time datetime=&quot;{post.isoDate}&quot;&gt;{post.displayDate}&lt;/time&gt;\n    &lt;div class=&quot;content&quot;&gt;{post.html}&lt;/div&gt;\n    &lt;nav class=&quot;tags&quot;&gt;{post.tags}&lt;/nav&gt;\n  &lt;/article&gt;\n&lt;/template&gt;\n\n&lt;script&gt;\n  // post data injected at compile time by the plugin\n  post = { title: &quot;&quot;, html: &quot;&quot;, tags: [], isoDate: &quot;&quot;, displayDate: &quot;&quot; }\n&lt;/script&gt;\n\n&lt;style&gt;\n  .content :host { max-width: 720px; margin: 0 auto; }\n  time { font-size: 0.75rem; color: var(--less-text-muted); }\n&lt;/style&gt;\n</code></pre>\n<p>Without the <code>.less</code> compiler:</p>\n<p>The same content can be rendered through the LessJS DSD renderer or a safe\nserver-side HTML template helper. It should not depend on Lit for plain Markdown\npages.</p>\n<h3>MDX support (future)</h3>\n<p>The <code>.md</code> parser should support:</p>\n<ul>\n<li><strong>Basic</strong>: Gray-matter frontmatter + marked/markdown-it</li>\n<li><strong>Extended (v2)</strong>: MDX - embed Lit Components or <code>.less</code> components inline in\nmarkdown</li>\n<li><strong>Code blocks</strong>: Syntax highlighting at build time (not client-side)</li>\n</ul>\n<h2>Implementation order</h2>\n<ol>\n<li>v0.8.0 stabilizes core API (render-dsd split, component unification)</li>\n<li><code>@openelement/blog</code> development starts after v0.8.0 — SSG plugin form only</li>\n<li>Dogfood on LessJS docs site during v0.9.0 phase</li>\n<li><code>.less</code> compiler support is added when v0.11.0 alpha is available</li>\n<li>LessJS docs site eats its own dogfood and replaces the current hardcoded blog\nroutes</li>\n</ol>\n<h2>Open Questions</h2>\n<ol>\n<li>Markdown parser: <code>marked</code> (lightweight) vs <code>markdown-it</code> (plugin ecosystem)\nvs custom?</li>\n<li>Should the blog template be customizable via\n<code>lessBlog({ template: &quot;my-template.less&quot; })</code>?</li>\n<li>RSS vs Atom vs both?</li>\n<li>Comments? (Disqus, utteranc.es, or leave it to the user)</li>\n</ol>\n<h2>Consequences</h2>\n<ul>\n<li><strong>Positive</strong>: Users get a one-line blog setup, competitive with VitePress</li>\n<li><strong>Positive</strong>: LessJS docs site can dogfood its own blog package</li>\n<li><strong>Positive</strong>: Zero framework runtime for plain blog pages (with <code>.less</code>\ncompiler)</li>\n<li><strong>Negative</strong>: The ideal compiler-backed architecture comes later</li>\n<li><strong>Negative</strong>: Markdown parsing at build time adds ~100ms to Phase 1</li>\n</ul>\n"
  },
  {
    "slug": "0003-pwa-support",
    "frontmatter": {
      "title": "PWA Support for LessJS SSG",
      "date": "2026-05-11",
      "draft": false,
      "tags": [
        "architecture",
        "decision"
      ],
      "type": "adr",
      "lang": "en"
    },
    "content": "\n## Status\n\n**PARTIALLY IMPLEMENTED** - available through `less({ pwa })` metadata and the\nofficial `deno task build` flow. The service worker strategy has changed from\nthe original full-precache design.\n\n## Context\n\nLessJS generates pure static HTML with Declarative Shadow DOM. This is the ideal\nsubstrate for a Progressive Web App:\n\n- All pages are pre-rendered HTML (no server needed)\n- Assets are versioned hashes (perfect cache keys)\n- API routes can live separately on serverless platforms\n- Service worker should avoid stale HTML by using NetworkFirst for HTML/API and\n  CacheFirst for hashed assets\n\n## Proposal\n\nAdd a `pwa` option to the `less()` plugin that automatically generates:\n\n1. `manifest.json` - Web App Manifest (name, icons, display, theme_color)\n2. `sw.js` - Service Worker with NetworkFirst (HTML/API) + CacheFirst (assets)\n3. HTML `<head>` injection - `<link rel=\"manifest\">`, `<meta\n   name=\"theme-color\">`, `<link rel=\"service-worker\">`\n\n### API\n\n```ts\n// vite.config.ts\nexport default defineConfig({\n  plugins: [less({\n    pwa: {\n      name: 'My LessJS App',\n      shortName: 'LessJS',\n      themeColor: '#000000',\n      backgroundColor: '#ffffff',\n      icons: [{ src: '/icon-192.png', sizes: '192x192', type: 'image/png' }],\n      // Current built-in strategy: NetworkFirst HTML/API, CacheFirst assets.\n    },\n  })],\n});\n```\n\n### Service Worker Strategy\n\n```js\n// Generated sw.js (~40 lines)\nself.addEventListener('install', () => self.skipWaiting());\nself.addEventListener('fetch', (e) => {\n  const url = new URL(e.request.url);\n  const isAsset = /\\.[a-z0-9]+$/i.test(url.pathname) && !url.pathname.includes('/api/');\n  e.respondWith(isAsset ? cacheFirst(e.request) : networkFirst(e.request));\n});\n```\n\n### SSG Integration\n\nInside the SSG phase, after pages are rendered:\n\n```ts\nif (options.pwa) {\n  writeFileSync(join(outputDir, 'manifest.json'), generateManifest(options.pwa));\n  writeFileSync(join(outputDir, 'sw.js'), generateSwScript(options.pwa));\n  // inject manifest link + sw registration into all HTML files\n  injectIntoHtml(outputDir, {\n    head: `<link rel=\"manifest\" href=\"/manifest.json\">`,\n    body: `<script>navigator.serviceWorker?.register('/sw.js')</script>`,\n  });\n}\n```\n\n## Consequences\n\n- **Positive**: Offline access, instant repeat visits, installable on mobile\n- **Positive**: Minimal code (~100 lines total across plugin + generator + sw\n  script)\n- **Neutral**: Service worker scope limited to site root (no cross-site impact)\n- **Negative**: Offline-first HTML is intentionally not provided by default;\n  stale static pages are a worse default\n- **Negative**: Cache invalidation needs a generated cache name; current\n  implementation uses a build-time timestamp\n",
    "html": "<h2>Status</h2>\n<p><strong>PARTIALLY IMPLEMENTED</strong> - available through <code>less({ pwa })</code> metadata and the\nofficial <code>deno task build</code> flow. The service worker strategy has changed from\nthe original full-precache design.</p>\n<h2>Context</h2>\n<p>LessJS generates pure static HTML with Declarative Shadow DOM. This is the ideal\nsubstrate for a Progressive Web App:</p>\n<ul>\n<li>All pages are pre-rendered HTML (no server needed)</li>\n<li>Assets are versioned hashes (perfect cache keys)</li>\n<li>API routes can live separately on serverless platforms</li>\n<li>Service worker should avoid stale HTML by using NetworkFirst for HTML/API and\nCacheFirst for hashed assets</li>\n</ul>\n<h2>Proposal</h2>\n<p>Add a <code>pwa</code> option to the <code>less()</code> plugin that automatically generates:</p>\n<ol>\n<li><code>manifest.json</code> - Web App Manifest (name, icons, display, theme_color)</li>\n<li><code>sw.js</code> - Service Worker with NetworkFirst (HTML/API) + CacheFirst (assets)</li>\n<li>HTML <code>&lt;head&gt;</code> injection - <code>&lt;link rel=&quot;manifest&quot;&gt;</code>, <code>&lt;meta name=&quot;theme-color&quot;&gt;</code>, <code>&lt;link rel=&quot;service-worker&quot;&gt;</code></li>\n</ol>\n<h3>API</h3>\n<pre><code class=\"language-ts\">// vite.config.ts\nexport default defineConfig({\n  plugins: [less({\n    pwa: {\n      name: &#39;My LessJS App&#39;,\n      shortName: &#39;LessJS&#39;,\n      themeColor: &#39;#000000&#39;,\n      backgroundColor: &#39;#ffffff&#39;,\n      icons: [{ src: &#39;/icon-192.png&#39;, sizes: &#39;192x192&#39;, type: &#39;image/png&#39; }],\n      // Current built-in strategy: NetworkFirst HTML/API, CacheFirst assets.\n    },\n  })],\n});\n</code></pre>\n<h3>Service Worker Strategy</h3>\n<pre><code class=\"language-js\">// Generated sw.js (~40 lines)\nself.addEventListener(&#39;install&#39;, () =&gt; self.skipWaiting());\nself.addEventListener(&#39;fetch&#39;, (e) =&gt; {\n  const url = new URL(e.request.url);\n  const isAsset = /\\.[a-z0-9]+$/i.test(url.pathname) &amp;&amp; !url.pathname.includes(&#39;/api/&#39;);\n  e.respondWith(isAsset ? cacheFirst(e.request) : networkFirst(e.request));\n});\n</code></pre>\n<h3>SSG Integration</h3>\n<p>Inside the SSG phase, after pages are rendered:</p>\n<pre><code class=\"language-ts\">if (options.pwa) {\n  writeFileSync(join(outputDir, &#39;manifest.json&#39;), generateManifest(options.pwa));\n  writeFileSync(join(outputDir, &#39;sw.js&#39;), generateSwScript(options.pwa));\n  // inject manifest link + sw registration into all HTML files\n  injectIntoHtml(outputDir, {\n    head: `&lt;link rel=&quot;manifest&quot; href=&quot;/manifest.json&quot;&gt;`,\n    body: `&lt;script&gt;navigator.serviceWorker?.register(&#39;/sw.js&#39;)&lt;/script&gt;`,\n  });\n}\n</code></pre>\n<h2>Consequences</h2>\n<ul>\n<li><strong>Positive</strong>: Offline access, instant repeat visits, installable on mobile</li>\n<li><strong>Positive</strong>: Minimal code (~100 lines total across plugin + generator + sw\nscript)</li>\n<li><strong>Neutral</strong>: Service worker scope limited to site root (no cross-site impact)</li>\n<li><strong>Negative</strong>: Offline-first HTML is intentionally not provided by default;\nstale static pages are a worse default</li>\n<li><strong>Negative</strong>: Cache invalidation needs a generated cache name; current\nimplementation uses a build-time timestamp</li>\n</ul>\n"
  },
  {
    "slug": "0002-less-compiler-eliminate-lit",
    "frontmatter": {
      "title": ".less Compiler — Optional Zero-Framework Component Authoring",
      "date": "2026-05-11",
      "draft": false,
      "tags": [
        "architecture",
        "decision"
      ],
      "type": "adr",
      "lang": "en"
    },
    "content": "\n## Status\n\n**DRAFT** — Proposed for v0.11.0 alpha (per ADR 0006 version strategy), not a v0.7–v0.10 blocker.\n\n## Context\n\nLessJS currently uses `lit` for the docs site and UI package, while core is moving toward a\nDSD-first renderer with framework adapters. Lit should remain usable, but it should not be the\nlong-term foundation of the framework contract. The current Lit route brings:\n\n- Lit runtime cost for Lit-authored islands\n- adapter complexity for server rendering and style extraction\n- upgrade/hydration terminology drift between old Lit SSR and new DSD-first rendering\n- SSR/DSD/upgrade paths that must stay in sync\n- Deno fmt panics on tagged template literals with HTML entities (dprint-core bug)\n- Unnecessary type complexity: `noImplicitOverride`, decorators, complex generics\n\n## Proposal\n\nIntroduce `.less` files — a component format purpose-built for LessJS. A compiler transforms\n`.less` files into vanilla Custom Elements at build time. The goal is zero framework runtime for\ncompiled components. Lit remains an adapter, not a forced dependency.\n\n### `.less` file format\n\n```less\n<!-- my-counter.less -->\n<template>\n  <button @click=\"decrement\">−</button>\n  <span>{count}</span>\n  <button @click=\"increment\">+</button>\n</template>\n\n<script>\n  count = 0\n  increment() { this.count++ }\n  decrement() { this.count-- }\n</script>\n\n<style>\n  :host { display: inline-flex; gap: 0.5rem; align-items: center; }\n</style>\n```\n\n### Compiler output\n\n```ts\n// dist/assets/my-counter.js — vanilla Custom Element, 0 deps\nconst tpl = document.querySelector('#my-counter-template');\nclass MyCounter extends HTMLElement {\n  #count = 0;\n  #root = this.attachShadow({ mode: 'open' });\n  get count() {\n    return this.#count;\n  }\n  set count(v) {\n    this.#count = v;\n    this.#update();\n  }\n  #update() {\n    this.#root.querySelector('span').textContent = this.#count;\n  }\n  connectedCallback() {\n    this.#root.append(tpl.content.cloneNode(true));\n    this.#root.querySelector('button:first-child')\n      .onclick = () => this.count--;\n    this.#root.querySelector('button:last-child')\n      .onclick = () => this.count++;\n  }\n}\ncustomElements.define('my-counter', MyCounter);\n```\n\n### What the compiler eliminates\n\n| Layer   | Before (Lit adapter)           | After (.less compiler)                 |\n| ------- | ------------------------------ | -------------------------------------- |\n| Runtime | Lit runtime for Lit components | 0kb framework runtime for compiled CEs |\n| SSR     | adapter-mediated rendering     | LessJS DSD renderer / template strings |\n| Upgrade | Custom Element upgrade         | Custom Element upgrade                 |\n| Build   | esbuild + Lit semantics        | standard TS/JS output                  |\n| Tests   | adapter tests required         | compiler fixture tests required        |\n\n### SSG integration\n\nThe route scanner (route-scanner.ts) already maps `app/routes/*.ts` → URL paths. Extend it to also scan `.less` files:\n\n- `app/routes/blog/hello-world.less` → `/blog/hello-world`\n- `app/routes/blog.less` → `/blog`\n- `app/islands/my-counter.less` → Island component\n\nPage `.less` files render directly (template is the page). Island `.less` files get lazy chunk treatment like today's Lit islands.\n\n### Backward compatibility\n\n- `vite.config.ts` option: `compiler: 'lit' | 'less' | 'auto'` (auto = `.less` files use compiler, `.ts` files use Lit)\n- Lit support retained as optional runtime throughout v0.x\n- v0.11.0 introduces `.less` as alpha and optional\n- v1.0 default remains an open decision; Lit compatibility must not be broken casually\n\n## Consequences\n\n**Positive:**\n\n- Zero framework runtime cost for compiled components\n- Fewer upgrade-order bugs because compiled components target the LessJS DSD model directly\n- No upstream dependency issues (dprint, node-domexception, parse5)\n- Simpler developer API: HTML + minimal script, no class boilerplate\n- True tree-shaking: only used components produce code\n- SSG becomes synchronous string concatenation\n\n**Negative:**\n\n- Not backward compatible with existing Lit code (migration needed)\n- Custom compiler = custom bugs (parser edge cases, sourcemap complexity)\n- HMR in dev mode needs special handling (fall back to Lit or implement compiler in watch mode)\n- No access to Lit ecosystem components (unless bridge is provided)\n\n## Open Questions\n\n1. Dev mode: run compiler on save, or fall back to Lit runtime for HMR?\n2. Islands in `.less` format — does the `<script>` get extracted as a lazy module?\n3. Slot/projection semantics — full HTML spec compliance needed in template compiler?\n4. SSR rendering — `template.innerHTML` is identical to client, but lifecycle hooks (connectedCallback) need SSG treatment\n",
    "html": "<h2>Status</h2>\n<p><strong>DRAFT</strong> — Proposed for v0.11.0 alpha (per ADR 0006 version strategy), not a v0.7–v0.10 blocker.</p>\n<h2>Context</h2>\n<p>LessJS currently uses <code>lit</code> for the docs site and UI package, while core is moving toward a\nDSD-first renderer with framework adapters. Lit should remain usable, but it should not be the\nlong-term foundation of the framework contract. The current Lit route brings:</p>\n<ul>\n<li>Lit runtime cost for Lit-authored islands</li>\n<li>adapter complexity for server rendering and style extraction</li>\n<li>upgrade/hydration terminology drift between old Lit SSR and new DSD-first rendering</li>\n<li>SSR/DSD/upgrade paths that must stay in sync</li>\n<li>Deno fmt panics on tagged template literals with HTML entities (dprint-core bug)</li>\n<li>Unnecessary type complexity: <code>noImplicitOverride</code>, decorators, complex generics</li>\n</ul>\n<h2>Proposal</h2>\n<p>Introduce <code>.less</code> files — a component format purpose-built for LessJS. A compiler transforms\n<code>.less</code> files into vanilla Custom Elements at build time. The goal is zero framework runtime for\ncompiled components. Lit remains an adapter, not a forced dependency.</p>\n<h3><code>.less</code> file format</h3>\n<pre><code class=\"language-less\">&lt;!-- my-counter.less --&gt;\n&lt;template&gt;\n  &lt;button @click=&quot;decrement&quot;&gt;−&lt;/button&gt;\n  &lt;span&gt;{count}&lt;/span&gt;\n  &lt;button @click=&quot;increment&quot;&gt;+&lt;/button&gt;\n&lt;/template&gt;\n\n&lt;script&gt;\n  count = 0\n  increment() { this.count++ }\n  decrement() { this.count-- }\n&lt;/script&gt;\n\n&lt;style&gt;\n  :host { display: inline-flex; gap: 0.5rem; align-items: center; }\n&lt;/style&gt;\n</code></pre>\n<h3>Compiler output</h3>\n<pre><code class=\"language-ts\">// dist/assets/my-counter.js — vanilla Custom Element, 0 deps\nconst tpl = document.querySelector(&#39;#my-counter-template&#39;);\nclass MyCounter extends HTMLElement {\n  #count = 0;\n  #root = this.attachShadow({ mode: &#39;open&#39; });\n  get count() {\n    return this.#count;\n  }\n  set count(v) {\n    this.#count = v;\n    this.#update();\n  }\n  #update() {\n    this.#root.querySelector(&#39;span&#39;).textContent = this.#count;\n  }\n  connectedCallback() {\n    this.#root.append(tpl.content.cloneNode(true));\n    this.#root.querySelector(&#39;button:first-child&#39;)\n      .onclick = () =&gt; this.count--;\n    this.#root.querySelector(&#39;button:last-child&#39;)\n      .onclick = () =&gt; this.count++;\n  }\n}\ncustomElements.define(&#39;my-counter&#39;, MyCounter);\n</code></pre>\n<h3>What the compiler eliminates</h3>\n<table>\n<thead>\n<tr>\n<th>Layer</th>\n<th>Before (Lit adapter)</th>\n<th>After (.less compiler)</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Runtime</td>\n<td>Lit runtime for Lit components</td>\n<td>0kb framework runtime for compiled CEs</td>\n</tr>\n<tr>\n<td>SSR</td>\n<td>adapter-mediated rendering</td>\n<td>LessJS DSD renderer / template strings</td>\n</tr>\n<tr>\n<td>Upgrade</td>\n<td>Custom Element upgrade</td>\n<td>Custom Element upgrade</td>\n</tr>\n<tr>\n<td>Build</td>\n<td>esbuild + Lit semantics</td>\n<td>standard TS/JS output</td>\n</tr>\n<tr>\n<td>Tests</td>\n<td>adapter tests required</td>\n<td>compiler fixture tests required</td>\n</tr>\n</tbody></table>\n<h3>SSG integration</h3>\n<p>The route scanner (route-scanner.ts) already maps <code>app/routes/*.ts</code> → URL paths. Extend it to also scan <code>.less</code> files:</p>\n<ul>\n<li><code>app/routes/blog/hello-world.less</code> → <code>/blog/hello-world</code></li>\n<li><code>app/routes/blog.less</code> → <code>/blog</code></li>\n<li><code>app/islands/my-counter.less</code> → Island component</li>\n</ul>\n<p>Page <code>.less</code> files render directly (template is the page). Island <code>.less</code> files get lazy chunk treatment like today&#39;s Lit islands.</p>\n<h3>Backward compatibility</h3>\n<ul>\n<li><code>vite.config.ts</code> option: <code>compiler: &#39;lit&#39; | &#39;less&#39; | &#39;auto&#39;</code> (auto = <code>.less</code> files use compiler, <code>.ts</code> files use Lit)</li>\n<li>Lit support retained as optional runtime throughout v0.x</li>\n<li>v0.11.0 introduces <code>.less</code> as alpha and optional</li>\n<li>v1.0 default remains an open decision; Lit compatibility must not be broken casually</li>\n</ul>\n<h2>Consequences</h2>\n<p><strong>Positive:</strong></p>\n<ul>\n<li>Zero framework runtime cost for compiled components</li>\n<li>Fewer upgrade-order bugs because compiled components target the LessJS DSD model directly</li>\n<li>No upstream dependency issues (dprint, node-domexception, parse5)</li>\n<li>Simpler developer API: HTML + minimal script, no class boilerplate</li>\n<li>True tree-shaking: only used components produce code</li>\n<li>SSG becomes synchronous string concatenation</li>\n</ul>\n<p><strong>Negative:</strong></p>\n<ul>\n<li>Not backward compatible with existing Lit code (migration needed)</li>\n<li>Custom compiler = custom bugs (parser edge cases, sourcemap complexity)</li>\n<li>HMR in dev mode needs special handling (fall back to Lit or implement compiler in watch mode)</li>\n<li>No access to Lit ecosystem components (unless bridge is provided)</li>\n</ul>\n<h2>Open Questions</h2>\n<ol>\n<li>Dev mode: run compiler on save, or fall back to Lit runtime for HMR?</li>\n<li>Islands in <code>.less</code> format — does the <code>&lt;script&gt;</code> get extracted as a lazy module?</li>\n<li>Slot/projection semantics — full HTML spec compliance needed in template compiler?</li>\n<li>SSR rendering — <code>template.innerHTML</code> is identical to client, but lifecycle hooks (connectedCallback) need SSG treatment</li>\n</ol>\n"
  },
  {
    "slug": "0001-keep-hono-vite-dev-server",
    "frontmatter": {
      "title": "@hono/vite-dev-server Evaluation",
      "date": "2026-04-28",
      "draft": false,
      "tags": [
        "architecture",
        "decision"
      ],
      "type": "adr",
      "lang": "en"
    },
    "content": "\n## Status: ✅ **KEPT** — Recommended retention\n\n## What It Is\n\nOfficial Hono × Vite development integration plugin.\nProvides:\n\n- **Middleware Mode**: Vite handles static assets; Hono handles API/SSR routes via Vite's `server.middlewareMode`\n- **HMR (Hot Module Replacement)**: Instant updates during development without full page reloads\n- **Entry Point Injection**: Auto-injects client-side `<script>` for module reloading\n- **SSR Streaming**: Streams SSR responses through Vite dev server\n\n## Current Usage\n\n```\npackages/less-core/src/index.ts  →  import honoDevServer from '@hono/vite-dev-server'\n                                   →  honoDevServer({ entry: VIRTUAL_ENTRY_ID, injectClientScript: true })\npackages/less-core/vite.config.build.ts →  external: ['@hono/vite-dev-server']  (dev-only!)\n```\n\n## Why It Exists (Not Removed)\n\n### The Alternative: Manual Implementation\n\nTo replace `@hono/vite-dev-server`, LessJS would need a custom Vite plugin that:\n\n| Feature                        |  Lines of Code | Complexity | Maintenance Risk                         |\n| ------------------------------ | -------------: | ---------: | ---------------------------------------- |\n| Middleware mode setup          |            ~20 |     Medium | Vite API changes between versions        |\n| HMR client injection           |            ~15 |       High | Must track Vite's HMR protocol changes   |\n| SSR transform pipeline         |            ~30 |  Very High | Must handle ESM/CJS interop, source maps |\n| Request forwarding (Vite→Hono) |            ~25 |     Medium | Edge cases with streaming, headers       |\n| Error overlay integration      |            ~10 |        Low | Nice-to-have but expected                |\n| **Total**                      | **~100 lines** |   **High** | **Ongoing**                              |\n\n### Cost-Benefit Analysis\n\n| Factor              |        Keep Plugin         |   Remove & Reimplement    |\n| ------------------- | :------------------------: | :-----------------------: |\n| Bundle size impact  |      **0** (dev-only)      |           **0**           |\n| Dependency count    |         +1 npm dep         |        -1 npm dep         |\n| Code to maintain    |      ~5 lines (usage)      |     ~100 lines (impl)     |\n| HMR reliability     | Battle-tested (Hono team)  |       Custom = bugs       |\n| Vite version compat |   Maintained by authors    | We track breaking changes |\n| DX quality          | Excellent (instant reload) |    Degraded or broken     |\n| **Verdict**         |         **✅ Win**         |        **❌ Lose**        |\n\n## Honest Counter-arguments (Why ANALYSIS Marked This P3)\n\n1. **\"One less dependency\"** — Valid concern, BUT this dep is:\n   - Dev-only (zero production footprint)\n   - From Hono's own org (not a random package)\n   - Already a transitive dep of `hono` ecosystem users likely have\n\n2. **\"Locks us to Hono\"** — True, but LessJS _is_ a Hono framework.\n   Replacing Hono would require rewriting everything regardless of this plugin.\n\n3. **\"Could break on Vite major bumps\"** — True, but:\n   - The Hono team maintains this plugin alongside their framework\n   - Our alternative implementation would break just as often (or worse)\n\n## Production Footprint: Zero\n\n```bash\n# Build output analysis (vite.config.build.ts):\nexternal: ['@hono/vite-dev-server']  # ← Never bundled into SSR/client chunks\n```\n\nThe plugin is only loaded when `vite dev` runs (command === 'serve').\nIn `vite build`, the import is tree-shaken and the package never touched.\n\n## Conclusion\n\n> **Keep @hono/vite-dev-server.** The tradeoff heavily favors retention:\n> zero production cost vs. ~100 lines of custom maintenance code,\n> battle-tested HMR vs. home-grown bugs, and official support vs.\n> tracking Vite internal APIs across versions.\n>\n> If a future requirement forces removal (e.g., switching from Hono),\n> the replacement cost is well-documented above (~100 lines).\n\n---\n\n_Evaluated: 2026-04-28 | Version reviewed: @hono/vite-dev-server@^0.25.3_\n",
    "html": "<h2>Status: ✅ <strong>KEPT</strong> — Recommended retention</h2>\n<h2>What It Is</h2>\n<p>Official Hono × Vite development integration plugin.\nProvides:</p>\n<ul>\n<li><strong>Middleware Mode</strong>: Vite handles static assets; Hono handles API/SSR routes via Vite&#39;s <code>server.middlewareMode</code></li>\n<li><strong>HMR (Hot Module Replacement)</strong>: Instant updates during development without full page reloads</li>\n<li><strong>Entry Point Injection</strong>: Auto-injects client-side <code>&lt;script&gt;</code> for module reloading</li>\n<li><strong>SSR Streaming</strong>: Streams SSR responses through Vite dev server</li>\n</ul>\n<h2>Current Usage</h2>\n<pre><code>packages/less-core/src/index.ts  →  import honoDevServer from &#39;@hono/vite-dev-server&#39;\n                                   →  honoDevServer({ entry: VIRTUAL_ENTRY_ID, injectClientScript: true })\npackages/less-core/vite.config.build.ts →  external: [&#39;@hono/vite-dev-server&#39;]  (dev-only!)\n</code></pre>\n<h2>Why It Exists (Not Removed)</h2>\n<h3>The Alternative: Manual Implementation</h3>\n<p>To replace <code>@hono/vite-dev-server</code>, LessJS would need a custom Vite plugin that:</p>\n<table>\n<thead>\n<tr>\n<th>Feature</th>\n<th>Lines of Code</th>\n<th>Complexity</th>\n<th>Maintenance Risk</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Middleware mode setup</td>\n<td>~20</td>\n<td>Medium</td>\n<td>Vite API changes between versions</td>\n</tr>\n<tr>\n<td>HMR client injection</td>\n<td>~15</td>\n<td>High</td>\n<td>Must track Vite&#39;s HMR protocol changes</td>\n</tr>\n<tr>\n<td>SSR transform pipeline</td>\n<td>~30</td>\n<td>Very High</td>\n<td>Must handle ESM/CJS interop, source maps</td>\n</tr>\n<tr>\n<td>Request forwarding (Vite→Hono)</td>\n<td>~25</td>\n<td>Medium</td>\n<td>Edge cases with streaming, headers</td>\n</tr>\n<tr>\n<td>Error overlay integration</td>\n<td>~10</td>\n<td>Low</td>\n<td>Nice-to-have but expected</td>\n</tr>\n<tr>\n<td><strong>Total</strong></td>\n<td><strong>~100 lines</strong></td>\n<td><strong>High</strong></td>\n<td><strong>Ongoing</strong></td>\n</tr>\n</tbody></table>\n<h3>Cost-Benefit Analysis</h3>\n<table>\n<thead>\n<tr>\n<th>Factor</th>\n<th>Keep Plugin</th>\n<th>Remove &amp; Reimplement</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Bundle size impact</td>\n<td><strong>0</strong> (dev-only)</td>\n<td><strong>0</strong></td>\n</tr>\n<tr>\n<td>Dependency count</td>\n<td>+1 npm dep</td>\n<td>-1 npm dep</td>\n</tr>\n<tr>\n<td>Code to maintain</td>\n<td>~5 lines (usage)</td>\n<td>~100 lines (impl)</td>\n</tr>\n<tr>\n<td>HMR reliability</td>\n<td>Battle-tested (Hono team)</td>\n<td>Custom = bugs</td>\n</tr>\n<tr>\n<td>Vite version compat</td>\n<td>Maintained by authors</td>\n<td>We track breaking changes</td>\n</tr>\n<tr>\n<td>DX quality</td>\n<td>Excellent (instant reload)</td>\n<td>Degraded or broken</td>\n</tr>\n<tr>\n<td><strong>Verdict</strong></td>\n<td><strong>✅ Win</strong></td>\n<td><strong>❌ Lose</strong></td>\n</tr>\n</tbody></table>\n<h2>Honest Counter-arguments (Why ANALYSIS Marked This P3)</h2>\n<ol>\n<li><p><strong>&quot;One less dependency&quot;</strong> — Valid concern, BUT this dep is:</p>\n<ul>\n<li>Dev-only (zero production footprint)</li>\n<li>From Hono&#39;s own org (not a random package)</li>\n<li>Already a transitive dep of <code>hono</code> ecosystem users likely have</li>\n</ul>\n</li>\n<li><p><strong>&quot;Locks us to Hono&quot;</strong> — True, but LessJS <em>is</em> a Hono framework.\nReplacing Hono would require rewriting everything regardless of this plugin.</p>\n</li>\n<li><p><strong>&quot;Could break on Vite major bumps&quot;</strong> — True, but:</p>\n<ul>\n<li>The Hono team maintains this plugin alongside their framework</li>\n<li>Our alternative implementation would break just as often (or worse)</li>\n</ul>\n</li>\n</ol>\n<h2>Production Footprint: Zero</h2>\n<pre><code class=\"language-bash\"># Build output analysis (vite.config.build.ts):\nexternal: [&#39;@hono/vite-dev-server&#39;]  # ← Never bundled into SSR/client chunks\n</code></pre>\n<p>The plugin is only loaded when <code>vite dev</code> runs (command === &#39;serve&#39;).\nIn <code>vite build</code>, the import is tree-shaken and the package never touched.</p>\n<h2>Conclusion</h2>\n<blockquote>\n<p><strong>Keep @hono/vite-dev-server.</strong> The tradeoff heavily favors retention:\nzero production cost vs. ~100 lines of custom maintenance code,\nbattle-tested HMR vs. home-grown bugs, and official support vs.\ntracking Vite internal APIs across versions.</p>\n<p>If a future requirement forces removal (e.g., switching from Hono),\nthe replacement cost is well-documented above (~100 lines).</p>\n</blockquote>\n<hr />\n<p><em>Evaluated: 2026-04-28 | Version reviewed: @hono/vite-dev-server@^0.25.3</em></p>\n"
  }
];

export function getPostBySlug(slug: string): GeneratedBlogPost | undefined {
  return posts.find((post) => post.slug === slug);
}
